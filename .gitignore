
# Private scope documents (consulting blueprints, not for public publish)
.planning/*-PROMPT.pdf

# Internal planning artifacts (CONTEXT, PLAN, SUMMARY, VERIFICATION per phase, milestone archives)
.planning/
