# {{team_name}}

{{one_paragraph_human_overview_of_what_this_team_does}}

## Workflows

{{workflows_table — one row per workflow with trigger + outcome}}

## Agents

{{agents_table — one row per agent with role + autonomy + blast_radius}}

## Tool inventory at a glance

| Tier | Count | Examples |
|---|---|---|
| EXISTS-MCP | {{n}} | {{tools}} |
| NEEDS-MCP-WRAPPER | {{n}} | {{tools}} |
| NEEDS-N8N-FLOW | {{n}} | {{tools}} |
| NEEDS-WEBHOOK | {{n}} | {{tools}} |
| MANUAL | {{n}} | {{tools}} |

See `integrations/INVENTORY.md` for the full evidence-backed table.

## Build it

This folder is a build-ready spec. Open it in any AI coding tool:

- **Claude Code**: `cd <this-folder> && claude` — reads `CLAUDE.md`
- **Codex / Cursor / Gemini / OpenClaw**: opens `AGENTS.md` as context

Then follow `ROADMAP.md` for the phased build plan.

## What this team is NOT

This is a **spec**, not running code. The team described here will work
when the build session implements it. Until then, it's a contract
between AgentBloc (the architect) and your build session (the builder).

## Provenance

Generated by AgentBloc {{agentbloc_version}} on {{emitted_at}}.

For evolution (when requirements change), rerun AgentBloc and pass this
folder. Phase 6 Spec Evolution will re-emit affected files.
