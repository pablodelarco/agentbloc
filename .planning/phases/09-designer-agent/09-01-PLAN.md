---
phase: 09-designer-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/agentbloc/references/orchestration-patterns.md
  - .claude/skills/agentbloc/references/agent-profile-schema.md
  - .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml
autonomous: true
requirements:
  - DSGN-02
  - DSGN-03
  - DSGN-04
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04

must_haves:
  truths:
    - "references/orchestration-patterns.md exists as a loadable reference with TOC, When This Applies, the 5-pattern table (Sequential / Parallel / Loop / Event-driven / Conversational), Topology Decision Table (pipeline / mesh / hierarchy / swarm), Framework Pattern Inheritance table (CrewAI / AG2 / Google ADK / LangGraph / Mastra / Paperclip), and Quick Reference"
    - "references/agent-profile-schema.md exists as a loadable reference with TOC, When This Applies, YAML Schema Definition block, three-tier Field Obligation Matrix (REQUIRED / RECOMMENDED / OPTIONAL), three bounded enums (Autonomy, Trigger, Topology), ordered Validation Checklist, Emission Protocol, Re-run Behavior (with D-26 surgical-patch rule), and Schema Versioning Rules"
    - "examples/arco-rooms-agent-profiles.yaml exists as valid YAML with exactly 3 agents (gestor-documental, gestor-cobros, recepcionista) per D-30, each with REQUIRED fields populated, and at least 1 workflow with type from the 5-pattern enum"
    - "The fixture's orchestration.workflows[].agents[] IDs all resolve to entries in agents[] (ORCH-04)"
    - "A reader can understand the full agent-profiles.yaml field set by reading ONLY agent-profile-schema.md (no validator code, because there is none per D-13 inheritance)"
    - "Designer Agent can cite orchestration-patterns.md when choosing Sequential / Parallel / Loop / Event-driven / Conversational per workflow (ROADMAP Phase 9 success criterion 4)"
  artifacts:
    - path: ".claude/skills/agentbloc/references/orchestration-patterns.md"
      provides: "Canonical 5-pattern orchestration catalog + topology decision table + framework pattern inheritance map (D-23, D-24, D-27)"
      min_lines: 120
      contains: "Sequential"
    - path: ".claude/skills/agentbloc/references/agent-profile-schema.md"
      provides: "Canonical agent-profiles.yaml schema definition + three-tier field obligation matrix + autonomy/trigger/topology bounded enums + prose validation checklist + emission protocol + re-run behavior + schema versioning rules (D-22, D-28)"
      min_lines: 150
      contains: "schema_version"
    - path: ".claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml"
      provides: "Canonical test fixture — 3-agent Arco Rooms team (gestor-documental + gestor-cobros + recepcionista) satisfying every REQUIRED check in agent-profile-schema.md Validation Checklist (D-30)"
      contains: "schema_version"
  key_links:
    - from: ".claude/skills/agentbloc/references/agent-profile-schema.md"
      to: ".claude/skills/agentbloc/references/orchestration-patterns.md"
      via: "cross-link in When This Applies + bounded-enum sections — workflow.type enum is sourced from orchestration-patterns.md 5-pattern table"
      pattern: "orchestration-patterns\\.md"
    - from: ".claude/skills/agentbloc/references/agent-profile-schema.md"
      to: ".claude/skills/agentbloc/references/business-graph-schema.md"
      via: "cross-link — trigger enum inherits cron/event/manual from business-graph-schema.md trigger enum, extends with inter-agent"
      pattern: "business-graph-schema\\.md"
    - from: ".claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml"
      to: ".claude/skills/agentbloc/references/agent-profile-schema.md"
      via: "fixture conforms to schema — every REQUIRED check passes on this YAML"
      pattern: "schema_version"
    - from: ".claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml"
      to: ".claude/skills/agentbloc/examples/arco-rooms-business-graph.json"
      via: "fixture family — the 3 agents derived from the 3 Business Graph processes (Invoice Collection -> gestor-documental, Payment Matching -> gestor-cobros, Owner Reporting -> recepcionista)"
      pattern: "arco-rooms"
---

<objective>
Create the three CONTRACT files for Phase 9: the orchestration-patterns reference (5-pattern catalog + topology decision table + framework inheritance), the agent-profile-schema reference (YAML schema + prose validator per D-13 inheritance), and the canonical 3-agent Arco Rooms test fixture that downstream Plan 09-02 (Designer subagent) reads and Plan 09-03 (SKILL.md / phase-2-design.md wiring) cites.

Purpose: Phase 9's entire design surface (Designer subagent prompt, SKILL.md gate, phase-2-design.md wiring) reads from these three files. Lock the contracts first, wire the subagent + skill second. This mirrors Phase 8's plan-01 pattern (schema + fixture first, interview wiring second) exactly — same decomposition, one phase later.

Output: Two ~130-210 line reference files structurally twinned with references/frameworks.md (D-27) and references/business-graph-schema.md (D-28), plus one ~90-140 line canonical YAML fixture derived from examples/arco-rooms-business-graph.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-designer-agent/09-CONTEXT.md
@.planning/phases/09-designer-agent/09-PATTERNS.md

@.claude/skills/agentbloc/references/frameworks.md
@.claude/skills/agentbloc/references/business-graph-schema.md
@.claude/skills/agentbloc/references/blast-radius.md
@.claude/skills/agentbloc/references/phase-2-design.md
@.claude/skills/agentbloc/examples/arco-rooms-business-graph.json

<interfaces>
<!-- Canonical agent-profiles.yaml shape (locked by CONTEXT.md D-22, D-24, D-26). -->
<!-- This is the contract the new agent-profile-schema.md must define and the fixture must satisfy. -->

```yaml
schema_version: 1                              # REQUIRED. Integer. Bumped only on breaking changes.
team:
  name: "string"                               # REQUIRED. e.g. "arco-rooms-team"
  topology: "pipeline | mesh | hierarchy | swarm"  # REQUIRED. See Topology Bounded Enum.
  topology_rationale: "string"                 # RECOMMENDED. One-line why this topology fits.
  modified_at: "ISO-8601 timestamp"            # OPTIONAL. Bumped on every conversational edit.
  briefing_agent_id: "string | null"           # OPTIONAL. Phase 14 briefing agent reference.

agents:                                        # REQUIRED. Length >= 1.
  - id: "string"                               # REQUIRED. kebab-case, unique within file.
    role: "string"                             # REQUIRED. CrewAI-shaped.
    goal: "string"                             # REQUIRED. Scoped outcome.
    backstory: "string | null"                 # RECOMMENDED. CrewAI narrative identity.
    tools:                                     # REQUIRED. Length >= 1.
      - "string"                               # MCP reference or tool name.
    triggers:                                  # REQUIRED. Length >= 1.
      - type: "cron | event | manual | inter-agent"
    autonomy: "full | semi | supervised"       # REQUIRED.
    outputs:                                   # RECOMMENDED.
      - type: "string"
        schema: "string | null"
    escalation: "string | null"                # RECOMMENDED. e.g. "telegram:pablo"
    dependencies:                              # RECOMMENDED.
      - "<other-agent-id>"                     # Must resolve to agents[] (ORCH-04).
    blast_radius: 1 | 2 | 3 | 4                # REQUIRED. Auto-scored per blast-radius.md.
    model: "opus | sonnet | haiku | null"      # OPTIONAL.

orchestration:
  workflows:                                   # REQUIRED. Length >= 1.
    - id: "string"                             # REQUIRED.
      type: "sequential | parallel | loop | event-driven | conversational"  # REQUIRED.
      agents: ["<agent-id>", ...]              # REQUIRED. All IDs must resolve (ORCH-04).
      trigger:                                 # REQUIRED.
        type: "cron | event | manual"
      why: "string"                            # RECOMMENDED. Citation of orchestration-patterns.md.
      steps: ["string", ...]                   # OPTIONAL. Used by sequential/loop.
      flow: "string | null"                    # OPTIONAL. Used by event-driven.
```

The 5 orchestration patterns (D-24):
- `sequential` (ADK SequentialAgent): ordered steps feed each other
- `parallel` (ADK ParallelAgent): independent multi-agent fan-out
- `loop` (ADK LoopAgent): same step repeats until condition met
- `event-driven` (Bus pattern): external trigger wakes agent, runs once, sleeps
- `conversational` (Negotiation): agents deliberate via SendMessage until consensus

The 4 topologies (D-23): pipeline, mesh (default on ambiguity), hierarchy, swarm.

The 3 autonomies: full (no approval), semi (approval before external side effects), supervised (approval on every action).

Structural twins:
- orchestration-patterns.md <- frameworks.md (D-27)
- agent-profile-schema.md <- business-graph-schema.md (D-28)
- arco-rooms-agent-profiles.yaml <- arco-rooms-business-graph.json (D-30 fixture family)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create references/orchestration-patterns.md with 5-pattern table + topology decision table + framework inheritance</name>
  <files>.claude/skills/agentbloc/references/orchestration-patterns.md</files>
  <read_first>
    - .planning/phases/09-designer-agent/09-PATTERNS.md (lines 157-341 — the "orchestration-patterns.md (NEW)" section with Patterns A through F: header+TOC, When This Applies, 5-pattern table, topology table, framework inheritance, Quick Reference)
    - .planning/phases/09-designer-agent/09-CONTEXT.md (D-23 topology decision table; D-24 5-pattern table; D-27 structural twin of frameworks.md; "Framework Pattern Inheritance" borrowing rules)
    - .claude/skills/agentbloc/references/frameworks.md (the full 126-line structural twin — copy the skeleton verbatim: H1, blockquote, TOC, When This Applies, H2 sections with tables, Quick Reference at the bottom)
    - .claude/skills/agentbloc/references/business-graph-schema.md (cross-link target — trigger enum inherits from this; topology reference sits next to the Business Graph schema)
    - .planning/REQUIREMENTS.md (ORCH-01, ORCH-02 — verify the 5 patterns and the "Designer cites this reference" requirement are both satisfied)
  </read_first>
  <action>
Create a NEW file at `.claude/skills/agentbloc/references/orchestration-patterns.md` (~130-170 lines) with EXACTLY the following 7 sections in this order. Use GitHub-flavored Markdown.

**Section 1 - H1 + blockquote + TOC** (lines 1-13 shape of frameworks.md):

```markdown
# Orchestration Patterns

> Loaded by SKILL.md at Phase 2 entry alongside [phase-2-design.md](phase-2-design.md) and [agent-profile-schema.md](agent-profile-schema.md). Defines the 5 universal orchestration patterns Designer Agent classifies each workflow into, plus the topology decision table Designer uses to pick team shape. Framework patterns are referenced, not imported. AgentBloc runs on Claude Code natively.

## Table of Contents

- [When This Applies](#when-this-applies)
- [The 5 Orchestration Patterns](#the-5-orchestration-patterns)
- [Topology Decision Table](#topology-decision-table)
- [Framework Pattern Inheritance](#framework-pattern-inheritance)
- [Pattern Selection Heuristics](#pattern-selection-heuristics)
- [Quick Reference](#quick-reference)
```

**Section 2 - `## When This Applies`**: one paragraph adapted from 09-PATTERNS.md Pattern B. Exact text to emit:

```markdown
## When This Applies

Claude loads this file at Phase 2 entry and the Designer Agent subagent re-loads it inside its forked context. Specific sections are referenced based on the design step:

- **Topology Selection (DESG-02 + DSGN-04):** Use the Topology Decision Table to pick `team.topology` from {pipeline, mesh, hierarchy, swarm}. Default to **mesh** on ambiguity.
- **Orchestration Pattern Classification (ORCH-01):** For each `orchestration.workflows[]` entry, pick one of the 5 patterns below and write it into `workflows[].type`. Cite the pattern name in `workflows[].why`.
- **Framework Inheritance:** When explaining rationale to the user, cite the pattern's origin (CrewAI / AG2 / Google ADK / LangGraph / Mastra / Paperclip). These are borrowed design patterns, never imported runtimes.
```

**Section 3 - `## The 5 Orchestration Patterns`**: one intro paragraph followed by a 5-column markdown table (per D-24). Exact emission:

```markdown
## The 5 Orchestration Patterns

Designer Agent classifies each `orchestration.workflows[]` entry into exactly one of these patterns. Write the picked value into `workflows[].type`. ADK naming is preserved for easy mapping to Google ADK primitives if TypeScript codegen is later added.

| Pattern | ADK Name | Signal From Business Graph | Designer Picks When | Arco Rooms Example |
|---------|----------|---------------------------|---------------------|--------------------|
| **Sequential** | `SequentialAgent` | Ordered steps with dependencies; each step feeds the next | Single-agent or multi-agent workflow has ordered `steps[]` where step N depends on step N-1 | Cobro mensual: verify -> remind -> generate -> update |
| **Parallel** | `ParallelAgent` | Multiple agents run independently; results merge | Multi-agent workflow with no inter-dependencies; weekly reports assembled from independent data sources | Weekly Report assembly from 3 data sources |
| **Loop** | `LoopAgent` | Same step repeats until condition met | Watchdog-style (poll until due date passes, retry until response) | Check-in reminder loop until guest confirms |
| **Event-driven** | Bus pattern | Agent wakes on external event, runs once, sleeps | Most AgentBloc flows - Gmail webhook, BBVA webhook, Telegram inbound | Recepcionista wakes on new Gmail message |
| **Conversational** | Negotiation | Agents deliberate via SendMessage until consensus | Rare; only when a business rule requires multi-party deliberation | Finance + legal agents must both approve a >= 1000 EUR refund |
```

**Section 4 - `## Topology Decision Table`**: one intro paragraph + a 4-column table (per D-23). Exact emission:

```markdown
## Topology Decision Table

Designer Agent picks `team.topology` from the signal set below. On ambiguity, default to **mesh** - matches ClaudeClaw `SendMessage`, degrades naturally to pipeline when only one agent is generated.

| Topology | Signal From Business Graph | Example | Agent Count |
|----------|---------------------------|---------|-------------|
| **Pipeline** | One linear process with ordered handoffs (verify -> remind -> generate) | Single-process cobro flow | 1-3 |
| **Mesh** | Multiple agents that peer-call each other (Recepcionista asks Gestor Cobros for payment status) | Arco Rooms 3-agent team (default) | 3-8 |
| **Hierarchy** | One team lead orchestrates per-domain workers; workers do not talk peer-to-peer | 5-15 agent org with briefing-agent at the top | 5-15 |
| **Swarm** | N independent agents performing similar work in parallel (one-per-property watchdog) | Multi-tenant ops | 5+ |

**Default on ambiguity:** `mesh`. Designer writes the rationale into `team.topology_rationale` so the user sees the reasoning.
```

**Section 5 - `## Framework Pattern Inheritance`**: one intro paragraph + a 3-column table (per D-27, condensed to stay under 170 lines). Exact emission:

```markdown
## Framework Pattern Inheritance

AgentBloc borrows design patterns, not runtimes. Each pattern below has a source framework and a one-line AgentBloc application. Designer cites the framework in conversational rationale when technical level permits.

| Framework | Pattern Borrowed | AgentBloc Application |
|-----------|-----------------|----------------------|
| **CrewAI** | `role` / `goal` / `backstory` triad per agent | agent-profile-schema.md fields; role is the canonical identity |
| **AG2** | CaptainAgent meta-agent that generates teams on demand | Designer Agent itself (this file's consumer) |
| **Google ADK** | SequentialAgent / ParallelAgent / LoopAgent primitives | The 5-pattern table's ADK Name column |
| **LangGraph** | Checkpointing schema shape (durable state between steps) | `.agentbloc/state/*.json` per-agent, Git-commitable |
| **Mastra** | Zod-style per-agent input/output schema validation | `outputs[].type + schema` in agent profiles |
| **Paperclip** | Control plane UX (approval queue, cost tracking, task locking, status badges) | Phase 14 Briefing Agent consumes agent logs this way |

**Rule:** These are borrowed patterns, never imported runtimes. AgentBloc runs on Claude Code. No Python runtime, no TypeScript agent framework, no `pip install`, no `npm install` of agent SDKs.
```

**Section 6 - `## Pattern Selection Heuristics`**: short prose-bullet list guiding Designer when the signal is ambiguous. Exact emission:

```markdown
## Pattern Selection Heuristics

When the Business Graph does not clearly signal one pattern:

1. **If the workflow has a cron trigger and one ordered steps list** -> pick `sequential`.
2. **If the workflow has an event trigger from an external service** (gmail, webhook, telegram inbound) -> pick `event-driven`.
3. **If the workflow description mentions "poll", "retry", "until", or "watch"** -> pick `loop`.
4. **If multiple agents read the same input and write independent outputs** -> pick `parallel`.
5. **If the workflow requires multi-party deliberation** (approval from more than one agent before a side effect) -> pick `conversational`. Rare.
6. **Default on ambiguity:** `event-driven`. Most AgentBloc flows are external-trigger-driven.

For topology:

1. **1-3 agents, linear flow:** pick `pipeline`.
2. **3-8 agents that peer-call each other:** pick `mesh` (default).
3. **5+ agents with a coordinator:** pick `hierarchy`.
4. **N independent agents doing the same kind of work in parallel:** pick `swarm`.
5. **Default on ambiguity:** `mesh`.
```

**Section 7 - `## Quick Reference`**: bullet list per D-27 Pattern F shape. Exact emission:

```markdown
## Quick Reference

- **Sequential:** ordered `steps[]` with dependencies. Cobro-style single-agent flows.
- **Parallel:** independent multi-agent fan-out. Report-assembly flows.
- **Loop:** poll-until-condition. Reminder watchdogs.
- **Event-driven:** external-trigger wake. Most AgentBloc flows.
- **Conversational:** multi-party deliberation. Rare; only when business rules require consensus.
- **Default topology on ambiguity:** `mesh`. Default orchestration pattern on ambiguity: `event-driven`.
- **Rule:** Designer cites this file in `workflows[].why` and in `team.topology_rationale` so the user sees the reasoning.
- **Framework rule:** Borrowed patterns, not imported runtimes. AgentBloc runs on Claude Code. No `pip`, no `npm` install of agent SDKs.
```

**Formatting rules for the whole file:**
- All cross-links to companion references use shorthand relative paths (`phase-2-design.md`, not `references/phase-2-design.md`) because this file sits in the same references/ directory.
- Every bounded enum / pattern / topology renders as a markdown table, NEVER as prose list (matches frameworks.md house style).
- Keep the file between 120 and 180 lines. Tighten prose before trimming tables.
- Do NOT use em-dashes `—` anywhere (project CLAUDE.md rule). Use ` - `, `:`, or recast the sentence.
- Do NOT reference CrewAI/LangGraph/n8n framework-specific internals beyond what the inheritance table already captures.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "^# Orchestration Patterns$" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "## Table of Contents" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "## When This Applies" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "## The 5 Orchestration Patterns" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "## Topology Decision Table" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "## Framework Pattern Inheritance" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "## Quick Reference" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "Sequential" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "Parallel" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "Loop" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "Event-driven" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "Conversational" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "pipeline" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "mesh" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "hierarchy" .claude/skills/agentbloc/references/orchestration-patterns.md &amp;&amp; grep -q "swarm" .claude/skills/agentbloc/references/orchestration-patterns.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/references/orchestration-patterns.md` exits 0
    - `grep -q "^# Orchestration Patterns$" .claude/skills/agentbloc/references/orchestration-patterns.md` matches (exact H1)
    - All 5 pattern names present: Sequential, Parallel, Loop, Event-driven, Conversational (separate greps, all must match)
    - All 4 topology names present: pipeline, mesh, hierarchy, swarm (separate greps, all must match)
    - `grep -q "CrewAI" .claude/skills/agentbloc/references/orchestration-patterns.md` AND `grep -q "AG2" ...` AND `grep -q "Google ADK" ...` AND `grep -q "LangGraph" ...` AND `grep -q "Mastra" ...` AND `grep -q "Paperclip" ...` - all 6 framework names present in inheritance table
    - `grep -q "Default on ambiguity" .claude/skills/agentbloc/references/orchestration-patterns.md` OR `grep -q "default to \*\*mesh\*\*" ...` - default-to-mesh rule present
    - `grep -q "phase-2-design\.md" .claude/skills/agentbloc/references/orchestration-patterns.md` AND `grep -q "agent-profile-schema\.md" .claude/skills/agentbloc/references/orchestration-patterns.md` - cross-links to both companion references
    - `wc -l .claude/skills/agentbloc/references/orchestration-patterns.md` returns between 120 and 200 lines
    - No em-dashes: `grep -c "—" .claude/skills/agentbloc/references/orchestration-patterns.md` returns 0
    - All 6 planned H2 section headers are present: `grep -c "^## " .claude/skills/agentbloc/references/orchestration-patterns.md` returns >= 6
  </acceptance_criteria>
  <done>
orchestration-patterns.md exists with H1 + blockquote + TOC, a When-This-Applies opener, the 5-pattern table (Sequential / Parallel / Loop / Event-driven / Conversational with ADK names + signals + when-to-pick + Arco Rooms examples), the 4-topology decision table (pipeline / mesh / hierarchy / swarm with default-mesh-on-ambiguity), the 6-framework inheritance table (CrewAI / AG2 / Google ADK / LangGraph / Mastra / Paperclip), a Pattern Selection Heuristics section with numbered fallback rules, and a Quick Reference footer. Cross-links to phase-2-design.md and agent-profile-schema.md use shorthand relative paths. File is 120-200 lines, contains zero em-dashes, and matches the structural shape of frameworks.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create references/agent-profile-schema.md with YAML schema + three bounded enums + validation checklist + emission protocol</name>
  <files>.claude/skills/agentbloc/references/agent-profile-schema.md</files>
  <read_first>
    - .planning/phases/09-designer-agent/09-PATTERNS.md (lines 343-606 — the "agent-profile-schema.md (NEW)" section with Patterns A through G: spine copy, When-This-Applies adaptation, commented-YAML schema block, Field Obligation Matrix, three bounded enums, Validation Checklist, Emission + Re-run + Versioning)
    - .planning/phases/09-designer-agent/09-CONTEXT.md (D-22 three-tier schema; D-26 surgical-patch re-run rule; D-28 structural twin of business-graph-schema.md)
    - .claude/skills/agentbloc/references/business-graph-schema.md (the full 137-line structural twin — copy the skeleton verbatim: H1 + blockquote + TOC + When This Applies + Schema Definition fenced block + Field Obligation Matrix + Bounded Enum table + Validation Checklist + Emission Protocol + Re-run Behavior + Schema Versioning Rules)
    - .claude/skills/agentbloc/references/blast-radius.md (referenced by the schema for auto-scoring per v1.0 D-09)
    - .planning/REQUIREMENTS.md (DSGN-02, DSGN-03, ORCH-03, ORCH-04 - verify the schema covers team + agents + profile fields + workflows-with-resolving-IDs)
  </read_first>
  <action>
Create a NEW file at `.claude/skills/agentbloc/references/agent-profile-schema.md` (~150-210 lines) with EXACTLY the following 11 sections in this order. Use GitHub-flavored Markdown.

**Section 1 - H1 + blockquote + TOC** (mirrors business-graph-schema.md lines 1-14):

```markdown
# Agent Profile Schema

> Schema reference loaded unconditionally at Phase 2 entry alongside [phase-2-design.md](phase-2-design.md) and [orchestration-patterns.md](orchestration-patterns.md). Defines the canonical `agent-profiles.yaml` emitted by the Designer Agent subagent and the validation checklist Designer walks before writing the file.

## Table of Contents

- [When This Applies](#when-this-applies)
- [Schema Definition](#schema-definition)
- [Field Obligation Matrix](#field-obligation-matrix)
- [Autonomy Bounded Enum](#autonomy-bounded-enum)
- [Trigger Bounded Enum](#trigger-bounded-enum)
- [Topology Bounded Enum](#topology-bounded-enum)
- [Validation Checklist](#validation-checklist)
- [Emission Protocol](#emission-protocol)
- [Re-run Behavior](#re-run-behavior)
- [Schema Versioning Rules](#schema-versioning-rules)
```

**Section 2 - `## When This Applies`**: one paragraph adapted from 09-PATTERNS.md Pattern B. Exact emission:

```markdown
## When This Applies

Designer Agent reads this file inside its forked context to produce the canonical `agent-profiles.yaml` at `.agentbloc/team/agent-profiles.yaml`. The schema defines what MUST, SHOULD, and MAY appear in the YAML. The Validation Checklist below is a deterministic list of pass/fail checks Designer walks before writing the file; failures surface as targeted follow-up questions through the main session's conversation. Downstream consumers - Phase 12 Deploy Pipeline (agent skills generation), Phase 13 Runtime (trigger wiring), Phase 14 Briefing Agent (team awareness), Phase 15 Anticipation (additive updates) - all read this artifact. The `triggers[].type` enum extends the Business Graph trigger enum in [business-graph-schema.md](business-graph-schema.md) with `inter-agent` for peer calls.
```

**Section 3 - `## Schema Definition`**: one fenced ` ```yaml ` block containing the full commented schema from the `<interfaces>` block in `<context>` above. Copy verbatim; every field has an inline comment declaring its obligation tier (REQUIRED / RECOMMENDED / OPTIONAL) and a one-example hint.

**Section 4 - `## Field Obligation Matrix`**: a 3-column markdown table rendering D-22 verbatim:

```markdown
| Tier | Fields | Behavior if missing |
|---|---|---|
| REQUIRED | `schema_version`, `team.name`, `team.topology`, `agents[]` (>=1), per-agent `id` + `role` + `goal` + `tools[]` (>=1) + `triggers[]` (>=1) + `autonomy` + `blast_radius`, `orchestration.workflows[]` (>=1) with `type` + `agents[]` (>=1) + `trigger` | Designer refuses to emit. Main session re-prompts user through targeted follow-up. |
| RECOMMENDED | `backstory`, `outputs[]`, `escalation`, `dependencies[]`, `team.topology_rationale`, `workflows[].why` | Designer emits with warnings. Phase 12 Deploy Pipeline operates with degraded output and flags gaps in DEPLOY-REPORT.md. |
| OPTIONAL | `team.modified_at`, `team.briefing_agent_id`, `model`, `workflows[].steps`, `workflows[].flow` | Silent defaults. Phase 12 proceeds without comment. |
```

Follow the table with one sentence: "Downstream consumers refuse to proceed on an unknown major `schema_version`, the same rule as business-graph-schema.md."

**Section 5 - `## Autonomy Bounded Enum`**: one intro sentence + 4-column markdown table:

```markdown
## Autonomy Bounded Enum

The `autonomy` field per agent is drawn from a fixed set. It drives whether Phase 14 Autonomy layer inserts an approval gate before each side effect.

| Enum Value | Definition | Side-Effect Behavior | When to Pick |
|-----------|-----------|----------------------|--------------|
| `full` | Agent acts without prompting | No approval gate; audit log only | Agent only touches internal state files (blast_radius 1-2) |
| `semi` | Agent confirms before external side effects | Telegram approval required before send or write-external | Agent has L3 writes or occasional external sends |
| `supervised` | Agent proposes every action, waits for approval | Every side effect waits for explicit human ack | L4 agents and high-stakes flows (financial, legal) |
```

**Section 6 - `## Trigger Bounded Enum`**: one intro sentence + 4-column markdown table (inherits business-graph-schema.md trigger enum + adds `inter-agent`):

```markdown
## Trigger Bounded Enum

Inherits the cron / event / manual types from [business-graph-schema.md](business-graph-schema.md) Trigger Bounded Enum, plus one new type for peer calls between agents (ORCH-03).

| Enum Value | Definition | Required Sub-fields | Example |
|-----------|-----------|---------------------|---------|
| `cron` | Time-based recurring trigger | `schedule` (cron string) | `{type: cron, schedule: "0 9 * * 1"}` |
| `event` | External-event-driven trigger | `source` (service) + `name` (event id) | `{type: event, source: gmail, name: new_message}` |
| `manual` | Human-initiated trigger | `description` (free text) | `{type: manual, description: "Operator runs on demand"}` |
| `inter-agent` | Peer-call from another agent via SendMessage | `caller` (agent-id) + `message` (contract) | `{type: inter-agent, caller: recepcionista, message: "payment-status-query"}` |

Any value outside this enum forces a clarification question before emission.
```

**Section 7 - `## Topology Bounded Enum`**: one intro sentence + 4-column markdown table cross-referencing orchestration-patterns.md:

```markdown
## Topology Bounded Enum

The `team.topology` field is drawn from a fixed set. Full signal rationale and decision table live in [orchestration-patterns.md](orchestration-patterns.md) Topology Decision Table.

| Enum Value | Signal from Business Graph | Default Use | Cross-Reference |
|-----------|---------------------------|-------------|-----------------|
| `pipeline` | Linear ordered process with handoffs | 1-3 agents, sequential flow | orchestration-patterns.md |
| `mesh` | Peer-calling agents (default on ambiguity) | 3-8 agents, mutual SendMessage | orchestration-patterns.md |
| `hierarchy` | One lead orchestrates workers | 5-15 agents, briefing-agent root | orchestration-patterns.md |
| `swarm` | N independent parallel agents | Rare; multi-tenant watchdogs | orchestration-patterns.md |

Default on ambiguity: `mesh`.
```

**Section 8 - `## Validation Checklist`**: 8 numbered checks per 09-PATTERNS.md Pattern F. Open with this paragraph:

```markdown
Designer walks this ordered list before writing `.agentbloc/team/agent-profiles.yaml`. Any FAIL blocks emission; Designer re-prompts the main session with the targeted follow-up text. REQUIRED-tier checks (1-7) block emission; RECOMMENDED check (8) emits with warnings.
```

Then emit the 8 checks in order, each as a bold line + `- FAIL:` bullet:

```markdown
**Check 1: `schema_version` present and equals current version (`1`)**
- FAIL: Set `schema_version: 1` automatically; no follow-up needed.

**Check 2: `team.name` non-empty string AND `team.topology` in {pipeline, mesh, hierarchy, swarm}**
- FAIL: Pick topology via [orchestration-patterns.md](orchestration-patterns.md) Topology Decision Table; default to `mesh` on ambiguity.

**Check 3: `agents[]` length >= 1**
- FAIL: Impossible from a valid Business Graph (processes[] >= 1). If triggered, re-read the Business Graph.

**Check 4: Every agent has unique `id`, non-empty `role`, non-empty `goal`, `tools[]` (>=1), `triggers[]` (>=1), `autonomy` in {full, semi, supervised}, `blast_radius` in {1,2,3,4}**
- FAIL: For each gap, surface the specific agent and field to the main session with a targeted question.

**Check 5: Every `triggers[].type` in {cron, event, manual, inter-agent} with required sub-field per Trigger Bounded Enum**
- FAIL: Ask "What triggers <agent-id>, a schedule, external event, human action, or another agent?" before emission.

**Check 6: `orchestration.workflows[]` length >= 1, every workflow has `type` in the 5-pattern enum, `agents[]` (>=1), and a `trigger`**
- FAIL: Classify via [orchestration-patterns.md](orchestration-patterns.md) 5-pattern table; default to `event-driven` on ambiguity.

**Check 7: Every `workflows[].agents[]` id AND every `agents[].dependencies[]` id resolves to an entry in `agents[]` (ORCH-04)**
- FAIL: Reject the YAML; log the unresolved id; re-read Business Graph for correct mapping.

**Check 8 (WARN, not FAIL): RECOMMENDED fields populated or explicitly marked `null`**
- WARN: Emit with nulls; flag gaps in the rendered table so user can accept or fix.
```

**Section 9 - `## Emission Protocol`** (6-8 numbered steps mirroring business-graph-schema.md lines 101-112). Exact emission:

```markdown
## Emission Protocol

Emission happens when the main session's Phase 2 Design Summary gate spawns the Designer Agent subagent (D-21). The steps:

1. Designer walks the Validation Checklist above in order.
2. For each REQUIRED failure (Checks 1-7), Designer returns a targeted follow-up to the main session and waits for the user's answer before resuming.
3. Once all REQUIRED checks pass, Designer writes the validated YAML silently to `.agentbloc/team/agent-profiles.yaml`. Create the `.agentbloc/team/` directory if it does not exist.
4. Designer returns a rendered TABLE + per-agent cards + ASCII topology diagram to the main session (per v1.0 D-05 and D-06). The YAML itself is NEVER shown to the user.
5. The main session presents the table / cards / diagram to the user and asks for confirmation.
6. After user confirmation, the main session sets the Phase 2 `agent_profiles_validated` sub-gate to `approved` and allows transition to Phase 3.

If the user edits any table, follow the Re-run Behavior below.
```

**Section 10 - `## Re-run Behavior`** (mirrors business-graph-schema.md lines 114-122 PLUS D-26 surgical-patch rule). Exact emission:

```markdown
## Re-run Behavior

If `.agentbloc/team/agent-profiles.yaml` already exists when the Summary gate is reached, the main session asks the user: "I already have an agent team on file for this project. Do you want to (a) keep the existing one, (b) overwrite it, or (c) apply specific edits to it?" Default is **apply edits** (surgical patch per D-26).

- **keep**: Skip emission, transition to Phase 3 with the existing team.
- **overwrite**: Designer regenerates the YAML from scratch against the Business Graph. Warn the user that previous conversational edits (renames, drops, additions) will be lost.
- **apply edits (default, D-26)**: Designer parses user intent into a structured patch (rename / delete / add-tool / remove-tool / change-autonomy / change-topology / change-blast-radius), applies it in-place to the existing YAML, bumps `team.modified_at`, re-runs the Validation Checklist, and re-renders the TABLE for user confirmation. Designer NEVER regenerates from the Business Graph for edits - regeneration would re-insert rejected or renamed agents, fighting user intent.

The `schema_version` on disk must match the current schema version. If it does not, refuse edit/merge and emit `action_required: schema_version_mismatch` to the conversation so the user knows a manual migration is needed.
```

**Section 11 - `## Schema Versioning Rules`** (mirrors business-graph-schema.md lines 124-137 exactly, with paths and consumer list adapted). Exact emission:

```markdown
## Schema Versioning Rules

The `schema_version` field is an integer. It starts at `1`. The version bumps only on breaking changes:

- A REQUIRED field is removed or renamed.
- An enum value is removed from a bounded type (e.g., dropping `semi` from `autonomy`).

Additive changes do NOT bump the version:

- Adding a new OPTIONAL field (e.g., `model` hint).
- Adding a new value to a bounded enum (e.g., adding `inter-agent` to triggers).
- Loosening a REQUIRED field to RECOMMENDED.

Downstream consumers (Phase 12 Deploy, Phase 13 Runtime, Phase 14 Briefing Agent, Phase 15 Anticipation) read `schema_version` and refuse to proceed on an unknown major version.
```

**Formatting rules for the whole file:**
- All cross-links to companion references use shorthand relative paths (`orchestration-patterns.md`, `business-graph-schema.md`, `phase-2-design.md`, `blast-radius.md` - no `references/` prefix) because this file sits in the same references/ directory.
- Every bounded enum renders as a markdown table, NEVER as prose list (matches business-graph-schema.md house style).
- Keep the file between 150 and 220 lines.
- Do NOT use em-dashes `—` anywhere (project CLAUDE.md rule). Use ` - `, `:`, or recast the sentence.
- Do NOT add a "Quick Reference" footer (not needed — TOC + bounded enum tables serve that role, same as business-graph-schema.md).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "^# Agent Profile Schema$" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## Table of Contents" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## When This Applies" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## Schema Definition" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## Field Obligation Matrix" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## Autonomy Bounded Enum" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## Trigger Bounded Enum" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## Topology Bounded Enum" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## Validation Checklist" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## Emission Protocol" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## Re-run Behavior" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "## Schema Versioning Rules" .claude/skills/agentbloc/references/agent-profile-schema.md &amp;&amp; grep -q "schema_version" .claude/skills/agentbloc/references/agent-profile-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/references/agent-profile-schema.md` exits 0
    - `grep -q "^# Agent Profile Schema$" .claude/skills/agentbloc/references/agent-profile-schema.md` matches (exact H1)
    - All 11 planned H2 sections present: `grep -c "^## " .claude/skills/agentbloc/references/agent-profile-schema.md` returns >= 10 (TOC is H2 but "Table of Contents" counts, plus the 10 content sections)
    - `grep -q "schema_version" .claude/skills/agentbloc/references/agent-profile-schema.md` matches
    - All 3 obligation tiers present: `grep -q "REQUIRED" ...` AND `grep -q "RECOMMENDED" ...` AND `grep -q "OPTIONAL" ...`
    - All 3 autonomy values present: `grep -q "\`full\`" OR grep -q "full.*agent acts without" ...`, `grep -q "semi" ...`, `grep -q "supervised" ...`
    - All 4 trigger types present in Trigger Bounded Enum: `grep -q "cron" ...`, `grep -q "event" ...`, `grep -q "manual" ...`, `grep -q "inter-agent" ...`
    - All 4 topology values present: `grep -q "pipeline" ...`, `grep -q "mesh" ...`, `grep -q "hierarchy" ...`, `grep -q "swarm" ...`
    - All 5 orchestration patterns named in Check 6 enum or the fenced schema: `grep -q "sequential" ...`, `grep -q "parallel" ...`, `grep -q "loop" ...`, `grep -q "event-driven" ...`, `grep -q "conversational" ...`
    - `grep -q "\.agentbloc/team/agent-profiles\.yaml" .claude/skills/agentbloc/references/agent-profile-schema.md` - output path locked
    - `grep -q "orchestration-patterns\.md" ...` AND `grep -q "business-graph-schema\.md" ...` AND `grep -q "phase-2-design\.md" ...` - cross-links to all three companion references
    - `grep -Eq "Check [0-9]" .claude/skills/agentbloc/references/agent-profile-schema.md` - numbered validation checks exist
    - `grep -q "ORCH-04" .claude/skills/agentbloc/references/agent-profile-schema.md` - requirement ID cited (resolves-IDs rule)
    - `grep -q "D-26\|surgical patch\|apply edits" .claude/skills/agentbloc/references/agent-profile-schema.md` - D-26 surgical-patch rule present in Re-run Behavior
    - `grep -q "` + "`" + "yaml" .claude/skills/agentbloc/references/agent-profile-schema.md` - schema fenced block uses YAML language identifier
    - `wc -l .claude/skills/agentbloc/references/agent-profile-schema.md` returns between 150 and 230 lines
    - No em-dashes: `grep -c "—" .claude/skills/agentbloc/references/agent-profile-schema.md` returns 0
  </acceptance_criteria>
  <done>
agent-profile-schema.md exists with all 11 planned H2 sections (Table of Contents + 10 content sections), contains the commented YAML Schema Definition block, three-tier Field Obligation Matrix (REQUIRED / RECOMMENDED / OPTIONAL), three bounded enum tables (Autonomy with full/semi/supervised, Trigger with cron/event/manual/inter-agent, Topology with pipeline/mesh/hierarchy/swarm), an 8-check Validation Checklist with ORCH-04 resolves-IDs check, Emission Protocol with 6 numbered steps, Re-run Behavior documenting the D-26 surgical-patch default, and Schema Versioning Rules. Cross-links to orchestration-patterns.md, business-graph-schema.md, phase-2-design.md, and blast-radius.md use shorthand relative paths. File is 150-230 lines with zero em-dashes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create examples/arco-rooms-agent-profiles.yaml as the canonical 3-agent test fixture</name>
  <files>.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml</files>
  <read_first>
    - .claude/skills/agentbloc/examples/arco-rooms-business-graph.json (the input Business Graph this fixture is derived from - 3 processes: Invoice Collection, Payment Matching, Owner Reporting; security_profile with GDPR; decision_patterns with 3 rules)
    - .claude/skills/agentbloc/references/agent-profile-schema.md (the schema created in Task 2 - this fixture MUST satisfy every REQUIRED check in the Validation Checklist)
    - .claude/skills/agentbloc/references/orchestration-patterns.md (the 5-pattern catalog from Task 1 - fixture workflows[].type values come from this enum)
    - .planning/phases/09-designer-agent/09-PATTERNS.md (lines 608-735 — the "arco-rooms-agent-profiles.yaml (NEW)" section with Patterns A and B giving the process-to-agent mapping AND the full YAML template to adapt)
    - .planning/phases/09-designer-agent/09-CONTEXT.md (D-30 locks the 3-agent scope: Gestor Cobros, Recepcionista, Gestor Documental. Anticipated 2 agents are Phase 15 work and MUST NOT appear here)
  </read_first>
  <action>
Create a NEW file at `.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml` containing pure YAML (no comments in the fixture itself - comments belong in the schema reference). The fixture MUST populate every REQUIRED field and SHOULD populate most RECOMMENDED fields so it can serve as the canonical "what a good agent-profiles.yaml looks like" reference for Phase 16.

**Process-to-agent mapping (per D-30 + 09-PATTERNS.md):**
- `gestor-documental` <- Business Graph process "Invoice Collection"
- `gestor-cobros` <- Business Graph process "Payment Matching" + overdue-invoice rule from decision_patterns
- `recepcionista` <- Business Graph process "Owner Reporting"

**ONLY these 3 agents. The 2 anticipated agents (Analista Rentabilidad, Gestor Incidencias) are Phase 15 work and MUST NOT appear in this fixture.**

Emit the following EXACT YAML content:

```yaml
schema_version: 1
team:
  name: arco-rooms-team
  topology: mesh
  topology_rationale: "3 agents peer-call each other (Recepcionista queries Gestor Cobros for payment status before sending owner reports; Gestor Cobros depends on Gestor Documental invoice output); mesh matches ClaudeClaw SendMessage pattern."
  briefing_agent_id: null

agents:
  - id: gestor-documental
    role: "Invoice Collection Specialist"
    goal: "Fetch, deduplicate, and persist utility invoices from 6 providers every night"
    backstory: "Owns the daily invoice-collection pipeline. Has credentials for Endesa, Aguas de Almeria, Naturgy, Movistar, Urbaser, and Mapfre. Knows which providers deliver by email and which require portal login."
    tools:
      - playwright-mcp
      - google-workspace-mcp
      - mapfre-api
    triggers:
      - type: cron
        schedule: "0 22 * * *"
    autonomy: full
    outputs:
      - type: state-file
        schema: ".agentbloc/state/invoices.json"
    escalation: "telegram:pablo"
    dependencies: []
    blast_radius: 2
    model: sonnet

  - id: gestor-cobros
    role: "Payment Reconciliation Engine"
    goal: "Match bank transactions to invoices with confidence scoring and enforce the overdue-7-day rule"
    backstory: "Owns the daily payment-matching pipeline. Knows the tenant registry and confidence-score regex patterns. Applies the decision rule: if an invoice is overdue by more than 7 days, send a formal notice to the tenant."
    tools:
      - bank-mcp
      - google-sheets-mcp
    triggers:
      - type: cron
        schedule: "30 22 * * *"
      - type: inter-agent
        caller: recepcionista
        message: payment-status-query
    autonomy: semi
    outputs:
      - type: state-file
        schema: ".agentbloc/state/matches.json"
    escalation: "telegram:pablo"
    dependencies:
      - gestor-documental
    blast_radius: 2
    model: opus

  - id: recepcionista
    role: "Daily Operations Reporter"
    goal: "Send per-owner Telegram summary of invoices, payments, and unmatched items; query Gestor Cobros for payment status before composing messages"
    backstory: "Owner-facing agent. Queries Gestor Cobros for payment status before composing messages. Sends to each owner's dedicated Telegram thread."
    tools:
      - telegram-mcp
    triggers:
      - type: cron
        schedule: "0 23 * * *"
    autonomy: semi
    outputs:
      - type: telegram-message
        schema: per-owner-daily-summary
    escalation: "telegram:pablo"
    dependencies:
      - gestor-cobros
    blast_radius: 4
    model: sonnet

orchestration:
  workflows:
    - id: cobro-diario
      type: sequential
      agents:
        - gestor-documental
        - gestor-cobros
        - recepcionista
      trigger:
        type: cron
        schedule: "0 22 * * *"
      why: "Sequential per orchestration-patterns.md: each agent's output feeds the next (invoices -> matches -> report)."
      steps:
        - "gestor-documental collects invoices from 6 utility providers"
        - "gestor-cobros matches bank transactions to invoices with confidence scoring"
        - "recepcionista sends per-owner Telegram summary"

    - id: unmatched-payment-alert
      type: event-driven
      agents:
        - recepcionista
      trigger:
        type: manual
        description: "Invoked when gestor-cobros detects 3+ unmatched items"
      why: "Event-driven per orchestration-patterns.md: Gestor Cobros wakes Recepcionista via inter-agent SendMessage when unmatched items exceed the 3-item threshold."
      flow: "gestor-cobros detects unmatched >= 3 items; sends inter-agent message to recepcionista; recepcionista composes Telegram alert to Pablo; returns."
```

**Formatting requirements:**
- 2-space indentation throughout
- String values with special characters wrapped in double quotes; simple strings unquoted
- File must parse as valid YAML (run `python3 -c "import yaml; yaml.safe_load(open('...'))"` clean)
- End file with a single trailing newline
- No comments in the fixture (keep schema reference the source of commentary)
- No em-dashes `—` anywhere

This fixture is the reference Phase 16 end-to-end validation will use — running the Designer Agent against `arco-rooms-business-graph.json` must produce a YAML structurally equivalent to this one (not byte-identical, but all REQUIRED fields populated, 3 specific agents, mesh topology, sequential + event-driven workflows).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml &amp;&amp; python3 -c "import yaml, sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); assert d['schema_version']==1; assert d['team']['topology']=='mesh'; assert len(d['agents'])==3; ids=[a['id'] for a in d['agents']]; assert 'gestor-documental' in ids and 'gestor-cobros' in ids and 'recepcionista' in ids; assert all('role' in a and 'goal' in a and 'tools' in a and len(a['tools'])&gt;=1 and 'triggers' in a and len(a['triggers'])&gt;=1 and 'autonomy' in a and a['autonomy'] in ('full','semi','supervised') and 'blast_radius' in a and a['blast_radius'] in (1,2,3,4) for a in d['agents']); assert len(d['orchestration']['workflows'])&gt;=1; assert all(w['type'] in ('sequential','parallel','loop','event-driven','conversational') for w in d['orchestration']['workflows']); all_ids=set(ids); [[sys.exit('unresolved agent id in workflow: '+ag) for ag in w['agents'] if ag not in all_ids] for w in d['orchestration']['workflows']]; [[sys.exit('unresolved dep: '+dep) for dep in a.get('dependencies',[]) if dep not in all_ids] for a in d['agents']]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml'))"` succeeds (valid YAML, no parse errors)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); sys.exit(0 if d['schema_version']==1 else 1)"` exits 0
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); sys.exit(0 if len(d['agents'])==3 else 1)"` exits 0 (EXACTLY 3 agents per D-30)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); ids=[a['id'] for a in d['agents']]; sys.exit(0 if set(ids)=={'gestor-documental','gestor-cobros','recepcionista'} else 1)"` exits 0 (the 3 canonical agent IDs match exactly)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); sys.exit(0 if d['team']['topology']=='mesh' else 1)"` exits 0 (mesh topology)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); sys.exit(0 if all('role' in a and 'goal' in a and 'tools' in a and len(a['tools'])>=1 and 'triggers' in a and len(a['triggers'])>=1 and 'autonomy' in a and 'blast_radius' in a for a in d['agents']) else 1)"` exits 0 (every agent has REQUIRED fields)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); sys.exit(0 if all(a['autonomy'] in ('full','semi','supervised') for a in d['agents']) else 1)"` exits 0 (autonomy enum satisfied)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); sys.exit(0 if all(t['type'] in ('cron','event','manual','inter-agent') for a in d['agents'] for t in a['triggers']) else 1)"` exits 0 (trigger enum satisfied)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); wfs=d['orchestration']['workflows']; sys.exit(0 if len(wfs)>=1 and all(w['type'] in ('sequential','parallel','loop','event-driven','conversational') for w in wfs) else 1)"` exits 0 (at least 1 workflow; all types from 5-pattern enum)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); ids={a['id'] for a in d['agents']}; sys.exit(0 if all(ag in ids for w in d['orchestration']['workflows'] for ag in w['agents']) else 1)"` exits 0 (ORCH-04: every workflow agent id resolves)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml')); ids={a['id'] for a in d['agents']}; sys.exit(0 if all(dep in ids for a in d['agents'] for dep in a.get('dependencies',[])) else 1)"` exits 0 (ORCH-04: every dependency id resolves)
    - `grep -q "Analista Rentabilidad\|analista-rentabilidad\|Gestor Incidencias\|gestor-incidencias" .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml` returns NO match (D-30: the 2 anticipated agents are excluded)
    - No em-dashes: `grep -c "—" .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml` returns 0
  </acceptance_criteria>
  <done>
A valid YAML fixture exists at `examples/arco-rooms-agent-profiles.yaml` with schema_version=1, team topology=mesh, EXACTLY 3 agents (gestor-documental + gestor-cobros + recepcionista per D-30), each with all REQUIRED fields populated (role, goal, tools[>=1], triggers[>=1], autonomy in full/semi/supervised, blast_radius in 1..4). The orchestration has >=1 workflow with type from the 5-pattern enum; all workflow agent IDs and all agent dependencies resolve to entries in agents[] (ORCH-04). The 2 Phase-15 anticipated agents (Analista Rentabilidad, Gestor Incidencias) are ABSENT. File contains zero em-dashes and parses cleanly with python3 yaml.safe_load.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, run these phase-level checks:

1. `test -f .claude/skills/agentbloc/references/orchestration-patterns.md` — 5-pattern reference exists
2. `test -f .claude/skills/agentbloc/references/agent-profile-schema.md` — schema reference exists
3. `test -f .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml` — fixture exists
4. `python3 -c "import yaml; yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml'))"` — fixture parses as valid YAML
5. `grep -c "^## " .claude/skills/agentbloc/references/orchestration-patterns.md` >= 6 (all H2 sections)
6. `grep -c "^## " .claude/skills/agentbloc/references/agent-profile-schema.md` >= 10 (all H2 sections including TOC)
7. `wc -l .claude/skills/agentbloc/references/orchestration-patterns.md` 120-200 lines
8. `wc -l .claude/skills/agentbloc/references/agent-profile-schema.md` 150-230 lines
9. Every Phase 9 requirement in this plan traced:
   - DSGN-02 (Designer emits agent-profiles.yaml with team + agents): schema file has `team.name`, `team.topology`, `agents[]` blocks; fixture populates them
   - DSGN-03 (per-agent profile fields): schema file has `role`, `goal`, `backstory`, `tools`, `triggers`, `autonomy`, `outputs`, `escalation`, `dependencies`; fixture populates them
   - DSGN-04 (topology from {pipeline, mesh, hierarchy, swarm}): schema Topology Bounded Enum + orchestration-patterns Topology Decision Table; fixture uses mesh
   - ORCH-01 (classify workflows into 5 patterns): orchestration-patterns 5-pattern table names Sequential / Parallel / Loop / Event-driven / Conversational
   - ORCH-02 (orchestration-patterns.md reference exists and Designer cites it): file exists; schema Check 6 points at it
   - ORCH-03 (orchestration.workflows with type + agents + trigger + steps/flow): schema Schema Definition block + Check 6; fixture has 2 workflows
   - ORCH-04 (workflow agent references resolve to agents[]): schema Check 7; fixture verification passes on it
</verification>

<success_criteria>
1. `references/orchestration-patterns.md` exists at 120-200 lines with the 5-pattern table, 4-topology decision table, 6-framework inheritance table, and Quick Reference.
2. `references/agent-profile-schema.md` exists at 150-230 lines with the YAML Schema Definition, three-tier Field Obligation Matrix, three bounded enums (Autonomy / Trigger / Topology), 8-check Validation Checklist, Emission Protocol, Re-run Behavior (including D-26 surgical-patch default), and Schema Versioning Rules.
3. `examples/arco-rooms-agent-profiles.yaml` exists, parses as valid YAML, has EXACTLY 3 agents (gestor-documental + gestor-cobros + recepcionista per D-30), mesh topology, all REQUIRED fields populated, all workflow agent IDs and dependencies resolving to agents[] entries (ORCH-04).
4. All cross-links between the three new files + existing companions (business-graph-schema.md, phase-2-design.md, blast-radius.md, frameworks.md) use shorthand relative paths.
5. Every file contains zero em-dashes (project CLAUDE.md convention).
6. Downstream Plan 09-02 (Designer subagent) has concrete file paths + schema checklist + reference patterns to read; Plan 09-03 (SKILL.md + phase-2-design.md wiring) has the `agent_profiles_validated` gate vocabulary + output path + companion references to cite.
</success_criteria>

<output>
After completion, create `.planning/phases/09-designer-agent/09-01-SUMMARY.md` documenting:
- The final schema shape (copy the fenced yaml block from agent-profile-schema.md)
- The 5-pattern table (copy from orchestration-patterns.md)
- The topology decision table (copy from orchestration-patterns.md)
- The 8-check validation checklist (list from agent-profile-schema.md)
- Final line counts of all three artifacts
- The exact canonical agent IDs in the fixture (gestor-documental, gestor-cobros, recepcionista) and confirmation that the 2 anticipated agents are absent
- Any deviations from the plan with rationale
- Handoff note for Plan 09-02: "The Designer subagent at .claude/agents/designer-agent.md reads these three files inside its forked context. agent-profile-schema.md is the contract Designer must satisfy. orchestration-patterns.md is the catalog Designer cites when classifying workflows and selecting topology. arco-rooms-agent-profiles.yaml is the verification target for Phase 16."
- Handoff note for Plan 09-03: "SKILL.md Phase 2 load list must reference both orchestration-patterns.md and agent-profile-schema.md. The agent_profiles_validated sub-gate maps to the Validation Checklist in agent-profile-schema.md (checks 1-7 block emission, check 8 warns). The Phase 3 precondition verifies .agentbloc/team/agent-profiles.yaml exists and validates."
</output>
