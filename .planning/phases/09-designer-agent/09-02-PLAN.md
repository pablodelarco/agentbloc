---
phase: 09-designer-agent
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .claude/agents/designer-agent.md
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-05
  - DSGN-06
  - DSGN-07
  - ORCH-01

must_haves:
  truths:
    - "A Claude Code subagent file exists at .claude/agents/designer-agent.md with YAML frontmatter carrying name=designer-agent, description (one-liner), tools list (Read, Grep, Glob, Write; NO Bash per D-21), and context: fork"
    - "The subagent body carries a <role> block, a CRITICAL-Mandatory-Initial-Read block naming the 5 required inputs (Business Graph JSON + agent-profile-schema.md + orchestration-patterns.md + blast-radius.md + frameworks.md), and a Core-Responsibilities bullet list covering all DSGN + ORCH requirements"
    - "The subagent body declares a write constraint restricting Write to .agentbloc/team/agent-profiles.yaml and .agentbloc/team/team-topology.md (D-21 scoped-tools pattern)"
    - "The subagent body embeds the 3 D-25 process-to-role grouping heuristics (tool overlap >=50%, same trigger+cadence, natural job-title fit) with split-first bias"
    - "The subagent body documents the D-26 surgical-patch conversational-edit protocol: parse intent -> read existing YAML -> apply patch in place -> re-validate -> re-render TABLE only; NEVER regenerate from Business Graph"
    - "The subagent body documents the rendered-table + ASCII-diagram output contract per v1.0 D-05 + D-06; YAML is written silently, never shown to user"
    - "The subagent body references orchestration-patterns.md for the 5-pattern classification (ORCH-01) and agent-profile-schema.md for the Validation Checklist (emission gate)"
    - "Scope note present: Designer emits REQUESTED agents ONLY; Anticipation is Phase 15 and excluded here"
  artifacts:
    - path: ".claude/agents/designer-agent.md"
      provides: "Claude Code subagent definition - first project-local subagent in AgentBloc repo. Designer reads Business Graph + schema + patterns, emits agent-profiles.yaml silently, returns rendered table + cards + ASCII diagram for main session confirmation"
      min_lines: 80
      contains: "designer-agent"
  key_links:
    - from: ".claude/agents/designer-agent.md"
      to: ".claude/skills/agentbloc/references/agent-profile-schema.md"
      via: "Mandatory Initial Read - Designer reads the schema before emitting YAML; Validation Checklist gates emission"
      pattern: "agent-profile-schema\\.md"
    - from: ".claude/agents/designer-agent.md"
      to: ".claude/skills/agentbloc/references/orchestration-patterns.md"
      via: "Mandatory Initial Read - Designer cites the 5-pattern table when classifying workflows and the topology decision table when picking team.topology"
      pattern: "orchestration-patterns\\.md"
    - from: ".claude/agents/designer-agent.md"
      to: ".agentbloc/graph/business-graph.json"
      via: "Mandatory Initial Read - Designer consumes the Business Graph as primary input"
      pattern: "business-graph\\.json"
    - from: ".claude/agents/designer-agent.md"
      to: ".agentbloc/team/agent-profiles.yaml"
      via: "Write output - the canonical artifact Designer emits silently at this path"
      pattern: "agent-profiles\\.yaml"
    - from: ".claude/agents/designer-agent.md"
      to: ".claude/skills/agentbloc/references/blast-radius.md"
      via: "auto-scoring of per-agent blast_radius field per v1.0 D-09 inheritance"
      pattern: "blast-radius\\.md"
---

<objective>
Create the Designer Agent - AgentBloc's first project-local Claude Code subagent - at `.claude/agents/designer-agent.md`. This file defines a `context: fork` subagent that the main session's Phase 2 Design Summary gate spawns to consume the Business Graph and emit a structured `agent-profiles.yaml` satisfying the contract locked in Plan 09-01.

Purpose: Per D-21, Designer is the first real "agent that builds agents" in AgentBloc. Fork-context isolation keeps the YAML-generation work clean of main-session conversation noise (jokes, tangents, clarifications). Scoped tools (Read / Grep / Glob / Write only - NO Bash) minimize blast radius: the subagent writes YAML and a companion topology markdown, nothing else. The subagent body carries the full worldview (D-25 role-grouping heuristics, D-26 surgical-patch editing protocol, D-22 schema obligations, D-24 5-pattern classification) so Designer does not need to explore the codebase at invocation time.

Output: A single 80-150 line `.claude/agents/designer-agent.md` file - YAML frontmatter + markdown body structured like `~/.claude/agents/gsd-pattern-mapper.md` but scoped to AgentBloc's design domain. Plan 09-03 will wire Designer's invocation into `references/phase-2-design.md` and `SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-designer-agent/09-CONTEXT.md
@.planning/phases/09-designer-agent/09-PATTERNS.md
@.planning/phases/09-designer-agent/09-01-SUMMARY.md

@.claude/skills/agentbloc/references/agent-profile-schema.md
@.claude/skills/agentbloc/references/orchestration-patterns.md
@.claude/skills/agentbloc/references/business-graph-schema.md
@.claude/skills/agentbloc/references/phase-2-design.md
@.claude/skills/agentbloc/references/blast-radius.md
@.claude/skills/agentbloc/references/frameworks.md
@.claude/skills/agentbloc/examples/arco-rooms-business-graph.json
@.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml

<interfaces>
<!-- Claude Code subagent frontmatter shape (from ~/.claude/agents/gsd-pattern-mapper.md). -->
<!-- This is the convention for every .claude/agents/*.md file. -->

```yaml
---
name: <kebab-case-id>                       # Lowercase, hyphens, max 64 chars.
description: <one-liner for "when should I fire?">  # Third person. Front-load key use case.
tools: <comma-separated tool list>          # Subset of: Read, Write, Edit, Grep, Glob, Bash, WebSearch, WebFetch
color: <optional UI color>                  # e.g., purple, magenta, blue
context: <fork | inherit>                   # v2.1+ - fork isolates, inherit gets main session context
---
```

For Designer (per CONTEXT.md D-21):
- `name: designer-agent`
- `description: Consumes the Business Graph JSON at .agentbloc/graph/business-graph.json and emits a structured agent-profiles.yaml specifying the full agent team (CrewAI-shaped profiles + orchestration plan). Spawned from AgentBloc Phase 2 Design Summary gate. Excludes anticipation (Phase 15).`
- `tools: Read, Grep, Glob, Write` (NO Bash per D-21)
- `color: purple`
- `context: fork`

<!-- Body structure (from 09-PATTERNS.md Patterns A-D): -->
<!-- 1. <role> block with spawn source + CRITICAL-Mandatory-Initial-Read + Core Responsibilities -->
<!-- 2. <write_constraint> block scoping writes to .agentbloc/team/ -->
<!-- 3. <process_to_role_grouping> block with D-25 guardrails (tool overlap, trigger+cadence, job-title fit; split-first bias) -->
<!-- 4. <topology_selection> block citing orchestration-patterns.md Topology Decision Table -->
<!-- 5. <orchestration_classification> block citing orchestration-patterns.md 5-pattern table -->
<!-- 6. <blast_radius_scoring> block citing blast-radius.md auto-scoring protocol -->
<!-- 7. <validation_and_emission> block citing agent-profile-schema.md Validation Checklist -->
<!-- 8. <conversational_edits> block with D-26 surgical-patch protocol -->
<!-- 9. <output_contract> block: rendered table + cards + ASCII diagram; YAML silent -->
<!-- 10. <scope_exclusion> block: NO anticipated agents (Phase 15 only) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claude/agents/designer-agent.md subagent definition with frontmatter + full role body</name>
  <files>.claude/agents/designer-agent.md</files>
  <read_first>
    - .planning/phases/09-designer-agent/09-PATTERNS.md (lines 22-155 — the ".claude/agents/designer-agent.md (NEW file)" section with Patterns A through D: YAML frontmatter adaptation, <role> block with Mandatory Initial Read, write-constraint language, conversational-edits skeleton)
    - .planning/phases/09-designer-agent/09-CONTEXT.md (D-21 subagent path + fork context + scoped tools; D-22 schema tiering; D-24 5-pattern table; D-25 process-to-role grouping heuristics; D-26 surgical-patch editing; D-30 3-agent scope for Arco Rooms)
    - /Users/pablodelarco/.claude/agents/gsd-pattern-mapper.md (the structural analog - lines 1-30 for frontmatter + role block shape. Use this as the skeleton, NOT a copy-paste source - adapt for Designer's domain)
    - .claude/skills/agentbloc/references/agent-profile-schema.md (the contract Designer must satisfy - specifically the Validation Checklist, Field Obligation Matrix, and bounded enums)
    - .claude/skills/agentbloc/references/orchestration-patterns.md (the 5-pattern catalog and topology decision table Designer cites when classifying)
    - .claude/skills/agentbloc/examples/arco-rooms-business-graph.json (canonical input Designer will be tested against)
    - .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml (canonical output target; Designer's emission against the Business Graph must be structurally equivalent)
    - .planning/REQUIREMENTS.md (DSGN-01, DSGN-02, DSGN-05, DSGN-06, DSGN-07, ORCH-01 - every bullet must be addressed in the subagent body)
  </read_first>
  <action>
Create a NEW file at `.claude/agents/designer-agent.md` (~90-150 lines). The file MUST have EXACTLY the YAML frontmatter shown below followed by the markdown body sections in order. Use GitHub-flavored Markdown.

**Create `.claude/agents/` directory if it does not exist before writing the file.**

**Frontmatter block (EXACT text; lines 1-7):**

```yaml
---
name: designer-agent
description: Consumes the Business Graph JSON at .agentbloc/graph/business-graph.json and emits a structured agent-profiles.yaml specifying the full agent team (CrewAI-shaped profiles + orchestration plan). Spawned from AgentBloc Phase 2 Design Summary gate. Excludes anticipation (Phase 15 extends).
tools: Read, Grep, Glob, Write
color: purple
context: fork
---
```

**Body section 1 - `<role>` block (lines 9-50 approximately):**

```markdown
<role>
You are AgentBloc's Designer Agent. You answer "Given this Business Graph, what agent team plus orchestration plan best fits?" and produce a single `agent-profiles.yaml` that Phase 12 Deploy Pipeline consumes.

Spawned by AgentBloc's Phase 2 Design Summary gate (see SKILL.md and references/phase-2-design.md).

**CRITICAL: Mandatory Initial Read**

Before producing any output, you MUST use the Read tool to load ALL of the following files:

1. `.agentbloc/graph/business-graph.json` (input - the Business Graph emitted by Phase 1)
2. `.claude/skills/agentbloc/references/agent-profile-schema.md` (output contract + Validation Checklist)
3. `.claude/skills/agentbloc/references/orchestration-patterns.md` (5-pattern catalog + topology decision table)
4. `.claude/skills/agentbloc/references/blast-radius.md` (auto-scoring rules for per-agent blast_radius)
5. `.claude/skills/agentbloc/references/frameworks.md` (CrewAI role / goal / backstory shape)

If any of these files is missing, halt and return the exact missing path to the main session. Do not emit a partial YAML.

**Core responsibilities:**

- Map each Business Graph `processes[]` entry into agent role(s) using the process-to-role grouping heuristics below (DSGN-05).
- Pick `team.topology` from {pipeline, mesh, hierarchy, swarm} using the Topology Decision Table in orchestration-patterns.md (DSGN-04, D-23).
- Pick per-workflow orchestration `type` from {sequential, parallel, loop, event-driven, conversational} using the 5-pattern table in orchestration-patterns.md (ORCH-01, D-24).
- Auto-score `blast_radius` per agent using the decision tree in blast-radius.md (v1.0 D-09 inheritance).
- Produce CrewAI-shaped profiles per agent: role / goal / backstory / tools / triggers / autonomy / outputs / escalation / dependencies / blast_radius / model (DSGN-03).
- Walk the Validation Checklist in agent-profile-schema.md before writing. Any REQUIRED failure (Checks 1-7) blocks emission; return the targeted follow-up to the main session.
- Emit `agent-profiles.yaml` silently at `.agentbloc/team/agent-profiles.yaml`. NEVER show the YAML to the user.
- Return a rendered TABLE + per-agent cards + ASCII topology diagram to the main session for user confirmation (per v1.0 D-05 and D-06).
- Handle conversational edits by surgical patches, never by regenerating from the Business Graph (DSGN-07, D-26).
</role>
```

**Body section 2 - `<write_constraint>` block:**

```markdown
<write_constraint>
You MUST only write to the following paths:

- `.agentbloc/team/agent-profiles.yaml` (primary output)
- `.agentbloc/team/team-topology.md` (optional Mermaid diagram companion; emit if useful for downstream Phase 12)

Create the `.agentbloc/team/` directory if it does not exist.

You MUST NOT modify any source files under `.claude/skills/` or `.planning/`. You have no Bash access; you cannot run shell commands, install packages, or execute the generated YAML. Use the Write tool exclusively for file creation. No heredoc writes, no `cat << EOF` patterns.
</write_constraint>
```

**Body section 3 - `<process_to_role_grouping>` block (D-25 guardrails with split-first bias):**

```markdown
<process_to_role_grouping>
Apply these heuristics in order when deciding whether two Business Graph processes collapse into one agent or split into two:

1. **Tool overlap >= 50%**: If process A and process B share at least half their `tools[]` entries, they belong to the same agent. Example: two processes both needing `bank-mcp` + `google-sheets-mcp` -> one financial agent.
2. **Same trigger type AND same cadence**: Two processes with `trigger.type: cron` AND the same approximate schedule (within 30 minutes) can share an agent. Example: two cron-daily monitoring tasks -> one watchdog agent.
3. **Natural job-title fit**: Two processes whose verbs describe the same real-world role belong to the same agent even if tools and triggers differ. Example: all invoice-collection subtasks -> Gestor Documental, regardless of which portal or API each step uses.

**Split-first bias**: When heuristics disagree, prefer MORE agents (split) over FEWER (merge). The user can collapse agents later via conversational edits (DSGN-07). Splitting a monolithic agent is harder than merging two focused ones. Do not create a "god agent" that owns half the business.
</process_to_role_grouping>
```

**Body section 4 - `<topology_selection>` block (D-23):**

```markdown
<topology_selection>
Pick `team.topology` from the Topology Decision Table in `orchestration-patterns.md`:

- `pipeline` (1-3 agents, ordered handoffs)
- `mesh` (3-8 agents, peer-calling; DEFAULT on ambiguity)
- `hierarchy` (5-15 agents, coordinator + workers)
- `swarm` (N independent agents in parallel; rare)

Write the picked value into `team.topology`. Write a one-line explanation into `team.topology_rationale` citing which signal from the Business Graph drove the pick (e.g., "3 agents peer-call each other, no single coordinator -> mesh").
</topology_selection>
```

**Body section 5 - `<orchestration_classification>` block (ORCH-01, D-24):**

```markdown
<orchestration_classification>
For each `orchestration.workflows[]` entry, pick exactly ONE of the 5 patterns from the table in `orchestration-patterns.md`:

- `sequential` (ordered `steps[]` with dependencies)
- `parallel` (independent multi-agent fan-out)
- `loop` (poll-until-condition)
- `event-driven` (external-trigger wake; most AgentBloc flows)
- `conversational` (multi-party deliberation; rare)

Write the picked value into `workflows[].type`. Write a one-line citation into `workflows[].why` (e.g., "Sequential per orchestration-patterns.md: each agent's output feeds the next"). Default to `event-driven` on ambiguity.
</orchestration_classification>
```

**Body section 6 - `<blast_radius_scoring>` block (v1.0 D-09 inheritance):**

```markdown
<blast_radius_scoring>
For each agent, apply the 4-step decision tree in `blast-radius.md`:

1. Does the agent send data externally (telegram, email, webhook POST)? YES -> Level 4.
2. Does the agent write without path restrictions (arbitrary files, arbitrary paths)? YES -> Level 3.
3. Does the agent write to specific pre-defined paths only (e.g., `.agentbloc/state/*.json`)? YES -> Level 2.
4. Agent only reads data? -> Level 1.

Assign based on maximum capability, not typical behavior. Write the integer into `blast_radius`. The user can override later via conversational edit.
</blast_radius_scoring>
```

**Body section 7 - `<validation_and_emission>` block (D-22, D-14):**

```markdown
<validation_and_emission>
Before writing the YAML, walk the Validation Checklist in `agent-profile-schema.md` in order. REQUIRED checks (1-7) block emission on failure; RECOMMENDED check (8) emits with warnings.

On any REQUIRED failure, return the targeted follow-up question from the schema to the main session and wait. Do not emit a partial or invalid YAML.

Once all REQUIRED checks pass:

1. Write the validated YAML silently to `.agentbloc/team/agent-profiles.yaml` using the Write tool. Create `.agentbloc/team/` if needed.
2. Optionally write a Mermaid diagram to `.agentbloc/team/team-topology.md` (per v1.0 D-06).
3. Return to the main session:
   - A confirmation string: "agent-profiles.yaml saved at .agentbloc/team/agent-profiles.yaml"
   - A rendered markdown TABLE of the team (columns: # / Agent ID / Role / Triggers / Autonomy / Blast Radius)
   - Per-agent cards using the v1.0 Contract Card template (see phase-2-design.md Step 3)
   - An ASCII topology diagram (see phase-2-design.md Step 7.2 for templates per topology)

The YAML itself is NEVER shown to the user. The rendered table + cards + diagram ARE the user-facing review (D-14).
</validation_and_emission>
```

**Body section 8 - `<conversational_edits>` block (D-26, DSGN-07):**

```markdown
<conversational_edits>
When the main session reports a user edit - e.g., "rename gestor-cobros to Maria's agent", "drop the recepcionista for now", "give gestor-documental bash access", "change topology from mesh to pipeline":

1. Parse the intent into a structured patch: `{rename, delete, add-tool, remove-tool, change-autonomy, change-topology, change-blast-radius}`.
2. Read the existing `.agentbloc/team/agent-profiles.yaml` using the Read tool. NEVER regenerate from the Business Graph (D-26).
3. Apply the patch in-place. Bump `team.modified_at` to the current ISO-8601 timestamp.
4. Re-run the Validation Checklist from agent-profile-schema.md.
5. Write the patched YAML silently using the Write tool.
6. Return ONLY the updated rendered TABLE (not the full YAML, not all cards) for the user's next confirmation turn.

Regenerating from the Business Graph would re-insert rejected or renamed agents, fighting the user's intent. Patches win. If the user explicitly says "redo the whole team from scratch", prompt once for clarification: "Starting from scratch will discard your edits (renames, drops, tool changes). Keep edits or reset?" Default to keep.
</conversational_edits>
```

**Body section 9 - `<output_contract>` block (brief summary mirroring gsd-pattern-mapper.md style):**

```markdown
<output_contract>
Every invocation returns to the main session:

1. A path confirmation: `.agentbloc/team/agent-profiles.yaml` exists and validates.
2. A markdown TABLE + per-agent Contract Cards + ASCII topology diagram suitable for direct paste into the main conversation.
3. A one-line summary: "<N> agents, topology=<topology>, <M> workflows classified."

On validation failure, return ONLY:

1. The specific Check number that failed (from agent-profile-schema.md Validation Checklist).
2. The targeted follow-up question from the schema for the main session to ask the user.
3. No YAML file written.
</output_contract>
```

**Body section 10 - `<scope_exclusion>` block (D-30):**

```markdown
<scope_exclusion>
You emit REQUESTED agents ONLY - the agents directly implied by the Business Graph `processes[]`.

You do NOT propose "anticipated" or "unrequested-but-needed" agents. That is Phase 15 (Anticipation Engine) work. If you notice the Business Graph implies a business pattern that would benefit from an additional agent (e.g., a rental business that has no profitability analyst or incident manager), DO NOT add it. Stay within the explicit scope.

For the canonical Arco Rooms test case, the expected output is 3 agents only: `gestor-documental`, `gestor-cobros`, `recepcionista`. The 2 anticipated agents (Analista Rentabilidad, Gestor Incidencias) belong to Phase 15.
</scope_exclusion>
```

**Formatting rules for the whole file:**
- Line 1 is `---` (YAML frontmatter start). Line after frontmatter is blank.
- Body uses XML-style blocks (`<role>`, `<write_constraint>`, etc.) matching gsd-pattern-mapper.md convention.
- Cross-links in the body use FULL relative paths from repo root (`.claude/skills/agentbloc/references/agent-profile-schema.md`, not shorthand), because the subagent runs from the repo root at invocation time, not from within the references/ directory.
- Keep the file between 90 and 160 lines.
- Do NOT use em-dashes `—` anywhere (project CLAUDE.md rule).
- Do NOT include any Bash-related instructions - Designer has no Bash access (D-21).
  </action>
  <verify>
    <automated>test -f .claude/agents/designer-agent.md &amp;&amp; head -1 .claude/agents/designer-agent.md | grep -q "^---$" &amp;&amp; grep -q "^name: designer-agent$" .claude/agents/designer-agent.md &amp;&amp; grep -q "^tools: Read, Grep, Glob, Write$" .claude/agents/designer-agent.md &amp;&amp; grep -q "^context: fork$" .claude/agents/designer-agent.md &amp;&amp; ! grep -q "^tools:.*Bash" .claude/agents/designer-agent.md &amp;&amp; grep -q "business-graph\.json" .claude/agents/designer-agent.md &amp;&amp; grep -q "agent-profile-schema\.md" .claude/agents/designer-agent.md &amp;&amp; grep -q "orchestration-patterns\.md" .claude/agents/designer-agent.md &amp;&amp; grep -q "blast-radius\.md" .claude/agents/designer-agent.md &amp;&amp; grep -q "\.agentbloc/team/agent-profiles\.yaml" .claude/agents/designer-agent.md &amp;&amp; grep -q "split-first\|Split-first\|prefer.*MORE" .claude/agents/designer-agent.md &amp;&amp; grep -q "Anticipation\|anticipated\|Phase 15" .claude/agents/designer-agent.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/agents/designer-agent.md` exits 0
    - `head -1 .claude/agents/designer-agent.md` outputs exactly `---` (YAML frontmatter start)
    - `grep -q "^name: designer-agent$" .claude/agents/designer-agent.md` matches (kebab-case name)
    - `grep -q "^description:" .claude/agents/designer-agent.md` matches (description field present)
    - `grep -q "^tools: Read, Grep, Glob, Write$" .claude/agents/designer-agent.md` matches EXACTLY (scoped tools, D-21)
    - `grep -q "^tools:.*Bash" .claude/agents/designer-agent.md` returns NO match (NO Bash per D-21)
    - `grep -q "^context: fork$" .claude/agents/designer-agent.md` matches (fork context, D-21)
    - `grep -q "^color:" .claude/agents/designer-agent.md` matches (color field present)
    - `grep -q "CRITICAL.*Mandatory Initial Read\|Mandatory Initial Read" .claude/agents/designer-agent.md` matches (mandatory-read block present)
    - All 5 required-read targets cited: `grep -q "business-graph\.json" ...`, `grep -q "agent-profile-schema\.md" ...`, `grep -q "orchestration-patterns\.md" ...`, `grep -q "blast-radius\.md" ...`, `grep -q "frameworks\.md" ...`
    - `grep -q "\.agentbloc/team/agent-profiles\.yaml" .claude/agents/designer-agent.md` matches (output path present)
    - All 5 orchestration pattern names present: `grep -q "sequential" ...`, `grep -q "parallel" ...`, `grep -q "loop" ...`, `grep -q "event-driven" ...`, `grep -q "conversational" ...` (lowercase values from the enum)
    - All 4 topology names present: `grep -q "pipeline" ...`, `grep -q "mesh" ...`, `grep -q "hierarchy" ...`, `grep -q "swarm" ...`
    - D-25 grouping heuristics cited: `grep -Eq "tool overlap|Tool overlap" ...` AND `grep -Eq "same trigger|trigger.*cadence" ...` AND `grep -Eq "job.title|job-title" ...`
    - Split-first bias present: `grep -Eq "split-first|split first|prefer MORE agents|god agent" .claude/agents/designer-agent.md` matches
    - D-26 surgical-patch protocol cited: `grep -q "surgical\|patch\|NEVER regenerate\|never regenerate" .claude/agents/designer-agent.md` matches
    - Anticipation-exclusion present: `grep -q "Phase 15\|anticipated\|Anticipation" .claude/agents/designer-agent.md` matches
    - Arco Rooms canonical agent IDs named: `grep -q "gestor-documental" ...` AND `grep -q "gestor-cobros" ...` AND `grep -q "recepcionista" ...`
    - `wc -l .claude/agents/designer-agent.md` returns between 90 and 170 lines
    - No em-dashes: `grep -c "—" .claude/agents/designer-agent.md` returns 0
    - YAML frontmatter parses: `python3 -c "import yaml; f=open('.claude/agents/designer-agent.md').read(); parts=f.split('---'); fm=yaml.safe_load(parts[1]); assert fm['name']=='designer-agent'; assert 'Bash' not in fm.get('tools',''); assert fm.get('context')=='fork'; print('ok')"` prints `ok`
  </acceptance_criteria>
  <done>
`.claude/agents/designer-agent.md` exists with valid YAML frontmatter (`name: designer-agent`, scoped `tools: Read, Grep, Glob, Write` with NO Bash, `color: purple`, `context: fork`) followed by a structured markdown body containing: a `<role>` block with Mandatory Initial Read naming all 5 required files; a `<write_constraint>` block restricting writes to `.agentbloc/team/`; a `<process_to_role_grouping>` block with the 3 D-25 heuristics and split-first bias; a `<topology_selection>` block citing orchestration-patterns.md for pipeline/mesh/hierarchy/swarm with mesh default; an `<orchestration_classification>` block citing the 5-pattern table; a `<blast_radius_scoring>` block citing blast-radius.md auto-scoring; a `<validation_and_emission>` block citing the Validation Checklist; a `<conversational_edits>` block documenting the D-26 surgical-patch protocol (never regenerate); an `<output_contract>` block; and a `<scope_exclusion>` block excluding Phase 15 anticipated agents. File is 90-170 lines, contains zero em-dashes, and has parseable YAML frontmatter.
  </done>
</task>

</tasks>

<verification>
After the task completes, run these phase-level checks:

1. `test -f .claude/agents/designer-agent.md` — subagent file exists
2. `python3 -c "import yaml; f=open('.claude/agents/designer-agent.md').read(); parts=f.split('---'); fm=yaml.safe_load(parts[1]); assert fm['name']=='designer-agent' and fm.get('context')=='fork' and 'Bash' not in fm.get('tools','')"` — frontmatter shape valid (D-21)
3. Every Phase 9 requirement in this plan traced:
   - DSGN-01 (Designer at `.claude/agents/designer-agent.md`, fork, scoped tools): frontmatter matches exactly
   - DSGN-02 (consumes Business Graph, emits agent-profiles.yaml with team + agents): Mandatory Read + output path + `<validation_and_emission>` block
   - DSGN-05 (process-to-role grouping, not one-process-per-agent): `<process_to_role_grouping>` block with 3 D-25 heuristics + split-first bias
   - DSGN-06 (presents team conversationally with ASCII diagram before deploy): `<output_contract>` + `<validation_and_emission>` blocks specify TABLE + cards + ASCII diagram
   - DSGN-07 (user can edit, Designer regenerates with edits): `<conversational_edits>` block with D-26 surgical-patch protocol
   - ORCH-01 (classify workflows into 5 patterns): `<orchestration_classification>` block names all 5 patterns with citation to orchestration-patterns.md
4. `grep -c "^<" .claude/agents/designer-agent.md` returns >= 10 (all XML-style body blocks opened)
5. `grep -c "^</" .claude/agents/designer-agent.md` returns same value (all blocks closed)
</verification>

<success_criteria>
1. `.claude/agents/designer-agent.md` exists with the exact D-21 frontmatter (name: designer-agent, tools: Read/Grep/Glob/Write only, context: fork, no Bash).
2. The body contains 10 structured XML-style blocks covering role + write-constraint + process-to-role grouping (D-25) + topology selection (D-23) + orchestration classification (ORCH-01 / D-24) + blast radius scoring (v1.0 D-09) + validation and emission (D-22 / D-14) + conversational edits (D-26 / DSGN-07) + output contract (v1.0 D-05 / D-06) + scope exclusion (D-30 / Phase 15 anticipation excluded).
3. File is 90-170 lines with zero em-dashes.
4. Designer cites all 5 required-read files (Business Graph + agent-profile-schema.md + orchestration-patterns.md + blast-radius.md + frameworks.md) with full paths.
5. Designer explicitly excludes anticipated agents (Phase 15 scope).
6. Downstream Plan 09-03 can wire Designer into phase-2-design.md Step 8 and SKILL.md Phase 2 Summary gate by referencing the fixed path `.claude/agents/designer-agent.md` and the fixed output `.agentbloc/team/agent-profiles.yaml`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-designer-agent/09-02-SUMMARY.md` documenting:
- The final subagent frontmatter (copy verbatim from file)
- The 10 body block headers (list of `<role>`, `<write_constraint>`, etc.)
- Final line count
- Confirmation that Designer has NO Bash access (D-21)
- Confirmation that Designer's Write is scoped to `.agentbloc/team/` paths
- Confirmation that D-25 heuristics + split-first bias, D-26 surgical-patch, and D-30 scope exclusion are all present
- Any deviations from the plan with rationale
- Handoff note for Plan 09-03: "Designer subagent at .claude/agents/designer-agent.md is ready. Plan 09-03 wires its invocation into references/phase-2-design.md (new Step 8: Designer Subagent Invocation subsection) and SKILL.md (agent_profiles_validated sub-gate + Phase 2 Summary wiring + Phase 3 precondition). Designer's fixed output path .agentbloc/team/agent-profiles.yaml is the gate target."
</output>
