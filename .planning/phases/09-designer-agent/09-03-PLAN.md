---
phase: 09-designer-agent
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .claude/skills/agentbloc/references/phase-2-design.md
  - .claude/skills/agentbloc/SKILL.md
autonomous: true
requirements:
  - DSGN-06
  - DSGN-07
  - ORCH-02

must_haves:
  truths:
    - "references/phase-2-design.md loads orchestration-patterns.md AND agent-profile-schema.md in the Design Opening companion-load sentence alongside blast-radius.md and frameworks.md (D-29 companion reference extension)"
    - "references/phase-2-design.md has a new Step 8 (Designer Subagent Invocation) subsection between Step 7 (Visual Presentation) and Design Gate - covering invocation path, fork context, input payload, output contract, and gate check per D-21 + D-22"
    - "references/phase-2-design.md has a new Conversational Editing Flow subsection after the Design Gate - covering the D-26 surgical-patch protocol (parse intent -> read existing YAML -> patch in place -> re-render table -> never regenerate from Business Graph)"
    - "references/phase-2-design.md Quick Reference table has 2 new rows linking Step 8 to DSGN-01..06 + ORCH-01..04 and the Conversational Editing Flow to DSGN-07"
    - "SKILL.md State Transitions section has a new Phase 2 specific bullet naming the agent_profiles_validated sub-gate as a transition requirement (D-29 Edit 1, mirroring Phase 8's Phase 1 specific bullet)"
    - "SKILL.md Phase 2 section has its load list extended to include orchestration-patterns.md AND agent-profile-schema.md (D-29 Edit 2 first part) and a new Summary Gate paragraph describing Designer subagent spawn (D-29 Edit 2 second part)"
    - "SKILL.md Phase 3 section has a Precondition paragraph gating on .agentbloc/team/agent-profiles.yaml exists and validates (D-29 Edit 3, mirroring the Phase 8 Phase 2 precondition exactly)"
    - "SKILL.md total line count stays under 250 (v1.0 budget preserved)"
  artifacts:
    - path: ".claude/skills/agentbloc/references/phase-2-design.md"
      provides: "Extended design protocol - Design Opening loads 2 new companion references; new Step 8 Designer Subagent Invocation subsection; new Conversational Editing Flow subsection after Design Gate; Quick Reference with 2 new rows"
      min_lines: 350
      contains: "Designer Subagent Invocation"
    - path: ".claude/skills/agentbloc/SKILL.md"
      provides: "Phase 2 entry loads orchestration-patterns.md + agent-profile-schema.md unconditionally AND spawns Designer subagent at Summary gate; Phase 3 entry has agent-profiles.yaml precondition; State Transitions names agent_profiles_validated sub-gate"
      contains: "agent_profiles_validated"
  key_links:
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/orchestration-patterns.md"
      via: "unconditional load at Phase 2 entry alongside phase-2-design.md + agent-profile-schema.md"
      pattern: "orchestration-patterns\\.md"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/agent-profile-schema.md"
      via: "unconditional load at Phase 2 entry; Validation Checklist is the gate target"
      pattern: "agent-profile-schema\\.md"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/agents/designer-agent.md"
      via: "Phase 2 Summary Gate paragraph spawns the Designer subagent"
      pattern: "designer-agent\\.md"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".agentbloc/team/agent-profiles.yaml"
      via: "Phase 3 precondition verifies the file exists and validates before transition"
      pattern: "agent-profiles\\.yaml"
    - from: ".claude/skills/agentbloc/references/phase-2-design.md"
      to: ".claude/agents/designer-agent.md"
      via: "Step 8 Designer Subagent Invocation references the subagent file path"
      pattern: "designer-agent\\.md"
---

<objective>
Wire the Plan 09-01 artifacts (orchestration-patterns.md + agent-profile-schema.md reference files) AND the Plan 09-02 Designer subagent into the skill's Phase 2 flow and SKILL.md gate ritual. Three surgical file edits: two new subsections in phase-2-design.md, three surgical edits in SKILL.md (exactly mirroring the Phase 8 three-edit pattern one phase later).

Purpose: Success Criteria 3-5 of ROADMAP Phase 9 require that (a) Designer is actually invoked from Phase 2, (b) Designer cites orchestration-patterns.md when classifying, and (c) the user can conversationally edit the YAML and Designer surgically patches (never regenerates). This plan is the wiring that makes Plan 09-01's contracts and Plan 09-02's Designer subagent load-bearing. This also locks the `agent_profiles_validated` gate vocabulary so Phase 3 cannot start without a validated YAML at `.agentbloc/team/agent-profiles.yaml`.

Output: Two surgical edits in `references/phase-2-design.md` (Design Opening companion-load extension + Step 8 new subsection + Conversational Editing Flow new subsection + Quick Reference 2 new rows) and three surgical edits in `SKILL.md` (State Transitions Phase-2-specific bullet + Phase 2 load list extension with Summary Gate paragraph + Phase 3 precondition paragraph). SKILL.md stays under 250 lines; `phase-2-design.md` grows from 313 to ~360-380 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-designer-agent/09-CONTEXT.md
@.planning/phases/09-designer-agent/09-PATTERNS.md
@.planning/phases/09-designer-agent/09-01-SUMMARY.md
@.planning/phases/08-business-graph-foundation/08-02-PLAN.md
@.planning/phases/08-business-graph-foundation/08-02-SUMMARY.md

@.claude/skills/agentbloc/references/phase-2-design.md
@.claude/skills/agentbloc/references/orchestration-patterns.md
@.claude/skills/agentbloc/references/agent-profile-schema.md
@.claude/skills/agentbloc/references/business-graph-schema.md
@.claude/skills/agentbloc/SKILL.md

<interfaces>
<!-- Extension points in phase-2-design.md (current line ranges; file is 313 lines) -->
<!-- Design Opening: lines 23-25 ("also load [references/blast-radius.md] and [references/frameworks.md]") -->
<!-- Step 7 Visual Presentation: lines 240-293 (ends with "...precede each card with a plain-language summary...") -->
<!-- Design Gate: lines 295-301 (asks user to confirm; accepts modification requests) -->
<!-- Quick Reference: lines 303-313 (7-row table of Step -> ID -> Output -> Cross-References) -->

<!-- Extension points in SKILL.md (current file is 163 lines; Phase 8 just landed) -->
<!-- State Transitions: lines 34-40 (5 bullets ending in "Phase 1 specific: ...") -->
<!-- Phase 2 section: lines 98-105 (Precondition at line 102; "You MUST read" block at lines 104-105) -->
<!-- Phase 3 section: lines 107-112 (description at 109; "You MUST read" block at lines 111-112) -->

<!-- Phase 8 precedent mirroring exactly (structural twin, one phase later) -->
<!-- Phase 8 Task 2 Edit 1: extended Phase 1 load list with +1 See-line for business-graph-schema.md -->
<!-- Phase 9 Task 2 Edit 1: extends Phase 2 load list with +2 See-lines for orchestration-patterns.md + agent-profile-schema.md -->
<!-- Phase 8 Task 2 Edit 2: appended "Phase 1 specific:" bullet to State Transitions -->
<!-- Phase 9 Task 2 Edit 2: appends "Phase 2 specific:" bullet to State Transitions -->
<!-- Phase 8 Task 2 Edit 3: inserted **Precondition:** paragraph between Phase 2 description and "You MUST read" block -->
<!-- Phase 9 Task 2 Edit 3: inserts **Precondition:** paragraph between Phase 3 description and "You MUST read" block -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend references/phase-2-design.md - Design Opening companion load + Step 8 Designer Subagent Invocation + Conversational Editing Flow + Quick Reference rows</name>
  <files>.claude/skills/agentbloc/references/phase-2-design.md</files>
  <read_first>
    - .claude/skills/agentbloc/references/phase-2-design.md (the full 313-line file - specifically lines 23-25 for Design Opening; lines 240-293 for Step 7 end; lines 295-301 for Design Gate; lines 303-313 for Quick Reference)
    - .claude/skills/agentbloc/references/orchestration-patterns.md (companion reference created in Plan 09-01 - Step 8 must reference it)
    - .claude/skills/agentbloc/references/agent-profile-schema.md (companion reference created in Plan 09-01 - Step 8 must cite the Validation Checklist)
    - .planning/phases/09-designer-agent/09-PATTERNS.md (lines 738-849 — the "phase-2-design.md (MODIFY)" section with Patterns A through C: companion-reference extension text, Step 8 Designer Invocation template, Conversational Editing Flow template)
    - .planning/phases/09-designer-agent/09-CONTEXT.md (D-21 Designer fork-context invocation; D-22 Validation Checklist gate; D-26 surgical-patch conversational edits; D-29 phase-2-design.md as the wiring target)
    - .planning/phases/08-business-graph-foundation/08-02-PLAN.md Task 1 (the structural precedent - shows the 4-edit surgical-insert pattern on phase-1-interview.md; Phase 9 applies the same technique to phase-2-design.md)
  </read_first>
  <action>
Apply FOUR surgical edits to `.claude/skills/agentbloc/references/phase-2-design.md`. The file is 313 lines. Make each edit by pattern-matching on the anchor text; do NOT renumber / reflow unrelated content. Preserve ALL existing sections (Steps 1-7, Design Gate, Quick Reference) unchanged except where explicitly edited below.

**Edit 1 - Extend Design Opening companion-load sentence (currently line 25):**

Find this EXACT block (lines 23-25, may be slightly different line numbers):

```
## Design Opening

You have the confirmed interview summary. Before starting design, also load [references/blast-radius.md](blast-radius.md) and [references/frameworks.md](frameworks.md). The Security Profile from the interview summary tells you which compliance regimes are active.
```

Replace with:

```
## Design Opening

You have the confirmed Business Graph. Before starting design, also load [references/blast-radius.md](blast-radius.md), [references/frameworks.md](frameworks.md), [references/orchestration-patterns.md](orchestration-patterns.md), and [references/agent-profile-schema.md](agent-profile-schema.md). The Security Profile from the Business Graph tells you which compliance regimes are active.
```

Net change: "interview summary" -> "Business Graph" (phrasing update reflecting Phase 8 ship), plus two additional companion reference links. No new paragraph.

**Edit 2 - Insert Step 8 Designer Subagent Invocation** (new H2 subsection, between end of Step 7 "Visual Presentation" content and the start of "## Design Gate").

Locate the current end of Step 7 (it ends with a paragraph about preceding each card with a plain-language summary; line ~293). The very next line is `## Design Gate`.

Insert this EXACT block BEFORE the `## Design Gate` heading, leaving a blank line before and after the new H2:

```markdown

## Step 8: Designer Subagent Invocation (DSGN-01..06, ORCH-01..04)

Once Steps 1-7 above are complete and the draft team is in your working memory, spawn the Designer Agent subagent to materialize the structured artifact.

### Invocation

Spawn the subagent defined at `.claude/agents/designer-agent.md` (`context: fork`). The subagent inherits no main-session conversation noise; its world is the Business Graph plus the schema references. Pass the following as the subagent's initial prompt context:

1. Path to Business Graph: `.agentbloc/graph/business-graph.json`
2. Required reading: [references/agent-profile-schema.md](agent-profile-schema.md), [references/orchestration-patterns.md](orchestration-patterns.md), [references/blast-radius.md](blast-radius.md), [references/frameworks.md](frameworks.md)
3. Output target: `.agentbloc/team/agent-profiles.yaml` (create `.agentbloc/team/` if missing)
4. Optional companion: `.agentbloc/team/team-topology.md` (Mermaid diagram per v1.0 Design Phase Step 7.3)
5. Scope note: Designer emits REQUESTED agents only. Anticipated agents are Phase 15 (Anticipation Engine) and excluded here.

### Output Contract

Designer returns to the main session:

- Confirmation string: "agent-profiles.yaml saved at .agentbloc/team/agent-profiles.yaml"
- A rendered markdown TABLE of the team plus per-agent Contract Cards (same templates as Step 3 and Step 7.1)
- An ASCII topology diagram (see Step 7.2 templates)

The YAML is NEVER shown to the user. The rendered table + cards + diagram ARE the user-facing review.

### Gate Check

After Designer returns, verify:

1. `.agentbloc/team/agent-profiles.yaml` exists on disk.
2. Every REQUIRED check (Checks 1-7) in [references/agent-profile-schema.md](agent-profile-schema.md) Validation Checklist passed during Designer's emission.
3. The rendered table + cards + ASCII diagram are presented to the user for confirmation.

Only after the user confirms the rendered team do you transition the Phase 2 `agent_profiles_validated` sub-gate (see SKILL.md State Transitions) to `approved`.

```

**Edit 3 - Insert Conversational Editing Flow** (new H2 subsection, between `## Design Gate` (currently ending at line 301) and `## Quick Reference` (currently line 303)).

Find the end of the `## Design Gate` paragraph (ends with "...prepare to transition to Phase 3."). The next line is `## Quick Reference`.

Insert this EXACT block BETWEEN Design Gate and Quick Reference, leaving a blank line before and after the new H2:

```markdown

## Conversational Editing Flow (DSGN-07)

After the user reviews the rendered team, they may request edits:

- "Rename gestor-cobros to Maria's agent."
- "Drop the recepcionista for now."
- "Give gestor-documental bash access."
- "Change topology from mesh to pipeline."

### Surgical Patch Protocol

For each user edit:

1. Parse the intent into a structured patch: `{rename, delete, add-tool, remove-tool, change-autonomy, change-topology, change-blast-radius}`.
2. Re-invoke Designer Agent with the patch payload AND the existing `.agentbloc/team/agent-profiles.yaml` as input. Designer NEVER regenerates from the Business Graph - regeneration would re-insert rejected or renamed agents, fighting user intent.
3. Designer applies the patch in-place, bumps `team.modified_at` to the current ISO-8601 timestamp, and re-runs the Validation Checklist from [references/agent-profile-schema.md](agent-profile-schema.md).
4. Designer returns ONLY the new rendered TABLE (not the full YAML, not all cards) for the user's next confirmation turn.
5. User confirms the new table.

### Never Regenerate

If the user says "redo the whole team from scratch", prompt once for clarification: "Starting from scratch will discard your edits (renames, drops, tool changes). Keep edits or reset?" Default to keep.

### Gate Re-entry

After every edit round, the Phase 2 `agent_profiles_validated` sub-gate returns to `pending` until the user confirms the re-rendered table. Multiple edit rounds are fine; each completes its own confirmation turn before the gate flips back to `approved`.

```

**Edit 4 - Extend Quick Reference table with 2 new rows** (currently lines 303-313).

Find the existing Quick Reference table:

```markdown
## Quick Reference

| Step | ID | What It Produces | Cross-References |
|------|----|-----------------|------------------|
| Agent Identification | DESG-01 | Agent list with roles | [frameworks.md](frameworks.md), [examples/arco-rooms.md](../examples/arco-rooms.md) |
| Topology Selection | DESG-02 | Topology type + rationale | [frameworks.md](frameworks.md) |
| Per-Agent Contracts | DESG-03 | Contract cards for each agent | [blast-radius.md](blast-radius.md), [prompt-injection.md](prompt-injection.md) |
| Schedule and Triggers | DESG-04 | Cron expressions, event triggers | [audit-logging.md](audit-logging.md) |
| Governance | DESG-05 | 7-area governance specification | [blast-radius.md](blast-radius.md), [credentials.md](credentials.md), [audit-logging.md](audit-logging.md), [incident-response.md](incident-response.md), [gdpr-patterns.md](gdpr-patterns.md) |
| Blast-Radius Scoring | DESG-06 | Level per agent + minimization | [blast-radius.md](blast-radius.md) |
| Visual Presentation | DESG-08 | Summary table, ASCII diagram, Mermaid diagram, contract cards | [blast-radius.md](blast-radius.md) |
```

Append TWO new rows to this table (after the Visual Presentation row, before the closing of the file):

```markdown
| Designer Subagent Invocation | DSGN-01..06, ORCH-01..04 | .agentbloc/team/agent-profiles.yaml + rendered team table + ASCII diagram | [agent-profile-schema.md](agent-profile-schema.md), [orchestration-patterns.md](orchestration-patterns.md), .claude/agents/designer-agent.md |
| Conversational Editing Flow | DSGN-07 | Surgical-patched YAML + re-rendered table | [agent-profile-schema.md](agent-profile-schema.md), .claude/agents/designer-agent.md |
```

**Formatting constraints for all 4 edits:**
- Preserve Steps 1-7 content unchanged (do not rewrite any existing H2 or H3 sections)
- Preserve the Design Gate section content unchanged (the new Conversational Editing Flow goes AFTER it, not inside it)
- Use shorthand relative paths for intra-references/ cross-links (`orchestration-patterns.md`, `agent-profile-schema.md`) because this file sits in references/
- Use full repo-root paths for cross-directory references (`.claude/agents/designer-agent.md`, `.agentbloc/team/agent-profiles.yaml`)
- Do NOT use em-dashes `—` anywhere (project CLAUDE.md rule)

**Net line impact:** +1 companion-reference link (Edit 1), ~35 lines new Step 8 (Edit 2), ~22 lines new Conversational Editing Flow (Edit 3), +2 Quick Reference rows (Edit 4). Total ~60 line net add. File should end between 360 and 385 lines.
  </action>
  <verify>
    <automated>grep -q "\[references/orchestration-patterns\.md\](orchestration-patterns\.md)" .claude/skills/agentbloc/references/phase-2-design.md &amp;&amp; grep -q "\[references/agent-profile-schema\.md\](agent-profile-schema.md)" .claude/skills/agentbloc/references/phase-2-design.md &amp;&amp; grep -q "^## Step 8: Designer Subagent Invocation" .claude/skills/agentbloc/references/phase-2-design.md &amp;&amp; grep -q "^## Conversational Editing Flow" .claude/skills/agentbloc/references/phase-2-design.md &amp;&amp; grep -q "\.claude/agents/designer-agent\.md" .claude/skills/agentbloc/references/phase-2-design.md &amp;&amp; grep -q "\.agentbloc/team/agent-profiles\.yaml" .claude/skills/agentbloc/references/phase-2-design.md &amp;&amp; grep -q "agent_profiles_validated" .claude/skills/agentbloc/references/phase-2-design.md &amp;&amp; grep -q "Surgical Patch Protocol" .claude/skills/agentbloc/references/phase-2-design.md &amp;&amp; grep -q "Never Regenerate\|NEVER regenerate\|never regenerate" .claude/skills/agentbloc/references/phase-2-design.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "\[references/orchestration-patterns\.md\](orchestration-patterns\.md)" .claude/skills/agentbloc/references/phase-2-design.md` matches (new companion-load link)
    - `grep -q "\[references/agent-profile-schema\.md\](agent-profile-schema.md)" .claude/skills/agentbloc/references/phase-2-design.md` matches (new companion-load link)
    - `grep -q "^## Step 8: Designer Subagent Invocation" .claude/skills/agentbloc/references/phase-2-design.md` matches (new Step 8 H2 section added)
    - `grep -q "^### Invocation$" .claude/skills/agentbloc/references/phase-2-design.md` matches (Step 8 Invocation subsection)
    - `grep -q "^### Output Contract$" .claude/skills/agentbloc/references/phase-2-design.md` matches (Step 8 Output Contract subsection)
    - `grep -q "^### Gate Check$" .claude/skills/agentbloc/references/phase-2-design.md` matches (Step 8 Gate Check subsection)
    - `grep -q "^## Conversational Editing Flow" .claude/skills/agentbloc/references/phase-2-design.md` matches (new Conversational Editing Flow H2 section)
    - `grep -q "^### Surgical Patch Protocol$" .claude/skills/agentbloc/references/phase-2-design.md` matches
    - `grep -q "\.claude/agents/designer-agent\.md" .claude/skills/agentbloc/references/phase-2-design.md` matches (Designer subagent path cited)
    - `grep -q "\.agentbloc/team/agent-profiles\.yaml" .claude/skills/agentbloc/references/phase-2-design.md` matches (output path cited)
    - `grep -q "context: fork" .claude/skills/agentbloc/references/phase-2-design.md` matches (D-21 fork context cited in Invocation)
    - `grep -q "agent_profiles_validated" .claude/skills/agentbloc/references/phase-2-design.md` matches (sub-gate named for SKILL.md integration)
    - `grep -q "NEVER regenerates\|never regenerate\|NEVER regenerate" .claude/skills/agentbloc/references/phase-2-design.md` matches (D-26 rule present)
    - `grep -q "Anticipat" .claude/skills/agentbloc/references/phase-2-design.md` matches (Phase 15 scope exclusion mentioned)
    - Quick Reference extended: `grep -q "Designer Subagent Invocation.*DSGN" .claude/skills/agentbloc/references/phase-2-design.md` AND `grep -q "Conversational Editing Flow.*DSGN-07" .claude/skills/agentbloc/references/phase-2-design.md`
    - `wc -l .claude/skills/agentbloc/references/phase-2-design.md` returns between 360 and 390
    - `grep -c "^## " .claude/skills/agentbloc/references/phase-2-design.md` returns >= 5 (existing When-This-Applies + Design-Opening + Design-Gate + Quick-Reference + 2 new sections; intermediate H2s like "## Step 1" through "## Step 7" also count - total >= 11)
    - No em-dashes: `grep -c "—" .claude/skills/agentbloc/references/phase-2-design.md` returns 0
    - Existing Steps 1-7 preserved: `grep -q "^## Step 1: Agent Identification" .claude/skills/agentbloc/references/phase-2-design.md` AND `grep -q "^## Step 7: Visual Presentation" .claude/skills/agentbloc/references/phase-2-design.md` AND `grep -q "^## Design Gate$" .claude/skills/agentbloc/references/phase-2-design.md`
  </acceptance_criteria>
  <done>
phase-2-design.md has the Design Opening companion-load sentence extended to reference orchestration-patterns.md + agent-profile-schema.md (in addition to existing blast-radius.md + frameworks.md), a new Step 8 Designer Subagent Invocation H2 section (with Invocation / Output Contract / Gate Check H3 subsections citing context:fork + the Validation Checklist + the Designer subagent path + the output path + the Phase 15 scope exclusion), a new Conversational Editing Flow H2 section (with Surgical Patch Protocol / Never Regenerate / Gate Re-entry H3 subsections explicitly stating "NEVER regenerates from the Business Graph" per D-26), and 2 new rows added to the Quick Reference table (Designer Subagent Invocation -> DSGN-01..06 + ORCH-01..04; Conversational Editing Flow -> DSGN-07). File is 360-390 lines, contains zero em-dashes, and preserves Steps 1-7 + Design Gate unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend SKILL.md - Phase 2 load list + Summary Gate wiring + State Transitions sub-gate + Phase 3 precondition</name>
  <files>.claude/skills/agentbloc/SKILL.md</files>
  <read_first>
    - .claude/skills/agentbloc/SKILL.md (the full 163-line file - specifically lines 34-40 for State Transitions, lines 98-105 for Phase 2 entry, lines 107-112 for Phase 3 entry)
    - .claude/skills/agentbloc/references/agent-profile-schema.md (the file the new Phase-2-load-list line points at; the Validation Checklist inside is what agent_profiles_validated sub-gate maps to)
    - .claude/skills/agentbloc/references/orchestration-patterns.md (the file the new Phase-2-load-list line points at)
    - .claude/agents/designer-agent.md (the file the new Summary Gate paragraph cites - verify this file exists before referencing)
    - .planning/phases/09-designer-agent/09-PATTERNS.md (lines 852-977 — the "SKILL.md (MODIFY)" section with three exact Before/After edit patterns mirroring Phase 8 Task 2 one phase later)
    - .planning/phases/09-designer-agent/09-CONTEXT.md (D-29 SKILL.md three-edit surgical pattern; "No new gate vocabulary beyond one value" - agent_profiles_validated is a sub-gate within Phase 2's approved state, NOT a new top-level Gate value)
    - .planning/phases/08-business-graph-foundation/08-02-PLAN.md Task 2 (the Phase 8 precedent - shows the three-edit SKILL.md pattern; Phase 9 applies it one phase later with Phase 2 in place of Phase 1 and Phase 3 in place of Phase 2)
  </read_first>
  <action>
Apply THREE surgical edits to `.claude/skills/agentbloc/SKILL.md`. The file is 163 lines. All edits are additive; do NOT remove existing bullets / lines / blocks. All edits strictly parallel the Phase 8 Task 2 three-edit pattern, one phase later.

**Edit 1 - Append the `Phase 2 specific:` bullet to State Transitions** (currently lines 36-40; Phase 8 just added the `Phase 1 specific:` bullet at line 40).

Find this EXACT block (after Phase 8 shipped):

```
### State Transitions

- `pending` to `approved`: User explicitly confirms ("yes", "approved", "ok", "adelante")
- `pending` to `blocked`: An issue prevents progression
- Phase number increments ONLY after current gate is `approved` AND user explicitly confirms
- Phase loopback: If new information invalidates a prior approved gate, reset that phase to `pending`. Announce: "New information affects Phase N. Returning to re-validate."
- Phase 1 specific: Gate transition to `approved` requires BOTH user confirmation of the rendered Business Graph tables AND the `business_graph_validated` sub-gate (all REQUIRED checks from [references/business-graph-schema.md](references/business-graph-schema.md) Validation Checklist have passed and the file at `.agentbloc/graph/business-graph.json` has been written).
```

Append EXACTLY ONE new bullet after the Phase-1-specific bullet (as the LAST item in the list):

```
- Phase 2 specific: Gate transition to `approved` requires BOTH user confirmation of the rendered team table and per-agent cards AND the `agent_profiles_validated` sub-gate (all REQUIRED checks from [references/agent-profile-schema.md](references/agent-profile-schema.md) Validation Checklist have passed and the file at `.agentbloc/team/agent-profiles.yaml` has been written by the Designer subagent).
```

Net: +1 bullet. Same micro-edit pattern as Phase 8 Task 2 Edit 2.

**Edit 2 - Extend Phase 2 load list + insert Summary Gate paragraph** (currently lines 98-105, after Phase 8 shipped).

Find this EXACT block:

```
### Phase 2: General Design

Translate the interview into a high-level agent team design. Identify agents (one per responsibility), map topology (pipeline, mesh, hierarchy, swarm), define contracts, schedules, and governance. Present as diagram + table.

**Precondition:** Verify `.agentbloc/graph/business-graph.json` exists and validates against the Validation Checklist in [references/business-graph-schema.md](references/business-graph-schema.md). If the file is missing or fails any REQUIRED check, return the state bar to Phase 1 with gate `pending` and re-run the Summary gate before attempting Phase 2 again.

You MUST read the complete design protocol before starting this phase:
See [references/phase-2-design.md](references/phase-2-design.md)
```

Apply TWO changes in this block:

(a) INSERT a new `**Summary Gate:**` paragraph between the existing `**Precondition:**` paragraph and the `You MUST read...` sentence. EXACT text to insert:

```
**Summary Gate:** After walking the design protocol, spawn the Designer Agent subagent at `.claude/agents/designer-agent.md` (`context: fork`) to emit `.agentbloc/team/agent-profiles.yaml`. The subagent writes silently; the rendered team table + per-agent cards + ASCII topology diagram are what the user reviews and confirms. See [references/phase-2-design.md](references/phase-2-design.md) Step 8 for the invocation protocol.
```

(b) REPLACE the existing `You MUST read...` sentence AND its single See-line with the extended version that loads both new references:

FROM:

```
You MUST read the complete design protocol before starting this phase:
See [references/phase-2-design.md](references/phase-2-design.md)
```

TO:

```
You MUST read the complete design protocol AND the orchestration patterns reference AND the agent profile schema before starting this phase:
See [references/phase-2-design.md](references/phase-2-design.md)
See [references/orchestration-patterns.md](references/orchestration-patterns.md)
See [references/agent-profile-schema.md](references/agent-profile-schema.md)
```

Final shape of the Phase 2 section becomes:

```
### Phase 2: General Design

Translate the interview into a high-level agent team design. Identify agents (one per responsibility), map topology (pipeline, mesh, hierarchy, swarm), define contracts, schedules, and governance. Present as diagram + table.

**Precondition:** Verify `.agentbloc/graph/business-graph.json` exists and validates against the Validation Checklist in [references/business-graph-schema.md](references/business-graph-schema.md). If the file is missing or fails any REQUIRED check, return the state bar to Phase 1 with gate `pending` and re-run the Summary gate before attempting Phase 2 again.

**Summary Gate:** After walking the design protocol, spawn the Designer Agent subagent at `.claude/agents/designer-agent.md` (`context: fork`) to emit `.agentbloc/team/agent-profiles.yaml`. The subagent writes silently; the rendered team table + per-agent cards + ASCII topology diagram are what the user reviews and confirms. See [references/phase-2-design.md](references/phase-2-design.md) Step 8 for the invocation protocol.

You MUST read the complete design protocol AND the orchestration patterns reference AND the agent profile schema before starting this phase:
See [references/phase-2-design.md](references/phase-2-design.md)
See [references/orchestration-patterns.md](references/orchestration-patterns.md)
See [references/agent-profile-schema.md](references/agent-profile-schema.md)
```

Net: +1 `**Summary Gate:**` paragraph, +2 See-lines, one-sentence update to the `You MUST read...` lead-in.

**Edit 3 - Insert Phase 3 precondition paragraph** (currently lines 107-112).

Find this EXACT block:

```
### Phase 3: Deep Integration Analysis

For each agent action, find the BEST integration method. Research APIs, MCP servers, npm packages, Playwright paths, email scraping, webhooks. Present options with pros/cons/setup for every service.

You MUST read the complete integration analysis protocol before starting this phase:
See [references/phase-3-integration.md](references/phase-3-integration.md)
```

INSERT a new `**Precondition:**` paragraph between the descriptive sentence (ending "...every service.") and the `You MUST read...` line:

```
**Precondition:** Verify `.agentbloc/team/agent-profiles.yaml` exists and validates against the Validation Checklist in [references/agent-profile-schema.md](references/agent-profile-schema.md). If the file is missing or fails any REQUIRED check, return the state bar to Phase 2 with gate `pending` and re-run the Summary gate before attempting Phase 3 again.
```

Final shape of the Phase 3 section becomes:

```
### Phase 3: Deep Integration Analysis

For each agent action, find the BEST integration method. Research APIs, MCP servers, npm packages, Playwright paths, email scraping, webhooks. Present options with pros/cons/setup for every service.

**Precondition:** Verify `.agentbloc/team/agent-profiles.yaml` exists and validates against the Validation Checklist in [references/agent-profile-schema.md](references/agent-profile-schema.md). If the file is missing or fails any REQUIRED check, return the state bar to Phase 2 with gate `pending` and re-run the Summary gate before attempting Phase 3 again.

You MUST read the complete integration analysis protocol before starting this phase:
See [references/phase-3-integration.md](references/phase-3-integration.md)
```

Net: +1 paragraph (+2 lines: one paragraph + one blank line). Exact parallel of Phase 8 Task 2 Edit 3.

**Formatting constraints for all 3 edits:**
- Preserve ALL other sections of SKILL.md unchanged (State Protocol, Hard Gates, Language and Technical Level, Phase 1 section, Phase 4/5/6 sections, Phase Transition Protocol, Quality Checklist, Reference Implementation)
- Total file length MUST stay under 250 lines (current 163 + ~15 net = ~178 - safe margin)
- Do NOT introduce em-dashes `—` anywhere
- Do NOT add `agent_profiles_validated` as a value in the state-bar example on line 31 (examples only show `pending` / `approved` / `blocked`) - the sub-gate is a Phase-2-specific REQUIREMENT, not a visible state-bar value
- Do NOT modify the Phase 1 section (Phase 8 already shipped its edits there)

**Net line impact:** Edit 1 = +1 line. Edit 2 = +5 lines (Summary Gate paragraph + 2 See-lines + one existing line rewritten). Edit 3 = +2 lines. Total ~+8 lines. File should end between 170 and 185 lines.
  </action>
  <verify>
    <automated>grep -q "references/orchestration-patterns\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "references/agent-profile-schema\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "agent_profiles_validated" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "\.claude/agents/designer-agent\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "\.agentbloc/team/agent-profiles\.yaml" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "AND the orchestration patterns reference AND the agent profile schema" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "Phase 2 specific:" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "\*\*Summary Gate:\*\*" .claude/skills/agentbloc/SKILL.md &amp;&amp; test $(wc -l &lt; .claude/skills/agentbloc/SKILL.md) -lt 250</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "See \[references/orchestration-patterns\.md\](references/orchestration-patterns\.md)" .claude/skills/agentbloc/SKILL.md` matches (new load-list line added for orchestration-patterns)
    - `grep -q "See \[references/agent-profile-schema\.md\](references/agent-profile-schema\.md)" .claude/skills/agentbloc/SKILL.md` matches (new load-list line added for agent-profile-schema)
    - `grep -q "AND the orchestration patterns reference AND the agent profile schema before starting this phase" .claude/skills/agentbloc/SKILL.md` matches (Phase 2 lead-in sentence updated)
    - `grep -q "agent_profiles_validated" .claude/skills/agentbloc/SKILL.md` matches (sub-gate vocabulary named in State Transitions)
    - `grep -q "Phase 2 specific:" .claude/skills/agentbloc/SKILL.md` matches (the new State Transitions bullet anchor)
    - `grep -q "\\*\\*Summary Gate:\\*\\*" .claude/skills/agentbloc/SKILL.md` matches (Summary Gate paragraph added to Phase 2)
    - `grep -q "\.claude/agents/designer-agent\.md" .claude/skills/agentbloc/SKILL.md` matches (Designer subagent path cited in Summary Gate)
    - `grep -q "context: fork" .claude/skills/agentbloc/SKILL.md` matches (D-21 fork context cited)
    - `grep -q "\.agentbloc/team/agent-profiles\.yaml" .claude/skills/agentbloc/SKILL.md` matches (canonical output path cited in Phase 3 precondition)
    - `grep -q "\\*\\*Precondition:\\*\\*" .claude/skills/agentbloc/SKILL.md` returns AT LEAST 2 matches (both Phase 2 precondition from Phase 8 AND new Phase 3 precondition)
    - `grep -c "^\\*\\*Precondition:\\*\\*\|\\*\\*Precondition:\\*\\*" .claude/skills/agentbloc/SKILL.md` returns >= 2
    - `test $(wc -l < .claude/skills/agentbloc/SKILL.md) -lt 250` exits 0 (file stays under 250 lines)
    - `test $(wc -l < .claude/skills/agentbloc/SKILL.md) -gt 170` exits 0 (file grew from 163 baseline - at least one edit landed)
    - `grep -c "^### Phase " .claude/skills/agentbloc/SKILL.md` returns 6 (6 phase sections preserved - no section added or removed)
    - Existing Phase 1 specific bullet preserved: `grep -q "Phase 1 specific:" .claude/skills/agentbloc/SKILL.md` still matches
    - Existing Phase 2 Precondition preserved: `grep -q "\.agentbloc/graph/business-graph\.json" .claude/skills/agentbloc/SKILL.md` still matches (business-graph reference from Phase 8 not removed)
    - No em-dashes introduced: `grep -c "—" .claude/skills/agentbloc/SKILL.md` returns 0
    - YAML frontmatter preserved: `head -13 .claude/skills/agentbloc/SKILL.md | grep -q "^name: agentbloc"` matches
  </acceptance_criteria>
  <done>
SKILL.md has a new "Phase 2 specific:" bullet appended to State Transitions (naming the agent_profiles_validated sub-gate as a Phase-2-specific requirement, mirroring the Phase 1 specific bullet from Phase 8). The Phase 2 section has its load-list lead-in sentence extended to "...design protocol AND the orchestration patterns reference AND the agent profile schema..." with 2 new See-lines for orchestration-patterns.md and agent-profile-schema.md, PLUS a new **Summary Gate:** paragraph between the existing Precondition and "You MUST read" block - naming `.claude/agents/designer-agent.md`, `context: fork`, and the output path. The Phase 3 section has a new **Precondition:** paragraph gating on `.agentbloc/team/agent-profiles.yaml` existence + validation (mirroring the Phase 2 precondition from Phase 8). File is 170-185 lines, stays under 250, contains zero em-dashes, and preserves all other sections unchanged. The phase count is still 6.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these phase-level checks that prove the ROADMAP Phase 9 success criteria are met end-to-end (in combination with Plans 09-01 and 09-02 artifacts):

**Success Criterion 1** - "Running Designer Agent against the Arco Rooms Business Graph produces a valid `agent-profiles.yaml` with 3 requested agents":
- `test -f .claude/agents/designer-agent.md` (Plan 09-02)
- `test -f .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml` (Plan 09-01 - the canonical target)
- `grep -q "\.claude/agents/designer-agent\.md" .claude/skills/agentbloc/SKILL.md` (Plan 09-03 wiring - this task)

**Success Criterion 2** - "The produced team YAML validates against a schema check and every workflow resolves every agent reference":
- `grep -q "Validation Checklist" .claude/skills/agentbloc/references/agent-profile-schema.md` (Plan 09-01)
- `grep -q "agent_profiles_validated" .claude/skills/agentbloc/SKILL.md` (this task - Edit 1)

**Success Criterion 3** - "Every agent profile carries role / goal / backstory / tools / triggers / autonomy / outputs / escalation / dependencies":
- `grep -q "role" .claude/skills/agentbloc/references/agent-profile-schema.md && grep -q "backstory" ...` (schema from Plan 09-01)

**Success Criterion 4** - "Designer Agent cites `references/orchestration-patterns.md` when selecting the 5 patterns":
- `grep -q "orchestration-patterns\.md" .claude/agents/designer-agent.md` (Plan 09-02)
- `grep -q "references/orchestration-patterns\.md" .claude/skills/agentbloc/SKILL.md` (this task - Edit 2 load list)
- `grep -q "orchestration-patterns\.md" .claude/skills/agentbloc/references/phase-2-design.md` (this task - Edit 1 companion-load + Edit 2 Step 8 block)

**Success Criterion 5** - "User can conversationally rename, merge, or drop agents and Designer regenerates the YAML with the edits applied (not from scratch)":
- `grep -q "Conversational Editing Flow\|Surgical Patch" .claude/skills/agentbloc/references/phase-2-design.md` (this task - Edit 3)
- `grep -q "surgical\|never regenerate\|NEVER regenerate" .claude/agents/designer-agent.md` (Plan 09-02)

All 5 ROADMAP Phase 9 success criteria now have their wiring in place.
</verification>

<success_criteria>
1. phase-2-design.md Design Opening loads 2 new companion references (orchestration-patterns.md, agent-profile-schema.md) in addition to blast-radius.md + frameworks.md.
2. phase-2-design.md has a new Step 8 Designer Subagent Invocation H2 section with Invocation / Output Contract / Gate Check subsections citing context:fork + Validation Checklist + output path + Phase 15 exclusion.
3. phase-2-design.md has a new Conversational Editing Flow H2 section with Surgical Patch Protocol / Never Regenerate / Gate Re-entry subsections explicitly stating the D-26 "never regenerate from Business Graph" rule.
4. phase-2-design.md Quick Reference has 2 new rows for the Designer Subagent Invocation and Conversational Editing Flow.
5. SKILL.md State Transitions has a new Phase-2-specific bullet naming the agent_profiles_validated sub-gate, paralleling the Phase-1-specific bullet from Phase 8.
6. SKILL.md Phase 2 section has its load list extended (2 new See-lines for orchestration-patterns.md + agent-profile-schema.md) AND a new Summary Gate paragraph spawning the Designer subagent.
7. SKILL.md Phase 3 section has a new Precondition paragraph gating on `.agentbloc/team/agent-profiles.yaml` existence + validation.
8. SKILL.md stays under 250 lines; phase-2-design.md ends between 360-390 lines.
9. No em-dashes introduced in either file beyond the pre-edit baseline count.
10. All 5 ROADMAP Phase 9 success criteria have their wiring in place (verified by the verification block above).
</success_criteria>

<output>
After completion, create `.planning/phases/09-designer-agent/09-03-SUMMARY.md` documenting:
- Exact Before/After diffs for each of the 4 phase-2-design.md edits and 3 SKILL.md edits
- Final line counts of both files (before and after)
- Confirmation that all 5 ROADMAP Phase 9 success criteria are wired end-to-end (by this plan in combination with Plans 09-01 and 09-02)
- Any deviations from the plan with rationale
- Handoff note for Phase 10 (Integration Discovery - MCP Path): "Phase 2 Design now emits `.agentbloc/team/agent-profiles.yaml` with shape locked in `references/agent-profile-schema.md`. Phase 10's Integration Discovery reads this file's `agents[].tools[]` arrays to know which integrations to search for. The subagent at `.claude/agents/designer-agent.md` is the invocation target. Phase 3 cannot proceed without a validated agent-profiles.yaml."
</output>
