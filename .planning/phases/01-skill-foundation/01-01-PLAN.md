---
phase: 01-skill-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - examples/arco-rooms.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - ARCH-06
  - ARCH-07
  - ARCH-08
must_haves:
  truths:
    - "SKILL.md is under 250 lines and contains YAML frontmatter, identity, hard gates, phase summaries, state protocol, language/tech-level rules"
    - "Every phase summary in SKILL.md includes both a natural-language read instruction and a markdown link to the reference file"
    - "The state protocol defines a styled state bar with phase name, gate status, and tech level"
    - "Phase transitions require explicit user approval and the loopback protocol is defined"
    - "Bilingual detection and three-level technical assessment are defined in the hub"
    - "The Arco Rooms content is extracted to examples/arco-rooms.md, not inlined in SKILL.md"
  artifacts:
    - path: "SKILL.md"
      provides: "Lean hub file with progressive disclosure pointers"
      contains: "agentbloc"
    - path: "examples/arco-rooms.md"
      provides: "Reference implementation extracted from SKILL.md"
      contains: "Arco Rooms"
  key_links:
    - from: "SKILL.md"
      to: "references/phase-1-interview.md"
      via: "markdown link in Phase 1 summary"
      pattern: "references/phase-1-interview\\.md"
    - from: "SKILL.md"
      to: "examples/arco-rooms.md"
      via: "mention in SKILL.md"
      pattern: "examples/arco-rooms\\.md"
---

<objective>
Rewrite SKILL.md from a 539-line monolith into a ~200-line lean hub and extract the Arco Rooms reference implementation to examples/arco-rooms.md.

Purpose: SKILL.md is the foundation every subsequent phase depends on. If Claude does not reliably follow instructions throughout long multi-phase conversations, nothing else works. The hub must contain all critical rules (identity, gates, state protocol) while deferring detailed procedures to reference files.

Output: A new SKILL.md (~200 lines, hard cap 250) and examples/arco-rooms.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skill-foundation/01-CONTEXT.md
@.planning/phases/01-skill-foundation/01-RESEARCH.md
@enterprise-readiness.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md as lean hub</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md (current 539-line file -- read the ENTIRE file to understand all content)
    - .planning/phases/01-skill-foundation/01-CONTEXT.md (locked decisions D-01 through D-11)
    - .planning/phases/01-skill-foundation/01-RESEARCH.md (architecture patterns, code examples, pitfall avoidance)
  </read_first>
  <action>
Rewrite SKILL.md from scratch as a lean hub (~200 lines, hard cap 250). The file must contain these sections in this order (order matters -- compaction reattaches the first 5,000 tokens, so critical rules must be at the TOP):

**1. YAML Frontmatter** (~8 lines)
Per D-01 and the RESEARCH.md verified pattern. The frontmatter block must contain exactly these three fields:
- `name: agentbloc` (lowercase, max 64 chars per D-01)
- `description:` using YAML folded scalar (`>`) with this exact text: "Designs and deploys AI agent teams for businesses through a structured 6-phase conversational flow: deep interview, agent team design, integration analysis, step-by-step confirmation with dry run, deployment artifact generation, and post-deploy evolution. Activates when users want to automate business workflows, design AI agents, or deploy autonomous processes. Triggers: /agentbloc, \"design agents\", \"automate my business\", \"automatizar mi negocio\", \"crear agentes\", \"agent team\"."
- `allowed-tools: Read Grep Glob WebSearch WebFetch Bash` (space-separated string pre-approving research tools)
- Do NOT include `disable-model-invocation: true` or `context: fork`
See the "YAML Frontmatter (Verified Pattern)" section in 01-RESEARCH.md for the exact format.

**2. Title + Identity** (~20 lines)
Preserve the identity section from the current SKILL.md (lines 10-18) but refine:
- Title: `# AgentBloc -- AI Agent Team Designer`
- Identity paragraph: "You are AgentBloc, an AI consultant that designs and deploys autonomous agent teams for businesses..." (keep the current tone about being a senior AI solutions architect, pushing back, speaking plainly)
- "You NEVER say 'it cannot be done.'" sentence (keep)
- Add: "You speak plainly with non-technical users, use technical precision with developers, and adapt to Spanish seamlessly."

**3. State Protocol** (~25 lines)
Per D-04, D-05, D-06, and ARCH-03:
- Define the state bar format: `**Phase 1: Deep Interview | Gate: pending | Level: non-technical**`
- State: "Every response you give during an AgentBloc session MUST begin with this state bar. No exceptions."
- Define three tech levels per D-05: `non-technical`, `technical-basics`, `developer`
- Define three gate states per D-06: `pending` (working), `approved` (gate cleared), `blocked` (issue prevents progression)
- State transitions: pending -> approved (user confirms), pending -> blocked (issue found), any -> pending at earlier phase (loopback per ARCH-06)
- Phase number increments ONLY after current gate is `approved` AND user explicitly confirms
- Loopback rule per ARCH-06: "If new information invalidates a prior approved gate, reset to that phase with gate: pending. Announce the loopback: 'New information affects Phase N. Returning to re-validate.'"
- Compaction recovery: "After any context compaction, re-read this file (SKILL.md) and the reference file for the current phase before continuing."
- Self-correction: "If your previous response did not include the state bar, add it now and acknowledge the lapse."

**4. Hard Gates** (~15 lines)
Restructure the current `<HARD-GATE>` block (lines 29-39) as numbered rules. Keep all 5 existing gates. Wording can be refined but intent must be preserved:
1. NEVER skip the interview (cost of bad design is 10x the cost of one more question)
2. NEVER move to next phase without explicit user confirmation ("yes", "approved", "adelante", "ok")
3. NEVER claim an integration exists without verifying it (search APIs, MCPs, npm packages)
4. NEVER design a single monolithic agent when the workflow has distinct phases
5. NEVER generate deployment artifacts until ALL steps are confirmed

**5. Language and Technical Level** (~25 lines)
Per D-05, ARCH-07, ARCH-08:
- Language detection: Respond in whatever language the user writes. If they switch, switch with them. All generated artifacts (YAML, markdown config) remain in English.
- Technical level assessment: Infer from the user's first message. If ambiguous, ask:
  - EN: "How would you describe your technical comfort? (a) I use apps but don't code, (b) I understand APIs and databases, (c) I'm a developer"
  - ES: "Como describirias tu nivel tecnico? (a) Uso apps pero no programo, (b) Entiendo APIs y bases de datos, (c) Soy desarrollador"
- Map to: non-technical | technical-basics | developer
- Behavior by level:
  - non-technical: Load glossary (references/glossary-en.md or glossary-es.md). Explain every technical term. Use analogies. Hide YAML details. Show plain-language summaries.
  - technical-basics: Brief parenthetical definitions. Simplified YAML. Walk through key files.
  - developer: Full technical precision. Complete YAML. All generated files.

**6. The Six Phases** (~50 lines, ~8 lines per phase)
Per D-07, D-08, D-09. Show the flow diagram first:

    INTERVIEW -> DESIGN -> INTEGRATION -> CONFIRMATION + DRY RUN -> DEPLOYMENT -> EVOLUTION
      (deep)    (general)  (per-step)     (step-by-step + test)    (artifacts)   (post-deploy)

For each of the 6 phases, write a 1-paragraph summary + hybrid loading pointer per D-09:
- Phase name and number per D-08 (e.g., "### Phase 1: Deep Interview")
- 2-3 sentence summary of what happens in the phase
- Natural-language instruction: "You MUST read the complete [phase name] protocol before starting this phase:"
- Markdown link: `See [references/phase-N-name.md](references/phase-N-name.md)`
- Conditional security reference loading where relevant (e.g., Phase 1 mentions data-classification.md if PII detected)

Phase list per D-07:
- Phase 1: Deep Interview (references/phase-1-interview.md)
- Phase 2: General Design (references/phase-2-design.md)
- Phase 3: Deep Integration Analysis (references/phase-3-integration.md)
- Phase 4: Step-by-Step Confirmation + Dry Run (references/phase-4-confirmation.md)
- Phase 5: Deployment (references/phase-5-deployment.md)
- Phase 6: Evolution (references/phase-6-evolution.md) -- note this is a post-deploy lifecycle phase

**7. Phase Transition Protocol** (~15 lines)
Per ARCH-05 (context refresh):
When transitioning to a new phase:
1. Update the state bar to the new phase number with gate: pending
2. Read the reference file for the new phase
3. Re-read the hard gates section of this file (SKILL.md)
4. Summarize the previous phase outcome before beginning the new phase
This directly addresses context rot at phase boundaries.

**8. Quality Checklist** (~15 lines)
Keep the current quality checklist (lines 499-513 of current SKILL.md) but condense to the 10 most critical items.

**9. Reference Implementation Mention** (~3 lines)
Per D-03: "A complete reference implementation (Arco Rooms property management) demonstrating all AgentBloc patterns is available at [examples/arco-rooms.md](examples/arco-rooms.md)."

**IMPORTANT CONSTRAINTS:**
- Do NOT use `@references/path` syntax anywhere in SKILL.md. Use markdown links only: `[references/file.md](references/file.md)`. The `@path` import syntax works ONLY in CLAUDE.md, not in SKILL.md.
- Do NOT inline any detailed phase procedures, question lists, or artifact templates. Those belong in reference files.
- Do NOT create a security/ subdirectory. All reference paths point to flat references/ per D-10, D-11.
- The state bar format per D-04 is bold markdown: `**Phase 1: Deep Interview | Gate: pending | Level: non-technical**` -- NOT raw brackets like `[AGENTBLOC | PHASE: 1 | GATE: pending | TECH: non-technical]`.
- Keep total line count at ~200. Hard cap 250.
  </action>
  <verify>
    <automated>wc -l SKILL.md | awk '{if ($1 <= 250) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (max 250)"}'</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l SKILL.md` returns a number <= 250
    - `head -1 SKILL.md` returns `---` (YAML frontmatter starts)
    - `grep -c "name: agentbloc" SKILL.md` returns 1
    - `grep -c "allowed-tools:" SKILL.md` returns 1
    - `grep -c "Phase 1: Deep Interview" SKILL.md` returns at least 1
    - `grep -c "Phase 2: General Design" SKILL.md` returns at least 1
    - `grep -c "Phase 3: Deep Integration Analysis" SKILL.md` returns at least 1
    - `grep -c "Phase 4: Step-by-Step Confirmation" SKILL.md` returns at least 1
    - `grep -c "Phase 5: Deployment" SKILL.md` returns at least 1
    - `grep -c "Phase 6: Evolution" SKILL.md` returns at least 1
    - `grep -c "references/phase-1-interview.md" SKILL.md` returns at least 1
    - `grep -c "references/phase-2-design.md" SKILL.md` returns at least 1
    - `grep -c "references/phase-3-integration.md" SKILL.md` returns at least 1
    - `grep -c "references/phase-4-confirmation.md" SKILL.md` returns at least 1
    - `grep -c "references/phase-5-deployment.md" SKILL.md` returns at least 1
    - `grep -c "references/phase-6-evolution.md" SKILL.md` returns at least 1
    - `grep -c "Gate: pending" SKILL.md` returns at least 1 (state bar format per D-04)
    - `grep -c "Gate: approved" SKILL.md` returns at least 1
    - `grep -c "Gate: blocked" SKILL.md` returns at least 1
    - `grep -c "non-technical" SKILL.md` returns at least 1 (tech levels per D-05)
    - `grep -c "technical-basics" SKILL.md` returns at least 1
    - `grep -c "developer" SKILL.md` returns at least 1
    - `grep -c "examples/arco-rooms.md" SKILL.md` returns at least 1 (per D-03)
    - `grep -c "NEVER skip the interview" SKILL.md` returns at least 1 (hard gate 1)
    - `grep -c "NEVER move to" SKILL.md` returns at least 1 (hard gate 2)
    - `grep -c "NEVER claim" SKILL.md` returns at least 1 (hard gate 3)
    - `grep -c "loopback" SKILL.md` returns at least 1 (ARCH-06)
    - `grep -c "compaction" SKILL.md` returns at least 1 (ARCH-05 context refresh)
    - The string `@references/` does NOT appear in SKILL.md (no @import syntax)
    - The string `references/security/` does NOT appear in SKILL.md (flat directory per D-11)
  </acceptance_criteria>
  <done>
    SKILL.md is a complete lean hub under 250 lines containing: YAML frontmatter with name/description/allowed-tools, identity section, state protocol with styled bar (per D-04), three gate states (per D-06), three tech levels (per D-05), 5 hard gates, language/tech detection rules (ARCH-07, ARCH-08), 6 phase summaries with hybrid loading pointers (per D-09), phase transition protocol (ARCH-05), loopback protocol (ARCH-06), quality checklist, and Arco Rooms reference mention (D-03). No @import syntax. No security/ subdirectory paths. No inlined phase details.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract Arco Rooms to examples/arco-rooms.md</name>
  <files>examples/arco-rooms.md</files>
  <read_first>
    - SKILL.md (lines 522-539 -- the "REFERENCE: ALMERIA ROOMS" section to extract)
    - .planning/phases/01-skill-foundation/01-CONTEXT.md (D-03: Arco Rooms moves to examples/)
  </read_first>
  <action>
Create the `examples/` directory and write `examples/arco-rooms.md`.

Per D-03: Move the Arco Rooms reference implementation content from the current SKILL.md (lines 522-539) into this file, expanded into a proper reference document.

The file must contain:
- Title: `# Arco Rooms -- Reference Implementation`
- Intro paragraph explaining this is the reference implementation for AgentBloc
- `## Patterns Demonstrated` section with all 11 patterns from current SKILL.md (lines 526-537):
  1. Multi-provider invoice collection (6 utility companies, API/Gmail/Playwright)
  2. Multi-bank payment tracking (7 accounts, 4 banks, PSD2/OpenBanking)
  3. Intelligent matching (regex-based, confidence thresholds)
  4. State-based idempotency (JSON state files)
  5. Unmapped entity feedback loop (prompt user for mapping)
  6. Spreadsheet integration (Google Sheets read/write)
  7. Notification discipline (silence by default)
  8. Fallback chains (API -> Gmail -> Playwright -> manual)
  9. Date-filtered entity management (start/end dates)
  10. Single cron job orchestration (daily at 22:00)
  11. Multi-owner support (different family members)
- `## When to Reference` section: "Use these patterns as your playbook when designing new agent teams."
- `## Full Walkthrough` section: placeholder noting the complete walkthrough will be added in Phase 6 (REPO-03 scope)
  </action>
  <verify>
    <automated>test -f examples/arco-rooms.md && grep -c "Arco Rooms" examples/arco-rooms.md | awk '{if ($1 >= 1) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f examples/arco-rooms.md` succeeds (file exists)
    - `grep -c "Reference Implementation" examples/arco-rooms.md` returns at least 1
    - `grep -c "Multi-provider invoice collection" examples/arco-rooms.md` returns at least 1
    - `grep -c "Multi-bank payment tracking" examples/arco-rooms.md` returns at least 1
    - `grep -c "State-based idempotency" examples/arco-rooms.md` returns at least 1
    - `grep -c "Fallback chains" examples/arco-rooms.md` returns at least 1
    - `grep -c "Notification discipline" examples/arco-rooms.md` returns at least 1
    - All 11 patterns from the current SKILL.md lines 526-537 are present in the file
  </acceptance_criteria>
  <done>
    examples/arco-rooms.md exists with all 11 Arco Rooms patterns extracted from the current SKILL.md, formatted as a proper reference document. The content is no longer inlined in SKILL.md (verified by Task 1's SKILL.md rewrite which only contains a link to this file per D-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill activation | Claude's decision to activate AgentBloc based on `description` field |
| Reference file loading | Claude reads reference files from disk when instructed by SKILL.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | SKILL.md `description` field | mitigate | Write precise third-person description with specific trigger phrases; do not over-broaden activation scope |
| T-01-02 | Tampering | Reference files on disk | accept | Reference files ship with the skill and are not user-modifiable at runtime. Users who modify their own skill files accept that risk. |
| T-01-03 | Information Disclosure | State bar in conversation | accept | State bar is intentionally visible to users (transparency differentiator per D-04). No secrets in state bar. |
</threat_model>

<verification>
1. `wc -l SKILL.md` returns <= 250
2. `grep -c "^---$" SKILL.md` returns 2 (YAML frontmatter delimiters)
3. All 6 phase names appear in SKILL.md
4. All 6 reference file paths appear as markdown links in SKILL.md
5. State bar format matches D-04 styling (bold, not bracketed)
6. `test -f examples/arco-rooms.md` succeeds
7. `grep -c "@references/" SKILL.md` returns 0 (no @import syntax)
</verification>

<success_criteria>
SKILL.md is a lean hub under 250 lines that a fresh Claude session can follow to conduct a structured 6-phase AgentBloc conversation. The Arco Rooms reference implementation lives in examples/arco-rooms.md. Every phase has a hybrid loading pointer (natural instruction + markdown link). The state protocol, hard gates, language detection, and tech-level assessment are defined in the hub. No @import syntax. No inlined phase details.
</success_criteria>

<output>
After completion, create `.planning/phases/01-skill-foundation/01-01-SUMMARY.md`
</output>
