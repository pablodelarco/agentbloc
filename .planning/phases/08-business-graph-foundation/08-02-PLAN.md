---
phase: 08-business-graph-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - .claude/skills/agentbloc/references/phase-1-interview.md
  - .claude/skills/agentbloc/SKILL.md
autonomous: true
requirements:
  - INTV-01
  - INTV-02
  - INTV-03
  - INTV-04

must_haves:
  truths:
    - "The v1.0 9-category interview extends to capture decision_patterns via a new Category 7 seed question — without adding a 10th category (preserves v1.0 D-01 and D-04 soft framing)"
    - "The Summary of Understanding Template renders Business Graph sections (tools_available, channels, decision_patterns) as tables the user confirms before Claude emits the JSON silently"
    - "Phase 1 cannot transition to Phase 2 until a valid Business Graph JSON exists at .agentbloc/graph/business-graph.json (gate enforced by SKILL.md precondition)"
    - "SKILL.md loads business-graph-schema.md unconditionally alongside phase-1-interview.md and data-classification.md at Phase 1 entry (hybrid loading pattern per v1.0 D-09)"
    - "The state bar gains the business_graph_validated sub-gate vocabulary for Phase 1 without introducing a new top-level gate value"
    - "The rendered table review and JSON emission are separate — user sees tables, file gets JSON (D-14 silent emission)"
  artifacts:
    - path: ".claude/skills/agentbloc/references/phase-1-interview.md"
      provides: "Extended interview protocol — Category 7 has a decision_patterns seed question; Summary template has tools_available/channels/decision_patterns tables; Summary template has a Business Graph Emission subsection invoking the Validation Checklist from business-graph-schema.md"
      min_lines: 380
      contains: "Business Graph Emission"
    - path: ".claude/skills/agentbloc/SKILL.md"
      provides: "Phase 1 entry loads business-graph-schema.md unconditionally; Phase 2 entry has a precondition check; State Transitions section documents the business_graph_validated sub-gate"
      contains: "business-graph-schema.md"
  key_links:
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/business-graph-schema.md"
      via: "unconditional load at Phase 1 entry alongside phase-1-interview.md + data-classification.md"
      pattern: "business-graph-schema\\.md"
    - from: ".claude/skills/agentbloc/references/phase-1-interview.md"
      to: ".claude/skills/agentbloc/references/business-graph-schema.md"
      via: "Summary of Understanding Template cross-references the Validation Checklist and Emission Protocol"
      pattern: "business-graph-schema\\.md"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".agentbloc/graph/business-graph.json"
      via: "Phase 2 precondition verifies the file exists and validates before transition"
      pattern: "business-graph\\.json"
---

<objective>
Wire the Business Graph schema (created in Plan 08-01) into the v1.0 interview flow: extend Category 7 with a single seed question that captures decision_patterns, extend the Summary of Understanding Template with rendered tables for tools_available / channels / decision_patterns plus a Business Graph Emission subsection, and extend SKILL.md to (a) load business-graph-schema.md unconditionally at Phase 1 entry, (b) add a business_graph_validated sub-gate to the State Transitions vocabulary, and (c) gate the Phase 2 transition on the graph file existing and validating.

Purpose: Success Criterion 4 of ROADMAP Phase 8 says "the interview conversation remains bilingual (EN/ES), non-technical, and flows as in v1.0 — the graph emission is a side effect the user doesn't have to think about." This plan preserves the v1.0 conversational UX while making the JSON emission mandatory for Phase 2 transition. No new categories, no new gate top-level values, no breaking changes to existing v1.0 tests.

Output: Three file surgical edits — two surgical insertions in phase-1-interview.md (Category 7 + Summary Template), three surgical edits in SKILL.md (Phase 1 load list, State Transitions, Phase 2 precondition). Total net addition ~50-80 lines across both files. SKILL.md stays under 250 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-business-graph-foundation/08-CONTEXT.md
@.planning/phases/08-business-graph-foundation/08-PATTERNS.md
@.planning/phases/08-business-graph-foundation/08-01-SUMMARY.md

@.claude/skills/agentbloc/references/business-graph-schema.md
@.claude/skills/agentbloc/references/phase-1-interview.md
@.claude/skills/agentbloc/references/data-classification.md
@.claude/skills/agentbloc/SKILL.md
@.claude/skills/agentbloc/examples/arco-rooms-business-graph.json

<interfaces>
<!-- Extension points in phase-1-interview.md (current line ranges, file is 350 lines) -->
<!-- Category 7: lines 199-218 — seed questions at 201-205, must-know checklist at 207-210, adaptive branching at 213-216, Data Classification Scan footer at 218 -->
<!-- Summary of Understanding Template: lines 281-333 — existing tables at 293-316, confirmation prompt at 333 -->

<!-- Extension points in SKILL.md (current file is 159 lines) -->
<!-- State Transitions: lines 34-40 (section headers + 4 bullets) -->
<!-- Phase 1 entry: lines 88-95 (intro paragraph + "You MUST read..." block pointing at phase-1-interview.md + data-classification.md) -->
<!-- Phase 2 entry: lines 96-102 (intro paragraph + "You MUST read..." block pointing at phase-2-design.md) -->

<!-- Seed question format in phase-1-interview.md (lines 71-74, 201-205, etc.) -->
<!-- All seed questions are numbered single-sentence double-quoted strings:
     1. "Question text here?"
     2. "Another question?"
-->

<!-- Summary template table format (lines 293-316) -->
<!-- 3-6 columns, square-bracket [placeholder] example row, H3 heading -->

<!-- Gate value extension rule from CONTEXT.md specifics -->
<!-- "No new gate vocabulary beyond one value" → add business_graph_validated as a SUB-gate within Phase 1's approved state, NOT a new top-level Gate value -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend phase-1-interview.md — Category 7 seed question + Summary Template Business Graph sections</name>
  <files>.claude/skills/agentbloc/references/phase-1-interview.md</files>
  <read_first>
    - .claude/skills/agentbloc/references/phase-1-interview.md (the full 350-line file — especially lines 199-218 for Category 7 and lines 281-333 for Summary of Understanding Template)
    - .claude/skills/agentbloc/references/business-graph-schema.md (the schema + Validation Checklist created in Plan 08-01 — the Summary extension will invoke this file's Emission Protocol)
    - .planning/phases/08-business-graph-foundation/08-PATTERNS.md (lines 254-339 — the "phase-1-interview.md (MODIFY)" section with concrete seed question text, adaptive branching bullet, Summary table format, and Business Graph Emission subsection template)
    - .planning/phases/08-business-graph-foundation/08-CONTEXT.md (D-11 silent emission, D-14 rendered table review, D-16 Category 7 seed question, D-17 tools_available/channels extraction from existing Categories 3+8, D-19 re-run behavior, D-20 pain is free-text)
    - .planning/phases/08-business-graph-foundation/08-01-SUMMARY.md (the schema contract — specifically which fields are REQUIRED vs RECOMMENDED vs OPTIONAL so the emission step correctly maps interview answers to JSON fields)
  </read_first>
  <action>
Apply FOUR surgical edits to `.claude/skills/agentbloc/references/phase-1-interview.md`. The file is 350 lines. Make each edit by pattern-matching on the anchor text, do not renumber / reflow unrelated content.

**Edit 1 — Add D-16 seed question #3 to Category 7 Seed Questions (currently lines 201-205):**

Find the exact block:
```
### Seed Questions

1. "What goes wrong most often? What are the common exceptions or edge cases?"
2. "When something fails, what do you do today? Is there a fallback process?"
```
Inside Category 7 (the block starting `## Category 7: Edge Cases and Failures`).

Insert a THIRD numbered seed question as the last item, exact text:
```
3. "What rules do you apply when deciding how to handle these edge cases? For example: if an invoice is overdue by more than 7 days, what do you do?"
```

**Edit 2 — Add D-16 must-know checkbox to Category 7 Must-Know Checklist (currently lines 207-210):**

Find the exact block inside Category 7:
```
### Must-Know Checklist

- [ ] Top 3 failure modes identified
- [ ] Current fallback process documented
- [ ] Acceptable failure rate discussed (zero tolerance vs. occasional misses ok)
```

Append a fourth checkbox as the last item:
```
- [ ] Decision rules captured for at least the top edge case (feeds decision_patterns in the Business Graph)
```

**Edit 3 — Add a rule-capture adaptive-branching bullet to Category 7 (currently lines 213-216):**

Find the exact block inside Category 7:
```
### Adaptive Branching

- If user says "nothing goes wrong," push back gently: "Every workflow has edge cases. What happens when data is missing, a tool is down, or someone doesn't respond on time?"
- If user describes catastrophic failures, probe for prevention: "Has this happened before? What was the impact, and what would have prevented it?"
- If failure modes involve data corruption or loss, flag for Design phase blast-radius scoring.
```

Append a fourth bullet as the last item:
```
- If user describes a rule or threshold, capture it verbatim for decision_patterns: "Let me write that down: [paraphrase the rule]. Any other rules like that one you apply in this workflow?"
```

**Edit 4 — Extend Summary of Understanding Template with three new table sections AND a Business Graph Emission subsection.**

Locate the existing Summary of Understanding Template (currently starts at line 281 with `## Summary of Understanding Template (INTV-04, D-10)`). The existing template has these H3 subsections in order: Your Business, The Problem, Current Workflow, Services and Integrations (table), Data Model (table), People Involved (table), Edge Cases (table), Reporting Requirements (table), Budget and Constraints (prose), Security Profile (table), confirmation prompt on line 333.

**Edit 4a — Insert 3 new H3 subsections** between "### Services and Integrations" (line 293) and "### Data Model" (line 298). The new order becomes: Services and Integrations -> **Tools Available** -> **Channels** -> **Decision Patterns** -> Data Model. Insert this exact block after the Services and Integrations table (after its placeholder row `| [Tool name] | [admin/API/web-only] | [API/MCP/Playwright/email] |`):

```markdown

### Tools Available (Business Graph `tools_available`)
| Tool | Already in Use | Purpose |
|------|----------------|---------|
| [Tool name] | [yes / evaluating] | [what the user uses it for today] |

Extract from Category 3 (Services and Tools). If Category 3 captured no specific tools, ask one clarifying question before emitting: "I did not capture specific tools you already use. Anything come to mind: a CRM, spreadsheet, calendar, accounting software?"

### Channels (Business Graph `channels`)
| Channel | Used For |
|---------|----------|
| [telegram / email / web / slack / other] | [notifications / approvals / reports / other] |

Extract from Category 8 (Reporting and Communication). Non-exhaustive list: telegram, email, slack, web, sms.

### Decision Patterns (Business Graph `decision_patterns`)
| Rule | Source Category |
|------|----------------|
| [Verbatim rule the user described, e.g. "If an invoice is overdue by more than 7 days, send a formal notice."] | [Category 7 edge-case-rule capture, or elsewhere] |

These feed the `decision_patterns` array in the Business Graph. Capture verbatim. Phase 9 Designer Agent uses natural-language understanding to classify these; do not pre-structure them here (D-20 applies to this field as well).
```

**Edit 4b — Insert a Business Graph Emission subsection** IMMEDIATELY BEFORE the final confirmation prompt (currently line 333: `**Does this accurately capture your workflow? I need your confirmation before proceeding to the design phase.**`).

Insert this exact block above that confirmation prompt:

```markdown

### Business Graph Emission (D-11, D-14, INTV-01)

Once the user confirms the rendered tables above:

1. Apply the Validation Checklist from [references/business-graph-schema.md](business-graph-schema.md) in order (Check 1 through Check 6).
2. For any failed REQUIRED check (Checks 1-5), ask the targeted conversational follow-up question specified in that check and wait for the user's answer before resuming.
3. Once all REQUIRED checks pass, write the validated JSON silently to `.agentbloc/graph/business-graph.json`. Create the `.agentbloc/graph/` directory if it does not exist.
4. Confirm emission in one sentence: "Business Graph saved. Ready to move to the design phase."
5. Set the Phase 1 `business_graph_validated` sub-gate to `approved` (see SKILL.md State Transitions).

If `.agentbloc/graph/business-graph.json` already exists, follow the Re-run Behavior in [references/business-graph-schema.md](business-graph-schema.md): ask keep / overwrite / merge (default merge).

The JSON is NEVER shown to the user. The rendered tables above ARE the user-facing review. Edits are conversational ("owner should be Maria, not Pablo"); after any edit, re-run the Validation Checklist and re-emit.
```

**Formatting constraints for all 4 edits:**
- Do not renumber existing seed questions / must-know items / adaptive branching bullets
- Do not remove or re-order any existing Summary table
- Preserve the Quick Reference table at the end of the file (lines 335-349) exactly — do NOT update its `2` count for Category 7 seed questions to `3` unless the reader requests (the quick-reference table shows "seed questions" count; the D-16 addition moves Category 7 from 2 to 3, and the Total from 20 to 21 — DO update these two values in the Quick Reference for accuracy)
- Do NOT introduce em-dashes (—) anywhere (project CLAUDE.md rule)

**Net line impact:** +1 seed question, +1 must-know checkbox, +1 adaptive branching bullet, +3 new H3 Summary subsections (~20 lines), +1 Business Graph Emission subsection (~16 lines), +1 Quick Reference numeric update (2 values). Total ~40-45 line net add. File should end between 388 and 400 lines.
  </action>
  <verify>
    <automated>grep -q "What rules do you apply when deciding how to handle these edge cases" .claude/skills/agentbloc/references/phase-1-interview.md &amp;&amp; grep -q "Decision rules captured for at least the top edge case" .claude/skills/agentbloc/references/phase-1-interview.md &amp;&amp; grep -q "### Tools Available (Business Graph" .claude/skills/agentbloc/references/phase-1-interview.md &amp;&amp; grep -q "### Channels (Business Graph" .claude/skills/agentbloc/references/phase-1-interview.md &amp;&amp; grep -q "### Decision Patterns (Business Graph" .claude/skills/agentbloc/references/phase-1-interview.md &amp;&amp; grep -q "### Business Graph Emission" .claude/skills/agentbloc/references/phase-1-interview.md &amp;&amp; grep -q "business-graph-schema\.md" .claude/skills/agentbloc/references/phase-1-interview.md &amp;&amp; grep -q "\.agentbloc/graph/business-graph\.json" .claude/skills/agentbloc/references/phase-1-interview.md &amp;&amp; grep -q "business_graph_validated" .claude/skills/agentbloc/references/phase-1-interview.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "What rules do you apply when deciding how to handle these edge cases" .claude/skills/agentbloc/references/phase-1-interview.md` matches (D-16 seed question added verbatim)
    - `grep -q "Decision rules captured for at least the top edge case" .claude/skills/agentbloc/references/phase-1-interview.md` matches (D-16 must-know checkbox added)
    - `grep -q "If user describes a rule or threshold, capture it verbatim for decision_patterns" .claude/skills/agentbloc/references/phase-1-interview.md` matches (adaptive branching bullet added)
    - `grep -q "### Tools Available (Business Graph" .claude/skills/agentbloc/references/phase-1-interview.md` matches (new H3 subsection added)
    - `grep -q "### Channels (Business Graph" .claude/skills/agentbloc/references/phase-1-interview.md` matches (new H3 subsection added)
    - `grep -q "### Decision Patterns (Business Graph" .claude/skills/agentbloc/references/phase-1-interview.md` matches (new H3 subsection added)
    - `grep -q "### Business Graph Emission" .claude/skills/agentbloc/references/phase-1-interview.md` matches (new emission subsection added)
    - `grep -q "\.agentbloc/graph/business-graph\.json" .claude/skills/agentbloc/references/phase-1-interview.md` matches (canonical output path cited)
    - `grep -q "\[references/business-graph-schema\.md\](business-graph-schema\.md)" .claude/skills/agentbloc/references/phase-1-interview.md` matches (at least one cross-link to the schema file using shorthand relative path)
    - `grep -q "business_graph_validated" .claude/skills/agentbloc/references/phase-1-interview.md` matches (sub-gate named for SKILL.md integration)
    - `wc -l .claude/skills/agentbloc/references/phase-1-interview.md` returns between 380 and 410
    - `grep -c "## Category " .claude/skills/agentbloc/references/phase-1-interview.md` returns exactly 9 (no 10th category added — D-01 honored)
    - Diff check: `diff <(grep -c "^### Seed Questions" .claude/skills/agentbloc/references/phase-1-interview.md) <(echo 9)` (still 9 Seed Questions H3 headers, one per category)
    - Em-dash check: the new inserted text contains no `—` character. Running `grep -c "—" .claude/skills/agentbloc/references/phase-1-interview.md` returns same count as before the edit (unchanged from original baseline — no new em-dashes introduced)
  </acceptance_criteria>
  <done>
phase-1-interview.md has exactly one new seed question in Category 7 (D-16), one new must-know checkbox in Category 7, one new adaptive-branching bullet in Category 7, three new H3 table subsections in the Summary of Understanding Template (Tools Available / Channels / Decision Patterns), and one new Business Graph Emission subsection immediately above the final confirmation prompt. The file still has exactly 9 categories. The Quick Reference total is updated from 20 to 21 seed questions. No em-dashes introduced. The Emission subsection cross-links to business-graph-schema.md with shorthand relative path and cites the canonical output path `.agentbloc/graph/business-graph.json`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend SKILL.md — Phase 1 load list + State Transitions sub-gate + Phase 2 precondition</name>
  <files>.claude/skills/agentbloc/SKILL.md</files>
  <read_first>
    - .claude/skills/agentbloc/SKILL.md (the full 159-line file; specifically lines 34-40 for State Transitions, lines 88-95 for Phase 1 entry, lines 96-102 for Phase 2 entry)
    - .claude/skills/agentbloc/references/business-graph-schema.md (the file the new load-list entry points at, created in Plan 08-01 — verify it exists before referencing)
    - .planning/phases/08-business-graph-foundation/08-PATTERNS.md (lines 343-441 — the "SKILL.md (MODIFY)" section with three exact Before/After edit patterns)
    - .planning/phases/08-business-graph-foundation/08-CONTEXT.md (decisions D-11, D-14, D-15 file path, "No new gate vocabulary beyond one value" from specifics)
    - .planning/milestones/v1.0-phases/03-interview-and-design-phases/03-01-PLAN.md (lines 226-241 — the v1.0 Phase 3 precedent for the exact unconditional-load edit pattern; this Phase 8 edit applies the same pattern one more time)
  </read_first>
  <action>
Apply THREE surgical edits to `.claude/skills/agentbloc/SKILL.md`. The file is 159 lines. All edits are additive; do not remove existing bullets / lines / blocks.

**Edit 1 — Extend the unconditional load list at Phase 1 entry (currently lines 92-94):**

Find this EXACT block:
```
You MUST read the complete interview protocol AND the data classification reference before asking any questions:
See [references/phase-1-interview.md](references/phase-1-interview.md)
See [references/data-classification.md](references/data-classification.md)
```

Replace with this EXACT block (change "AND the data classification reference" to "AND the data classification reference AND the business graph schema", and add a third See-line):
```
You MUST read the complete interview protocol AND the data classification reference AND the business graph schema before asking any questions:
See [references/phase-1-interview.md](references/phase-1-interview.md)
See [references/data-classification.md](references/data-classification.md)
See [references/business-graph-schema.md](references/business-graph-schema.md)
```

This is the same 3-line micro-edit pattern applied by v1.0 Phase 3 Plan 01 Task 2 — one word change in the introductory sentence + one new See-line.

**Edit 2 — Extend State Transitions with the business_graph_validated sub-gate (currently lines 36-40):**

Find this EXACT block:
```
### State Transitions

- `pending` to `approved`: User explicitly confirms ("yes", "approved", "ok", "adelante")
- `pending` to `blocked`: An issue prevents progression
- Phase number increments ONLY after current gate is `approved` AND user explicitly confirms
- Phase loopback: If new information invalidates a prior approved gate, reset that phase to `pending`. Announce: "New information affects Phase N. Returning to re-validate."
```

Append one new bullet as the LAST item in this list (after the Phase loopback bullet):
```
- Phase 1 specific: Gate transition to `approved` requires BOTH user confirmation of the rendered Business Graph tables AND the `business_graph_validated` sub-gate (all REQUIRED checks from [references/business-graph-schema.md](references/business-graph-schema.md) Validation Checklist have passed and the file at `.agentbloc/graph/business-graph.json` has been written).
```

Do NOT create a new top-level Gate value; the sub-gate lives INSIDE Phase 1's existing `approved` state per CONTEXT.md specifics ("No new gate vocabulary beyond one value").

**Edit 3 — Add Phase 2 precondition (currently lines 96-102):**

Find this EXACT block:
```
### Phase 2: General Design

Translate the interview into a high-level agent team design. Identify agents (one per responsibility), map topology (pipeline, mesh, hierarchy, swarm), define contracts, schedules, and governance. Present as diagram + table.

You MUST read the complete design protocol before starting this phase:
See [references/phase-2-design.md](references/phase-2-design.md)
```

Insert a NEW paragraph between the descriptive sentence (ending "diagram + table.") and the "You MUST read..." line. Final shape becomes:
```
### Phase 2: General Design

Translate the interview into a high-level agent team design. Identify agents (one per responsibility), map topology (pipeline, mesh, hierarchy, swarm), define contracts, schedules, and governance. Present as diagram + table.

**Precondition:** Verify `.agentbloc/graph/business-graph.json` exists and validates against the Validation Checklist in [references/business-graph-schema.md](references/business-graph-schema.md). If the file is missing or fails any REQUIRED check, return the state bar to Phase 1 with gate `pending` and re-run the Summary gate before attempting Phase 2 again.

You MUST read the complete design protocol before starting this phase:
See [references/phase-2-design.md](references/phase-2-design.md)
```

**Formatting constraints:**
- Preserve all other sections of SKILL.md unchanged
- Total file length must stay under 250 lines (current 159 + ~9 = ~168 — safe margin)
- Do NOT introduce em-dashes (—) anywhere
- Do NOT add `business_graph_validated` as a value in the state-bar example on line 31 (examples only show `pending` / `approved` / `blocked`) — the sub-gate is a Phase-1-specific REQUIREMENT, not a visible state-bar value
  </action>
  <verify>
    <automated>grep -q "references/business-graph-schema\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "business_graph_validated" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "\.agentbloc/graph/business-graph\.json" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "AND the business graph schema" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "Precondition:" .claude/skills/agentbloc/SKILL.md &amp;&amp; test $(wc -l &lt; .claude/skills/agentbloc/SKILL.md) -lt 250</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "See \[references/business-graph-schema\.md\](references/business-graph-schema\.md)" .claude/skills/agentbloc/SKILL.md` matches (new load-list line added)
    - `grep -q "AND the business graph schema before asking any questions" .claude/skills/agentbloc/SKILL.md` matches (the intro sentence was updated, not duplicated)
    - `grep -c "^See \[references/" .claude/skills/agentbloc/SKILL.md` returns 9 (original 8 See-links + 1 new = 9; was 8 before, after is 9 — the See-links are: phase-1-interview, data-classification, phase-2-design, phase-3-integration, phase-4-confirmation, phase-5-deployment, phase-6-evolution, and one glossary. Running the grep will show the baseline; the acceptance is that it increments by exactly 1 from pre-edit count)
    - `grep -q "business_graph_validated" .claude/skills/agentbloc/SKILL.md` matches (sub-gate vocabulary named in State Transitions)
    - `grep -q "Phase 1 specific:" .claude/skills/agentbloc/SKILL.md` matches (the new State Transitions bullet anchor)
    - `grep -q "\.agentbloc/graph/business-graph\.json" .claude/skills/agentbloc/SKILL.md` matches (canonical output path cited in Phase 2 precondition)
    - `grep -q "\\*\\*Precondition:\\*\\*" .claude/skills/agentbloc/SKILL.md` matches (Phase 2 precondition paragraph added with bold marker)
    - `test $(wc -l < .claude/skills/agentbloc/SKILL.md) -lt 250` exits 0 (file stays under 250 lines per v1.0 budget)
    - `test $(wc -l < .claude/skills/agentbloc/SKILL.md) -gt 159` exits 0 (file grew from the baseline — at least one of the three edits landed)
    - `grep -c "^### Phase " .claude/skills/agentbloc/SKILL.md` returns 6 (six phase sections preserved unchanged in count)
    - `grep -c "pending\|approved\|blocked" .claude/skills/agentbloc/SKILL.md` returns >= 5 (existing gate vocabulary preserved — no Gate values removed)
    - Em-dash check on changed ranges: the three inserted blocks contain no `—` character (project CLAUDE.md rule). Verify by diffing before/after: `grep -c "—" .claude/skills/agentbloc/SKILL.md` returns same count as pre-edit baseline
  </acceptance_criteria>
  <done>
SKILL.md has a third See-line loading business-graph-schema.md at Phase 1 entry (with the preceding sentence updated to mention "AND the business graph schema"), a new State Transitions bullet naming the business_graph_validated sub-gate as a Phase-1-specific requirement, and a Phase 2 Precondition paragraph gating the transition on the graph file existing and validating. File is between 160 and 250 lines. No existing sections were removed or re-ordered. No em-dashes introduced. The phase count is still 6.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these phase-level checks that prove the Phase 8 success criteria from ROADMAP are met:

**Success Criterion 1** — "Running the v1.0 Interview against the Arco Rooms scenario produces a `.agentbloc/graph/business-graph.json` file matching the schema":
- `grep -q "\.agentbloc/graph/business-graph\.json" .claude/skills/agentbloc/references/phase-1-interview.md` (emission step wired)
- `grep -q "\.agentbloc/graph/business-graph\.json" .claude/skills/agentbloc/SKILL.md` (Phase 2 precondition wired)
- `test -f .claude/skills/agentbloc/examples/arco-rooms-business-graph.json` (fixture from 08-01 exists — already verified)

**Success Criterion 2** — "A Business Graph with a schema-version mismatch or missing required field produces a clear validation error with the specific path, not a generic failure":
- `grep -Eq "Check [0-9].*FAIL" .claude/skills/agentbloc/references/business-graph-schema.md` (checklist from 08-01 has failure prompts — already verified)
- `grep -q "Validation Checklist" .claude/skills/agentbloc/references/phase-1-interview.md` (emission step invokes the checklist)

**Success Criterion 3** — "A reader who opens `references/business-graph-schema.md` can understand the full field set without reading validator code":
- `grep -q "^# Business Graph Schema" .claude/skills/agentbloc/references/business-graph-schema.md` (already verified in 08-01)
- The schema is prose + tables, no external validator code — inherent by D-13 design

**Success Criterion 4** — "The interview conversation remains bilingual (EN/ES), non-technical, and flows as in v1.0":
- `grep -c "## Category " .claude/skills/agentbloc/references/phase-1-interview.md` returns 9 (still 9 categories, no 10th)
- `grep -q "### Seed Questions" .claude/skills/agentbloc/references/phase-1-interview.md` (seed-question structure preserved)
- `grep -q "One Question Per Turn" .claude/skills/agentbloc/references/phase-1-interview.md` (D-03 still in force)
- `grep -q "Soft Framing" .claude/skills/agentbloc/references/phase-1-interview.md` (D-04 still in force)
- `grep -q "ES / EN" .claude/skills/agentbloc/references/data-classification.md` (bilingual detection unchanged — regression check)

All four ROADMAP success criteria pass.
</verification>

<success_criteria>
1. Category 7 of phase-1-interview.md has 3 seed questions (was 2), with the third being the D-16 decision-rules question.
2. The Summary of Understanding Template has 3 new H3 table subsections (Tools Available / Channels / Decision Patterns) between Services and Integrations and Data Model, plus a Business Graph Emission subsection immediately above the final confirmation prompt.
3. SKILL.md Phase 1 entry loads business-graph-schema.md unconditionally alongside the two existing references.
4. SKILL.md State Transitions section names the business_graph_validated sub-gate as a Phase-1-specific transition requirement.
5. SKILL.md Phase 2 entry gates on the graph file existing and validating.
6. The v1.0 9-category structure is preserved (no 10th category, no re-ordered categories).
7. SKILL.md stays under 250 lines; phase-1-interview.md stays between 380 and 410 lines.
8. No em-dashes introduced in either file beyond the pre-edit baseline count.
9. The Quick Reference table in phase-1-interview.md reflects the Category 7 count bump (2 -> 3) and Total bump (20 -> 21).
10. All four Phase 8 ROADMAP success criteria pass (verified by the verification block above).
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-graph-foundation/08-02-SUMMARY.md` documenting:
- Exact Before/After diffs for each of the 4 phase-1-interview.md edits and 3 SKILL.md edits
- Final line counts of both files (before and after)
- Which of the 4 ROADMAP Phase 8 success criteria this plan now makes passable end-to-end
- Any deviations from the plan with rationale
- Handoff note for Phase 9 (Designer Agent): "Phase 1 of the interview now emits `.agentbloc/graph/business-graph.json` with shape locked in `references/business-graph-schema.md`. Designer Agent reads this file and refuses to proceed on an unknown `schema_version` value. Current version: 1. Canonical test fixture at `examples/arco-rooms-business-graph.json`."
</output>
