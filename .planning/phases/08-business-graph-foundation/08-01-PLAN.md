---
phase: 08-business-graph-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/agentbloc/references/business-graph-schema.md
  - .claude/skills/agentbloc/examples/arco-rooms-business-graph.json
autonomous: true
requirements:
  - BGRAPH-01
  - BGRAPH-02
  - BGRAPH-03
  - BGRAPH-04

must_haves:
  truths:
    - "references/business-graph-schema.md exists as a loadable reference with a TOC, schema definition, field-obligation matrix, trigger bounded enum, and validation checklist"
    - "The schema carries an integer schema_version field starting at 1, with versioning rules documented (bumped only on breaking changes)"
    - "The schema defines a bounded enum trigger.type in {cron, event, manual} with type-specific required sub-fields (schedule / source+name / description)"
    - "The validation checklist is an ordered, deterministic prose list Claude can walk through to pass/fail each REQUIRED field and produce a targeted follow-up on failure"
    - "A reader can understand the full Business Graph field set by reading only business-graph-schema.md (no need to read validator code, because there is none)"
    - "examples/arco-rooms-business-graph.json exists, validates against every REQUIRED check in the schema, and realistically reflects the Arco Rooms scenario already documented in examples/arco-rooms.md"
  artifacts:
    - path: ".claude/skills/agentbloc/references/business-graph-schema.md"
      provides: "Canonical Business Graph schema definition, field obligation matrix (REQUIRED/RECOMMENDED/OPTIONAL), trigger bounded enum, prose validation checklist, emission protocol, re-run behavior, and schema versioning rules"
      min_lines: 130
      contains: "schema_version"
    - path: ".claude/skills/agentbloc/examples/arco-rooms-business-graph.json"
      provides: "Canonical test fixture — a realistic Business Graph JSON for the Arco Rooms scenario that every REQUIRED validation check passes"
      contains: "schema_version"
  key_links:
    - from: ".claude/skills/agentbloc/references/business-graph-schema.md"
      to: ".claude/skills/agentbloc/references/phase-1-interview.md"
      via: "cross-link in 'When This Applies' and 'Emission Protocol' sections — emission happens in the Summary gate of phase-1-interview.md"
      pattern: "phase-1-interview\\.md"
    - from: ".claude/skills/agentbloc/references/business-graph-schema.md"
      to: ".claude/skills/agentbloc/references/data-classification.md"
      via: "cross-link — security_profile field in the schema is the JSON-structured version of the running tally produced by the interview's data-classification scan"
      pattern: "data-classification\\.md"
    - from: ".claude/skills/agentbloc/examples/arco-rooms-business-graph.json"
      to: ".claude/skills/agentbloc/references/business-graph-schema.md"
      via: "fixture conforms to the schema — every REQUIRED check defined in the schema passes on this JSON"
      pattern: "schema_version"
---

<objective>
Create the canonical Business Graph schema reference file and a test fixture that validates against it. This plan produces the contract every downstream v2.0 phase (Designer Agent, Deploy Pipeline, Briefing Agent) reads from, and freezes the shape the Arco Rooms end-to-end test will verify against in Phase 16.

Purpose: Phase 9 Designer Agent reads `.agentbloc/graph/business-graph.json`. Its quality ceiling is determined by how well-specified the schema is. Phase 8 Plan 1 locks the schema as a prose-checklist-validated contract — no external tooling (no ajv / jsonschema), honoring AgentBloc's markdown-only constraint (D-13). The Arco Rooms fixture is the canonical reference both humans and TAP tests use to confirm "a valid Business Graph looks like this."

Output: A ~130-200 line `references/business-graph-schema.md` structurally twinned with `references/data-classification.md`, plus `examples/arco-rooms-business-graph.json` — a realistic, schema-valid fixture derived from the existing `examples/arco-rooms.md` walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-business-graph-foundation/08-CONTEXT.md
@.planning/phases/08-business-graph-foundation/08-PATTERNS.md

@.claude/skills/agentbloc/references/data-classification.md
@.claude/skills/agentbloc/references/blast-radius.md
@.claude/skills/agentbloc/references/audit-logging.md
@.claude/skills/agentbloc/references/phase-1-interview.md
@.claude/skills/agentbloc/examples/arco-rooms.md

<interfaces>
<!-- Canonical Business Graph JSON shape (locked by CONTEXT.md D-12, D-18, D-20). -->
<!-- This is the contract the new business-graph-schema.md must define and the fixture must satisfy. -->

```jsonc
{
  "schema_version": 1,                        // REQUIRED. Integer. Bumped only on breaking changes.
  "business": {
    "type": "string",                         // REQUIRED. e.g. "rental-property-management"
    "size": "string | null",                  // RECOMMENDED. e.g. "7 properties, 1 operator"
    "owner": "string | null"                  // RECOMMENDED. e.g. "Maria"
  },
  "processes": [                              // REQUIRED. Length >= 1.
    {
      "name": "string",                       // REQUIRED.
      "steps": ["string"],                    // REQUIRED. Length >= 1.
      "trigger": {                            // RECOMMENDED.
        "type": "cron | event | manual",      // Bounded enum per D-18.
        // cron requires: "schedule": "<cron string>"
        // event requires: "source": "<service>", "name": "<event id>"
        // manual requires: "description": "<free text>"
      },
      "tools": ["string"],                    // RECOMMENDED. Tool names referenced in this process.
      "frequency": "string | null",           // RECOMMENDED. e.g. "weekly", "daily-9am"
      "current_actor": "string | null",       // RECOMMENDED. Who does this today.
      "pain": "string"                        // REQUIRED. Free-text per D-20.
    }
  ],
  "tools_available": ["string"],              // OPTIONAL. Extracted from interview Category 3.
  "channels": ["string"],                     // OPTIONAL. Extracted from interview Category 8. e.g. ["telegram","email"]
  "decision_patterns": ["string"],            // OPTIONAL. Free-text rules (D-16 seed question).
  "security_profile": {                       // OPTIONAL. Structured version of v1.0 D-10 tally.
    "data_classes": ["PII","Financial"],
    "regimes_activated": ["GDPR"]
  },
  "business_context": "string | null"         // OPTIONAL. Free-text additional context.
}
```

Bounded enum — `process.trigger.type`:
- `cron` + required `schedule` (cron string)
- `event` + required `source` (service name) + `name` (event id)
- `manual` + required `description` (free text)

Structural analog (twin): references/data-classification.md — same TOC shape, same "When This Applies" opener, same table-driven rules style.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create references/business-graph-schema.md with schema + field obligation matrix + trigger enum</name>
  <files>.claude/skills/agentbloc/references/business-graph-schema.md</files>
  <read_first>
    - .planning/phases/08-business-graph-foundation/08-PATTERNS.md (lines 19-251 — the "business-graph-schema.md (NEW file)" section with Patterns A through H, each giving exact text and structural shape to replicate)
    - .planning/phases/08-business-graph-foundation/08-CONTEXT.md (decisions D-11 through D-20; D-12 specifically defines the three-tier field obligation table; D-18 defines the trigger bounded enum; D-15 locks the output path)
    - .claude/skills/agentbloc/references/data-classification.md (the structural twin — copy the exact skeleton: H1 title, one-line blockquote, Table of Contents, "When This Applies" paragraph, table-driven rules, Compliance Activation Matrix at bottom)
    - .claude/skills/agentbloc/references/blast-radius.md (lines 32-53 — Scoring Decision Tree numbered-prose pattern to adapt for the Validation Checklist)
    - .claude/skills/agentbloc/references/phase-1-interview.md (lines 263-279 — Master Completion Checklist pattern for the gate-style "walk this list before emitting" shape)
    - .claude/skills/agentbloc/references/audit-logging.md (style reference for the JSONC schema block — terse commented field list)
    - .planning/REQUIREMENTS.md (BGRAPH-01..04 — verify every bullet is addressed by the finished file)
  </read_first>
  <action>
Create a NEW file at `.claude/skills/agentbloc/references/business-graph-schema.md` (~150-180 lines) with EXACTLY the following 8 sections in this order. Use GitHub-flavored Markdown.

**Section 1 — H1 + blockquote + TOC** (lines 1-15 shape of data-classification.md):

```markdown
# Business Graph Schema

> Schema reference loaded unconditionally at Phase 1 entry alongside [phase-1-interview.md](phase-1-interview.md) and [data-classification.md](data-classification.md). Defines the canonical Business Graph JSON emitted by the Summary gate and the validation checklist Claude applies before writing the file.

## Table of Contents

- [When This Applies](#when-this-applies)
- [Schema Definition](#schema-definition)
- [Field Obligation Matrix](#field-obligation-matrix)
- [Trigger Bounded Enum](#trigger-bounded-enum)
- [Validation Checklist](#validation-checklist)
- [Emission Protocol](#emission-protocol)
- [Re-run Behavior](#re-run-behavior)
- [Schema Versioning Rules](#schema-versioning-rules)
```

**Section 2 — `## When This Applies`**: one paragraph following the exact adaptation text supplied in 08-PATTERNS.md Pattern B (the block beginning "Claude reads this file during the Interview Phase Summary gate to produce the canonical Business Graph JSON at `.agentbloc/graph/business-graph.json`..."). Include the explicit downstream consumers list (Phase 9 Designer Agent, Phase 12 Deploy Pipeline, Phase 14 Briefing Agent) and cross-link `phase-1-interview.md` and `data-classification.md` with shorthand relative paths (no `references/` prefix).

**Section 3 — `## Schema Definition`**: one fenced ` ```jsonc ` block containing the full schema from the `<interfaces>` block above in `<context>` (copy verbatim). Every field has an inline comment declaring its obligation tier (REQUIRED / RECOMMENDED / OPTIONAL) per D-12 and a one-example hint. Keep the block self-contained — do not split across multiple code fences.

**Section 4 — `## Field Obligation Matrix`**: a markdown table rendering D-12 verbatim:

```markdown
| Tier | Fields | Behavior if missing |
|---|---|---|
| REQUIRED | `schema_version`, `business.type`, `processes[]` (length >= 1), per-process `name` + `steps[]` + `pain` | Validation fails. Gate blocks Phase 2 transition. Claude asks the user the missing question. |
| RECOMMENDED | `business.size`, `business.owner`, per-process `trigger`, `tools`, `frequency`, `current_actor` | Validation warns but does not fail. Default to `null` or `"unknown"`. Phase 2 Designer Agent proceeds with degraded output and flags the gap. |
| OPTIONAL | `tools_available[]`, `channels[]`, `decision_patterns[]`, `security_profile`, `business_context` | Silent defaults. Empty arrays, `null` values. Designer Agent proceeds without comment. |
```

Follow the table with a one-sentence note that Designer Agent (Phase 9) reads `schema_version` and refuses to proceed on an unknown major version.

**Section 5 — `## Trigger Bounded Enum`**: one paragraph explaining D-18, then a 4-column markdown table copied exactly from 08-PATTERNS.md Pattern C adaptation:

```markdown
| Enum Value | Definition | Required Sub-fields | Example |
|------------|-----------|---------------------|---------|
| `cron` | Time-based recurring trigger | `schedule` (cron string) | `{"type":"cron","schedule":"0 9 * * 1"}` |
| `event` | External-event-driven trigger | `source` (service name) + `name` (event id) | `{"type":"event","source":"gmail","name":"new_message"}` |
| `manual` | Human-initiated trigger | `description` (free text) | `{"type":"manual","description":"Operator runs weekly"}` |
```

Follow the table with one sentence: "Any value outside this enum forces a clarification question before emission; extensions (`webhook`, `loop`) are deferred to a future schema version bump."

**Section 6 — `## Validation Checklist`**: the six numbered checks copied verbatim from 08-PATTERNS.md Pattern E (Check 1 through Check 6), opened with this paragraph:

```markdown
Claude walks this ordered list before writing `.agentbloc/graph/business-graph.json`. Any FAIL triggers a conversational follow-up before emission; the REQUIRED tier checks (1-5) block emission; the RECOMMENDED tier check (6) emits with warnings.
```

Then the checks in order: schema_version, business.type, processes[], per-process name/steps/pain, trigger.type enum + sub-field, RECOMMENDED fields WARN-only. Each check is a bold line ("**Check N:** ...") followed by a `- FAIL:` bullet with the targeted follow-up question verbatim from 08-PATTERNS.md Pattern E.

**Section 7 — `## Emission Protocol`** (8-15 lines): Describes D-11 + D-14. Concrete text to emit:

```markdown
Emission happens during the [phase-1-interview.md](phase-1-interview.md) Summary of Understanding gate (D-11). The steps:

1. Walk the Validation Checklist above in order.
2. For each REQUIRED failure (Checks 1-5), ask the targeted conversational follow-up and wait for the user's answer before resuming.
3. Once all REQUIRED checks pass, render the Business Graph to the user as the tables in the Summary of Understanding Template (business -> processes -> tools_available -> channels -> decision_patterns -> security_profile). The JSON itself is never shown to the user (D-14).
4. After user confirmation ("yes" / "adelante" / etc.), write the JSON silently to `.agentbloc/graph/business-graph.json`. Create the `.agentbloc/graph/` directory if it does not exist.
5. Confirm emission in one sentence: "Business Graph saved. Ready to move to the design phase."
6. Set the Phase 1 `business_graph_validated` sub-gate to `approved` and allow transition to Phase 2.

If the user edits any table, re-run the Validation Checklist and re-emit the JSON.
```

**Section 8 — `## Re-run Behavior`** (8-12 lines): Describes D-19. Concrete text:

```markdown
If `.agentbloc/graph/business-graph.json` already exists when the Summary gate is reached, Claude asks the user: "I already have a Business Graph on file for this project. Do you want to (a) keep the existing one, (b) overwrite it, or (c) merge new processes into it?" Default is **merge** (additive).

- **keep**: Skip emission, transition to Phase 2 with the existing graph.
- **overwrite**: Replace the file entirely after a fresh Validation Checklist pass.
- **merge**: Add newly captured processes to the existing `processes[]` array. If a new process shares a `name` with an existing one, present both to the user and ask whether to rename, overwrite the old, or skip the new.

The `schema_version` on disk must match the current schema version. If it does not, refuse merge and emit `"action_required": "schema_version_mismatch"` to the conversation so the user knows a manual migration is needed.
```

**Section 9 — `## Schema Versioning Rules`** (6-10 lines): Integer versioning per D-12. Concrete text:

```markdown
The `schema_version` field is an integer. It starts at `1`. The version bumps only on breaking changes:

- A REQUIRED field is removed or renamed.
- An enum value is removed from a bounded type (e.g., dropping `cron` from `trigger.type`).

Additive changes do NOT bump the version:

- Adding a new OPTIONAL field.
- Adding a new value to a bounded enum.
- Loosening a REQUIRED field to RECOMMENDED.

Downstream consumers (Phase 9 Designer Agent, Phase 12 Deploy, Phase 14 Briefing) read `schema_version` and refuse to proceed on an unknown major version.
```

**Formatting rules for the whole file:**
- All cross-links to companion references use shorthand relative paths (`phase-1-interview.md`, not `references/phase-1-interview.md`).
- Every bounded-enum / tier / rule renders as a markdown table, never as a prose list (matches data-classification.md house style).
- Keep the file between 130 and 200 lines. If it grows beyond 200, tighten prose, not tables.
- Do NOT use em-dashes (—) anywhere (project CLAUDE.md rule).
- Do NOT add a trailing "Quick Reference" summary section (not needed — TOC serves that role).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/business-graph-schema.md &amp;&amp; grep -q "schema_version" .claude/skills/agentbloc/references/business-graph-schema.md &amp;&amp; grep -q "## Table of Contents" .claude/skills/agentbloc/references/business-graph-schema.md &amp;&amp; grep -q "## When This Applies" .claude/skills/agentbloc/references/business-graph-schema.md &amp;&amp; grep -q "## Schema Definition" .claude/skills/agentbloc/references/business-graph-schema.md &amp;&amp; grep -q "## Field Obligation Matrix" .claude/skills/agentbloc/references/business-graph-schema.md &amp;&amp; grep -q "## Trigger Bounded Enum" .claude/skills/agentbloc/references/business-graph-schema.md &amp;&amp; grep -q "## Validation Checklist" .claude/skills/agentbloc/references/business-graph-schema.md &amp;&amp; grep -q "## Emission Protocol" .claude/skills/agentbloc/references/business-graph-schema.md &amp;&amp; grep -q "## Re-run Behavior" .claude/skills/agentbloc/references/business-graph-schema.md &amp;&amp; grep -q "## Schema Versioning Rules" .claude/skills/agentbloc/references/business-graph-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/references/business-graph-schema.md` exits 0
    - `grep -q "^# Business Graph Schema$" .claude/skills/agentbloc/references/business-graph-schema.md` matches (exact H1)
    - `grep -q "schema_version" .claude/skills/agentbloc/references/business-graph-schema.md` matches
    - `grep -Eq "cron.*event.*manual|cron, event, manual|\\{cron, event, manual\\}" .claude/skills/agentbloc/references/business-graph-schema.md` matches (bounded enum documented)
    - `grep -q "REQUIRED" .claude/skills/agentbloc/references/business-graph-schema.md` AND `grep -q "RECOMMENDED" .claude/skills/agentbloc/references/business-graph-schema.md` AND `grep -q "OPTIONAL" .claude/skills/agentbloc/references/business-graph-schema.md` — all three tiers present
    - `grep -q "\.agentbloc/graph/business-graph\.json" .claude/skills/agentbloc/references/business-graph-schema.md` — output path locked
    - `grep -q "phase-1-interview\.md" .claude/skills/agentbloc/references/business-graph-schema.md` AND `grep -q "data-classification\.md" .claude/skills/agentbloc/references/business-graph-schema.md` — cross-links to both companion references
    - `grep -Eq "Check [0-9]" .claude/skills/agentbloc/references/business-graph-schema.md` — numbered validation checks exist
    - `grep -q "```jsonc" .claude/skills/agentbloc/references/business-graph-schema.md` — schema fenced block uses jsonc
    - `wc -l .claude/skills/agentbloc/references/business-graph-schema.md` returns between 130 and 220 lines
    - `grep -q "—" .claude/skills/agentbloc/references/business-graph-schema.md` returns NO matches (zero em-dashes per project rule)
    - All 9 planned H2 section headers are present: "## When This Applies", "## Schema Definition", "## Field Obligation Matrix", "## Trigger Bounded Enum", "## Validation Checklist", "## Emission Protocol", "## Re-run Behavior", "## Schema Versioning Rules" — verified by running `grep -c "^## " .claude/skills/agentbloc/references/business-graph-schema.md` returning >= 8
  </acceptance_criteria>
  <done>
business-graph-schema.md exists with all 8 documented H2 sections, the JSONC schema definition block, the three-tier field obligation table, the bounded trigger enum table, the six-check validation checklist, emission protocol, re-run behavior, and schema versioning rules. Cross-links to both phase-1-interview.md and data-classification.md use shorthand relative paths. File is 130-220 lines, contains zero em-dashes, and follows the structural shape of data-classification.md verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create examples/arco-rooms-business-graph.json as the canonical test fixture</name>
  <files>.claude/skills/agentbloc/examples/arco-rooms-business-graph.json</files>
  <read_first>
    - .claude/skills/agentbloc/references/business-graph-schema.md (the file created in Task 1 — this fixture must satisfy every REQUIRED check in its Validation Checklist)
    - .claude/skills/agentbloc/examples/arco-rooms.md (existing walkthrough — lines 1-100 contain the business facts: 7-30 properties in Almeria Spain, daily invoice collection from 6 utility providers + 4 banks + Telegram reporting, GDPR-activated, 3 processes: invoice collection / payment matching / reporting)
    - .planning/phases/08-business-graph-foundation/08-CONTEXT.md (D-12 three tiers; D-18 trigger enum; D-20 pain is free-text; D-15 file path convention)
    - .planning/phases/08-business-graph-foundation/08-PATTERNS.md (Pattern F — Schema Definition block, for field ordering and inline-comment style)
  </read_first>
  <action>
Create a NEW file at `.claude/skills/agentbloc/examples/arco-rooms-business-graph.json` containing pure JSON (no comments — comments belong in the schema reference, not the fixture). The fixture MUST populate every REQUIRED field and MUST populate most RECOMMENDED fields so it can serve as the canonical "what a good Business Graph looks like" reference for Phase 16.

Use these concrete values derived from `examples/arco-rooms.md`:

- `schema_version`: `1`
- `business.type`: `"rental-property-management"`
- `business.size`: `"~30 rental properties across multiple owners, 1 operator"`
- `business.owner`: `"Pablo"` (keeping the existing example persona)
- `processes[0]`: Invoice Collection
  - `name`: `"Invoice Collection"`
  - `steps`: array of 5 strings describing: log into 6 utility provider portals, download new invoices, scrape invoice emails in Gmail, deduplicate against state file, write collected invoices to `.agentbloc/state/invoices.json`
  - `trigger`: `{"type": "cron", "schedule": "0 22 * * *"}` (daily 22:00)
  - `tools`: `["Endesa portal", "Aguas de Almeria portal", "Naturgy portal", "Movistar portal", "Urbaser portal", "Mapfre API", "Gmail", "Google Sheets"]`
  - `frequency`: `"daily"`
  - `current_actor`: `"Pablo (manual, ~1 hour/day)"`
  - `pain`: `"Logging into 6 provider portals every evening is tedious and error-prone; missing an invoice means late fees."`
- `processes[1]`: Payment Matching
  - `name`: `"Payment Matching"`
  - `steps`: array of 5 strings: read bank transactions via PSD2 API, load invoices from state, match by regex with confidence scoring, flag low-confidence / unmatched items, write matches to `.agentbloc/state/matches.json`
  - `trigger`: `{"type": "cron", "schedule": "30 22 * * *"}` (daily 22:30)
  - `tools`: `["bank-mcp (PSD2)", "Google Sheets (tenant registry)"]`
  - `frequency`: `"daily"`
  - `current_actor`: `"Pablo (manual, ~1 hour/day)"`
  - `pain`: `"Bank transaction descriptions vary across 4 banks, making manual matching slow and error-prone; unmatched items cause tenant disputes."`
- `processes[2]`: Owner Reporting
  - `name`: `"Owner Reporting"`
  - `steps`: array of 4 strings: consolidate daily invoice + payment results, format per-owner Telegram summary, send via Telegram Bot API, log delivery confirmation
  - `trigger`: `{"type": "cron", "schedule": "0 23 * * *"}` (daily 23:00)
  - `tools`: `["Telegram Bot API"]`
  - `frequency`: `"daily"`
  - `current_actor`: `"Pablo (manual, evening message drafting)"`
  - `pain`: `"Owners expect daily updates; missing an update creates trust issues, but writing them manually is tedious."`
- `tools_available`: `["Google Sheets", "Gmail", "Telegram", "Mapfre API", "Endesa portal", "Aguas de Almeria portal", "Naturgy portal", "Movistar portal", "Urbaser portal", "4 bank accounts (PSD2)"]`
- `channels`: `["telegram", "email"]`
- `decision_patterns`: array of 3 strings:
  - `"If an invoice is overdue by more than 7 days, send a formal notice to the tenant."`
  - `"If a bank transaction has no matching invoice after 48 hours, escalate to Pablo for manual review."`
  - `"If a provider portal is unreachable for 3 consecutive days, send a Telegram alert and fall back to Gmail scraping."`
- `security_profile`:
  - `data_classes`: `["PII", "Financial"]`
  - `regimes_activated`: `["GDPR"]`
- `business_context`: `"Almeria Spain-based property management business serving ~30 rental properties across multiple owners. Jurisdiction: Spain (EU), GDPR applies. Solo operator who is non-technical and manages end-to-end collection -> matching -> reporting workflow daily."`

**Formatting requirements:**
- 2-space indentation
- Keys in the ORDER declared in the schema definition block (schema_version -> business -> processes -> tools_available -> channels -> decision_patterns -> security_profile -> business_context)
- File must parse as valid JSON (no trailing commas, no comments, no JSONC)
- End file with a single trailing newline

This fixture is the reference Phase 16 end-to-end validation will use — a test running the interview against Arco Rooms should produce a Business Graph structurally equivalent to this one.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/examples/arco-rooms-business-graph.json &amp;&amp; python3 -c "import json, sys; d=json.load(open('.claude/skills/agentbloc/examples/arco-rooms-business-graph.json')); assert d['schema_version']==1; assert d['business']['type']; assert len(d['processes'])>=1; [ (p['name'], p['steps'], p['pain'], p['trigger']['type']) for p in d['processes'] ]; assert all(p['trigger']['type'] in ('cron','event','manual') for p in d['processes']); assert all(len(p['steps'])>=1 for p in d['processes']); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/examples/arco-rooms-business-graph.json` exits 0
    - `python3 -c "import json; json.load(open('.claude/skills/agentbloc/examples/arco-rooms-business-graph.json'))"` succeeds (valid JSON)
    - `python3 -c "import json,sys; d=json.load(open('.claude/skills/agentbloc/examples/arco-rooms-business-graph.json')); sys.exit(0 if d['schema_version']==1 else 1)"` exits 0
    - `python3 -c "import json,sys; d=json.load(open('.claude/skills/agentbloc/examples/arco-rooms-business-graph.json')); sys.exit(0 if len(d['processes'])>=3 else 1)"` exits 0 (at least 3 processes)
    - `python3 -c "import json,sys; d=json.load(open('.claude/skills/agentbloc/examples/arco-rooms-business-graph.json')); sys.exit(0 if all('name' in p and 'steps' in p and len(p['steps'])>=1 and 'pain' in p and p['pain'] for p in d['processes']) else 1)"` exits 0 (every process has name + steps + pain — the REQUIRED per-process fields)
    - `python3 -c "import json,sys; d=json.load(open('.claude/skills/agentbloc/examples/arco-rooms-business-graph.json')); sys.exit(0 if all(p.get('trigger',{}).get('type') in ('cron','event','manual') for p in d['processes']) else 1)"` exits 0 (every process has a valid trigger.type in the bounded enum)
    - `python3 -c "import json,sys; d=json.load(open('.claude/skills/agentbloc/examples/arco-rooms-business-graph.json')); sys.exit(0 if 'GDPR' in d.get('security_profile',{}).get('regimes_activated',[]) else 1)"` exits 0 (GDPR activation reflects Arco Rooms EU jurisdiction)
    - `grep -q "rental-property-management" .claude/skills/agentbloc/examples/arco-rooms-business-graph.json` — business.type set correctly
    - `grep -q "decision_patterns" .claude/skills/agentbloc/examples/arco-rooms-business-graph.json` — includes the D-16 free-text rules field
    - `grep -q "telegram" .claude/skills/agentbloc/examples/arco-rooms-business-graph.json` — channels captured
  </acceptance_criteria>
  <done>
A valid JSON fixture exists at `examples/arco-rooms-business-graph.json` with schema_version=1, 3 processes (Invoice Collection / Payment Matching / Owner Reporting) each with name + steps + pain + valid trigger, decision_patterns capturing 3 free-text rules, security_profile with GDPR activation, and business_context describing the Almeria Spain scenario. The file parses cleanly and every REQUIRED check in the Task 1 validation checklist would pass on it.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these phase-level checks:

1. `test -f .claude/skills/agentbloc/references/business-graph-schema.md` — schema reference exists
2. `test -f .claude/skills/agentbloc/examples/arco-rooms-business-graph.json` — fixture exists
3. `python3 -c "import json; json.load(open('.claude/skills/agentbloc/examples/arco-rooms-business-graph.json'))"` — fixture parses as valid JSON
4. `grep -c "^## " .claude/skills/agentbloc/references/business-graph-schema.md` returns >= 8 (all documented H2 sections)
5. `wc -l .claude/skills/agentbloc/references/business-graph-schema.md` returns between 130 and 220
6. Every BGRAPH requirement traced:
   - BGRAPH-01 (schema definition): grep for `business`, `processes`, `tools_available`, `channels`, `decision_patterns` in the schema file
   - BGRAPH-02 (schema_version): grep for `schema_version` in both the schema file and the fixture
   - BGRAPH-03 (trigger bounded enum): grep for `cron`, `event`, `manual` in schema; verify fixture trigger.type values
   - BGRAPH-04 (validator): grep for `## Validation Checklist` in schema file — the prose-checklist IS the validator (D-13)
</verification>

<success_criteria>
1. `references/business-graph-schema.md` exists, is 130-220 lines, contains all 8 H2 sections, defines the schema + three-tier field obligation matrix + bounded trigger enum + ordered validation checklist + emission protocol + re-run behavior + versioning rules.
2. The file cross-links to `phase-1-interview.md` and `data-classification.md` using shorthand relative paths.
3. The file contains zero em-dashes (project CLAUDE.md convention).
4. `examples/arco-rooms-business-graph.json` exists, parses as valid JSON, has schema_version=1, at least 3 processes with REQUIRED fields populated, valid trigger enum values, GDPR in regimes_activated.
5. A reader who opens ONLY `business-graph-schema.md` understands the full field set (Success Criterion 3 from ROADMAP Phase 8).
6. The fixture is ready for Phase 16 end-to-end validation — the "canonical test target" the Arco Rooms interview run must produce.
</success_criteria>

<output>
After completion, create `.planning/phases/08-business-graph-foundation/08-01-SUMMARY.md` documenting:
- The final schema shape (copy the fenced jsonc block from the schema file)
- The validation checklist (6 checks)
- The exact file paths and line counts of both artifacts
- Any deviations from the plan (with rationale)
- Handoff note for Plan 08-02: "The schema file at references/business-graph-schema.md is the contract Plan 08-02 wires into phase-1-interview.md and SKILL.md. Any changes to field obligations after this point require a schema_version bump."
</output>
