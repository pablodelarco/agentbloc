---
phase: 11-integration-discovery-browser-fallback
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/agentbloc/references/browser-stack.md
  - .claude/skills/agentbloc/references/legal-posture.md
  - scripts/anti-bot-lint.sh
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - BROWSER-05
  - BROWSER-06
  - BROWSER-07
  - BROWSER-09
  - BROWSER-12

must_haves:
  truths:
    - "browser-stack.md documents the pinned stack (playwright, patchright, curlconverter, har-sdk validator, fetch-har) with version pins"
    - "browser-stack.md enumerates the 9-package anti-bot deny-list (playwright-extra, puppeteer-extra-plugin-stealth, puppeteer-extra, 2captcha, anticaptcha, deathbycaptcha, capsolver, puppeteer-extra-plugin-anonymize-ua, puppeteer-extra-plugin-user-preferences)"
    - "browser-stack.md contains the Ralph retry protocol (capped budget, exponential backoff 1s/4s/16s, NO fingerprint adjustment)"
    - "legal-posture.md contains a 5-row jurisdictional variance matrix (US CFAA+DMCA / UK CMA / EU GDPR / DE BDSG / BR LGPD)"
    - "legal-posture.md documents DISCOVERY-LICENSE-NOTICE.md template and OPT_IN_LEDGER.jsonl append-only format"
    - "scripts/anti-bot-lint.sh is executable (mode +x) and exits 1 when any deny-listed package name appears in any of the 5 scanned manifest files"
    - "CI workflow invokes the lint BEFORE any dependency install step"
  artifacts:
    - path: ".claude/skills/agentbloc/references/browser-stack.md"
      provides: "Pinned browser automation stack + anti-bot deny-list + Ralph retry protocol + posture stack variant guidance"
      min_lines: 150
      contains: "playwright-extra"
    - path: ".claude/skills/agentbloc/references/legal-posture.md"
      provides: "Jurisdictional variance matrix + DISCOVERY-LICENSE-NOTICE.md template + OPT_IN_LEDGER.jsonl format + tool-provider disclaimer"
      min_lines: 180
      contains: "OPT_IN_LEDGER"
    - path: "scripts/anti-bot-lint.sh"
      provides: "CI-enforceable deny-list scan over package.json / .mcp.json / pyproject.toml / requirements.txt / Gemfile"
      min_lines: 40
      contains: "DENY-LIST VIOLATION"
    - path: ".github/workflows/ci.yml"
      provides: "Anti-bot deny-list lint job wired into push + pull_request triggers"
      min_lines: 56
      contains: "Anti-bot deny-list lint"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/anti-bot-lint.sh"
      via: "bash scripts/anti-bot-lint.sh"
      pattern: "bash scripts/anti-bot-lint.sh"
    - from: ".claude/skills/agentbloc/references/browser-stack.md"
      to: "scripts/anti-bot-lint.sh"
      via: "deny-list cross-reference in prose"
      pattern: "anti-bot-lint.sh"
    - from: ".claude/skills/agentbloc/references/legal-posture.md"
      to: ".claude/skills/agentbloc/references/credentials.md"
      via: "See-line cross-reference"
      pattern: "credentials.md"
---

<objective>
Lock the browser-fallback stack pins, the anti-bot deny-list, the Ralph retry protocol, the jurisdictional legal posture, and CI enforcement of the deny-list. This plan ships BROWSER-05 (CI deny-list lint), BROWSER-06 (Patchright pin policy), BROWSER-07 (stack pins captured), BROWSER-09 (Ralph retry belt-and-suspenders coverage), and BROWSER-12 (jurisdictional variance).

Purpose: Prevent two categories of shipwreck. First, accidental install of a deny-listed stealth plugin or CAPTCHA solver (which would turn "detect-and-degrade" into "detect-and-bypass" and expose the user to CFAA/CMA/GDPR risk). Second, silent jurisdictional misclassification (which would produce a DISCOVERY-REPORT.md lacking the tool-provider disclaimer + attestation record required for GDPR Article 30 compliance).

Output: Two new reference files (browser-stack.md, legal-posture.md) loaded unconditionally at Phase 3 entry (browser-stack.md) and inside the browser-discovery subagent's forked context (legal-posture.md) + one executable CI enforcement script + one extended CI workflow step running the lint before any dependency install.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-integration-discovery-browser-fallback/11-CONTEXT.md
@.planning/phases/11-integration-discovery-browser-fallback/11-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.claude/skills/agentbloc/references/frameworks.md
@.claude/skills/agentbloc/references/orchestration-patterns.md
@.claude/skills/agentbloc/references/credentials.md
@.github/workflows/ci.yml

<interfaces>
<!-- Locked decisions / inputs the executor uses verbatim. Extracted from 11-CONTEXT.md. -->
<!-- Executor MUST NOT invent replacement content. -->

Pinned stack (per D-48 ALLOWED + BROWSER-07, from 11-CONTEXT.md line 167):
- playwright@^1.59.1              # stock Playwright (Posture A default)
- patchright@^1.59.4               # CDP-leak patches only (Posture B); NEVER for fingerprint spoofing
- @playwright/mcp@^0.0.70          # default MCP path (Microsoft-maintained)
- curlconverter@^4.12.0            # HAR -> curl replay conversion
- @har-sdk/validator@^2.6.1        # HAR file validation
- fetch-har@^12.0.1                # HAR replay driver

Anti-bot DENY-LIST (per D-48, from 11-CONTEXT.md line 168 - NINE packages, exact names verbatim):
1. playwright-extra
2. puppeteer-extra-plugin-stealth
3. puppeteer-extra
4. 2captcha
5. anticaptcha
6. deathbycaptcha
7. capsolver
8. puppeteer-extra-plugin-anonymize-ua
9. puppeteer-extra-plugin-user-preferences

Jurisdictional matrix rows (per D-54, from 11-CONTEXT.md lines 249-255 - FIVE rows):
| Jurisdiction | Relevant Law | Broadest Interpretation | Safe-Harbor Condition | Highest-Risk Failure Mode |
| US | CFAA + DMCA section 1201 | Post-Van Buren narrow "gates-up-or-down" | Logged-in user automating own account with documented API + OAuth scope | INTERNAL-HARDENED endpoint + ToS-RED |
| UK | Computer Misuse Act 1990 + CPS 2020 | Bypassing any access control (incl. rate limits) can be criminal | Well-behaved target + documented API + no rate-limit circumvention | Cloudflare challenge bypass |
| EU | GDPR Art 5(1)(a) + 6 + national implementations | Processing others' personal data visible in your admin without lawful basis | User is data controller with documented lawful purpose | Scraping admin panels containing third-party PII without redaction |
| DE | BDSG section 202a (Ausspaehen von Daten) | Broad spying-on-data interpretation | Same as EU + explicit jurisdictional banner | German WAF (multiple native-language CAPTCHAs) |
| BR | LGPD (similar to GDPR) | Processing must have legal basis + data-subject rights | Same as EU | Brazilian jurisdiction + ToS-RED service |

OPT_IN_LEDGER.jsonl line schema (per D-46, from 11-CONTEXT.md line 156):
{"service_slug":"...","opted_in_at":"ISO-8601","ip":"...","jurisdiction":"ISO-3166-alpha-2","tos_tier":"TOS-GREEN|TOS-AMBER|TOS-RED","tos_url":"...","tos_excerpt_sha256":"...","attestation":"I am the authorized account holder..."}

Corrections require a second ledger entry with corrects_entry: <sha256 of prior line>.

Ralph retry protocol (per D-55, from 11-CONTEXT.md line 182):
- Retry budget from governance.yaml (default 3; hard cap 5)
- Exponential backoff: 1s, 4s, 16s
- Each retry records rationale in .agentbloc/discovery/<service-slug>/state.json under retries[]
- DIFFERENT TIMING, NOT DIFFERENT FINGERPRINT

Bash lint script LOCKED logic (per D-56, from 11-CONTEXT.md lines 264-278):
DENY=("playwright-extra" "puppeteer-extra-plugin-stealth" "puppeteer-extra" "2captcha" "anticaptcha" "deathbycaptcha" "capsolver" "puppeteer-extra-plugin-anonymize-ua" "puppeteer-extra-plugin-user-preferences")
SCAN_FILES=("package.json" ".mcp.json" "pyproject.toml" "requirements.txt" "Gemfile")
Grep patterns per package: "\"$pkg\"\|'$pkg'\|$pkg==" (matches double-quoted JSON key, single-quoted JSON key, or pip-style pin).
Exit 1 on first match with: "DENY-LIST VIOLATION: <pkg> found in <file>"
Exit 0 with: "anti-bot deny-list lint: clean"

Load-points (per D-58, from 11-CONTEXT.md lines 304-308):
- browser-stack.md loads unconditionally at SKILL.md Phase 3 entry (Plan 11-04 will wire this)
- legal-posture.md loads ONLY inside browser-discovery subagent's forked context (Plan 11-03 will wire this)
- scripts/anti-bot-lint.sh runs in CI on every push + pull_request to main
</interfaces>

<project_rule>
Per /Users/pablodelarco/CLAUDE.md: "Never put a -- in your text". All markdown files produced by this plan MUST NOT contain em-dash characters. The bash script also MUST NOT contain em-dash characters. Replace with double-hyphens, "and", or a colon+newline depending on rhetorical role.
</project_rule>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create browser-stack.md (declarative stack + anti-bot deny-list + Ralph retry)</name>
  <files>.claude/skills/agentbloc/references/browser-stack.md</files>
  <read_first>
    1. `.planning/phases/11-integration-discovery-browser-fallback/11-CONTEXT.md` focus on D-48 (ALLOWED + DENY-LIST lines 166-170), D-49 (posture A/B/C lines 172-180), D-55 (Ralph retry line 182).
    2. `.planning/phases/11-integration-discovery-browser-fallback/11-PATTERNS.md` section `.claude/skills/agentbloc/references/browser-stack.md` (lines 106-148). Dual structural twin: `orchestration-patterns.md` + `frameworks.md`.
    3. `.planning/research/STACK.md` (423 lines) for rationale backing the six-row pinned stack table and the nine-package deny-list. Quote-of-record for "why this version" column values.
    4. `.claude/skills/agentbloc/references/orchestration-patterns.md` (121 lines) for the H1 + blockquote + TOC + When This Applies + curated-table-with-pattern-notes + Quick Reference structure.
    5. `.claude/skills/agentbloc/references/frameworks.md` (126 lines) for the curated-table-with-rationale column style.
  </read_first>
  <action>
Create `.claude/skills/agentbloc/references/browser-stack.md` (target 150-240 lines) following the orchestration-patterns.md + frameworks.md dual structural twin. Produce these sections in order.

1) H1 + blockquote + TOC.
   - H1: `# Browser Automation Stack`
   - Blockquote one-liner: Loaded unconditionally at SKILL.md Phase 3 entry alongside `phase-3-integration.md`, `mcp-integration-protocol.md`, `mcp-ecosystem-registry.md`, `integration-manifest-schema.md`, and `browser-fallback.md`. Declares the pinned stack, the nine-package anti-bot deny-list, the Patchright usage boundary, and the Ralph retry protocol for browser-fallback Step 4. CI enforces the deny-list via `scripts/anti-bot-lint.sh`.
   - TOC with anchors: When This Applies / Pinned Stack / Anti-Bot Deny-List / Patchright Usage Rules / Ralph Retry Protocol / Stack Variant Guidance (Posture A/B/C) / Quick Reference.

2) When This Applies. Prose paragraph: Loaded at Phase 3 entry (unconditional load-point per D-58); also read by the `browser-discovery` subagent on every invocation; also read by `scripts/anti-bot-lint.sh` via cross-reference in the script header comment. The subagent MUST NOT install or invoke any deny-listed package; on request it refuses and cites this file.

3) Pinned Stack table, six rows.
   Columns: `| Package | Pin | Release Date | Purpose | Why This Version |`
   Rows exact package names + pins per the interfaces block (playwright@^1.59.1, patchright@^1.59.4, @playwright/mcp@^0.0.70, curlconverter@^4.12.0, @har-sdk/validator@^2.6.1, fetch-har@^12.0.1). Release-date column uses "2026-03" or "2026-Q1" granularity (pull from STACK.md; if not present, use "2026-Q1" as acceptable default). Purpose column one-line each. Why-this-version column cites STACK.md rationale (e.g., for patchright: "Last compatible CDP-leak patch set for Playwright 1.59.x branch per STACK.md section on Patchright compatibility").

4) Anti-Bot Deny-List table, nine rows. Columns: `| Deny-Listed Package | Category | Why Prohibited | Alternative |`
   Rows (EXACT package names verbatim):
   - playwright-extra - Browser wrapper - Injects stealth plugins; declaring detect-and-bypass intent - Use stock `playwright@^1.59.1` plus `patchright` for Posture B CDP patches only.
   - puppeteer-extra-plugin-stealth - Stealth plugin - Fingerprint spoofing; violates detect-and-degrade - Halt on Posture C and emit DISCOVERY-BLOCKED-REPORT.md.
   - puppeteer-extra - Browser wrapper - Sibling of playwright-extra; same posture violation - Same.
   - 2captcha - CAPTCHA solver service - Paid CAPTCHA bypass; defeats the human challenge - Halt on Posture C.
   - anticaptcha - CAPTCHA solver service - Same. - Halt on Posture C.
   - deathbycaptcha - CAPTCHA solver service - Same. - Halt on Posture C.
   - capsolver - CAPTCHA solver service - Same. - Halt on Posture C.
   - puppeteer-extra-plugin-anonymize-ua - User-Agent spoofing plugin - Fingerprint spoofing - Stock User-Agent `AgentBloc-Discovery/2.0`.
   - puppeteer-extra-plugin-user-preferences - Browser-preferences spoofing - Fingerprint spoofing - None; do not spoof preferences.

5) Patchright Usage Rules. Prose block:
   - Patchright is ALLOWED only for Posture B (Cloudflare UAM or similar detected-but-navigable signals per D-49).
   - Patchright MUST NOT be invoked to spoof navigator properties, plugin lists, User-Agent, TLS JA3/JA4, or any fingerprint surface. Its ONLY sanctioned use is CDP-leak patches (hiding that a `CDP` session is attached).
   - If a scenario tempts Patchright for fingerprint adjustment, that is the signal to halt at Posture C and emit DISCOVERY-BLOCKED-REPORT.md.
   - Violating this rule is equivalent to shipping a stealth plugin and puts the user in CFAA/CMA/GDPR exposure.

6) Ralph Retry Protocol. Three sub-sections per D-55:
   - Retry budget: governance.yaml default 3; absolute hard cap 5 regardless of governance.yaml; state persisted at `.agentbloc/discovery/<service-slug>/state.json` under `retries[]`.
   - Exponential backoff schedule: attempt 1 at t=0, attempt 2 after 1s, attempt 3 after 4s, attempt 4 after 16s, attempt 5 after (capped; no attempt 6).
   - Forbidden adjustments between attempts: User-Agent change, TLS fingerprint change, header randomization, proxy rotation inside a single retry cycle, any plugin injection. ALLOWED adjustments: timing jitter, wait-for-selector extension, retry on transient 5xx, honor Retry-After header.

7) Stack Variant Guidance (Posture A/B/C). Table: `| Posture | Signal | Tool Invoked |`
   - A Friendly: OAuth login + public API docs + no WAF + ToS-GREEN. Use `playwright` + `@playwright/mcp`. Patchright NOT invoked.
   - B Detected-but-navigable: Cloudflare UAM + session cookie required + ToS-AMBER. Use `patchright` for CDP-leak patch. Same MCP surface.
   - C Hardened: DataDome, PerimeterX, Kasada, Akamai Bot Manager, CAPTCHA challenge, behavioral fingerprinting, TOS-RED. HALT. Emit DISCOVERY-BLOCKED-REPORT.md naming the detected vendor. No tool switch, no retry.

8) Quick Reference bullet list: 6 bullets summarizing stack pins, deny-list size (9 packages), Patchright scope (Posture B CDP-leak only), Ralph retry (3 attempts default, hard cap 5), posture C behavior (halt + blocked report), CI enforcement (scripts/anti-bot-lint.sh).

PROHIBITED CHARACTERS in this file:
- No em-dash characters. Use double-hyphens, "and", or colon+newline.
- No `-` forming a line-leading bullet followed by em-dash.

PROHIBITED PATTERNS:
- No "simplified version" or "v1 of" scope reductions; the deny-list MUST contain all nine packages; the stack table MUST contain all six rows.

EXPECTED LINE COUNT: 150-240 lines.

EMIT PATH: `.claude/skills/agentbloc/references/browser-stack.md`.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; wc -l .claude/skills/agentbloc/references/browser-stack.md | awk '{if ($1 &gt;= 150 &amp;&amp; $1 &lt;= 280) exit 0; else {print "line count out of band"; exit 1}}' &amp;&amp; grep -c "playwright-extra" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -c "puppeteer-extra-plugin-stealth" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -c "2captcha" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -c "anticaptcha" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -c "deathbycaptcha" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -c "capsolver" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -c "puppeteer-extra-plugin-anonymize-ua" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -c "puppeteer-extra-plugin-user-preferences" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -c "puppeteer-extra" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -q "playwright@\^1.59.1" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -q "patchright@\^1.59.4" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; grep -q "Ralph" .claude/skills/agentbloc/references/browser-stack.md &amp;&amp; [ "$(grep -c $'\u2014' .claude/skills/agentbloc/references/browser-stack.md)" -eq 0 ]</automated>
  </verify>
  <done>
    File exists at `.claude/skills/agentbloc/references/browser-stack.md`, 150-280 lines, contains all nine deny-listed package names exactly, contains the six pinned stack rows (playwright, patchright, playwright-mcp, curlconverter, har-sdk validator, fetch-har), contains Ralph retry protocol with 1s/4s/16s backoff, contains posture A/B/C stack variant guidance, zero em-dash characters. Quick Reference section summarizes the file in 6 bullets.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create legal-posture.md (jurisdictional matrix + DISCOVERY-LICENSE-NOTICE + OPT_IN_LEDGER format + attestation + tool-provider disclaimer)</name>
  <files>.claude/skills/agentbloc/references/legal-posture.md</files>
  <read_first>
    1. `.planning/phases/11-integration-discovery-browser-fallback/11-CONTEXT.md` focus on D-46 (OPT_IN_LEDGER line 154-158), D-47 (DISCOVERY-LICENSE-NOTICE line 160-162), D-54 (jurisdictional matrix lines 247-257).
    2. `.planning/phases/11-integration-discovery-browser-fallback/11-PATTERNS.md` section `.claude/skills/agentbloc/references/legal-posture.md` (lines 316-378). Dual structural twin: `frameworks.md` + `credentials.md`.
    3. `.planning/research/PITFALLS.md` (561 lines) Pitfall 1 (legal exposure) and Pitfall 2 (private-vs-public API) - direct input for the jurisdictional matrix rows + tool-provider disclaimer.
    4. `.claude/skills/agentbloc/references/frameworks.md` (126 lines) for the H1 + blockquote + TOC + curated-table-with-rationale shape.
    5. `.claude/skills/agentbloc/references/credentials.md` (117 lines) for the decision-tree step grammar + the security-reference posture that Phase 11 extends via See-line cross-reference.
  </read_first>
  <action>
Create `.claude/skills/agentbloc/references/legal-posture.md` (target 180-280 lines) following the frameworks.md + credentials.md dual structural twin. Produce these sections in order.

1) H1 + blockquote + TOC.
   - H1: `# Legal Posture for Browser Discovery`
   - Blockquote: Loaded by the `browser-discovery` subagent in its forked context on every invocation (per D-58 context-budget discipline, NOT unconditionally at Phase 3 entry). Also available for the user to audit at any time. Defines the five-jurisdiction variance matrix, the DISCOVERY-LICENSE-NOTICE.md template, the OPT_IN_LEDGER.jsonl append-only format, the user attestation protocol, and the tool-provider disclaimer AgentBloc must surface in every DISCOVERY-REPORT.md header.
   - TOC: When This Applies / Jurisdictional Variance Matrix / ToS Tier Classification Protocol / DISCOVERY-LICENSE-NOTICE.md Template / OPT_IN_LEDGER.jsonl Format / User Attestation Protocol / Tool-Provider Disclaimer / Jurisdictional Red Flags / Quick Reference.

2) When This Applies. Prose paragraph: This file is loaded by the `browser-discovery` subagent (context: fork per D-21) at the top of every discovery invocation. The user may also grep this file directly when auditing whether a particular service was discovered lawfully. Cross-reference: `credentials.md` governs credential posture once opt-in passes; this file governs the opt-in gate itself.

3) Jurisdictional Variance Matrix. Five-row markdown table, columns: `| Jurisdiction | Relevant Law | Broadest Interpretation | Safe-Harbor Condition | Highest-Risk Failure Mode |`. Rows exactly as specified in the interfaces block (US CFAA+DMCA section 1201 / UK CMA 1990 + CPS 2020 / EU GDPR Art 5(1)(a)+6 / DE BDSG section 202a / BR LGPD). Use "section 1201" not the sign character. Use "Ausspaehen von Daten" not German umlaut.
   Post-table prose (2-3 paragraphs):
   - Van Buren v. United States (2021 US Supreme Court) narrowed CFAA's "exceeds authorized access" to "gates-up-or-down", but this is NOT a blanket safe harbor; INTERNAL-HARDENED endpoints (D-53) still carry civil exposure under DMCA section 1201 if a technical access-control measure is circumvented.
   - hiQ Labs v. LinkedIn (2022 9th Cir.) established that publicly-accessible data scraping is not automatically CFAA-violating, but this decision does NOT extend to logged-in INTERNAL endpoints and does NOT address ToS breach (still a state-law contract claim).
   - EU/DE/BR rows tilt heavier on data-protection law than access law: if captured content contains third-party PII without lawful basis, the violation is in the processing, not the access. See PII redaction pipeline in `output-firewall.md`.

4) ToS Tier Classification Protocol. Decision-tree step grammar (credentials.md shape).
   - Step 1: Fetch the ToS in-session (Playwright browser_navigate + browser_snapshot), not from training memory. SHA256 the fetched excerpt and store in DISCOVERY-LICENSE-NOTICE.md.
   - Step 2: Grep the fetched excerpt for trigger keywords: `bot`, `automated`, `automation`, `scrape`, `scraping`, `reverse engineer`, `circumvent`, `bypass`, `unauthorized access`, `artificial intelligence`, `machine`, `crawler`.
   - Step 3: Classify:
     - TOS-GREEN: No trigger keywords + explicit API/developer terms section exists.
     - TOS-AMBER: Silent ToS (no trigger keywords but also no API terms section) OR ToS mentions automation only in a neutral context (e.g., "bots may be used for accessibility").
     - TOS-RED: Any trigger keyword appears in a prohibitive context (e.g., "You may not use automated means to access..."). This auto-triggers Posture-C-style halt per D-49.
   - Step 4: Data-subject scope check: does this account contain third-party PII (customer emails, employee records)? If YES, GDPR/LGPD lawful-basis determination required BEFORE opt-in attestation.
   - Step 5: Jurisdictional exposure check: ask the user their jurisdiction (ISO-3166 alpha-2) and cross-reference the Variance Matrix row. Surface highest-risk failure mode to the user in the opt-in prompt.

5) DISCOVERY-LICENSE-NOTICE.md Template (per D-47). Code block with placeholder fields:
```
# Discovery License Notice: <service-slug>

**Generated:** <ISO-8601>
**ToS URL:** <fetched-in-session URL>
**ToS Excerpt (first 2000 chars):**
<verbatim excerpt>
**ToS Excerpt SHA256:** <64-hex>
**ToS Tier:** TOS-GREEN | TOS-AMBER | TOS-RED
**Jurisdictional Banner:** <user jurisdiction + highest-risk failure mode row from Variance Matrix>

## User Attestation

I, the authorized account holder for the service at <service URL>, declare:
1. I am the account holder or have explicit written authorization from the account holder.
2. I am using AgentBloc to automate my own account activity, not a third party's.
3. I have read the ToS excerpt above and the classification.
4. My jurisdiction is <ISO-3166 alpha-2>.
5. I accept the highest-risk failure mode noted above.

**Attestation timestamp:** <ISO-8601>
**Client IP at attestation:** <IP>

## Tool-Provider Disclaimer

AgentBloc is a general-purpose research tool. The user is solely responsible for lawful use. AgentBloc takes no position on whether any specific Discovery run is lawful in the user's jurisdiction. This tool enforces opt-in gates (per D-37), anti-bot deny-list (per D-48), PII redaction (per D-52), and posture-C halt (per D-49), but legal determination rests with the user.
```
   Committed-to-repo rationale: DISCOVERY-LICENSE-NOTICE.md lives at `.agentbloc/discovery/<service-slug>/DISCOVERY-LICENSE-NOTICE.md`, committed to the user's repo (D-47). Provides GDPR Article 30 record-of-processing trail.

6) OPT_IN_LEDGER.jsonl Format (per D-46). Location: `.agentbloc/discovery/OPT_IN_LEDGER.jsonl` (project-level, one file for all services). Append-only. One JSON object per line. Schema:
```
{"service_slug":"mapfre-insurance-portal","opted_in_at":"2026-04-21T18:30:00Z","ip":"203.0.113.42","jurisdiction":"ES","tos_tier":"TOS-AMBER","tos_url":"https://mapfre.es/legal/tos","tos_excerpt_sha256":"<64-hex>","attestation":"I am the authorized account holder..."}
```
   Correction protocol: corrections require a SECOND ledger entry with `corrects_entry: <sha256 of prior line>` field. The original line is NEVER edited or deleted (append-only discipline; GDPR Article 30 requires immutable audit trail).
   Rationale: per-project (not per-user-global) resolves research Open Decision #6. Ledger survives session boundaries. Version-controlled with the rest of the repo.

7) User Attestation Protocol. Required fields for each attestation event:
   - Timestamp (ISO-8601 UTC)
   - Client IP (captured via user environment; user may set `.env` `AGENTBLOC_USER_IP`)
   - Jurisdiction (ISO-3166 alpha-2; the user states this in the opt-in prompt)
   - Scope (service_slug + ToS URL + ToS excerpt SHA256)
   Attestation text template (copy-paste from DISCOVERY-LICENSE-NOTICE.md User Attestation section). Appended to OPT_IN_LEDGER.jsonl AS the ledger line. Claude NEVER launches the browser session before the attestation line is appended (D-37 approval gate).

8) Tool-Provider Disclaimer. Copy-paste-ready boilerplate block (exact text reused in DISCOVERY-REPORT.md header by `discovery-report-schema.md`):
   `AgentBloc is a general-purpose research tool. The user is solely responsible for lawful use. AgentBloc takes no position on whether any specific Discovery run is lawful in the user's jurisdiction.`

9) Jurisdictional Red Flags. Bullet list of auto-halt conditions:
   - TOS-RED classification (auto-triggers posture-C-style halt per D-49, emit DISCOVERY-BLOCKED-REPORT.md).
   - Regulated verticals WITHOUT documented API access: healthcare (HIPAA), banking (BSA/AML), insurance (state-by-state licensing), education (FERPA). Halt + surface targeted question to user.
   - Non-US user declaration + US service with TOS-AMBER/RED: EU/DE/BR/UK user scraping US service is NOT protected by Van Buren narrow-reading; surface additional warning before opt-in.
   - ToS explicitly prohibits "artificial intelligence" or "machine" access: auto-classify TOS-RED regardless of other signals.

10) Quick Reference. Six-bullet summary: five jurisdictions documented, attestation required before browser launch, ledger is append-only, DISCOVERY-LICENSE-NOTICE.md committed per service, tool-provider disclaimer in every DISCOVERY-REPORT.md, auto-halt on TOS-RED.

PROHIBITED CHARACTERS:
- No em-dash characters. Use double-hyphens, "and", or colon+newline.
- No section-sign character (use "section" word; e.g. "CFAA section 1030").
- No German umlauts in this file's prose (use ae/oe/ue transliteration where the pattern-file references "Ausspaehen von Daten").

PROHIBITED PATTERNS:
- No "simplified jurisdictional model"; the matrix MUST have all five rows.
- No "v1 disclaimer"; the tool-provider disclaimer text is locked.

EXPECTED LINE COUNT: 180-280 lines.

EMIT PATH: `.claude/skills/agentbloc/references/legal-posture.md`.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; wc -l .claude/skills/agentbloc/references/legal-posture.md | awk '{if ($1 &gt;= 180 &amp;&amp; $1 &lt;= 320) exit 0; else {print "line count out of band"; exit 1}}' &amp;&amp; grep -q "CFAA" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "Computer Misuse Act" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "GDPR" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "BDSG" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "LGPD" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "OPT_IN_LEDGER" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "DISCOVERY-LICENSE-NOTICE" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "tool-provider disclaimer\|Tool-Provider Disclaimer" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "TOS-GREEN" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "TOS-AMBER" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "TOS-RED" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "corrects_entry" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; grep -q "credentials.md" .claude/skills/agentbloc/references/legal-posture.md &amp;&amp; [ "$(grep -c $'\u2014' .claude/skills/agentbloc/references/legal-posture.md)" -eq 0 ]</automated>
  </verify>
  <done>
    File exists at `.claude/skills/agentbloc/references/legal-posture.md`, 180-320 lines, contains the five-jurisdiction matrix (US CFAA / UK CMA / EU GDPR / DE BDSG / BR LGPD), contains DISCOVERY-LICENSE-NOTICE.md template with SHA256 + attestation + tool-provider disclaimer blocks, contains OPT_IN_LEDGER.jsonl append-only format with corrects_entry correction protocol, contains all three ToS tiers (GREEN/AMBER/RED), contains a See-line cross-reference to `credentials.md`, zero em-dash characters. Quick Reference bullet list summarizes the file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create scripts/anti-bot-lint.sh (executable) + extend .github/workflows/ci.yml with deny-list lint step</name>
  <files>scripts/anti-bot-lint.sh, .github/workflows/ci.yml</files>
  <read_first>
    1. `.planning/phases/11-integration-discovery-browser-fallback/11-CONTEXT.md` focus on D-56 (bash script text lines 264-278 + ci.yml step lines 280-284).
    2. `.planning/phases/11-integration-discovery-browser-fallback/11-PATTERNS.md` sections `scripts/anti-bot-lint.sh` (lines 538-567) + `.github/workflows/ci.yml` (lines 570-598).
    3. `/Users/pablodelarco/agentbloc/.github/workflows/ci.yml` (56 lines) for the existing 4-job structure (lint-markdown, validate-yaml, test-scenarios, check-links) and the actions/checkout@v4 convention.
  </read_first>
  <action>
Ship both artifacts as a single atomic task (they are coupled: the CI workflow references the script path).

PART A - Create `scripts/anti-bot-lint.sh`.

Content (bash, 40-80 lines, target around 55 lines including header + comments):

```bash
#!/usr/bin/env bash
#
# anti-bot-lint.sh
#
# Purpose: Enforce the nine-package anti-bot deny-list from
# .claude/skills/agentbloc/references/browser-stack.md by grepping
# every known manifest file in the repo for deny-listed package names.
#
# Exits 1 on first match with: "DENY-LIST VIOLATION: <pkg> found in <file>"
# Exits 0 on clean scan with: "anti-bot deny-list lint: clean"
#
# Runs in CI (.github/workflows/ci.yml) on every push and pull_request to main.
# Runs BEFORE any dependency install step so a poisoned manifest is caught
# before node_modules or .venv pollution.
#
# Cross-reference: references BROWSER-05 from REQUIREMENTS.md + D-48 + D-56
# from .planning/phases/11-integration-discovery-browser-fallback/11-CONTEXT.md.
#
# NO dependencies beyond POSIX bash + grep. Do NOT add node, jq, yq, or python.
#

set -euo pipefail

DENY=(
  "playwright-extra"
  "puppeteer-extra-plugin-stealth"
  "puppeteer-extra"
  "2captcha"
  "anticaptcha"
  "deathbycaptcha"
  "capsolver"
  "puppeteer-extra-plugin-anonymize-ua"
  "puppeteer-extra-plugin-user-preferences"
)

SCAN_FILES=(
  "package.json"
  ".mcp.json"
  "pyproject.toml"
  "requirements.txt"
  "Gemfile"
)

for file in "${SCAN_FILES[@]}"; do
  [ -f "$file" ] || continue
  for pkg in "${DENY[@]}"; do
    if grep -q "\"$pkg\"\|'$pkg'\|$pkg==" "$file" 2>/dev/null; then
      echo "DENY-LIST VIOLATION: $pkg found in $file"
      exit 1
    fi
  done
done

echo "anti-bot deny-list lint: clean"
```

Then run `chmod +x scripts/anti-bot-lint.sh`. Commit the executable bit (git tracks it).

NOTES:
- The script MUST NOT contain em-dash characters. The header comment above uses only ASCII hyphens and "and".
- `set -euo pipefail` is mandatory. No plain `set -e`.
- The DENY array MUST list all nine packages EXACTLY as in the `<interfaces>` block.
- The SCAN_FILES array MUST list all five manifest filenames.
- The grep pattern `"\"$pkg\"\|'$pkg'\|$pkg=="` matches double-quoted JSON keys (package.json, .mcp.json), single-quoted keys (some pyproject.toml styles), and pip-style pinned requirements (`requirements.txt`).
- The final `echo "anti-bot deny-list lint: clean"` is mandatory (downstream CI log parsing may look for it).

PART B - Extend `.github/workflows/ci.yml`.

The existing file has 4 jobs: `lint-markdown`, `validate-yaml`, `test-scenarios`, `check-links`. Add a NEW job `anti-bot-lint` (separate job per 11-PATTERNS.md rationale: separate job surfaces failure clearly in PR status checks rather than burying in another job's output).

Placement: AFTER `check-links` (last current job). The new job block:

```yaml
  anti-bot-lint:
    name: Anti-bot Deny-list Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Anti-bot deny-list lint
        run: bash scripts/anti-bot-lint.sh
```

Wiring rules:
- The job has a SINGLE step that runs the script. No dependency install precedes it; checkout@v4 is the only preparation needed.
- The step name is `Anti-bot deny-list lint` EXACTLY (the plan's grep check greps for this literal string in the YAML).
- The job key `anti-bot-lint` MUST NOT contain uppercase letters; GitHub Actions allows it but the project convention uses lowercase-hyphen keys.
- Do NOT modify any of the other 4 jobs.
- The trailing newline of the YAML file MUST be preserved.

PROHIBITED:
- Do NOT add the step as a new step INSIDE `lint-markdown` or any other existing job.
- Do NOT use a matrix strategy; this is a single-OS lint.
- Do NOT add `continue-on-error: true`; the job MUST block the PR on violation.
- Do NOT add em-dash characters anywhere in ci.yml (the existing file is clean; preserve this).

ORDER of operations for the executor:
1) Write `scripts/anti-bot-lint.sh` with the content above.
2) Run `chmod +x scripts/anti-bot-lint.sh` via Bash tool.
3) Edit `.github/workflows/ci.yml` by appending the new job block after the `check-links` job.
4) Run the lint locally to self-verify: `bash scripts/anti-bot-lint.sh` should print `anti-bot deny-list lint: clean` and exit 0.
5) Run a synthetic negative test: create a temp file `/tmp/synthetic-package.json` with `"playwright-extra": "^1.0.0"` and run `cd /tmp && bash $OLDPWD/scripts/anti-bot-lint.sh` to confirm it exits 1 with the expected message. DELETE the synthetic file afterward. Do NOT leave it in the repo.
  </action>
  <verify>
    <automated>test -x scripts/anti-bot-lint.sh &amp;&amp; bash scripts/anti-bot-lint.sh &amp;&amp; grep -q "Anti-bot deny-list lint" .github/workflows/ci.yml &amp;&amp; grep -q "bash scripts/anti-bot-lint.sh" .github/workflows/ci.yml &amp;&amp; grep -q "anti-bot-lint:" .github/workflows/ci.yml &amp;&amp; grep -qc "playwright-extra" scripts/anti-bot-lint.sh &amp;&amp; grep -qc "puppeteer-extra-plugin-stealth" scripts/anti-bot-lint.sh &amp;&amp; grep -qc "2captcha" scripts/anti-bot-lint.sh &amp;&amp; grep -qc "anticaptcha" scripts/anti-bot-lint.sh &amp;&amp; grep -qc "deathbycaptcha" scripts/anti-bot-lint.sh &amp;&amp; grep -qc "capsolver" scripts/anti-bot-lint.sh &amp;&amp; grep -qc "puppeteer-extra-plugin-anonymize-ua" scripts/anti-bot-lint.sh &amp;&amp; grep -qc "puppeteer-extra-plugin-user-preferences" scripts/anti-bot-lint.sh &amp;&amp; grep -qc "puppeteer-extra" scripts/anti-bot-lint.sh &amp;&amp; grep -q "set -euo pipefail" scripts/anti-bot-lint.sh &amp;&amp; grep -q "SCAN_FILES" scripts/anti-bot-lint.sh &amp;&amp; [ "$(grep -c $'\u2014' scripts/anti-bot-lint.sh)" -eq 0 ] &amp;&amp; [ "$(grep -c $'\u2014' .github/workflows/ci.yml)" -eq 0 ] &amp;&amp; printf '{"dependencies":{"playwright-extra":"^1.0.0"}}' &gt; /tmp/synthetic-package.json &amp;&amp; ( cd /tmp &amp;&amp; bash "$OLDPWD/scripts/anti-bot-lint.sh" 2&gt;&amp;1 | grep -q "DENY-LIST VIOLATION: playwright-extra" ); rc=$?; rm -f /tmp/synthetic-package.json; [ $rc -eq 0 ]</automated>
  </verify>
  <done>
    `scripts/anti-bot-lint.sh` exists with executable bit set, contains all nine deny-listed package names and all five scanned manifest filenames, uses `set -euo pipefail`, exits 0 with "anti-bot deny-list lint: clean" on clean repo and exits 1 with "DENY-LIST VIOLATION: <pkg> found in <file>" on a synthetic poisoned package.json; `.github/workflows/ci.yml` contains a new `anti-bot-lint` job with step name "Anti-bot deny-list lint" invoking `bash scripts/anti-bot-lint.sh`; neither file contains em-dash characters.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-author -> repo manifest | A contributor (human or AI) proposes a dependency that would install a stealth plugin or CAPTCHA solver. Must be blocked in CI before merge. |
| CI runner -> installed dependencies | Once `npm install` or `pip install` runs, a poisoned manifest becomes resident on disk. Lint MUST run BEFORE install step. |
| browser-discovery subagent -> Patchright | Patchright is ALLOWED for Posture B CDP-leak patches only. A prompt-injected request to "also spoof User-Agent" must be refused; prose rule in browser-stack.md is the guardrail. |
| user -> browser-discovery subagent | Before launch, the user must sign DISCOVERY-LICENSE-NOTICE.md and the attestation line must append to OPT_IN_LEDGER.jsonl. Legal classification must match the user's jurisdiction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering / Elevation | `.github/workflows/ci.yml` CI pipeline step order | mitigate | Place `anti-bot-lint` job as a SEPARATE top-level job with only `actions/checkout@v4` preceding the lint step. No `npm install` or `pip install` precedes it. Lint runs on ubuntu-latest with no third-party actions beyond checkout. Bypass would require editing the workflow itself, which is PR-reviewable. Per D-56. |
| T-11-06 | Tampering | `package.json` / `.mcp.json` / `pyproject.toml` / `requirements.txt` / `Gemfile` manifest files | mitigate | `scripts/anti-bot-lint.sh` greps all five manifest filenames for the nine deny-listed package names on every push and pull_request to main. Exits 1 on any match, blocking the PR. Per D-48 + D-56. Belt-and-suspenders: browser-stack.md prose rule instructs the browser-discovery subagent to refuse any install request citing a deny-listed name. |
| T-11-07 | Spoofing / Tampering | Patchright usage surface inside browser-discovery subagent | mitigate | browser-stack.md Patchright Usage Rules section documents the ABSOLUTE boundary: Patchright is for Posture B CDP-leak patches ONLY, NEVER for User-Agent, navigator, plugins, or TLS spoofing. If a scenario tempts fingerprint adjustment, halt at Posture C and emit DISCOVERY-BLOCKED-REPORT.md. Per D-48 + D-49. Ralph retry protocol explicitly forbids fingerprint change between retries (D-55); only timing jitter is allowed. |
| T-11-08 | Repudiation / Information Disclosure | Jurisdictional classification in DISCOVERY-LICENSE-NOTICE.md | mitigate | legal-posture.md ships a five-row jurisdictional variance matrix covering US/UK/EU/DE/BR. User states their jurisdiction (ISO-3166 alpha-2) at attestation time; Claude surfaces the matching highest-risk failure mode before browser launch. TOS-RED classification auto-halts. Regulated-vertical services without documented API access auto-halt. Non-US user + US service with TOS-AMBER/RED surfaces additional warning. Tool-provider disclaimer is included in every DISCOVERY-REPORT.md header. OPT_IN_LEDGER.jsonl append-only record (D-46) supports GDPR Article 30 record-of-processing audit trail. Per D-47 + D-54. |
</threat_model>

<verification>
Phase-level automated checks for this plan:

1. `test -f .claude/skills/agentbloc/references/browser-stack.md` passes.
2. `test -f .claude/skills/agentbloc/references/legal-posture.md` passes.
3. `test -x scripts/anti-bot-lint.sh` passes (executable bit set).
4. `bash scripts/anti-bot-lint.sh` exits 0 with `anti-bot deny-list lint: clean` output.
5. Synthetic poisoned package.json test: `printf '{"dependencies":{"playwright-extra":"^1.0.0"}}' > /tmp/synthetic-package.json && cd /tmp && bash $OLDPWD/scripts/anti-bot-lint.sh` exits 1 with `DENY-LIST VIOLATION: playwright-extra found in package.json` message.
6. `grep -q "Anti-bot deny-list lint" .github/workflows/ci.yml` passes.
7. `grep -q "bash scripts/anti-bot-lint.sh" .github/workflows/ci.yml` passes.
8. All nine deny-listed package names appear in both `browser-stack.md` AND `scripts/anti-bot-lint.sh`.
9. All five jurisdictional rows (US / UK / EU / DE / BR) appear in `legal-posture.md`.
10. Zero em-dash characters in `browser-stack.md`, `legal-posture.md`, `scripts/anti-bot-lint.sh`, and the added portion of `.github/workflows/ci.yml`.
11. `browser-stack.md` line count in [150, 280]; `legal-posture.md` line count in [180, 320]; `scripts/anti-bot-lint.sh` line count in [40, 80].
</verification>

<success_criteria>
- BROWSER-05 satisfied: CI deny-list lint rejects any of the nine deny-listed packages in any of the five scanned manifests; wired into `.github/workflows/ci.yml` on push + pull_request to main.
- BROWSER-06 satisfied: Patchright pin `^1.59.4` documented in browser-stack.md Pinned Stack table with Patchright Usage Rules explicitly prohibiting fingerprint spoofing and scoping to Posture B CDP-leak patches only.
- BROWSER-07 satisfied: All six stack pins captured in browser-stack.md Pinned Stack table (playwright, patchright, @playwright/mcp, curlconverter, @har-sdk/validator, fetch-har) with version + release-date + why-this-version columns.
- BROWSER-09 satisfied: Ralph retry protocol documented in browser-stack.md (1s/4s/16s exponential backoff, hard cap 5 attempts, no fingerprint adjustment between retries, state persisted to `.agentbloc/discovery/<service-slug>/state.json` retries[]).
- BROWSER-12 satisfied: Five-row jurisdictional variance matrix (US CFAA+DMCA / UK CMA / EU GDPR / DE BDSG / BR LGPD) in legal-posture.md plus DISCOVERY-LICENSE-NOTICE.md template plus OPT_IN_LEDGER.jsonl append-only format plus tool-provider disclaimer boilerplate.
- Zero em-dash characters in any file produced or modified by this plan.
- All four files committed to git with the commit message `feat(11): emit 11-02-PLAN.md - stack + legal + CI anti-bot lint`.
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-discovery-browser-fallback/11-02-SUMMARY.md` summarizing: files created (browser-stack.md + legal-posture.md + scripts/anti-bot-lint.sh), files modified (.github/workflows/ci.yml), requirements closed (BROWSER-05, BROWSER-06, BROWSER-07, BROWSER-09, BROWSER-12), CI lint behavior (exits 0 on clean, 1 on poisoned manifest), and the next downstream plan (11-03 subagent definition, 11-04 wiring).
</output>
