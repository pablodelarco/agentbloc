---
phase: 11-integration-discovery-browser-fallback
plan: 04
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
  - 11-03
files_modified:
  - .claude/skills/agentbloc/references/phase-3-integration.md
  - .claude/skills/agentbloc/SKILL.md
autonomous: true
requirements:
  - BROWSER-01

must_haves:
  truths:
    - "references/phase-3-integration.md Priority 3 heading unmarks the Phase 11 scope marker: new heading is `### Priority 3: Playwright Browser Automation (Four-Step Fallback)` (was `### Priority 3: Playwright Browser Automation [Phase 11 scope]`)"
    - "references/phase-3-integration.md Priority 3 body replaces the forward-looking stub paragraph with a concrete two-sentence paragraph citing browser-fallback.md (Step 4 canonical protocol) AND browser-stack.md (pinned versions + anti-bot deny-list + scripts/anti-bot-lint.sh CI enforcement) AND DISCOVERY-BLOCKED-REPORT.md for Posture C halts per BROWSER-09"
    - "references/phase-3-integration.md Priority 3 preserves the v1.0 Summary bullet block verbatim (login portal / standard web forms / 2FA / CAPTCHA / microsoft/playwright-mcp)"
    - "references/phase-3-integration.md Steps 3-7 + Integration Gate + Quick Reference untouched (preservation per D-40 surgical-edit discipline)"
    - "SKILL.md Phase 3 entry See-line load-list extended from 4 to 6 See-lines: adds `browser-fallback.md` + `browser-stack.md` appended AFTER `integration-manifest-schema.md`, in order (D-58 context-budget-conscious addition)"
    - "SKILL.md Phase 3 `You MUST read...` connector sentence rewritten to match the extended load-list: appends `AND the browser-fallback protocol AND the browser stack reference`"
    - "SKILL.md does NOT load `discovery-report-schema.md`, `output-firewall.md`, or `legal-posture.md` at Phase 3 entry (D-58 context-budget discipline , these load only in the browser-discovery subagent's forked context on invocation)"
    - "SKILL.md Phase 3 existing content preserved verbatim: existing Precondition paragraph (agent-profiles.yaml validation), existing Summary Gate paragraph (Verification Loop + D-14 mirror), existing 4 See-lines for phase-3-integration.md + mcp-integration-protocol.md + mcp-ecosystem-registry.md + integration-manifest-schema.md"
    - "SKILL.md Phase 1 / Phase 2 / Phase 4 / Phase 5 / Phase 6 entries + Hard Gates + Language and Technical Level + Quality Checklist + Reference Implementation + State Transitions section all preserved verbatim; NO new sub-gate added (browser fallback is a sub-path of existing `mcp_integrations_verified` per D-58)"
    - "phase-3-integration.md total line count stays between 388 and 430 lines (current 398 + budget of ~+12 lines for the three surgical edits inside Priority 3 = ~410)"
    - "SKILL.md total line count stays between 178 and 195 lines (current 178 + budget of ~+3 for 2 See-lines + 1 connector-sentence rewrite = ~181)"
    - "Zero new em-dashes introduced in either file (project convention: no em-dashes in text per CLAUDE.md)"
  artifacts:
    - path: ".claude/skills/agentbloc/references/phase-3-integration.md"
      provides: "Phase 3 integration protocol with Priority 3 Four-Step Fallback wiring: heading unmarked, forward See-line replaced with concrete reference to browser-fallback.md + browser-stack.md + DISCOVERY-BLOCKED-REPORT.md, v1.0 Summary bullets preserved. Steps 3-7 + Integration Gate + Quick Reference preserved verbatim."
      min_lines: 388
      contains: "(Four-Step Fallback)"
    - path: ".claude/skills/agentbloc/SKILL.md"
      provides: "Phase 3 entry loads browser-fallback.md + browser-stack.md unconditionally (6 total See-lines at Phase 3). Other 3 Phase 11 references (discovery-report-schema.md, output-firewall.md, legal-posture.md) load only in browser-discovery subagent forked context , NOT at Phase 3 entry per D-58 context budget discipline."
      min_lines: 178
      contains: "browser-fallback.md"
  key_links:
    - from: ".claude/skills/agentbloc/references/phase-3-integration.md"
      to: ".claude/skills/agentbloc/references/browser-fallback.md"
      via: "Priority 3 Four-Step Fallback See-line , canonical Step 4 protocol"
      pattern: "browser-fallback\\.md"
    - from: ".claude/skills/agentbloc/references/phase-3-integration.md"
      to: ".claude/skills/agentbloc/references/browser-stack.md"
      via: "Priority 3 Four-Step Fallback See-line , pinned versions + anti-bot deny-list"
      pattern: "browser-stack\\.md"
    - from: ".claude/skills/agentbloc/references/phase-3-integration.md"
      to: "scripts/anti-bot-lint.sh"
      via: "Priority 3 body names the CI deny-list enforcement script per BROWSER-05"
      pattern: "scripts/anti-bot-lint\\.sh"
    - from: ".claude/skills/agentbloc/references/phase-3-integration.md"
      to: ".agentbloc/discovery/<service-slug>/DISCOVERY-BLOCKED-REPORT.md"
      via: "Priority 3 body names the Posture C halt artifact per BROWSER-09"
      pattern: "DISCOVERY-BLOCKED-REPORT\\.md"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/browser-fallback.md"
      via: "unconditional load at Phase 3 entry (D-58) , imperative Step 4 protocol"
      pattern: "browser-fallback\\.md"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/browser-stack.md"
      via: "unconditional load at Phase 3 entry (D-58) , stack pins + deny-list"
      pattern: "browser-stack\\.md"
---

<objective>
Wire the Plan 11-01 (browser-fallback.md), Plan 11-02 (browser-stack.md + scripts/anti-bot-lint.sh + CI workflow), and Plan 11-03 (browser-discovery subagent) artifacts into the existing Phase 3 integration protocol and SKILL.md gate ritual. Two surgical file edits: three-edit pattern in phase-3-integration.md Priority 3 (heading unmark + paragraph replacement + summary preservation) per D-57, and one-edit pattern in SKILL.md Phase 3 entry (See-line load-list extension with 2 new references + connector sentence rewrite) per D-58.

Purpose: Phase 10 stubbed a `[Phase 11 scope]` marker on `phase-3-integration.md` Priority 3 with a forward See-line to `browser-fallback.md`. Plan 11-04 is the atomic unit that closes that contract , unmarks the stub, replaces the forward See-line with a concrete paragraph citing the three new Wave 1-2 references, and preserves the v1.0 Summary bullets. ROADMAP Phase 11 success criterion 1 (legal opt-in enforcement wired into user flow) requires the Phase 3 entry loads `browser-fallback.md` so Claude reads Step 1 (per-service legal opt-in gate) before ever invoking the browser-discovery subagent. Success criterion 3 (CI deny-list active) is surfaced in the Priority 3 paragraph by naming `scripts/anti-bot-lint.sh`. Context-budget discipline per Phase 10 plan-eng-review P-1: only 2 new refs load unconditionally at Phase 3 entry; the other 3 Phase 11 references (discovery-report-schema.md, output-firewall.md, legal-posture.md) load only in the browser-discovery subagent's forked context on invocation.

Output: Three surgical edits inside Priority 3 of `references/phase-3-integration.md` (heading unmark + paragraph replacement + preservation of v1.0 Summary bullets verbatim) and one surgical edit in `SKILL.md` Phase 3 entry See-line load-list (append 2 new See-lines + rewrite the connector sentence). `phase-3-integration.md` grows from 398 to ~410 lines (within 388-430 budget); `SKILL.md` grows from 178 to ~181 lines (within 178-195 budget, well under the 250-line v1.0 cap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-integration-discovery-browser-fallback/11-CONTEXT.md
@.planning/phases/11-integration-discovery-browser-fallback/11-PATTERNS.md
@.planning/phases/11-integration-discovery-browser-fallback/11-01-SUMMARY.md
@.planning/phases/11-integration-discovery-browser-fallback/11-02-SUMMARY.md
@.planning/phases/11-integration-discovery-browser-fallback/11-03-SUMMARY.md
@.planning/phases/10-integration-discovery-mcp-path/10-03-PLAN.md
@.planning/phases/10-integration-discovery-mcp-path/10-03-SUMMARY.md

@.claude/skills/agentbloc/SKILL.md
@.claude/skills/agentbloc/references/phase-3-integration.md

<interfaces>
<!-- D-57 surgical edit targets in phase-3-integration.md (current 398 lines post-Phase-10). -->
<!-- Edit scope: Priority 3 section only (current lines 92-100). -->
<!-- Preserve verbatim: lines 1-91 (including new Priority 1 MCP Server + new Priority 2 Official API per Phase 10) AND lines 101+ (Priority 4 Email Scraping through Steps 3-7 / Integration Gate / Quick Reference). -->

<!-- D-58 surgical edit target in SKILL.md (current 178 lines post-Phase-10). -->
<!-- Edit scope: Phase 3 entry See-line load-list block (current lines 121-125). -->
<!-- Preserve verbatim: Phase 3 entry Precondition (line 117), Summary Gate paragraph (line 119), existing 4 See-lines (lines 122-125). Append 2 new See-lines after line 125. Rewrite connector sentence on line 121. -->

<!-- Current state of phase-3-integration.md Priority 3 (lines 92-100, verified via Read before planning): -->

Current lines 92-100:
```
### Priority 3: Playwright Browser Automation [Phase 11 scope]

See forthcoming [references/browser-fallback.md](browser-fallback.md) (Phase 11 BROWSER-01..12) for the full Patchright + HAR capture + injection detector + PII redaction protocol. Phase 10 stubs this priority; Phase 11 wires it in.

**Summary (v1.0, preserved):**
- If no API or MCP server exists, WebSearch for `{service_name} login portal` or `{service_name} web dashboard`
- Whether the portal uses standard web forms (automatable)
- Whether 2FA/CAPTCHA is required (complicates automation)
- Playwright MCP (microsoft/playwright-mcp) is the automation tool -- HIGH trust, Microsoft-maintained
```

After Plan 11-04 Task 1 edit (heading unmark + paragraph replacement + v1.0 bullets preserved):
```
### Priority 3: Playwright Browser Automation (Four-Step Fallback)

See [references/browser-fallback.md](browser-fallback.md) for the canonical Step 4 protocol: per-service legal opt-in → subagent invocation → HAR capture with checkpoint → endpoint classification → output firewall → DISCOVERY-REPORT.md emission. See [references/browser-stack.md](browser-stack.md) for pinned versions (playwright@^1.59.1 + patchright@^1.59.4 for Posture B CDP-leak patches only) + anti-bot deny-list (9 forbidden packages including playwright-extra, CAPTCHA solvers, fingerprint spoofers , enforced by CI via `scripts/anti-bot-lint.sh`). Posture C (hardened anti-bot: DataDome / PerimeterX / CAPTCHA challenge) always halts cleanly via DISCOVERY-BLOCKED-REPORT.md per BROWSER-09 , no bypass attempts.

**Summary (v1.0, preserved):**
- If no API or MCP server exists, WebSearch for `{service_name} login portal` or `{service_name} web dashboard`
- Whether the portal uses standard web forms (automatable)
- Whether 2FA/CAPTCHA is required (complicates automation)
- Playwright MCP (microsoft/playwright-mcp) is the automation tool -- HIGH trust, Microsoft-maintained
```

Net delta on phase-3-integration.md: +12 lines (new paragraph has 5 logical sentences that wrap to ~3 physical lines in an editor but count as 1 content line; the real growth is the prose expansion + 2 new See-links + posture note). Current 398 + 12 = ~410 lines.

<!-- Current state of SKILL.md Phase 3 entry See-line block (lines 121-125, verified via Read before planning): -->

Current lines 121-125:
```
You MUST read the complete integration analysis protocol AND the MCP integration protocol AND the ecosystem registry AND the integration manifest schema before starting this phase:
See [references/phase-3-integration.md](references/phase-3-integration.md)
See [references/mcp-integration-protocol.md](references/mcp-integration-protocol.md)
See [references/mcp-ecosystem-registry.md](references/mcp-ecosystem-registry.md)
See [references/integration-manifest-schema.md](references/integration-manifest-schema.md)
```

After Plan 11-04 Task 2 edit (connector rewritten + 2 new See-lines appended in order):
```
You MUST read the complete integration analysis protocol AND the MCP integration protocol AND the ecosystem registry AND the integration manifest schema AND the browser-fallback protocol AND the browser stack reference before starting this phase:
See [references/phase-3-integration.md](references/phase-3-integration.md)
See [references/mcp-integration-protocol.md](references/mcp-integration-protocol.md)
See [references/mcp-ecosystem-registry.md](references/mcp-ecosystem-registry.md)
See [references/integration-manifest-schema.md](references/integration-manifest-schema.md)
See [references/browser-fallback.md](references/browser-fallback.md)
See [references/browser-stack.md](references/browser-stack.md)
```

Net delta on SKILL.md: +2 lines (2 new See-lines) + 0 (connector sentence rewritten in place, same line count). Current 178 + 2 = 180 lines.

<!-- D-58 context-budget discipline (critical , do NOT add what is not on this list): -->
<!-- discovery-report-schema.md: NOT loaded at Phase 3 entry. Loaded only by the browser-discovery subagent in its forked context on invocation (per D-44 table in 11-CONTEXT.md). -->
<!-- output-firewall.md: NOT loaded at Phase 3 entry. Loaded only by the browser-discovery subagent in its forked context on invocation. -->
<!-- legal-posture.md: NOT loaded at Phase 3 entry. Loaded only by the browser-discovery subagent in its forked context on invocation + available for user audit. -->
<!-- Rationale: Phase 3 unconditional load trend line is ~1,230 lines post-Phase-11 (vs ~1,800+ if all 5 refs loaded unconditionally). Phase 10 plan-eng-review P-1 observation. -->

<!-- D-58 gate vocabulary (critical , do NOT add what is not already there): -->
<!-- NO new sub-gate added to State Transitions. Browser fallback is a sub-path of the existing `mcp_integrations_verified` sub-gate. A manifest entry with resolution_method: browser-fallback becomes status: verified when DISCOVERY-REPORT.md exists + validates + SHA256 matches. -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase-3-integration.md surgical edit | 398-line file with v1.0 content AND Phase-10-produced content being preserved; edit scope is 9 lines inside Priority 3 only; Steps 3-7 + Integration Gate + Quick Reference must not be touched |
| SKILL.md surgical edit | 178-line file under 250-line v1.0 cap; edit scope is 5 lines inside Phase 3 entry See-line block only; Phase 1/Phase 2/Phase 4/Phase 5/Phase 6 entries + Hard Gates + State Transitions + Quality Checklist must not be touched |
| Browser-fallback forward-link resolution | Phase 10 left a broken forward See-line to `browser-fallback.md`; Plans 11-01/02/03 create the target files; Plan 11-04 must run AFTER Waves 1-2 so the cited references exist at time of citation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | T (tampering) | Surgical edit deletes v1.0 content accidentally (e.g., loses the 4-bullet Summary block, or the heading ### Priority 3 gets malformed) | mitigate | Each edit uses the Edit tool's `old_string` / `new_string` pattern with EXACT matching multi-line context; Task 1 acceptance criteria asserts all 4 Summary bullets present verbatim (`grep -q "login portal"`, `grep -q "2FA"`, `grep -q "microsoft/playwright-mcp"`); Steps 3-7 preservation greps (`^## Step 3: Evidence Verification` etc.) catch any accidental region-wide replacement; line-count bound 388-430 catches over-deletion or over-insertion |
| T-11-15 | D (denial of service) | SKILL.md exceeds 195-line D-58 budget due to cumulative edits | mitigate | Budget cap: 178 + 3 target = 181 lines; acceptance criterion strict upper bound 195 (safety margin of 14 lines); `wc -l` grep-level check; well under 250-line v1.0 cap regardless |
| T-11-16 | I (information disclosure) | `discovery-report-schema.md` / `output-firewall.md` / `legal-posture.md` accidentally loaded at Phase 3 entry , blows context budget + surfaces legal/security content to main session that was intended for subagent fork only | mitigate | D-58 explicit context-budget discipline + grep-for-absence acceptance criteria: `grep -q "discovery-report-schema\.md" .claude/skills/agentbloc/SKILL.md` MUST return exit 1; same for output-firewall.md and legal-posture.md; any match = test fail = edit must be reapplied correctly |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Surgical edit to references/phase-3-integration.md Priority 3 , heading unmark + paragraph replacement + v1.0 Summary bullets preserved verbatim</name>
  <files>.claude/skills/agentbloc/references/phase-3-integration.md</files>
  <read_first>
    - .planning/phases/11-integration-discovery-browser-fallback/11-CONTEXT.md (D-57 three-edit specification for Priority 3 unmark + paragraph replacement; lines 290-302 are the locked Before/After snippet)
    - .planning/phases/11-integration-discovery-browser-fallback/11-PATTERNS.md (lines 602-638 , the "references/phase-3-integration.md (surgical edit)" section citing Phase 10 commit 28050c4 as line-for-line precedent)
    - .claude/skills/agentbloc/references/phase-3-integration.md (the FULL 398-line current file , read it in its entirety before editing so you know Priority 3 is at lines 92-100 and nothing before or after those lines must change)
    - .claude/skills/agentbloc/references/browser-fallback.md (the target of the new See-line , verify this exists from Plan 11-01 before running Task 1 so the citation resolves)
    - .claude/skills/agentbloc/references/browser-stack.md (the target of the new See-line , verify this exists from Plan 11-02 before running Task 1 so the citation resolves)
    - .planning/phases/10-integration-discovery-mcp-path/10-03-PLAN.md (line-for-line analog , Task 1 Edit 3 in that plan inserted the [Phase 11 scope] marker that this plan now unmarks; structural twin of the surgical-edit discipline)
    - .planning/REQUIREMENTS.md (BROWSER-01: subagent invocation wired into user-facing flow via phase-3-integration.md Priority 3)
  </read_first>
  <action>
Apply EXACTLY ONE surgical edit to `.claude/skills/agentbloc/references/phase-3-integration.md` using the Edit tool. The edit covers 9 contiguous lines (current lines 92-100) and replaces them with ~12 lines (heading unmark + expanded paragraph + preserved Summary block). Read the full current file first (398 lines) to confirm you have correct `old_string` context.

**Single combined Edit , Priority 3 section: heading unmark + forward-See-line paragraph replacement + v1.0 Summary bullets preserved.**

Use the Edit tool with this `old_string` (this is the EXACT current Priority 3 section, 9 lines as seen in the file today):

```
### Priority 3: Playwright Browser Automation [Phase 11 scope]

See forthcoming [references/browser-fallback.md](browser-fallback.md) (Phase 11 BROWSER-01..12) for the full Patchright + HAR capture + injection detector + PII redaction protocol. Phase 10 stubs this priority; Phase 11 wires it in.

**Summary (v1.0, preserved):**
- If no API or MCP server exists, WebSearch for `{service_name} login portal` or `{service_name} web dashboard`
- Whether the portal uses standard web forms (automatable)
- Whether 2FA/CAPTCHA is required (complicates automation)
- Playwright MCP (microsoft/playwright-mcp) is the automation tool -- HIGH trust, Microsoft-maintained
```

And this `new_string` (heading descriptor replaces the [Phase 11 scope] marker; paragraph replaced with concrete two-sentence reference to browser-fallback.md + browser-stack.md + BROWSER-09 halt artifact + scripts/anti-bot-lint.sh; v1.0 Summary block preserved verbatim):

```
### Priority 3: Playwright Browser Automation (Four-Step Fallback)

See [references/browser-fallback.md](browser-fallback.md) for the canonical Step 4 protocol: per-service legal opt-in → subagent invocation → HAR capture with checkpoint → endpoint classification → output firewall → DISCOVERY-REPORT.md emission. See [references/browser-stack.md](browser-stack.md) for pinned versions (playwright@^1.59.1 + patchright@^1.59.4 for Posture B CDP-leak patches only) + anti-bot deny-list (9 forbidden packages including playwright-extra, CAPTCHA solvers, fingerprint spoofers , enforced by CI via `scripts/anti-bot-lint.sh`). Posture C (hardened anti-bot: DataDome / PerimeterX / CAPTCHA challenge) always halts cleanly via DISCOVERY-BLOCKED-REPORT.md per BROWSER-09 , no bypass attempts.

**Summary (v1.0, preserved):**
- If no API or MCP server exists, WebSearch for `{service_name} login portal` or `{service_name} web dashboard`
- Whether the portal uses standard web forms (automatable)
- Whether 2FA/CAPTCHA is required (complicates automation)
- Playwright MCP (microsoft/playwright-mcp) is the automation tool -- HIGH trust, Microsoft-maintained
```

The edit (a) removes the `[Phase 11 scope]` marker from the heading and replaces it with the descriptor `(Four-Step Fallback)`, (b) replaces the 1-sentence forward See-line paragraph with a 3-sentence concrete paragraph citing `browser-fallback.md` (canonical Step 4 protocol with 6-step flow enumeration) + `browser-stack.md` (pinned versions + 9-package deny-list + CI script path) + Posture C halt artifact (DISCOVERY-BLOCKED-REPORT.md per BROWSER-09), (c) preserves the v1.0 Summary block verbatim , all 4 bullets + the `**Summary (v1.0, preserved):**` header stay byte-for-byte identical.

**Preservation requirements (critical):**
- Do NOT edit anything before line 91 (the blank line after the Priority 2 "SDK availability" bullet stays intact as the separator before Priority 3).
- Do NOT edit anything after line 100 (the blank line before `### Priority 4: Email Scraping` stays intact).
- Verify after editing that Priorities 4, 5, 6 still exist: `grep -q "^### Priority 4: Email Scraping$"`, `grep -q "^### Priority 5: Webhook Interception$"`, `grep -q "^### Priority 6:"`.
- Verify Steps 3-7 headers still exist: `grep -q "^## Step 3: Evidence Verification$"`, `grep -q "^## Step 4: Trust Scoring$"`, `grep -q "^## Step 5: Decision Matrix Construction$"`, `grep -q "^## Step 6: Security Cross-Reference$"`, `grep -q "^## Step 7: Integration Presentation and Approval$"`.
- Verify Integration Gate + Quick Reference still exist: `grep -q "^## Integration Gate$"`, `grep -q "^## Quick Reference$"`.
- Verify Phase-10-produced content still intact: `grep -q "^### Priority 1: MCP Server (Four-Step Search)$"`, `grep -q "^### Priority 2: Official API$"`.
- Verify file is between 388 and 430 lines: the net delta of this edit is ~+12 lines (1 old heading line + 1 old paragraph line replaced with 1 new heading line + 1 new paragraph line of ~3x the character length; 4 v1.0 Summary bullets stay the same). Current 398 + 12 = ~410, well within budget.
- The Unicode arrows (`→`) in the new paragraph's Step 4 enumeration are ALLOWED (they are not em-dashes and match box-drawing usage in other references like mcp-integration-protocol.md). Em-dashes (`,`) ARE present intentionally in the parenthetical "`playwright-extra, CAPTCHA solvers, fingerprint spoofers , enforced by CI via`" and "`no bypass attempts.`" , these are the em-dashes already in the locked D-57 snippet. Per CLAUDE.md "Never put a , in your text" this is a conflict: the surgical edit must use the EXACT D-57 locked text (including the em-dashes) because D-57 is locked. The line `grep -c "," .claude/skills/agentbloc/references/phase-3-integration.md` will now be 2 (up from 0). This is a documented exception for this surgical edit only, justified because the D-57 snippet is a locked decision from CONTEXT.md and Plan 11-04's scope is "surgical edit to exact D-57 text." If the executor wishes to eliminate em-dashes they must replace them with `--` (two hyphens) in BOTH occurrences of the new_string above and note the deviation in the SUMMARY; otherwise ship as locked.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "^### Priority 3: Playwright Browser Automation (Four-Step Fallback)$" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "browser-fallback\.md" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "browser-stack\.md" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "DISCOVERY-BLOCKED-REPORT\.md" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "scripts/anti-bot-lint\.sh" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; ! grep -q "\[Phase 11 scope\]" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "^## Step 3: Evidence Verification" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "^## Step 4: Trust Scoring" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "^## Step 5: Decision Matrix Construction" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "login portal" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "microsoft/playwright-mcp" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; LINES=$(wc -l &lt; .claude/skills/agentbloc/references/phase-3-integration.md) &amp;&amp; [ "$LINES" -ge 388 ] &amp;&amp; [ "$LINES" -le 430 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/references/phase-3-integration.md` exits 0
    - NEW Priority 3 heading present: `grep -q "^### Priority 3: Playwright Browser Automation (Four-Step Fallback)$" .claude/skills/agentbloc/references/phase-3-integration.md` matches
    - OLD [Phase 11 scope] marker GONE: `grep -q "\[Phase 11 scope\]" .claude/skills/agentbloc/references/phase-3-integration.md` returns exit 1 (no match)
    - OLD forward-stub paragraph GONE: `grep -q "Phase 10 stubs this priority" .claude/skills/agentbloc/references/phase-3-integration.md` returns exit 1
    - OLD "forthcoming" language GONE: `grep -q "forthcoming" .claude/skills/agentbloc/references/phase-3-integration.md` returns exit 1
    - NEW references cited: `grep -q "browser-fallback\.md" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "browser-stack\.md" .claude/skills/agentbloc/references/phase-3-integration.md`
    - NEW CI script cited: `grep -q "scripts/anti-bot-lint\.sh" .claude/skills/agentbloc/references/phase-3-integration.md`
    - NEW Posture C halt artifact cited: `grep -q "DISCOVERY-BLOCKED-REPORT\.md" .claude/skills/agentbloc/references/phase-3-integration.md`
    - NEW BROWSER-09 citation present: `grep -q "BROWSER-09" .claude/skills/agentbloc/references/phase-3-integration.md`
    - NEW 6-step protocol enumeration present: `grep -q "per-service legal opt-in" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "HAR capture with checkpoint" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "DISCOVERY-REPORT\.md emission" .claude/skills/agentbloc/references/phase-3-integration.md`
    - NEW pinned-version prose present: `grep -q "playwright@\^1\.59\.1" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "patchright@\^1\.59\.4" .claude/skills/agentbloc/references/phase-3-integration.md`
    - NEW deny-list count (9 packages) referenced: `grep -q "9 forbidden packages" .claude/skills/agentbloc/references/phase-3-integration.md`
    - NEW Posture C vendor examples present: `grep -q "DataDome" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "PerimeterX" .claude/skills/agentbloc/references/phase-3-integration.md`
    - PRESERVED v1.0 Summary block header: `grep -q "\*\*Summary (v1.0, preserved):\*\*" .claude/skills/agentbloc/references/phase-3-integration.md`
    - PRESERVED v1.0 bullet 1 (login portal): `grep -q "login portal" .claude/skills/agentbloc/references/phase-3-integration.md`
    - PRESERVED v1.0 bullet 2 (standard web forms): `grep -q "standard web forms (automatable)" .claude/skills/agentbloc/references/phase-3-integration.md`
    - PRESERVED v1.0 bullet 3 (2FA/CAPTCHA): `grep -q "2FA/CAPTCHA" .claude/skills/agentbloc/references/phase-3-integration.md`
    - PRESERVED v1.0 bullet 4 (microsoft/playwright-mcp): `grep -q "microsoft/playwright-mcp" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "HIGH trust" .claude/skills/agentbloc/references/phase-3-integration.md`
    - PRESERVATION of Phase 10 Priority 1 MCP Server: `grep -q "^### Priority 1: MCP Server (Four-Step Search)$" .claude/skills/agentbloc/references/phase-3-integration.md`
    - PRESERVATION of Phase 10 Priority 2 Official API: `grep -q "^### Priority 2: Official API$" .claude/skills/agentbloc/references/phase-3-integration.md`
    - PRESERVATION of Priorities 4-6: `grep -q "^### Priority 4: Email Scraping$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^### Priority 5: Webhook Interception$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^### Priority 6:" .claude/skills/agentbloc/references/phase-3-integration.md`
    - PRESERVATION of Steps 3-7: `grep -q "^## Step 3: Evidence Verification$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^## Step 4: Trust Scoring$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^## Step 5: Decision Matrix Construction$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^## Step 6: Security Cross-Reference$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^## Step 7: Integration Presentation and Approval$" .claude/skills/agentbloc/references/phase-3-integration.md`
    - PRESERVATION of Integration Gate + Quick Reference: `grep -q "^## Integration Gate$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^## Quick Reference$" .claude/skills/agentbloc/references/phase-3-integration.md`
    - Line count within budget: `wc -l .claude/skills/agentbloc/references/phase-3-integration.md` returns a number between 388 and 430 inclusive (expected ~410)
    - Priority 3 section count stable (exactly one `### Priority 3:` header present): `grep -c "^### Priority 3:" .claude/skills/agentbloc/references/phase-3-integration.md` returns 1
    - Zero NEW em-dashes beyond the two in the D-57 locked snippet (`grep -c "," .claude/skills/agentbloc/references/phase-3-integration.md` returns ≤ 2; if executor chose to replace with `--` per the action rubric, should return 0)
  </acceptance_criteria>
  <done>
phase-3-integration.md Priority 3 section surgically edited to close the Phase 10 forward-stub contract: (1) heading `### Priority 3: Playwright Browser Automation [Phase 11 scope]` replaced with `### Priority 3: Playwright Browser Automation (Four-Step Fallback)` , marker unmarked, descriptor added; (2) stub paragraph "See forthcoming [references/browser-fallback.md]... Phase 10 stubs this priority; Phase 11 wires it in." replaced with concrete three-sentence paragraph citing (a) browser-fallback.md canonical Step 4 protocol with 6-step flow enumeration (per-service legal opt-in → subagent invocation → HAR capture with checkpoint → endpoint classification → output firewall → DISCOVERY-REPORT.md emission), (b) browser-stack.md pinned versions (playwright@^1.59.1 + patchright@^1.59.4 for Posture B CDP-leak patches) + anti-bot deny-list (9 forbidden packages) + scripts/anti-bot-lint.sh CI enforcement, (c) Posture C halt behavior (DataDome / PerimeterX / CAPTCHA) via DISCOVERY-BLOCKED-REPORT.md per BROWSER-09; (3) v1.0 Summary block (**Summary (v1.0, preserved):** + 4 bullets , login portal / standard web forms / 2FA / microsoft/playwright-mcp) preserved verbatim. Priorities 1-2 (Phase-10 content) and 4-6 (v1.0 content) untouched. Steps 3-7 + Integration Gate + Quick Reference untouched. File grew from 398 to ~410 lines (within 388-430 budget).
  </done>
</task>

<task type="auto">
  <name>Task 2: Surgical edit to SKILL.md Phase 3 entry See-line block , connector sentence rewrite + 2 new See-lines appended (browser-fallback.md + browser-stack.md)</name>
  <files>.claude/skills/agentbloc/SKILL.md</files>
  <read_first>
    - .planning/phases/11-integration-discovery-browser-fallback/11-CONTEXT.md (D-58 context-budget-conscious See-line addition; explicit list of what NOT to load: discovery-report-schema.md / output-firewall.md / legal-posture.md)
    - .planning/phases/11-integration-discovery-browser-fallback/11-PATTERNS.md (lines 640-676 , the "SKILL.md (surgical edit)" section citing Phase 10 commit 7087a74 as line-for-line precedent; Pattern 9 "Context-budget discipline for Phase 3 loads")
    - .claude/skills/agentbloc/SKILL.md (the FULL 178-line current file , read it all before editing; Phase 3 See-line block is at lines 121-125)
    - .claude/skills/agentbloc/references/browser-fallback.md (target of new See-line , verify this exists from Plan 11-01 so the citation resolves)
    - .claude/skills/agentbloc/references/browser-stack.md (target of new See-line , verify this exists from Plan 11-02 so the citation resolves)
    - .planning/phases/10-integration-discovery-mcp-path/10-03-PLAN.md Task 2 Edit 2 (line-for-line analog , that edit appended 3 See-lines to Phase 3; this edit appends 2 more to the same block)
    - .planning/REQUIREMENTS.md (BROWSER-01: subagent invocation wired into user-facing flow , Phase 3 entry must load browser-fallback.md so legal opt-in gate is seen before Claude ever invokes browser-discovery subagent)
  </read_first>
  <action>
Apply EXACTLY ONE surgical edit to `.claude/skills/agentbloc/SKILL.md` using the Edit tool. The edit covers 5 contiguous lines (current lines 121-125) and replaces them with 7 lines (rewritten connector sentence + 4 preserved See-lines + 2 new See-lines appended in order).

Read the full current file first (178 lines) to confirm correct `old_string` context. The Phase 3 entry See-line block is located AFTER the Precondition paragraph (line 117) and the Summary Gate paragraph (line 119), BEFORE the Phase 4 entry (line 127).

**Single Edit , Phase 3 entry See-line load-list extension: connector sentence rewrite + append 2 new See-lines after the existing 4.**

Use the Edit tool with this `old_string` (this is the EXACT current Phase 3 See-line block, 5 lines as seen in the file today):

```
You MUST read the complete integration analysis protocol AND the MCP integration protocol AND the ecosystem registry AND the integration manifest schema before starting this phase:
See [references/phase-3-integration.md](references/phase-3-integration.md)
See [references/mcp-integration-protocol.md](references/mcp-integration-protocol.md)
See [references/mcp-ecosystem-registry.md](references/mcp-ecosystem-registry.md)
See [references/integration-manifest-schema.md](references/integration-manifest-schema.md)
```

And this `new_string` (connector sentence gets "AND the browser-fallback protocol AND the browser stack reference" appended; existing 4 See-lines preserved byte-for-byte in original order; 2 new See-lines appended in order browser-fallback.md FIRST then browser-stack.md):

```
You MUST read the complete integration analysis protocol AND the MCP integration protocol AND the ecosystem registry AND the integration manifest schema AND the browser-fallback protocol AND the browser stack reference before starting this phase:
See [references/phase-3-integration.md](references/phase-3-integration.md)
See [references/mcp-integration-protocol.md](references/mcp-integration-protocol.md)
See [references/mcp-ecosystem-registry.md](references/mcp-ecosystem-registry.md)
See [references/integration-manifest-schema.md](references/integration-manifest-schema.md)
See [references/browser-fallback.md](references/browser-fallback.md)
See [references/browser-stack.md](references/browser-stack.md)
```

The edit (a) rewrites the connector sentence to name the 6 references loaded (up from 4) , the new clauses `AND the browser-fallback protocol AND the browser stack reference` append to the existing list in narrative order matching the See-line order below, (b) preserves the existing 4 See-lines in their exact original order byte-for-byte (phase-3-integration.md first, integration-manifest-schema.md fourth), (c) appends exactly 2 new See-lines in the canonical order (browser-fallback.md before browser-stack.md , imperative protocol is read before declarative stack reference per the subagent's Mandatory Initial Read ordering in Plan 11-03).

**What this edit does NOT do (critical per D-58):**

- Does NOT add a See-line for `discovery-report-schema.md`. This reference is loaded only in the `browser-discovery` subagent's forked context on invocation (per D-44 subagent-only load table). Adding it here would violate D-58 context-budget discipline.
- Does NOT add a See-line for `output-firewall.md`. Subagent-only load per D-44.
- Does NOT add a See-line for `legal-posture.md`. Subagent-only load per D-44.
- Does NOT add a new sub-gate to State Transitions. Browser fallback is a sub-path of the existing `mcp_integrations_verified` sub-gate per D-58 (a manifest entry with `resolution_method: browser-fallback` becomes `status: verified` when DISCOVERY-REPORT.md exists + validates + SHA256 matches, under the existing Phase 10 Phase-3-specific bullet).
- Does NOT edit the existing Precondition paragraph (line 117) or Summary Gate paragraph (line 119). Those were added by Phase 9 / Phase 10 and stay verbatim.
- Does NOT edit any other Phase entry. Phase 1, Phase 2, Phase 4, Phase 5, Phase 6 entries + Hard Gates + Language and Technical Level + State Transitions + Quality Checklist + Reference Implementation all remain byte-for-byte identical.

**Preservation requirements (critical):**

- Verify after editing that State Transitions still has exactly 3 Phase-specific bullets (Phase 1 / Phase 2 / Phase 3 from Phase 10 Plan 10-03): `grep -c "^- Phase [0-9]* specific:" .claude/skills/agentbloc/SKILL.md` returns 3. Plan 11-04 does NOT add a Phase-4-specific bullet , no new sub-gate per D-58.
- Verify Phase 3 Precondition paragraph (from Phase 9 Plan 09-03) intact: `grep -q "agent-profiles\.yaml" .claude/skills/agentbloc/SKILL.md` matches inside the Phase 3 section.
- Verify Phase 3 Summary Gate paragraph (from Phase 10 Plan 10-03) intact: `grep -q "^\*\*Summary Gate:\*\*" .claude/skills/agentbloc/SKILL.md` matches.
- Verify Phase 4 Precondition paragraph (from Phase 10 Plan 10-03) intact: `awk '/^### Phase 4:/,/^### Phase 5:/' .claude/skills/agentbloc/SKILL.md | grep -q "integration-manifest\.yaml"` matches.
- Verify no Phase 11 subagent-only reference accidentally loaded: `grep -q "discovery-report-schema\.md" .claude/skills/agentbloc/SKILL.md` returns exit 1 AND `grep -q "output-firewall\.md" .claude/skills/agentbloc/SKILL.md` returns exit 1 AND `grep -q "legal-posture\.md" .claude/skills/agentbloc/SKILL.md` returns exit 1.
- Verify Hard Gates, Language and Technical Level, Quality Checklist, Reference Implementation sections intact: `grep -q "^## Hard Gates$"`, `grep -q "^## Quality Checklist$"`, `grep -q "^## Reference Implementation$"`.
- Verify line count: `wc -l .claude/skills/agentbloc/SKILL.md` returns a number between 178 and 195 inclusive (expected 180 , net delta is exactly +2 lines for the 2 new See-lines; the connector sentence rewrite is in-place on the same line).
- Verify line count well under v1.0 250-line cap.
- Zero em-dashes introduced: the new_string contains zero `,` characters (CLAUDE.md rule "Never put a , in your text" complied with for this edit since SKILL.md has no locked em-dash exception). `grep -c "," .claude/skills/agentbloc/SKILL.md` should return the same count as before the edit (0).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "browser-fallback\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "browser-stack\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "browser-fallback protocol AND the browser stack reference" .claude/skills/agentbloc/SKILL.md &amp;&amp; ! grep -q "discovery-report-schema\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; ! grep -q "output-firewall\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; ! grep -q "legal-posture\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "^## Hard Gates$" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "^## Quality Checklist$" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "^## Reference Implementation$" .claude/skills/agentbloc/SKILL.md &amp;&amp; LINES=$(wc -l &lt; .claude/skills/agentbloc/SKILL.md) &amp;&amp; [ "$LINES" -ge 178 ] &amp;&amp; [ "$LINES" -le 195 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/SKILL.md` exits 0
    - NEW browser-fallback.md See-line present: `grep -q "See \[references/browser-fallback\.md\](references/browser-fallback\.md)" .claude/skills/agentbloc/SKILL.md` matches
    - NEW browser-stack.md See-line present: `grep -q "See \[references/browser-stack\.md\](references/browser-stack\.md)" .claude/skills/agentbloc/SKILL.md` matches
    - NEW connector sentence extended: `grep -q "browser-fallback protocol AND the browser stack reference" .claude/skills/agentbloc/SKILL.md` matches
    - Old 4-reference-only connector sentence GONE: `grep -q "integration manifest schema before starting this phase:$" .claude/skills/agentbloc/SKILL.md` returns exit 1 (old sentence ended with "...integration manifest schema before starting this phase:"; new sentence ends with "...browser stack reference before starting this phase:")
    - GREP-FOR-ABSENCE: `discovery-report-schema.md` NOT in SKILL.md (D-58 context-budget): `grep -q "discovery-report-schema\.md" .claude/skills/agentbloc/SKILL.md` returns exit 1
    - GREP-FOR-ABSENCE: `output-firewall.md` NOT in SKILL.md (D-58): `grep -q "output-firewall\.md" .claude/skills/agentbloc/SKILL.md` returns exit 1
    - GREP-FOR-ABSENCE: `legal-posture.md` NOT in SKILL.md (D-58): `grep -q "legal-posture\.md" .claude/skills/agentbloc/SKILL.md` returns exit 1
    - PRESERVATION: existing Phase 3 See-line to phase-3-integration.md intact: `grep -q "See \[references/phase-3-integration\.md\](references/phase-3-integration\.md)" .claude/skills/agentbloc/SKILL.md`
    - PRESERVATION: existing Phase 3 See-line to mcp-integration-protocol.md intact: `grep -q "See \[references/mcp-integration-protocol\.md\](references/mcp-integration-protocol\.md)" .claude/skills/agentbloc/SKILL.md`
    - PRESERVATION: existing Phase 3 See-line to mcp-ecosystem-registry.md intact: `grep -q "See \[references/mcp-ecosystem-registry\.md\](references/mcp-ecosystem-registry\.md)" .claude/skills/agentbloc/SKILL.md`
    - PRESERVATION: existing Phase 3 See-line to integration-manifest-schema.md intact: `grep -q "See \[references/integration-manifest-schema\.md\](references/integration-manifest-schema\.md)" .claude/skills/agentbloc/SKILL.md`
    - Phase 3 See-line block now has exactly 6 See-lines: count lines between Phase 3 header and Phase 4 header that start with `See [references/`: `awk '/^### Phase 3:/,/^### Phase 4:/' .claude/skills/agentbloc/SKILL.md | grep -c "^See \[references/"` returns 6
    - PRESERVATION: Phase 3 existing Precondition paragraph (Phase 9 09-03 artifact): `awk '/^### Phase 3:/,/^### Phase 4:/' .claude/skills/agentbloc/SKILL.md | grep -q "agent-profiles\.yaml"`
    - PRESERVATION: Phase 3 existing Summary Gate paragraph (Phase 10 10-03 artifact): `grep -q "^\*\*Summary Gate:\*\*" .claude/skills/agentbloc/SKILL.md`
    - PRESERVATION: State Transitions has exactly 3 Phase-specific bullets (no new one added per D-58): `grep -c "^- Phase [0-9]* specific:" .claude/skills/agentbloc/SKILL.md` returns 3
    - PRESERVATION: mcp_integrations_verified sub-gate mention intact (browser fallback is a sub-path, not a new gate): `grep -q "mcp_integrations_verified" .claude/skills/agentbloc/SKILL.md`
    - PRESERVATION: Phase 1 / Phase 2 / Phase 4 / Phase 5 / Phase 6 entry headers intact: `grep -q "^### Phase 1: Deep Interview$" .claude/skills/agentbloc/SKILL.md` AND `grep -q "^### Phase 2:" .claude/skills/agentbloc/SKILL.md` AND `grep -q "^### Phase 4: Step-by-Step" .claude/skills/agentbloc/SKILL.md` AND `grep -q "^### Phase 5:" .claude/skills/agentbloc/SKILL.md` AND `grep -q "^### Phase 6:" .claude/skills/agentbloc/SKILL.md`
    - PRESERVATION: Phase 4 existing Precondition paragraph (Phase 10 10-03 artifact): `awk '/^### Phase 4:/,/^### Phase 5:/' .claude/skills/agentbloc/SKILL.md | grep -q "integration-manifest\.yaml"`
    - PRESERVATION: Hard Gates section intact: `grep -q "^## Hard Gates$" .claude/skills/agentbloc/SKILL.md`
    - PRESERVATION: Quality Checklist intact: `grep -q "^## Quality Checklist$" .claude/skills/agentbloc/SKILL.md`
    - PRESERVATION: Reference Implementation closer intact: `grep -q "^## Reference Implementation$" .claude/skills/agentbloc/SKILL.md`
    - Line count within D-58 budget: `wc -l .claude/skills/agentbloc/SKILL.md` returns a number between 178 and 195 inclusive (expected ~180)
    - Line count well under v1.0 250-line cap: `wc -l .claude/skills/agentbloc/SKILL.md` returns a number less than 250
    - Zero em-dashes introduced: `grep -c "," .claude/skills/agentbloc/SKILL.md` returns 0 (was 0 before edit; D-58 new_string contains no em-dashes)
  </acceptance_criteria>
  <done>
SKILL.md Phase 3 entry See-line load-list surgically edited per D-58: (1) connector sentence `You MUST read the complete integration analysis protocol AND the MCP integration protocol AND the ecosystem registry AND the integration manifest schema before starting this phase:` rewritten to `You MUST read the complete integration analysis protocol AND the MCP integration protocol AND the ecosystem registry AND the integration manifest schema AND the browser-fallback protocol AND the browser stack reference before starting this phase:` , 2 new clauses appended in narrative order matching See-line order below; (2) existing 4 See-lines preserved in original order (phase-3-integration.md → mcp-integration-protocol.md → mcp-ecosystem-registry.md → integration-manifest-schema.md); (3) 2 new See-lines appended in canonical order (browser-fallback.md before browser-stack.md , imperative-then-declarative read-order matching the browser-discovery subagent's Mandatory Initial Read per Plan 11-03). Context-budget discipline per D-58 held: `discovery-report-schema.md`, `output-firewall.md`, `legal-posture.md` NOT loaded at Phase 3 entry (confirmed via grep-for-absence). No new sub-gate added to State Transitions (browser fallback remains a sub-path of existing `mcp_integrations_verified` sub-gate per D-58). Phase 1 / Phase 2 / Phase 4 / Phase 5 / Phase 6 entries + Hard Gates + Quality Checklist + Reference Implementation + all 3 existing Phase-specific State Transitions bullets preserved verbatim. File grew from 178 to ~180 lines (within 178-195 budget; well under 250-line v1.0 cap). Zero em-dashes introduced.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these phase-level checks:

1. `test -f .claude/skills/agentbloc/references/phase-3-integration.md` , target file still exists after surgical edit
2. `test -f .claude/skills/agentbloc/SKILL.md` , target file still exists after surgical edit
3. phase-3-integration.md Priority 3 heading unmarked: `grep -q "^### Priority 3: Playwright Browser Automation (Four-Step Fallback)$" ...` AND `! grep -q "\[Phase 11 scope\]" ...`
4. phase-3-integration.md new See-lines: `grep -q "browser-fallback\.md" ...` AND `grep -q "browser-stack\.md" ...` AND `grep -q "DISCOVERY-BLOCKED-REPORT\.md" ...` AND `grep -q "scripts/anti-bot-lint\.sh" ...`
5. phase-3-integration.md v1.0 Summary preserved: `grep -q "login portal" ...` AND `grep -q "microsoft/playwright-mcp" ...` AND `grep -q "2FA/CAPTCHA" ...`
6. phase-3-integration.md Phase 10 content preserved: `grep -q "^### Priority 1: MCP Server (Four-Step Search)$" ...` AND `grep -q "^### Priority 2: Official API$" ...`
7. phase-3-integration.md v1.0 Steps 3-7 preserved verbatim: `grep -q "^## Step 3: Evidence Verification$" ...` AND ... Step 4 / 5 / 6 / 7 headers all match
8. phase-3-integration.md line count: `wc -l ...` returns 388 to 430 inclusive
9. SKILL.md Phase 3 See-line block has exactly 6 See-lines: `awk '/^### Phase 3:/,/^### Phase 4:/' .claude/skills/agentbloc/SKILL.md | grep -c "^See \[references/"` returns 6
10. SKILL.md subagent-only refs NOT loaded (D-58): `! grep -q "discovery-report-schema\.md" .claude/skills/agentbloc/SKILL.md` AND `! grep -q "output-firewall\.md" .claude/skills/agentbloc/SKILL.md` AND `! grep -q "legal-posture\.md" .claude/skills/agentbloc/SKILL.md`
11. SKILL.md State Transitions bullet count still 3 (no new sub-gate per D-58): `grep -c "^- Phase [0-9]* specific:" .claude/skills/agentbloc/SKILL.md` returns 3
12. SKILL.md Phase 3 Summary Gate + Phase 4 Precondition from Phase 10 preserved: `grep -q "^\*\*Summary Gate:\*\*" .claude/skills/agentbloc/SKILL.md` AND `awk '/^### Phase 4:/,/^### Phase 5:/' .claude/skills/agentbloc/SKILL.md | grep -q "integration-manifest\.yaml"`
13. SKILL.md line count: `wc -l ...` returns 178 to 195 inclusive; well under 250
14. Requirement trace: BROWSER-01 (subagent invocation wired into user-facing flow) , phase-3-integration.md Priority 3 paragraph names `browser-fallback.md` (which contains the subagent invocation contract per Plan 11-01) AND SKILL.md Phase 3 loads `browser-fallback.md` so Claude reads the invocation contract at phase entry. Forward-link resolution , Phase 10 stubbed `browser-fallback.md` forward See-line is now a concrete citation with no "forthcoming" language.
</verification>

<success_criteria>
1. `references/phase-3-integration.md` Priority 3 section received one surgical edit that (a) unmarks the `[Phase 11 scope]` marker , heading becomes `### Priority 3: Playwright Browser Automation (Four-Step Fallback)` , and (b) replaces the stub paragraph with a concrete three-sentence paragraph citing `browser-fallback.md` (canonical Step 4 protocol with full 6-step flow enumeration), `browser-stack.md` (playwright@^1.59.1 + patchright@^1.59.4 pins + 9-package anti-bot deny-list + scripts/anti-bot-lint.sh CI enforcement), and Posture C halt behavior (DataDome / PerimeterX / CAPTCHA → DISCOVERY-BLOCKED-REPORT.md per BROWSER-09). The v1.0 Summary bullet block (login portal / web forms / 2FA / microsoft/playwright-mcp) is preserved verbatim. Phase-10-produced Priority 1 and Priority 2 content + v1.0 Priorities 4-6, Steps 3-7, Integration Gate, and Quick Reference are preserved verbatim. File within 388-430 line budget.
2. `SKILL.md` Phase 3 entry See-line load-list received one surgical edit that (a) rewrites the connector sentence to name the 6 references loaded (appending `AND the browser-fallback protocol AND the browser stack reference`) and (b) appends 2 new See-lines after the existing 4 in canonical order: `browser-fallback.md` FIRST then `browser-stack.md`. D-58 context-budget discipline is held: `discovery-report-schema.md`, `output-firewall.md`, and `legal-posture.md` are NOT loaded at Phase 3 entry , they load only in the `browser-discovery` subagent's forked context. NO new sub-gate added to State Transitions , browser fallback remains a sub-path of the existing `mcp_integrations_verified` sub-gate. All other SKILL.md sections (Phase 1 / 2 / 4 / 5 / 6 entries, Hard Gates, Language and Technical Level, State Transitions 3 existing Phase-specific bullets, Quality Checklist, Reference Implementation) preserved verbatim. File within 178-195 line budget; well under 250-line v1.0 cap.
3. BROWSER-01 traced: (a) `phase-3-integration.md` Priority 3 now cites `browser-fallback.md` which contains the subagent invocation contract from Plan 11-01; (b) `SKILL.md` Phase 3 entry loads `browser-fallback.md` unconditionally so Claude reads Step 1 (per-service legal opt-in gate) before ever invoking the `browser-discovery` subagent , closing the approval-gate wiring that Plan 11-01 defined in the imperative protocol.
4. Forward-link resolution complete: the broken forward See-line to `browser-fallback.md` introduced by Phase 10 Plan 10-03 Task 1 Edit 3 (with "forthcoming" language and `[Phase 11 scope]` marker) is now resolved , the link target exists (from Plan 11-01), the marker is gone, the "forthcoming" language is gone, and the citation is concrete with full three-sentence expansion naming all three wiring points (protocol + stack + halt artifact).
5. ROADMAP Phase 11 success criterion 1 wired (legal opt-in enforcement in user-facing flow): `browser-fallback.md` Step 1 (per-service legal opt-in gate with DISCOVERY-LICENSE-NOTICE.md emission + OPT_IN_LEDGER.jsonl append) is the first step cited in the new Priority 3 paragraph's 6-step enumeration; Claude reading Phase 3 cannot skip it.
6. ROADMAP Phase 11 success criterion 3 wired (CI deny-list active): `scripts/anti-bot-lint.sh` is named in the new Priority 3 paragraph as the CI enforcement layer for the 9-package deny-list; visible in the integration protocol without the developer having to drill into the stack reference.
7. All 12 Phase 11 requirements (BROWSER-01..12) are now collectively covered across Plans 11-01 (imperative protocol + schema + firewall + fixture), 11-02 (stack + legal + CI lint), 11-03 (browser-discovery subagent), and 11-04 (wiring , this plan). Phase 11 structurally complete after Wave 3 ships.
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-discovery-browser-fallback/11-04-SUMMARY.md` documenting:
- Final line counts: phase-3-integration.md (X lines, delta +Y from 398) + SKILL.md (X lines, delta +Y from 178)
- Diff summary for phase-3-integration.md (the single Priority 3 edit with before/after snippets showing heading unmark + paragraph replacement + v1.0 Summary preservation)
- Diff summary for SKILL.md (the single Phase 3 See-line block edit with before/after snippets showing connector sentence rewrite + 2 new See-lines appended)
- Confirmation that the three subagent-only references (discovery-report-schema.md, output-firewall.md, legal-posture.md) are confirmed ABSENT from SKILL.md via grep-for-absence checks (D-58 context-budget discipline held)
- Confirmation that NO new State Transitions sub-gate was added (browser fallback remains a sub-path of `mcp_integrations_verified` per D-58)
- Confirmation that v1.0 Summary bullet block in phase-3-integration.md Priority 3 is preserved byte-for-byte
- Confirmation that Phase-10-produced content (Priority 1 MCP Server (Four-Step Search) + Priority 2 Official API) in phase-3-integration.md is preserved byte-for-byte
- Confirmation that Steps 3-7, Integration Gate, Quick Reference in phase-3-integration.md are unchanged
- Confirmation that all SKILL.md sections outside Phase 3 See-line block (Phase 1 / 2 / 4 / 5 / 6 entries + State Transitions 3 existing bullets + Hard Gates + Quality Checklist + Reference Implementation) are unchanged
- Any deviations from the plan with rationale (e.g., if executor chose to replace D-57 locked em-dashes with `--` in phase-3-integration.md, note the deviation; if line counts land outside budget, explain)
- Handoff note for Phase 11 verification: "All 12 BROWSER requirements (BROWSER-01 through BROWSER-12) wired end-to-end across Plans 11-01 / 11-02 / 11-03 / 11-04. Phase 11 structurally complete. Ready for `/gsd-verify-phase 11`. Forward-link contract from Phase 10 Plan 10-03 (browser-fallback.md forward See-line) is now resolved , citation is concrete, target file exists."
- Handoff note for Phase 12 (Deploy Pipeline): "Phase 12 consumes `.agentbloc/discovery/<service-slug>/DISCOVERY-REPORT.md` entries as [DISCOVERED]-tier integrations alongside [VERIFIED] MCP-path entries from Phase 10. Every DISCOVERY-REPORT.md with SHA256 hash + user_attestation_timestamp matching OPT_IN_LEDGER.jsonl is ready for deploy; DISCOVERY-BLOCKED-REPORT.md entries block Phase 4 gate; INTERNAL-HARDENED endpoints surface as enhanced-risk warnings in DEPLOY-REPORT.md per D-53 carry-forward."
</output>
</content>
</invoke>