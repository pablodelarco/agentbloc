---
phase: 14-autonomy-monitoring-control
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - .claude/skills/agentbloc/templates/briefing-agent.md.tmpl
  - .claude/skills/agentbloc/references/agent-memory-schema.md
  - .claude/skills/agentbloc/references/audit-logging.md
autonomous: true
requirements_addressed:
  - MONITOR-03
  - MONITOR-04
  - MONITOR-06
  - CTRL-02
  - CTRL-04
estimated_tasks: 3

must_haves:
  truths:
    - "templates/briefing-agent.md.tmpl exists as a Phase-12-style deployed-agent SKILL.md template (autonomy=full default; pure {{var}} substitution per D-62; ~70 lines total) emitting the daily briefing flow per D-88: read all per-agent JSONL logs from `.claude/agents/logs/<TODAY>/`, group by agent, summarize counts + escalations + cost + tokens + status, dispatch via `briefing-renderer.sh format-<target> <input.json>` for pluggable presentation per MONITOR-06"
    - "agent-memory-schema.md surgically extended to bump last-run.json schema_version to 2 + add `cost_usd: number` and `token_count: {input, output, cached_input}` optional fields per D-98; backward-compatible (omitting both is valid; older v1 deployments continue to work); no rewrites of upstream content"
    - "audit-logging.md surgically aligned with Phase 13 D-75 correlation-ID format per D-97; v1.0 sess-NNN format references replaced with one-line legacy note + main format updated to D-75; pre-existing examples preserved as legacy markers"
    - "Zero em-dashes (U+2014) across all NEW prose in 3 files (existing em-dashes in unchanged upstream content out of scope per Phase 13 precedent)"
    - "All Phase 12 + Phase 13 + Phase 14 (Plan 14-01) cross-references intact post-edit"
  artifacts:
    - path: ".claude/skills/agentbloc/templates/briefing-agent.md.tmpl"
      provides: "MONITOR-03 + MONITOR-04 default briefing agent template emitted by deploy-engine for every team; CTRL-04 status-badge surfacing; pluggable presentation per MONITOR-06"
      min_lines: 60
      max_lines: 90
      contains: "briefing-renderer.sh"
    - path: ".claude/skills/agentbloc/references/agent-memory-schema.md"
      provides: "Extended Phase 12 contract: last-run.json schema_version 2 with cost_usd + token_count fields per CTRL-02 + D-98"
      min_lines: 211
      max_lines: 240
      contains: "schema_version: 2"
    - path: ".claude/skills/agentbloc/references/audit-logging.md"
      provides: "Aligned correlation-ID format with Phase 13 D-75; legacy v1.0 sess-NNN as historical note"
      min_lines: 60
      max_lines: 200
      contains: "correlation-id.md"

verification:
  - "grep -q 'briefing-renderer.sh' .claude/skills/agentbloc/templates/briefing-agent.md.tmpl"
  - "grep -q 'schema_version.*2' .claude/skills/agentbloc/references/agent-memory-schema.md"
  - "grep -q 'cost_usd' .claude/skills/agentbloc/references/agent-memory-schema.md"
  - "grep -q 'token_count' .claude/skills/agentbloc/references/agent-memory-schema.md"
  - "grep -q 'correlation-id.md' .claude/skills/agentbloc/references/audit-logging.md"

threat_model: "Phase 14-02 mutates 2 existing references (agent-memory-schema.md + audit-logging.md) via surgical edits only. Threats: T-14-06 schema_version migration (mitigation: v1 -> v2 backward-compatible; consumers refuse on schema_version > 2 only); T-14-07 audit-trail discontinuity (mitigation: legacy sess-NNN preserved as historical note; existing log files remain parseable). Both threats inherit Phase 12 + Phase 13 surgical-edit discipline."

---

# Plan 14-02: Briefing Agent Template + Monitor Schema Extensions

> 1 new template + 2 surgical schema extensions to existing Phase 12 + v1.0 references. The briefing agent is itself a deployed agent generated by deploy-engine; this plan provides the template + extends the schemas it consumes.

<context>
@.planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md
@.planning/phases/14-autonomy-monitoring-control/14-01-PLAN.md
@.claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl
@.claude/skills/agentbloc/references/agent-memory-schema.md
@.claude/skills/agentbloc/references/audit-logging.md
@.claude/skills/agentbloc/references/correlation-id.md
@.claude/skills/agentbloc/references/jsonl-log-schema.md
@.claude/skills/agentbloc/references/reporting-hierarchy.md

<interfaces>
Decision anchors (from 14-CONTEXT.md):
- D-88 = briefing-agent.md.tmpl (deployed-agent template; daily wake reads logs + dispatches via renderer)
- D-97 = audit-logging.md correlation-ID format alignment with D-75
- D-98 = agent-memory-schema.md last-run.json schema_version=2 with cost_usd + token_count

Twin patterns:
- briefing-agent.md.tmpl twin -> deployed-agent-skill-full.md.tmpl (Phase 12 D-62 pure {{var}} template; same autonomy=full prose discipline)
- agent-memory-schema.md surgical extension twin -> Phase 12 D-65 schema versioning pattern
- audit-logging.md alignment edit twin -> Phase 13 D-75 correlation-id.md format spec
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `.claude/skills/agentbloc/templates/briefing-agent.md.tmpl` , default briefing agent SKILL.md template (Phase-12-style; autonomy=full; pluggable renderer dispatch)</name>
  <files>.claude/skills/agentbloc/templates/briefing-agent.md.tmpl</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-88 briefing template; line budget 60-90)
    - .claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl (FULL file; PRIMARY structural twin; copy autonomy=full skeleton)
    - .claude/skills/agentbloc/references/jsonl-log-schema.md (Plan 14-01 Task 1 output; for log path + schema)
    - .claude/skills/agentbloc/references/reporting-hierarchy.md (Plan 14-01 Task 5 output; for chain rationale)
    - .claude/skills/agentbloc/references/activity-feed.md (Plan 14-01 Task 7 output; for activity-feed-merge.sh contract)
  </read_first>
  <action>
Write `.claude/skills/agentbloc/templates/briefing-agent.md.tmpl` (60-90 lines, zero em-dashes). Pure `{{var}}` substitution per D-62; zero Jinja blocks. Structure mirrors deployed-agent-skill-full.md.tmpl.

Frontmatter (lines 1-10):
```yaml
---
name: {{agent.id}}
description: Daily briefing agent for {{team.name}} , consolidates per-agent JSONL logs into a Telegram message; pluggable renderer for v2.5 web dashboard transition. autonomy=full (read-only; no side-effect risk).
team: {{team.name}}
autonomy: full
trigger:
  cron: {{agent.cron}}
agent_role: briefing
---
```

Body sections (in order, ~50 lines):

1. `# {{agent.role_label}}` (briefing-agent for the team)
2. `## Mandatory Initial Read` , read these files at every wake: `.agentbloc/agents/{{agent.id}}/memory.md`, `.agentbloc/agents/{{agent.id}}/state.json`, `.agentbloc/agents/{{agent.id}}/last-run.json`, `.agentbloc/agents/registry.yaml`.
3. `## Wake Protocol` , 6-step flow:
   1. Kill-switch pre-check: read `.agentbloc/KILL_SWITCH`; if present, halt (briefing skipped today; user resumes when removed).
   2. Correlation-ID ingest: read `AGENTBLOC_CORRELATION_ID` env var; if missing, generate via D-75 format `cron-{{utc-z}}-{{nonce6}}`.
   3. Glob today's log files: `.claude/agents/logs/<TODAY>/*.jsonl` (excluding the briefing's own future writes).
   4. Per-agent summarization: for each agent_id in registry.yaml, count action types from log lines, sum token_count + cost_usd, read each agent's last-run.json status field, count escalations from the day's escalations.jsonl + count pending approvals from approvals.jsonl (proposals without responses).
   5. Trigger activity-feed merge: invoke `bash .agentbloc/runtime/activity-feed-merge.sh <TODAY>`.
   6. Dispatch via pluggable renderer: read `registry.yaml monitor.presentation` (default `telegram`); invoke `bash .agentbloc/runtime/briefing-renderer.sh format-<presentation> <summary-json-stdin>`; render output sent via `bash .agentbloc/runtime/briefing-renderer.sh send-<presentation> <thread-id> <rendered-message>`.
4. `## Side-effect Approval Routing` , autonomy=full means no approval needed; briefing posts to Telegram briefing thread without intervention. Status badges follow CTRL-04 emoji set: 🟢 active · 🟡 idle · 🔴 error.
5. `## Memory Protocol` , standard agent memory contract per agent-memory-schema.md; briefing memory.md stores the run-history high-water marks (last 30 days).
6. `## Kill-Switch Discipline` , unchanged v1.0 SECR-05 + Phase 13 D-77 pattern.
7. `## Pluggable Renderer` , supports format-telegram (v2.0), format-html (v2.5 stub), format-json (v3.0 stub). Renderer script lives at `.agentbloc/runtime/briefing-renderer.sh`. v2.5/v3.0 stubs print `(presentation target X not yet implemented in v2.0)`.
8. `<!-- agentbloc:template autonomy=full role=briefing schema_version=1 -->` on last line.

Template anchor variables: `{{agent.id}}`, `{{agent.role_label}}`, `{{team.name}}`, `{{agent.cron}}`, `{{team.briefing_thread_id}}`. NO Jinja blocks.
  </action>
  <acceptance_criteria>
    - File exists at `.claude/skills/agentbloc/templates/briefing-agent.md.tmpl`
    - Lines 60..90; em-dashes = 0
    - Frontmatter contains `name: {{agent.id}}` + `autonomy: full` + `agent_role: briefing`
    - Body contains: Mandatory Initial Read, Wake Protocol (6 steps), Side-effect Approval Routing, Memory Protocol, Kill-Switch Discipline, Pluggable Renderer sections
    - Cites `briefing-renderer.sh` script
    - Cites `activity-feed-merge.sh` script
    - Cites pluggable presentation targets (telegram + html + json)
    - Status badges 🟢 🟡 🔴 cited
    - Last line: `<!-- agentbloc:template autonomy=full role=briefing schema_version=1 -->`
    - Zero Jinja blocks: `grep -E '\{%.+%\}' file` returns nothing
    - Pure `{{var}}` substitution discipline
  </acceptance_criteria>
  <done>briefing-agent.md.tmpl wired. MONITOR-04 (default daily briefing template) closed. Plan 14-03 wires this template into deploy-engine.md emission_targets so every team auto-deploys a briefing agent.</done>
</task>

<task type="auto">
  <name>Task 2: Surgically extend `.claude/skills/agentbloc/references/agent-memory-schema.md` , bump last-run.json schema_version to 2; add cost_usd + token_count optional fields per D-98</name>
  <files>.claude/skills/agentbloc/references/agent-memory-schema.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-98 last-run.json extension)
    - .claude/skills/agentbloc/references/agent-memory-schema.md (FULL file; identify last-run.json schema section + insertion point)
  </read_first>
  <action>
Surgically edit `.claude/skills/agentbloc/references/agent-memory-schema.md`. Discipline: insertion-only; no rewrites of upstream content.

1. Locate the last-run.json schema section (around L100-L143 per Phase 12 baseline).
2. Bump `schema_version: 1` references to `schema_version: 2` in the canonical schema example. Add a "Schema Version" subsection note: "Phase 14 bumps last-run.json schema to v2; v1 deployments remain readable (consumers parse cost_usd + token_count as null when absent)."
3. Add 2 new field entries to the schema field table:
   - `cost_usd` , number, optional , Today's running total USD cost for this agent (incremented after each wake by claude-wrap.sh per CTRL-02 + D-91).
   - `token_count` , object, optional , Today's running token totals for this agent: `{input: number, output: number, cached_input: number}`.
4. Update the canonical JSON schema example block to include both fields (with realistic example values).
5. Add a cross-reference line: "See `references/billing-rates.md` for the rate table claude-wrap.sh consults; `references/jsonl-log-schema.md` for the per-call cost_usd source-of-truth that aggregates into this rolling total."

Use Edit tool for surgical insertions. Do NOT rewrite Phase 12 D-65 schema versioning prose.
  </action>
  <acceptance_criteria>
    - File exists; lines 211..240
    - `grep -q 'schema_version: 2' file` returns true
    - `grep -q 'cost_usd' file` returns true
    - `grep -q 'token_count' file` returns true
    - `grep -q 'billing-rates.md' file` returns true
    - Phase 12 D-65 schema versioning section preserved (no rewrites)
    - Three-file contract intro (memory.md / state.json / last-run.json) preserved
  </acceptance_criteria>
  <done>agent-memory-schema.md extended. CTRL-02 cost-tracking schema closed at last-run.json layer. Plan 14-03 wires claude-wrap.sh emission into runtime-engine.md.</done>
</task>

<task type="auto">
  <name>Task 3: Surgically align `.claude/skills/agentbloc/references/audit-logging.md` correlation-ID format with Phase 13 D-75 per D-97</name>
  <files>.claude/skills/agentbloc/references/audit-logging.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-97 correlation-ID alignment)
    - .claude/skills/agentbloc/references/audit-logging.md (FULL file; identify correlation-ID section)
    - .claude/skills/agentbloc/references/correlation-id.md (Phase 13 D-75 canonical format)
  </read_first>
  <action>
Surgically edit `.claude/skills/agentbloc/references/audit-logging.md`. Discipline: minimal-surface alignment; preserve v1.0 examples as legacy notes.

1. Locate the "Correlation ID Pattern" section (around L46+).
2. Add a one-line note at the top of the section: `> **Phase 14 alignment (D-97):** v2.0 correlation-IDs follow the D-75 format documented in `references/correlation-id.md`. The legacy `sess-{agent_name}-{NNN}` examples below remain valid for v1.0 deployments + are the historical lineage for the child-ID `-sub-NNN` convention preserved in v2.0.`
3. Update the section's primary "Generation rules" intro to reference D-75: change "Generated at session start: `sess-{agent_name}-{NNN}`" to "v2.0 generation: `<source>-<UTC-Z-compact>-<nonce6>` per `references/correlation-id.md` (D-75); v1.0 legacy: `sess-{agent_name}-{NNN}`."
4. Preserve all existing examples + chain diagrams as-is (they remain useful illustrations of the parent-sub child convention).

Use Edit tool for the 2 surgical edits. Do NOT rewrite the v1.0 example sections.
  </action>
  <acceptance_criteria>
    - File exists
    - `grep -q 'correlation-id.md' file` returns true (cross-reference added)
    - `grep -q 'D-97' file` returns true (Phase 14 alignment note)
    - `grep -q 'D-75' file` returns true (canonical format reference)
    - Legacy `sess-` examples still present (preserved per D-83 surgical-edit discipline)
    - "Correlation ID Pattern" H2 section preserved
  </acceptance_criteria>
  <done>audit-logging.md aligned. D-97 correlation-ID drift removed. Phase 14 + v1.0 audit trails parseable by same tooling. Plan 14-03 (SKILL.md surgical wiring) can reference both files consistently.</done>
</task>

</tasks>
