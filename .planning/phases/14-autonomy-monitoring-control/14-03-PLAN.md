---
phase: 14-autonomy-monitoring-control
plan: 03
type: execute
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - .claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl
  - .claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl
  - .claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl
  - .claude/skills/agentbloc/references/phase-5-deployment.md
  - .claude/skills/agentbloc/references/incident-response.md
  - .claude/skills/agentbloc/SKILL.md
  - .claude/agents/deploy-engine.md
  - .claude/agents/runtime-engine.md
autonomous: true
requirements_addressed:
  - AUTON-01
  - MONITOR-03
  - MONITOR-04
  - MONITOR-06
  - CTRL-01
  - CTRL-04
estimated_tasks: 7

must_haves:
  truths:
    - "All 3 deployed-agent-skill templates (full/semi/supervised) gain a 'Side-effect Approval Routing' paragraph per D-94 inserted between Memory Protocol + Kill-Switch Discipline sections; per-template prose differs by autonomy level (full = no approval; semi = external-side-effect approval via autonomy-gate.sh + Telegram per D-85; supervised = stricter scope including cross-agent internal side-effects); $TOOL_REASONING env var contract cited in semi + supervised variants"
    - "phase-5-deployment.md gains Step 7.6 Monitor Wiring Hand-off section per D-95 between Phase 13 Step 7.5 Runtime Wiring Hand-off and existing Step 8 Job Definition Template; cites all 8 Plan 14-01 + 14-02 references; lists 'Closes requirements: AUTON-01..05, MONITOR-01..06, CTRL-01..05'; cites the monitor_wired sub-gate per D-93"
    - "incident-response.md gains H2 'Escalation Protocol' section per D-96 appended after Phase 13 Runtime Kill-Switch Semantics; documents 4-part template + persistent-halt + /resume slash-command + 2 worked examples; cross-references approval-router.md (shared Telegram-routing infrastructure) + escalation-protocol.md (Plan 14-01 Task 4 source-of-truth)"
    - "SKILL.md gains 4 new See-lines in Phase 5 entry (autonomy-controller.md + approval-router.md + jsonl-log-schema.md + reporting-hierarchy.md per D-93); monitor_wired sub-gate ANDed with deployment_artifacts_emitted + runtime_wired in State Transitions; Phase 6 Precondition extension verifies registry.yaml monitor.briefing_agent_id non-null; Summary Gate paragraph cites BRIEFING-FIRST-RUN.md as Phase 14 structural-completion signal"
    - "deploy-engine.md surgically extended <emission_targets> + <inputs> + <write_constraint> to include briefing-agent SKILL.md emission (uses briefing-agent.md.tmpl) + 3 Telegram thread IDs creation (approvals + briefing + escalations) persisted in registry.yaml monitor block; D-67 narrow Bash allow-list unchanged (briefing agent SKILL.md is a Write target; thread creation is via existing Bash(claude mcp list) for verification only , thread creation itself is a deferred Phase 14 manual step documented as user action in DEPLOY-REPORT.md Pending User Actions per D-70 single-emit discipline)"
    - "runtime-engine.md surgically extended <emission_targets> to include 4 new shell scripts emitted to .agentbloc/runtime/: approval-router.sh + escalation-router.sh + claude-wrap.sh + activity-feed-merge.sh per D-91/D-93/D-94; D-80 narrow Bash allow-list unchanged (Write tool emits the scripts; they execute at runtime not at deploy time)"
    - "Zero em-dashes (U+2014) added in NEW prose across the 8 surgically edited files (existing em-dashes in unchanged upstream content out of scope per Phase 13 D-83 precedent)"
    - "All Phase 12 + Phase 13 cross-references intact post-edit (D-83 surgical-edit discipline verified by grep-for-presence on key anchors)"
  artifacts:
    - path: ".claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl"
      provides: "Surgical extension: 'Side-effect Approval Routing' paragraph documenting full=proceed-without-approval semantics + PostToolUse audit cite"
      min_lines: 62
      max_lines: 80
      contains: "Side-effect Approval Routing"
    - path: ".claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl"
      provides: "Surgical extension: 'Side-effect Approval Routing' paragraph documenting semi=approval-on-external-side-effect via autonomy-gate.sh + Telegram per D-85; $TOOL_REASONING contract"
      min_lines: 62
      max_lines: 80
      contains: "$TOOL_REASONING"
    - path: ".claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl"
      provides: "Surgical extension: 'Side-effect Approval Routing' paragraph documenting supervised=stricter-scope (external + cross-agent internal); $TOOL_REASONING contract"
      min_lines: 62
      max_lines: 85
      contains: "$TOOL_REASONING"
    - path: ".claude/skills/agentbloc/references/phase-5-deployment.md"
      provides: "Surgical insertion of Step 7.6 Monitor Wiring Hand-off between Phase 13 Step 7.5 + existing Step 8; closes 16 Phase 14 requirements"
      min_lines: 1376
      max_lines: 1430
      contains: "Step 7.6"
    - path: ".claude/skills/agentbloc/references/incident-response.md"
      provides: "Surgical append of Escalation Protocol H2 section after Phase 13 Runtime Kill-Switch Semantics"
      min_lines: 227
      max_lines: 290
      contains: "Escalation Protocol"
    - path: ".claude/skills/agentbloc/SKILL.md"
      provides: "Surgical edits per D-93: 4 new Phase 5 See-lines + monitor_wired sub-gate + Phase 6 Precondition extension + Summary Gate paragraph"
      min_lines: 203
      max_lines: 230
      contains: "monitor_wired"
    - path: ".claude/agents/deploy-engine.md"
      provides: "Surgical extension of emission_targets to include briefing-agent SKILL.md (using briefing-agent.md.tmpl) + 3 Telegram thread IDs in registry.yaml monitor block"
      min_lines: 171
      max_lines: 220
      contains: "briefing-agent.md.tmpl"
    - path: ".claude/agents/runtime-engine.md"
      provides: "Surgical extension of emission_targets to include 4 shell scripts: approval-router.sh + escalation-router.sh + claude-wrap.sh + activity-feed-merge.sh"
      min_lines: 189
      max_lines: 240
      contains: "claude-wrap.sh"

verification:
  - "grep -q 'Side-effect Approval Routing' .claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl"
  - "grep -q 'Side-effect Approval Routing' .claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl"
  - "grep -q 'Side-effect Approval Routing' .claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl"
  - "grep -q '\\$TOOL_REASONING' .claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl"
  - "grep -q '\\$TOOL_REASONING' .claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl"
  - "grep -q 'Step 7.6' .claude/skills/agentbloc/references/phase-5-deployment.md"
  - "grep -q 'Escalation Protocol' .claude/skills/agentbloc/references/incident-response.md"
  - "grep -q 'monitor_wired' .claude/skills/agentbloc/SKILL.md"
  - "grep -q 'briefing-agent.md.tmpl' .claude/agents/deploy-engine.md"
  - "grep -q 'claude-wrap.sh' .claude/agents/runtime-engine.md"

threat_model: "Phase 14-03 mutates 8 existing files via surgical insertion-only edits per D-83. Threats: T-14-08 cross-reference drift (mitigation: explicit grep-for-presence on Phase 12 + 13 anchors after each edit); T-14-09 SKILL.md context-budget violation (mitigation: D-58 grep-for-absence verified on subagent-only files: briefing-agent.md.tmpl + autonomy-gate.sh shell script + claude-wrap.sh shell script remain absent from SKILL.md); T-14-10 deploy-engine + runtime-engine subagent scope creep (mitigation: D-67 + D-80 narrow Bash allow-lists unchanged; new emission targets are Write-only artifacts emitted as files, not new Bash invocations). All threats inherit from Phase 13 surgical-edit discipline."

---

# Plan 14-03: SKILL.md + Deployed-Skill Templates + Subagent Surgical Wiring

> 7 surgical edits across 8 existing files (3 deployed-skill templates + phase-5-deployment.md + incident-response.md + SKILL.md + deploy-engine.md + runtime-engine.md). Closes the Phase 14 structural loop. No NEW files created (Plan 14-01 + 14-02 already emitted all 12 net-new artifacts).

<context>
@.planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md
@.planning/phases/14-autonomy-monitoring-control/14-01-PLAN.md
@.planning/phases/14-autonomy-monitoring-control/14-02-PLAN.md
@.claude/skills/agentbloc/SKILL.md
@.claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl
@.claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl
@.claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl
@.claude/skills/agentbloc/references/phase-5-deployment.md
@.claude/skills/agentbloc/references/incident-response.md
@.claude/agents/deploy-engine.md
@.claude/agents/runtime-engine.md

<interfaces>
Decision anchors (from 14-CONTEXT.md):
- D-93 = SKILL.md surgical wiring (4 See-lines + monitor_wired sub-gate + Phase 6 Precondition extension)
- D-94 = deployed-agent-skill template approval-routing paragraph (3 templates)
- D-95 = phase-5-deployment.md Step 7.6 Monitor Wiring Hand-off
- D-96 = incident-response.md Escalation Protocol H2 section

Twin patterns:
- All 7 surgical edits follow Phase 13 D-83 precedent: insertion-only; no upstream rewrites; preservation verified by grep-for-presence on key anchors after each edit.
- Plan 14-03 mirrors Plan 13-03 cadence: 4 surgical edits to existing files.
- deploy-engine.md + runtime-engine.md extensions follow Phase 13 D-67 + D-80 narrow Bash discipline: NEW Write targets only; NO new Bash allow-list entries.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgically extend `.claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl` with 'Side-effect Approval Routing' paragraph (autonomy=full semantics)</name>
  <files>.claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-94 surgical extension)
    - .claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl (FULL file; identify insertion point between Memory Protocol + Kill-Switch Discipline sections)
    - .claude/skills/agentbloc/references/autonomy-controller.md (Plan 14-01 Task 2; for autonomy=full semantics)
  </read_first>
  <action>
Surgically insert a new H2 section "Side-effect Approval Routing" between the Memory Protocol section and the Kill-Switch Discipline section of `.claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl`.

Locate insertion point: after Memory Protocol section close, before Kill-Switch Discipline section open (around L48-L52 area in current 62-line template).

Insert these ~8 lines (zero em-dashes; pure {{var}} substitution; no Jinja blocks):

```markdown

## Side-effect Approval Routing

You operate at `autonomy: full`. External-side-effect tool invocations (send_*, post_*, create_*, update_*, delete_*, transfer_*, pay_*) proceed WITHOUT human approval. Every call is recorded by the PostToolUse audit hook to `.claude/agents/logs/<DATE>/{{agent.id}}.jsonl` per `references/jsonl-log-schema.md`. The PreToolUse `autonomy-gate.sh` hook reads your `autonomy: full` from `registry.yaml` + permits all tools by default. You are responsible for honoring per-tool blast-radius constraints documented in `references/blast-radius.md`.

```

Use Edit tool with old_string = exact line containing the Memory Protocol section close + new_string = same line + the new section. Do NOT rewrite the existing 8 lines that are already in the template.
  </action>
  <acceptance_criteria>
    - File exists; lines 62..80 (was 62; expect ~70 after insertion)
    - `grep -q 'Side-effect Approval Routing' file` returns true
    - `grep -q 'autonomy-gate.sh' file` returns true
    - Existing sections preserved: `## Memory Protocol`, `## Kill-Switch Discipline`
    - `<!-- agentbloc:template autonomy=full schema_version=1 -->` last line preserved
    - Zero Jinja blocks: `grep -E '\{%.+%\}' file` returns nothing
  </acceptance_criteria>
  <done>full template extended. AUTON-01 prose layer (full variant) closed. Tasks 2 + 3 below extend semi + supervised variants symmetrically.</done>
</task>

<task type="auto">
  <name>Task 2: Surgically extend `.claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl` with 'Side-effect Approval Routing' paragraph (autonomy=semi semantics + $TOOL_REASONING contract)</name>
  <files>.claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-94 surgical extension)
    - .claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl (FULL file; identify insertion point)
    - .claude/skills/agentbloc/references/approval-router.md (Plan 14-01 Task 3; for semi-approval flow)
  </read_first>
  <action>
Surgically insert "Side-effect Approval Routing" H2 section between Memory Protocol + Kill-Switch Discipline sections.

Insert these ~10 lines (zero em-dashes):

```markdown

## Side-effect Approval Routing

You operate at `autonomy: semi`. Before invoking any external-side-effect tool (`send_*`, `post_*`, `create_*`, `update_*`, `delete_*`, `transfer_*`, `pay_*` MCP tools, or any Bash invocation that writes outside `.agentbloc/agents/{{agent.id}}/`), you MUST export `$TOOL_REASONING` (1-2 sentence rationale) describing why this action is necessary, what it changes, and reversibility. The PreToolUse `autonomy-gate.sh` hook reads your autonomy + reasoning, dispatches an approval request to the team's Telegram approvals thread per `references/approval-router.md`, and waits up to 600s for `/approve <correlation_id>` or `/deny <correlation_id>` reply. Approved tools proceed; denied/timeout tools are blocked with `result: blocked` logged to your JSONL. Internal-side-effect tools (writes within your own `.agentbloc/agents/{{agent.id}}/` memory) proceed without approval. Read-only tools (Read, Grep, Glob, fetch-pattern MCP) NEVER trigger approval.

```

Use Edit tool. Preserve existing 8-line autonomy=semi prose at L48-L55 and the kill-switch comment last line.
  </action>
  <acceptance_criteria>
    - File exists; lines 62..80
    - `grep -q 'Side-effect Approval Routing' file` returns true
    - `grep -q '\\$TOOL_REASONING' file` returns true
    - `grep -q 'autonomy-gate.sh' file` returns true
    - `grep -q 'approval-router.md' file` returns true
    - Existing semi-autonomy prose at Memory Protocol section preserved
    - `<!-- agentbloc:template autonomy=semi schema_version=1 -->` last line preserved
    - Zero Jinja blocks
  </acceptance_criteria>
  <done>semi template extended. AUTON-01 + AUTON-02 prose layer (semi variant) closed.</done>
</task>

<task type="auto">
  <name>Task 3: Surgically extend `.claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl` with 'Side-effect Approval Routing' paragraph (autonomy=supervised stricter scope)</name>
  <files>.claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-94 surgical extension)
    - .claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl (FULL file; identify insertion point)
    - .claude/skills/agentbloc/references/approval-router.md (Plan 14-01 Task 3)
  </read_first>
  <action>
Surgically insert "Side-effect Approval Routing" H2 section. ~12 lines (zero em-dashes):

```markdown

## Side-effect Approval Routing

You operate at `autonomy: supervised`. EVERY external-side-effect tool AND every cross-agent internal-side-effect tool (writes to `.agentbloc/agents/<other-id>/...`) requires human approval before execution. Before invoking any such tool, you MUST export `$TOOL_REASONING` (1-2 sentence rationale). The PreToolUse `autonomy-gate.sh` hook ALWAYS dispatches an approval request to the team's Telegram approvals thread per `references/approval-router.md`, regardless of tool category (the only exemption is your own internal memory writes within `.agentbloc/agents/{{agent.id}}/state.json`). The hook waits up to 600s for `/approve <correlation_id>` or `/deny <correlation_id>` reply. Approved tools proceed; denied/timeout tools are blocked with `result: blocked` logged. The supervised tier is the safest autonomy level + appropriate for high-blast-radius agents (financial, legal, customer-facing) per `references/blast-radius.md`.

```

Use Edit tool. Preserve existing supervised-autonomy prose + kill-switch comment.
  </action>
  <acceptance_criteria>
    - File exists; lines 62..85
    - `grep -q 'Side-effect Approval Routing' file` returns true
    - `grep -q '\\$TOOL_REASONING' file` returns true
    - `grep -q 'autonomy-gate.sh' file` returns true
    - `grep -q 'cross-agent' file` returns true
    - Existing supervised prose preserved
    - `<!-- agentbloc:template autonomy=supervised schema_version=1 -->` last line preserved
    - Zero Jinja blocks
  </acceptance_criteria>
  <done>supervised template extended. AUTON-01 + AUTON-02 prose layer (supervised variant) closed. All 3 deployed-agent-skill templates now encode autonomy-aware approval routing per D-94.</done>
</task>

<task type="auto">
  <name>Task 4: Surgically insert Step 7.6 Monitor Wiring Hand-off into `.claude/skills/agentbloc/references/phase-5-deployment.md` per D-95</name>
  <files>.claude/skills/agentbloc/references/phase-5-deployment.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-95 Step 7.6 specification)
    - .claude/skills/agentbloc/references/phase-5-deployment.md (FULL file; identify Step 7.5 close + Step 8 open as insertion boundary)
  </read_first>
  <action>
Surgically insert new H3 section "Step 7.6: Monitor Wiring Hand-off (Phase 14)" between existing Step 7.5 close and Step 8 (Job Definition Template) open. Mirror Phase 13 Step 7.5 grammar exactly.

Locate insertion point: after `### Step 7.5: Runtime Wiring Hand-off (Phase 13)` section close (around L840+).

Insert ~25 lines (zero em-dashes):

```markdown

### Step 7.6: Monitor Wiring Hand-off (Phase 14)

After runtime-engine emits crontab + n8n routes (Step 7.5), the Phase 14 monitor wiring activates: deploy-engine emits the briefing-agent SKILL.md using `templates/briefing-agent.md.tmpl` per D-88; runtime-engine emits 4 shell scripts to `.agentbloc/runtime/`: `approval-router.sh` (per D-85), `escalation-router.sh` (per D-86), `claude-wrap.sh` (per D-91), `activity-feed-merge.sh` (per D-90). The user creates 3 Telegram threads (approvals, briefing, escalations) per CTRL-01 + the IDs are persisted in `registry.yaml monitor` block by deploy-engine.

(This step slots between the Phase 13 Step 7.5 Runtime Wiring Hand-off above and the existing Step 8 Job Definition Template below; it is conceptually Step 7.6 of the deployment chain.)

**See:**
- [references/jsonl-log-schema.md](jsonl-log-schema.md) for the canonical 12-field log entry schema (D-87) consumed by every deployed agent.
- [references/autonomy-controller.md](autonomy-controller.md) for the D-84 two-layer enforcement contract + tool-classification table.
- [references/approval-router.md](approval-router.md) for the D-85 approval-router.sh shell contract + Telegram dispatch + slash-command syntax.
- [references/escalation-protocol.md](escalation-protocol.md) for the D-86 4-part escalation template + persistent-halt + /resume slash-command.
- [references/reporting-hierarchy.md](reporting-hierarchy.md) for the MONITOR-05 hierarchy chain + v2.0 flat / v2.5 team-lead split.
- [references/task-locking.md](task-locking.md) for the D-89 file+flock locking contract.
- [references/activity-feed.md](activity-feed.md) for the D-90 daily merge + activity-feed-merge.sh shell contract.
- [references/billing-rates.md](billing-rates.md) for the CTRL-02 cost-rate table consumed by claude-wrap.sh.
- [templates/briefing-agent.md.tmpl](../templates/briefing-agent.md.tmpl) for the D-88 default briefing agent template.

**Closes requirements:** AUTON-01 (per-agent autonomy levels), AUTON-02 (external-side-effect approval routing), AUTON-03 (append-only approval log), AUTON-04 (escalation with full context), AUTON-05 (4-part escalation message), MONITOR-01 (canonical JSONL log schema), MONITOR-02 (per-agent per-day log path), MONITOR-03 (registry monitor block), MONITOR-04 (default briefing agent), MONITOR-05 (hierarchical reporting chain), MONITOR-06 (pluggable presentation layer), CTRL-01 (separate Telegram approval thread), CTRL-02 (per-log-line cost tracking), CTRL-03 (file+flock task locking), CTRL-04 (status badges), CTRL-05 (daily activity feed merge).

**`monitor_wired` sub-gate (per D-93):** This step closes the third sub-gate of Phase 5 -> Phase 6 transition. The sub-gate is set true when (1) deploy-engine has emitted the briefing-agent SKILL.md + persisted briefing_agent_id non-null in registry.yaml monitor block, (2) runtime-engine has emitted all 4 shell scripts to `.agentbloc/runtime/`, AND (3) the user has created the 3 Telegram threads + the IDs are persisted (approval_thread_id + briefing_thread_id + escalations_thread_id all non-null). If any condition is unmet, Phase 6 entry halts.

```

Use Edit tool. Do NOT rewrite Step 7.5 or Step 8 sections. Surgical insertion only.
  </action>
  <acceptance_criteria>
    - File exists; lines 1376..1430
    - `grep -q '^### Step 7.6: Monitor Wiring Hand-off' file` returns true
    - `grep -q 'jsonl-log-schema.md' file` returns true
    - `grep -q 'autonomy-controller.md' file` returns true
    - `grep -q 'approval-router.md' file` returns true
    - `grep -q 'briefing-agent.md.tmpl' file` returns true
    - `grep -q 'monitor_wired' file` returns true
    - "Closes requirements: AUTON-01..05, MONITOR-01..06, CTRL-01..05" cited (or equivalent enumeration with all 16 IDs)
    - Existing Step 7.5 (Phase 13 Runtime Wiring Hand-off) preserved
    - Existing Step 8 Job Definition Template preserved (no rewrite)
  </acceptance_criteria>
  <done>phase-5-deployment.md extended. Step 7.6 Monitor Wiring Hand-off in place. Closes 16 Phase 14 requirements at the user-visible deploy-flow layer.</done>
</task>

<task type="auto">
  <name>Task 5: Surgically append H2 'Escalation Protocol' section to `.claude/skills/agentbloc/references/incident-response.md` per D-96</name>
  <files>.claude/skills/agentbloc/references/incident-response.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-96 escalation protocol section)
    - .claude/skills/agentbloc/references/incident-response.md (FULL file; identify Phase 13 Runtime Kill-Switch Semantics section close as insertion point)
    - .claude/skills/agentbloc/references/escalation-protocol.md (Plan 14-01 Task 4 source-of-truth)
  </read_first>
  <action>
Surgically append H2 "Escalation Protocol" section after Phase 13 "Runtime Kill-Switch Semantics" section close (around L226).

Insert ~30 lines (zero em-dashes):

```markdown

## Escalation Protocol

Phase 14 introduces a third Telegram thread per team: `escalations`, distinct from `approvals` (CTRL-01) and `briefing` (MONITOR-04). Escalations route agent failures + critical errors with a structured 4-part message (what tried / why failed / options / recommended next action) per AUTON-04 + AUTON-05.

### Trigger

An agent escalates when one of:
1. An uncaught exception during wake.
2. A critical-action tool returning `result: failure` (where critical = the action was the agent's primary purpose for this wake, e.g., `mcp__plaid__list_transactions` for gestor-cobros).
3. An explicit `escalate(...)` call from agent prose when the agent assesses it cannot make progress.

### Persistent Halt + /resume

On escalation, the agent:
1. Appends a JSONL log entry with `priority: critical` + `action: escalation` per `references/jsonl-log-schema.md`.
2. Sets `last-run.json status: error` per `references/agent-memory-schema.md` (Phase 14 schema_version 2).
3. Invokes `escalation-router.sh telegram-escalate <agent-id> <correlation_id> ...` to dispatch the 4-part Telegram message to the escalations thread.
4. Exits the wake with `wake_outcome: escalated`.

Subsequent wakes (cron / webhook / inter) check `last-run.json status` first; if `error`, the wake template section 1 (after kill-switch check) detects it + short-circuits with `wake_outcome: skipped-prior-error` UNTIL a `/resume <correlation_id> [free-text instructions]` reply lands in the escalations thread. The `/resume` reply (handled by an inbound n8n route created by deploy-engine per registry.yaml escalations binding) updates `last-run.json status: idle` + appends free-text instructions to `memory.md` Open Items section (D-64 location). Next cron/webhook fires resumes work.

### Kill-Switch vs Escalation Sequencing

Kill-switch checks ALWAYS precede escalation router invocation: an agent halted by kill-switch does NOT escalate (the halt IS the alarm). Escalations explicitly bypass the kill-switch check ONLY when the escalation IS the kill-switch firing event itself (avoids self-suppression of the alarm).

### See

- [references/escalation-protocol.md](escalation-protocol.md) for the full 4-part template + 3 worked examples (gmail rate-limit, plaid auth-revoked, BBVA 2FA-expired).
- [references/approval-router.md](approval-router.md) for the shared Telegram-routing infrastructure (escalation-router.sh + approval-router.sh follow the same shell-script + long-poll pattern).

```

Use Edit tool. Preserve all upstream sections (v1.0 dual-path kill-switch + Phase 13 Runtime Kill-Switch Semantics + Quick Reference).
  </action>
  <acceptance_criteria>
    - File exists; lines 227..290
    - `grep -q '^## Escalation Protocol' file` returns true
    - `grep -q 'escalation-protocol.md' file` returns true (cross-reference)
    - `grep -q '/resume' file` returns true
    - `grep -q 'wake_outcome: escalated' file` returns true
    - "4-part" or "4-section" cited
    - Existing Phase 13 Runtime Kill-Switch Semantics section preserved (D-83 surgical-edit discipline)
    - v1.0 dual-path content preserved
  </acceptance_criteria>
  <done>incident-response.md extended. Escalation Protocol section in place. AUTON-04 + AUTON-05 user-facing reference closure.</done>
</task>

<task type="auto">
  <name>Task 6: Surgical wiring of `.claude/skills/agentbloc/SKILL.md` per D-93 (4 new See-lines + monitor_wired sub-gate + Phase 6 Precondition extension + Summary Gate update)</name>
  <files>.claude/skills/agentbloc/SKILL.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-93 SKILL.md surgical edits)
    - .claude/skills/agentbloc/SKILL.md (FULL file; identify Phase 5 entry, State Transitions, Phase 6 entry, Summary Gate)
  </read_first>
  <action>
4 surgical edits to SKILL.md:

1. **Phase 5 entry: 4 new See-lines.** Locate Phase 5 entry (around L130-L155); after the Phase 13 See-lines for n8n-integration.md + runtime-coordination.md + correlation-id.md (L151-L153 currently), insert 4 new See-lines:

```markdown
See [references/jsonl-log-schema.md](references/jsonl-log-schema.md)
See [references/autonomy-controller.md](references/autonomy-controller.md)
See [references/approval-router.md](references/approval-router.md)
See [references/reporting-hierarchy.md](references/reporting-hierarchy.md)
```

2. **State Transitions: monitor_wired sub-gate.** Locate the State Transitions section paragraph that mentions `runtime_wired` (around L43-L44). Surgically extend the sub-gate sentence: change "requires `runtime_wired` AND `deployment_artifacts_emitted` sub-gates" to "requires `deployment_artifacts_emitted` AND `runtime_wired` AND `monitor_wired` sub-gates (Phase 14 D-81-pattern)".

3. **Phase 6 Precondition extension.** Locate Phase 6 Precondition (around L159). Surgically extend the precondition list: add a new bullet/paragraph: "AND `monitor.briefing_agent_id` non-null (briefing agent deployed) AND `monitor.approval_thread_id` non-null (Telegram approvals thread created); if either unmet, return Phase 5 + re-run Step 7.6 (Monitor Wiring Hand-off) via deploy-engine + runtime-engine subagents."

4. **Summary Gate paragraph extension.** Locate Summary Gate paragraph (around L146). Surgically extend: change "deploy-engine subagent emits DEPLOY-REPORT.md ... runtime-engine emits RUNTIME-REPORT.md" to "deploy-engine subagent emits DEPLOY-REPORT.md ... runtime-engine emits RUNTIME-REPORT.md ... briefing-agent first-run emits BRIEFING-FIRST-RUN.md (Phase 14 structural-completion signal closing the `monitor_wired` sub-gate). Phase 5 -> Phase 6 transition requires ALL THREE sub-gates true."

Use Edit tool for each of the 4 surgical edits. Do NOT rewrite Phase 1-4 entries, Hard Gates, Quality Checklist, or Reference Implementation sections. D-58 absence checks: `grep -E 'briefing-agent.md.tmpl|autonomy-gate.sh|approval-router.sh|claude-wrap.sh|activity-feed-merge.sh|escalation-router.sh' SKILL.md` MUST return zero matches after edits (these are subagent-only / runtime-only artifacts NOT cited in SKILL.md).
  </action>
  <acceptance_criteria>
    - File exists; lines 203..230
    - 4 new See-lines present: `grep -q 'jsonl-log-schema.md' SKILL.md` AND `grep -q 'autonomy-controller.md' SKILL.md` AND `grep -q 'approval-router.md' SKILL.md` AND `grep -q 'reporting-hierarchy.md' SKILL.md`
    - `grep -q 'monitor_wired' SKILL.md` returns true
    - `grep -q 'BRIEFING-FIRST-RUN.md' SKILL.md` returns true (Summary Gate update)
    - `grep -q 'briefing_agent_id' SKILL.md` returns true (Phase 6 Precondition)
    - D-58 absence: `grep -E 'briefing-agent.md.tmpl|autonomy-gate.sh|approval-router.sh|claude-wrap.sh|activity-feed-merge.sh|escalation-router.sh' SKILL.md` returns ZERO matches (subagent-only files NOT cited in SKILL.md)
    - Phase 1-4 entries + Hard Gates + Quality Checklist + Reference Implementation preserved
  </acceptance_criteria>
  <done>SKILL.md surgically wired. monitor_wired sub-gate in place. Phase 14 user-visible flow integration complete.</done>
</task>

<task type="auto">
  <name>Task 7: Surgically extend `.claude/agents/deploy-engine.md` and `.claude/agents/runtime-engine.md` <emission_targets> + briefing-agent + 4 shell scripts</name>
  <files>.claude/agents/deploy-engine.md, .claude/agents/runtime-engine.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-93 + D-94 subagent extensions)
    - .claude/agents/deploy-engine.md (FULL file; identify <emission_targets> + <inputs> + <write_constraint> blocks)
    - .claude/agents/runtime-engine.md (FULL file; identify <emission_targets> block)
  </read_first>
  <action>
Two surgical extensions, one per subagent file, in single atomic commit:

**A. deploy-engine.md surgical extension:**

1. In `<inputs>` block: add "`templates/briefing-agent.md.tmpl` (Phase 14 default briefing agent template)" to the templates discovery list.
2. In `<write_constraint>` block: add `.claude/skills/<team>-briefing/SKILL.md` to the 7-path write allow-list (becomes 8-path).
3. In `<emission_targets>` block: add new bullet "`.claude/skills/<team>-briefing/SKILL.md` (Phase 14 default briefing agent emitted using `templates/briefing-agent.md.tmpl` per D-88; one per team; named `<team-slug>-briefing`)".
4. In `<emission_targets>` block: add new bullet for registry.yaml monitor block: "`.agentbloc/agents/registry.yaml` extended with `monitor:` block (sibling to `runtime:`) containing `briefing_agent_id`, `briefing_thread_id`, `approval_thread_id`, `escalations_thread_id`, `presentation`, `briefing_cron`, `lock_defaults`, `locked_resources` per Phase 14 D-93. Telegram thread IDs are persisted but thread CREATION is a Pending User Action documented in DEPLOY-REPORT.md (deploy-engine cannot create threads via narrow Bash allow-list per D-67); user creates threads manually in Telegram + supplies IDs to runtime-engine for thread-ID injection."

D-67 narrow Bash allow-list UNCHANGED: only Write tool emits the new SKILL.md + the registry monitor block; no new Bash invocations.

**B. runtime-engine.md surgical extension:**

1. In `<emission_targets>` block: add 4 new bullets for shell scripts:
   - "`.agentbloc/runtime/approval-router.sh` (Phase 14 D-85 Telegram approval dispatcher; 600s long-poll; slash-command parser)"
   - "`.agentbloc/runtime/escalation-router.sh` (Phase 14 D-86 Telegram escalation dispatcher; 4-part template POST)"
   - "`.agentbloc/runtime/claude-wrap.sh` (Phase 14 D-91 + CTRL-02 cost wrapper around claude -p; intercepts token usage from API response trailer; computes cost_usd via billing-rates.md)"
   - "`.agentbloc/runtime/activity-feed-merge.sh` (Phase 14 D-90 daily merger; globs per-agent JSONL files + sorts ascending; idempotent re-runs)"

2. In `<emission_targets>` block: add bullet for autonomy-gate hook installation note: "`.claude/hooks/autonomy-gate.sh` PreToolUse hook (Phase 14 D-84 enforcement; intercepts side-effect tools; reads autonomy from registry.yaml; dispatches to approval-router.sh on semi/supervised; sequences AFTER kill-switch-check.sh per alphabetical hook order)".

D-80 narrow Bash allow-list UNCHANGED: scripts are Write targets emitted as files; they execute at runtime not at deploy time. Do NOT add any new Bash entries to the runtime-engine frontmatter.

Use Edit tool for each surgical insertion. Both files extended in same atomic commit (one file at a time via separate Edit calls but committed together).
  </action>
  <acceptance_criteria>
    - deploy-engine.md exists; lines 171..220 (was 170; expect ~190 after edits)
    - `grep -q 'briefing-agent.md.tmpl' .claude/agents/deploy-engine.md` returns true
    - `grep -q '<team>-briefing' .claude/agents/deploy-engine.md` returns true
    - `grep -q 'monitor:' .claude/agents/deploy-engine.md` returns true
    - deploy-engine.md frontmatter Bash allow-list UNCHANGED (still 4 entries: shasum + crontab -l + claude agents list + claude mcp list per D-67)
    - runtime-engine.md exists; lines 189..240 (was 188; expect ~215 after edits)
    - `grep -q 'approval-router.sh' .claude/agents/runtime-engine.md` returns true
    - `grep -q 'escalation-router.sh' .claude/agents/runtime-engine.md` returns true
    - `grep -q 'claude-wrap.sh' .claude/agents/runtime-engine.md` returns true
    - `grep -q 'activity-feed-merge.sh' .claude/agents/runtime-engine.md` returns true
    - `grep -q 'autonomy-gate.sh' .claude/agents/runtime-engine.md` returns true
    - runtime-engine.md frontmatter Bash allow-list UNCHANGED (still 4 entries: crontab:* + shasum:* + claude agents list + claude mcp list per D-80)
    - Existing <role> + <write_constraint> + <coordination_protocol> blocks preserved in both files
  </acceptance_criteria>
  <done>deploy-engine + runtime-engine extended. Phase 14 monitor wiring fully integrated into the existing subagent fleet. Plan 14-03 complete; Phase 14 structurally closed pending /gsd-verify-phase 14 + ROADMAP/STATE close-out commit.</done>
</task>

</tasks>
