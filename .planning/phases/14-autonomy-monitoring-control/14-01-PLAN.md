---
phase: 14-autonomy-monitoring-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/agentbloc/references/jsonl-log-schema.md
  - .claude/skills/agentbloc/references/autonomy-controller.md
  - .claude/skills/agentbloc/references/approval-router.md
  - .claude/skills/agentbloc/references/escalation-protocol.md
  - .claude/skills/agentbloc/references/reporting-hierarchy.md
  - .claude/skills/agentbloc/references/task-locking.md
  - .claude/skills/agentbloc/references/activity-feed.md
  - .claude/skills/agentbloc/references/billing-rates.md
  - .claude/skills/agentbloc/examples/arco-rooms-monitor-fixtures.md
autonomous: true
requirements_addressed:
  - AUTON-01
  - AUTON-02
  - AUTON-03
  - AUTON-04
  - AUTON-05
  - MONITOR-01
  - MONITOR-02
  - MONITOR-05
  - CTRL-01
  - CTRL-02
  - CTRL-03
  - CTRL-05
estimated_tasks: 9

must_haves:
  truths:
    - "references/jsonl-log-schema.md exists as the canonical 12-field log schema (D-87) consumed by every deployed agent + briefing-agent + activity-feed merger; cites RFC 8785 JCS canonicalization + path convention `.claude/agents/logs/<YYYY-MM-DD>/<agent-id>.jsonl` + per-day per-agent file rationale + approvals.jsonl + escalations.jsonl sibling files using same schema"
    - "references/autonomy-controller.md exists documenting D-84 two-layer enforcement (SKILL.md prose + PreToolUse hook autonomy-gate.sh); definitive table of MCP tool name patterns -> side-effect category (external/internal/read-only); per-autonomy behavior matrix (full=proceed, semi=approval, supervised=always-approval); $TOOL_REASONING env var contract"
    - "references/approval-router.md exists documenting D-85 approval flow (PreToolUse hook -> approval-router.sh -> Telegram -> long-poll -> append to approvals.jsonl); slash-command syntax `/approve <correlation_id>` and `/deny <correlation_id>`; thread separation per CTRL-01; 600s default timeout; approval-router.sh shell script contract"
    - "references/escalation-protocol.md exists documenting D-86 escalation flow (4-part template what-tried/why-failed/options/recommended-next; persistent halt status=error until /resume; escalations Telegram thread distinct from approvals); 3 worked examples (gmail rate-limit, plaid auth-revoked, BBVA 2FA-expired)"
    - "references/reporting-hierarchy.md exists documenting MONITOR-05 chain (individual agents -> team leads -> briefing agent -> human); v2.0 ships flat hierarchy (every agent writes own log; briefing reads all); team-lead aggregation v2.5 placeholder text; critical escalations bypass briefing"
    - "references/task-locking.md exists documenting D-89 file+flock locking (`.agentbloc/locks/<resource-slug>.lock` JSON content); flock(1) atomicity guarantee; 30-min default expiry; resource-slug grammar (kebab-case domain-scoped); release semantics; deferred-locked wake_outcome handling"
    - "references/activity-feed.md exists documenting D-90 daily-merge contract (`.claude/agents/logs/<YYYY-MM-DD>/activity-feed.jsonl`); briefing-agent triggers `activity-feed-merge.sh <DATE>` at briefing time; idempotent re-runs; byte-identical to source per-agent log entries"
    - "references/billing-rates.md exists with static rate table for v2.0 ship date (Sonnet 4.6 + Opus 4.7 + Haiku 4.5 input/output/cache token rates per million); claude-wrap.sh references this when computing cost_usd; subscription-mode notional-cost note"
    - "examples/arco-rooms-monitor-fixtures.md exists with 5 sample JSONL log lines (action types: tool_call success, decision, escalation, approval_request, approval_response) + 1 day's activity-feed.jsonl (chronological merge of 3 agents) + 1 sample briefing message + 1 sample escalation message + 1 lock file + 1 approval-thread message exchange"
    - "Zero em-dashes (U+2014) across all 9 emitted files; grep -c '—' per file returns 0"
    - "All 9 files reachable from Plan 14-02 briefing-agent template + Plan 14-03 SKILL.md surgical wiring"
  artifacts:
    - path: ".claude/skills/agentbloc/references/jsonl-log-schema.md"
      provides: "MONITOR-01 12-field canonical log schema + MONITOR-02 path convention + RFC 8785 JCS canonicalization + approvals.jsonl + escalations.jsonl sibling schema"
      min_lines: 180
      max_lines: 220
      contains: "schema_version"
    - path: ".claude/skills/agentbloc/references/autonomy-controller.md"
      provides: "AUTON-01 + AUTON-02 enforcement contract: SKILL.md prose layer + PreToolUse hook layer; tool-classification table; per-autonomy behavior matrix"
      min_lines: 160
      max_lines: 200
      contains: "autonomy-gate.sh"
    - path: ".claude/skills/agentbloc/references/approval-router.md"
      provides: "AUTON-02 + AUTON-03 + CTRL-01 approval routing: approval-router.sh shell contract; Telegram dispatch + long-poll; slash-command syntax; approvals.jsonl append discipline"
      min_lines: 150
      max_lines: 190
      contains: "/approve"
    - path: ".claude/skills/agentbloc/references/escalation-protocol.md"
      provides: "AUTON-04 + AUTON-05 escalation: 4-part template + persistent-halt semantics + /resume slash-command + 3 worked examples"
      min_lines: 140
      max_lines: 180
      contains: "Recommended:"
    - path: ".claude/skills/agentbloc/references/reporting-hierarchy.md"
      provides: "MONITOR-05 hierarchy chain: agents -> team leads -> briefing -> human; v2.0 flat; v2.5 team-lead placeholder; critical-escalation bypass"
      min_lines: 80
      max_lines: 120
      contains: "team leads"
    - path: ".claude/skills/agentbloc/references/task-locking.md"
      provides: "CTRL-03 file+flock locking: lock-file JSON schema + acquire/release semantics + expiry + resource-slug grammar + deferred-locked wake outcome"
      min_lines: 90
      max_lines: 130
      contains: "flock"
    - path: ".claude/skills/agentbloc/references/activity-feed.md"
      provides: "CTRL-05 daily-merge feed: activity-feed.jsonl path + activity-feed-merge.sh contract + idempotency + byte-identity rule"
      min_lines: 60
      max_lines: 100
      contains: "activity-feed.jsonl"
    - path: ".claude/skills/agentbloc/references/billing-rates.md"
      provides: "CTRL-02 cost rates table: Claude API token rates as of v2.0 ship date; claude-wrap.sh consumer contract; subscription-mode notional-cost"
      min_lines: 50
      max_lines: 90
      contains: "claude-wrap.sh"
    - path: ".claude/skills/agentbloc/examples/arco-rooms-monitor-fixtures.md"
      provides: "Phase 16 golden-file fixture: 5 sample JSONL lines + 1 activity-feed.jsonl + 1 briefing message + 1 escalation message + 1 lock file + 1 approval exchange"
      min_lines: 180
      max_lines: 280
      contains: "schema_version"
  key_links:
    - from: ".claude/skills/agentbloc/references/jsonl-log-schema.md"
      to: ".claude/skills/agentbloc/references/correlation-id.md"
      reason: "log entries cite correlation-id.md for the D-75 format that populates the correlation_id field"
    - from: ".claude/skills/agentbloc/references/autonomy-controller.md"
      to: ".claude/skills/agentbloc/references/blast-radius.md"
      reason: "autonomy-to-blast-radius mapping cites v1.0 + DSGN-04 blast-radius scoring"
    - from: ".claude/skills/agentbloc/references/approval-router.md"
      to: ".claude/skills/agentbloc/references/telegram-patterns.md"
      reason: "thread-per-domain pattern + Telegram MCP usage borrowed from telegram-patterns.md"
    - from: ".claude/skills/agentbloc/references/escalation-protocol.md"
      to: ".claude/skills/agentbloc/references/incident-response.md"
      reason: "escalations are runtime kill-switch siblings; cross-reference for triage"
    - from: ".claude/skills/agentbloc/references/activity-feed.md"
      to: ".claude/skills/agentbloc/references/jsonl-log-schema.md"
      reason: "merge consumes per-agent JSONL files defined by the schema"
    - from: ".claude/skills/agentbloc/examples/arco-rooms-monitor-fixtures.md"
      to: ".claude/skills/agentbloc/references/jsonl-log-schema.md"
      reason: "fixture lines validate against the schema; Phase 16 golden-file harness diffs structurally"

verification:
  - "ls -la .claude/skills/agentbloc/references/jsonl-log-schema.md"
  - "ls -la .claude/skills/agentbloc/references/autonomy-controller.md"
  - "ls -la .claude/skills/agentbloc/references/approval-router.md"
  - "ls -la .claude/skills/agentbloc/references/escalation-protocol.md"
  - "ls -la .claude/skills/agentbloc/references/reporting-hierarchy.md"
  - "ls -la .claude/skills/agentbloc/references/task-locking.md"
  - "ls -la .claude/skills/agentbloc/references/activity-feed.md"
  - "ls -la .claude/skills/agentbloc/references/billing-rates.md"
  - "ls -la .claude/skills/agentbloc/examples/arco-rooms-monitor-fixtures.md"
  - "grep -c '—' .claude/skills/agentbloc/references/jsonl-log-schema.md .claude/skills/agentbloc/references/autonomy-controller.md .claude/skills/agentbloc/references/approval-router.md .claude/skills/agentbloc/references/escalation-protocol.md .claude/skills/agentbloc/references/reporting-hierarchy.md .claude/skills/agentbloc/references/task-locking.md .claude/skills/agentbloc/references/activity-feed.md .claude/skills/agentbloc/references/billing-rates.md .claude/skills/agentbloc/examples/arco-rooms-monitor-fixtures.md | awk -F: '{s+=$2} END{exit s>0?1:0}'"

threat_model: "Phase 14 consumes credentials (Telegram bot token in approval-router.sh, escalation-router.sh) + writes append-only logs + persists approval decisions. Threats: T-14-01 prompt injection via Telegram message content (mitigation: approval-router.sh treats incoming text as data, not instructions; matches /approve|/deny prefix only); T-14-02 cost-tracking integrity (mitigation: claude-wrap.sh reads token usage from Claude API response trailer, not agent self-report); T-14-03 lock-file race condition (mitigation: flock(1) atomicity guarantees single-writer); T-14-04 PII leak via JSONL log details field (mitigation: agent-side redaction per audit-logging.md PII rules; details field is structured JSON not freeform); T-14-05 KILL_SWITCH bypass via escalation channel (mitigation: kill-switch checks happen BEFORE escalation router invocation). All threats inherit from Phase 12/13 mitigations + Phase 14 adds defense-in-depth at the hook layer."

---

# Plan 14-01: Monitor + Autonomy + Control Plane References

> 9 net-new reference + fixture artifacts for AUTON-01..05 + MONITOR-01,02,05 + CTRL-01,02,03,05. All artifacts are pure markdown emitted under `.claude/skills/agentbloc/{references,examples}/`. Each task is a single-artifact atomic commit.

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md
@.planning/phases/13-multi-agent-runtime/13-CONTEXT.md
@.claude/skills/agentbloc/references/audit-logging.md
@.claude/skills/agentbloc/references/correlation-id.md
@.claude/skills/agentbloc/references/incident-response.md
@.claude/skills/agentbloc/references/agent-memory-schema.md
@.claude/skills/agentbloc/references/blast-radius.md
@.claude/skills/agentbloc/references/telegram-patterns.md
@.claude/skills/agentbloc/references/n8n-integration.md
@.claude/skills/agentbloc/references/runtime-coordination.md
@.claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md

<interfaces>
The 9 artifacts collectively encode 8 of the 16 Phase 14 reqs (Plan 14-02 covers MONITOR-03/04/06 via briefing-agent.md.tmpl; Plan 14-03 covers CTRL-04 + closes via SKILL.md surgical wiring + extends Phase 12/13 schemas).

Decision anchors (from 14-CONTEXT.md):
- D-87 = jsonl-log-schema.md (canonical 12-field schema + path convention)
- D-84 = autonomy-controller.md (two-layer enforcement)
- D-85 = approval-router.md (Telegram dispatch + slash command)
- D-86 = escalation-protocol.md (4-part template + persistent halt)
- D-88 = reporting-hierarchy.md (MONITOR-05 chain)
- D-89 = task-locking.md (flock + JSON lock file)
- D-90 = activity-feed.md (daily merge)
- D-91 = billing-rates.md (Sonnet/Opus/Haiku rates table)

Twin references (structural patterns to copy):
- jsonl-log-schema.md twin -> agent-memory-schema.md (schema spec format with field tables + JCS notes)
- autonomy-controller.md twin -> blast-radius.md (governance reference with policy tables)
- approval-router.md twin -> telegram-patterns.md (Telegram-mediated workflow with shell-script contract)
- escalation-protocol.md twin -> incident-response.md (crisis-handling reference with worked examples)
- reporting-hierarchy.md twin -> orchestration-patterns.md (concept reference with v2.0/v2.5 split)
- task-locking.md twin -> credentials.md (operational pattern with shell semantics)
- activity-feed.md twin -> audit-logging.md (log-handling reference with merge semantics)
- billing-rates.md twin -> mcp-ecosystem-registry.md (static-table reference)
- arco-rooms-monitor-fixtures.md twin -> arco-rooms-runtime-artifacts.md (multi-format structural fixture)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `.claude/skills/agentbloc/references/jsonl-log-schema.md` , canonical 12-field log schema (D-87) + path convention + JCS canonicalization + approvals/escalations sibling files</name>
  <files>.claude/skills/agentbloc/references/jsonl-log-schema.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-87 schema; line budget 180-220)
    - .claude/skills/agentbloc/references/agent-memory-schema.md (FULL file; PRIMARY structural twin)
    - .claude/skills/agentbloc/references/audit-logging.md (legacy v1.0 schema being superseded; reference for compatibility note)
    - .claude/skills/agentbloc/references/correlation-id.md (D-75 format for correlation_id field)
  </read_first>
  <action>
Write a NEW file at `.claude/skills/agentbloc/references/jsonl-log-schema.md` (180-220 lines, zero em-dashes). Sections in order:

1. **H1 + blockquote**: `# JSONL Log Schema (Phase 14)` + blockquote stating "Canonical 12-field schema for every deployed agent's structured log entries. Loaded by briefing-agent at wake; consumed by activity-feed merger; aligned with v1.0 audit-logging.md correlation-ID format per D-97."
2. **TOC** (10 sections)
3. **When This Applies**: Phase 14 emits this schema; deployed agents write conforming JSONL lines on every wake; briefing-agent + activity-feed-merge.sh consume; loaded UNCONDITIONALLY at Phase 5 entry per D-58.
4. **Schema (12 fields)**: full JSON example block + per-field table (field, type, required, description, ENUM values for action/result/priority).
5. **Path Convention**: `.claude/agents/logs/<YYYY-MM-DD>/<agent-id>.jsonl` per REQUIREMENTS.md MONITOR-02 literal; per-day per-agent rationale (concurrent-write safety via O_APPEND); UTC midnight day boundary; one-file-per-day-per-agent vs alternatives table.
6. **Append-Only Discipline**: writes use `>>` redirect; no edits; compaction is v2.5 scope.
7. **JCS Canonicalization (D-60)**: each line is RFC 8785 JCS-canonicalized so SHA256 fingerprints stable across runs (enables idempotent activity-feed re-merge).
8. **Sibling Files**: approvals.jsonl + escalations.jsonl at same path; same schema; agent_id = originating agent; action ENUM extension (approval_request, approval_response, escalation).
9. **Briefing Input Pattern**: briefing-agent globs `.claude/agents/logs/<TODAY>/*.jsonl`, sorts by timestamp ascending, summarizes per agent.
10. **Cross-References**: correlation-id.md, agent-memory-schema.md (last-run.json sibling), audit-logging.md (legacy compatibility note: v1.0 sess-NNN format mapping to D-75).
  </action>
  <acceptance_criteria>
    - File exists; line count 180..220; em-dash count = 0
    - H1 = `# JSONL Log Schema (Phase 14)` on first line
    - Sections present: When This Applies, Schema (12 fields), Path Convention, Append-Only Discipline, JCS Canonicalization, Sibling Files, Briefing Input Pattern, Cross-References
    - Schema example contains: schema_version, timestamp, correlation_id, agent_id, team, action, result, priority (8 required), tool, details, duration_ms, token_count, cost_usd, requires_human, locked_by (7 optional)
    - action ENUM lists: tool_call, decision, error, state_change, escalation, approval_request, approval_response
    - result ENUM lists: success, failure, skipped, blocked
    - priority ENUM lists: info, warn, critical
    - Path `.claude/agents/logs/<YYYY-MM-DD>/<agent-id>.jsonl` cited verbatim
    - JCS / RFC 8785 cited
  </acceptance_criteria>
  <done>jsonl-log-schema.md wired. MONITOR-01 (canonical schema) + MONITOR-02 (log path) closed at the reference layer. Briefing-agent (Plan 14-02) + activity-feed.md (Task 7 below) consume this schema.</done>
</task>

<task type="auto">
  <name>Task 2: Create `.claude/skills/agentbloc/references/autonomy-controller.md` , D-84 two-layer enforcement (SKILL.md prose + PreToolUse hook autonomy-gate.sh) + tool-classification table + per-autonomy matrix</name>
  <files>.claude/skills/agentbloc/references/autonomy-controller.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-84 enforcement contract; line budget 160-200)
    - .claude/skills/agentbloc/references/blast-radius.md (FULL file; PRIMARY structural twin; copy governance grammar)
    - .claude/skills/agentbloc/references/agent-profile-schema.md (autonomy ENUM full|semi|supervised)
    - .claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl (Phase 12 template baseline; for prose-layer cross-reference)
  </read_first>
  <action>
Write `.claude/skills/agentbloc/references/autonomy-controller.md` (160-200 lines, zero em-dashes). Sections:

1. **H1 + blockquote**: `# Autonomy Controller (Phase 14)` + blockquote "Two-layer enforcement of per-agent autonomy: SKILL.md prose informs reasoning; PreToolUse hook (autonomy-gate.sh) provides deterministic enforcement. Defense-in-depth pattern matching Phase 12 narrow-Bash + Phase 13 three-point kill-switch."
2. **TOC** (8 sections)
3. **When This Applies**: every deployed agent at Phase 5; loaded at Phase 5 entry; informs PreToolUse hook installation by deploy-engine; informs prose insertion in deployed-agent-skill templates.
4. **Two-Layer Architecture**: layer 1 = SKILL.md prose (Phase 12 templates already encode; Phase 14 surgically extends with approval-routing paragraph per D-94); layer 2 = `.claude/hooks/autonomy-gate.sh` PreToolUse hook (intercepts side-effect tools; reads registry.yaml for agent autonomy; sequences AFTER kill-switch-check.sh per alphabetical hook order).
5. **Tool Classification Table**: 3 categories (external side-effect / internal side-effect / read-only) with MCP tool name patterns + Bash patterns + examples.
6. **Per-Autonomy Behavior Matrix**: 3x3 table (full/semi/supervised x external/internal/read-only) showing proceed/approve/always-approve.
7. **$TOOL_REASONING Contract**: agent must export $TOOL_REASONING (1-2 sentence rationale) before invoking external-side-effect tool; hook reads env var; passes to approval-router.sh as part of context.
8. **autonomy-gate.sh Shell Contract**: input (TOOL_NAME + TOOL_ARGS + agent context); behavior tree (read autonomy -> dispatch); exit codes (0 proceed, 1 block); approval-router.sh delegation.
9. **Cross-References**: approval-router.md, blast-radius.md, deployed-agent-skill-schema.md (Phase 12 templates), incident-response.md (kill-switch sequencing).
  </action>
  <acceptance_criteria>
    - File exists; lines 160..200; em-dashes = 0
    - H1 + sections: Two-Layer Architecture, Tool Classification Table, Per-Autonomy Behavior Matrix, $TOOL_REASONING Contract, autonomy-gate.sh Shell Contract, Cross-References
    - References autonomy-gate.sh by name
    - Cites both layers (SKILL.md prose + PreToolUse hook)
    - Tool classification table has all 3 categories
    - Per-autonomy matrix shows full=proceed, semi=approval-on-external, supervised=always-approval
  </acceptance_criteria>
  <done>autonomy-controller.md wired. AUTON-01 closure at reference layer; PreToolUse hook contract documented for Plan 14-03 surgical insertion into deployed-agent-skill templates.</done>
</task>

<task type="auto">
  <name>Task 3: Create `.claude/skills/agentbloc/references/approval-router.md` , D-85 approval flow + Telegram dispatch + slash-command syntax + approvals.jsonl append discipline</name>
  <files>.claude/skills/agentbloc/references/approval-router.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-85 approval flow; line budget 150-190)
    - .claude/skills/agentbloc/references/telegram-patterns.md (FULL file; PRIMARY structural twin; copy thread-per-domain prose)
    - .claude/skills/agentbloc/references/correlation-id.md (correlation_id used as slash-command arg)
  </read_first>
  <action>
Write `.claude/skills/agentbloc/references/approval-router.md` (150-190 lines, zero em-dashes). Sections:

1. **H1 + blockquote**: `# Approval Router (Phase 14)` + blockquote "Routes side-effect tool approvals through a dedicated Telegram thread (CTRL-01). Single shell script `.agentbloc/runtime/approval-router.sh` emitted by runtime-engine + invoked by autonomy-gate.sh PreToolUse hook."
2. **TOC** (8 sections)
3. **When This Applies**: PreToolUse hook (autonomy-gate.sh) calls approval-router.sh on side-effect tools for `semi` + `supervised` agents.
4. **6-Step Flow**: (1) hook invocation, (2) registry read for thread+timeout, (3) Telegram POST to approvals thread, (4) long-poll for /approve|/deny reply, (5) append to approvals.jsonl, (6) exit 0/1.
5. **Telegram Message Format**: full literal example block of the structured approval request message with all 5 lines (action / reversibility / reasoning / slash-command syntax).
6. **Slash-Command Syntax**: `/approve <correlation_id> [optional reasoning]` and `/deny <correlation_id> [optional reasoning]`; correlation_id is disambiguator for concurrent pending approvals; reasoning captured in approvals.jsonl.
7. **Long-Poll Implementation**: Telegram getUpdates with offset to avoid duplicate consumption; default 600s timeout (configurable per agent); poll loop pseudocode.
8. **approvals.jsonl Append Discipline**: schema_version=1; per-day file at `.claude/agents/logs/<YYYY-MM-DD>/approvals.jsonl`; fields per AUTON-03 (proposal + decision + decider + outcome).
9. **Thread Separation per CTRL-01**: rationale; deploy-engine creates approvals thread + briefing thread + escalations thread separately at deploy time.
10. **approval-router.sh Shell Contract**: input args (agent-id, tool, args-summary, reversibility); environment ($TOOL_REASONING, $TELEGRAM_BOT_TOKEN); exit codes (0 approved, 1 denied/timeout); error handling.
11. **Cross-References**: autonomy-controller.md (caller), telegram-patterns.md (Telegram MCP usage), correlation-id.md (correlation_id format), jsonl-log-schema.md (approvals.jsonl schema).
  </action>
  <acceptance_criteria>
    - File exists; lines 150..190; em-dashes = 0
    - Sections: 6-Step Flow, Telegram Message Format, Slash-Command Syntax, Long-Poll Implementation, approvals.jsonl Append Discipline, Thread Separation per CTRL-01, approval-router.sh Shell Contract, Cross-References
    - `/approve <correlation_id>` and `/deny <correlation_id>` syntax cited verbatim
    - 600s default timeout cited
    - approvals.jsonl path `.claude/agents/logs/<YYYY-MM-DD>/approvals.jsonl` cited
    - Cross-references to autonomy-controller.md, telegram-patterns.md, jsonl-log-schema.md
  </acceptance_criteria>
  <done>approval-router.md wired. AUTON-02 + AUTON-03 + CTRL-01 closure at reference layer. Plan 14-03 wires approval-router.sh emission into runtime-engine.md.</done>
</task>

<task type="auto">
  <name>Task 4: Create `.claude/skills/agentbloc/references/escalation-protocol.md` , D-86 4-part template + persistent halt + /resume slash-command + 3 worked examples</name>
  <files>.claude/skills/agentbloc/references/escalation-protocol.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-86 escalation flow; line budget 140-180)
    - .claude/skills/agentbloc/references/incident-response.md (FULL file; PRIMARY structural twin; copy crisis-handling grammar; cite Runtime Kill-Switch Semantics for sibling pattern)
  </read_first>
  <action>
Write `.claude/skills/agentbloc/references/escalation-protocol.md` (140-180 lines, zero em-dashes). Sections:

1. **H1 + blockquote**: `# Escalation Protocol (Phase 14)` + blockquote "Routes agent failures + critical errors through a dedicated Telegram thread (`escalations`, distinct from approvals + briefing). Persistent-halt semantics until /resume reply. 4-part message template per AUTON-05 (what tried / why failed / options / recommended next action)."
2. **TOC** (7 sections)
3. **When This Applies**: any agent wake that hits an uncaught exception, critical-action `result: failure`, or an explicit `escalate(...)` call from agent prose.
4. **6-Step Flow**: (1) JSONL log entry priority=critical, (2) last-run.json status=error, (3) escalation-router.sh telegram-escalate invocation, (4) Telegram POST to escalations thread with 4-part template, (5) wake exits with wake_outcome=escalated, (6) subsequent wakes short-circuit until /resume.
5. **4-Part Template (verbatim)**: full literal example block with `🚨 ESCALATION` + agent + correlation + What I tried + Why it failed + Options (numbered) + Recommended + Reply slash-commands.
6. **Persistent-Halt Semantics**: status=error halts subsequent wakes; wake template section 1 (after kill-switch check) detects + short-circuits with wake_outcome=skipped-prior-error; rationale (avoid repeated cost on known-broken state).
7. **/resume + /halt Slash-Commands**: `/resume <correlation_id> [free-text instructions]` updates status=idle + appends instructions to memory.md Open Items (D-64 location); `/halt <correlation_id>` confirms persistent halt + escalates higher.
8. **3 Worked Examples**: (a) Gestor Documental + gmail rate-limit; (b) Gestor Cobros + plaid auth-revoked; (c) Recepcionista + BBVA 2FA-expired. Each example shows the literal Telegram message + the resume reply + the resulting state mutation.
9. **Cross-References**: incident-response.md Runtime Kill-Switch Semantics (sibling pattern), approval-router.md (shared Telegram-routing infrastructure), agent-memory-schema.md (last-run.json status field), jsonl-log-schema.md (escalations.jsonl schema).
  </action>
  <acceptance_criteria>
    - File exists; lines 140..180; em-dashes = 0
    - Sections: 6-Step Flow, 4-Part Template (verbatim), Persistent-Halt Semantics, /resume + /halt Slash-Commands, 3 Worked Examples (gmail rate-limit, plaid auth-revoked, BBVA 2FA-expired)
    - Template contains literal lines: `What I tried:`, `Why it failed:`, `Options:`, `Recommended:`
    - `/resume <correlation_id>` and `/halt <correlation_id>` syntax cited
    - status=error persistent-halt semantic documented
    - Cross-references to incident-response.md, approval-router.md, jsonl-log-schema.md
  </acceptance_criteria>
  <done>escalation-protocol.md wired. AUTON-04 + AUTON-05 closure at reference layer. Plan 14-03 surgically appends Escalation Protocol H2 section into incident-response.md citing this reference.</done>
</task>

<task type="auto">
  <name>Task 5: Create `.claude/skills/agentbloc/references/reporting-hierarchy.md` , MONITOR-05 hierarchy chain + v2.0 flat + v2.5 team-lead placeholder</name>
  <files>.claude/skills/agentbloc/references/reporting-hierarchy.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-88 briefing-agent; line budget 80-120)
    - .claude/skills/agentbloc/references/orchestration-patterns.md (FULL file; PRIMARY structural twin; copy concept-reference grammar)
  </read_first>
  <action>
Write `.claude/skills/agentbloc/references/reporting-hierarchy.md` (80-120 lines, zero em-dashes). Sections:

1. **H1 + blockquote**: `# Reporting Hierarchy (Phase 14)` + blockquote "MONITOR-05 chain: individual agents -> team leads -> briefing agent -> human. v2.0 ships flat (every agent writes own log; briefing reads all). Team-lead aggregation v2.5 scope."
2. **TOC** (5 sections)
3. **When This Applies**: every team deploys with a briefing agent; reporting flows through the chain at the briefing-agent's daily wake.
4. **The 4-Layer Chain**: diagram + per-layer description (individual agents = JSONL log writers; team leads = v2.5 mid-tier aggregators; briefing agent = consolidator + dispatcher; human = recipient).
5. **v2.0 Flat Hierarchy**: every agent writes to `.claude/agents/logs/<DATE>/<agent-id>.jsonl`; briefing agent globs all + summarizes; no mid-tier; rationale (simpler; matches small-team scale).
6. **v2.5 Team-Lead Placeholder**: prose for future extension (registry.yaml team_leads array; lead_for nested map; mid-tier aggregator script); v2.0 ships zero-changes-needed structure.
7. **Critical Escalations Bypass Briefing**: AUTON-04/D-86 escalations go directly to escalations Telegram thread; briefing summarizes them next morning but does not gate them; rationale (latency).
8. **Cross-References**: jsonl-log-schema.md (agent log path), escalation-protocol.md (bypass), briefing-agent.md.tmpl (Plan 14-02 template).
  </action>
  <acceptance_criteria>
    - File exists; lines 80..120; em-dashes = 0
    - Sections: The 4-Layer Chain, v2.0 Flat Hierarchy, v2.5 Team-Lead Placeholder, Critical Escalations Bypass Briefing, Cross-References
    - Chain documented as: individual agents -> team leads -> briefing -> human
    - v2.0 flat + v2.5 placeholder both cited
    - Cross-references to jsonl-log-schema.md, escalation-protocol.md
  </acceptance_criteria>
  <done>reporting-hierarchy.md wired. MONITOR-05 closure at reference layer.</done>
</task>

<task type="auto">
  <name>Task 6: Create `.claude/skills/agentbloc/references/task-locking.md` , D-89 file+flock locking + JSON lock-file schema + acquire/release semantics + resource-slug grammar</name>
  <files>.claude/skills/agentbloc/references/task-locking.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-89 locking contract; line budget 90-130)
    - .claude/skills/agentbloc/references/credentials.md (FULL file; PRIMARY structural twin; copy operational-pattern grammar)
  </read_first>
  <action>
Write `.claude/skills/agentbloc/references/task-locking.md` (90-130 lines, zero em-dashes). Sections:

1. **H1 + blockquote**: `# Task Locking (Phase 14)` + blockquote "CTRL-03 file+flock locking for shared resources (bank accounts, tenant records, calendar slots). POSIX flock(1) atomicity; per-host scope; multi-host = v2.5."
2. **TOC** (6 sections)
3. **When This Applies**: any agent working on a shared resource declared in registry.yaml monitor.locked_resources; check at section 4 (Input parse) of wake-job templates.
4. **Lock File Schema**: full JSON content `{agent_id, correlation_id, acquired_at, expires_at}` with field types + semantics; path `.agentbloc/locks/<resource-slug>.lock`.
5. **Acquire / Release Pseudocode**: pseudocode block for atomic acquire (flock -n + write content + handle expired existing), explicit release (write null + flock release), expiry-handling logic (current UTC > expires_at -> force release + re-acquire).
6. **Resource-Slug Grammar**: kebab-case domain-scoped; examples `bank-bbva-es76-1234`, `tenant-arco-rooms-apartment-3a`, `calendar-google-pablo-monday-9am`; derived from registry.yaml monitor.locked_resources + entity-id from working_state.
7. **deferred-locked Wake Outcome**: agent writes wake_outcome=deferred-locked + exits without further work; no escalation (locks are normal contention); JSONL log line includes locked_by field per MONITOR-01.
8. **Cross-References**: jsonl-log-schema.md (locked_by field), agent-memory-schema.md (working_state used to derive resource-slug), wake-job templates (acquisition timing).
  </action>
  <acceptance_criteria>
    - File exists; lines 90..130; em-dashes = 0
    - Sections: Lock File Schema, Acquire / Release Pseudocode, Resource-Slug Grammar, deferred-locked Wake Outcome
    - flock(1) cited
    - Path `.agentbloc/locks/<resource-slug>.lock` cited verbatim
    - Lock JSON has all 4 fields: agent_id, correlation_id, acquired_at, expires_at
    - Resource-slug examples (bank, tenant, calendar) present
    - deferred-locked wake outcome cited
  </acceptance_criteria>
  <done>task-locking.md wired. CTRL-03 closure at reference layer.</done>
</task>

<task type="auto">
  <name>Task 7: Create `.claude/skills/agentbloc/references/activity-feed.md` , D-90 daily-merge contract + activity-feed-merge.sh + idempotency</name>
  <files>.claude/skills/agentbloc/references/activity-feed.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-90 merge contract; line budget 60-100)
    - .claude/skills/agentbloc/references/audit-logging.md (FULL file; PRIMARY structural twin; copy log-handling grammar)
  </read_first>
  <action>
Write `.claude/skills/agentbloc/references/activity-feed.md` (60-100 lines, zero em-dashes). Sections:

1. **H1 + blockquote**: `# Activity Feed (Phase 14)` + blockquote "CTRL-05 daily-merged JSONL of all agents' activity. Generated by briefing-agent at briefing time. Operational debugging + future v2.5 dashboard ingestion."
2. **TOC** (5 sections)
3. **When This Applies**: briefing-agent wake.md section 5 (Execute) calls `activity-feed-merge.sh <YYYY-MM-DD>` to generate the day's feed.
4. **Path + Generation**: `.claude/agents/logs/<YYYY-MM-DD>/activity-feed.jsonl` (sibling to per-agent files); merger globs the day's per-agent JSONL + approvals.jsonl + escalations.jsonl + sorts ascending by timestamp.
5. **Byte-Identity Rule**: feed entries are byte-identical to source per-agent log entries; no enrichment, no transformation; merge is the only operation. RFC 8785 JCS canonicalization preserved.
6. **Idempotency**: safe to re-run `activity-feed-merge.sh <DATE>` for same date; output deterministic; merger overwrites prior feed with same content.
7. **activity-feed-merge.sh Shell Contract**: input arg = date (YYYY-MM-DD); behavior (glob + sort + write); exit codes (0 success, 1 read failure, 2 write failure).
8. **Cross-References**: jsonl-log-schema.md (per-agent file format), briefing-agent.md.tmpl (Plan 14-02 caller).
  </action>
  <acceptance_criteria>
    - File exists; lines 60..100; em-dashes = 0
    - Sections: Path + Generation, Byte-Identity Rule, Idempotency, activity-feed-merge.sh Shell Contract
    - Path `.claude/agents/logs/<YYYY-MM-DD>/activity-feed.jsonl` cited verbatim
    - activity-feed-merge.sh script name cited
    - Cross-references to jsonl-log-schema.md, briefing-agent.md.tmpl
  </acceptance_criteria>
  <done>activity-feed.md wired. CTRL-05 closure at reference layer.</done>
</task>

<task type="auto">
  <name>Task 8: Create `.claude/skills/agentbloc/references/billing-rates.md` , Sonnet/Opus/Haiku token rates table + claude-wrap.sh consumer contract</name>
  <files>.claude/skills/agentbloc/references/billing-rates.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (D-91 cost tracking; line budget 50-90)
    - .claude/skills/agentbloc/references/mcp-ecosystem-registry.md (FULL file; PRIMARY structural twin; copy static-table grammar)
  </read_first>
  <action>
Write `.claude/skills/agentbloc/references/billing-rates.md` (50-90 lines, zero em-dashes). Sections:

1. **H1 + blockquote**: `# Billing Rates (Phase 14)` + blockquote "Static rate table for Claude API tokens as of v2.0 ship date (April 2026). Consumed by `.agentbloc/runtime/claude-wrap.sh` to compute per-call cost_usd. Update when API pricing changes."
2. **TOC** (4 sections)
3. **When This Applies**: every deployed agent's cron entry runs through claude-wrap.sh wrapper that intercepts Claude API token usage from the response trailer + computes cost_usd via this table.
4. **Rates Table** (per million tokens, USD):

| Model | Input | Output | Cached Input |
|---|---|---|---|
| Opus 4.7 | $15.00 | $75.00 | $1.50 |
| Sonnet 4.6 | $3.00 | $15.00 | $0.30 |
| Haiku 4.5 | $0.80 | $4.00 | $0.08 |

(Use real published rates as of April 2026; cite "Verify against current API pricing page; update when rates change" footer.)

5. **claude-wrap.sh Consumer Contract**: input (model name + token usage from response); behavior (lookup rate -> compute cost_usd); output (annotated JSONL log line with cost_usd field).
6. **Subscription Mode Note**: when governance.yaml billing.mode=max_subscription, cost_usd is computed as-if API-key for budgeting parity; briefing annotates "(included in Claude Max subscription)".
7. **Cross-References**: jsonl-log-schema.md (cost_usd field), agent-memory-schema.md (last-run.json running totals per D-98).
  </action>
  <acceptance_criteria>
    - File exists; lines 50..90; em-dashes = 0
    - Rates table has all 3 models (Opus 4.7, Sonnet 4.6, Haiku 4.5) with input + output + cached input
    - claude-wrap.sh cited
    - Subscription mode note present
    - Cross-references to jsonl-log-schema.md, agent-memory-schema.md
  </acceptance_criteria>
  <done>billing-rates.md wired. CTRL-02 cost-rate source closed at reference layer; claude-wrap.sh emission deferred to Plan 14-03 runtime-engine.md surgical extension.</done>
</task>

<task type="auto">
  <name>Task 9: Create `.claude/skills/agentbloc/examples/arco-rooms-monitor-fixtures.md` , Phase 16 golden-file fixtures (5 JSONL lines + activity-feed + briefing message + escalation message + lock file + approval exchange)</name>
  <files>.claude/skills/agentbloc/examples/arco-rooms-monitor-fixtures.md</files>
  <read_first>
    - .planning/phases/14-autonomy-monitoring-control/14-CONTEXT.md (fixture scope)
    - .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md (FULL file; PRIMARY structural twin; copy multi-format fixture grammar)
    - .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md (for correlation-ID continuity with Phase 13 fixtures)
  </read_first>
  <action>
Write `.claude/skills/agentbloc/examples/arco-rooms-monitor-fixtures.md` (180-280 lines, zero em-dashes). Sections (each as code block with realistic Arco Rooms data, reusing correlation IDs from Phase 13 fixtures where possible):

1. **H1 + blockquote**: `# Arco Rooms Monitor Fixtures (Phase 14)` + blockquote "Golden-file fixtures for Phase 16 validation. Reuses correlation IDs from arco-rooms-correlation-flow.md Scenario A/B/C for continuity. All JSONL lines RFC 8785 JCS-canonicalized."
2. **TOC** (7 sections)
3. **Sample 1: Per-Agent JSONL Log Lines (5 entries)**: code block with 5 JSONL lines covering action types (tool_call success, decision, escalation, approval_request, approval_response). Use cron-20260501T080000Z-a3f21b correlation IDs for gestor-cobros + webhook-telegram-20260424T... for recepcionista.
4. **Sample 2: 1 Day's activity-feed.jsonl**: code block with 8-12 chronologically merged JSONL lines from 3 Arco Rooms agents on 2026-05-01.
5. **Sample 3: Briefing Telegram Message**: code block (multi-line text) showing daily briefing message format with the 3-emoji status badges (🟢 active · 🟡 idle · 🔴 error), per-agent breakdown, totals.
6. **Sample 4: Escalation Telegram Message**: code block showing 4-part escalation template for gestor-documental gmail rate-limit incident.
7. **Sample 5: Lock File**: code block JSON for `.agentbloc/locks/bank-bbva-es76-1234.lock` with realistic content acquired by gestor-cobros.
8. **Sample 6: Approval Thread Exchange**: code block showing the approval request message + the human reply `/approve cron-20260501T080000Z-a3f21b yes, this is the May invoice run` + the resulting approvals.jsonl entry.
9. **Cross-References**: jsonl-log-schema.md (line schema), correlation-id.md (ID format), arco-rooms-correlation-flow.md (parent fixtures).
  </action>
  <acceptance_criteria>
    - File exists; lines 180..280; em-dashes = 0
    - 6 sample sections present (5 JSONL lines + activity-feed + briefing + escalation + lock + approval exchange)
    - Reuses cron-20260501T080000Z-a3f21b correlation ID from Phase 13 fixtures
    - JSONL lines validate against jsonl-log-schema.md (12-field schema)
    - Briefing message contains 🟢 emoji
    - Escalation message contains "What I tried:" "Why it failed:" "Options:" "Recommended:"
    - Lock file JSON has 4 fields (agent_id, correlation_id, acquired_at, expires_at)
    - Approval exchange shows /approve slash-command
  </acceptance_criteria>
  <done>arco-rooms-monitor-fixtures.md wired. Phase 16 golden-file harness has reference data for Phase 14 monitor flow. Plan 14-01 complete; Plan 14-02 (briefing-agent template + monitor schema extensions) can begin.</done>
</task>

</tasks>
