---
phase: 10-integration-discovery-mcp-path
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - .claude/skills/agentbloc/references/phase-3-integration.md
  - .claude/skills/agentbloc/SKILL.md
autonomous: true
requirements:
  - INTEG-02
  - INTEG-05

must_haves:
  truths:
    - "references/phase-3-integration.md Phase 3 entry also-load section extends to include mcp-integration-protocol.md + mcp-ecosystem-registry.md + integration-manifest-schema.md (D-40 load-list extension matching Phase 9 Plan 09-03 pattern)"
    - "references/phase-3-integration.md Step 2 Multi-Method Search Protocol intro sentence reorders priorities per D-40: MCP-first (promoted from Priority 2 to Priority 1), Official API demoted from Priority 1 to Priority 2, other 4 priorities unchanged"
    - "references/phase-3-integration.md former Priority 2 MCP Server section is renamed Priority 1: MCP Server (Four-Step Search) with See-line delegating full detail to mcp-integration-protocol.md while preserving the v1.0 summary (package + stars + commit + publisher + tools)"
    - "references/phase-3-integration.md former Priority 1 Official API section is renamed Priority 2: Official API (fallback when no MCP exists or can be generated) with preserved v1.0 bullet content"
    - "references/phase-3-integration.md Priority 3 Playwright Browser Automation section gets a `[Phase 11 scope]` marker + forward See-line; v1.0 bullet summary preserved"
    - "SKILL.md State Transitions section has a new Phase 3 specific bullet naming the mcp_integrations_verified sub-gate as a transition requirement (D-41 Edit 1, mirroring the Phase 1 specific and Phase 2 specific bullets)"
    - "SKILL.md Phase 3 entry has (a) a new Summary Gate paragraph describing manifest emission at `.agentbloc/integrations/integration-manifest.yaml` with D-14 rendered-table review, and (b) its See-line load list extended with mcp-integration-protocol.md + mcp-ecosystem-registry.md + integration-manifest-schema.md (D-41 Edit 2)"
    - "SKILL.md Phase 4 entry has a new Precondition paragraph gating on `.agentbloc/integrations/integration-manifest.yaml` exists AND every entry has status: verified + healthcheck_at timestamp (D-41 Edit 3, mirroring the Phase 3 precondition inserted in Phase 9 Plan 09-03)"
    - "SKILL.md total line count stays at or under 195 (170 current + budget of +25 per D-41 = 195 max; Phase 8 + Phase 9 budget discipline held at +12 each, so +25 is loose headroom)"
    - "phase-3-integration.md total line count stays between 388 (current) and 430 (budget of +42 lines for the three D-40 surgical edits — reorder + delegation + marker)"
  artifacts:
    - path: ".claude/skills/agentbloc/references/phase-3-integration.md"
      provides: "Extended integration protocol — Phase 3 entry loads 3 new companion references; Priority 1 and Priority 2 swapped per D-40 MCP-first promotion; Priority 3 marked [Phase 11 scope] with forward-link. All v1.0 Steps 3-7 (evidence / trust / decision matrix / security / presentation) preserved verbatim."
      min_lines: 388
      contains: "Four-Step Search"
    - path: ".claude/skills/agentbloc/SKILL.md"
      provides: "Phase 3 entry loads the 3 new references unconditionally AND emits integration manifest at Summary gate; Phase 4 entry has integration-manifest.yaml precondition; State Transitions names mcp_integrations_verified sub-gate."
      contains: "mcp_integrations_verified"
  key_links:
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/mcp-integration-protocol.md"
      via: "unconditional load at Phase 3 entry alongside phase-3-integration.md"
      pattern: "mcp-integration-protocol\\.md"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/mcp-ecosystem-registry.md"
      via: "unconditional load at Phase 3 entry; Step 2 ecosystem lookup target"
      pattern: "mcp-ecosystem-registry\\.md"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/integration-manifest-schema.md"
      via: "unconditional load at Phase 3 entry; Validation Checklist is the gate target"
      pattern: "integration-manifest-schema\\.md"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".agentbloc/integrations/integration-manifest.yaml"
      via: "Phase 4 precondition verifies this file exists + every entry has status: verified"
      pattern: "integration-manifest\\.yaml"
    - from: ".claude/skills/agentbloc/references/phase-3-integration.md"
      to: ".claude/skills/agentbloc/references/mcp-integration-protocol.md"
      via: "Priority 1 MCP Server section delegates full 4-step detail to mcp-integration-protocol.md via See-line"
      pattern: "mcp-integration-protocol\\.md"
    - from: ".claude/skills/agentbloc/references/phase-3-integration.md"
      to: ".claude/skills/agentbloc/references/mcp-ecosystem-registry.md"
      via: "Priority 1 MCP Server section references the curated registry for Step 2 lookup"
      pattern: "mcp-ecosystem-registry\\.md"
---

<objective>
Wire the Plan 10-01 artifacts (mcp-integration-protocol.md + mcp-ecosystem-registry.md + integration-manifest-schema.md) AND the Plan 10-02 mcp-builder skill into the v1.0 Phase 3 integration protocol and SKILL.md gate ritual. Two surgical file edits: three-edit-pattern in phase-3-integration.md (load-list extension + priority reorder + delegation + Phase-11 marker) per D-40, and three-edit-pattern in SKILL.md (State Transitions Phase-3-specific bullet + Phase 3 entry extension + Phase 4 precondition) per D-41. Exactly mirrors Phase 9 Plan 09-03 one phase later.

Purpose: ROADMAP Phase 10 success criteria require that (a) MCP-first becomes the operational Priority 1 (not positioning-only), (b) the 4-step search is actually invoked from Phase 3, (c) integration manifest emission gates the Phase 4 precondition, and (d) verification failures surface with specific-gap naming per INTEG-05. This plan is the wiring that makes Plan 10-01's contracts and Plan 10-02's mcp-builder skill load-bearing. Also locks the `mcp_integrations_verified` gate vocabulary so Phase 4 cannot start without a verified manifest at `.agentbloc/integrations/integration-manifest.yaml`.

Output: Three surgical edits in `references/phase-3-integration.md` (Phase 3 entry load-list extension + Step 2 priority reorder with delegation See-line + Priority 3 Phase-11-scope marker) and three surgical edits in `SKILL.md` (State Transitions Phase-3-specific bullet + Phase 3 entry load list extension with Summary Gate paragraph + Phase 4 precondition paragraph). SKILL.md stays under 195 lines (170 current + budget of +25); `phase-3-integration.md` stays at 388-430 lines (388 current + budget of +42).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-integration-discovery-mcp-path/10-CONTEXT.md
@.planning/phases/10-integration-discovery-mcp-path/10-PATTERNS.md
@.planning/phases/10-integration-discovery-mcp-path/10-01-SUMMARY.md
@.planning/phases/10-integration-discovery-mcp-path/10-02-SUMMARY.md
@.planning/phases/09-designer-agent/09-03-PLAN.md
@.planning/phases/09-designer-agent/09-03-SUMMARY.md

@.claude/skills/agentbloc/SKILL.md
@.claude/skills/agentbloc/references/phase-3-integration.md
@.claude/skills/agentbloc/references/mcp-integration-protocol.md
@.claude/skills/agentbloc/references/mcp-ecosystem-registry.md
@.claude/skills/agentbloc/references/integration-manifest-schema.md
@.claude/skills/mcp-builder/SKILL.md

<interfaces>
<!-- D-40 surgical edit targets in phase-3-integration.md (current 388 lines). -->
<!-- Edit scope: Phase 3 entry also-load block (lines 26-28) + Step 2 intro sentence (line 66) + Priority 1/2/3 section headers (lines 68, 76, 85). -->
<!-- Preserve verbatim: Step 1 Service Inventory (lines 40-62), Step 3 Evidence Verification (lines 119+), Step 4 Trust Scoring (lines 155+), Steps 5-7, Integration Gate, Quick Reference. -->

<!-- D-41 surgical edit targets in SKILL.md (current 170 lines). -->
<!-- Edit 1: State Transitions section — insert new Phase 3 specific bullet after line 41 (current Phase 2 specific bullet). -->
<!-- Edit 2: Phase 3 entry (lines 112-119) — insert Summary Gate paragraph + extend See-line load list. -->
<!-- Edit 3: Phase 4 entry (lines 121-126) — insert Precondition paragraph after the section intro sentence. -->

<!-- Exact Phase 9 D-29 precedent (git commit 783b538) for SKILL.md three-edit pattern: -->
<!-- Edit 1: inserted "Phase 2 specific:" bullet after "Phase 1 specific:" bullet in State Transitions -->
<!-- Edit 2: inserted Summary Gate paragraph + 2 new See-lines in Phase 2 entry -->
<!-- Edit 3: inserted Precondition paragraph at top of Phase 3 entry body -->
<!-- Phase 10 D-41 mirrors this exactly, one phase forward: -->
<!-- Edit 1: insert "Phase 3 specific:" bullet after "Phase 2 specific:" bullet -->
<!-- Edit 2: insert Summary Gate paragraph + 3 new See-lines in Phase 3 entry -->
<!-- Edit 3: insert Precondition paragraph at top of Phase 4 entry body -->

<!-- Exact phase-3-integration.md target lines: -->

Current lines 26-28 (Phase 3 entry also-load block):
```
At Phase 3 entry, also load:
- [references/credentials.md](credentials.md) for the credential decision tree (used in Step 6)
- [references/prompt-injection.md](prompt-injection.md) for the defense layer pipeline (used in Step 6)
```
After Plan 10-03 Task 1 edit:
```
At Phase 3 entry, also load:
- [references/credentials.md](credentials.md) for the credential decision tree (used in Step 6)
- [references/prompt-injection.md](prompt-injection.md) for the defense layer pipeline (used in Step 6)
- [references/mcp-integration-protocol.md](mcp-integration-protocol.md) for the 4-step MCP search flow (used in Step 2 Priority 1)
- [references/mcp-ecosystem-registry.md](mcp-ecosystem-registry.md) for the curated MCP registry (used in Step 2 Priority 1 Step 2)
- [references/integration-manifest-schema.md](integration-manifest-schema.md) for the integration manifest output contract (used at Integration Gate)
```

Current line 66 (Step 2 intro sentence):
```
For each service in the inventory, search integration methods in this strict priority order. This follows decision D-01: official API (best) > MCP server (native) > Playwright browser automation > email scraping > webhook interception > manual notification (last resort).
```
After Plan 10-03 Task 1 edit:
```
For each service in the inventory, search integration methods in this strict priority order. This follows v2.0 decision D-40 (MCP-first positioning from PROJECT.md): MCP server (four-step search) > official API (fallback when no MCP) > Playwright browser automation [Phase 11 scope] > email scraping > webhook interception > manual notification (last resort). See [mcp-integration-protocol.md](mcp-integration-protocol.md) for the full MCP search flow.
```

Current line 68-75 (Priority 1: Official API):
```
### Priority 1: Official API

WebSearch for `{service_name} API documentation`. If found, record:
- API endpoint base URL
- Authentication method (OAuth, API key, basic auth)
- Rate limits and quotas
- SDK availability (npm, Python, etc.)
```
After Plan 10-03 Task 1 edit (demoted to Priority 2; content preserved):
```
### Priority 2: Official API

Fallback when no MCP exists or can be generated. WebSearch for `{service_name} API documentation`. If found, record:
- API endpoint base URL
- Authentication method (OAuth, API key, basic auth)
- Rate limits and quotas
- SDK availability (npm, Python, etc.)
```

Current line 76-84 (Priority 2: MCP Server):
```
### Priority 2: MCP Server

Search for existing MCP servers. Use PulseMCP (`list_servers` tool if available) or WebSearch for `{service_name} MCP server site:pulsemcp.com OR site:github.com`. If found, record:
- Package name (npm or GitHub)
- GitHub stars count
- Last commit date
- Publisher (individual or organization)
- Available tools/capabilities
```
After Plan 10-03 Task 1 edit (promoted to Priority 1; delegates detail):
```
### Priority 1: MCP Server (Four-Step Search)

See [mcp-integration-protocol.md](mcp-integration-protocol.md) for the canonical 4-step flow: existing `.mcp.json` -> ecosystem registry lookup (via [mcp-ecosystem-registry.md](mcp-ecosystem-registry.md)) -> wrapper generation via `.claude/skills/mcp-builder/SKILL.md` -> browser-fallback (Phase 11 scope).

**Summary for quick reference** (full detail in the protocol file):
- Package name (npm or GitHub) from mcp-ecosystem-registry.md or `.mcp/generated/<tool-id>/`
- GitHub stars count (trust_tier criterion per v1.0 INTG-04)
- Last commit date (trust_tier criterion)
- Publisher (individual or organization)
- Available tools/capabilities (populated by D-34 `tools/list` probe during Verification Loop)

After the 4-step search resolves, the D-34 three-check Verification Loop (Ping / Scope match / Shape probe) runs. Any FAIL triggers the Halt-and-Name Protocol per D-35 — no silent degradation. Output lands in `.agentbloc/integrations/integration-manifest.yaml` per [integration-manifest-schema.md](integration-manifest-schema.md).
```

Current line 85-90 (Priority 3: Playwright Browser Automation):
```
### Priority 3: Playwright Browser Automation

If no API or MCP server exists, WebSearch for `{service_name} login portal` or `{service_name} web dashboard`. Note:
- Whether the portal uses standard web forms (automatable)
- Whether 2FA/CAPTCHA is required (complicates automation)
- Playwright MCP (microsoft/playwright-mcp) is the automation tool -- HIGH trust, Microsoft-maintained
```
After Plan 10-03 Task 1 edit (Phase 11 scope marker; v1.0 summary preserved):
```
### Priority 3: Playwright Browser Automation [Phase 11 scope]

See forthcoming [references/browser-fallback.md](browser-fallback.md) (Phase 11 BROWSER-01..12) for the full Patchright + HAR capture + injection detector + PII redaction protocol. Phase 10 stubs this priority; Phase 11 wires it in.

**Summary (v1.0, preserved):**
- If no API or MCP server exists, WebSearch for `{service_name} login portal` or `{service_name} web dashboard`
- Whether the portal uses standard web forms (automatable)
- Whether 2FA/CAPTCHA is required (complicates automation)
- Playwright MCP (microsoft/playwright-mcp) is the automation tool -- HIGH trust, Microsoft-maintained
```

<!-- Exact SKILL.md target lines (D-41 three edits): -->

Current line 41 (State Transitions, existing Phase 2 specific bullet):
```
- Phase 2 specific: Gate transition to `approved` requires BOTH user confirmation of the rendered team table and per-agent cards AND the `agent_profiles_validated` sub-gate (all REQUIRED checks from [references/agent-profile-schema.md](references/agent-profile-schema.md) Validation Checklist have passed and the file at `.agentbloc/team/agent-profiles.yaml` has been written by the Designer subagent).
```
After Plan 10-03 Task 2 Edit 1 (insert Phase 3 specific bullet after line 41):
```
- Phase 3 specific: Gate transition to `approved` requires BOTH user confirmation of the rendered integrations table AND the `mcp_integrations_verified` sub-gate (all REQUIRED checks from [references/integration-manifest-schema.md](references/integration-manifest-schema.md) Validation Checklist have passed, every tool entry has `status: verified` with a `healthcheck_at` timestamp, and the file at `.agentbloc/integrations/integration-manifest.yaml` has been written).
```

Current lines 112-119 (Phase 3 entry):
```
### Phase 3: Deep Integration Analysis

For each agent action, find the BEST integration method. Research APIs, MCP servers, npm packages, Playwright paths, email scraping, webhooks. Present options with pros/cons/setup for every service.

**Precondition:** Verify `.agentbloc/team/agent-profiles.yaml` exists and validates against the Validation Checklist in [references/agent-profile-schema.md](references/agent-profile-schema.md). If the file is missing or fails any REQUIRED check, return the state bar to Phase 2 with gate `pending` and re-run the Summary gate before attempting Phase 3 again.

You MUST read the complete integration analysis protocol before starting this phase:
See [references/phase-3-integration.md](references/phase-3-integration.md)
```
After Plan 10-03 Task 2 Edit 2 (insert Summary Gate paragraph + extend See-line list):
```
### Phase 3: Deep Integration Analysis

For each agent action, find the BEST integration method. Research APIs, MCP servers, npm packages, Playwright paths, email scraping, webhooks. Present options with pros/cons/setup for every service.

**Precondition:** Verify `.agentbloc/team/agent-profiles.yaml` exists and validates against the Validation Checklist in [references/agent-profile-schema.md](references/agent-profile-schema.md). If the file is missing or fails any REQUIRED check, return the state bar to Phase 2 with gate `pending` and re-run the Summary gate before attempting Phase 3 again.

**Summary Gate:** After walking the 4-step MCP search + three-check Verification Loop, write `.agentbloc/integrations/integration-manifest.yaml` silently. The rendered integrations table + per-tool evidence rows are what the user reviews and confirms (D-14 mirror). See [references/mcp-integration-protocol.md](references/mcp-integration-protocol.md) Verification Loop for the D-34 three-check protocol and Halt-and-Name Protocol for D-35 failure handling.

You MUST read the complete integration analysis protocol AND the MCP integration protocol AND the ecosystem registry AND the integration manifest schema before starting this phase:
See [references/phase-3-integration.md](references/phase-3-integration.md)
See [references/mcp-integration-protocol.md](references/mcp-integration-protocol.md)
See [references/mcp-ecosystem-registry.md](references/mcp-ecosystem-registry.md)
See [references/integration-manifest-schema.md](references/integration-manifest-schema.md)
```

Current lines 121-126 (Phase 4 entry):
```
### Phase 4: Step-by-Step Confirmation + Dry Run

Get explicit approval for every single step each agent will perform. Then execute a mandatory dry run: agents run against real data with all side-effect tools stubbed. Validate outputs before going live.

You MUST read the complete confirmation and dry run protocol before starting this phase:
See [references/phase-4-confirmation.md](references/phase-4-confirmation.md)
```
After Plan 10-03 Task 2 Edit 3 (insert Precondition paragraph):
```
### Phase 4: Step-by-Step Confirmation + Dry Run

Get explicit approval for every single step each agent will perform. Then execute a mandatory dry run: agents run against real data with all side-effect tools stubbed. Validate outputs before going live.

**Precondition:** Verify `.agentbloc/integrations/integration-manifest.yaml` exists AND every tool entry has `status: verified` with a `healthcheck_at` timestamp (per [references/integration-manifest-schema.md](references/integration-manifest-schema.md) Validation Checklist). If the file is missing, any entry is `status: failed`, or any REQUIRED check fails, return the state bar to Phase 3 with gate `pending` and re-run the Summary gate before attempting Phase 4 again.

You MUST read the complete confirmation and dry run protocol before starting this phase:
See [references/phase-4-confirmation.md](references/phase-4-confirmation.md)
```

Line count delta: +3 lines (State Transitions Edit 1) + 5 lines (Phase 3 Edit 2: Summary Gate para + 3 See-lines + connector prose rewrite) + 3 lines (Phase 4 Edit 3) = +11 lines. SKILL.md 170 + 11 = 181 lines, well under 195 budget.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase-3-integration.md surgical edit | 388-line file with v1.0 content being preserved; edits must not delete any Steps 3-7 prose or change decision matrix shape |
| SKILL.md surgical edit | 170-line file under 250-line v1.0 cap; edits must not exceed budget or mutate existing Phase 1/Phase 2 wiring |
| Forward See-line to browser-fallback.md | Link is broken in Phase 10; Phase 11 creates the target file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | T (tampering) | Surgical edit deletes v1.0 content accidentally | mitigate | Every edit uses the Edit tool's `old_string` / `new_string` pattern to replace EXACT text; Tasks 1 and 2 each include acceptance criteria asserting preserved content (Step 3 Evidence Verification, Step 4 Trust Scoring, Integration Gate, Quick Reference all still present after edit); line-count budget caps per D-41 catch over-insertions |
| T-10-11 | D (denial of service) | SKILL.md exceeds 250-line v1.0 cap due to cumulative Phase 10 edits | mitigate | D-41 budget: 170 + 11 target = 181 lines; acceptance criterion caps at 195 (safety margin); line-count verification is a grep-level check |
| T-10-12 | S (spoofing) | Forward See-line to browser-fallback.md becomes a missing-file error at runtime | accept | Phase 11 scope is explicit in the Priority 3 section; broken link is acceptable and documented as "forthcoming" in the See-line text; Phase 11 will create the file and silently resolve the link |
| T-10-13 | E (elevation of privilege) | Phase 4 precondition allows transition on a partially-verified manifest | mitigate | Precondition grep: "every tool entry has `status: verified`" + "healthcheck_at timestamp"; integration-manifest-schema.md Validation Checklist Checks 4-6 are the gate |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Surgical edits to references/phase-3-integration.md — Phase 3 entry load-list extension + Step 2 priority reorder + delegation See-line + Priority 3 Phase-11-scope marker</name>
  <files>.claude/skills/agentbloc/references/phase-3-integration.md</files>
  <read_first>
    - .planning/phases/10-integration-discovery-mcp-path/10-PATTERNS.md (lines 513-555 — the "phase-3-integration.md (MODIFY, surgical edit)" section with Patterns A (load-list extension), B (priority-ladder reorder), C (delegation-stub), D (Phase-11 stub marker), E (preservation rule))
    - .planning/phases/10-integration-discovery-mcp-path/10-CONTEXT.md (D-40 three-edit specification; preservation rule: Steps 3-7 stay verbatim)
    - .claude/skills/agentbloc/references/phase-3-integration.md (the FULL 388-line file — read it in its entirety before editing so you know which text to leave verbatim)
    - .claude/skills/agentbloc/references/mcp-integration-protocol.md (the target file of the delegation See-line from Plan 10-01)
    - .claude/skills/agentbloc/references/mcp-ecosystem-registry.md (the target file of the Step 2 Priority 1 Step-2 lookup from Plan 10-01)
    - .claude/skills/agentbloc/references/integration-manifest-schema.md (the target file of the Integration Gate load-list from Plan 10-01)
    - .planning/phases/09-designer-agent/09-03-PLAN.md (the exact Phase 9 precedent — this plan mirrors the "surgical edit to phase-2-design.md" pattern one phase forward)
    - .planning/REQUIREMENTS.md (INTEG-02 — ecosystem install path reinforced by Step 2 Priority 1 delegation; INTEG-05 — halt-and-name wiring surfaces in Priority 1 summary)
  </read_first>
  <action>
Apply EXACTLY three surgical edits to `.claude/skills/agentbloc/references/phase-3-integration.md` using the Edit tool. Read the full current file first (388 lines) to confirm you have correct `old_string` context for each edit.

**Edit 1 — Phase 3 entry also-load block extension (targets current lines 26-28).**

Use the Edit tool with this `old_string`:

```
At Phase 3 entry, also load:
- [references/credentials.md](credentials.md) for the credential decision tree (used in Step 6)
- [references/prompt-injection.md](prompt-injection.md) for the defense layer pipeline (used in Step 6)
```

And this `new_string`:

```
At Phase 3 entry, also load:
- [references/credentials.md](credentials.md) for the credential decision tree (used in Step 6)
- [references/prompt-injection.md](prompt-injection.md) for the defense layer pipeline (used in Step 6)
- [references/mcp-integration-protocol.md](mcp-integration-protocol.md) for the 4-step MCP search flow (used in Step 2 Priority 1)
- [references/mcp-ecosystem-registry.md](mcp-ecosystem-registry.md) for the curated MCP registry (used in Step 2 Priority 1 Step 2)
- [references/integration-manifest-schema.md](integration-manifest-schema.md) for the integration manifest output contract (used at Integration Gate)
```

This adds 3 new bullets to the companion-load list, preserves the 2 existing bullets verbatim, and adds zero content above or below.

**Edit 2 — Step 2 intro sentence priority reorder (targets current line 66) AND Priority 1/Priority 2 section swap (targets current lines 68-84).**

This is a single combined edit because the three sub-changes share contiguous context. Use the Edit tool with this `old_string`:

```
For each service in the inventory, search integration methods in this strict priority order. This follows decision D-01: official API (best) > MCP server (native) > Playwright browser automation > email scraping > webhook interception > manual notification (last resort).

### Priority 1: Official API

WebSearch for `{service_name} API documentation`. If found, record:
- API endpoint base URL
- Authentication method (OAuth, API key, basic auth)
- Rate limits and quotas
- SDK availability (npm, Python, etc.)

### Priority 2: MCP Server

Search for existing MCP servers. Use PulseMCP (`list_servers` tool if available) or WebSearch for `{service_name} MCP server site:pulsemcp.com OR site:github.com`. If found, record:
- Package name (npm or GitHub)
- GitHub stars count
- Last commit date
- Publisher (individual or organization)
- Available tools/capabilities
```

And this `new_string`:

```
For each service in the inventory, search integration methods in this strict priority order. This follows v2.0 decision D-40 (MCP-first positioning from PROJECT.md): MCP server (four-step search) > official API (fallback when no MCP) > Playwright browser automation [Phase 11 scope] > email scraping > webhook interception > manual notification (last resort). See [mcp-integration-protocol.md](mcp-integration-protocol.md) for the full MCP search flow.

### Priority 1: MCP Server (Four-Step Search)

See [mcp-integration-protocol.md](mcp-integration-protocol.md) for the canonical 4-step flow: existing `.mcp.json` -> ecosystem registry lookup (via [mcp-ecosystem-registry.md](mcp-ecosystem-registry.md)) -> wrapper generation via `.claude/skills/mcp-builder/SKILL.md` -> browser-fallback (Phase 11 scope).

**Summary for quick reference** (full detail in the protocol file):
- Package name (npm or GitHub) from mcp-ecosystem-registry.md or `.mcp/generated/<tool-id>/`
- GitHub stars count (trust_tier criterion per v1.0 INTG-04)
- Last commit date (trust_tier criterion)
- Publisher (individual or organization)
- Available tools/capabilities (populated by D-34 `tools/list` probe during Verification Loop)

After the 4-step search resolves, the D-34 three-check Verification Loop (Ping / Scope match / Shape probe) runs. Any FAIL triggers the Halt-and-Name Protocol per D-35 - no silent degradation. Output lands in `.agentbloc/integrations/integration-manifest.yaml` per [integration-manifest-schema.md](integration-manifest-schema.md).

### Priority 2: Official API

Fallback when no MCP exists or can be generated. WebSearch for `{service_name} API documentation`. If found, record:
- API endpoint base URL
- Authentication method (OAuth, API key, basic auth)
- Rate limits and quotas
- SDK availability (npm, Python, etc.)
```

This edit (a) reorders the intro sentence's priority list, (b) replaces the old Priority 1 Official API section with the new Priority 1 MCP Server (Four-Step Search) delegation section, (c) replaces the old Priority 2 MCP Server section with the new Priority 2 Official API section (with a "Fallback when no MCP" lead sentence), preserving the 4 bullet fields verbatim. The v1.0 MCP fields (package / stars / last_commit / publisher / tools) are preserved in the new Priority 1 summary block.

**Edit 3 — Priority 3 Playwright Browser Automation [Phase 11 scope] marker (targets current lines 85-90).**

Use the Edit tool with this `old_string`:

```
### Priority 3: Playwright Browser Automation

If no API or MCP server exists, WebSearch for `{service_name} login portal` or `{service_name} web dashboard`. Note:
- Whether the portal uses standard web forms (automatable)
- Whether 2FA/CAPTCHA is required (complicates automation)
- Playwright MCP (microsoft/playwright-mcp) is the automation tool -- HIGH trust, Microsoft-maintained
```

And this `new_string`:

```
### Priority 3: Playwright Browser Automation [Phase 11 scope]

See forthcoming [references/browser-fallback.md](browser-fallback.md) (Phase 11 BROWSER-01..12) for the full Patchright + HAR capture + injection detector + PII redaction protocol. Phase 10 stubs this priority; Phase 11 wires it in.

**Summary (v1.0, preserved):**
- If no API or MCP server exists, WebSearch for `{service_name} login portal` or `{service_name} web dashboard`
- Whether the portal uses standard web forms (automatable)
- Whether 2FA/CAPTCHA is required (complicates automation)
- Playwright MCP (microsoft/playwright-mcp) is the automation tool -- HIGH trust, Microsoft-maintained
```

This edit (a) adds the `[Phase 11 scope]` marker to the section heading, (b) inserts a forward See-line with the "forthcoming" language (the broken link is intentional per D-40 — Phase 11 will create `browser-fallback.md`), (c) preserves all 4 v1.0 bullet points verbatim under a "Summary (v1.0, preserved)" heading.

**Preservation requirements (critical):**
- Do NOT edit anything before line 26 or anything after line 90 of the current file. Priorities 4-6, Steps 3-7, Integration Gate, Quick Reference, and every word of the v1.0 content in those regions stays EXACTLY as written.
- Verify after editing that Steps 3, 4, 5, 6, 7 headers still exist (`grep -q "^## Step 3: Evidence Verification$"` etc.).
- Verify Quick Reference section still exists at the bottom of the file.
- Verify file is between 388 and 430 lines (current 388 + budget of ~+42 lines for the three edits; the net delta is roughly +3 (Edit 1 load-list) + +12 (Edit 2 Priority 1 expansion) + +3 (Edit 3 marker) = +18 lines, so final should be ~406).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "mcp-integration-protocol\.md" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "mcp-ecosystem-registry\.md" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "integration-manifest-schema\.md" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "### Priority 1: MCP Server (Four-Step Search)" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "### Priority 2: Official API" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "### Priority 3: Playwright Browser Automation \[Phase 11 scope\]" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "v2.0 decision D-40" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "^## Step 3: Evidence Verification" .claude/skills/agentbloc/references/phase-3-integration.md &amp;&amp; grep -q "^## Step 4: Trust Scoring" .claude/skills/agentbloc/references/phase-3-integration.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/references/phase-3-integration.md` exits 0
    - 3 new references cited in Phase 3 entry load-list:
      - `grep -q "mcp-integration-protocol\.md" .claude/skills/agentbloc/references/phase-3-integration.md` matches
      - `grep -q "mcp-ecosystem-registry\.md" .claude/skills/agentbloc/references/phase-3-integration.md` matches
      - `grep -q "integration-manifest-schema\.md" .claude/skills/agentbloc/references/phase-3-integration.md` matches
    - Priority reordering:
      - `grep -q "^### Priority 1: MCP Server (Four-Step Search)$" .claude/skills/agentbloc/references/phase-3-integration.md` matches (new Priority 1)
      - `grep -q "^### Priority 2: Official API$" .claude/skills/agentbloc/references/phase-3-integration.md` matches (new Priority 2)
      - `grep -q "^### Priority 3: Playwright Browser Automation \[Phase 11 scope\]$" .claude/skills/agentbloc/references/phase-3-integration.md` matches (Phase 11 marker)
    - OLD Priority-1-Official-API header is GONE: `grep -q "^### Priority 1: Official API$" .claude/skills/agentbloc/references/phase-3-integration.md` returns exit 1 (no match)
    - OLD Priority-2-MCP-Server header is GONE: `grep -q "^### Priority 2: MCP Server$" .claude/skills/agentbloc/references/phase-3-integration.md` returns exit 1 (no match)
    - OLD Priority-3-Playwright-without-marker header is GONE: `grep -q "^### Priority 3: Playwright Browser Automation$" .claude/skills/agentbloc/references/phase-3-integration.md` returns exit 1 (no match)
    - Step 2 intro sentence names D-40: `grep -q "v2.0 decision D-40" .claude/skills/agentbloc/references/phase-3-integration.md`
    - Step 2 intro still names the 6 methods: `grep -q "MCP server.*official API.*Playwright.*email scraping.*webhook interception.*manual notification" .claude/skills/agentbloc/references/phase-3-integration.md` (regex spans the updated sentence)
    - Priority 1 body delegates to mcp-integration-protocol.md: `grep -A5 "Priority 1: MCP Server" .claude/skills/agentbloc/references/phase-3-integration.md | grep -q "mcp-integration-protocol\.md"`
    - Priority 1 body preserves v1.0 fields: `grep -B0 -A20 "Priority 1: MCP Server" .claude/skills/agentbloc/references/phase-3-integration.md | grep -q "Package name"` AND `...grep -q "GitHub stars"` AND `...grep -q "Last commit"` AND `...grep -q "Publisher"`
    - Priority 1 body names D-34 verification + D-35 halt-and-name: `grep -q "D-34" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "D-35\|Halt-and-Name\|halt-and-name" .claude/skills/agentbloc/references/phase-3-integration.md`
    - Priority 1 body names mcp-builder path: `grep -q "mcp-builder" .claude/skills/agentbloc/references/phase-3-integration.md`
    - Priority 3 forward See-line exists: `grep -q "browser-fallback\.md" .claude/skills/agentbloc/references/phase-3-integration.md`
    - Priority 3 preserves v1.0 summary: `grep -A6 "Priority 3: Playwright" .claude/skills/agentbloc/references/phase-3-integration.md | grep -q "login portal"` AND `...grep -q "2FA"` AND `...grep -q "microsoft/playwright-mcp"`
    - Preservation of Steps 3-7 (verbatim v1.0 content): `grep -q "^## Step 3: Evidence Verification$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^## Step 4: Trust Scoring$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^## Step 5: Decision Matrix Construction$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^## Step 6: Security Cross-Reference$" .claude/skills/agentbloc/references/phase-3-integration.md` AND `grep -q "^## Step 7: Integration Presentation and Approval$" .claude/skills/agentbloc/references/phase-3-integration.md`
    - Preservation of Integration Gate: `grep -q "^## Integration Gate$" .claude/skills/agentbloc/references/phase-3-integration.md`
    - Preservation of Quick Reference closer: `grep -q "^## Quick Reference$" .claude/skills/agentbloc/references/phase-3-integration.md`
    - Line count within budget: `wc -l .claude/skills/agentbloc/references/phase-3-integration.md` returns a number between 388 and 430 inclusive
    - No em-dashes added: `grep -c "—" .claude/skills/agentbloc/references/phase-3-integration.md` returns 0 (the v1.0 file had 0; new content must keep 0)
    - Regression guard (plan-eng-review iron rule T-1): after the priority-ladder reorder, run `grep -rE "Priority 1:\s*Official API|Priority 2:\s*MCP Server" tests/ 2>/dev/null` — if any match surfaces, the executor MUST flag it in the SUMMARY as a regression risk before proceeding (any hardcoded v1.0 priority string in TAP scenarios would now be out of date). If zero matches, record "regression-guard: clean" in the SUMMARY. Non-blocking: the project's tests/scenarios/*.jsonl are conversational transcripts and do not currently hardcode priority labels, so clean result is expected; the guard is insurance against future hardcoding.
  </acceptance_criteria>
  <done>
phase-3-integration.md received three surgical edits that mirror the Phase 9 09-03 pattern one phase forward: (1) Phase 3 entry also-load list extended with mcp-integration-protocol.md + mcp-ecosystem-registry.md + integration-manifest-schema.md bullets; (2) Step 2 Multi-Method Search Protocol intro sentence reordered per D-40 MCP-first (MCP server now Priority 1 in the narrative) with a See-line to mcp-integration-protocol.md, plus the section headers for Priority 1 and Priority 2 swapped — new Priority 1 MCP Server (Four-Step Search) delegates full detail to the protocol file while preserving the v1.0 field summary (package / stars / commit / publisher / tools) and naming D-34 + D-35 + mcp-builder path; new Priority 2 Official API preserves the 4 v1.0 bullets verbatim with a "Fallback when no MCP" lead sentence; (3) Priority 3 Playwright Browser Automation got a [Phase 11 scope] marker + forward See-line to browser-fallback.md with the v1.0 4-bullet summary preserved verbatim under "Summary (v1.0, preserved)". Steps 3 through 7, Integration Gate, and Quick Reference sections are untouched verbatim. File grew from 388 to ~406 lines (within 388-430 budget). Zero new em-dashes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Surgical edits to SKILL.md — State Transitions Phase-3-specific bullet + Phase 3 entry load-list extension with Summary Gate + Phase 4 entry Precondition</name>
  <files>.claude/skills/agentbloc/SKILL.md</files>
  <read_first>
    - .planning/phases/10-integration-discovery-mcp-path/10-PATTERNS.md (lines 557-610 — the "SKILL.md (MODIFY, surgical edit)" section with State Transitions bullet pattern + Phase entry extension pattern + Phase 4 precondition pattern; all three mirror git commit 783b538 from Phase 9 D-29)
    - .planning/phases/10-integration-discovery-mcp-path/10-CONTEXT.md (D-41 three-edit specification; budget discipline: SKILL.md stays under 250 lines)
    - .claude/skills/agentbloc/SKILL.md (the FULL 170-line current file — read it all before editing)
    - .claude/skills/agentbloc/references/integration-manifest-schema.md (the target of the new State Transitions bullet See-line and Phase 4 precondition cross-link — read lines 1-20 to confirm cross-link form and file path)
    - .claude/skills/agentbloc/references/mcp-integration-protocol.md (the target of the new Phase 3 Summary Gate paragraph See-line to Verification Loop)
    - .claude/skills/agentbloc/references/mcp-ecosystem-registry.md (the target of the new Phase 3 See-line)
    - .planning/phases/09-designer-agent/09-03-PLAN.md (the exact Phase 9 D-29 precedent — this plan mirrors the three SKILL.md edits one phase forward)
  </read_first>
  <action>
Apply EXACTLY three surgical edits to `.claude/skills/agentbloc/SKILL.md` using the Edit tool. Read the full current file first (170 lines) to confirm correct `old_string` context for each edit.

**Edit 1 — State Transitions: insert new Phase 3 specific bullet after the existing Phase 2 specific bullet.**

Use the Edit tool with this `old_string` (this is the current line 41):

```
- Phase 2 specific: Gate transition to `approved` requires BOTH user confirmation of the rendered team table and per-agent cards AND the `agent_profiles_validated` sub-gate (all REQUIRED checks from [references/agent-profile-schema.md](references/agent-profile-schema.md) Validation Checklist have passed and the file at `.agentbloc/team/agent-profiles.yaml` has been written by the Designer subagent).
```

And this `new_string` (appends a Phase 3 specific bullet on a new line):

```
- Phase 2 specific: Gate transition to `approved` requires BOTH user confirmation of the rendered team table and per-agent cards AND the `agent_profiles_validated` sub-gate (all REQUIRED checks from [references/agent-profile-schema.md](references/agent-profile-schema.md) Validation Checklist have passed and the file at `.agentbloc/team/agent-profiles.yaml` has been written by the Designer subagent).
- Phase 3 specific: Gate transition to `approved` requires BOTH user confirmation of the rendered integrations table AND the `mcp_integrations_verified` sub-gate (all REQUIRED checks from [references/integration-manifest-schema.md](references/integration-manifest-schema.md) Validation Checklist have passed, every tool entry has `status: verified` with a `healthcheck_at` timestamp, and the file at `.agentbloc/integrations/integration-manifest.yaml` has been written).
```

This adds +1 line to the State Transitions section.

**Edit 2 — Phase 3 entry: insert Summary Gate paragraph + extend the See-line load list from 1 See-line to 4 See-lines.**

Use the Edit tool with this `old_string` (current Phase 3 entry body after its Precondition paragraph, lines 118-119):

```
You MUST read the complete integration analysis protocol before starting this phase:
See [references/phase-3-integration.md](references/phase-3-integration.md)
```

And this `new_string`:

```
**Summary Gate:** After walking the 4-step MCP search + three-check Verification Loop, write `.agentbloc/integrations/integration-manifest.yaml` silently. The rendered integrations table + per-tool evidence rows are what the user reviews and confirms (D-14 mirror). See [references/mcp-integration-protocol.md](references/mcp-integration-protocol.md) Verification Loop for the D-34 three-check protocol and Halt-and-Name Protocol for D-35 failure handling.

You MUST read the complete integration analysis protocol AND the MCP integration protocol AND the ecosystem registry AND the integration manifest schema before starting this phase:
See [references/phase-3-integration.md](references/phase-3-integration.md)
See [references/mcp-integration-protocol.md](references/mcp-integration-protocol.md)
See [references/mcp-ecosystem-registry.md](references/mcp-ecosystem-registry.md)
See [references/integration-manifest-schema.md](references/integration-manifest-schema.md)
```

This adds +7 lines to the Phase 3 entry (1 new Summary Gate paragraph + 1 blank line + 1 rewritten connector sentence + 3 new See-lines).

**Edit 3 — Phase 4 entry: insert Precondition paragraph at top of Phase 4 body.**

Use the Edit tool with this `old_string` (current Phase 4 entry body, lines 123-126):

```
### Phase 4: Step-by-Step Confirmation + Dry Run

Get explicit approval for every single step each agent will perform. Then execute a mandatory dry run: agents run against real data with all side-effect tools stubbed. Validate outputs before going live.

You MUST read the complete confirmation and dry run protocol before starting this phase:
```

And this `new_string`:

```
### Phase 4: Step-by-Step Confirmation + Dry Run

Get explicit approval for every single step each agent will perform. Then execute a mandatory dry run: agents run against real data with all side-effect tools stubbed. Validate outputs before going live.

**Precondition:** Verify `.agentbloc/integrations/integration-manifest.yaml` exists AND every tool entry has `status: verified` with a `healthcheck_at` timestamp (per [references/integration-manifest-schema.md](references/integration-manifest-schema.md) Validation Checklist). If the file is missing, any entry is `status: failed`, or any REQUIRED check fails, return the state bar to Phase 3 with gate `pending` and re-run the Summary gate before attempting Phase 4 again.

You MUST read the complete confirmation and dry run protocol before starting this phase:
```

This adds +2 lines to the Phase 4 entry (1 Precondition paragraph + 1 blank line).

**Cumulative line count delta: +1 (Edit 1) + +7 (Edit 2) + +2 (Edit 3) = +10 lines. SKILL.md 170 + 10 = 180 lines, well under the 195-line budget (D-41), and well under the 250-line v1.0 cap.**

**Preservation requirements (critical):**
- Do NOT edit the Phase 1 section, Phase 2 section (beyond the existing State Transitions Phase 2 bullet, which stays verbatim as the anchor for Edit 1), Phase 5 section, Phase 6 section, Hard Gates, Language and Technical Level, Quality Checklist, or Reference Implementation sections.
- After all three edits, the file must still contain: YAML frontmatter unchanged; `# AgentBloc -- AI Agent Team Designer` H1 unchanged; `## State Protocol` section with Phase 1 specific and Phase 2 specific bullets both intact; `## Hard Gates` section with its 5 rules; `### Phase 1: Deep Interview` through `### Phase 6: Evolution` all intact with Phase 1 and Phase 2 preconditions unchanged.
- Verify line count: `wc -l .claude/skills/agentbloc/SKILL.md` returns a number between 170 and 195 inclusive.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "^- Phase 3 specific:" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "mcp_integrations_verified" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "integration-manifest-schema\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "mcp-integration-protocol\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "mcp-ecosystem-registry\.md" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "\.agentbloc/integrations/integration-manifest\.yaml" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -q "^\*\*Summary Gate:\*\*" .claude/skills/agentbloc/SKILL.md &amp;&amp; grep -c "^- Phase [0-9]* specific:" .claude/skills/agentbloc/SKILL.md | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/SKILL.md` exits 0
    - NEW State Transitions bullet present (Edit 1): `grep -q "^- Phase 3 specific:" .claude/skills/agentbloc/SKILL.md`
    - All three Phase-specific State Transitions bullets present: `grep -c "^- Phase [0-9]* specific:" .claude/skills/agentbloc/SKILL.md` returns >= 3 (Phase 1 + Phase 2 + Phase 3)
    - Phase 3 specific bullet names the new sub-gate: `grep -q "mcp_integrations_verified" .claude/skills/agentbloc/SKILL.md`
    - Phase 3 specific bullet cites integration-manifest-schema.md: line matching `Phase 3 specific` also matches `integration-manifest-schema\.md` — verify with `grep "Phase 3 specific" .claude/skills/agentbloc/SKILL.md | grep -q "integration-manifest-schema\.md"`
    - Phase 3 specific bullet names the healthcheck_at timestamp requirement: line matching `Phase 3 specific` also matches `healthcheck_at` — `grep "Phase 3 specific" .claude/skills/agentbloc/SKILL.md | grep -q "healthcheck_at"`
    - Phase 3 specific bullet names the manifest path: line matching `Phase 3 specific` also matches `\.agentbloc/integrations/integration-manifest\.yaml`
    - Summary Gate paragraph inserted in Phase 3 entry (Edit 2): `grep -q "^\*\*Summary Gate:\*\*" .claude/skills/agentbloc/SKILL.md`
    - Summary Gate paragraph cites D-14 + D-34 + D-35: `grep -B0 -A2 "Summary Gate" .claude/skills/agentbloc/SKILL.md | grep -q "D-14\|D-34\|Verification Loop"`
    - Phase 3 entry load-list extended with 3 new See-lines (Edit 2):
      - `grep -q "^See \[references/mcp-integration-protocol\.md\]" .claude/skills/agentbloc/SKILL.md` OR `grep -q "See .references/mcp-integration-protocol\.md." .claude/skills/agentbloc/SKILL.md`
      - `grep -q "See .references/mcp-ecosystem-registry\.md." .claude/skills/agentbloc/SKILL.md`
      - `grep -q "See .references/integration-manifest-schema\.md." .claude/skills/agentbloc/SKILL.md`
    - Phase 3 existing See-line to phase-3-integration.md still present: `grep -q "See .references/phase-3-integration\.md." .claude/skills/agentbloc/SKILL.md`
    - Phase 4 Precondition paragraph inserted (Edit 3):
      - `grep -q "^### Phase 4: Step-by-Step" .claude/skills/agentbloc/SKILL.md` (header unchanged)
      - `grep -Ec "^\*\*Precondition:\*\*" .claude/skills/agentbloc/SKILL.md` returns >= 3 (Phase 2 + Phase 3 + Phase 4 now all have Preconditions; Phase 4's is new from this plan)
      - The Phase 4 Precondition names integration-manifest.yaml: find Phase 4 section content and grep — use `awk '/^### Phase 4:/,/^### Phase 5:/' .claude/skills/agentbloc/SKILL.md | grep -q "integration-manifest\.yaml"`
      - The Phase 4 Precondition names status: verified: `awk '/^### Phase 4:/,/^### Phase 5:/' .claude/skills/agentbloc/SKILL.md | grep -q "status: verified"`
      - The Phase 4 Precondition names healthcheck_at: `awk '/^### Phase 4:/,/^### Phase 5:/' .claude/skills/agentbloc/SKILL.md | grep -q "healthcheck_at"`
    - Preservation: Phase 1 specific bullet unchanged: `grep -q "^- Phase 1 specific:.*business_graph_validated" .claude/skills/agentbloc/SKILL.md`
    - Preservation: Phase 2 specific bullet unchanged: `grep -q "^- Phase 2 specific:.*agent_profiles_validated" .claude/skills/agentbloc/SKILL.md`
    - Preservation: Phase 2 Summary Gate paragraph (from Phase 9 09-03) intact: `grep -q "spawn the Designer Agent subagent" .claude/skills/agentbloc/SKILL.md`
    - Preservation: Phase 3 existing Precondition paragraph (from Phase 9 09-03) intact: `awk '/^### Phase 3:/,/^### Phase 4:/' .claude/skills/agentbloc/SKILL.md | grep -q "agent-profile-schema\.md"`
    - Preservation: Hard Gates section intact: `grep -q "^## Hard Gates$" .claude/skills/agentbloc/SKILL.md`
    - Preservation: Quality Checklist section intact: `grep -q "^## Quality Checklist$" .claude/skills/agentbloc/SKILL.md`
    - Preservation: Reference Implementation closer intact: `grep -q "^## Reference Implementation$" .claude/skills/agentbloc/SKILL.md`
    - Line count within budget: `wc -l .claude/skills/agentbloc/SKILL.md` returns a number between 170 and 195 inclusive (strict upper cap per D-41)
    - Line count well under v1.0 250-line cap: `wc -l .claude/skills/agentbloc/SKILL.md` returns a number less than 250
    - No em-dashes added: `grep -c "—" .claude/skills/agentbloc/SKILL.md` returns 0
  </acceptance_criteria>
  <done>
SKILL.md received three surgical edits exactly mirroring the Phase 9 09-03 commit 783b538 pattern one phase forward: (1) State Transitions section now has Phase-1-specific + Phase-2-specific + Phase-3-specific bullets, with the Phase 3 specific bullet naming mcp_integrations_verified sub-gate, the integration-manifest-schema.md Validation Checklist reference, the `status: verified` + `healthcheck_at` requirement, and the `.agentbloc/integrations/integration-manifest.yaml` file path; (2) Phase 3 entry gained a Summary Gate paragraph citing D-14 silent emission + D-34 Verification Loop + D-35 Halt-and-Name protocol from mcp-integration-protocol.md, and the See-line load list grew from 1 to 4 references (phase-3-integration.md + mcp-integration-protocol.md + mcp-ecosystem-registry.md + integration-manifest-schema.md); (3) Phase 4 entry gained a Precondition paragraph gating on integration-manifest.yaml existing + every entry `status: verified` + `healthcheck_at` timestamp per the integration-manifest-schema.md Validation Checklist, with state-bar-return-to-Phase-3 on any REQUIRED failure. All existing Phase 1 / Phase 2 / Phase 5 / Phase 6 content, Hard Gates, Quality Checklist, and Reference Implementation sections preserved verbatim. File grew from 170 to ~180 lines (within 170-195 budget per D-41; well under 250-line v1.0 cap). Zero new em-dashes.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these phase-level checks:

1. `test -f .claude/skills/agentbloc/references/phase-3-integration.md` — file still exists after surgical edits
2. `test -f .claude/skills/agentbloc/SKILL.md` — file still exists after surgical edits
3. phase-3-integration.md has 3 new cross-references:
   - `grep -q "mcp-integration-protocol\.md" .claude/skills/agentbloc/references/phase-3-integration.md`
   - `grep -q "mcp-ecosystem-registry\.md" .claude/skills/agentbloc/references/phase-3-integration.md`
   - `grep -q "integration-manifest-schema\.md" .claude/skills/agentbloc/references/phase-3-integration.md`
4. phase-3-integration.md Priority 1/2 swap: `grep -q "^### Priority 1: MCP Server" ...` AND `grep -q "^### Priority 2: Official API" ...`
5. phase-3-integration.md Priority 3 marker: `grep -q "\[Phase 11 scope\]" ...`
6. phase-3-integration.md Steps 3-7 preserved: `grep -q "^## Step 3:" ...` AND `grep -q "^## Step 4:" ...` AND `grep -q "^## Step 5:" ...` AND `grep -q "^## Step 6:" ...` AND `grep -q "^## Step 7:" ...`
7. SKILL.md has 3 Phase-specific State Transitions bullets: `grep -c "^- Phase [0-9]* specific:" .claude/skills/agentbloc/SKILL.md` returns >= 3
8. SKILL.md Phase 3 entry extended: `grep -q "Summary Gate" .claude/skills/agentbloc/SKILL.md` AND 4 See-lines in Phase 3 section
9. SKILL.md Phase 4 entry has new Precondition: `awk '/^### Phase 4:/,/^### Phase 5:/' .claude/skills/agentbloc/SKILL.md | grep -q "integration-manifest\.yaml"`
10. SKILL.md budget: `wc -l .claude/skills/agentbloc/SKILL.md` between 170 and 195
11. phase-3-integration.md budget: `wc -l .claude/skills/agentbloc/references/phase-3-integration.md` between 388 and 430
12. Every Phase 10 requirement in this plan traced:
   - INTEG-02 (ecosystem path reinforced): phase-3-integration.md new Priority 1 names the Step-2 ecosystem registry lookup + mcp-ecosystem-registry.md; SKILL.md Phase 3 loads mcp-ecosystem-registry.md
   - INTEG-05 (halt-and-name UX wired into SKILL.md gates): phase-3-integration.md Priority 1 summary names D-35 Halt-and-Name; SKILL.md Phase 3 Summary Gate paragraph cites D-35 failure handling; SKILL.md Phase 3 specific bullet blocks gate transition until every entry has status: verified
</verification>

<success_criteria>
1. `references/phase-3-integration.md` received three surgical edits: (a) Phase 3 entry also-load list extended with 3 new bullets (mcp-integration-protocol.md + mcp-ecosystem-registry.md + integration-manifest-schema.md); (b) Step 2 intro sentence reordered per D-40 MCP-first + Priority 1 and Priority 2 section headers swapped (new Priority 1: MCP Server (Four-Step Search) delegates to mcp-integration-protocol.md while preserving v1.0 field summary + naming D-34 + D-35 + mcp-builder; new Priority 2: Official API with "Fallback when no MCP" lead sentence preserving 4 v1.0 bullets); (c) Priority 3 Playwright Browser Automation got `[Phase 11 scope]` marker + forward See-line to browser-fallback.md + v1.0 4-bullet summary preserved under "Summary (v1.0, preserved)". Steps 3-7, Integration Gate, and Quick Reference preserved verbatim. File between 388 and 430 lines.
2. `SKILL.md` received three surgical edits mirroring Phase 9 09-03 one phase forward: (a) State Transitions Phase-3-specific bullet inserted after Phase-2-specific bullet, naming `mcp_integrations_verified` sub-gate + integration-manifest-schema.md Validation Checklist + status: verified + healthcheck_at + integration-manifest.yaml path; (b) Phase 3 entry gained Summary Gate paragraph citing D-14 + D-34 + D-35 and load-list grew to 4 See-lines; (c) Phase 4 entry gained Precondition paragraph gating on integration-manifest.yaml + every entry status: verified + healthcheck_at. Phase 1 / 2 / 5 / 6 content, Hard Gates, Quality Checklist, and Reference Implementation sections preserved verbatim. File between 170 and 195 lines; well under v1.0 250-line cap.
3. INTEG-02 traced: ecosystem install path is the operational Step 2 Priority 1 sub-step in phase-3-integration.md (reinforced by mcp-ecosystem-registry.md load) AND SKILL.md loads the registry reference at Phase 3 entry.
4. INTEG-05 traced: Halt-and-Name protocol (specific-gap naming, no silent degradation) is cited in phase-3-integration.md Priority 1 summary AND in SKILL.md Phase 3 Summary Gate paragraph; the `mcp_integrations_verified` sub-gate in State Transitions blocks Phase 4 transition until every entry has status: verified.
5. All 6 Phase 10 requirements (INTEG-01..06) are now collectively covered across Plans 10-01 (contracts: INTEG-01/02/04/06), 10-02 (mcp-builder for INTEG-03), and 10-03 (wiring: INTEG-02 reinforced + INTEG-05 gate).
6. All 5 ROADMAP Phase 10 success criteria are now wired end-to-end: existing .mcp.json skips to verification (Step 1 in protocol + Priority 1 summary), ecosystem install proposes npx -y (Step 2 + D-37 in protocol + SKILL.md load), wrapper generated via mcp-builder (Step 3 in protocol + Plan 10-02 skill + Priority 1 summary cites mcp-builder path), verification failure surfaces specific gap (D-34 + D-35 in protocol + SKILL.md Phase 3 Summary Gate + State Transitions sub-gate), evidence URL + version + last_commit in DEPLOY-REPORT.md (D-39 fields in schema + Phase 4 Precondition enforces healthcheck_at).
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-discovery-mcp-path/10-03-SUMMARY.md` documenting:
- Final line counts: phase-3-integration.md (X lines, delta +Y from 388) + SKILL.md (X lines, delta +Y from 170)
- Diff summary for phase-3-integration.md (list the 3 surgical edits with before/after snippets)
- Diff summary for SKILL.md (list the 3 surgical edits with before/after snippets)
- Confirmation that Steps 3-7 + Integration Gate + Quick Reference in phase-3-integration.md are unchanged
- Confirmation that Phase 1 / 2 / 5 / 6 content + Hard Gates + Quality Checklist + Reference Implementation in SKILL.md are unchanged
- Any deviations from the plan with rationale (e.g., if line counts land outside budget, explain)
- Handoff note for Phase 10 verification: "All 6 INTEG requirements (INTEG-01 through INTEG-06) wired end-to-end across Plans 10-01 / 10-02 / 10-03. Phase 10 structurally complete. Ready for `/gsd-verify-phase 10`. Next phase: Phase 11 Integration Discovery - Browser Fallback (BROWSER-01..12). Phase 11 creates `references/browser-fallback.md` to resolve the forward See-line inserted in phase-3-integration.md Priority 3 by this plan."
- Handoff note for Phase 12 (Deploy Pipeline): "Phase 12 consumes `.agentbloc/integrations/integration-manifest.yaml` to render .mcp.json merges + ClaudeClaw job configs. Every manifest entry with status: verified is ready for deploy; status: failed blocks Phase 4 gate; [UNVERIFIED] entries (missing evidence fields) surface as warnings in DEPLOY-REPORT.md per v1.0 INTG-06 carry-forward."
</output>
</content>
</invoke>