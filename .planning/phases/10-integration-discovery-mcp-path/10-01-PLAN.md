---
phase: 10-integration-discovery-mcp-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/agentbloc/references/mcp-integration-protocol.md
  - .claude/skills/agentbloc/references/mcp-ecosystem-registry.md
  - .claude/skills/agentbloc/references/integration-manifest-schema.md
  - .claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml
autonomous: true
requirements:
  - INTEG-01
  - INTEG-02
  - INTEG-04
  - INTEG-06

must_haves:
  truths:
    - "references/mcp-integration-protocol.md exists as a loadable reference with TOC, When This Applies, Step 1 Existing .mcp.json, Step 2 Ecosystem Registry, Step 3 Wrapper Generation, Step 4 Browser Fallback (Phase 11 stub), Verification Loop (D-34 three checks), Halt-and-Name (D-35), Evidence Protocol (D-39 extending v1.0 INTG-03), and Quick Reference"
    - "references/mcp-ecosystem-registry.md exists as a loadable reference with TOC, When This Applies, at least 6 service-category H2 sections (Communication, Google Workspace, E-Commerce, CRM, Accounting, Browser Automation, Development), Trust Tier Criteria section, and Quick Reference. Seeded from CLAUDE.md ecosystem table with at least 15 entries across categories."
    - "references/integration-manifest-schema.md exists as a loadable reference with TOC, When This Applies, YAML Schema Definition per D-36, three-tier Field Obligation Matrix (REQUIRED / RECOMMENDED / OPTIONAL), three bounded enums (Resolution Method / Trust Tier / Status), 8-check Validation Checklist, Emission Protocol, Re-run Behavior, and Schema Versioning Rules."
    - "examples/arco-rooms-integration-manifest.yaml exists as valid YAML with at least 8 tool entries covering all three MCP resolution methods (existing / ecosystem / wrapper), all used_by[] values resolving to the 3 Arco Rooms agents (gestor-documental, gestor-cobros, recepcionista), and no browser-fallback or failed entries (happy-path fixture per D-42)."
    - "A reader can understand the full integration-manifest.yaml field set by reading ONLY integration-manifest-schema.md (no validator code, because there is none per D-13 inheritance)"
    - "The 4-step search protocol in mcp-integration-protocol.md honors MCP-first positioning (Step 1/2/3 all MCP paths before Step 4 browser-fallback stub) and cites the v1.0 evidence protocol carry-forward per INTEG-06 / D-39"
  artifacts:
    - path: ".claude/skills/agentbloc/references/mcp-integration-protocol.md"
      provides: "Canonical 4-step MCP search protocol + D-34 three-check verification loop + D-35 halt-and-name protocol + D-39 MCP-extended evidence record. Dual structural twin of orchestration-patterns.md (imperative flow) + phase-3-integration.md (step grammar)."
      min_lines: 130
      contains: "Step 1"
    - path: ".claude/skills/agentbloc/references/mcp-ecosystem-registry.md"
      provides: "Curated MCP server registry seeded from CLAUDE.md MCP Server Ecosystem tables. Per-category tables with package / publisher / trust_tier / last_commit / required_scopes / tools_declared columns. Structural twin of frameworks.md."
      min_lines: 130
      contains: "HIGH"
    - path: ".claude/skills/agentbloc/references/integration-manifest-schema.md"
      provides: "Canonical integration-manifest.yaml schema definition + three-tier field obligation matrix + resolution_method/trust_tier/status bounded enums + prose validation checklist + emission protocol + re-run behavior + schema versioning rules. Dual structural twin of agent-profile-schema.md + business-graph-schema.md."
      min_lines: 160
      contains: "schema_version"
    - path: ".claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml"
      provides: "Canonical test fixture — at least 8 tools across 3 resolution methods (existing / ecosystem / wrapper) satisfying every REQUIRED check in integration-manifest-schema.md Validation Checklist. Fixture family with arco-rooms-agent-profiles.yaml (shares agent IDs in used_by[])."
      contains: "schema_version"
  key_links:
    - from: ".claude/skills/agentbloc/references/integration-manifest-schema.md"
      to: ".claude/skills/agentbloc/references/mcp-integration-protocol.md"
      via: "cross-link in When This Applies + Validation Checklist (checks 4-6 delegate to protocol's D-34 three-check verification loop)"
      pattern: "mcp-integration-protocol\\.md"
    - from: ".claude/skills/agentbloc/references/mcp-integration-protocol.md"
      to: ".claude/skills/agentbloc/references/mcp-ecosystem-registry.md"
      via: "cross-link — Step 2 (ecosystem registry lookup) consults this file as the lookup table"
      pattern: "mcp-ecosystem-registry\\.md"
    - from: ".claude/skills/agentbloc/references/mcp-integration-protocol.md"
      to: ".claude/skills/agentbloc/references/credentials.md"
      via: "cross-link — D-34 Check 2 (scope-match) delegates to credentials decision tree for scope naming and rotation policy"
      pattern: "credentials\\.md"
    - from: ".claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml"
      to: ".claude/skills/agentbloc/references/integration-manifest-schema.md"
      via: "fixture conforms to schema — every REQUIRED check passes on this YAML"
      pattern: "schema_version"
    - from: ".claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml"
      to: ".claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml"
      via: "fixture family — used_by[] values resolve to agent IDs in the Phase 9 fixture (gestor-documental / gestor-cobros / recepcionista)"
      pattern: "gestor-documental\\|gestor-cobros\\|recepcionista"
---

<objective>
Create the three CONTRACT files for Phase 10 — the mcp-integration-protocol reference (4-step search + D-34 verification + D-35 halt-and-name + D-39 evidence protocol), the mcp-ecosystem-registry reference (curated lookup table seeded from CLAUDE.md), and the integration-manifest-schema reference (D-36 YAML schema + prose validator per D-13 inheritance) — plus the canonical 8-tool Arco Rooms test fixture that downstream Plan 10-03 cites and Phase 16 end-to-end validation replays.

Purpose: Phase 10's entire integration surface (SKILL.md Phase 3 gate, phase-3-integration.md surgical edits, mcp-builder skill wrapper output) reads from these three contract files. Lock the contracts first, wire the builder + skill second (Plans 10-02 and 10-03). Mirrors the Phase 8 Plan 01 (schema + fixture first, wiring second) and Phase 9 Plan 01 (dual-schema + fixture first, subagent + wiring second) pattern exactly — same decomposition, one phase later.

Output: Three ~130-220 line reference files structurally twinned with orchestration-patterns.md (D-31 protocol reference), frameworks.md (D-31 registry reference), and agent-profile-schema.md + business-graph-schema.md (D-31 schema reference), plus one canonical YAML fixture derived from examples/arco-rooms-agent-profiles.yaml tools[] arrays.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-integration-discovery-mcp-path/10-CONTEXT.md
@.planning/phases/10-integration-discovery-mcp-path/10-PATTERNS.md

@.claude/skills/agentbloc/references/orchestration-patterns.md
@.claude/skills/agentbloc/references/frameworks.md
@.claude/skills/agentbloc/references/agent-profile-schema.md
@.claude/skills/agentbloc/references/business-graph-schema.md
@.claude/skills/agentbloc/references/phase-3-integration.md
@.claude/skills/agentbloc/references/credentials.md
@.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml
@.claude/skills/agentbloc/examples/arco-rooms-business-graph.json

<interfaces>
<!-- Canonical integration-manifest.yaml shape (locked by 10-CONTEXT.md D-36). -->
<!-- This is the contract the new integration-manifest-schema.md must define and the fixture must satisfy. -->

```yaml
schema_version: 1                              # REQUIRED. Integer. Bumped only on breaking changes.
generated_at: "ISO-8601"                       # REQUIRED. When first written.
modified_at: "ISO-8601"                        # RECOMMENDED. Bumped on every re-verification.
tools:                                         # REQUIRED. Length >= 1.
  - tool_id: "gmail"                           # REQUIRED. kebab-case, unique within file, matches an entry in agent-profiles.yaml tools[].
    resolution_method: "existing | ecosystem | wrapper | browser-fallback | failed"   # REQUIRED. See Resolution Method Bounded Enum.
    mcp_server:                                # REQUIRED.
      package: "string"                        # REQUIRED. npm package name, OR path under .mcp/generated/<tool-id>/ for wrapper, OR existing .mcp.json key for existing.
      version: "string"                        # REQUIRED. Package version or wrapper commit SHA.
      installed_via: "string | null"           # OPTIONAL. e.g., "npx -y @smithery-ai/gmail-mcp" or "wrapper" or ".mcp.json existing".
    evidence:                                  # REQUIRED.
      url: "string"                            # REQUIRED. GitHub/npm/docs URL.
      last_commit: "ISO-8601 date | null"      # RECOMMENDED. Per v1.0 INTG-03.
      publisher: "string | null"               # RECOMMENDED. Per v1.0 INTG-03.
      trust_tier: "HIGH | MEDIUM | LOW"        # REQUIRED. Per v1.0 INTG-04.
      tools_declared: ["string", ...]          # RECOMMENDED. Result of tools/list probe (D-39).
      required_scopes: ["string", ...]         # RECOMMENDED. Credential scopes declared (D-39).
      healthcheck_at: "ISO-8601 timestamp"     # RECOMMENDED. When D-34 three checks last passed (D-39).
    used_by: ["<agent-id>", ...]               # RECOMMENDED. Must resolve to agents[] in agent-profiles.yaml (Check 7).
    status: "pending | verified | failed"      # REQUIRED. See Status Bounded Enum.
    failure_reason: "string | null"            # OPTIONAL. Populated only when status=failed.
```

The 5 resolution methods (D-36 bounded enum):
- `existing` — tool already in `.mcp.json` before Phase 3; skip directly to D-34 verification
- `ecosystem` — tool found in mcp-ecosystem-registry.md; propose `npx -y <package>` for user approval (D-37)
- `wrapper` — no MCP exists but public API does; generate via mcp-builder skill (Plan 10-02) at `.mcp/generated/<tool-id>/`
- `browser-fallback` — Phase 11 stub only; do NOT populate in Phase 10 fixture
- `failed` — none of the above resolved; D-35 halt-and-name protocol triggers

The 3 trust tiers (v1.0 INTG-04 inheritance): HIGH / MEDIUM / LOW.

The 3 status values: `pending` (initial) / `verified` (all D-34 checks passed) / `failed` (any check failed).

Structural twins:
- mcp-integration-protocol.md <- orchestration-patterns.md (imperative flow) + phase-3-integration.md (step grammar) per D-31
- mcp-ecosystem-registry.md <- frameworks.md (curated table) per D-31
- integration-manifest-schema.md <- agent-profile-schema.md + business-graph-schema.md (dual spine) per D-31
- arco-rooms-integration-manifest.yaml <- arco-rooms-agent-profiles.yaml (fixture family — same agent IDs)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mcp-ecosystem-registry.md entries | Declared package names and publishers are untrusted (could have supply-chain compromise); trust_tier gate + last_commit date gate this risk |
| arco-rooms-integration-manifest.yaml | Fixture is static test data; no runtime exposure in Phase 10, but becomes input to Phase 16 end-to-end validation |
| mcp-integration-protocol.md Step 2 | Ecosystem lookup; registry data becomes executable npx command in Plan 10-03 wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T (tampering) | Registry entry with compromised package | mitigate | Trust tier gate per v1.0 INTG-04 (HIGH requires >500 stars + <90d commit); evidence protocol D-39 enforces URL + publisher + last_commit fields on every entry; Phase 10 registry seeds ONLY from CLAUDE.md verified list |
| T-10-02 | I (information disclosure) | Fixture leaking real credentials | accept | Fixture contains NO real credentials — all entries reference env var names (e.g., GOOGLE_OAUTH_TOKEN) not values; grep confirms no token-like strings |
| T-10-03 | S (spoofing) | Schema validator false-passes malformed manifest | mitigate | 8-check Validation Checklist per D-13 inheritance; Check 4 (resolution_method enum) + Check 6 (trust_tier enum) + Check 7 (used_by resolves) catch spoofing attempts |
| T-10-04 | D (denial of service) | Schema rejecting valid manifests due to over-strict REQUIRED tier | accept | REQUIRED tier covers only fields the Phase 12 Deploy Pipeline cannot degrade on (package, version, url, trust_tier, status); everything else RECOMMENDED with null defaults |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create references/mcp-integration-protocol.md with 4-step search + D-34 verification loop + D-35 halt-and-name + D-39 evidence protocol</name>
  <files>.claude/skills/agentbloc/references/mcp-integration-protocol.md</files>
  <read_first>
    - .planning/phases/10-integration-discovery-mcp-path/10-PATTERNS.md (lines 22-101 — the "mcp-integration-protocol.md" section with Patterns A (file-opening), B (step-section grammar from phase-3-integration.md lines 64-117), C (bounded-enum table), D (Quick Reference closer), E (cross-reference inline))
    - .planning/phases/10-integration-discovery-mcp-path/10-CONTEXT.md (D-31 three-reference split rationale; D-34 three-check verification; D-35 halt-and-name + VERIFICATION-FAILED.md artifact; D-37 approval-gated install; D-38 credential gap UX; D-39 MCP-extended evidence record; D-40 MCP-first promotion)
    - .claude/skills/agentbloc/references/orchestration-patterns.md (the 121-line structural twin — copy skeleton verbatim: H1 + blockquote + TOC + When This Applies + bounded-enum table + Quick Reference)
    - .claude/skills/agentbloc/references/phase-3-integration.md (lines 1-170 — step-heading grammar analog; specifically Step 2 Multi-Method Search Protocol shape + Step 3 Evidence Verification)
    - .claude/skills/agentbloc/references/credentials.md (cross-link target — D-34 Check 2 scope-match delegates here)
    - .claude/skills/agentbloc/references/prompt-injection.md (cross-link target — evidence protocol assigns defense layer per ingestion surface)
    - .planning/REQUIREMENTS.md (INTEG-01, INTEG-02, INTEG-04, INTEG-06 — verify the 4-step search + verification + evidence protocol are all covered by this file)
  </read_first>
  <action>
Create a NEW file at `.claude/skills/agentbloc/references/mcp-integration-protocol.md` (~150-220 lines) with EXACTLY the following 9 sections in this order. Use GitHub-flavored Markdown. This file is a dual structural twin — copy the H1 + blockquote + TOC + "When This Applies" + Quick Reference spine from orchestration-patterns.md; copy the "Step N: <action> / Priority N: <path>" heading grammar from phase-3-integration.md lines 64-117.

**Section 1 — H1 + blockquote + TOC** (mirrors orchestration-patterns.md lines 1-12):

```markdown
# MCP Integration Protocol

> Loaded by SKILL.md at Phase 3 entry alongside [phase-3-integration.md](phase-3-integration.md), [mcp-ecosystem-registry.md](mcp-ecosystem-registry.md), and [integration-manifest-schema.md](integration-manifest-schema.md). Defines the 4-step MCP search (existing `.mcp.json` -> ecosystem registry -> wrapper generation -> browser fallback) that Phase 3 walks for every tool entry in `.agentbloc/team/agent-profiles.yaml`, plus the three-check verification loop and halt-and-name protocol. Per v2.0 positioning (PROJECT.md Constraints), every external integration goes through an MCP server. Official-API direct calls are a fallback in [phase-3-integration.md](phase-3-integration.md), not the primary path.

## Table of Contents

- [When This Applies](#when-this-applies)
- [Step 1: Check Existing .mcp.json](#step-1-check-existing-mcpjson)
- [Step 2: Query Ecosystem Registry](#step-2-query-ecosystem-registry)
- [Step 3: Generate Wrapper via mcp-builder](#step-3-generate-wrapper-via-mcp-builder)
- [Step 4: Browser Fallback (Phase 11 Scope)](#step-4-browser-fallback-phase-11-scope)
- [Verification Loop](#verification-loop)
- [Halt-and-Name Protocol](#halt-and-name-protocol)
- [Evidence Protocol](#evidence-protocol)
- [Quick Reference](#quick-reference)
```

**Section 2 — `## When This Applies`**: one paragraph adapted from orchestration-patterns.md When-This-Applies shape. Exact emission:

```markdown
## When This Applies

Claude loads this file at Phase 3 entry (see SKILL.md Phase 3). For each tool declared in an agent's `tools[]` array in `.agentbloc/team/agent-profiles.yaml`, Claude walks Steps 1 through 4 in order, stops on the first method that resolves, and then runs the Verification Loop. The output is one entry per tool in `.agentbloc/integrations/integration-manifest.yaml` (schema in [integration-manifest-schema.md](integration-manifest-schema.md)). Specific sections are referenced based on the tool's resolution state:

- **Fresh tool (first Phase 3 run):** walk Steps 1-4 in order; stop at first method that resolves.
- **Re-verification:** read existing manifest; re-run Verification Loop on every entry with `status != verified`.
- **Failed verification:** apply the Halt-and-Name Protocol; write `.agentbloc/integrations/<tool-id>/VERIFICATION-FAILED.md` per D-35; block Phase 3 gate until user resolves.

This file is imperative (step-by-step flow Claude follows); the registry ([mcp-ecosystem-registry.md](mcp-ecosystem-registry.md)) is declarative (lookup table Claude consults in Step 2); the schema ([integration-manifest-schema.md](integration-manifest-schema.md)) is the output contract. The three files together cover Phase 3 top to bottom.
```

**Section 3 — `## Step 1: Check Existing .mcp.json`**: imperative step with Priority 1 subsection. Exact emission:

```markdown
## Step 1: Check Existing .mcp.json

**Action:** Read `.mcp.json` (if present) and look for a top-level `mcpServers` entry whose key matches the agent's tool name (case-insensitive, hyphen/underscore normalized).

**Input:** `tools[].entry` from `.agentbloc/team/agent-profiles.yaml` (e.g., `playwright-mcp`, `google-workspace-mcp`).

**If found:**
- Write the manifest entry with `resolution_method: existing` per [integration-manifest-schema.md](integration-manifest-schema.md) Resolution Method Bounded Enum.
- Populate `mcp_server.package` from the `.mcp.json` entry's `command` or `args`; `installed_via: ".mcp.json existing"`.
- Skip directly to the Verification Loop below. Do NOT re-query the ecosystem registry or generate a wrapper.

**If not found:**
- Proceed to Step 2.

**Arco Rooms example:** `playwright-mcp` is already present in `.mcp.json` because gstack ships it. Phase 3 skips to verification for this tool.

**Rationale (D-31 + INTEG-01):** Steps 1-4 are ordered cheapest-to-most-expensive. Step 1 is free (a single file read). Always check before spending an ecosystem lookup or a wrapper generation.
```

**Section 4 — `## Step 2: Query Ecosystem Registry`**: Priority 2 subsection + D-37 approval-gated install. Exact emission:

```markdown
## Step 2: Query Ecosystem Registry

**Action:** Consult [mcp-ecosystem-registry.md](mcp-ecosystem-registry.md) for an entry matching the agent's tool name. The registry is a curated list with trust tiers pre-computed per v1.0 INTG-04.

**Input:** Same tool name from Step 1 that did NOT resolve.

**If found in registry:**
1. Compute the `npx -y <package>` install command from the registry entry.
2. Present the command + trust_tier + evidence (URL, last_commit, publisher) to the user in the conversation.
3. Wait for explicit user approval ("yes" / "install" / "adelante").
4. On approval, write the MCP entry into `.mcp.json` via the Edit tool. Claude does NOT execute `npx` itself (D-37).
5. Tell the user: "Run this command in your shell: `npx -y <package>`, then say `installed` when done."
6. On user confirmation, write the manifest entry with `resolution_method: ecosystem`, `mcp_server.installed_via: "npx -y <package>"`, and proceed to the Verification Loop.

**If not found in registry:**
- Proceed to Step 3.

**Arco Rooms example:** `gmail-mcp` resolves to `@smithery-ai/gmail-mcp` in the registry; user approves the install; manifest entry records `resolution_method: ecosystem`.

**Rationale (D-37):** Claude never runs `npx` because arbitrary install scripts are a real supply-chain attack surface. `.mcp.json` is declarative config — safe for Claude to edit. The user runs the actual install in their own shell, preserving the auditable boundary.
```

**Section 5 — `## Step 3: Generate Wrapper via mcp-builder`**: Priority 3 subsection + D-33 wrapper output shape. Exact emission:

```markdown
## Step 3: Generate Wrapper via mcp-builder

**Action:** Invoke the `mcp-builder` skill (top-level skill at `.claude/skills/mcp-builder/`) with (a) the calling agent's `tools[].entry` + `outputs.schema` from `agent-profiles.yaml`, and (b) a public API spec URL or OpenAPI document the user provides.

**Input:** Tool name from Step 2 that did NOT resolve + URL of the target service's public API documentation.

**Process:**
1. mcp-builder reads the agent's tool entry and output schema to determine the minimum viable tool surface needed (D-33b least-privilege posture).
2. mcp-builder fetches the API spec via WebFetch and generates three files under `.mcp/generated/<tool-id>/`:
   - `package.json` (name + version + deps limited to `@modelcontextprotocol/sdk`)
   - `index.ts` (MCP server implementation with 1-N tool handlers matching the agent's needs)
   - `README.md` (what API it wraps, required env vars, tool surface documented inline)
3. mcp-builder returns the generated path + tool surface summary to the main session.
4. User runs `bun install` in `.mcp/generated/<tool-id>/` in their own shell.
5. Claude writes the manifest entry with `resolution_method: wrapper`, `mcp_server.package: ".mcp/generated/<tool-id>/"`, `mcp_server.installed_via: "wrapper"`, and proceeds to the Verification Loop.

**If mcp-builder fails** (API spec unresolvable, schema insufficient, forbidden scope):
- Proceed to Step 4.

**Arco Rooms example:** `bbva-mcp` has no ecosystem entry; mcp-builder generates a PSD2 wrapper at `.mcp/generated/bbva-mcp/` exposing exactly `list_transactions(since_iso)` and `get_balance()` — the only two calls Gestor Cobros needs. Full BBVA API surface is NOT exposed.

**Rationale (D-33 + D-33b):** TypeScript + `@modelcontextprotocol/sdk` + Bun matches ClaudeClaw runtime (PROJECT.md Constraints). Least-privilege tool surface means less attack surface and testable verification in Phase 10 (you can probe one tool; you cannot probe every API endpoint).
```

**Section 6 — `## Step 4: Browser Fallback (Phase 11 Scope)`**: stub only; defers to Phase 11. Exact emission:

```markdown
## Step 4: Browser Fallback (Phase 11 Scope)

**Action:** STUB — the browser-fallback subagent at `.claude/agents/browser-discovery.md` is Phase 11 (BROWSER-01..12) work. Phase 10 records the resolution_method enum value `browser-fallback` in [integration-manifest-schema.md](integration-manifest-schema.md) for forward compatibility, but the actual Playwright + Patchright + HAR capture + injection detector + PII redaction flow is NOT implemented here.

**If Steps 1-3 all failed:**
- Phase 10 treats this as a halt condition (no browser fallback yet).
- Write the manifest entry with `resolution_method: failed`, `status: failed`, `failure_reason: "browser-fallback (Phase 11) not yet available"`.
- Apply the Halt-and-Name Protocol below.
- The user can either (a) wait for Phase 11 to ship, (b) manually craft a wrapper and place it at `.mcp/generated/<tool-id>/`, or (c) remove the tool from the agent's `tools[]` and re-run Phase 2 Designer editing.

**Forward reference:** See [references/browser-fallback.md](browser-fallback.md) (forthcoming Phase 11) for the full protocol. This link is broken in Phase 10 — Phase 11 will create the file.

**Rationale (Phase boundary):** Scope lock per 10-CONTEXT.md § Out of scope. Browser-fallback is deferred explicitly; Phase 10 ships the 3 MCP paths plus a stub slot so Phase 11 can add the fourth without schema migration.
```

**Section 7 — `## Verification Loop`**: D-34 three-check protocol as ordered numbered list. Exact emission:

```markdown
## Verification Loop

After a tool resolves via Step 1, 2, or 3, Claude runs three prose checks before stamping `status: verified` on the manifest entry. All three must PASS. Any FAIL triggers the Halt-and-Name Protocol.

**Check 1 — Ping (liveness)**
- Call `tools/list` on the MCP server. Confirm the server responds and declares at least one tool.
- FAIL: Set `status: failed`, `failure_reason: "server does not respond to tools/list"`, write VERIFICATION-FAILED.md per D-35.

**Check 2 — Scope match (authorization)**
- Confirm (a) the declared `tools_declared[]` intersects the agent's `tools[]` entry — the server exposes at least one tool the agent will actually use; AND (b) every required credential scope in `required_scopes[]` is present in `.env` OR the scope stub exists in `.env.example`.
- FAIL on missing scope: Auto-append the env var name to `.env.example` per D-38 with comment `# required by <agent-id> for <mcp-server> scope <scope-name>`, then halt with the specific missing var named in the conversation.
- FAIL on missing tool overlap: Surface "The <package> MCP does not expose the tool <agent-id> needs — proposed tools are <tools_declared>" and propose either reducing the agent's tool list or switching to Step 3 (wrapper) for the missing tool.
- Credential scope conventions live in [credentials.md](credentials.md).

**Check 3 — Shape probe (correctness)**
- Call the target tool with a dry-run argument set (a minimal argument the server will accept).
- Compare the response shape to the agent's `outputs.schema` in agent-profiles.yaml.
- FAIL: Surface BOTH shapes (declared vs observed) side-by-side in the conversation with the specific field mismatches quoted. Do not silently degrade.

**All three PASS:**
- Stamp `evidence.healthcheck_at` with the current ISO-8601 timestamp.
- Populate `evidence.tools_declared[]` from the `tools/list` result.
- Populate `evidence.required_scopes[]` from the server's README or declared metadata.
- Set `status: verified` on the manifest entry.
- Move to the next tool in the agent's `tools[]` array.
```

**Section 8 — `## Halt-and-Name Protocol`**: D-35 halt protocol + VERIFICATION-FAILED.md artifact. Exact emission:

```markdown
## Halt-and-Name Protocol

When any Verification Loop check FAILS, Claude does the following in one turn:

1. Write `.agentbloc/integrations/<tool-id>/VERIFICATION-FAILED.md` with the failing check number, the quoted failure, and a one-paragraph recommended fix.
2. Update the manifest entry to `status: failed` and `failure_reason: <specific-check>` (e.g., `"Check 2: scope gmail.modify missing from GOOGLE_OAUTH_TOKEN"`).
3. Block the Phase 3 gate — set the state bar's gate field to `blocked` until the failure is resolved.
4. Surface a targeted conversation to the user naming the specific gap. Template:

> "The `<package>` server is installed but Check <N> failed: `<specific-failure>`. To resolve: (a) <primary-fix>, or (b) <alternative-fix>. Which do you prefer?"

**Example — scope missing:** "The `gmail-mcp` server is installed but the `GOOGLE_OAUTH_TOKEN` scope is missing `gmail.modify`. To resolve: (a) add that scope to your Google OAuth consent screen, or (b) reduce the agent's tool scope to read-only by removing `send_message` from `recepcionista.tools[]`. Which do you prefer?"

**Example — credential missing:** "The `bbva-mcp` wrapper needs `BBVA_PSD2_CLIENT_ID` and `BBVA_PSD2_CLIENT_SECRET` in your `.env` file. I've appended them to `.env.example` with inline comments. Add the real values and say `credentials added`, then I will re-run verification."

**Pipeline behavior:** The pipeline does NOT silently degrade or proceed with a partially-verified integration. The file at `VERIFICATION-FAILED.md` gives the user a paper trail they can share when asking support for help. Per INTEG-05, verification failures surface with the specific gap named.

**Resuming:** After the user resolves the gap (adds scope, adds credential, edits tool list), the main session re-runs the Verification Loop for just that tool. Other already-verified tools are NOT re-verified.
```

**Section 9 — `## Evidence Protocol`**: D-39 extending v1.0 INTG-03 + `[UNVERIFIED]` carry-forward. Exact emission:

```markdown
## Evidence Protocol

Every manifest entry must carry the v1.0 evidence record (INTG-03) plus four v2.0 MCP-specific fields (D-39). The v1.0 record is inherited verbatim from [phase-3-integration.md](phase-3-integration.md) Step 3; the v2.0 extensions are captured during the Verification Loop above.

**v1.0 record (inherited from INTG-03):**
- `evidence.url` — GitHub / npm / official docs URL for the MCP server
- `mcp_server.version` — package version or wrapper commit SHA
- `evidence.last_commit` — ISO-8601 date of the most recent repo commit
- `evidence.publisher` — npm publisher name, GitHub org, or vendor name
- `evidence.trust_tier` — HIGH / MEDIUM / LOW per v1.0 INTG-04 (criteria in [mcp-ecosystem-registry.md](mcp-ecosystem-registry.md) Trust Tier Criteria)

**v2.0 MCP-specific extensions (D-39):**
- `evidence.tools_declared[]` — result of the Check 1 `tools/list` probe
- `evidence.required_scopes[]` — credential scopes declared by the server or inferred from its README
- `evidence.healthcheck_at` — ISO-8601 timestamp when the three-check loop last passed
- `mcp_server.installed_via` — the `npx -y <package>` command (ecosystem), the path under `.mcp/generated/<tool-id>/` (wrapper), or `.mcp.json existing` (existing)

**Missing any REQUIRED field:**
- Mark the entry `[UNVERIFIED]` per v1.0 INTG-06 (inherited).
- `[UNVERIFIED]` entries can still be accepted if the user explicitly overrides.
- The manifest carries the `[UNVERIFIED]` flag forward so Phase 12 Deploy Pipeline surfaces it in `DEPLOY-REPORT.md`.

**Evidence protocol rules:**
- Never silently omit a field. Missing evidence is a data point the user needs to see.
- Trust tier drift is real. An MCP that was HIGH at verification time might drop to MEDIUM 6 months later; `healthcheck_at` + `trust_tier` give Phase 6 Evolution a signal to re-verify on cadence.
- The evidence record is the contract with Phase 12 (Deploy Pipeline). Phase 12's quality ceiling is determined by how complete this record is — every field is load-bearing.
```

**Section 10 — `## Quick Reference`**: bullet list per orchestration-patterns.md Quick Reference shape. Exact emission:

```markdown
## Quick Reference

- **Step 1:** existing `.mcp.json` entry — cheapest, check first.
- **Step 2:** ecosystem registry — user-approved `npx -y <package>` install; Claude edits `.mcp.json`, user runs the shell command (D-37).
- **Step 3:** wrapper via mcp-builder skill — TypeScript + `@modelcontextprotocol/sdk` + Bun, output at `.mcp/generated/<tool-id>/` with minimum viable tool surface (D-33b).
- **Step 4:** browser-fallback — Phase 11 scope; Phase 10 stubs the enum value and halts cleanly.
- **Verification (D-34):** three checks — Ping / Scope match / Shape probe. All must PASS before `status: verified`.
- **On failure (D-35):** write VERIFICATION-FAILED.md, block Phase 3 gate, name the specific gap in the conversation. No silent degradation (INTEG-05).
- **Evidence (D-39 extending INTG-03):** URL + version + last_commit + publisher + trust_tier + tools_declared + required_scopes + healthcheck_at. Missing any = `[UNVERIFIED]`.
- **Default on ambiguity:** `ecosystem` (Step 2). Ecosystem lookup is cheaper than wrapper generation.
- **Rule:** MCP-first (PROJECT.md Constraints). Every external integration goes through an MCP server; official-API direct calls are a fallback in [phase-3-integration.md](phase-3-integration.md), not the primary path.
- **Cross-reference:** Downstream consumers of `.agentbloc/integrations/integration-manifest.yaml` are the Phase 12 Deploy Pipeline (renders the manifest into `.mcp.json` merges + ClaudeClaw job configs) and the Phase 16 TAP end-to-end tests (replays the Arco Rooms fixture). Both read from the same schema defined in [integration-manifest-schema.md](integration-manifest-schema.md).
```

**Formatting rules for the whole file:**
- All cross-links use shorthand relative paths (`phase-3-integration.md`, `mcp-ecosystem-registry.md`, `integration-manifest-schema.md`, `credentials.md`, `prompt-injection.md`) because this file sits in the references/ directory.
- Keep the file between 140 and 240 lines.
- Do NOT use em-dashes `—` anywhere (project CLAUDE.md rule). Use ` - `, `:`, or recast the sentence.
- The 4 steps use consistent "**Action:** / **Input:** / **If found:** / **If not found:**" grammar.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "^# MCP Integration Protocol$" .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "## Table of Contents" .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "## When This Applies" .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "## Step 1" .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "## Step 2" .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "## Step 3" .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "## Step 4" .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "## Verification Loop" .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "## Halt-and-Name Protocol" .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "## Evidence Protocol" .claude/skills/agentbloc/references/mcp-integration-protocol.md &amp;&amp; grep -q "## Quick Reference" .claude/skills/agentbloc/references/mcp-integration-protocol.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/references/mcp-integration-protocol.md` exits 0
    - `grep -q "^# MCP Integration Protocol$" .claude/skills/agentbloc/references/mcp-integration-protocol.md` matches (exact H1)
    - All 9 planned H2 section headers present: Table of Contents + When This Applies + Step 1 + Step 2 + Step 3 + Step 4 + Verification Loop + Halt-and-Name Protocol + Evidence Protocol + Quick Reference (separate greps, all must match)
    - `grep -c "^## " .claude/skills/agentbloc/references/mcp-integration-protocol.md` returns >= 9
    - `grep -q "resolution_method" .claude/skills/agentbloc/references/mcp-integration-protocol.md` matches (cites the schema enum)
    - All 5 resolution_method enum values present: `grep -q "existing" ...`, `grep -q "ecosystem" ...`, `grep -q "wrapper" ...`, `grep -q "browser-fallback" ...`, `grep -q "failed" ...`
    - D-34 three checks named: `grep -q "Ping" ...`, `grep -q "Scope match" ...`, `grep -q "Shape probe" ...`
    - All 3 trust tiers present: `grep -q "HIGH" ...`, `grep -q "MEDIUM" ...`, `grep -q "LOW" ...`
    - D-37 approval-gated install rule present: `grep -q "npx -y" ...` AND `grep -q "Claude does NOT" .claude/skills/agentbloc/references/mcp-integration-protocol.md`
    - D-35 VERIFICATION-FAILED.md named: `grep -q "VERIFICATION-FAILED\.md" .claude/skills/agentbloc/references/mcp-integration-protocol.md`
    - D-38 .env.example auto-append rule present: `grep -q "\.env\.example" .claude/skills/agentbloc/references/mcp-integration-protocol.md`
    - Cross-link to companion registry: `grep -q "mcp-ecosystem-registry\.md" .claude/skills/agentbloc/references/mcp-integration-protocol.md`
    - Cross-link to companion schema: `grep -q "integration-manifest-schema\.md" .claude/skills/agentbloc/references/mcp-integration-protocol.md`
    - Cross-link to credentials.md: `grep -q "credentials\.md" .claude/skills/agentbloc/references/mcp-integration-protocol.md`
    - Cross-link to phase-3-integration.md: `grep -q "phase-3-integration\.md" .claude/skills/agentbloc/references/mcp-integration-protocol.md`
    - INTEG-05 no-silent-degradation rule: `grep -q "silent" .claude/skills/agentbloc/references/mcp-integration-protocol.md` OR `grep -q "halt" .claude/skills/agentbloc/references/mcp-integration-protocol.md`
    - INTEG-06 UNVERIFIED carry-forward: `grep -q "UNVERIFIED" .claude/skills/agentbloc/references/mcp-integration-protocol.md`
    - `wc -l .claude/skills/agentbloc/references/mcp-integration-protocol.md` returns between 140 and 260 lines
    - No em-dashes: `grep -c "—" .claude/skills/agentbloc/references/mcp-integration-protocol.md` returns 0
  </acceptance_criteria>
  <done>
mcp-integration-protocol.md exists at 140-260 lines with H1 + blockquote + TOC, When-This-Applies opener, four ordered Step H2 sections (Step 1 Existing .mcp.json, Step 2 Ecosystem Registry with D-37 approval gate + npx discipline, Step 3 Wrapper via mcp-builder with D-33/D-33b least-privilege, Step 4 Browser Fallback stubbed as Phase 11), Verification Loop section with the D-34 three prose checks (Ping / Scope match / Shape probe), Halt-and-Name Protocol referencing VERIFICATION-FAILED.md + D-38 .env.example auto-append, Evidence Protocol extending v1.0 INTG-03 with four D-39 MCP-specific fields, and Quick Reference footer. Cross-links to phase-3-integration.md / mcp-ecosystem-registry.md / integration-manifest-schema.md / credentials.md all use shorthand paths. Zero em-dashes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create references/mcp-ecosystem-registry.md with curated MCP registry seeded from CLAUDE.md + trust tier criteria</name>
  <files>.claude/skills/agentbloc/references/mcp-ecosystem-registry.md</files>
  <read_first>
    - .planning/phases/10-integration-discovery-mcp-path/10-PATTERNS.md (lines 103-185 — the "mcp-ecosystem-registry.md" section with Patterns A (file-opening), B (curated table), C (best-practice + example), D (trust-tier policy), E (Quick Reference closer))
    - .planning/phases/10-integration-discovery-mcp-path/10-CONTEXT.md (D-31 registry rationale; Claude's Discretion "Registry curation depth — seed from CLAUDE.md ecosystem table ~20 entries")
    - /Users/pablodelarco/agentbloc/CLAUDE.md (the "MCP Server Ecosystem: Verified Available" tables starting at "## Communication & Reporting" — seed the registry from these ~7 subsections; Telegram, Slack, Google Workspace, E-Commerce, CRM, Accounting, Browser Automation, Development, Workflow Automation categories)
    - .claude/skills/agentbloc/references/frameworks.md (the full 126-line structural twin — copy the skeleton verbatim: H1 + blockquote + TOC + When This Applies + per-category H2 sections with 3-column tables + Quick Reference at the bottom)
    - .claude/skills/agentbloc/references/phase-3-integration.md (lines 155-170 — Trust Tier Definitions; copy the HIGH/MEDIUM/LOW definitions verbatim for the Trust Tier Criteria section)
    - .planning/REQUIREMENTS.md (INTEG-02 — verify the curated registry is referenced by Step 2 ecosystem lookup)
  </read_first>
  <action>
Create a NEW file at `.claude/skills/agentbloc/references/mcp-ecosystem-registry.md` (~140-220 lines) with the following structure. Use GitHub-flavored Markdown. Structural twin of frameworks.md — copy the H1 + blockquote + TOC + "When This Applies" + per-category H2 + Quick Reference spine.

**Section 1 — H1 + blockquote + TOC** (mirrors frameworks.md lines 1-14):

```markdown
# MCP Ecosystem Registry

> Loaded by SKILL.md at Phase 3 entry alongside [mcp-integration-protocol.md](mcp-integration-protocol.md) and [integration-manifest-schema.md](integration-manifest-schema.md). Curated registry of known MCP servers seeded from CLAUDE.md Technology Stack "MCP Server Ecosystem: Verified Available" tables. Consulted by Step 2 of the 4-step search — if the user's tool has an entry here, Claude proposes `npx -y <package>` to the user for approval per D-37. Trust tiers pre-computed per v1.0 INTG-04.

## Table of Contents

- [When This Applies](#when-this-applies)
- [Communication and Reporting](#communication-and-reporting)
- [Google Workspace](#google-workspace)
- [E-Commerce and Payments](#e-commerce-and-payments)
- [CRM and Business Tools](#crm-and-business-tools)
- [Accounting and Finance](#accounting-and-finance)
- [Browser Automation](#browser-automation)
- [Development and Infrastructure](#development-and-infrastructure)
- [Workflow Automation (Meta)](#workflow-automation-meta)
- [Trust Tier Criteria](#trust-tier-criteria)
- [Quick Reference](#quick-reference)
```

**Section 2 — `## When This Applies`**: one paragraph per frameworks.md When-This-Applies shape. Exact emission:

```markdown
## When This Applies

Claude loads this file at Phase 3 entry and consults a single category section based on the agent's tool name during Step 2 of the 4-step search (see [mcp-integration-protocol.md](mcp-integration-protocol.md) Step 2). Specific lookups:

- **Tool name matches an entry:** Claude proposes `npx -y <package>` plus the entry's trust tier and evidence to the user, waits for approval per D-37, then writes the MCP into `.mcp.json` and proceeds to the Verification Loop.
- **Tool name does not match any entry:** Claude proceeds to Step 3 (wrapper generation via mcp-builder skill).
- **Entry trust_tier is LOW:** Claude surfaces the trust tier prominently in the proposal and names the alternative (e.g., "if you prefer a higher-trust option, I can generate a wrapper instead").

Additions to this registry land via user-facing PRs or dogfooding feedback, not as a v2.0 concern. The seed list is ~20 entries across 8 categories — enough to cover the Arco Rooms canonical test case and the most common SMB automation targets.
```

**Section 3-10 — Category H2 sections** (8 category sections, each with an intro paragraph + 6-column markdown table + optional example paragraph). Use the 6-column header across all tables:

```markdown
| tool_id | package | publisher | trust_tier | last_commit | required_scopes |
```

**Section 3 — `## Communication and Reporting`** (seed from CLAUDE.md lines 35-40 "Communication & Reporting"):

```markdown
## Communication and Reporting

MCP servers for messaging, alerts, and reporting channels. Telegram is AgentBloc's primary reporting channel per PROJECT.md.

| tool_id | package | publisher | trust_tier | last_commit | required_scopes |
|---------|---------|-----------|------------|-------------|-----------------|
| telegram-mcp | telegram-mcp (guangxiangdebizi) | guangxiangdebizi | MEDIUM | 2026-01 | TELEGRAM_BOT_TOKEN |
| telegram-mtproto-mcp | mcp-telegram (sparfenyuk) | sparfenyuk | MEDIUM | 2026-02 | TELEGRAM_API_ID + TELEGRAM_API_HASH |
| slack-mcp | slack-mcp-plugin | slackapi | HIGH | 2026-03 | SLACK_BOT_TOKEN |
| slack-community-mcp | slack-mcp-server (korotovsky) | korotovsky | MEDIUM | 2026-02 | SLACK_USER_TOKEN |

**When to pick:** Telegram for mobile-first threading + voice approval; Slack for team-native deployments. Both have native MCP.
```

**Section 4 — `## Google Workspace`** (seed from CLAUDE.md lines 43-48):

```markdown
## Google Workspace

All-in-one Google Workspace MCP covers Gmail / Calendar / Docs / Sheets / Slides / Chat / Forms / Tasks / Drive. Scoped MCPs for single services exist too.

| tool_id | package | publisher | trust_tier | last_commit | required_scopes |
|---------|---------|-----------|------------|-------------|-----------------|
| google-workspace-mcp | google_workspace_mcp (taylorwilsdon) | taylorwilsdon | MEDIUM | 2026-03 | GOOGLE_OAUTH_TOKEN (scopes: gmail.readonly, calendar.readonly, drive.file) |
| google-sheets-mcp | mcp-google-sheets (xing5) | xing5 | MEDIUM | 2026-01 | GOOGLE_OAUTH_TOKEN (scope: spreadsheets) |
| gmail-mcp | @smithery-ai/gmail-mcp | smithery-ai | MEDIUM | 2026-02 | GOOGLE_OAUTH_TOKEN (scope: gmail.readonly OR gmail.modify) |

**When to pick:** google-workspace-mcp (taylorwilsdon) for multi-service access; scope-specific MCPs for least-privilege setups.
```

**Section 5 — `## E-Commerce and Payments`** (seed from CLAUDE.md lines 51-54):

```markdown
## E-Commerce and Payments

| tool_id | package | publisher | trust_tier | last_commit | required_scopes |
|---------|---------|-----------|------------|-------------|-----------------|
| shopify-mcp | shopify-mcp (community) | community | MEDIUM | 2026-02 | SHOPIFY_ADMIN_API_TOKEN |
| stripe-mcp | stripe-mcp (community) | community | MEDIUM | 2026-03 | STRIPE_SECRET_KEY |

**When to pick:** Standard payment and commerce integrations.
```

**Section 6 — `## CRM and Business Tools`** (seed from CLAUDE.md lines 57-61):

```markdown
## CRM and Business Tools

| tool_id | package | publisher | trust_tier | last_commit | required_scopes |
|---------|---------|-----------|------------|-------------|-----------------|
| hubspot-mcp | hubspot-mcp (community) | community | MEDIUM | 2026-02 | HUBSPOT_ACCESS_TOKEN |
| salesforce-mcp | salesforce-mcp (community) | community | LOW | 2025-12 | SALESFORCE_OAUTH_TOKEN |
| notion-mcp | notion-mcp (community) | community | MEDIUM | 2026-03 | NOTION_API_TOKEN |

**When to pick:** Notion for knowledge management; HubSpot / Salesforce for CRM flows.
```

**Section 7 — `## Accounting and Finance`** (seed from CLAUDE.md lines 64-68):

```markdown
## Accounting and Finance

| tool_id | package | publisher | trust_tier | last_commit | required_scopes |
|---------|---------|-----------|------------|-------------|-----------------|
| xero-mcp | xero-mcp-server | XeroAPI | HIGH | 2026-03 | XERO_CLIENT_ID + XERO_CLIENT_SECRET (scopes: accounting.transactions, accounting.contacts) |
| bank-mcp | bank-mcp (elcukro) | elcukro | MEDIUM | 2026-02 | PLAID_CLIENT_ID OR TELLER_APPLICATION_TOKEN (read-only) |

**When to pick:** Xero for SMB accounting; bank-mcp for multi-provider bank access (Plaid US, Teller US, Enable Banking EU, Tink EU). PSD2 (EU) and Plaid (US) are the two primary read-only banking paths.
```

**Section 8 — `## Browser Automation`** (seed from CLAUDE.md lines 71-74):

```markdown
## Browser Automation

Playwright MCP is the standard fallback when no native MCP exists. Used by Phase 11 browser-fallback flow.

| tool_id | package | publisher | trust_tier | last_commit | required_scopes |
|---------|---------|-----------|------------|-------------|-----------------|
| playwright-mcp | @playwright/mcp | microsoft | HIGH | 2026-04 | (none — runs locally) |
| playwright-community-mcp | @executeautomation/mcp-playwright | executeautomation | MEDIUM | 2026-03 | (none) |

**When to pick:** Microsoft Playwright MCP for accessibility-snapshot-based automation (25+ tools, no vision model required).
```

**Section 9 — `## Development and Infrastructure`** (seed from CLAUDE.md lines 77-81):

```markdown
## Development and Infrastructure

Official reference MCP servers from `modelcontextprotocol/servers` repo.

| tool_id | package | publisher | trust_tier | last_commit | required_scopes |
|---------|---------|-----------|------------|-------------|-----------------|
| filesystem-mcp | @modelcontextprotocol/server-filesystem | modelcontextprotocol | HIGH | 2026-03 | (local path scoping) |
| git-mcp | @modelcontextprotocol/server-git | modelcontextprotocol | HIGH | 2026-03 | (repository path) |
| memory-mcp | @modelcontextprotocol/server-memory | modelcontextprotocol | HIGH | 2026-02 | (none) |

**When to pick:** Official reference servers — safe defaults for file ops, git operations, persistent agent memory.
```

**Section 10 — `## Workflow Automation (Meta)`** (seed from CLAUDE.md lines 84-86):

```markdown
## Workflow Automation (Meta)

Meta-connectors. Use only when a direct MCP does not exist for the target service.

| tool_id | package | publisher | trust_tier | last_commit | required_scopes |
|---------|---------|-----------|------------|-------------|-----------------|
| zapier-mcp | zapier-mcp (Zapier) | Zapier | MEDIUM | 2026-03 | ZAPIER_API_KEY |

**When to pick:** Last resort meta-connector — adds cost, latency, and a dependency layer; prefer a direct MCP for the target service when one exists.
```

**Section 11 — `## Trust Tier Criteria`** (copies from phase-3-integration.md lines 159-165 verbatim; structural twin of frameworks.md "Pattern Application by Tech Level" section):

```markdown
## Trust Tier Criteria

Trust tiers are pre-computed per v1.0 INTG-04 and recorded in each registry entry. Re-evaluated on every Phase 10 re-verification (D-39 `healthcheck_at` updates the last-seen evidence).

| Tier | Criteria | When to Pick |
|------|----------|--------------|
| **HIGH** | Official vendor-maintained (Anthropic, Microsoft, Google, Slack, Xero, Stripe, etc.), OR community project with >500 GitHub stars AND active maintenance (commit within 90 days) | Auto-pass proposal; user approval is default-yes |
| **MEDIUM** | Community-maintained project with 100-500 GitHub stars, commit within 180 days, clear documentation (README with examples) | Propose with evidence; user approval required |
| **LOW** | Project with <100 GitHub stars, OR >180 days since last commit, OR unclear/anonymous maintainer, OR no documentation | Surface warning; suggest wrapper generation (Step 3) as alternative; user must explicitly accept risk |

**Drift:** An MCP that was HIGH at registry-seed time might drop to MEDIUM or LOW. Phase 6 Evolution (v1.0 EVOL-02 inherited) re-verifies on a weekly cadence; the `healthcheck_at` + `trust_tier` fields in the manifest carry the drift signal forward.
```

**Section 12 — `## Quick Reference`** (mirrors frameworks.md Quick Reference shape):

```markdown
## Quick Reference

- **Communication:** Telegram (primary, MEDIUM) / Slack (HIGH official).
- **Google Workspace:** google-workspace-mcp (all-in-one MEDIUM) OR scope-specific MCPs for least-privilege.
- **E-Commerce / Payments:** Shopify (MEDIUM) / Stripe (MEDIUM).
- **CRM:** Notion (MEDIUM) / HubSpot (MEDIUM) / Salesforce (LOW — re-verify before use).
- **Accounting / Finance:** Xero (HIGH) / bank-mcp (MEDIUM — multi-provider).
- **Browser:** Playwright MCP (HIGH Microsoft-maintained).
- **Development:** Filesystem / Git / Memory — all official reference servers (HIGH).
- **Meta:** Zapier (MEDIUM) — last resort only.
- **Default on ambiguity:** if no entry matches the tool name, fall through to Step 3 (wrapper generation) per [mcp-integration-protocol.md](mcp-integration-protocol.md).
- **Rule:** Registry entries are seed data for Step 2 of the 4-step search. Every entry's trust tier is load-bearing — downstream consumers (Phase 12 Deploy Pipeline) refuse LOW-tier entries without user override.
```

**Formatting rules for the whole file:**
- All cross-links use shorthand relative paths (`mcp-integration-protocol.md`, `integration-manifest-schema.md`, `phase-3-integration.md`).
- Every category renders as a markdown table with the same 6-column header — consistent lookup shape for Claude.
- Keep the file between 140 and 240 lines.
- Do NOT use em-dashes `—` anywhere (project CLAUDE.md rule). Use ` - `, `:`, or recast the sentence.
- Trust tier values ALWAYS appear as literal `HIGH` / `MEDIUM` / `LOW` (uppercase, no backticks) in tables — matches v1.0 INTG-04 vocabulary.
- Dates in registry rows use ISO-8601 short form `YYYY-MM`; `healthcheck_at` in actual manifest uses full ISO-8601 with Z.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/mcp-ecosystem-registry.md &amp;&amp; grep -q "^# MCP Ecosystem Registry$" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md &amp;&amp; grep -q "## Table of Contents" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md &amp;&amp; grep -q "## When This Applies" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md &amp;&amp; grep -q "## Communication" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md &amp;&amp; grep -q "## Google Workspace" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md &amp;&amp; grep -q "## Trust Tier Criteria" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md &amp;&amp; grep -q "## Quick Reference" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/references/mcp-ecosystem-registry.md` exits 0
    - `grep -q "^# MCP Ecosystem Registry$" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md` matches
    - `grep -c "^## " .claude/skills/agentbloc/references/mcp-ecosystem-registry.md` returns >= 11 (TOC + 8 category + Trust Tier Criteria + Quick Reference)
    - All 3 trust tiers present: `grep -q "HIGH" ...`, `grep -q "MEDIUM" ...`, `grep -q "LOW" ...` in registry entries
    - At least 15 tool entries: `grep -Ec "^\| [a-z-]+-mcp " .claude/skills/agentbloc/references/mcp-ecosystem-registry.md` returns >= 12 (tool rows in tables)
    - Telegram entry present: `grep -q "telegram-mcp" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md`
    - Playwright entry present: `grep -q "playwright-mcp" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md`
    - Xero entry present: `grep -q "xero-mcp" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md`
    - bank-mcp entry present: `grep -q "bank-mcp" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md`
    - google-workspace-mcp entry present: `grep -q "google-workspace-mcp" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md`
    - gmail-mcp entry present: `grep -q "gmail-mcp" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md`
    - Trust Tier Criteria section has HIGH/MEDIUM/LOW definitions with stars threshold: `grep -q "500 GitHub stars" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md`
    - Cross-link to mcp-integration-protocol.md: `grep -q "mcp-integration-protocol\.md" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md`
    - Cross-link to integration-manifest-schema.md: `grep -q "integration-manifest-schema\.md" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md`
    - 6-column table header present: `grep -q "tool_id | package | publisher | trust_tier" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md`
    - `wc -l .claude/skills/agentbloc/references/mcp-ecosystem-registry.md` returns between 140 and 260 lines
    - No em-dashes: `grep -c "—" .claude/skills/agentbloc/references/mcp-ecosystem-registry.md` returns 0
  </acceptance_criteria>
  <done>
mcp-ecosystem-registry.md exists at 140-260 lines with H1 + blockquote + TOC, When-This-Applies opener, 8 category H2 sections (Communication and Reporting, Google Workspace, E-Commerce and Payments, CRM and Business Tools, Accounting and Finance, Browser Automation, Development and Infrastructure, Workflow Automation Meta) each with a 6-column markdown table (tool_id / package / publisher / trust_tier / last_commit / required_scopes) seeded from CLAUDE.md Technology Stack, at least 15 total tool entries across all categories, Trust Tier Criteria section with HIGH/MEDIUM/LOW definitions carrying v1.0 INTG-04 criteria forward (>500 stars + <90 days = HIGH), and Quick Reference footer. Cross-links to mcp-integration-protocol.md and integration-manifest-schema.md use shorthand paths. Zero em-dashes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create references/integration-manifest-schema.md with D-36 YAML schema + three bounded enums + 8-check validation + emission protocol</name>
  <files>.claude/skills/agentbloc/references/integration-manifest-schema.md</files>
  <read_first>
    - .planning/phases/10-integration-discovery-mcp-path/10-PATTERNS.md (lines 187-346 — the "integration-manifest-schema.md" section with Patterns A (file-opening), B (YAML-with-comments schema block), C (Field Obligation Matrix), D (Bounded Enum), E (Validation Checklist), F (Emission Protocol), G (Re-run), H (Schema Versioning))
    - .planning/phases/10-integration-discovery-mcp-path/10-CONTEXT.md (D-22 three-tier inheritance; D-31 schema reference rationale; D-36 full schema; D-34 three-check verification wiring; D-35 halt-and-name; D-14 silent artifact + rendered table review)
    - .claude/skills/agentbloc/references/agent-profile-schema.md (the 178-line structural twin — copy skeleton verbatim: H1 + blockquote + TOC + When This Applies + Schema Definition fenced YAML + Field Obligation Matrix + Bounded Enum sections + Validation Checklist + Emission Protocol + Re-run Behavior + Schema Versioning Rules)
    - .claude/skills/agentbloc/references/business-graph-schema.md (secondary structural twin — compare Emission Protocol + Re-run Behavior sections for language consistency)
    - .planning/REQUIREMENTS.md (INTEG-04, INTEG-06 — verify the schema covers verification + evidence record)
  </read_first>
  <action>
Create a NEW file at `.claude/skills/agentbloc/references/integration-manifest-schema.md` (~160-240 lines) with EXACTLY the following 11 sections in this order. Use GitHub-flavored Markdown. Structural twin of agent-profile-schema.md — copy the spine verbatim and adapt field-specific content.

**Section 1 — H1 + blockquote + TOC** (mirrors agent-profile-schema.md lines 1-16):

```markdown
# Integration Manifest Schema

> Schema reference loaded unconditionally at Phase 3 entry alongside [phase-3-integration.md](phase-3-integration.md), [mcp-integration-protocol.md](mcp-integration-protocol.md), and [mcp-ecosystem-registry.md](mcp-ecosystem-registry.md). Defines the canonical `integration-manifest.yaml` emitted by Phase 3 Summary gate after the 4-step search and three-check verification loop complete, plus the validation checklist Claude walks before writing the file.

## Table of Contents

- [When This Applies](#when-this-applies)
- [Schema Definition](#schema-definition)
- [Field Obligation Matrix](#field-obligation-matrix)
- [Resolution Method Bounded Enum](#resolution-method-bounded-enum)
- [Trust Tier Bounded Enum](#trust-tier-bounded-enum)
- [Status Bounded Enum](#status-bounded-enum)
- [Validation Checklist](#validation-checklist)
- [Emission Protocol](#emission-protocol)
- [Re-run Behavior](#re-run-behavior)
- [Schema Versioning Rules](#schema-versioning-rules)
```

**Section 2 — `## When This Applies`**: one paragraph adapted from agent-profile-schema.md When-This-Applies. Exact emission:

```markdown
## When This Applies

Claude reads this file during the Phase 3 Summary gate to produce the canonical `integration-manifest.yaml` at `.agentbloc/integrations/integration-manifest.yaml`. The schema defines what MUST, SHOULD, and MAY appear in the YAML. The Validation Checklist below is a deterministic list of pass/fail checks Claude walks before writing the file; failures surface as targeted follow-up questions in the conversation or trigger the Halt-and-Name Protocol per [mcp-integration-protocol.md](mcp-integration-protocol.md). Downstream consumers — Phase 12 Deploy Pipeline (renders manifest entries into `.mcp.json` merges + ClaudeClaw job configs), Phase 6 Evolution v1.0 inheritance (re-verifies on weekly cadence reading `healthcheck_at`), Phase 16 end-to-end TAP tests (replays the Arco Rooms canonical fixture) — all read this artifact. The `resolution_method` and `trust_tier` bounded enums define the execution paths Claude takes during the 4-step search; the `status` enum drives gate behavior per D-35.
```

**Section 3 — `## Schema Definition`**: one fenced ` ```yaml ` block containing the full D-36 schema with inline REQUIRED/RECOMMENDED/OPTIONAL tier comments. Exact emission:

````markdown
## Schema Definition

```yaml
schema_version: 1                              # REQUIRED. Integer. Bumped only on breaking changes.
generated_at: "ISO-8601 timestamp"             # REQUIRED. When first written by Phase 3.
modified_at: "ISO-8601 timestamp"              # RECOMMENDED. Bumped on every re-verification run.

tools:                                         # REQUIRED. Length >= 1.
  - tool_id: "string"                          # REQUIRED. kebab-case, unique within file. Matches an entry in agent-profiles.yaml agents[].tools[].
    resolution_method: "existing | ecosystem | wrapper | browser-fallback | failed"   # REQUIRED. See Resolution Method Bounded Enum.
    mcp_server:                                # REQUIRED.
      package: "string"                        # REQUIRED. npm package (ecosystem), path under .mcp/generated/<tool-id>/ (wrapper), or .mcp.json key (existing).
      version: "string"                        # REQUIRED. Package semver or wrapper commit SHA.
      installed_via: "string | null"           # OPTIONAL. e.g. "npx -y @smithery-ai/gmail-mcp" or "wrapper" or ".mcp.json existing".
    evidence:                                  # REQUIRED.
      url: "string"                            # REQUIRED. GitHub / npm / official docs URL.
      last_commit: "ISO-8601 date | null"      # RECOMMENDED. Per v1.0 INTG-03.
      publisher: "string | null"               # RECOMMENDED. Per v1.0 INTG-03.
      trust_tier: "HIGH | MEDIUM | LOW"        # REQUIRED. See Trust Tier Bounded Enum.
      tools_declared: ["string", ...]          # RECOMMENDED. Result of tools/list probe (D-39).
      required_scopes: ["string", ...]         # RECOMMENDED. Declared credential scopes (D-39).
      healthcheck_at: "ISO-8601 timestamp"     # RECOMMENDED. When D-34 three checks last passed (D-39).
    used_by: ["<agent-id>", ...]               # RECOMMENDED. Must resolve to agents[] in agent-profiles.yaml (Check 7).
    status: "pending | verified | failed"     # REQUIRED. See Status Bounded Enum.
    failure_reason: "string | null"            # OPTIONAL. Populated only when status=failed; names the specific Check that failed.
```
````

**Section 4 — `## Field Obligation Matrix`**: 3-column markdown table per D-22 inheritance. Exact emission:

```markdown
## Field Obligation Matrix

| Tier | Fields | Behavior if missing |
|---|---|---|
| REQUIRED | `schema_version`, `generated_at`, `tools[]` (>=1), per-tool `tool_id` + `resolution_method` + `mcp_server.package` + `mcp_server.version` + `evidence.url` + `evidence.trust_tier` + `status` | Claude refuses to emit. Main session re-prompts through targeted follow-up, or the Halt-and-Name Protocol triggers. |
| RECOMMENDED | `modified_at`, `evidence.last_commit`, `evidence.publisher`, `evidence.tools_declared`, `evidence.required_scopes`, `evidence.healthcheck_at`, `used_by` | Claude emits with warnings. Missing any evidence field flags the entry `[UNVERIFIED]` per v1.0 INTG-06. Phase 12 Deploy Pipeline surfaces the warning in DEPLOY-REPORT.md. |
| OPTIONAL | `mcp_server.installed_via`, `failure_reason` (non-null only when status=failed) | Silent defaults. Phase 12 proceeds without comment. |

Downstream consumers refuse to proceed on an unknown major `schema_version`, the same rule as business-graph-schema.md and agent-profile-schema.md.
```

**Section 5 — `## Resolution Method Bounded Enum`**: 4-column markdown table per D-18 inheritance. Exact emission:

```markdown
## Resolution Method Bounded Enum

The `resolution_method` field per tool is drawn from a fixed set. It drives which execution path Claude took during the 4-step search in [mcp-integration-protocol.md](mcp-integration-protocol.md). Downstream Phase 12 Deploy Pipeline reads this field to decide whether to merge into `.mcp.json` vs copy a wrapper vs skip.

| Enum Value | Definition | Required Sub-fields | Example |
|-----------|-----------|---------------------|---------|
| `existing` | Tool already in `.mcp.json` before Phase 3 ran | `mcp_server.installed_via: ".mcp.json existing"` | `{resolution_method: existing, mcp_server: {installed_via: ".mcp.json existing"}}` |
| `ecosystem` | Tool resolved via mcp-ecosystem-registry.md; user approved `npx -y <package>` install (D-37) | `mcp_server.installed_via: "npx -y <package>"` | `{resolution_method: ecosystem, mcp_server: {installed_via: "npx -y @smithery-ai/gmail-mcp"}}` |
| `wrapper` | Tool resolved via mcp-builder skill generating at `.mcp/generated/<tool-id>/` (D-33) | `mcp_server.package: ".mcp/generated/<tool-id>/"`, `mcp_server.installed_via: "wrapper"` | `{resolution_method: wrapper, mcp_server: {package: ".mcp/generated/bbva-mcp/", installed_via: "wrapper"}}` |
| `browser-fallback` | Phase 11 scope (BROWSER-01..12); reserved enum slot only in Phase 10 | (populated by Phase 11) | `{resolution_method: browser-fallback, status: pending}` |
| `failed` | None of Steps 1-4 resolved; Halt-and-Name Protocol triggered per D-35 | `failure_reason` populated with specific Check number | `{resolution_method: failed, status: failed, failure_reason: "Check 2: scope gmail.modify missing"}` |

Any value outside this enum blocks emission. Phase 10 does NOT populate `browser-fallback` in produced manifests; that enum value is reserved for Phase 11.
```

**Section 6 — `## Trust Tier Bounded Enum`**: 3-column markdown table cross-referencing mcp-ecosystem-registry.md Trust Tier Criteria. Exact emission:

```markdown
## Trust Tier Bounded Enum

The `evidence.trust_tier` field per tool is drawn from a fixed set. Criteria live in [mcp-ecosystem-registry.md](mcp-ecosystem-registry.md) Trust Tier Criteria per v1.0 INTG-04.

| Enum Value | Criteria | When to Pick |
|-----------|----------|--------------|
| `HIGH` | Official vendor-maintained OR community with >500 stars + commit within 90 days | Auto-pass proposal; default-yes approval |
| `MEDIUM` | Community 100-500 stars, commit within 180 days, clear docs | Propose with evidence; user approval required |
| `LOW` | <100 stars OR >180 days since last commit OR unclear maintainer OR no docs | Surface warning; suggest wrapper alternative; explicit user override required |

Cross-reference: [mcp-ecosystem-registry.md](mcp-ecosystem-registry.md).
```

**Section 7 — `## Status Bounded Enum`**: 3-column markdown table. Exact emission:

```markdown
## Status Bounded Enum

The `status` field per tool is drawn from a fixed set. It drives the Phase 3 gate behavior.

| Enum Value | Definition | Phase 3 gate behavior |
|-----------|-----------|------------------------|
| `pending` | Tool entry created but Verification Loop not yet complete | Gate stays `pending` until all `tools[]` reach `verified` or `failed` |
| `verified` | All three D-34 checks (Ping / Scope match / Shape probe) passed; `healthcheck_at` stamped | Gate can transition to `approved` when every entry is verified |
| `failed` | One or more D-34 checks failed; Halt-and-Name Protocol triggered | Gate `blocked`; user must resolve (add scope, add credential, or edit agent tool list) before re-verification |

Cross-reference: [mcp-integration-protocol.md](mcp-integration-protocol.md) Verification Loop and Halt-and-Name Protocol.
```

**Section 8 — `## Validation Checklist`**: 8 numbered checks per D-13 inheritance + D-34 three-check wiring. Exact emission:

```markdown
## Validation Checklist

Claude walks this ordered list before writing `.agentbloc/integrations/integration-manifest.yaml`. Any FAIL blocks emission; the targeted follow-up surfaces in the conversation per D-14 rendered-table review pattern. REQUIRED-tier checks (1-7) block emission; RECOMMENDED check (8) emits with warnings.

**Check 1: `schema_version` present and equals current version (`1`)**
- FAIL: Set `schema_version: 1` automatically; no user follow-up needed.

**Check 2: Every `tools[].tool_id` unique, kebab-case, and matches an entry in `.agentbloc/team/agent-profiles.yaml` agents[].tools[]**
- FAIL: If the manifest has a tool not in any agent's tools[], remove it (dead entry). If an agent has a tool not in the manifest, the 4-step search missed it — re-run Step 1 for that tool.

**Check 3: Every `tools[].resolution_method` in {existing, ecosystem, wrapper, browser-fallback, failed} with the required sub-fields populated per the Resolution Method Bounded Enum**
- FAIL: Surface the specific tool and missing sub-field to the main session with a targeted question ("Which install path did tool <tool-id> resolve via?"), block emission.

**Check 4 (D-34 Ping): Every `tools[]` entry with status=verified has `evidence.tools_declared[]` length >= 1 (the MCP server responded to `tools/list` and declared at least one tool)**
- FAIL: Re-run the Verification Loop Ping check on that tool. If Ping fails: set `status: failed`, `failure_reason: "Check 4: server does not respond to tools/list"`, write VERIFICATION-FAILED.md per D-35.

**Check 5 (D-34 Scope match): Every `tools[]` entry with status=verified has (a) `evidence.tools_declared[]` intersecting its `used_by[]` agents' tools[] entries, AND (b) every `evidence.required_scopes[]` entry present in `.env` OR stubbed in `.env.example`**
- FAIL on scope missing: Auto-append the env var name to `.env.example` per D-38 with comment `# required by <used_by agent> for <package> scope <scope>`; halt with the specific missing var named.
- FAIL on tool overlap: Surface the gap; propose switching to Step 3 (wrapper) for the missing tool or reducing the agent's tool list.

**Check 6 (D-34 Shape probe): Every `tools[]` entry with status=verified has been called with a dry-run argument and the response shape matches the used_by agents' `outputs.schema` (when the output schema is defined in agent-profiles.yaml)**
- FAIL: Surface both shapes (declared vs observed) side-by-side in the conversation with specific field mismatches quoted. Block emission until user decides whether to accept the mismatch or re-generate the wrapper.

**Check 7: Every `tools[].used_by[]` agent id resolves to an entry in `.agentbloc/team/agent-profiles.yaml` agents[]**
- FAIL: Reject the YAML; log the unresolved id; re-read `.agentbloc/team/agent-profiles.yaml` for correct mapping. If the agent was renamed via Phase 9 conversational edit (D-26), update the manifest to match.

**Check 8 (WARN, not FAIL): RECOMMENDED fields populated (`evidence.last_commit`, `evidence.publisher`, `evidence.tools_declared`, `evidence.required_scopes`, `evidence.healthcheck_at`, `used_by`, `modified_at`) or explicitly `null`**
- WARN: Emit with `null` defaults; flag any missing field as `[UNVERIFIED]` per v1.0 INTG-06 in the rendered table so user can accept or fix.
```

**Section 9 — `## Emission Protocol`**: 6 numbered steps per business-graph-schema.md Emission Protocol shape, adapted to Phase 3 Summary gate. Exact emission:

```markdown
## Emission Protocol

Emission happens during the Phase 3 Summary gate. The steps:

1. Walk the Validation Checklist above in order.
2. For each REQUIRED failure (Checks 1-7), either apply the targeted remediation (auto-append, re-run Verification Loop) or surface a targeted follow-up to the user and wait for resolution. Do not emit a partial manifest.
3. Once all REQUIRED checks pass, render the integrations to the user as a markdown table + per-tool evidence rows + security summary (D-14 pattern — the rendered table is what the user confirms; the YAML itself is NEVER shown).
4. After user confirmation ("yes" / "adelante" / etc.), write the YAML silently to `.agentbloc/integrations/integration-manifest.yaml`. Create the `.agentbloc/integrations/` directory if it does not exist.
5. Confirm emission in one sentence: "Integration manifest saved. Ready to move to the confirmation and dry-run phase."
6. Set the Phase 3 `mcp_integrations_verified` sub-gate to `approved` and allow transition to Phase 4.

If the user edits any entry during table review, re-run the Validation Checklist on the affected entry and re-emit the manifest. Per D-35, NEVER silently proceed with a partially-verified integration.

**Rendered table shape for user review** (7 columns):

| # | tool_id | resolution_method | package | trust_tier | used_by | status |

Per-tool evidence rows render the four D-39 fields (`tools_declared`, `required_scopes`, `healthcheck_at`, `last_commit`) as sub-bullets beneath the table row, only when non-null.
```

**Section 10 — `## Re-run Behavior`**: 3-option prompt + schema_version mismatch clause. Exact emission:

```markdown
## Re-run Behavior

If `.agentbloc/integrations/integration-manifest.yaml` already exists when the Phase 3 Summary gate is reached, Claude asks the user: "I already have an integration manifest on file for this project. Do you want to (a) keep the existing one, (b) overwrite it, or (c) re-verify existing entries and add any new tools?" Default is **re-verify** (additive per D-36 idempotency rationale).

- **keep**: Skip emission, transition to Phase 4 with the existing manifest. Warn the user that stale `healthcheck_at` timestamps may cause Phase 6 Evolution to flag entries.
- **overwrite**: Replace the file entirely after a fresh 4-step search + Verification Loop pass for every tool in `.agentbloc/team/agent-profiles.yaml`. Warn the user that prior `healthcheck_at` history will be lost.
- **re-verify (default)**: Read the existing manifest; re-run the Verification Loop on every entry; bump `modified_at` to the current ISO-8601 timestamp; add entries for any new tools in agent-profiles.yaml that are not yet in the manifest; flag removed tools from the manifest when no agent uses them anymore. Designer Agent edits (D-26 conversational edits renaming agents) propagate via Check 7 on `used_by[]` resolution.

The `schema_version` on disk must match the current schema version. If it does not, refuse re-verify and emit `action_required: schema_version_mismatch` to the conversation so the user knows a manual migration is needed.
```

**Section 11 — `## Schema Versioning Rules`**: mirrors agent-profile-schema.md Schema Versioning Rules verbatim, adapted paths. Exact emission:

```markdown
## Schema Versioning Rules

The `schema_version` field is an integer. It starts at `1`. The version bumps only on breaking changes:

- A REQUIRED field is removed or renamed.
- An enum value is removed from a bounded type (e.g., dropping `ecosystem` from `resolution_method`).

Additive changes do NOT bump the version:

- Adding a new OPTIONAL field.
- Adding a new value to a bounded enum (e.g., Phase 11 adding `browser-fallback` was planned at schema v1 and does NOT bump).
- Loosening a REQUIRED field to RECOMMENDED.

Downstream consumers (Phase 12 Deploy Pipeline, Phase 6 Evolution, Phase 16 TAP tests) read `schema_version` and refuse to proceed on an unknown major version.
```

**Formatting rules for the whole file:**
- All cross-links use shorthand relative paths (`mcp-integration-protocol.md`, `mcp-ecosystem-registry.md`, `phase-3-integration.md`, `credentials.md`).
- Every bounded enum renders as a markdown table, NEVER as prose list (matches agent-profile-schema.md house style).
- Keep the file between 160 and 260 lines.
- Do NOT use em-dashes `—` anywhere (project CLAUDE.md rule). Use ` - `, `:`, or recast the sentence.
- Do NOT add a "Quick Reference" footer (not needed — TOC + bounded enum tables serve that role, same as agent-profile-schema.md).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "^# Integration Manifest Schema$" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## Table of Contents" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## When This Applies" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## Schema Definition" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## Field Obligation Matrix" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## Resolution Method Bounded Enum" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## Trust Tier Bounded Enum" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## Status Bounded Enum" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## Validation Checklist" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## Emission Protocol" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## Re-run Behavior" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "## Schema Versioning Rules" .claude/skills/agentbloc/references/integration-manifest-schema.md &amp;&amp; grep -q "schema_version" .claude/skills/agentbloc/references/integration-manifest-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/references/integration-manifest-schema.md` exits 0
    - `grep -q "^# Integration Manifest Schema$" .claude/skills/agentbloc/references/integration-manifest-schema.md` matches
    - All 11 planned H2 sections present: `grep -c "^## " .claude/skills/agentbloc/references/integration-manifest-schema.md` returns >= 10
    - `grep -q "schema_version" .claude/skills/agentbloc/references/integration-manifest-schema.md` matches
    - All 3 obligation tiers present: `grep -q "REQUIRED" ...`, `grep -q "RECOMMENDED" ...`, `grep -q "OPTIONAL" ...`
    - All 5 resolution_method enum values present in Resolution Method Bounded Enum: `grep -q "\`existing\`" ...` OR `grep -q "| \`existing\` |" ...`, AND `grep -q "ecosystem" ...`, `grep -q "wrapper" ...`, `grep -q "browser-fallback" ...`, `grep -q "\`failed\`" ...`
    - All 3 trust_tier values present: `grep -q "HIGH" ...`, `grep -q "MEDIUM" ...`, `grep -q "LOW" ...`
    - All 3 status values present: `grep -q "\`pending\`\|pending | " ...`, `grep -q "verified" ...`, `grep -q "\`failed\`\|failed | " ...`
    - `grep -q "\.agentbloc/integrations/integration-manifest\.yaml" .claude/skills/agentbloc/references/integration-manifest-schema.md` - output path locked
    - `grep -q "mcp-integration-protocol\.md" .claude/skills/agentbloc/references/integration-manifest-schema.md` - cross-link to protocol
    - `grep -q "mcp-ecosystem-registry\.md" .claude/skills/agentbloc/references/integration-manifest-schema.md` - cross-link to registry
    - `grep -q "phase-3-integration\.md" .claude/skills/agentbloc/references/integration-manifest-schema.md` - cross-link to v1.0 reference
    - `grep -Eq "Check [0-9]" .claude/skills/agentbloc/references/integration-manifest-schema.md` - numbered validation checks
    - All 8 checks present: `grep -c "^\*\*Check [0-9]" .claude/skills/agentbloc/references/integration-manifest-schema.md` returns >= 8
    - D-34 three checks named in checks 4-6: `grep -q "Ping\|tools/list" ...`, `grep -q "Scope match\|required_scopes" ...`, `grep -q "Shape probe\|response shape" ...`
    - D-39 evidence fields all present: `grep -q "tools_declared" ...`, `grep -q "required_scopes" ...`, `grep -q "healthcheck_at" ...`, `grep -q "installed_via" ...`
    - D-35 halt-and-name cross-link present: `grep -q "VERIFICATION-FAILED\.md\|Halt-and-Name" ...`
    - INTEG-06 UNVERIFIED carry-forward: `grep -q "UNVERIFIED" .claude/skills/agentbloc/references/integration-manifest-schema.md`
    - `wc -l .claude/skills/agentbloc/references/integration-manifest-schema.md` returns between 160 and 280 lines
    - No em-dashes: `grep -c "—" .claude/skills/agentbloc/references/integration-manifest-schema.md` returns 0
  </acceptance_criteria>
  <done>
integration-manifest-schema.md exists at 160-280 lines with all 11 planned H2 sections (Table of Contents + 10 content sections), contains the commented YAML Schema Definition block with D-36 schema, three-tier Field Obligation Matrix (REQUIRED / RECOMMENDED / OPTIONAL), three bounded enum tables (Resolution Method with existing/ecosystem/wrapper/browser-fallback/failed, Trust Tier with HIGH/MEDIUM/LOW, Status with pending/verified/failed), an 8-check Validation Checklist wiring the D-34 three-check verification into Checks 4-6, Emission Protocol with 6 numbered steps targeting `.agentbloc/integrations/integration-manifest.yaml` and the `mcp_integrations_verified` sub-gate, Re-run Behavior with re-verify default, and Schema Versioning Rules. Cross-links to mcp-integration-protocol.md / mcp-ecosystem-registry.md / phase-3-integration.md use shorthand relative paths. All four D-39 evidence fields (tools_declared, required_scopes, healthcheck_at, installed_via) present. Zero em-dashes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create examples/arco-rooms-integration-manifest.yaml as the canonical 8-tool test fixture</name>
  <files>.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml</files>
  <read_first>
    - .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml (the Phase 9 fixture — extract the tools[] lists for each of the 3 agents: gestor-documental has playwright-mcp + google-workspace-mcp + mapfre-api; gestor-cobros has bank-mcp + google-sheets-mcp; recepcionista has telegram-mcp)
    - .claude/skills/agentbloc/references/integration-manifest-schema.md (the schema created in Task 3 - this fixture MUST satisfy every REQUIRED check in the Validation Checklist)
    - .claude/skills/agentbloc/references/mcp-ecosystem-registry.md (the registry created in Task 2 - fixture ecosystem entries must match registry packages verbatim)
    - .planning/phases/10-integration-discovery-mcp-path/10-PATTERNS.md (lines 454-511 — the "arco-rooms-integration-manifest.yaml" section with tool distribution: ~3 existing, ~3 ecosystem, ~2 wrapper; used_by[] resolution)
    - .planning/phases/10-integration-discovery-mcp-path/10-CONTEXT.md (D-36 full schema; D-42 fixture shape; D-30 agent ID lock)
  </read_first>
  <action>
Create a NEW file at `.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml` containing pure YAML (no comments in the fixture itself — comments belong in the schema reference). The fixture MUST cover all three MCP resolution methods per D-42, populate every REQUIRED field, and populate most RECOMMENDED fields so it serves as the canonical "what a good integration manifest looks like" reference for Phase 16.

**Tool distribution per D-42 (8 entries total):**
- ~3 via `resolution_method: existing`: `playwright-mcp` (gstack ships it) + `google-workspace-mcp` (existing entry) + `telegram-mcp` (already added)
- ~3 via `resolution_method: ecosystem`: `gmail-mcp` (@smithery-ai/gmail-mcp) + `google-sheets-mcp` (mcp-google-sheets) + `notion-mcp`
- ~2 via `resolution_method: wrapper`: `bank-mcp` (Bank PSD2 wrapper for BBVA — note: bank-mcp also exists in ecosystem-registry MEDIUM but for this fixture assume the user picked wrapper for BBVA-specific least-privilege surface) + `mapfre-api` (custom insurance API wrapper)
- zero via `browser-fallback` (Phase 11 scope)
- zero via `failed` (happy-path fixture)

**used_by[] mapping per Phase 9 fixture:**
- gestor-documental: playwright-mcp, google-workspace-mcp, mapfre-api
- gestor-cobros: bank-mcp, google-sheets-mcp
- recepcionista: telegram-mcp
- Additional plumbing tools NOT in agent-profiles.yaml tools[] but covered by fixture for completeness: gmail-mcp (used_by gestor-documental — rationale: google-workspace-mcp also covers Gmail but this fixture shows both existing + ecosystem resolution), notion-mcp (used_by recepcionista — rationale: owner reports include notion dashboard link)

**Actually, simplify:** keep used_by[] values strictly within Phase 9 fixture tool names to keep Check 7 passing without requiring re-editing agent-profiles.yaml. Map only the 6 tools from Phase 9 plus 2 extras that are acceptable additions (gmail-mcp scoped to gestor-documental for invoice-email ingestion, notion-mcp scoped to recepcionista for owner-doc storage). This simulates the user adding tools via DSGN-07 conversational edit between Phase 9 and Phase 10.

Emit the following EXACT YAML content:

```yaml
schema_version: 1
generated_at: "2026-04-21T17:42:00Z"
modified_at: "2026-04-21T17:42:00Z"

tools:
  - tool_id: playwright-mcp
    resolution_method: existing
    mcp_server:
      package: "@playwright/mcp"
      version: "0.0.28"
      installed_via: ".mcp.json existing"
    evidence:
      url: "https://github.com/microsoft/playwright-mcp"
      last_commit: "2026-04-02"
      publisher: "microsoft"
      trust_tier: HIGH
      tools_declared:
        - browser_navigate
        - browser_snapshot
        - browser_click
        - browser_type
      required_scopes: []
      healthcheck_at: "2026-04-21T17:42:00Z"
    used_by:
      - gestor-documental
    status: verified
    failure_reason: null

  - tool_id: google-workspace-mcp
    resolution_method: existing
    mcp_server:
      package: "google_workspace_mcp"
      version: "1.4.2"
      installed_via: ".mcp.json existing"
    evidence:
      url: "https://github.com/taylorwilsdon/google_workspace_mcp"
      last_commit: "2026-03-14"
      publisher: "taylorwilsdon"
      trust_tier: MEDIUM
      tools_declared:
        - gmail_list_messages
        - gmail_get_message
        - drive_list_files
        - drive_download_file
      required_scopes:
        - "gmail.readonly"
        - "drive.file"
      healthcheck_at: "2026-04-21T17:42:00Z"
    used_by:
      - gestor-documental
    status: verified
    failure_reason: null

  - tool_id: telegram-mcp
    resolution_method: existing
    mcp_server:
      package: "telegram-mcp"
      version: "0.3.8"
      installed_via: ".mcp.json existing"
    evidence:
      url: "https://github.com/guangxiangdebizi/telegram-mcp"
      last_commit: "2026-01-20"
      publisher: "guangxiangdebizi"
      trust_tier: MEDIUM
      tools_declared:
        - send_message
        - send_voice
        - create_thread
      required_scopes:
        - "TELEGRAM_BOT_TOKEN"
      healthcheck_at: "2026-04-21T17:42:00Z"
    used_by:
      - recepcionista
    status: verified
    failure_reason: null

  - tool_id: gmail-mcp
    resolution_method: ecosystem
    mcp_server:
      package: "@smithery-ai/gmail-mcp"
      version: "0.2.5"
      installed_via: "npx -y @smithery-ai/gmail-mcp"
    evidence:
      url: "https://github.com/smithery-ai/gmail-mcp"
      last_commit: "2026-02-18"
      publisher: "smithery-ai"
      trust_tier: MEDIUM
      tools_declared:
        - list_unread
        - get_message
        - mark_read
      required_scopes:
        - "gmail.readonly"
      healthcheck_at: "2026-04-21T17:42:00Z"
    used_by:
      - gestor-documental
    status: verified
    failure_reason: null

  - tool_id: google-sheets-mcp
    resolution_method: ecosystem
    mcp_server:
      package: "mcp-google-sheets"
      version: "0.4.1"
      installed_via: "npx -y mcp-google-sheets"
    evidence:
      url: "https://github.com/xing5/mcp-google-sheets"
      last_commit: "2026-01-30"
      publisher: "xing5"
      trust_tier: MEDIUM
      tools_declared:
        - read_range
        - write_range
        - append_row
      required_scopes:
        - "spreadsheets"
      healthcheck_at: "2026-04-21T17:42:00Z"
    used_by:
      - gestor-cobros
    status: verified
    failure_reason: null

  - tool_id: notion-mcp
    resolution_method: ecosystem
    mcp_server:
      package: "notion-mcp"
      version: "0.5.3"
      installed_via: "npx -y notion-mcp"
    evidence:
      url: "https://github.com/community/notion-mcp"
      last_commit: "2026-03-05"
      publisher: "community"
      trust_tier: MEDIUM
      tools_declared:
        - create_page
        - update_page
        - search
      required_scopes:
        - "NOTION_API_TOKEN"
      healthcheck_at: "2026-04-21T17:42:00Z"
    used_by:
      - recepcionista
    status: verified
    failure_reason: null

  - tool_id: bank-mcp
    resolution_method: wrapper
    mcp_server:
      package: ".mcp/generated/bank-mcp/"
      version: "commit:a3f2b19"
      installed_via: "wrapper"
    evidence:
      url: "https://www.bbva.es/clientes/particulares/banca-electronica/api-de-datos.html"
      last_commit: "2026-04-20"
      publisher: "agentbloc-wrapper"
      trust_tier: MEDIUM
      tools_declared:
        - list_transactions
        - get_balance
      required_scopes:
        - "BBVA_PSD2_CLIENT_ID"
        - "BBVA_PSD2_CLIENT_SECRET"
      healthcheck_at: "2026-04-21T17:42:00Z"
    used_by:
      - gestor-cobros
    status: verified
    failure_reason: null

  - tool_id: mapfre-api
    resolution_method: wrapper
    mcp_server:
      package: ".mcp/generated/mapfre-api/"
      version: "commit:b1c4d82"
      installed_via: "wrapper"
    evidence:
      url: "https://www.mapfre.es/empresas/oficina-directa/api/"
      last_commit: "2026-04-20"
      publisher: "agentbloc-wrapper"
      trust_tier: MEDIUM
      tools_declared:
        - get_policy
        - list_claims
      required_scopes:
        - "MAPFRE_API_KEY"
      healthcheck_at: "2026-04-21T17:42:00Z"
    used_by:
      - gestor-documental
    status: verified
    failure_reason: null
```

**Formatting requirements:**
- 2-space indentation throughout
- String values with special characters wrapped in double quotes; simple strings unquoted
- Lists use `-` syntax with 2-space continuation indent
- File must parse as valid YAML (run `python3 -c "import yaml; yaml.safe_load(open('...'))"` clean)
- End file with a single trailing newline
- No comments in the fixture (keep schema reference the source of commentary)
- No em-dashes `—` anywhere
- All `used_by[]` values must be one of: `gestor-documental`, `gestor-cobros`, `recepcionista` (the 3 agents from Phase 9 fixture per D-30)

This fixture is the reference Phase 16 end-to-end validation will use — running Phase 3's 4-step search against `arco-rooms-agent-profiles.yaml` tools[] should produce a YAML structurally equivalent to this one (not byte-identical, timestamps differ; but the same 8 entries, same resolution_method distribution, same used_by mapping).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml &amp;&amp; python3 -c "import yaml, sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); assert d['schema_version']==1, 'schema_version'; assert 'generated_at' in d, 'generated_at'; tools=d['tools']; assert len(tools)>=8, f'expected >=8 tools, got {len(tools)}'; methods=[t['resolution_method'] for t in tools]; assert methods.count('existing')>=3, f'need >=3 existing, got {methods.count(chr(39)+chr(101)+chr(120)+chr(105)+chr(115)+chr(116)+chr(105)+chr(110)+chr(103)+chr(39))}'; assert methods.count('ecosystem')>=3, 'need >=3 ecosystem'; assert methods.count('wrapper')>=2, 'need >=2 wrapper'; assert methods.count('browser-fallback')==0, 'browser-fallback is Phase 11'; assert methods.count('failed')==0, 'happy-path fixture'; valid_agents={'gestor-documental','gestor-cobros','recepcionista'}; [[sys.exit('unresolved used_by: '+u) for u in t.get('used_by',[]) if u not in valid_agents] for t in tools]; [sys.exit('missing REQUIRED: '+t['tool_id']) for t in tools if not all(k in t for k in ('tool_id','resolution_method','mcp_server','evidence','status'))]; [sys.exit('missing mcp_server REQUIRED: '+t['tool_id']) for t in tools if not all(k in t['mcp_server'] for k in ('package','version'))]; [sys.exit('missing evidence REQUIRED: '+t['tool_id']) for t in tools if not all(k in t['evidence'] for k in ('url','trust_tier'))]; [sys.exit('trust_tier enum: '+t['tool_id']) for t in tools if t['evidence']['trust_tier'] not in ('HIGH','MEDIUM','LOW')]; [sys.exit('status enum: '+t['tool_id']) for t in tools if t['status'] not in ('pending','verified','failed')]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml'))"` succeeds (valid YAML)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); sys.exit(0 if d['schema_version']==1 else 1)"` exits 0
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); sys.exit(0 if len(d['tools'])>=8 else 1)"` exits 0 (>=8 tools per D-42)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); m=[t['resolution_method'] for t in d['tools']]; sys.exit(0 if m.count('existing')>=3 and m.count('ecosystem')>=3 and m.count('wrapper')>=2 else 1)"` exits 0 (D-42 distribution: >=3 existing, >=3 ecosystem, >=2 wrapper)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); m=[t['resolution_method'] for t in d['tools']]; sys.exit(0 if m.count('browser-fallback')==0 and m.count('failed')==0 else 1)"` exits 0 (no browser-fallback; no failed — happy-path fixture)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); valid={'gestor-documental','gestor-cobros','recepcionista'}; sys.exit(0 if all(u in valid for t in d['tools'] for u in t.get('used_by',[])) else 1)"` exits 0 (Check 7: every used_by[] resolves to Phase 9 agent ids per D-30)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); sys.exit(0 if all(all(k in t for k in ('tool_id','resolution_method','mcp_server','evidence','status')) for t in d['tools']) else 1)"` exits 0 (every tool has REQUIRED top-level fields)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); sys.exit(0 if all(all(k in t['mcp_server'] for k in ('package','version')) for t in d['tools']) else 1)"` exits 0 (every mcp_server has REQUIRED sub-fields)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); sys.exit(0 if all(t['evidence']['trust_tier'] in ('HIGH','MEDIUM','LOW') for t in d['tools']) else 1)"` exits 0 (trust_tier enum satisfied)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); sys.exit(0 if all(t['status'] in ('pending','verified','failed') for t in d['tools']) else 1)"` exits 0 (status enum satisfied)
    - `python3 -c "import yaml,sys; d=yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml')); sys.exit(0 if all(t['resolution_method'] in ('existing','ecosystem','wrapper','browser-fallback','failed') for t in d['tools']) else 1)"` exits 0 (resolution_method enum satisfied)
    - All 3 agent IDs appear in at least one used_by[]: `grep -q "gestor-documental" .claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml` AND `grep -q "gestor-cobros" ...` AND `grep -q "recepcionista" ...`
    - No em-dashes: `grep -c "—" .claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml` returns 0
  </acceptance_criteria>
  <done>
A valid YAML fixture exists at `examples/arco-rooms-integration-manifest.yaml` with schema_version=1, generated_at/modified_at ISO-8601 timestamps, and at least 8 tool entries distributed per D-42: >=3 via existing (playwright-mcp, google-workspace-mcp, telegram-mcp), >=3 via ecosystem (gmail-mcp, google-sheets-mcp, notion-mcp), >=2 via wrapper (bank-mcp for BBVA PSD2, mapfre-api). Every tool populates REQUIRED fields (tool_id / resolution_method / mcp_server.{package, version} / evidence.{url, trust_tier} / status). Every used_by[] resolves to gestor-documental, gestor-cobros, or recepcionista (Phase 9 D-30 scope-lock; Check 7 passes). Zero browser-fallback entries (Phase 11 scope). Zero failed entries (happy-path fixture). All enums satisfied. Zero em-dashes. Python yaml.safe_load parses cleanly.
  </done>
</task>

</tasks>

<verification>
After all four tasks complete, run these phase-level checks:

1. `test -f .claude/skills/agentbloc/references/mcp-integration-protocol.md` — protocol reference exists
2. `test -f .claude/skills/agentbloc/references/mcp-ecosystem-registry.md` — registry reference exists
3. `test -f .claude/skills/agentbloc/references/integration-manifest-schema.md` — schema reference exists
4. `test -f .claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml` — fixture exists
5. `python3 -c "import yaml; yaml.safe_load(open('.claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml'))"` — fixture parses as valid YAML
6. `grep -c "^## " .claude/skills/agentbloc/references/mcp-integration-protocol.md` >= 9 (all H2 sections)
7. `grep -c "^## " .claude/skills/agentbloc/references/mcp-ecosystem-registry.md` >= 11 (TOC + 8 categories + Trust Tier Criteria + Quick Reference)
8. `grep -c "^## " .claude/skills/agentbloc/references/integration-manifest-schema.md` >= 10 (TOC + 10 content sections)
9. `wc -l .claude/skills/agentbloc/references/mcp-integration-protocol.md` 140-260 lines
10. `wc -l .claude/skills/agentbloc/references/mcp-ecosystem-registry.md` 140-260 lines
11. `wc -l .claude/skills/agentbloc/references/integration-manifest-schema.md` 160-280 lines
12. Every Phase 10 requirement in this plan traced:
   - INTEG-01 (existing .mcp.json path): mcp-integration-protocol.md Step 1 covers it; integration-manifest-schema.md resolution_method enum has `existing` value; fixture has 3+ existing entries
   - INTEG-02 (ecosystem registry path): mcp-integration-protocol.md Step 2 covers it + D-37 approval gate; mcp-ecosystem-registry.md is the lookup table; fixture has 3+ ecosystem entries
   - INTEG-04 (three-check verification): mcp-integration-protocol.md Verification Loop section with Ping/Scope match/Shape probe; integration-manifest-schema.md Validation Checklist Checks 4-6 wire them in
   - INTEG-06 (evidence protocol): mcp-integration-protocol.md Evidence Protocol section extends v1.0 INTG-03 with D-39 fields; integration-manifest-schema.md evidence sub-field is REQUIRED with trust_tier + RECOMMENDED with tools_declared/required_scopes/healthcheck_at/last_commit/publisher
</verification>

<success_criteria>
1. `references/mcp-integration-protocol.md` exists at 140-260 lines with the 4-step search flow, D-34 three-check Verification Loop, D-35 Halt-and-Name Protocol referencing VERIFICATION-FAILED.md, D-38 `.env.example` auto-append rule, D-39 Evidence Protocol, and Quick Reference.
2. `references/mcp-ecosystem-registry.md` exists at 140-260 lines with 8 category tables (Communication/Google Workspace/E-Commerce/CRM/Accounting/Browser/Development/Workflow), at least 15 tool entries seeded from CLAUDE.md, HIGH/MEDIUM/LOW trust tiers across entries, v1.0 INTG-04 Trust Tier Criteria section, and Quick Reference.
3. `references/integration-manifest-schema.md` exists at 160-280 lines with D-36 YAML Schema Definition, three-tier Field Obligation Matrix (REQUIRED/RECOMMENDED/OPTIONAL), three bounded enums (Resolution Method with existing/ecosystem/wrapper/browser-fallback/failed, Trust Tier with HIGH/MEDIUM/LOW, Status with pending/verified/failed), 8-check Validation Checklist wiring D-34 three checks into Checks 4-6, Emission Protocol targeting `.agentbloc/integrations/integration-manifest.yaml` + `mcp_integrations_verified` sub-gate, Re-run Behavior with re-verify default, and Schema Versioning Rules.
4. `examples/arco-rooms-integration-manifest.yaml` exists, parses as valid YAML, has >=8 tool entries distributed per D-42 (>=3 existing, >=3 ecosystem, >=2 wrapper, zero browser-fallback, zero failed), every used_by[] resolves to gestor-documental/gestor-cobros/recepcionista (Check 7 passes).
5. All cross-links between the three new files + existing companions (phase-3-integration.md, credentials.md, agent-profile-schema.md, business-graph-schema.md) use shorthand relative paths.
6. Every file contains zero em-dashes (project CLAUDE.md convention).
7. Downstream Plan 10-02 (mcp-builder skill) has concrete field contracts to generate against; Plan 10-03 (phase-3-integration.md + SKILL.md wiring) has the `mcp_integrations_verified` sub-gate vocabulary + output path + companion references to cite.
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-discovery-mcp-path/10-01-SUMMARY.md` documenting:
- The final schema shape (copy the fenced yaml block from integration-manifest-schema.md)
- The 3 bounded enum tables (Resolution Method / Trust Tier / Status)
- The 8-check validation checklist (list from integration-manifest-schema.md)
- Final line counts of all three reference files + fixture
- The exact 8+ tool IDs in the fixture with resolution_method distribution
- Any deviations from the plan with rationale
- Handoff note for Plan 10-02: "mcp-builder skill generates wrappers at `.mcp/generated/<tool-id>/` with TypeScript + @modelcontextprotocol/sdk + Bun per D-33. The generated `README.md` must document tools_declared and required_scopes so Phase 3 Verification Loop can populate them. Wrapper output shape is codified by Resolution Method Bounded Enum `wrapper` row in integration-manifest-schema.md."
- Handoff note for Plan 10-03: "SKILL.md Phase 3 load list must reference all three new files (mcp-integration-protocol.md, mcp-ecosystem-registry.md, integration-manifest-schema.md). The `mcp_integrations_verified` sub-gate maps to the Validation Checklist in integration-manifest-schema.md (checks 1-7 block emission, check 8 warns). The Phase 4 precondition verifies `.agentbloc/integrations/integration-manifest.yaml` exists and every entry has `status: verified` with `healthcheck_at`. phase-3-integration.md Priority 1 MCP section delegates to mcp-integration-protocol.md via See-line."
</output>
</content>
</invoke>