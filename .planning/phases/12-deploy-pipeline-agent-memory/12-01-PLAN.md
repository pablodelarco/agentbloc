---
phase: 12-deploy-pipeline-agent-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/agentbloc/references/deploy-protocol.md
  - .claude/skills/agentbloc/references/deployed-agent-skill-schema.md
  - .claude/skills/agentbloc/references/agent-memory-schema.md
  - .claude/skills/agentbloc/references/deploy-report-schema.md
  - .claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl
  - .claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl
  - .claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl
  - .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md
  - .claude/skills/agentbloc/examples/arco-rooms-registry.yaml
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-04
  - DEPLOY-05
  - DEPLOY-06
  - DEPLOY-07
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04

plan_header_override_notice: |
  PHASE 12 TRIPLE LITERAL OVERRIDE (user-approved 2026-04-24, see 12-CONTEXT.md D-59a/b/c and 12-DISCUSSION-LOG.md Output Directory Convention). REQUIREMENTS.md is internally inconsistent: DEPLOY-01 literal writes `skills/{agent-id}/SKILL.md` (project root), MEM-01 literal writes `.claude/agents/{agent-id}/{memory,state,last-run}`, DEPLOY-05 literal writes `.claude/agents/registry.yaml`. All three literals are OVERRIDDEN on architectural grounds:

  - D-59a overrides DEPLOY-01: SKILL.md ships at `.claude/skills/<agent-id>/SKILL.md` (Claude Code native skill discovery path; deployed agents become peer skills to agentbloc and mcp-builder; runtime-agnostic; OpenClaw/ClaudeClaw compatible per native convention).
  - D-59b overrides MEM-01: memory files ship at `.agentbloc/agents/<agent-id>/{memory.md, state.json, last-run.json}` (namespace hygiene; `.claude/agents/` reserved for Claude Code native subagent definitions only).
  - D-59c overrides DEPLOY-05: registry at `.agentbloc/agents/registry.yaml` (co-location with the state it indexes).

  The unifying architectural principle is the stable-vs-mutable split: SKILL.md contracts are versioned, reviewed, audited (belong in `.claude/skills/`); memory + state files are machine-written on every wake (belong in `.agentbloc/`, customer-mutable namespace per Phase 11 precedent). Phase 16 audit readers must see this justification.

must_haves:
  truths:
    - "references/deploy-protocol.md exists as a loadable reference with H1 + blockquote + TOC + When This Applies + ASCII Flow Diagram (box-drawing chars only, zero em-dashes) + Step 1 Input Validation + Step 2 Fingerprint Compare + Step 3 Diff Presentation + Step 4 Template Render + Step 5 Write + Step 6 Registry Update + Step 7 Mcp-json Merge + Step 8 Post-Deploy Verification + Halt Protocol + Quick Reference (13 sections total, structural twin of mcp-integration-protocol.md + browser-fallback.md)"
    - "references/deployed-agent-skill-schema.md exists as a loadable reference with TOC + When This Applies + Template Anchor Point Table (frozen allow-list per D-62: agent.id, agent.role, agent.goal, agent.backstory, agent.tools, agent.autonomy_language, agent.escalation, agent.dependencies, agent.blast_radius, agent.model, agent.memory_refs, team.name, team.briefing_agent_id; NO credential-bearing fields per threat model) + Field Obligation Matrix (REQUIRED / RECOMMENDED / OPTIONAL) + Bounded Enums (autonomy_level: full|semi|supervised, model: opus|sonnet|haiku, blast_radius: 1-4) + Validation Checklist (8+ prose checks) + Emission Protocol + Re-run Behavior + Schema Versioning Rules"
    - "references/agent-memory-schema.md exists as a loadable reference with TOC + When This Applies + memory.md Template (4 H2 sections: Domain Knowledge, Decisions, Integration Quirks, Open Items) + state.json Schema (schema_version, agent_id, last_wake_at, working_state{}, processed_ids[], locks[], retries[]) + last-run.json Schema (action, result, timestamp, status: active|idle|error) + RFC 8785 Canonicalization Rules (D-60 for JSON) + Validation Checklist + Re-run Semantics"
    - "references/deploy-report-schema.md exists as a loadable reference with TOC + When This Applies + DEPLOY-REPORT.md Schema (frontmatter: schema_version, deployment_id, deployed_at (masked to TIMESTAMP for fingerprint), team_name, agent_count, verification_status, sha256) + Body Sections (Created, Updated, Skipped, Pending User Actions, Post-Deploy Verification) + DEPLOY-FAILED-REPORT.md Schema (halt-and-name twin of DISCOVERY-BLOCKED-REPORT.md per D-70) + Fingerprint Field Rules (D-60 SHA256 + timestamp-masking + RFC 8785 for JSON sections) + Bounded Enums (verification_status: PASSED|PARTIAL|FAILED; halt_reason: template-load-failure|yaml-parse-error|disk-full|permission-denied|verification-failed|user-rejected)"
    - "templates/deployed-agent-skill-full.md.tmpl, templates/deployed-agent-skill-semi.md.tmpl, templates/deployed-agent-skill-supervised.md.tmpl exist as three per-autonomy-level Jinja-lite templates (D-62 split); each is a pure `{{var}}` substitution template with zero in-template conditional blocks or loops; the only differences between the three files are the autonomy-variant prose sections; all three share identical anchor-point placement so template tests can compare them against the frozen allow-list"
    - "examples/arco-rooms-deploy-report.md exists as a canonical fixture demonstrating a successful deploy of the Arco Rooms agent team (gestor-documental, recepcionista, gestor-cobros) with Created + Updated + Skipped + Pending sections populated, verification_status: PASSED, schema-conformant YAML frontmatter, zero PII leakage (synthetic test data only). Fixture family links back to arco-rooms-agent-profiles.yaml + arco-rooms-integration-manifest.yaml."
    - "examples/arco-rooms-registry.yaml exists as a canonical fixture matching the registry schema defined in 12-CONTEXT.md D-63 (team.name: arco-rooms; team.lead: gestor-cobros; topology: hierarchy; 3 agents with skill_path .claude/skills/<agent-id>/SKILL.md + memory_dir .agentbloc/agents/<agent-id>/; reporting_hierarchy populated)"
    - "A reader of deploy-protocol.md can invoke the deploy-engine subagent in Phase 5 Step 4 using only the information in this file plus its delegate references (deployed-agent-skill-schema, agent-memory-schema, deploy-report-schema) with no codebase exploration required"
    - "Every machine-written JSON artifact (state.json, last-run.json, DEPLOY_HISTORY.jsonl lines) specifies RFC 8785 JSON Canonicalization before SHA256 fingerprint computation per D-60"
    - "Zero em-dashes (U+2014) across all 9 emitted files; acceptance grep per file returns 0"
  artifacts:
    - path: ".claude/skills/agentbloc/references/deploy-protocol.md"
      provides: "Imperative 8-step protocol for Phase 5 Step 4: input validation (read agent-profiles.yaml + integration-manifest.yaml) + fingerprint compare (D-60) + diff presentation (D-61) + template render (D-62 three-template split) + atomic write + registry update + .mcp.json merge (D-66 keep-existing-with-conflict-warning) + post-deploy verification (D-69 tools/list + retry). Delegates to deployed-agent-skill-schema, agent-memory-schema, deploy-report-schema for contract details. Halt protocol cites DEPLOY-FAILED-REPORT.md per D-70."
      min_lines: 220
      contains: "Step 1"
    - path: ".claude/skills/agentbloc/references/deployed-agent-skill-schema.md"
      provides: "Canonical template anchor-point contract: frozen allow-list of substitution variables + bounded enums (autonomy_level, model, blast_radius) + validation checklist + emission protocol. Derived from Phase 9 agent-profiles.yaml schema. NO credential-bearing fields in the allow-list (threat model: prevents env-var leakage into committed SKILL.md files)."
      min_lines: 180
      contains: "{{agent.role}}"
    - path: ".claude/skills/agentbloc/references/agent-memory-schema.md"
      provides: "Three-file per-agent runtime contract at .agentbloc/agents/<agent-id>/ (D-59b): memory.md section template (domain knowledge, agent-editable markdown) + state.json schema (machine-written working state with schema_version) + last-run.json schema (most recent execution entry). RFC 8785 canonicalization rules for JSON SHA256 fingerprinting per D-60."
      min_lines: 180
      contains: "schema_version"
    - path: ".claude/skills/agentbloc/references/deploy-report-schema.md"
      provides: "Dual artifact contract: DEPLOY-REPORT.md (happy path: Created / Updated / Skipped / Pending User Actions / Post-Deploy Verification sections) + DEPLOY-FAILED-REPORT.md (halt-and-name twin of DISCOVERY-BLOCKED-REPORT.md per D-70, named halt_reason enum, no retry semantics). SHA256 fingerprint protocol citing D-60 + RFC 8785 for JSON sections."
      min_lines: 180
      contains: "verification_status"
    - path: ".claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl"
      provides: "Jinja-lite template for deployed agents with autonomy_level: full. Zero in-template conditionals or loops. Pure {{var}} substitution. Autonomy-variant prose section declares 'full autonomy, no prompt required' language per D-62."
      min_lines: 60
      contains: "{{agent.role}}"
    - path: ".claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl"
      provides: "Jinja-lite template for deployed agents with autonomy_level: semi. Identical skeleton to full template. Autonomy-variant prose section declares 'semi-autonomous, confirm before side-effects' language per D-62."
      min_lines: 60
      contains: "{{agent.role}}"
    - path: ".claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl"
      provides: "Jinja-lite template for deployed agents with autonomy_level: supervised. Identical skeleton to full + semi templates. Autonomy-variant prose section declares 'supervised, propose and wait for approval' language per D-62."
      min_lines: 60
      contains: "{{agent.role}}"
    - path: ".claude/skills/agentbloc/examples/arco-rooms-deploy-report.md"
      provides: "Canonical happy-path deploy report fixture for the Arco Rooms team. Frontmatter schema-conformant per deploy-report-schema.md. Body populated with 3 agents created (gestor-documental + recepcionista + gestor-cobros), registry.yaml created, zero updated, zero skipped, 2 pending user actions (Telegram credentials + Xero OAuth), verification_status: PARTIAL (cron soft-failed because Phase 13 not yet shipped). Synthetic test data only; zero real PII."
      min_lines: 100
      contains: "arco-rooms"
    - path: ".claude/skills/agentbloc/examples/arco-rooms-registry.yaml"
      provides: "Canonical registry fixture matching D-63 schema. team.name: arco-rooms; team.lead: gestor-cobros; topology: hierarchy; 3 agents (gestor-documental + recepcionista + gestor-cobros) with skill_path at .claude/skills/<agent-id>/SKILL.md + memory_dir at .agentbloc/agents/<agent-id>/; reporting_hierarchy populated with gestor-cobros as parent."
      min_lines: 40
      contains: "schema_version"
  key_links:
    - from: ".claude/skills/agentbloc/references/deploy-protocol.md"
      to: ".claude/skills/agentbloc/references/deployed-agent-skill-schema.md"
      provides: "anchor-point contract for Step 4 template render"
    - from: ".claude/skills/agentbloc/references/deploy-protocol.md"
      to: ".claude/skills/agentbloc/references/agent-memory-schema.md"
      provides: "three-file contract for memory directory initialization in Step 5"
    - from: ".claude/skills/agentbloc/references/deploy-protocol.md"
      to: ".claude/skills/agentbloc/references/deploy-report-schema.md"
      provides: "output contract for DEPLOY-REPORT.md emission in Step 8"
    - from: ".claude/skills/agentbloc/references/deploy-protocol.md"
      to: ".claude/skills/agentbloc/references/prompt-injection.md"
      provides: "v1.0 prompt-injection defense layer cross-referenced for deployed agents"
---

<task type="auto">
  <name>Task 1: Create `.claude/skills/agentbloc/references/deploy-protocol.md` , imperative 8-step Deploy Pipeline protocol (structural twin of mcp-integration-protocol.md + browser-fallback.md)</name>
  <files>.claude/skills/agentbloc/references/deploy-protocol.md</files>
  <read_first>
    - .planning/phases/12-deploy-pipeline-agent-memory/12-CONTEXT.md (D-59a/b/c triple-override; D-60 fingerprint with RFC 8785; D-61 unified-diff presentation; D-62 three-template split; D-66 .mcp.json merge semantics; D-69 canonical tools/list verification; D-70 halt-and-name)
    - .planning/phases/12-deploy-pipeline-agent-memory/12-PATTERNS.md (deploy-protocol.md section with twin recipe)
    - .claude/skills/agentbloc/references/mcp-integration-protocol.md (FULL file; structural twin , copy H1 + blockquote + TOC + When This Applies + ASCII Flow Diagram + per-step grammar)
    - .claude/skills/agentbloc/references/browser-fallback.md (FULL file; structural twin , copy 8-step protocol pattern with state.json checkpoint semantics)
    - .planning/REQUIREMENTS.md DEPLOY-01..08 (lines 89-96)
    - CLAUDE.md (zero em-dashes, no AI attribution)
  </read_first>
  <interfaces>
    <consumes>
      - `agent-profiles.yaml` from Phase 9 (input for template substitution)
      - `integration-manifest.yaml` from Phase 10 (input for .mcp.json merge + MCP health-check)
      - `DISCOVERY-REPORT.md` from Phase 11 (optional input if agents consume discovered integrations)
    </consumes>
    <provides>
      - Canonical imperative 8-step protocol callable from Phase 5 Step 4
      - Flow diagram visually matching the step order
      - Halt protocol naming DEPLOY-FAILED-REPORT.md as the terminal artifact (per D-70)
      - References to the four delegate schemas (deployed-agent-skill-schema, agent-memory-schema, deploy-report-schema, prompt-injection)
    </provides>
  </interfaces>
  <action>
Create a NEW file at `.claude/skills/agentbloc/references/deploy-protocol.md` (~220-320 lines) with EXACTLY the following 13 sections in this order. Structural twin of `references/mcp-integration-protocol.md` , copy H1 + blockquote + TOC + When This Applies + ASCII Flow Diagram + per-step grammar (Action/Input/If found/If not found/Arco Rooms example/Rationale) + Halt-and-Name Protocol + Quick Reference spine. CRITICAL: zero em-dashes (U+2014). Use hyphen-minus + space when a dash is needed, or just a hyphen. ASCII diagrams use box-drawing chars only: `┌ ┐ └ ┘ │ ─ ► ▼`.

**H1:** `# Deploy Pipeline Protocol`

**Blockquote (verbatim):** `> When an agent-profiles.yaml (Phase 9) + integration-manifest.yaml (Phase 10) are approved, Phase 5 Step 4 invokes the deploy-engine subagent to materialize the team into a running ClaudeClaw-compatible deployment. This protocol is the imperative contract the subagent follows.`

**TOC:** bullet list of the 13 sections below.

**Section 1 `## When This Applies`:** one paragraph + bullet list (mirror mcp-integration-protocol.md lines 17-25). Prose: "The deploy-engine subagent (`.claude/agents/deploy-engine.md`, Plan 12-02) loads this file in its forked context on invocation. The protocol runs during Phase 5 Step 4 of the AgentBloc flow, after user has approved agent-profiles.yaml (Phase 9) and the integration manifest (Phase 10). Downstream consumers: Phase 13 Multi-Agent Runtime (wakes deployed agents); Phase 14 Monitor (reads registry.yaml); Phase 16 End-to-End Validation (replays the Arco Rooms fixture). Loaded UNCONDITIONALLY at Phase 5 entry per D-58 context-budget discipline; three delegate schemas (deployed-agent-skill-schema.md, agent-memory-schema.md, deploy-report-schema.md) are NOT loaded at Phase 5 entry (subagent-only)."

**Section 2 `## Flow Diagram`:** ASCII box-drawing diagram of the 8 steps in linear sequence with halt arrows to DEPLOY-FAILED-REPORT.md. Use `┌┐└┘│─►▼` only. Example structure:

```
┌─────────────────────────────────────┐
│ INPUT: agent-profiles.yaml          │
│        integration-manifest.yaml    │
└──────────────┬──────────────────────┘
               ▼
    ┌─────────────────────┐
    │ Step 1: Validate    │ ──fail──► DEPLOY-FAILED-REPORT.md
    └──────────┬──────────┘
               ▼
    ┌─────────────────────┐
    │ Step 2: Fingerprint │
    │  (SHA256 + RFC 8785)│
    └──────────┬──────────┘
               ▼   [... continue for Steps 3-8 ...]
```

**Sections 3-10 , `## Step N: <Name>` (one per step):**

Each step follows Phase 10/11 precedent grammar: `**Action:**` + `**Input:**` + `**If found / If success:**` + `**If not found / If failure:**` + `**Arco Rooms example:**` + `**Rationale:**`.

- **Step 1: Input Validation** , read agent-profiles.yaml, integration-manifest.yaml. Verify every agent has required fields per deployed-agent-skill-schema.md anchor allow-list. If any missing, halt with `halt_reason: yaml-parse-error` naming the offending field.
- **Step 2: Fingerprint Compare** , for each artifact the deploy will emit, compute SHA256 over canonicalized body (timestamp-masked + RFC 8785 JCS for JSON). Compare to existing file's fingerprint. Same = skip. Different = proceed to Step 3 diff presentation. Cites D-60.
- **Step 3: Diff Presentation** , for every changed artifact, emit unified diff with 5-line context, save to `.agentbloc/deploy/pending-diffs/<agent-id>-<artifact>.diff`, print to stdout, embed in DEPLOY-REPORT.md `<details>` block. Ask user for approval. Cites D-61 and D-37.
- **Step 4: Template Render** , for each agent, pick `deployed-agent-skill-<autonomy>.md.tmpl` based on agent.autonomy_level field (full/semi/supervised). Pre-compute `{{agent.tools}}` bullet-list from integration-manifest.yaml. Substitute anchor points. NO in-template branching. Cites D-62.
- **Step 5: Atomic Write** , write each rendered artifact to its canonical path. SKILL.md to `.claude/skills/<agent-id>/SKILL.md` per D-59a. memory.md + state.json + last-run.json to `.agentbloc/agents/<agent-id>/` per D-59b. Cites D-59a/b.
- **Step 6: Registry Update** , write `.agentbloc/agents/registry.yaml` per D-63 schema (team shape + agent roster + reporting hierarchy). Cites D-59c and D-63.
- **Step 7: .mcp.json Merge** , merge integration-manifest.yaml's MCP server entries into project `.mcp.json`. Keep existing entries; warn on conflicts; approval-gate any overwrite via D-61 diff flow. Cites D-66.
- **Step 8: Post-Deploy Verification** , invoke `claude agents list` (parse, cross-check all agent-ids present); for each MCP server in manifest, issue `tools/list` JSON-RPC with 5s warm + 10s cold-start + retry=3 policy; invoke `crontab -l` (soft-fail if Phase 13 not yet shipped). Roll up to verification_status: PASSED / PARTIAL / FAILED. Cites D-69.

**Section 11 `## Halt Protocol`:** Any hard-fail (validation failure, template load error, YAML parse error, disk full, permission denied, verification hard-fail, user-rejected diff) emits `.agentbloc/deploy/DEPLOY-FAILED-REPORT.md` with `halt_reason` enum + concrete evidence + resumption advice. NO retry. NO partial-write commits. Cites D-70.

**Section 12 `## Quick Reference`:** bullet list summarizing each step's trigger + halt condition + delegate schema reference.

**Section 13 `## Cross-References`:** explicit links to `deployed-agent-skill-schema.md` (Step 4), `agent-memory-schema.md` (Step 5), `deploy-report-schema.md` (Step 8), `prompt-injection.md` (v1.0 cross-cutting defense), `browser-stack.md` (if Phase 11 Posture B Patchright was used during discovery).
  </action>
  <verify>
    <automated>test $(grep -c "—" .claude/skills/agentbloc/references/deploy-protocol.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/references/deploy-protocol.md) -ge 220 && grep -q "^# Deploy Pipeline Protocol" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## When This Applies" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Flow Diagram" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Step 1: Input Validation" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Step 2: Fingerprint Compare" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Step 3: Diff Presentation" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Step 4: Template Render" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Step 5: Atomic Write" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Step 6: Registry Update" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Step 7: .mcp.json Merge" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Step 8: Post-Deploy Verification" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Halt Protocol" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "^## Quick Reference" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "RFC 8785" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "DEPLOY-FAILED-REPORT.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "tools/list" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "deployed-agent-skill-schema\.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "agent-memory-schema\.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "deploy-report-schema\.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "\.claude/skills/<agent-id>/SKILL\.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "\.agentbloc/agents/<agent-id>/" .claude/skills/agentbloc/references/deploy-protocol.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.claude/skills/agentbloc/references/deploy-protocol.md` exists
    - `grep -c "—" <file>` returns 0 (zero em-dashes)
    - `wc -l <file>` returns at least 220
    - H1 `# Deploy Pipeline Protocol` on first line
    - All 13 sections present in order via grep for each H2 header
    - 8 step headers present (Step 1 Input Validation through Step 8 Post-Deploy Verification)
    - Key strings present: `RFC 8785` (D-60), `DEPLOY-FAILED-REPORT.md` (D-70), `tools/list` (D-69), `.claude/skills/<agent-id>/SKILL.md` (D-59a), `.agentbloc/agents/<agent-id>/` (D-59b)
    - All three delegate schemas cross-referenced: deployed-agent-skill-schema.md, agent-memory-schema.md, deploy-report-schema.md
  </acceptance_criteria>
  <done>
Phase 5 Step 4 has a canonical imperative 8-step protocol. deploy-engine subagent (Plan 12-02) will load this file in its Mandatory Initial Read. Matches mcp-integration-protocol.md + browser-fallback.md structural precedent. Zero em-dashes. DEPLOY-01 + DEPLOY-02 + DEPLOY-06 + DEPLOY-07 + DEPLOY-08 partially covered (full coverage requires delegate schemas in Tasks 2-4).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create `.claude/skills/agentbloc/references/deployed-agent-skill-schema.md` , anchor-point contract for template substitution (twin of agent-profile-schema.md)</name>
  <files>.claude/skills/agentbloc/references/deployed-agent-skill-schema.md</files>
  <read_first>
    - .planning/phases/12-deploy-pipeline-agent-memory/12-CONTEXT.md (D-62 template anchor points + autonomy_level enum; threat model , no credential-bearing fields)
    - .claude/skills/agentbloc/references/agent-profile-schema.md (FULL file; PRIMARY structural twin , copy skeleton: H1 + blockquote + TOC + When This Applies + Schema Definition + Field Obligation Matrix + Bounded Enums + Validation Checklist + Emission Protocol + Re-run Behavior + Schema Versioning Rules)
    - .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml (input contract; anchor points derive from this schema's field set)
    - CLAUDE.md
  </read_first>
  <action>
Create `.claude/skills/agentbloc/references/deployed-agent-skill-schema.md` (~180-280 lines) with these 10 sections:

**H1:** `# Deployed Agent SKILL.md Schema`

**Blockquote:** `> The frozen template anchor-point contract for the Jinja-lite templates at .claude/skills/agentbloc/templates/deployed-agent-skill-<autonomy>.md.tmpl. Phase 12 Step 4 substitutes these anchors verbatim; no new anchors may be added without a schema_version bump.`

**TOC:** bullet list of the 10 sections.

**Section 1 `## When This Applies`:** loaded by deploy-engine subagent on invocation (NOT at Phase 5 entry per D-58); defines the anchor-point allow-list that the three autonomy-level templates reference.

**Section 2 `## Schema Definition`:** YAML-like anchor-point table:

```
{{agent.id}}                  # slug, matches .claude/skills/<agent-id>/ directory
{{agent.role}}                # human-readable role title
{{agent.goal}}                # 1-2 sentence goal statement
{{agent.backstory}}           # 3-5 sentence context
{{agent.tools}}               # PRE-COMPUTED bullet list (deploy-engine renders from integration-manifest.yaml BEFORE substitution)
{{agent.autonomy_language}}   # pre-computed autonomy-variant prose (one of three per autonomy_level)
{{agent.escalation}}          # escalation protocol prose
{{agent.dependencies}}        # dependency-on-other-agents prose (from agent-profiles.yaml)
{{agent.blast_radius}}        # integer 1-4 + semantic label
{{agent.model}}               # opus | sonnet | haiku
{{agent.memory_refs}}         # literal block pointing at .agentbloc/agents/<agent-id>/{memory,state,last-run} + v1.0 SECR-05 kill-switch pre-check prose
{{team.name}}                 # team slug from agent-profiles.yaml
{{team.briefing_agent_id}}    # agent-id of briefing-agent (null if Phase 15 anticipation not yet run)
```

**Section 3 `## Field Obligation Matrix`:** three-tier REQUIRED / RECOMMENDED / OPTIONAL per field. agent.id, agent.role, agent.goal, agent.tools, agent.autonomy_language, agent.memory_refs are REQUIRED; others RECOMMENDED/OPTIONAL per prior-phase precedent.

**Section 4 `## Bounded Enum: autonomy_level`:** `full | semi | supervised` (selects which of the three templates to load).

**Section 5 `## Bounded Enum: model`:** `opus | sonnet | haiku` (matches .planning/PROJECT.md STACK guidance).

**Section 6 `## Bounded Enum: blast_radius`:** `1 | 2 | 3 | 4` mapped to read-only / write-scoped / write-unrestricted / send-external (matches Phase 8 blast_radius taxonomy).

**Section 7 `## Credential-Bearing Fields Exclusion (Threat Model):`** explicit prose: no anchor point may resolve to an env-var value or raw credential. Env-var references live in `.mcp.json` and are loaded at runtime; SKILL.md must only reference them by name (e.g., `uses the TELEGRAM_BOT_TOKEN env var`). Adding `{{agent.api_key}}` or any credential anchor is a schema violation and must be caught in Validation Checklist Check 3.

**Section 8 `## Validation Checklist`:** 8+ prose checks:
1. All REQUIRED fields populated.
2. autonomy_level matches one of the three enum values.
3. No anchor point references env-var values or raw credentials (grep for `api_key`, `secret`, `password`, `token=` in substituted output returns 0).
4. blast_radius value in 1-4.
5. model value matches enum.
6. agent.id matches the parent directory slug at .claude/skills/<agent-id>/.
7. agent.memory_refs literal block cites all three files (memory.md + state.json + last-run.json).
8. agent.tools bullet list was pre-computed (no leftover `{% for tool in agent.tools %}` Jinja loop syntax in output).

**Section 9 `## Emission Protocol`:** deploy-engine reads the template, performs the 13 substitutions per agent, writes the result to `.claude/skills/<agent-id>/SKILL.md` atomically. Appends fingerprint HTML comment per D-60.

**Section 10 `## Re-run Behavior + Schema Versioning`:** schema_version currently 1; bumping requires coordinated template update across all three autonomy-level files. Re-run compares fingerprint; matching = skip, differing = D-61 diff flow.
  </action>
  <verify>
    <automated>test $(grep -c "—" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/references/deployed-agent-skill-schema.md) -ge 180 && grep -q "^# Deployed Agent SKILL.md Schema" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "^## When This Applies" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "^## Schema Definition" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "^## Field Obligation Matrix" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "autonomy_level" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "blast_radius" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "{{agent.role}}" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "{{agent.tools}}" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "{{agent.memory_refs}}" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "Credential-Bearing Fields Exclusion" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "full | semi | supervised" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "opus | sonnet | haiku" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "^## Validation Checklist" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "^## Emission Protocol" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md && grep -q "schema_version" .claude/skills/agentbloc/references/deployed-agent-skill-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "—"` returns 0; `wc -l` >= 180
    - All 10 H2 sections present in order
    - All 13 anchor points present in Schema Definition verbatim
    - Three bounded enums present (autonomy_level, model, blast_radius)
    - Credential-Bearing Fields Exclusion section explicitly forbids api_key / secret / password / token anchors
    - 8+ validation checklist items present (grep for `^[0-9]+\.` pattern returns >=8)
  </acceptance_criteria>
  <done>
deploy-engine subagent has the frozen anchor-point contract for Step 4 template substitution. All three autonomy-level templates (Task 5) must emit these anchors verbatim. Threat model mitigation (no credential leakage) locked in Section 7. DEPLOY-01 substantially covered (paired with Task 5 templates).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create `.claude/skills/agentbloc/references/agent-memory-schema.md` , three-file per-agent runtime contract at `.agentbloc/agents/<agent-id>/` with RFC 8785 canonicalization (D-59b + D-60 + D-64)</name>
  <files>.claude/skills/agentbloc/references/agent-memory-schema.md</files>
  <read_first>
    - .planning/phases/12-deploy-pipeline-agent-memory/12-CONTEXT.md (D-59b path; D-60 RFC 8785 for JSON; D-64 memory.md structure; D-65 state.json schema; D-73 last-run.json shape)
    - .claude/skills/agentbloc/references/discovery-report-schema.md (FULL file; structural twin , schema-defined-with-validation-checklist pattern)
    - .claude/skills/agentbloc/references/integration-manifest-schema.md (FULL file; secondary twin)
    - CLAUDE.md
  </read_first>
  <action>
Create `.claude/skills/agentbloc/references/agent-memory-schema.md` (~180-280 lines) with these 11 sections:

**H1:** `# Agent Memory Schema`

**Blockquote:** `> Canonical contract for .agentbloc/agents/<agent-id>/{memory.md, state.json, last-run.json}. Deploy-engine initializes these three files on every new deploy; runtime (Phase 13) reads and writes them.`

**TOC, Section 1 When This Applies, Section 11 Re-run Semantics + Versioning** per prior-phase schema precedent.

**Section 2 `## memory.md Template (D-64):`** fixed 4-section markdown template with H2 navigation:
```markdown
# Agent Memory: <agent-id>

<!-- agentbloc:schema version=1 -->

## Domain Knowledge
<!-- Long-lived facts about the agent's domain: tenants, contracts, account numbers. Agent-editable markdown. -->

## Decisions
<!-- Append-only log of agent decisions with rationale. Format: `- YYYY-MM-DD: decision + rationale`. -->

## Integration Quirks
<!-- Known gotchas, API workarounds, rate-limit observations. -->

## Open Items
<!-- Unresolved questions for operator review. -->
```

**Section 3 `## state.json Schema (D-65 + D-60 RFC 8785):`**
```json
{
  "schema_version": 1,
  "agent_id": "<agent-id>",
  "last_wake_at": "<ISO-8601 UTC with Z suffix>",
  "working_state": { /* role-specific fields namespaced here */ },
  "processed_ids": [],
  "locks": [],
  "retries": [],
  "_agentbloc_fingerprint": { "sha256": "<64-hex>", "generated_at": "<ISO-8601>" }
}
```

RFC 8785 canonicalization rules: before SHA256 fingerprint, strip `_agentbloc_fingerprint` field, mask `last_wake_at` to `<TIMESTAMP>`, re-serialize per RFC 8785 (sorted keys, UTF-8 no BOM, shortest-number, no insignificant whitespace).

**Section 4 `## last-run.json Schema (D-73):`**
```json
{
  "schema_version": 1,
  "agent_id": "<agent-id>",
  "action": "<action-name>",
  "result": "<result-summary>",
  "timestamp": "<ISO-8601 UTC with Z suffix>",
  "status": "active | idle | error",
  "correlation_id": "<uuid-v4>",
  "_agentbloc_fingerprint": { "sha256": "<64-hex>", "generated_at": "<ISO-8601>" }
}
```

Same RFC 8785 canonicalization rules.

**Section 5 `## Bounded Enum: status`:** `active | idle | error` with semantics.

**Section 6 `## Timezone Discipline`:** all ISO-8601 timestamps MUST carry `Z` UTC suffix (matching Phase 11 `<checkpoint_resume>` discipline); local-time strings are rejected.

**Section 7 `## Validation Checklist`:** 8+ prose checks (schema_version matches 1; all three files exist in .agentbloc/agents/<agent-id>/; state.json + last-run.json parse as valid JSON; timestamps have Z suffix; _agentbloc_fingerprint field present; memory.md has all 4 H2 sections).

**Section 8 `## Initialization Protocol (Deploy Time):`** deploy-engine creates memory.md with H2 skeleton + agent-id placeholder; state.json with schema_version: 1 + empty arrays + null working_state fields; last-run.json with status: idle + timestamp: <deploy-timestamp>.

**Section 9 `## Runtime Protocol (Phase 13 Read/Write Semantics):`** on agent wake, read all three in order (memory.md → state.json → last-run.json); on agent completion, update state.json via RFC 8785 canonicalization + last-run.json with new entry + optionally append to memory.md Decisions section.

**Section 10 `## Cross-Reference:`** links to .planning/phases/11-integration-discovery-browser-fallback/11-CONTEXT.md D-50 state.json pattern (Phase 11 precedent).
  </action>
  <verify>
    <automated>test $(grep -c "—" .claude/skills/agentbloc/references/agent-memory-schema.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/references/agent-memory-schema.md) -ge 180 && grep -q "^# Agent Memory Schema" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "^## memory.md Template" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "^## state.json Schema" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "^## last-run.json Schema" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "schema_version" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "RFC 8785" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "_agentbloc_fingerprint" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "## Domain Knowledge" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "## Decisions" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "## Integration Quirks" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "## Open Items" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "active | idle | error" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "Z UTC suffix" .claude/skills/agentbloc/references/agent-memory-schema.md && grep -q "\.agentbloc/agents/<agent-id>/" .claude/skills/agentbloc/references/agent-memory-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists; em-dashes 0; wc -l >= 180
    - All 11 sections present in order
    - 4 memory.md H2 sections present in template (Domain Knowledge, Decisions, Integration Quirks, Open Items)
    - state.json schema shows schema_version, agent_id, last_wake_at with Z suffix, working_state, _agentbloc_fingerprint
    - RFC 8785 canonicalization rules cited for both JSON schemas
    - status enum `active | idle | error` present
    - .agentbloc/agents/<agent-id>/ path cited (D-59b)
  </acceptance_criteria>
  <done>
Three-file per-agent contract locked. deploy-engine will initialize these on every new agent deploy. Phase 13 runtime will read/write per Runtime Protocol. RFC 8785 canonicalization rule enforces deterministic fingerprinting of JSON artifacts. MEM-01..04 covered.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create `.claude/skills/agentbloc/references/deploy-report-schema.md` , DEPLOY-REPORT.md + DEPLOY-FAILED-REPORT.md dual-artifact contract (D-70 halt-and-name)</name>
  <files>.claude/skills/agentbloc/references/deploy-report-schema.md</files>
  <read_first>
    - .planning/phases/12-deploy-pipeline-agent-memory/12-CONTEXT.md (D-60 fingerprint with RFC 8785; D-70 halt-and-name; D-68 DEPLOY-REPORT.md body sections)
    - .claude/skills/agentbloc/references/discovery-report-schema.md (FULL file; primary structural twin)
    - .claude/skills/agentbloc/references/browser-fallback.md (Halt Protocol section; DISCOVERY-BLOCKED-REPORT.md as twin for DEPLOY-FAILED-REPORT.md)
    - CLAUDE.md
  </read_first>
  <action>
Create `.claude/skills/agentbloc/references/deploy-report-schema.md` (~180-280 lines) with these 12 sections:

**H1:** `# Deploy Report Schema`

**Blockquote:** `> Dual-artifact contract for Phase 12 Step 8 emission. DEPLOY-REPORT.md is the happy-path summary; DEPLOY-FAILED-REPORT.md is the halt-and-name twin (per D-70) emitted instead of DEPLOY-REPORT.md on any hard-fail. Both live at .agentbloc/deploy/.`

**Section 2 When This Applies** per precedent.

**Section 3 `## DEPLOY-REPORT.md Frontmatter Schema`:**
```yaml
---
schema_version: 1
deployment_id: <uuid-v4>
deployed_at: <ISO-8601 UTC with Z suffix>
team_name: <from agent-profiles.yaml>
agent_count: <integer>
verification_status: PASSED | PARTIAL | FAILED
sha256: <64-hex>
---
```

**Section 4 `## DEPLOY-REPORT.md Body Sections`:** five H2 sections:
- `## Created` , table: filepath + sha256 + generation-source-ref (one row per newly emitted artifact).
- `## Updated` , table: filepath + old-sha256 + new-sha256 + link to `.agentbloc/deploy/pending-diffs/<name>.diff` (one row per fingerprint-differed artifact).
- `## Skipped` , table: filepath + sha256 + reason `idempotent-match` (one row per fingerprint-matched artifact).
- `## Pending User Actions` , bullet list: credentials missing per integration, ToS opt-in needed for DISCOVERED-tier entries, `.mcp.json` conflicts awaiting decision. Each entry names the exact file/env-var/decision-point + recommended resolution.
- `## Post-Deploy Verification` , table: check + status (PASS/FAIL/SKIP) + note. Rows: SKILL.md loads + per-MCP-server tools/list response + crontab registered (soft-fail if Phase 13 not shipped).

**Section 5 `## Bounded Enum: verification_status`:** `PASSED` (all checks PASS) | `PARTIAL` (Check 1 PASS, Check 2 or 3 has soft-fail) | `FAILED` (Check 1 FAIL or Check 2 hard-fail on required integration).

**Section 6 `## DEPLOY-FAILED-REPORT.md Schema`:** halt-and-name twin emitted INSTEAD of DEPLOY-REPORT.md on any hard-fail:
```yaml
---
schema_version: 1
deployment_id: <uuid-v4>
halted_at: <ISO-8601 UTC with Z suffix>
halt_reason: template-load-failure | yaml-parse-error | disk-full | permission-denied | verification-failed | user-rejected-diff
halt_step: <step number 1-8>
team_name: <from agent-profiles.yaml>
---
```

Body: `## Failure Details` (concrete evidence: which file, which field, which step, what went wrong) + `## Partial State` (list of files that WERE written before halt , may need manual rollback) + `## Resumption Advice` (what user should fix before re-running).

**Section 7 `## Bounded Enum: halt_reason`:** six values with definitions.

**Section 8 `## Fingerprint Protocol (D-60)`:** both artifacts carry HTML-comment fingerprint `<!-- agentbloc:fingerprint sha256=<64-hex> generated_at=<ISO-8601> -->` at end of body. Markdown body hashed per D-60 timestamp-masking; any JSON tables (Post-Deploy Verification) inside must be RFC 8785 canonicalized before hashing.

**Section 9 `## Validation Checklist`:** 8+ prose checks (schema_version = 1; verification_status matches enum; every referenced diff file exists at pending-diffs/; every Pending User Action names a concrete resolution; halt_reason enum matches on failed reports; NO DEPLOY-REPORT.md AND DEPLOY-FAILED-REPORT.md co-exist for same deployment_id).

**Section 10 `## Emission Protocol`:** deploy-engine emits exactly ONE of the two artifacts at the END of every deploy run. Append to `.agentbloc/deploy/DEPLOY_HISTORY.jsonl` one line per run (D-64 append-only ledger).

**Section 11 `## Re-run Behavior`:** new deployment_id per run; prior reports preserved (append-only); Phase 14 can diff across runs.

**Section 12 `## Cross-References`:** DISCOVERY-BLOCKED-REPORT.md (Phase 11) as halt-and-name twin; deploy-protocol.md Step 8 (emission trigger).
  </action>
  <verify>
    <automated>test $(grep -c "—" .claude/skills/agentbloc/references/deploy-report-schema.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/references/deploy-report-schema.md) -ge 180 && grep -q "^# Deploy Report Schema" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "DEPLOY-REPORT\.md" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "DEPLOY-FAILED-REPORT\.md" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "verification_status" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "PASSED | PARTIAL | FAILED" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "halt_reason" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "template-load-failure" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "user-rejected-diff" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "^## Created" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "^## Updated" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "^## Skipped" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "^## Pending User Actions" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "^## Post-Deploy Verification" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "RFC 8785" .claude/skills/agentbloc/references/deploy-report-schema.md && grep -q "DEPLOY_HISTORY\.jsonl" .claude/skills/agentbloc/references/deploy-report-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists; em-dashes 0; wc -l >= 180
    - All 12 sections present
    - DEPLOY-REPORT.md and DEPLOY-FAILED-REPORT.md both specified with full schemas
    - 5 body sections present in DEPLOY-REPORT.md (Created, Updated, Skipped, Pending User Actions, Post-Deploy Verification)
    - 6-value halt_reason enum present
    - 3-value verification_status enum present
    - RFC 8785 canonicalization cited for JSON sections
    - DEPLOY_HISTORY.jsonl append-only ledger cited
  </acceptance_criteria>
  <done>
Both deploy emission artifacts fully contracted. deploy-engine (Plan 12-02) emits exactly one per run. DEPLOY-07 + D-70 halt-and-name covered.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create three per-autonomy-level Jinja-lite templates at `.claude/skills/agentbloc/templates/deployed-agent-skill-{full,semi,supervised}.md.tmpl` (D-62 three-template split)</name>
  <files>
    - .claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl
    - .claude/skills/agentbloc/templates/deployed-agent-skill-semi.md.tmpl
    - .claude/skills/agentbloc/templates/deployed-agent-skill-supervised.md.tmpl
  </files>
  <read_first>
    - .planning/phases/12-deploy-pipeline-agent-memory/12-CONTEXT.md D-62 three-template split
    - .claude/skills/agentbloc/references/deployed-agent-skill-schema.md (produced in Task 2; anchor-point allow-list)
    - .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml (sample input shape)
    - .claude/agents/designer-agent.md (SKILL.md structural precedent for the emitted output)
    - CLAUDE.md
  </read_first>
  <action>
Create three template files. Each file uses ONLY `{{var}}` substitution , ZERO `{% if %}`, `{% for %}`, `{% else %}`, `{% endfor %}`, `{% endif %}` blocks. Pre-computation of `{{agent.tools}}` and `{{agent.autonomy_language}}` happens in the deploy-engine BEFORE substitution; the template sees already-rendered strings for these.

**All three files share this identical skeleton (differences are ONLY in the autonomy_language default prose , but since that's pre-computed, the three files are in fact identical; the three-file split is a defensive discipline that prevents future editors from introducing conditionals):**

```markdown
---
name: {{agent.id}}
description: {{agent.role}} , {{agent.goal}}
tools:
{{agent.tools}}
model: {{agent.model}}
---

# {{agent.role}}

## Role

{{agent.role}}

## Goal

{{agent.goal}}

## Backstory

{{agent.backstory}}

## Autonomy

{{agent.autonomy_language}}

## Tools

{{agent.tools}}

## Escalation

{{agent.escalation}}

## Dependencies

{{agent.dependencies}}

## Blast Radius

{{agent.blast_radius}}

## Memory

{{agent.memory_refs}}

## Team

Part of team `{{team.name}}`. Briefing agent: `{{team.briefing_agent_id}}`.

<!-- agentbloc:template autonomy=<full|semi|supervised> schema_version=1 -->
<!-- The kill-switch at .agentbloc/KILL_SWITCH is checked on every wake per v1.0 SECR-05. Do not disable this check. -->
```

The `<!-- agentbloc:template autonomy=... -->` comment is the ONLY per-file difference: it names which autonomy file was used. This gives downstream tooling (Phase 14 monitoring, Phase 16 validation) a grep-able signal.

For the full variant: replace `<full|semi|supervised>` with `full`.
For the semi variant: replace with `semi`.
For the supervised variant: replace with `supervised`.

Every template file is ~60-80 lines.
  </action>
  <verify>
    <automated>for f in full semi supervised; do test -f .claude/skills/agentbloc/templates/deployed-agent-skill-$f.md.tmpl; test $(grep -c "—" .claude/skills/agentbloc/templates/deployed-agent-skill-$f.md.tmpl) -eq 0; test $(grep -c "{% if" .claude/skills/agentbloc/templates/deployed-agent-skill-$f.md.tmpl) -eq 0; test $(grep -c "{% for" .claude/skills/agentbloc/templates/deployed-agent-skill-$f.md.tmpl) -eq 0; grep -q "{{agent.role}}" .claude/skills/agentbloc/templates/deployed-agent-skill-$f.md.tmpl; grep -q "{{agent.tools}}" .claude/skills/agentbloc/templates/deployed-agent-skill-$f.md.tmpl; grep -q "autonomy=$f" .claude/skills/agentbloc/templates/deployed-agent-skill-$f.md.tmpl; done</automated>
  </verify>
  <acceptance_criteria>
    - All three .tmpl files exist
    - Each is 60+ lines, 0 em-dashes
    - ZERO Jinja conditional/loop blocks in ANY file (grep for `{% if`, `{% for`, `{% else` all return 0)
    - All 13 anchor points present in all three files
    - `autonomy=<level>` marker in HTML comment differs per file (full / semi / supervised)
    - Diffing the three files shows ONLY the autonomy marker differs (idiomatic structural split per D-62)
  </acceptance_criteria>
  <done>
Three autonomy-level templates ready. deploy-engine Step 4 picks one based on agent.autonomy_level. Zero in-template branching enforced. D-62 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 6: Create canonical fixtures `.claude/skills/agentbloc/examples/arco-rooms-deploy-report.md` + `.claude/skills/agentbloc/examples/arco-rooms-registry.yaml`</name>
  <files>
    - .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md
    - .claude/skills/agentbloc/examples/arco-rooms-registry.yaml
  </files>
  <read_first>
    - .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml (Phase 9 fixture input)
    - .claude/skills/agentbloc/examples/arco-rooms-integration-manifest.yaml (Phase 10 fixture input)
    - .claude/skills/agentbloc/examples/mapfre-discovery-report.md (Phase 11 fixture; structural twin for arco-rooms-deploy-report.md)
    - .claude/skills/agentbloc/references/deploy-report-schema.md (produced Task 4)
    - .planning/phases/12-deploy-pipeline-agent-memory/12-CONTEXT.md D-63 (registry schema)
    - CLAUDE.md
  </read_first>
  <action>
Create both fixtures to demonstrate a successful Arco Rooms team deploy.

**arco-rooms-deploy-report.md (~100-140 lines):** Frontmatter schema-conformant per deploy-report-schema.md with deployment_id UUID-v4, deployed_at ISO-8601 Z, team_name: arco-rooms, agent_count: 3, verification_status: PARTIAL (Check 3 soft-failed because Phase 13 not yet shipped), sha256 valid 64-hex. Body:
- Created: 3 SKILL.md files at `.claude/skills/{gestor-documental,recepcionista,gestor-cobros}/SKILL.md` + 3 memory directories + 1 registry.yaml + .mcp.json delta (3 new entries: Google Workspace, Xero, Telegram Bot).
- Updated: empty (first deploy).
- Skipped: empty.
- Pending User Actions: TELEGRAM_BOT_TOKEN missing from .env; Xero OAuth flow pending; Google Workspace service-account key upload required.
- Post-Deploy Verification: `claude agents list` PASS (3 agent-ids present); tools/list per MCP server: Google Workspace PASS (5s warm), Xero PASS (8s warm), Telegram PASS (6s warm); crontab -l SKIP (Phase 13 not shipped yet, soft-fail with note).

Every endpoint / every credential is synthetic test data only (zero real PII).

**arco-rooms-registry.yaml (~40-60 lines):** Matches D-63 schema:
```yaml
schema_version: 1
team:
  name: arco-rooms
  lead: gestor-cobros
  topology: hierarchy
  briefing_agent_id: null    # Phase 15 anticipation not yet run
  deployed_at: <ISO-8601 Z>
  last_deploy_id: <uuid-v4>
agents:
  - id: gestor-documental
    role: Document Manager
    skill_path: .claude/skills/gestor-documental/SKILL.md
    memory_dir: .agentbloc/agents/gestor-documental/
    autonomy: supervised
    blast_radius: 2
    triggers: [...]
    dependencies: []
  - id: recepcionista
    role: Front Desk Agent
    skill_path: .claude/skills/recepcionista/SKILL.md
    memory_dir: .agentbloc/agents/recepcionista/
    autonomy: semi
    blast_radius: 2
    triggers: [...]
    dependencies: [gestor-documental]
  - id: gestor-cobros
    role: Billing Agent (team lead)
    skill_path: .claude/skills/gestor-cobros/SKILL.md
    memory_dir: .agentbloc/agents/gestor-cobros/
    autonomy: full
    blast_radius: 3
    triggers: [...]
    dependencies: [recepcionista, gestor-documental]
reporting_hierarchy:
  gestor-cobros: [recepcionista, gestor-documental]
dashboard_agent: null
```

Populate `triggers` fields from arco-rooms-agent-profiles.yaml (use field references, not literal values, if agent-profiles.yaml schema differs). Populate `dependencies` from the same source.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md && test -f .claude/skills/agentbloc/examples/arco-rooms-registry.yaml && test $(grep -c "—" .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md) -eq 0 && test $(grep -c "—" .claude/skills/agentbloc/examples/arco-rooms-registry.yaml) -eq 0 && test $(wc -l < .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md) -ge 100 && test $(wc -l < .claude/skills/agentbloc/examples/arco-rooms-registry.yaml) -ge 40 && grep -q "arco-rooms" .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md && grep -q "verification_status: PARTIAL" .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md && grep -q "gestor-documental" .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md && grep -q "recepcionista" .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md && grep -q "gestor-cobros" .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md && grep -q "schema_version: 1" .claude/skills/agentbloc/examples/arco-rooms-registry.yaml && grep -q "topology: hierarchy" .claude/skills/agentbloc/examples/arco-rooms-registry.yaml && grep -q "lead: gestor-cobros" .claude/skills/agentbloc/examples/arco-rooms-registry.yaml && grep -q "\.claude/skills/gestor-documental/SKILL\.md" .claude/skills/agentbloc/examples/arco-rooms-registry.yaml && grep -q "\.agentbloc/agents/gestor-documental/" .claude/skills/agentbloc/examples/arco-rooms-registry.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; em-dashes 0
    - arco-rooms-deploy-report.md >= 100 lines with schema-conformant frontmatter + 5 body sections
    - arco-rooms-registry.yaml >= 40 lines matching D-63 schema
    - All 3 Arco Rooms agent-ids present (gestor-documental, recepcionista, gestor-cobros)
    - skill_path uses .claude/skills/ per D-59a
    - memory_dir uses .agentbloc/agents/ per D-59b
    - reporting_hierarchy populated with gestor-cobros as parent (matches team lead)
    - Zero real PII (synthetic data only)
  </acceptance_criteria>
  <done>
Canonical Arco Rooms fixtures for Phase 16 replay + Phase 14 monitoring demos. Consumer-side end-to-end example of Phase 9 profiles → Phase 10 manifest → Phase 12 deploy report + registry.
  </done>
</task>

<verification>
**Structure and preservation:**
- [ ] All 9 new files created at specified paths
- [ ] Line counts within budgets (protocol >=220, schemas >=180, templates >=60, fixtures >=100 and >=40)
- [ ] Zero em-dashes across all 9 files

**Cross-file integrity:**
- [ ] deploy-protocol.md references deployed-agent-skill-schema.md + agent-memory-schema.md + deploy-report-schema.md
- [ ] All three templates share identical anchor-point set (diff between them only in autonomy= marker)
- [ ] arco-rooms-registry.yaml agent paths match D-59a/b (`.claude/skills/` for SKILL.md, `.agentbloc/agents/` for memory_dir)

**Schema and enum correctness:**
- [ ] autonomy_level enum `full | semi | supervised` in deployed-agent-skill-schema.md matches template file naming
- [ ] halt_reason enum has 6 values in deploy-report-schema.md
- [ ] verification_status enum has 3 values
- [ ] blast_radius 1-4 bounded in deployed-agent-skill-schema.md

**D-59a/b/c triple-override surface check:**
- [ ] Plan header (this file's frontmatter `plan_header_override_notice`) explicitly documents all three overrides with architectural principle each
- [ ] No emitted file uses the rejected REQUIREMENTS-literal paths (no `skills/<id>/SKILL.md` at project root without leading dot, no `.claude/agents/<id>/{memory,state,last-run}`, no `.claude/agents/registry.yaml`)

**Style discipline:**
- [ ] Zero em-dashes (grep -c "—" returns 0 per file)
- [ ] No AI attribution in any emitted content
- [ ] All timestamps in JSON schemas specify Z UTC suffix

**Requirement coverage:**
- [ ] DEPLOY-01 covered by Tasks 1+2+5 (protocol + schema + templates)
- [ ] DEPLOY-02 referenced in deploy-protocol.md Step 8 (crontab verification)
- [ ] DEPLOY-04 covered by Task 3 (agent-memory-schema.md three-file contract)
- [ ] DEPLOY-05 covered by Task 6 (arco-rooms-registry.yaml fixture + schema in CONTEXT D-63)
- [ ] DEPLOY-06 covered by Tasks 1+4 (fingerprint + diff + idempotent skip)
- [ ] DEPLOY-07 covered by Task 4 (deploy-report-schema.md)
- [ ] MEM-01..04 covered by Task 3 (agent-memory-schema.md)
</verification>

<success_criteria>
1. All 9 files created with all acceptance criteria green
2. 6 atomic commits (one per task) + 1 SUMMARY commit on master
3. Zero em-dashes across all emitted files
4. Plan header's `plan_header_override_notice` is propagated verbatim into deploy-protocol.md Section 1 + deployed-agent-skill-schema.md Section 1 + agent-memory-schema.md Section 1 + deploy-report-schema.md Section 1 (each "When This Applies" section cites the triple-override explicitly)
5. Requirements DEPLOY-01 + DEPLOY-04 + DEPLOY-05 + DEPLOY-06 + DEPLOY-07 + MEM-01..04 fully or partially covered (remaining partial coverage comes from Plans 12-02 and 12-03)
6. SUMMARY.md at `.planning/phases/12-deploy-pipeline-agent-memory/12-01-SUMMARY.md` lists all 9 files with line counts + decisions applied (D-59a/b/c, D-60, D-62, D-63, D-64, D-65, D-66, D-68, D-69, D-70, D-73) + acceptance criteria pass/fail per task
</success_criteria>
</content>
</invoke>