---
phase: 12-deploy-pipeline-agent-memory
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - .claude/skills/agentbloc/references/phase-5-deployment.md
  - .claude/skills/agentbloc/SKILL.md
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-07

must_haves:
  truths:
    - "phase-5-deployment.md has a new Priority 1 (ClaudeClaw-native deploy) section citing deploy-protocol.md + deploy-engine subagent + the three-namespace model. v1.0 Priority 2..N content preserved verbatim. v1.0 Summary block preserved verbatim. Line count within budget (current ~N to current ~N+25)."
    - "SKILL.md Phase 5 entry See-line block has 4 new See-lines appended: deploy-protocol.md + deployed-agent-skill-schema.md is NOT present (subagent-only per D-58) , correction: only deploy-protocol.md loads at Phase 5 entry; schemas + templates load ONLY inside deploy-engine forked context. Per D-58 context-budget discipline: Phase 5 See-line gets exactly 1 new ref (deploy-protocol.md) not 4."
    - "SKILL.md Phase 5 entry Precondition block (if any exists per Phase 9+10 precedent) is extended with `deployment_artifacts_emitted` sub-gate that gates Phase 6 Evolution behind successful deploy report emission."
    - "Phase 10 Priority 1 (MCP Server) content in phase-5-deployment.md (if any , check in read_first) + v1.0 Priorities 2..N are preserved byte-for-byte."
    - "SKILL.md existing See-lines for Phase 5 are preserved byte-for-byte; the 1 new See-line is appended after the last existing one in canonical order."
    - "Zero em-dashes in both emitted file edits (grep -c '—' returns 0 for phase-5-deployment.md AND for SKILL.md AFTER the edits)."
    - "D-58 context-budget discipline held: grep for absence of `deployed-agent-skill-schema.md`, `agent-memory-schema.md`, `deploy-report-schema.md`, `deployed-agent-skill-.*\\.md\\.tmpl` in SKILL.md Phase 5 entry returns 0 match for each (these are subagent-only, NOT loaded at Phase 5 entry)."
  artifacts:
    - path: ".claude/skills/agentbloc/references/phase-5-deployment.md"
      provides: "After edit: Priority 1 ClaudeClaw-native deploy section citing deploy-protocol.md Canonical 8-step flow + deploy-engine subagent invocation + the three-namespace model (.claude/skills/ stable contracts, .agentbloc/agents/ mutable state, .claude/agents/ reserved for native subagents). v1.0 Summary block preserved."
      min_lines: 0
      contains: "deploy-protocol.md"
    - path: ".claude/skills/agentbloc/SKILL.md"
      provides: "After edit: Phase 5 entry See-line block appended with deploy-protocol.md (1 new See-line, per D-58 context-budget); Precondition sub-gate extended with deployment_artifacts_emitted."
      min_lines: 178
      contains: "deploy-protocol.md"
  key_links:
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/phase-5-deployment.md"
      provides: "Phase 5 entry load-list loads phase-5-deployment.md unconditionally (existing link)"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/deploy-protocol.md"
      provides: "Phase 5 entry load-list extended to load deploy-protocol.md unconditionally (new link, Plan 12-03 Task 2)"
    - from: ".claude/skills/agentbloc/references/phase-5-deployment.md"
      to: ".claude/agents/deploy-engine.md"
      provides: "Priority 1 section names deploy-engine as the canonical subagent invoked for ClaudeClaw-native deploy (new link, Plan 12-03 Task 1)"
---

<task type="auto">
  <name>Task 1: Surgical edit `.claude/skills/agentbloc/references/phase-5-deployment.md` , insert Priority 1 ClaudeClaw-native deploy section citing deploy-protocol.md + deploy-engine subagent + three-namespace model; preserve v1.0 Priorities 2..N + v1.0 Summary block verbatim</name>
  <files>.claude/skills/agentbloc/references/phase-5-deployment.md</files>
  <read_first>
    - .claude/skills/agentbloc/references/phase-5-deployment.md (FULL current file , read before editing so you know what to preserve; Phase 10 may or may not have already added a Priority 1 stub; check for `[Phase 12 scope]` marker that Phase 10 or earlier phases may have left)
    - .claude/skills/agentbloc/references/deploy-protocol.md (produced Plan 12-01; verify it exists before the edit so Priority 1 citation resolves)
    - .claude/agents/deploy-engine.md (produced Plan 12-02; verify it exists before the edit so the subagent invocation reference resolves)
    - .planning/phases/12-deploy-pipeline-agent-memory/12-CONTEXT.md D-59a/b/c triple-override (citation target for Priority 1 rationale)
    - .planning/phases/10-integration-discovery-mcp-path/10-03-PLAN.md Task 1 (line-for-line analog: phase-3-integration.md Priority 1 MCP promotion with [Phase 11 scope] marker pattern)
    - .planning/phases/11-integration-discovery-browser-fallback/11-04-PLAN.md Task 1 (analog: phase-3-integration.md Priority 3 unmark after forward-stub contract from Phase 10)
    - CLAUDE.md (zero em-dashes, no AI attribution)
  </read_first>
  <interfaces>
    <consumes>
      - Existing phase-5-deployment.md content (v1.0 Summary block + v1.0 Priorities 2..N)
      - Any [Phase 12 scope] marker that earlier phases may have left (similar to how Phase 10 marked Phase 11 stub in phase-3-integration.md)
    </consumes>
    <provides>
      - Priority 1 section naming the ClaudeClaw-native deploy as the canonical Step 4 action, delegating to deploy-protocol.md + deploy-engine subagent
      - Three-namespace-model inline summary (.claude/skills/ + .agentbloc/agents/ + .claude/agents/ with purposes)
      - Preserved v1.0 Priorities 2..N (whatever they are) + Summary block
    </provides>
  </interfaces>
  <action>
**Step 1: Read current phase-5-deployment.md in full.** Note its line count, identify whether a `[Phase 12 scope]` marker exists on any Priority heading (probably not; this is first Phase 12 wiring), identify v1.0 Priorities 2..N + Summary block. Locate the insertion point for the new Priority 1: BEFORE the first existing Priority section (Priority 2 in the v1.0 order) but AFTER any introductory prose or H2 sections above the Priority list.

**Step 2: Insert a new Priority 1 section.** If NO `[Phase 12 scope]` marker exists, simply INSERT a new H3 `### Priority 1: ClaudeClaw-Native Deploy` BEFORE what is currently the first Priority. If a `[Phase 12 scope]` marker exists on any existing Priority heading from earlier phase wiring, UNMARK that marker (delete `[Phase 12 scope]`) and REWRITE the section body per below, same as Phase 11 Plan 11-04 unmarked the Phase 10 forward-stub marker.

**New Priority 1 body (insert verbatim):**

```markdown
### Priority 1: ClaudeClaw-Native Deploy (Canonical 8-Step Flow)

See [references/deploy-protocol.md](deploy-protocol.md) for the canonical Step 4 execution protocol: input validation, SHA256 fingerprint with RFC 8785 JSON canonicalization per D-60, unified-diff presentation for changed artifacts per D-61, template substitution via the three autonomy-level templates per D-62, atomic write to the three-namespace split (`.claude/skills/<agent-id>/SKILL.md` for stable contracts per D-59a, `.agentbloc/agents/<agent-id>/` for mutable memory+state+last-run per D-59b, `.agentbloc/agents/registry.yaml` per D-59c), keep-existing-with-conflict-warning merge into `.mcp.json` per D-66, post-deploy verification via canonical `tools/list` MCP health-check per D-69, and DEPLOY-REPORT.md or DEPLOY-FAILED-REPORT.md terminal emission per D-70.

See [.claude/agents/deploy-engine.md](../../agents/deploy-engine.md) for the subagent definition that orchestrates the protocol. The subagent uses a narrow Bash allow-list (shasum, crontab -l, claude agents list, claude mcp list) per D-67; it has NO WebFetch and does NOT spawn sub-subagents.

**Three-namespace model (verbatim citation of D-59a/b/c triple-override):**
- `.claude/skills/<agent-id>/SKILL.md` is the stable contract: prompts, tool lists, autonomy rules. Versioned, reviewed, audited. Claude Code native skill discovery path; deployed agents are peer skills to agentbloc and mcp-builder.
- `.agentbloc/agents/<agent-id>/{memory.md, state.json, last-run.json}` is customer mutable runtime state: machine-written, accretive, outside Claude Code reserved namespace.
- `.claude/agents/` remains reserved for Claude Code native subagent definitions (designer-agent, browser-discovery, deploy-engine) exclusively; customer-deployed agents NEVER live there.

**Runtime-agnostic:** The three-namespace layout works with any Claude Code-based runtime (plain Claude Code + system cron, OpenClaw/ClaudeClaw per native convention, LangGraph invoking `claude -p`, n8n orchestrators). REQUIREMENTS.md literal `skills/{agent-id}/` (project root, no leading dot) was OVERRIDDEN on architectural grounds: project-root `skills/` is not Claude Code native; `.claude/skills/<agent-id>/` is. See 12-CONTEXT.md D-59a for the full rationale.
```

**Step 3: Preserve everything else byte-for-byte.** v1.0 Priority 2..N content + v1.0 Summary block. Do NOT reorder sections. Do NOT rewrite prose outside the new Priority 1.

**Step 4: Zero em-dashes check.** After edit, `grep -c "—" .claude/skills/agentbloc/references/phase-5-deployment.md` must return 0. If any em-dash was introduced in the new section, replace with hyphen-minus + space, semicolon, or parenthetical rewording. If any em-dash exists OUTSIDE the new section (legacy from v1.0 content), do NOT touch it , this task only edits the new Priority 1 section.

**Step 5: Line-count budget.** Net delta +20 to +30 lines (new Priority 1 section is the 5-paragraph markdown block above). Verify final `wc -l` is within [original, original+30].

**Edit discipline:** Use the Edit tool with old_string + new_string. Do NOT use Write to replace the whole file. Match existing file EXACTLY byte-for-byte at the insertion point; include enough surrounding context (e.g., 2-3 lines before insertion point) to make the Edit unambiguous.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "^### Priority 1: ClaudeClaw-Native Deploy" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "deploy-protocol\.md" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "deploy-engine\.md" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "\.claude/skills/<agent-id>/SKILL\.md" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "\.agentbloc/agents/<agent-id>/" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "RFC 8785" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "DEPLOY-REPORT\.md" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "DEPLOY-FAILED-REPORT\.md" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "Three-namespace model" .claude/skills/agentbloc/references/phase-5-deployment.md && ! grep -q "\[Phase 12 scope\]" .claude/skills/agentbloc/references/phase-5-deployment.md && test $(grep -c "—" .claude/skills/agentbloc/references/phase-5-deployment.md) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - File exists (was edited in place)
    - New `### Priority 1: ClaudeClaw-Native Deploy (Canonical 8-Step Flow)` H3 present
    - Citations present: deploy-protocol.md, deploy-engine.md, .claude/skills/<agent-id>/SKILL.md path (D-59a), .agentbloc/agents/<agent-id>/ path (D-59b)
    - Key decision keywords cited: RFC 8785 (D-60), DEPLOY-REPORT.md + DEPLOY-FAILED-REPORT.md (D-70)
    - Three-namespace-model block present with verbatim paths
    - Any pre-existing `[Phase 12 scope]` marker REMOVED (grep returns no match)
    - Zero em-dashes in the full file after edit
    - v1.0 Summary block preserved (if detectable via grep against keywords from current file , e.g., if current file has "login portal" or similar)
    - Net line delta <= +30
  </acceptance_criteria>
  <done>
phase-5-deployment.md has the canonical Priority 1 ClaudeClaw-native deploy section wired to Plan 12-01 references and Plan 12-02 subagent. Three-namespace model documented inline. D-59a/b/c triple-override surfaced where user-facing readers will see it (this file loads unconditionally at Phase 5 entry per D-58). DEPLOY-01 + DEPLOY-02 + DEPLOY-07 wiring completed at reference layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Surgical edit `.claude/skills/agentbloc/SKILL.md` Phase 5 entry , append 1 new See-line for deploy-protocol.md + add `deployment_artifacts_emitted` sub-gate to State Transitions, per D-58 context-budget discipline</name>
  <files>.claude/skills/agentbloc/SKILL.md</files>
  <read_first>
    - .claude/skills/agentbloc/SKILL.md (FULL current ~178-185 line file; read before editing; Phase 5 entry See-line block location varies , use grep/anchoring to locate precisely)
    - .claude/skills/agentbloc/references/deploy-protocol.md (produced Plan 12-01; target of new See-line)
    - .planning/phases/12-deploy-pipeline-agent-memory/12-CONTEXT.md D-58 (context-budget discipline: only 1 new ref loads at Phase 5 entry; schemas + templates stay subagent-only)
    - .planning/phases/11-integration-discovery-browser-fallback/11-04-PLAN.md Task 2 (line-for-line analog: Phase 3 See-line block extension with connector sentence rewrite)
    - .planning/phases/10-integration-discovery-mcp-path/10-03-PLAN.md Task 2 (analog: SKILL.md mcp_integrations_verified sub-gate addition)
    - CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - Existing SKILL.md Phase 5 entry See-line block + any existing Precondition / State Transitions sub-gates
    </consumes>
    <provides>
      - Phase 5 See-line block extended with exactly 1 new See-line: deploy-protocol.md. Connector sentence rewritten to enumerate the new ref (similar to Phase 11 Plan 11-04 Task 2 pattern)
      - State Transitions: new `deployment_artifacts_emitted` sub-gate gating Phase 6 Evolution (mirror Phase 10's mcp_integrations_verified pattern)
    </provides>
  </interfaces>
  <action>
**Step 1: Read SKILL.md in full.** Note the exact current line count (178-185 expected), locate:
- Phase 5 entry See-line block (probably has a connector sentence like "You MUST read ... before starting this phase:" followed by 1 or more See-lines to phase-5-deployment.md and any Phase-10/11-added refs)
- Precondition block (if any)
- State Transitions section (may have mcp_integrations_verified sub-gate from Phase 10, and Phase-specific bullets)

**Step 2: Extend Phase 5 See-line block per D-58.**

Connector sentence rewrite: from the current form to include deploy-protocol.md in the enumeration. Example (ADAPT to actual current connector text):

OLD (example , actual text may differ slightly):
```
You MUST read the complete deployment protocol before starting this phase:
See [references/phase-5-deployment.md](references/phase-5-deployment.md)
```

NEW:
```
You MUST read the complete deployment protocol AND the canonical deploy 8-step flow before starting this phase:
See [references/phase-5-deployment.md](references/phase-5-deployment.md)
See [references/deploy-protocol.md](references/deploy-protocol.md)
```

Per D-58 context-budget discipline, ONLY deploy-protocol.md is added. Do NOT add:
- deployed-agent-skill-schema.md (subagent-only per D-58)
- agent-memory-schema.md (subagent-only per D-58)
- deploy-report-schema.md (subagent-only per D-58)
- any of the three .tmpl template files (subagent-only per D-58)

The deploy-engine subagent loads those four schemas in its Mandatory Initial Read (Plan 12-02 Section 3). Main session never sees them at Phase 5 entry.

**Step 3: State Transitions `deployment_artifacts_emitted` sub-gate.**

Find the State Transitions section (mirror Phase 10's `mcp_integrations_verified` sub-gate addition per Plan 10-03 Task 2). Append a new Phase 5 → Phase 6 sub-gate:

```
- Phase 5 specific: `deployment_artifacts_emitted` must be true before Phase 6 Evolution entry. Set by deploy-engine subagent after DEPLOY-REPORT.md is written (on success) or explicitly set to false if DEPLOY-FAILED-REPORT.md is emitted instead. Phase 6 entry checks this sub-gate; if false, halt Phase 6 and surface the DEPLOY-FAILED-REPORT.md for user resolution.
```

Exact insertion location depends on SKILL.md current structure. If there are already 3 Phase-specific bullets (from Phase 10), this becomes the 4th. If the State Transitions section does not yet have any Phase-5-specific bullet, this is the first one for Phase 5.

**Step 4: Preservation checks.**

- Existing See-lines for Phase 5 are preserved byte-for-byte.
- Existing connector sentence structure is preserved (only the word-level enumeration is extended).
- Phase 1 / Phase 2 / Phase 3 / Phase 4 / Phase 6 entries are untouched.
- Hard Gates section untouched.
- Quality Checklist untouched.
- Reference Implementation section untouched.
- Line count after edit: between 178 and 195 inclusive. Net delta is +2 lines (1 new See-line + 1 connector-word addition) plus +1-3 lines for the new State Transitions bullet. Target ~181-184 final.

**Step 5: D-58 grep-for-absence checks.**

After editing, verify these subagent-only references are NOT present in SKILL.md:
- `grep -q "deployed-agent-skill-schema\.md" SKILL.md` returns NO match (exit 1)
- `grep -q "agent-memory-schema\.md" SKILL.md` returns NO match
- `grep -q "deploy-report-schema\.md" SKILL.md` returns NO match
- `grep -q "deployed-agent-skill-.*\.md\.tmpl" SKILL.md` returns NO match

If ANY of these greps returns a match, the D-58 context-budget discipline is violated. Abort, remove the offending reference, re-verify.

**Step 6: Zero em-dashes.**

After edit, `grep -c "—" .claude/skills/agentbloc/SKILL.md` returns 0.

**Edit discipline:** Use Edit tool with unique old_string / new_string anchors. Two Edit calls (one for See-line block, one for State Transitions sub-gate) preferred over one combined Edit for safety.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/SKILL.md && grep -q "deploy-protocol\.md" .claude/skills/agentbloc/SKILL.md && grep -q "deployment_artifacts_emitted" .claude/skills/agentbloc/SKILL.md && ! grep -q "deployed-agent-skill-schema\.md" .claude/skills/agentbloc/SKILL.md && ! grep -q "agent-memory-schema\.md" .claude/skills/agentbloc/SKILL.md && ! grep -q "deploy-report-schema\.md" .claude/skills/agentbloc/SKILL.md && ! grep -q "deployed-agent-skill-.*\.md\.tmpl" .claude/skills/agentbloc/SKILL.md && test $(grep -c "—" .claude/skills/agentbloc/SKILL.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/SKILL.md) -ge 178 && test $(wc -l < .claude/skills/agentbloc/SKILL.md) -le 195</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md exists (edited in place)
    - `deploy-protocol.md` See-line present in Phase 5 entry block
    - `deployment_artifacts_emitted` sub-gate appears in State Transitions
    - D-58 context-budget discipline held: NONE of deployed-agent-skill-schema.md, agent-memory-schema.md, deploy-report-schema.md, deployed-agent-skill-*.md.tmpl appear in SKILL.md (grep-for-absence passes for each)
    - Zero em-dashes in SKILL.md after edit
    - Line count 178-195 inclusive
    - Existing Phase 1-4 + Phase 6 entries preserved (detectable via phase-name grep if current SKILL.md has them)
    - Hard Gates section preserved
    - Reference Implementation section preserved
  </acceptance_criteria>
  <done>
Phase 5 entry loads deploy-protocol.md unconditionally. Main session sees the canonical 8-step flow before invoking deploy-engine. Phase 6 Evolution entry gated behind deployment_artifacts_emitted sub-gate. D-58 context-budget held: 4 schemas + 3 templates load ONLY in deploy-engine forked context. Wiring complete. Phase 12 structurally closed after Wave 3 commits.
  </done>
</task>

<verification>
**Post-edit state:**
- [ ] phase-5-deployment.md has new Priority 1 section citing deploy-protocol.md + deploy-engine.md + three-namespace model
- [ ] SKILL.md Phase 5 See-line block gains deploy-protocol.md (1 new ref, per D-58)
- [ ] SKILL.md State Transitions gains `deployment_artifacts_emitted` sub-gate
- [ ] NO new em-dashes introduced in either file

**Preservation:**
- [ ] v1.0 Priorities 2..N in phase-5-deployment.md preserved byte-for-byte
- [ ] v1.0 Summary block in phase-5-deployment.md preserved
- [ ] Phase 1/2/3/4/6 entries in SKILL.md preserved byte-for-byte
- [ ] Existing SKILL.md See-lines (for phase-5-deployment.md and any Phase-10/11 additions) preserved
- [ ] Hard Gates + Quality Checklist + Reference Implementation in SKILL.md preserved

**D-58 context-budget discipline:**
- [ ] `deployed-agent-skill-schema.md` NOT in SKILL.md (subagent-only)
- [ ] `agent-memory-schema.md` NOT in SKILL.md (subagent-only)
- [ ] `deploy-report-schema.md` NOT in SKILL.md (subagent-only)
- [ ] `deployed-agent-skill-*.md.tmpl` NOT in SKILL.md (subagent-only)

**Cross-reference integrity:**
- [ ] phase-5-deployment.md Priority 1 cites deploy-engine.md (file produced Plan 12-02)
- [ ] SKILL.md Phase 5 See-line cites deploy-protocol.md (file produced Plan 12-01)
- [ ] DEPLOY-01 + DEPLOY-02 + DEPLOY-07 have wiring paths visible from Phase 5 entry down through to deploy-engine subagent Mandatory Initial Read

**Style:**
- [ ] Zero em-dashes in both files after edit (grep returns 0 each)
- [ ] No AI attribution / no Co-Authored-By
- [ ] Line counts within budget (phase-5-deployment.md: original +30 max; SKILL.md: 178-195)
</verification>

<success_criteria>
1. phase-5-deployment.md: Priority 1 ClaudeClaw-Native Deploy section inserted or [Phase 12 scope] marker unmarked with full Priority 1 body. Citations: deploy-protocol.md + deploy-engine.md + three-namespace model (D-59a/b/c). v1.0 Priorities 2..N + Summary block preserved. Zero em-dashes. Line count within budget.
2. SKILL.md: Phase 5 See-line block appended with deploy-protocol.md (1 new ref per D-58). Connector sentence rewritten to enumerate. State Transitions gains `deployment_artifacts_emitted` sub-gate. Zero em-dashes. D-58 grep-for-absence passes for all 4 subagent-only references.
3. BROWSER-like wiring contract closed: a Phase 5 user-facing load now sees the canonical 8-step flow + pointer to deploy-engine subagent, and any Phase 6 entry gates behind successful DEPLOY-REPORT.md emission.
4. 2 atomic commits (one per task) + 1 SUMMARY commit on master.
5. SUMMARY.md at `.planning/phases/12-deploy-pipeline-agent-memory/12-03-SUMMARY.md` lists the two surgical edits + preservation checks + decisions applied (D-58 context-budget, D-59a/b/c wiring surface, D-61 diff visible to user at Phase 5 entry level via Priority 1 mention, D-69 verification linkage, D-70 halt artifact linkage).
</success_criteria>
</content>
</invoke>