---
phase: 13-multi-agent-runtime
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .claude/agents/runtime-engine.md
autonomous: true
requirements_addressed:
  - RUNTIME-01
  - RUNTIME-02
  - RUNTIME-04
  - RUNTIME-05
  - RUNTIME-06
  - RUNTIME-07
estimated_tasks: 1

must_haves:
  truths:
    - "`.claude/agents/runtime-engine.md` exists as a Claude Code native subagent definition with YAML frontmatter (name, description, tools list with narrower Bash allow-list than Phase 12 deploy-engine per D-80, color, context: fork) + 6 XML blocks (`<role>`, `<invocation_contract>`, `<inputs>`, `<write_constraint>`, `<coordination_protocol>`, `<emission_targets>`) mirroring the Phase 12 deploy-engine.md + Phase 11 browser-discovery.md 6-block cadence"
    - "Frontmatter tools list enumerates exactly: Read, Grep, Glob, Write, Edit, AND a narrow 5-command Bash allow-list per D-80 (`crontab -` (stdin install), `crontab -l`, `shasum -a 256`, `claude agents list`, `claude mcp list`). NO WebFetch. NO Task (no subagent spawning from within runtime-engine). EXPLICITLY REJECT `crontab -e` (would hang forked subagent on interactive editor)"
    - "Mandatory Initial Read block loads the 3 Plan 13-01 references (n8n-integration.md + runtime-coordination.md + correlation-id.md) + 2 templates' existence check (wake-job-cron/webhook/inter.md.tmpl via Glob, not Read at initial load) + registry.yaml + agent-profiles.yaml + .agentbloc/deploy/crontab.proposed"
    - "`<write_constraint>` XML block enumerates EXACTLY the allowed write paths per D-80: `.agentbloc/agents/<agent-id>/wake-*.md` (materialized wake.md files), `.agentbloc/runtime/**` (crontab.applied + n8n-routes/*.json + RUNTIME-REPORT.md + RUNTIME-FAILED-REPORT.md + RUNTIME_HISTORY.jsonl + TEAM_SESSIONS.jsonl + helpers.sh + inbox/<agent-id>/*.json), `.agentbloc/agents/registry.yaml` (Edit only; additive runtime block per D-78), `.agentbloc/deploy/DEPLOY-REPORT.md` (Edit only; append runtime-wiring section), `.agentbloc/logs/audit.jsonl` (append only)"
    - "`<invocation_contract>` cites D-83 as last step of deploy-protocol.md (invoked after deploy-engine success); cites D-81 runtime_wired sub-gate; cites D-35/D-71 halt-and-name RUNTIME-FAILED-REPORT.md"
    - "`<inputs>` enumerates 3 hard inputs: registry.yaml (from Phase 12 deploy-engine) + agent-profiles.yaml (from Phase 9 Designer) + crontab.proposed (from Phase 12 deploy-engine); HALT if any missing"
    - "`<coordination_protocol>` cites D-76 TeamCreate + writeStateHandoff + single-agent bypass; cites D-78 coordination_preference field; cites D-77 three-point kill-switch enforcement"
    - "`<emission_targets>` enumerates concrete outputs: wake.md per (agent, trigger) tuple via D-73 Option D dispatch; crontab.applied manifest + stdin install via D-80; n8n route .json stubs per webhook trigger (RESEARCH amendment); registry.yaml runtime block additive edit per D-78; RUNTIME-REPORT.md on success OR RUNTIME-FAILED-REPORT.md on halt"
    - "Every RUNTIME-01..07 requirement cited at least once (grep-verifiable; covers RUNTIME-01 cron registration, RUNTIME-02 n8n route emission, RUNTIME-04 coordination, RUNTIME-05 single-agent bypass, RUNTIME-06 correlation seeding, RUNTIME-07 kill-switch route stub + three-point integration)"
    - "Line count 160..210 inclusive (per D-82 budget)"
    - "Zero em-dashes (grep -c '—' returns 0)"
  artifacts:
    - path: ".claude/agents/runtime-engine.md"
      provides: "Claude Code native subagent that materializes wake.md files + installs crontab entries (stdin form) + emits n8n route .json stubs + extends registry.yaml with runtime block + emits RUNTIME-REPORT.md on success OR RUNTIME-FAILED-REPORT.md on halt. SECOND AgentBloc subagent after deploy-engine to use Bash (narrow 5-command allow-list per D-80; even narrower than deploy-engine because runtime-engine mutates crontab system-level state)."
      min_lines: 160
      max_lines: 210
      contains: "<write_constraint>"
  key_links:
    - from: ".claude/agents/runtime-engine.md"
      to: ".claude/skills/agentbloc/references/n8n-integration.md"
      provides: "envelope + route schema for n8n route .json emission"
    - from: ".claude/agents/runtime-engine.md"
      to: ".claude/skills/agentbloc/references/runtime-coordination.md"
      provides: "TeamCreate/SendMessage + writeStateHandoff + crontab stdin install discipline"
    - from: ".claude/agents/runtime-engine.md"
      to: ".claude/skills/agentbloc/references/correlation-id.md"
      provides: "correlation-ID format spec + helpers.sh generator emission"
    - from: ".claude/agents/runtime-engine.md"
      to: ".claude/skills/agentbloc/templates/wake-job-cron.md.tmpl"
      provides: "template for cron-trigger wake.md materialization via D-73 Option D dispatch"
    - from: ".claude/agents/runtime-engine.md"
      to: ".claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl"
      provides: "template for webhook-trigger wake.md materialization"
    - from: ".claude/agents/runtime-engine.md"
      to: ".claude/skills/agentbloc/templates/wake-job-inter.md.tmpl"
      provides: "template for inter-agent wake.md materialization"
    - from: ".claude/agents/runtime-engine.md"
      to: ".agentbloc/agents/registry.yaml"
      provides: "additive runtime block per D-78 (extends Phase 12 output)"
    - from: ".claude/agents/runtime-engine.md"
      to: ".agentbloc/deploy/crontab.proposed"
      provides: "input consumed; diffed vs current crontab -l; stdin-installed on approval"
---

<objective>
Create `.claude/agents/runtime-engine.md` as the Claude Code native subagent that executes Phase 13 runtime wiring. This is the FOURTH AgentBloc subagent (after designer-agent Phase 9, browser-discovery Phase 11, deploy-engine Phase 12) and the SECOND to use Bash (narrower than deploy-engine because it mutates crontab system-level state).

Purpose: runtime-engine is the final step of `deploy-protocol.md` (invoked by deploy-engine after DEPLOY-REPORT.md emission). It reads registry.yaml (Phase 12) + agent-profiles.yaml (Phase 9) + crontab.proposed (Phase 12); materializes one wake.md per (agent, trigger) tuple via D-73 Option D template dispatch; emits n8n route .json stubs per webhook trigger; diffs crontab.proposed vs current `crontab -l` and installs via stdin form (NEVER `crontab -e`); extends registry.yaml with the D-78 runtime block; emits RUNTIME-REPORT.md on success or RUNTIME-FAILED-REPORT.md on halt.

Output: `.claude/agents/runtime-engine.md` (160-210 lines; structural twin of `.claude/agents/deploy-engine.md` + `.claude/agents/browser-discovery.md`). Committed to git. Ready for Plan 13-03 surgical wiring into deploy-protocol.md Step 7 + phase-5-deployment.md Step 7.5 + SKILL.md Phase 5 See-lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-multi-agent-runtime/13-CONTEXT.md
@.planning/phases/13-multi-agent-runtime/13-RESEARCH.md
@.planning/phases/13-multi-agent-runtime/13-VALIDATION.md
@.planning/phases/13-multi-agent-runtime/13-01-PLAN.md
@.claude/agents/deploy-engine.md
@.claude/agents/browser-discovery.md
@.claude/agents/designer-agent.md
@.claude/skills/agentbloc/references/n8n-integration.md
@.claude/skills/agentbloc/references/runtime-coordination.md
@.claude/skills/agentbloc/references/correlation-id.md
@.claude/skills/agentbloc/templates/wake-job-cron.md.tmpl
@.claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl
@.claude/skills/agentbloc/templates/wake-job-inter.md.tmpl

<interfaces>
<!-- Load-bearing contracts runtime-engine.md MUST encode. Extracted from deploy-engine.md + Phase 13 CONTEXT decisions. -->

Locked frontmatter (per D-80):
```yaml
---
name: runtime-engine
description: >
  Materializes Phase 13 runtime artifacts for a deployed agent team: wake.md files
  per trigger path, crontab.applied manifest (stdin-installed via crontab -),
  n8n route .json stubs per webhook trigger, helpers.sh correlation-ID generator,
  and an additive runtime block in registry.yaml per D-78. Invoked by deploy-engine
  as the final step of deploy-protocol.md (after DEPLOY-REPORT.md emission);
  closes the runtime_wired sub-gate. Emits RUNTIME-REPORT.md on success or
  RUNTIME-FAILED-REPORT.md on halt (halt-and-name per D-35/D-71).
tools:
  - Read
  - Grep
  - Glob
  - Write
  - Edit
  - Bash(crontab:*)
  - Bash(shasum:*)
  - Bash(claude agents list)
  - Bash(claude mcp list)
color: blue
context: fork
---
```

Notes on Bash tool scopes:
- `Bash(crontab:*)` covers `crontab -l` (read current) + `crontab -` (stdin install). The subagent prose EXPLICITLY forbids `crontab -e` even though tool scope would permit it.
- `Bash(shasum:*)` covers `shasum -a 256` for fingerprint computation.
- `Bash(claude agents list)` + `Bash(claude mcp list)` are exact-match prefixes (verification read-only).
- NO `Bash(bash -c ...)`, NO `Bash(sh ...)`, NO generic wildcard Bash.

6-block XML structure (ordered, matching deploy-engine.md + browser-discovery.md cadence):
1. `<role>` wraps CRITICAL Mandatory Initial Read + Core Responsibilities bullets
2. `<invocation_contract>` declares when the subagent runs + where it lives in deploy-protocol.md Step 7
3. `<inputs>` enumerates 3 hard inputs with halt-if-missing discipline
4. `<write_constraint>` enumerates allow-listed write paths (per D-80)
5. `<coordination_protocol>` cites D-76 + D-77 + D-78 for multi-agent + kill-switch + coordination_preference
6. `<emission_targets>` enumerates concrete outputs (wake.md count, crontab install, n8n routes, registry additive edit, RUNTIME-REPORT.md)

Allow-listed write paths (per D-80):
- `.agentbloc/agents/<agent-id>/wake-cron.md`
- `.agentbloc/agents/<agent-id>/wake-webhook-<source>-<event-slug>.md`
- `.agentbloc/agents/<agent-id>/wake-inter.md`
- `.agentbloc/agents/<agent-id>/inbox/*.json` (writeStateHandoff fallback inbox, created empty during wiring)
- `.agentbloc/runtime/crontab.applied`
- `.agentbloc/runtime/n8n-routes/*.json`
- `.agentbloc/runtime/helpers.sh`
- `.agentbloc/runtime/RUNTIME-REPORT.md`
- `.agentbloc/runtime/RUNTIME-FAILED-REPORT.md`
- `.agentbloc/runtime/RUNTIME_HISTORY.jsonl` (append-only)
- `.agentbloc/runtime/TEAM_SESSIONS.jsonl` (append-only; initially empty file)
- `.agentbloc/agents/registry.yaml` (Edit only; additive runtime block)
- `.agentbloc/deploy/DEPLOY-REPORT.md` (Edit only; append runtime-wiring section reference)
- `.agentbloc/logs/audit.jsonl` (append only; wire events)

Disallowed paths (explicit):
- `.claude/` (reserved: agents, skills, hooks, commands)
- `.planning/` (reserved: design artifacts)
- `.agentbloc/team/` (reserved: Designer Agent output)
- `.agentbloc/discovery/` (reserved: browser-discovery output)
- `.mcp.json` (Phase 12 scope; runtime-engine does not touch MCP config)
- Any writes outside the above paths

8-step runtime-engine responsibilities (from D-80):
1. Read registry.yaml + agent-profiles.yaml + crontab.proposed
2. Materialize wake.md per agent + trigger via D-73 Option D template dispatch (cron/webhook/inter)
3. Emit n8n route .json stubs per webhook trigger (RESEARCH amendment: .json, not .yaml)
4. Emit helpers.sh with agentbloc-gen-correlation function per D-75
5. Diff crontab.proposed vs current `crontab -l`; present diff; on approval install via stdin form
6. Write crontab.applied manifest with SHA256 fingerprint
7. Extend registry.yaml with runtime block per D-78 (Edit, not Write; additive only)
8. Emit RUNTIME-REPORT.md OR RUNTIME-FAILED-REPORT.md; append RUNTIME_HISTORY.jsonl entry

halt_reason enum (per D-71 extended for Phase 13):
- `missing-input` (registry.yaml / agent-profiles.yaml / crontab.proposed absent)
- `template-load-failure` (wake-job-*.md.tmpl missing or contains {% directive)
- `crontab-install-failure` (stdin install returned non-zero)
- `registry-edit-failure` (runtime block insertion produced invalid YAML)
- `user-rejected-crontab-diff` (D-37 approval gate declined)
- `permission-denied` (write failed EACCES)
- `disk-full` (write failed ENOSPC)

Twin structure (line-by-line):
- deploy-engine.md PRIMARY (section order, frontmatter shape, XML block grammar, Mandatory Initial Read pattern)
- browser-discovery.md SECONDARY (write_constraint enumerated-path pattern, halt-and-name naming)
- designer-agent.md TERTIARY (role/responsibilities cadence)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `.claude/agents/runtime-engine.md` , Claude Code native subagent orchestrating Phase 13 runtime wiring with narrow Bash allow-list per D-80 + 6 XML blocks + explicit `crontab -e` rejection (structural twin of deploy-engine.md)</name>
  <files>.claude/agents/runtime-engine.md</files>
  <read_first>
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md (D-73 wake template dispatch, D-74 envelope schema, D-75 correlation-ID format, D-76 TeamCreate contract, D-77 kill-switch three-point enforcement, D-78 registry runtime block, D-80 Bash allow-list + crontab -e rejection, D-81 runtime_wired sub-gate, D-82 line budget 160-210, D-83 invocation as final step of deploy-protocol.md)
    - .planning/phases/13-multi-agent-runtime/13-RESEARCH.md (refinement: writeStateHandoff PRIMARY for non-interactive; amendment: n8n routes use .json extension)
    - .claude/agents/deploy-engine.md (FULL file; PRIMARY structural twin; copy frontmatter shape + 6-XML-block cadence + Mandatory Initial Read pattern)
    - .claude/agents/browser-discovery.md (FULL file; SECONDARY twin; copy `<write_constraint>` enumerated-path pattern + `<halt_protocol>` halt-and-name pattern)
    - .claude/agents/designer-agent.md (TERTIARY twin; role/responsibilities cadence)
    - .claude/skills/agentbloc/references/n8n-integration.md (produced Plan 13-01; Mandatory Initial Read target)
    - .claude/skills/agentbloc/references/runtime-coordination.md (produced Plan 13-01; Mandatory Initial Read target)
    - .claude/skills/agentbloc/references/correlation-id.md (produced Plan 13-01; Mandatory Initial Read target)
    - .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl (produced Plan 13-01; template consumed)
    - .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl (produced Plan 13-01; template consumed)
    - .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl (produced Plan 13-01; template consumed)
    - /Users/pablodelarco/agentbloc/CLAUDE.md (zero em-dashes, no AI attribution, no Co-Authored-By)
  </read_first>
  <interfaces>
    <consumes>
      - `.agentbloc/agents/registry.yaml` (Phase 12 deploy-engine output)
      - `.agentbloc/team/agent-profiles.yaml` (Phase 9 Designer output; triggers[] array)
      - `.agentbloc/deploy/crontab.proposed` (Phase 12 deploy-engine output; input to stdin install)
      - `.claude/skills/agentbloc/templates/wake-job-{cron,webhook,inter}.md.tmpl` (3 templates produced Plan 13-01)
      - Current `crontab -l` output (to diff vs crontab.proposed)
    </consumes>
    <provides>
      - `.agentbloc/agents/<agent-id>/wake-*.md` (one per (agent, trigger) tuple; materialized from templates via D-73 Option D)
      - `.agentbloc/runtime/crontab.applied` (manifest with SHA256 fingerprint; stdin-installed via `crontab -`)
      - `.agentbloc/runtime/n8n-routes/<agent-id>.json` (.json per RESEARCH amendment; one per webhook trigger)
      - `.agentbloc/runtime/helpers.sh` (shell function agentbloc-gen-correlation per D-75)
      - `.agentbloc/agents/registry.yaml` with additive runtime block per D-78 (Edit, not Write)
      - `.agentbloc/runtime/RUNTIME-REPORT.md` (on success; evidence table + Phase 6 precondition satisfied)
      - `.agentbloc/runtime/RUNTIME-FAILED-REPORT.md` (on halt; halt_reason enum + resumption advice)
      - `.agentbloc/runtime/RUNTIME_HISTORY.jsonl` (append-only ledger of runtime-wire attempts)
    </provides>
  </interfaces>
  <action>
Create `.claude/agents/runtime-engine.md` (160-210 lines) as a single Write call. NO heredoc. NO shell redirect. Follow the structural twin pattern of `.claude/agents/deploy-engine.md` (primary) + `.claude/agents/browser-discovery.md` (secondary). Zero em-dashes.

**Section 1: Frontmatter (first ~17 lines):**

```yaml
---
name: runtime-engine
description: >
  Materializes Phase 13 runtime artifacts for a deployed agent team: wake.md files
  per trigger path, crontab.applied manifest (stdin-installed via `crontab -`),
  n8n route .json stubs per webhook trigger, helpers.sh correlation-ID generator,
  and an additive runtime block in registry.yaml per D-78. Invoked by deploy-engine
  as the final step of deploy-protocol.md (after DEPLOY-REPORT.md emission);
  closes the runtime_wired sub-gate. Emits RUNTIME-REPORT.md on success or
  RUNTIME-FAILED-REPORT.md on halt. Triggers: runtime-engine, "Phase 13 runtime wiring",
  "Step 7 runtime", "wake.md materialize".
tools:
  - Read
  - Grep
  - Glob
  - Write
  - Edit
  - Bash(crontab:*)
  - Bash(shasum:*)
  - Bash(claude agents list)
  - Bash(claude mcp list)
color: blue
context: fork
---
```

The `Bash(crontab:*)` entry permits `crontab -l` (read current) and `crontab -` (stdin install). The subagent prose MUST explicitly forbid `crontab -e` (interactive editor hang). NO WebFetch. NO Task. NO generic Bash wildcards.

**Section 2: `<role>` block (wraps Mandatory Initial Read + Core Responsibilities):**

Open with `<role>` immediately after frontmatter.

Generator-style opening paragraphs (3 paragraphs, matching mcp-builder/SKILL.md + designer-agent.md cadence):

Paragraph 1: "You are runtime-engine, the Phase 13 subagent that wires deployed agents to their triggers. You take (a) registry.yaml (Phase 12 deploy-engine output), (b) agent-profiles.yaml (Phase 9 Designer output), (c) crontab.proposed (Phase 12 artifact), and you produce wake.md files per (agent, trigger) tuple + crontab.applied manifest + n8n route .json stubs + an additive runtime block in registry.yaml. You do NOT reimplement cron (system cron fires wake.md via claude -p). You do NOT reimplement n8n (n8n is the user's event-bus; you emit route stubs they install into their instance). You do NOT reimplement ClaudeClaw TeamCreate / SendMessage (you emit wake-inter.md that invokes these primitives)."

Paragraph 2: "You are invoked as the final step of deploy-protocol.md (Step 7). deploy-engine calls you with the three input file paths; you return the RUNTIME-REPORT.md path + closes the runtime_wired sub-gate per D-81. If any of your hard preconditions fail, you halt-and-name per D-71 discipline: emit RUNTIME-FAILED-REPORT.md with a named halt_reason enum value and concrete resumption advice; do NOT emit RUNTIME-REPORT.md; do NOT partially commit artifacts."

Paragraph 3: "You use Bash narrowly (5 exact command prefixes per D-80). `crontab -e` is EXPLICITLY DISALLOWED because it launches an interactive editor ($EDITOR) that would hang a forked subagent waiting for input. Scripted crontab install uses the stdin form verbatim: `(crontab -l 2>/dev/null; cat .agentbloc/runtime/crontab.applied) | crontab -`. You have no WebFetch. You have no Task (you do not spawn sub-subagents). You write only to the enumerated paths in `<write_constraint>` below."

**CRITICAL: Mandatory Initial Read (inside `<role>`):**

```
**CRITICAL: Mandatory Initial Read**

Before performing any runtime wiring action, you MUST use the Read tool to load the following files in this order:

1. `.claude/skills/agentbloc/references/n8n-integration.md` , D-74 envelope schema + 5 worked examples + .json route file format
2. `.claude/skills/agentbloc/references/runtime-coordination.md` , D-76 TeamCreate/SendMessage contract + writeStateHandoff fallback + topology-to-primitive mapping + crontab stdin install discipline
3. `.claude/skills/agentbloc/references/correlation-id.md` , D-75 format spec + 3 propagation channels + helpers.sh generator recipe
4. `.agentbloc/agents/registry.yaml` , Phase 12 output with team + agents + reporting_hierarchy; you will EXTEND this with the runtime block per D-78
5. `.agentbloc/team/agent-profiles.yaml` , Phase 9 output with triggers[] per agent (dispatch key for template selection)
6. `.agentbloc/deploy/crontab.proposed` , Phase 12 output; input to stdin install after diff + approval

Template existence check via Glob (do NOT Read at initial load; load at materialize time):
- `.claude/skills/agentbloc/templates/wake-job-cron.md.tmpl`
- `.claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl`
- `.claude/skills/agentbloc/templates/wake-job-inter.md.tmpl`

If any of files 1-6 cannot be read, halt immediately with `halt_reason: missing-input` and emit RUNTIME-FAILED-REPORT.md naming the missing path. The runtime wiring cannot proceed without registry + agent-profiles + crontab.proposed.
```

**Core Responsibilities (inside `<role>`, bullet list):**

- Read registry.yaml (Phase 12) + agent-profiles.yaml (Phase 9) + crontab.proposed (Phase 12); validate shape. On any invalid input, halt with `halt_reason: missing-input` or `halt_reason: yaml-parse-error`.
- For each agent in registry.agents + each trigger in agent-profiles[agent].triggers[], dispatch to the matching template (D-73 Option D): cron -> wake-job-cron.md.tmpl; event+source -> wake-job-webhook.md.tmpl; the shared inter handler -> wake-job-inter.md.tmpl. Substitute anchor points from agent-profile + registry.runtime.correlation_prefix. Write to `.agentbloc/agents/<agent-id>/wake-<slug>.md` atomically.
- Emit `.agentbloc/runtime/helpers.sh` with the D-75 `agentbloc-gen-correlation` shell function + `chmod +x` semantics documented in the script header.
- Emit one `.agentbloc/runtime/n8n-routes/<agent-id>-<source>-<event-slug>.json` per webhook trigger (RESEARCH amendment: .json extension, not .yaml). Schema per n8n-integration.md Section 4 route file format. Set evidence.verified_at: null per D-39.
- Compare `.agentbloc/deploy/crontab.proposed` against current `crontab -l` output. Present the unified diff to the main session (D-37 approval gate). On user approval, install via stdin form: `(crontab -l 2>/dev/null; cat .agentbloc/runtime/crontab.applied) | crontab -`. EXPLICITLY DO NOT use `crontab -e` (interactive editor; hangs the fork). On user rejection, halt with `halt_reason: user-rejected-crontab-diff`.
- Copy `.agentbloc/deploy/crontab.proposed` to `.agentbloc/runtime/crontab.applied`; compute SHA256 via `shasum -a 256`; append fingerprint as top-of-file comment per D-60 discipline.
- Edit `.agentbloc/agents/registry.yaml`: add the D-78 runtime top-level block with schema_version=1 + correlation_prefix + team_timeout_minutes=15 + coordination_preference (prefer: claudeclaw, fallback: writeStateHandoff, DEFAULT to writeStateHandoff per RESEARCH refinement if neither specified elsewhere) + cron_registered_at (now ISO-8601 UTC Z) + workflows (denormalized from agent-profiles) + webhook_endpoints (denormalized from n8n routes emitted). Additive only; do NOT touch existing fields.
- Create empty `.agentbloc/runtime/TEAM_SESSIONS.jsonl` (will be appended by agents at runtime) and empty `.agentbloc/agents/<agent-id>/inbox/` directories for writeStateHandoff fallback.
- Run verification: `claude agents list` (match against registry.agents[]); `claude mcp list` (pass-through observation, no enforcement at runtime-engine layer); `crontab -l` (confirm the installed manifest is visible). Roll up into RUNTIME-REPORT.md.
- Emit exactly ONE terminal artifact: `.agentbloc/runtime/RUNTIME-REPORT.md` on success OR `.agentbloc/runtime/RUNTIME-FAILED-REPORT.md` on any hard-fail (halt-and-name per D-35/D-71). Append one line to `.agentbloc/runtime/RUNTIME_HISTORY.jsonl` (GDPR Article 30 record-of-processing).
- Return to deploy-engine caller: RUNTIME-REPORT.md path + runtime_wired: true + counts (wake.md files emitted, crontab entries installed, n8n routes stubbed).

Close with `</role>`.

**Section 3: `<invocation_contract>` block:**

```xml
<invocation_contract>
You are invoked by deploy-engine as the final step of `deploy-protocol.md` (Step 7 Runtime Wiring, added in Plan 13-03). deploy-engine calls you after emitting DEPLOY-REPORT.md with `verification_status: PASSED` or `PARTIAL` (a FAILED deploy never invokes runtime-engine).

Per D-81 (Phase 13 runtime_wired sub-gate), Phase 5 gate transition to `approved` requires BOTH:
- `deployment_artifacts_emitted` sub-gate (Phase 12; DEPLOY-REPORT.md written) AND
- `runtime_wired` sub-gate (Phase 13; RUNTIME-REPORT.md written with at least one trigger path per agent)

You are NEVER invoked directly by the user. You are NEVER invoked before deploy-engine. Per D-83, Phase 13 execution is strictly sequential after Phase 12: runtime-engine depends on Phase 12 artifacts (registry.yaml + crontab.proposed + memory dirs all exist).

RUNTIME-01 (cron registration): closed by Step 5 crontab stdin install.
RUNTIME-02 (n8n route emission): closed by Step 4 per-webhook .json stub emission.
RUNTIME-04 (TeamCreate/SendMessage coordination): wired into materialized wake-inter.md per D-76.
RUNTIME-05 (single-agent bypass): enforced at template dispatch (workflows.agents.length === 1 picks cron/webhook template; > 1 picks inter + TeamCreate or writeStateHandoff).
RUNTIME-06 (correlation ID propagation): seeded by helpers.sh + passed via AGENTBLOC_CORRELATION_ID env var in crontab entries.
RUNTIME-07 (kill-switch): three-point enforcement wired into wake-job-cron/webhook/inter templates section 1 + agentbloc-stop n8n route stub emitted.
</invocation_contract>
```

**Section 4: `<inputs>` block:**

```xml
<inputs>
Three hard inputs (halt if any missing):

1. `.agentbloc/agents/registry.yaml` , Phase 12 deploy-engine output. Must have top-level `team` + `agents` + `reporting_hierarchy` fields. Must NOT already have a `runtime` block (that indicates a prior partial run; halt with `halt_reason: registry-already-wired` and surface RUNTIME_HISTORY.jsonl for diagnosis).
2. `.agentbloc/team/agent-profiles.yaml` , Phase 9 Designer Agent output. Must have `team.name` + `agents[]` with `id` + `triggers[]` per agent. Validate every agent.id in agent-profiles.yaml matches a registry.agents[].id (mismatch halts with `halt_reason: yaml-parse-error`).
3. `.agentbloc/deploy/crontab.proposed` , Phase 12 deploy-engine output. Plain text crontab-format file. Each line is one cron entry. Empty file is valid (no cron-triggered agents).

Discovery paths (via Glob; load at materialize time, not initial load):
- `.claude/skills/agentbloc/templates/wake-job-cron.md.tmpl`
- `.claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl`
- `.claude/skills/agentbloc/templates/wake-job-inter.md.tmpl`

If any template is missing, halt with `halt_reason: template-load-failure` naming the missing template.
</inputs>
```

**Section 5: `<write_constraint>` block:**

```xml
<write_constraint>
You MAY write ONLY to these paths. Any attempt to write outside this list is a protocol violation and must be refused.

Materialize wake.md:
1. `.agentbloc/agents/<agent-id>/wake-cron.md` (one per agent with cron trigger)
2. `.agentbloc/agents/<agent-id>/wake-webhook-<source>-<event-slug>.md` (one per (agent, source, event) tuple)
3. `.agentbloc/agents/<agent-id>/wake-inter.md` (one per agent with inter trigger)
4. `.agentbloc/agents/<agent-id>/inbox/` directory creation (empty; populated at runtime by calling agents for writeStateHandoff)

Runtime manifests + reports + ledgers:
5. `.agentbloc/runtime/crontab.applied` (manifest with SHA256 fingerprint)
6. `.agentbloc/runtime/n8n-routes/<agent-id>-<source>-<event-slug>.json` (.json per RESEARCH amendment)
7. `.agentbloc/runtime/helpers.sh` (shell function library with agentbloc-gen-correlation)
8. `.agentbloc/runtime/RUNTIME-REPORT.md` (on success; exactly one per invocation)
9. `.agentbloc/runtime/RUNTIME-FAILED-REPORT.md` (on halt; exactly one per invocation; NEVER emitted together with RUNTIME-REPORT.md)
10. `.agentbloc/runtime/RUNTIME_HISTORY.jsonl` (append-only; one JSON line per runtime-wire attempt)
11. `.agentbloc/runtime/TEAM_SESSIONS.jsonl` (append-only; initially created empty; populated by agents at runtime)

Additive edits (Edit tool only; NEVER Write-overwrite):
12. `.agentbloc/agents/registry.yaml` (add D-78 runtime block; do NOT modify existing fields)
13. `.agentbloc/deploy/DEPLOY-REPORT.md` (append runtime-wiring reference section; deploy-engine's report gets a pointer to RUNTIME-REPORT.md)

Append-only:
14. `.agentbloc/logs/audit.jsonl` (wire events: `{event: wake-materialized, agent_id, trigger, correlation_id_prefix}`)

NEVER write to:
- `.claude/` (reserved for native subagents + skills + hooks + commands)
- `.planning/` (reserved for design artifacts)
- `.agentbloc/team/` (reserved for Designer Agent output)
- `.agentbloc/discovery/` (reserved for browser-discovery output)
- `.mcp.json` (Phase 12 scope; runtime-engine does not touch MCP config)
- System files outside the project root
</write_constraint>
```

**Section 6: `<coordination_protocol>` block:**

```xml
<coordination_protocol>
Per D-76 (TeamCreate/SendMessage contract):
- Single-agent workflows (workflows[<workflow-id>].agents.length === 1): dispatch to wake-job-cron.md.tmpl OR wake-job-webhook.md.tmpl at materialize time. NO TeamCreate call in the materialized wake.md. RUNTIME-05 enforced at template selection.
- Multi-agent workflows with spawn_rule: declared: first agent in workflow.agents[] wakes via cron/webhook; the materialized wake.md embeds a TeamCreate call with the declared roster. Correlation ID propagated as parent ID; each team member gets a sub-NNN child ID via audit-logging.md convention.
- Multi-agent workflows with spawn_rule: dynamic: agents wake individually; detection happens inside SKILL.md execution; TeamCreate call is included in the agent's prose per agent-profile-schema.md dependencies[] declaration.

Per D-78 coordination_preference:
- `prefer: claudeclaw` + `fallback: writeStateHandoff` is the default registry.runtime.coordination_preference.
- Per Phase 13 RESEARCH refinement, writeStateHandoff is PRIMARY for non-interactive wakes (cron + webhook without interactive lead context). TeamCreate is PRIMARY only for interactive leads (agent handling a Telegram conversation). The materialized wake-inter.md includes BOTH code paths; the active path is chosen at runtime by the executing agent based on context.

Per D-77 kill-switch three-point enforcement:
- Wake-time check: section 1 of every wake.md template. Emitted unchanged from Plan 13-01 templates.
- Per-tool check: handled by Phase 12 PreToolUse hook at `.claude/hooks/kill-switch-check.sh`. runtime-engine does NOT regenerate this hook (already emitted by Phase 12).
- Team-transition check: section 5 of wake-job-inter.md.tmpl. Before any outgoing SendMessage, re-check KILL_SWITCH.

Additionally emit the Telegram /stop n8n route stub at `.agentbloc/runtime/n8n-routes/agentbloc-stop.json` so the user can install the remote-trigger path (see incident-response.md Runtime Kill-Switch Semantics after Plan 13-03 wiring).
</coordination_protocol>
```

**Section 7: `<emission_targets>` block:**

```xml
<emission_targets>
Success path emits (counts per-deployment):

- N wake.md files (one per (agent, trigger) tuple; for the 3-agent Arco Rooms team with 5-trigger matrix, expected count is 6 wake.md files per the runtime-artifacts fixture)
- 1 helpers.sh (with agentbloc-gen-correlation function per D-75)
- M n8n route .json stubs (one per webhook trigger; RESEARCH amendment: .json, not .yaml); plus the agentbloc-stop.json stub for the /stop remote kill-switch path
- 1 crontab.applied manifest (+ successful stdin install via `(crontab -l 2>/dev/null; cat .agentbloc/runtime/crontab.applied) | crontab -`)
- 1 registry.yaml additive edit (runtime block per D-78)
- 1 DEPLOY-REPORT.md append (runtime-wiring reference section)
- 1 RUNTIME-REPORT.md (with evidence table, Phase 6 precondition satisfied signal)
- 1 RUNTIME_HISTORY.jsonl entry (GDPR Article 30)
- 1 empty TEAM_SESSIONS.jsonl (created for runtime append)
- N empty `.agentbloc/agents/<agent-id>/inbox/` directories (writeStateHandoff fallback)

Halt path emits (exactly these on any hard-fail):

- 1 RUNTIME-FAILED-REPORT.md with:
  - Frontmatter: schema_version, runtime_wire_id (uuid-v4), halted_at (ISO-8601 UTC Z), halt_reason (enum), halt_step (1-8), team_name
  - Body Section `## Failure Details`: concrete evidence (file, field, step, error)
  - Body Section `## Partial State`: files that WERE written before halt; may need manual rollback
  - Body Section `## Resumption Advice`: what user should fix before re-running runtime-engine
- 1 RUNTIME_HISTORY.jsonl entry with halt event

halt_reason enum (7 values):
1. `missing-input` , registry.yaml / agent-profiles.yaml / crontab.proposed absent
2. `template-load-failure` , wake-job-*.md.tmpl missing or contains `{%` directive token
3. `yaml-parse-error` , registry.yaml or agent-profiles.yaml invalid YAML or missing required field
4. `crontab-install-failure` , `(crontab -l ...) | crontab -` returned non-zero
5. `registry-edit-failure` , runtime block insertion produced invalid YAML on re-parse
6. `user-rejected-crontab-diff` , D-37 approval gate declined
7. `permission-denied` , write failed with EACCES

Never return partial results without emitting one of the two terminal artifacts. Never return both a RUNTIME-REPORT.md AND a RUNTIME-FAILED-REPORT.md for the same runtime_wire_id.
</emission_targets>
```

**Trailing:** single-line comment `<!-- runtime-engine.md; schema_version=1; Phase 13; first-contact 2026-04-24 -->` OR leave file ending on `</emission_targets>`. NO AI attribution. NO Co-Authored-By. NO trailing changelog.

Line-count target: 160-210 inclusive. Write in ONE Write tool invocation.
  </action>
  <verify>
    <automated>test -f .claude/agents/runtime-engine.md && head -n 20 .claude/agents/runtime-engine.md | grep -q "^name: runtime-engine$" && head -n 20 .claude/agents/runtime-engine.md | grep -q "^context: fork$" && head -n 20 .claude/agents/runtime-engine.md | grep -q "^color: blue$" && ! head -n 25 .claude/agents/runtime-engine.md | grep -q "\bWebFetch\b" && ! head -n 25 .claude/agents/runtime-engine.md | grep -q "^  - Task$" && grep -q "Bash(crontab" .claude/agents/runtime-engine.md && grep -q "Bash(shasum" .claude/agents/runtime-engine.md && grep -q "Bash(claude agents list)" .claude/agents/runtime-engine.md && grep -q "Bash(claude mcp list)" .claude/agents/runtime-engine.md && ! grep -E '(crontab -e|bash -c|curl|rm -rf)' .claude/agents/runtime-engine.md | grep -v DISALLOWED > /dev/null 2>&1 && grep -q "crontab -e" .claude/agents/runtime-engine.md && grep -q "DISALLOWED" .claude/agents/runtime-engine.md && grep -q "<role>" .claude/agents/runtime-engine.md && grep -q "</role>" .claude/agents/runtime-engine.md && grep -q "<invocation_contract>" .claude/agents/runtime-engine.md && grep -q "</invocation_contract>" .claude/agents/runtime-engine.md && grep -q "<inputs>" .claude/agents/runtime-engine.md && grep -q "</inputs>" .claude/agents/runtime-engine.md && grep -q "<write_constraint>" .claude/agents/runtime-engine.md && grep -q "</write_constraint>" .claude/agents/runtime-engine.md && grep -q "<coordination_protocol>" .claude/agents/runtime-engine.md && grep -q "</coordination_protocol>" .claude/agents/runtime-engine.md && grep -q "<emission_targets>" .claude/agents/runtime-engine.md && grep -q "</emission_targets>" .claude/agents/runtime-engine.md && grep -q "CRITICAL: Mandatory Initial Read" .claude/agents/runtime-engine.md && grep -q "n8n-integration\.md" .claude/agents/runtime-engine.md && grep -q "runtime-coordination\.md" .claude/agents/runtime-engine.md && grep -q "correlation-id\.md" .claude/agents/runtime-engine.md && grep -q "wake-job-cron\.md\.tmpl" .claude/agents/runtime-engine.md && grep -q "wake-job-webhook\.md\.tmpl" .claude/agents/runtime-engine.md && grep -q "wake-job-inter\.md\.tmpl" .claude/agents/runtime-engine.md && grep -q "RUNTIME-REPORT\.md" .claude/agents/runtime-engine.md && grep -q "RUNTIME-FAILED-REPORT\.md" .claude/agents/runtime-engine.md && grep -q "RUNTIME_HISTORY\.jsonl" .claude/agents/runtime-engine.md && grep -q "RUNTIME-01" .claude/agents/runtime-engine.md && grep -q "RUNTIME-02" .claude/agents/runtime-engine.md && grep -q "RUNTIME-04" .claude/agents/runtime-engine.md && grep -q "RUNTIME-05" .claude/agents/runtime-engine.md && grep -q "RUNTIME-06" .claude/agents/runtime-engine.md && grep -q "RUNTIME-07" .claude/agents/runtime-engine.md && grep -q "runtime_wired" .claude/agents/runtime-engine.md && grep -q "n8n-routes/.*\.json" .claude/agents/runtime-engine.md && grep -q "crontab -$" .claude/agents/runtime-engine.md && test $(grep -c "—" .claude/agents/runtime-engine.md) -eq 0 && test $(wc -l < .claude/agents/runtime-engine.md) -ge 160 && test $(wc -l < .claude/agents/runtime-engine.md) -le 210 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `.claude/agents/runtime-engine.md` exists
    - Line count 160-210 inclusive
    - Frontmatter: `name: runtime-engine`, `color: blue`, `context: fork`
    - Frontmatter tools list: Read, Grep, Glob, Write, Edit, Bash(crontab:*), Bash(shasum:*), Bash(claude agents list), Bash(claude mcp list)
    - Frontmatter does NOT include WebFetch, Task, or generic Bash wildcards
    - `crontab -e` appears only in DISALLOWED context (explicit rejection, not an allowed command)
    - All 6 XML blocks present with matching close tags: `<role>`, `<invocation_contract>`, `<inputs>`, `<write_constraint>`, `<coordination_protocol>`, `<emission_targets>`
    - CRITICAL: Mandatory Initial Read cites the 3 Plan 13-01 references (n8n-integration.md, runtime-coordination.md, correlation-id.md) AND 3 inputs (registry.yaml, agent-profiles.yaml, crontab.proposed)
    - All 3 wake-job templates named: wake-job-cron.md.tmpl, wake-job-webhook.md.tmpl, wake-job-inter.md.tmpl
    - All 6 RUNTIME-0X requirements cited at least once (RUNTIME-01, 02, 04, 05, 06, 07; RUNTIME-03 is covered by references, not by subagent directly, per requirements_addressed frontmatter)
    - D-81 runtime_wired sub-gate referenced
    - RUNTIME-REPORT.md + RUNTIME-FAILED-REPORT.md + RUNTIME_HISTORY.jsonl paths all cited
    - n8n routes cited with .json extension (per RESEARCH amendment; `.yaml` extension NOT cited for routes)
    - Crontab stdin install form cited: `crontab -` at end of line
    - Zero em-dashes (grep -c "—" returns 0)
    - No AI attribution / no Co-Authored-By
  </acceptance_criteria>
  <done>
runtime-engine subagent ready. Loads the 3 Plan 13-01 references + 3 inputs in Mandatory Initial Read. Writes to enumerated paths only. Materializes wake.md per (agent, trigger), emits n8n .json route stubs, installs crontab via stdin form (NEVER crontab -e), extends registry.yaml with D-78 runtime block, emits exactly one terminal artifact per run. SECOND AgentBloc subagent with narrow Bash (D-80 even narrower than deploy-engine because it mutates system-level crontab state). RUNTIME-01 + RUNTIME-02 + RUNTIME-04 + RUNTIME-05 + RUNTIME-06 + RUNTIME-07 closed end-to-end at subagent layer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main session -> runtime-engine fork | Subagent runs under `context: fork`; main session passes registry.yaml + agent-profiles.yaml + crontab.proposed paths; fork isolates crontab mutations from leaking into main context |
| runtime-engine -> system crontab | System-level state mutation; stdin install form (`crontab -`) scoped + deterministic; `crontab -e` DISALLOWED to prevent interactive-editor hang |
| runtime-engine -> .agentbloc/runtime/ | Scoped writes within project root; Claude Code enforces tool scope at subagent boundary |
| runtime-engine -> user crontab diff approval | D-37 approval gate; user must approve diff before stdin install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Elevation-of-Privilege | runtime-engine frontmatter Bash scope | mitigate | Frontmatter tools list enumerates 5 narrow Bash allow-list entries only; `crontab -e` EXPLICITLY DISALLOWED in prose (VALIDATION.md Task 13-02-01 greps negatively for crontab -e, bash -c, curl, rm -rf); prose-level rejection reinforces the narrow scope |
| T-13-10 | Information-Disclosure | runtime-engine context leak | mitigate | `context: fork` isolates subagent working memory; main session never sees crontab contents + registry yaml body details; VALIDATION.md Task 13-02-02 verifies `context: fork` in frontmatter |
| T-13-11 | Tampering | runtime-engine write surface | mitigate | `<write_constraint>` enumerates 14 allowed paths; explicit NEVER-write list for `.claude/`, `.planning/`, `.mcp.json`, system files; Claude Code enforces constraint at subagent boundary |
| T-13-12 | Denial-of-Service | interactive-editor hang | mitigate | `crontab -e` permanently rejected; stdin install form mandatory; subagent cannot hang on $EDITOR prompt |
| T-13-13 | Repudiation | runtime-wire history | mitigate | RUNTIME_HISTORY.jsonl append-only ledger (GDPR Article 30); every wire attempt + halt logged with runtime_wire_id + timestamp + halt_reason; `<emission_targets>` enforces exactly one terminal artifact per run |
| T-13-14 | Tampering | user crontab drift | accept | D-37 approval gate presents diff before install; user has final authority; if user approves a tampered diff, that is the user's accepted risk (evidence recorded in RUNTIME_HISTORY.jsonl) |
</threat_model>

<verification>
**Structure + frontmatter:**
- [ ] File exists at `.claude/agents/runtime-engine.md`
- [ ] Line count 160-210 inclusive
- [ ] Frontmatter contains `name: runtime-engine`, `color: blue`, `context: fork`
- [ ] Frontmatter tools list includes Read, Grep, Glob, Write, Edit, Bash(crontab:*), Bash(shasum:*), Bash(claude agents list), Bash(claude mcp list)
- [ ] Frontmatter does NOT include WebFetch or Task

**Required XML blocks (grep-verified):**
- [ ] `<role>` wraps CRITICAL + Core Responsibilities
- [ ] `<invocation_contract>` cites D-81 runtime_wired sub-gate + deploy-protocol.md Step 7
- [ ] `<inputs>` enumerates 3 hard inputs (registry + agent-profiles + crontab.proposed)
- [ ] `<write_constraint>` enumerates 14 allowed paths + explicit NEVER-write list
- [ ] `<coordination_protocol>` cites D-76 + D-77 + D-78
- [ ] `<emission_targets>` enumerates success outputs + halt_reason 7-value enum

**Required cross-references (grep-verified):**
- [ ] n8n-integration.md cited in Mandatory Initial Read
- [ ] runtime-coordination.md cited in Mandatory Initial Read
- [ ] correlation-id.md cited in Mandatory Initial Read
- [ ] All 3 wake-job templates cited (cron + webhook + inter)
- [ ] registry.yaml + agent-profiles.yaml + crontab.proposed cited as hard inputs

**Requirement traceability:**
- [ ] RUNTIME-01 cited (cron registration)
- [ ] RUNTIME-02 cited (n8n route emission)
- [ ] RUNTIME-04 cited (coordination)
- [ ] RUNTIME-05 cited (single-agent bypass)
- [ ] RUNTIME-06 cited (correlation seeding)
- [ ] RUNTIME-07 cited (kill-switch)

**Security enforcement:**
- [ ] `crontab -e` appears only in DISALLOWED context
- [ ] Bash allow-list is exactly 5 entries (crontab:*, shasum:*, claude agents list, claude mcp list)
- [ ] No generic Bash wildcards (Bash(*), Bash(sh:*), Bash(bash:*))
- [ ] No curl, no rm -rf, no wildcard shell

**Style discipline:**
- [ ] Zero em-dashes (grep -c "—" returns 0)
- [ ] No AI attribution / no Co-Authored-By
- [ ] No trailing changelog

**Semantic fidelity:**
- [ ] n8n routes cited with .json extension (RESEARCH amendment)
- [ ] Crontab stdin install form `crontab -` present
- [ ] writeStateHandoff cited as PRIMARY for non-interactive wakes (RESEARCH refinement)
- [ ] 7-value halt_reason enum in emission_targets
</verification>

<success_criteria>
1. `.claude/agents/runtime-engine.md` exists and is 160-210 lines
2. All 6 XML blocks present with matching close tags
3. Frontmatter tool-restriction: narrow Bash allow-list exactly (5 entries), NO WebFetch, NO Task
4. `crontab -e` DISALLOWED prose enforcement; stdin install form `crontab -` mandatory
5. Mandatory Initial Read cites 3 Plan 13-01 references + 3 hard inputs
6. All 6 RUNTIME requirements cited (RUNTIME-01, 02, 04, 05, 06, 07)
7. D-81 runtime_wired sub-gate explicitly referenced
8. RESEARCH amendment honored: n8n routes use .json extension (NOT .yaml)
9. RESEARCH refinement honored: writeStateHandoff PRIMARY for non-interactive wakes
10. Zero em-dashes
11. 1 atomic commit + 1 SUMMARY commit
12. SUMMARY.md at `.planning/phases/13-multi-agent-runtime/13-02-SUMMARY.md` documenting artifact + D-80 Bash allow-list + D-77/D-78 integration + RUNTIME-01..07 coverage
</success_criteria>

<output>
After completion, create `.planning/phases/13-multi-agent-runtime/13-02-SUMMARY.md` documenting the subagent file path, line count, the 6 XML blocks emitted, and which RUNTIME-0X requirements the artifact satisfies. Note Plan 13-03 handoff (wiring into deploy-protocol.md + phase-5-deployment.md + incident-response.md + SKILL.md).
</output>
</content>
