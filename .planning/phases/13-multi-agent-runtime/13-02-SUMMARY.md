# Plan 13-02 Summary: runtime-engine Subagent

Plan 13-02 emits the Phase 13 runtime-engine subagent (`.claude/agents/runtime-engine.md`, 188 lines, 0 em-dashes) as the FOURTH AgentBloc subagent and SECOND to use Bash. The narrow Bash allow-list per D-80 is even narrower than deploy-engine's because runtime-engine mutates system-level crontab state.

## Artifact

| File | Lines | Em-dashes | Commit |
|------|-------|-----------|--------|
| `.claude/agents/runtime-engine.md` | 188 | 0 | `b760423` |

Within the 160-210 line budget per D-82.

## 6 XML blocks emitted

| Block | Purpose |
|-------|---------|
| `<role>` | 3-paragraph generator-style opening + Mandatory Initial Read (3 references + 3 inputs) + Core Responsibilities (10 bullets covering 8-step protocol per D-80) |
| `<invocation_contract>` | D-81 runtime_wired sub-gate + deploy-protocol.md Step 7 invocation + RUNTIME-01..07 closure mapping |
| `<inputs>` | 3 hard inputs (registry.yaml, agent-profiles.yaml, crontab.proposed) + template Glob discovery |
| `<write_constraint>` | 14 allowed paths + explicit NEVER-write list + forbidden tool patterns |
| `<coordination_protocol>` | D-76 TeamCreate/SendMessage + D-78 coordination_preference (RESEARCH refinement: writeStateHandoff PRIMARY for non-interactive) + D-77 three-point kill-switch enforcement |
| `<emission_targets>` | Success path emissions + halt path with 7-value halt_reason enum |

## D-80 Bash allow-list (narrow)

Frontmatter `tools` enumerates exactly 5 Bash entries:
- `Bash(crontab:*)` (covers `crontab -l` read + `crontab -` stdin install)
- `Bash(shasum:*)` (SHA256 fingerprint per D-60)
- `Bash(claude agents list)` (verification read-only)
- `Bash(claude mcp list)` (verification read-only)

NO WebFetch. NO Task. NO generic Bash wildcards. NO `bash -c`. NO `sh ...`.

The prose explicitly DISALLOWS `crontab -e` even though tool scope `Bash(crontab:*)` would technically permit it — the interactive editor would hang the forked subagent waiting for `$EDITOR` input. The mandatory install incantation is a fenced bash block:

```bash
(crontab -l 2>/dev/null; cat .agentbloc/runtime/crontab.applied) | crontab -
```

## RUNTIME-0X requirement coverage

| Requirement | Closure mechanism (cited in `<invocation_contract>`) |
|-------------|------------------------------------------------------|
| RUNTIME-01 | crontab stdin install (Step 5 of 8-step protocol) |
| RUNTIME-02 | Per-webhook .json route stub emission (Step 4) |
| RUNTIME-04 | TeamCreate/SendMessage wired into materialized wake-inter.md per D-76 |
| RUNTIME-05 | Single-agent bypass enforced at template dispatch (workflow.agents.length === 1) |
| RUNTIME-06 | helpers.sh emission + AGENTBLOC_CORRELATION_ID env var seeding in cron entries |
| RUNTIME-07 | Three-point kill-switch (wake-time + per-tool via Phase 12 hook + team-transition) + agentbloc-stop.json route stub |

(RUNTIME-03 — registry runtime block — is closed at the reference layer in Plan 13-01, not by the subagent.)

## D-77/D-78 integration

D-77 three-point kill-switch enforcement is emitted at three layers:
1. Wake-time check: section 1 of all 3 wake-job templates (Plan 13-01 emission, unchanged here)
2. Per-tool check: Phase 12 PreToolUse hook at `.claude/hooks/kill-switch-check.sh` (NOT regenerated by runtime-engine)
3. Team-transition check: section 5 of wake-job-inter.md.tmpl (Plan 13-01 emission, unchanged here)

D-78 coordination_preference: runtime-engine writes the registry.runtime block with default `prefer: writeStateHandoff` for non-interactive wakes per RESEARCH refinement; `prefer: claudeclaw` only when an interactive lead is present.

## Plan 13-03 handoff

Plan 13-03 surgical edits will:
1. Add Step 7 (Runtime Wiring) to `references/deploy-protocol.md` — invokes runtime-engine after DEPLOY-REPORT.md emission per D-83
2. Add Step 7.5 to `references/phase-5-deployment.md` — user-visible runtime materialization step + Phase 6 precondition
3. Add Runtime Kill-Switch Semantics section to `references/incident-response.md` — cites D-77 three-point enforcement + the agentbloc-stop.json route stub emitted here
4. Update SKILL.md Phase 5 See-lines to point at the 3 Plan 13-01 references + the runtime-engine subagent emitted here

No content emitted by Plan 13-02 is rewritten in Plan 13-03; surgical insertion-point discipline per D-40 + D-83.

## Verification status

All Plan 13-02 automated verifies passed at commit time. Em-dash gate clean (0). All 6 XML blocks present with matching close tags. Cross-references to all 3 Plan 13-01 references + 3 Plan 13-01 templates intact. Plan 13-03 can begin immediately.
