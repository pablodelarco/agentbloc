---
phase: 13-multi-agent-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/agentbloc/references/n8n-integration.md
  - .claude/skills/agentbloc/references/runtime-coordination.md
  - .claude/skills/agentbloc/references/correlation-id.md
  - .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl
  - .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl
  - .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl
  - .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md
  - .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md
autonomous: true
requirements_addressed:
  - RUNTIME-02
  - RUNTIME-03
  - RUNTIME-04
  - RUNTIME-05
  - RUNTIME-06
  - RUNTIME-07
estimated_tasks: 8

must_haves:
  truths:
    - "references/n8n-integration.md exists as a loadable reference documenting the D-74 4-field JSON envelope (schema_version, correlation_id, agent_id, trigger with nested payload), 5 worked examples from PDF page 5 trigger matrix (Gmail, Plaid, web form, Google Calendar, Telegram), n8n route .json file format per RESEARCH amendment, runtime-agnostic fallback pattern (HTTP listener + claude -p with payload-file), and bounded trigger.source enum (gmail | plaid | bbva | google-calendar | telegram | form | custom-<name>)"
    - "references/runtime-coordination.md exists as a loadable reference documenting D-76 TeamCreate/SendMessage primitive contract, writeStateHandoff PRIMARY for non-interactive wakes per RESEARCH refinement, TeamCreate PRIMARY only for interactive leads, topology-to-primitive mapping (pipeline = staggered cron; mesh = TeamCreate + SendMessage; hierarchy = lead spawns team; swarm = TeamCreate + peer SendMessage), single-agent bypass enforcement (workflows[].agents.length > 1 triggers TeamCreate; length === 1 is direct execution), team dissolution semantics per D-77 (all-members-returned | kill-switch | timeout | error), crontab stdin install form (NEVER crontab -e)"
    - "references/correlation-id.md exists as a loadable reference documenting D-75 format <trigger-source>-<UTC-Z-compact>-<nonce6>, bounded trigger-source enum (cron | webhook-<source> | telegram | inter | manual), 3 propagation channels (AGENTBLOC_CORRELATION_ID env var at claude -p invocation + JSON payload top-level field + SendMessage metadata), child-ID sub-<NNN> append rule inherited from audit-logging.md, 6-hex nonce math (16^6 = 16.7M collision space per second per source), at least 4 grep recipes, scalability note for v2.5 SQLite migration"
    - "templates/wake-job-cron.md.tmpl exists as a 6-section wake-job template for cron triggers with D-73 fixed section order (Kill-switch pre-check, Correlation-ID ingest, Memory + state read, Input parse, Execute, State + log write), pure {{var}} substitution only (zero {% if %} or {% for %} directives), kill-switch pre-check at section 1 per D-77"
    - "templates/wake-job-webhook.md.tmpl exists as a 6-section wake-job template for webhook triggers with section 4 (Input parse) specialized for payload parsing per D-74 envelope schema, identical skeleton to wake-job-cron.md.tmpl, kill-switch pre-check at section 1"
    - "templates/wake-job-inter.md.tmpl exists as a 6-section wake-job template for inter-agent triggers with section 4 (Input parse) specialized for SendMessage body parsing + team dispatch logic, identical skeleton to wake-job-cron.md.tmpl, kill-switch pre-check at section 1"
    - "examples/arco-rooms-correlation-flow.md exists as a canonical fixture with exactly 3 scenarios per D-79: Scenario A (cron wake of Gestor Cobros with correlation_id cron-<UTC-Z>-<nonce>), Scenario B (Telegram tenant message webhook + TeamCreate with Recepcionista + Gestor Cobros and sub-001 child ID), Scenario C (KILL_SWITCH mid-team with dissolution_reason: kill-switch logged)"
    - "examples/arco-rooms-runtime-artifacts.md exists as a structural fixture showing literal file contents: 3 wake.md artifacts (one cron for Gestor Cobros; one webhook-telegram for Recepcionista; one inter for team formation), 1 crontab.applied manifest with Gestor Cobros + Gestor Documental cron entries, 3 n8n route JSON stubs (telegram + gmail + plaid; .json per RESEARCH amendment, not .yaml), 1 extended registry.yaml with full runtime block per D-78"
    - "Zero em-dashes (U+2014) across all 8 emitted files; grep -c '—' per file returns 0"
    - "All 8 files reachable from Plan 13-02 runtime-engine subagent (subagent cites references at Mandatory Initial Read; subagent materializes wake.md from templates; subagent consumes fixtures for golden-file behavior in Phase 16)"
  artifacts:
    - path: ".claude/skills/agentbloc/references/n8n-integration.md"
      provides: "Event-bus contract for Phase 13 runtime: D-74 4-field JSON envelope + 5 worked examples + n8n route .json schema + runtime-agnostic fallback + bounded trigger.source enum"
      min_lines: 180
      max_lines: 220
      contains: "schema_version: 1"
    - path: ".claude/skills/agentbloc/references/runtime-coordination.md"
      provides: "D-76 TeamCreate/SendMessage primitive contract + topology-to-primitive mapping + writeStateHandoff fallback + single-agent bypass + team dissolution semantics per D-77 + crontab stdin install discipline"
      min_lines: 180
      max_lines: 220
      contains: "workflows[].agents.length > 1"
    - path: ".claude/skills/agentbloc/references/correlation-id.md"
      provides: "D-75 correlation-ID format spec + 3 propagation channels + sub-ID child convention + grep recipes + v2.5 SQLite scalability note"
      min_lines: 120
      max_lines: 150
      contains: "AGENTBLOC_CORRELATION_ID"
    - path: ".claude/skills/agentbloc/templates/wake-job-cron.md.tmpl"
      provides: "D-73 6-section cron-trigger wake template; pure {{var}} substitution; kill-switch pre-check at section 1"
      min_lines: 80
      max_lines: 100
      contains: ".agentbloc/KILL_SWITCH"
    - path: ".claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl"
      provides: "D-73 6-section webhook-trigger wake template with payload-parse section; identical skeleton to cron template"
      min_lines: 80
      max_lines: 100
      contains: ".agentbloc/KILL_SWITCH"
    - path: ".claude/skills/agentbloc/templates/wake-job-inter.md.tmpl"
      provides: "D-73 6-section inter-agent wake template with SendMessage body parse + team dispatch; identical skeleton to cron template"
      min_lines: 80
      max_lines: 100
      contains: ".agentbloc/KILL_SWITCH"
    - path: ".claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md"
      provides: "Canonical 3-scenario narrative fixture per D-79: cron wake + webhook + TeamCreate + KILL_SWITCH mid-team"
      min_lines: 180
      max_lines: 240
      contains: "Scenario A"
    - path: ".claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md"
      provides: "Structural fixture per D-79: literal wake.md + crontab.applied + n8n route .json stubs + extended registry.yaml runtime block"
      min_lines: 200
      max_lines: 280
      contains: "runtime:"
  key_links:
    - from: ".claude/skills/agentbloc/references/n8n-integration.md"
      to: ".claude/skills/agentbloc/references/correlation-id.md"
      provides: "envelope carries correlation_id as top-level field; n8n-integration cites correlation-id.md format spec"
    - from: ".claude/skills/agentbloc/references/runtime-coordination.md"
      to: ".claude/skills/agentbloc/references/n8n-integration.md"
      provides: "webhook-triggered multi-agent workflows cite n8n envelope + route schema"
    - from: ".claude/skills/agentbloc/references/runtime-coordination.md"
      to: ".claude/skills/agentbloc/references/correlation-id.md"
      provides: "SendMessage metadata.correlation_id contract cites correlation-id.md format"
    - from: ".claude/skills/agentbloc/templates/wake-job-cron.md.tmpl"
      to: ".claude/skills/agentbloc/references/correlation-id.md"
      provides: "template section 2 correlation-ID ingest cites correlation-id.md format"
    - from: ".claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md"
      to: ".claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md"
      provides: "narrative fixture cross-references structural fixture for literal file shapes"
---

<objective>
Create the Phase 13 runtime contracts + fixtures: 3 new references (n8n-integration.md + runtime-coordination.md + correlation-id.md), 3 new wake-job templates (cron + webhook + inter), and 2 new Arco Rooms fixtures (correlation flow narrative + runtime artifacts structural). This is the structural foundation Phase 13 Plans 13-02 (subagent) and 13-03 (wiring) build on.

Purpose: RUNTIME-03 (n8n webhook-to-agent mapping documented), RUNTIME-04 (TeamCreate/SendMessage contract defined), RUNTIME-05 (single-agent bypass enforced at template level), RUNTIME-06 (correlation-ID format + propagation channels documented), RUNTIME-07 (kill-switch pre-check in every wake template), RUNTIME-02 (n8n event-bus contract with route .json files).

Output: 8 new files committed to git. Ready for Plan 13-02 runtime-engine subagent to load these in its Mandatory Initial Read and consume the templates for wake.md materialization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-multi-agent-runtime/13-CONTEXT.md
@.planning/phases/13-multi-agent-runtime/13-RESEARCH.md
@.planning/phases/13-multi-agent-runtime/13-VALIDATION.md
@.claude/skills/agentbloc/references/mcp-integration-protocol.md
@.claude/skills/agentbloc/references/browser-fallback.md
@.claude/skills/agentbloc/references/audit-logging.md
@.claude/skills/agentbloc/references/scheduling.md
@.claude/skills/agentbloc/references/telegram-patterns.md
@.claude/skills/agentbloc/references/orchestration-patterns.md
@.claude/skills/agentbloc/references/agent-profile-schema.md
@.claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl
@.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml
@.claude/skills/agentbloc/examples/arco-rooms-registry.yaml
@.claude/skills/agentbloc/examples/arco-rooms-deploy-report.md
@.claude/skills/agentbloc/examples/mapfre-discovery-report.md

<interfaces>
<!-- Load-bearing contracts the 8 new files MUST emit. Extracted from 13-CONTEXT.md decisions. -->

D-74 n8n envelope (verbatim, emitted in n8n-integration.md + arco-rooms-runtime-artifacts.md):
```json
{
  "schema_version": 1,
  "correlation_id": "<trigger-source>-<UTC-Z>-<nonce6>",
  "agent_id": "<agent-id>",
  "trigger": {
    "source": "gmail | plaid | bbva | google-calendar | telegram | form | custom-<name>",
    "event_name": "<source-specific event name>",
    "received_at": "<ISO-8601 UTC>"
  },
  "payload": { /* event-source-specific body */ }
}
```

D-75 correlation-ID format (verbatim, emitted in correlation-id.md):
```
cron-20260424T090000Z-a3f21b
webhook-plaid-20260424T091523Z-b8c41e
webhook-telegram-20260424T092045Z-c7d92a
inter-20260424T093101Z-f0e82a
manual-20260424T094512Z-02db9c
```

Regex (emitted in correlation-id.md + verifiable in VALIDATION.md Task 13-01-03):
```
^(cron|webhook-[a-z][a-z0-9-]*|telegram|inter|manual)-[0-9]{8}T[0-9]{6}Z-[a-f0-9]{6}(-sub-[0-9]{3})*$
```

D-73 wake-job 6-section structure (identical skeleton in all 3 templates):
```
## 1. Kill-switch pre-check
## 2. Correlation-ID ingest
## 3. Memory + state read
## 4. Input parse
## 5. Execute
## 6. State + log write
```

D-76 single-agent bypass enforcement (emitted in runtime-coordination.md):
- workflows[].agents.length === 1 -> direct wake via wake-job-cron.md.tmpl or wake-job-webhook.md.tmpl, NO TeamCreate
- workflows[].agents.length > 1 AND spawn_rule: declared -> first agent wakes, immediately TeamCreate(agents=workflow.agents, correlation_id=<ID>)
- workflows[].agents.length > 1 AND spawn_rule: dynamic -> agent detects dependency, calls TeamCreate([self, detected-agent, ...transitive])

D-78 registry runtime block (emitted in arco-rooms-runtime-artifacts.md):
```yaml
runtime:
  schema_version: 1
  correlation_prefix: "<team-name>"
  team_timeout_minutes: 15
  coordination_preference:
    prefer: "claudeclaw"
    fallback: "writeStateHandoff"
  cron_registered_at: "<ISO-8601 | null>"
  crontab_manifest: ".agentbloc/runtime/crontab.applied"
  workflows: { ... }
  webhook_endpoints: [ ... ]
```

Crontab stdin install form (emitted in runtime-coordination.md; rejected alternative: crontab -e):
```bash
(crontab -l 2>/dev/null; cat .agentbloc/runtime/crontab.applied) | crontab -
```

5 worked examples (from PDF page 5 trigger matrix, emitted in n8n-integration.md):
1. Gmail filter -> Gestor Documental (new-invoice-email event)
2. Plaid webhook -> Gestor Cobros (payment-received event)
3. Web form submit -> Recepcionista (contact-form-submission event)
4. Google Calendar watch -> Agente Agenda (calendar-change event) [anticipated agent stub for Phase 15 continuity]
5. Telegram message -> Recepcionista (tenant-message event)

3 scenarios for arco-rooms-correlation-flow.md (per D-79):
- Scenario A (cron wake): 1st of month at 09:00 Europe/Madrid, Gestor Cobros wakes, correlation_id cron-20260501T080000Z-<nonce6> (UTC 08:00 Z for CEST)
- Scenario B (webhook + TeamCreate): Telegram tenant -> Recepcionista wake -> TeamCreate([recepcionista, gestor-cobros]) with sub-001 child ID
- Scenario C (KILL_SWITCH mid-team): Scenario B in progress, /stop fires, both agents return halted-kill-switch, team dissolves with dissolution_reason: kill-switch

Arco Rooms trigger matrix (from PDF page 3, emitted in arco-rooms-runtime-artifacts.md):
- Gestor Cobros: 1 cron (monthly 1st 09:00 Europe/Madrid) + 1 webhook (Plaid payment-received) -> 2 wake.md files
- Recepcionista: 1 webhook (Telegram tenant-message) + 1 inter (receives SendMessage from Gestor Cobros) -> 2 wake.md files
- Gestor Documental: 1 cron (weekly Monday 08:00) + 1 webhook (Gmail new-invoice-email) -> 2 wake.md files

Twin references (structural patterns to copy):
- n8n-integration.md twin -> mcp-integration-protocol.md (imperative contract, worked examples, schema block)
- runtime-coordination.md twin -> browser-fallback.md (protocol prose with decision trees, fallback semantics)
- correlation-id.md twin -> audit-logging.md (format spec, propagation rules, grep recipes)
- wake-job-*.md.tmpl twin -> deployed-agent-skill-full.md.tmpl (pure {{var}} substitution, H2 section skeleton)
- arco-rooms-correlation-flow.md twin -> mapfre-discovery-report.md + arco-rooms-deploy-report.md (narrative fixture)
- arco-rooms-runtime-artifacts.md twin -> arco-rooms-registry.yaml (structural fixture)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `.claude/skills/agentbloc/references/n8n-integration.md` , event-bus contract documenting D-74 4-field JSON envelope + 5 worked examples + n8n route .json schema + runtime-agnostic fallback (structural twin of mcp-integration-protocol.md)</name>
  <files>.claude/skills/agentbloc/references/n8n-integration.md</files>
  <read_first>
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md (D-74 envelope shape; D-82 line budget 180-220; D-75 correlation-ID format for envelope.correlation_id field)
    - .planning/phases/13-multi-agent-runtime/13-RESEARCH.md (amendment: n8n route files use .json extension, not .yaml)
    - .claude/skills/agentbloc/references/mcp-integration-protocol.md (FULL file; PRIMARY structural twin; copy H1 + blockquote + TOC + When This Applies + imperative contract + worked examples grammar)
    - .claude/skills/agentbloc/references/telegram-patterns.md (for Telegram route worked example + /stop integration cross-reference)
    - .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml (for realistic agent_id + trigger.source values in worked examples)
    - /Users/pablodelarco/agentbloc/CLAUDE.md (zero em-dashes; no AI attribution)
  </read_first>
  <interfaces>
    <consumes>
      - agent-profiles.yaml triggers[] array (determines which n8n routes need to exist per deployed agent)
      - correlation-id.md format spec (envelope.correlation_id MUST match the D-75 format)
    </consumes>
    <provides>
      - Canonical 4-field JSON envelope schema that every n8n webhook MUST conform to
      - 5 worked examples (Gmail, Plaid, form submit, Google Calendar, Telegram) with literal route .json files
      - n8n route .json file schema at `.agentbloc/runtime/n8n-routes/<agent-id>.json`
      - Runtime-agnostic fallback pattern (HTTP listener + claude -p --payload-file for non-ClaudeClaw runtimes)
      - Bounded trigger.source enum: gmail | plaid | bbva | google-calendar | telegram | form | custom-<name>
    </provides>
  </interfaces>
  <action>
Create a NEW file at `.claude/skills/agentbloc/references/n8n-integration.md` (180-220 lines) with EXACTLY these sections in order. Structural twin of `references/mcp-integration-protocol.md`: copy H1 + blockquote + TOC + When This Applies + per-contract grammar. CRITICAL: zero em-dashes (U+2014). Use hyphen-minus + space or comma + rewrite. Use the Write tool, not heredoc.

**H1:** `# n8n Integration Contract`

**Blockquote (verbatim):** `> Phase 13 runtime reference. Every n8n webhook that wakes an AgentBloc agent conforms to the 4-field JSON envelope defined here. The envelope is the routing contract at the ClaudeClaw-boundary layer; event semantics are preserved inside the nested payload. Runtime-agnostic fallback (HTTP listener + claude -p) is documented for plain-Claude-Code deployments without ClaudeClaw.`

**TOC:** bullet list of the 10 sections below.

**Section 1 `## When This Applies`:** one paragraph. Prose: "Phase 13 runtime-engine subagent (`.claude/agents/runtime-engine.md`) loads this file in its forked context on invocation. The contract runs every time an agent's triggers[] array in agent-profiles.yaml contains a trigger.type of `event` (webhook). Phase 13 emits one n8n route .json stub per (agent, event-source, event-name) tuple at `.agentbloc/runtime/n8n-routes/<agent-id>.json`. The user installs these stubs into their n8n instance manually; Phase 13 does NOT auto-push routes. Loaded UNCONDITIONALLY at Phase 5 entry per D-58 context-budget discipline."

**Section 2 `## Envelope Schema (D-74)`:** the 4-field JSON envelope, verbatim:

```json
{
  "schema_version": 1,
  "correlation_id": "<trigger-source>-<UTC-Z>-<nonce6>",
  "agent_id": "<agent-id>",
  "trigger": {
    "source": "gmail | plaid | bbva | google-calendar | telegram | form | custom-<name>",
    "event_name": "<source-specific event name>",
    "received_at": "<ISO-8601 UTC>"
  },
  "payload": { /* event-source-specific body */ }
}
```

Prose after the block: "Four top-level fields are REQUIRED. `schema_version: 1` is an integer (matches D-22 three-tier obligation discipline). `correlation_id` conforms to the D-75 format (see `references/correlation-id.md`). `agent_id` is the routing key; n8n sets it based on the route's node configuration (one n8n webhook -> one agent by convention). `trigger.source` is a bounded enum (see Section 3). `payload` is event-source-specific; the schema is declared in the agent's n8n route .json file (Section 5)."

**Section 3 `## Bounded Enum: trigger.source`:** table of the 7 bounded values + description + example event_name for each.

| trigger.source | Description | Example event_name |
|----------------|-------------|--------------------|
| `gmail` | Gmail filter fires on incoming email matching criteria | `new-invoice-email` |
| `plaid` | Plaid webhook on bank-account event | `payment-received` |
| `bbva` | BBVA banking API webhook | `transaction-posted` |
| `google-calendar` | Google Calendar watch on calendar changes | `calendar-change` |
| `telegram` | Telegram bot receives message | `tenant-message` |
| `form` | Web form submit via HTTP POST | `contact-form-submission` |
| `custom-<name>` | Any other integration; `<name>` is kebab-case slug | `custom-zoho-invoice-sent` |

**Section 4 `## Worked Example 1: Gmail filter -> Gestor Documental`:** full envelope example + literal n8n route .json file.

Envelope (realistic sample):
```json
{
  "schema_version": 1,
  "correlation_id": "webhook-gmail-20260424T091523Z-b8c41e",
  "agent_id": "gestor-documental",
  "trigger": {
    "source": "gmail",
    "event_name": "new-invoice-email",
    "received_at": "2026-04-24T09:15:23Z"
  },
  "payload": {
    "message_id": "<synthetic-uuid>",
    "from": "billing@supplier.example",
    "subject": "Invoice #INV-2026-00412",
    "attachment_ids": ["att-1"]
  }
}
```

Route .json at `.agentbloc/runtime/n8n-routes/gestor-documental.json` (emitted by Phase 13 runtime-engine):
```json
{
  "schema_version": 1,
  "agent_id": "gestor-documental",
  "trigger": {
    "source": "gmail",
    "event_name": "new-invoice-email"
  },
  "n8n_config": {
    "webhook_path": "/webhook/gestor-documental-invoice",
    "http_method": "POST",
    "filter": "subject CONTAINS 'Invoice'"
  },
  "payload_schema": {
    "message_id": "string",
    "from": "string (email)",
    "subject": "string",
    "attachment_ids": "array of strings"
  },
  "evidence": {
    "verified_at": null
  }
}
```

**Section 5 `## Worked Example 2: Plaid webhook -> Gestor Cobros`:** same grammar as Section 4. trigger.source=plaid, event_name=payment-received, payload carries account_id + amount + transaction_id.

**Section 6 `## Worked Example 3: Web form submit -> Recepcionista`:** same grammar. trigger.source=form, event_name=contact-form-submission, payload carries form_data object + submitted_at.

**Section 7 `## Worked Example 4: Google Calendar watch -> Agente Agenda`:** same grammar. trigger.source=google-calendar, event_name=calendar-change. Add note: "Agente Agenda is an anticipated agent stubbed here for Phase 15 continuity; Phase 13 ships the route schema so Phase 15 needs zero changes to n8n-integration.md."

**Section 8 `## Worked Example 5: Telegram message -> Recepcionista`:** same grammar. trigger.source=telegram, event_name=tenant-message, payload carries chat_id + sender + text. Add note: "Telegram is also the transport for the v1.0 SECR-05 kill-switch remote-trigger (/stop command); see `references/incident-response.md` for the separate agentbloc-stop route YAML stub emitted by Phase 13 runtime-engine (distinct from this per-agent wake route)."

**Section 9 `## Runtime-Agnostic Fallback`:** prose + decision tree. Prose: "When ClaudeClaw is not the runtime substrate (plain Claude Code + system cron + n8n), AgentBloc degrades gracefully to a file-based fallback. The n8n HTTP node POSTs the envelope to a local HTTP listener (options: `python -m http.server` wrapper, `claudeclaw webhook --port 8080`, or a small Bun server). The listener writes the envelope to `.agentbloc/runtime/inbox/<agent-id>/<correlation-id>.json`, then invokes `claude -p --payload-file <path> .agentbloc/agents/<agent-id>/wake-webhook.md` as a foreground subprocess. This preserves the envelope shape (agents consume the same fields) and the correlation-ID chain (env var + payload file); what changes is the wake invocation mechanism, not the contract."

Decision tree:
```
IF registry.runtime.coordination_preference.prefer == "claudeclaw":
  n8n -> ClaudeClaw job endpoint -> claude -p
ELSE IF registry.runtime.coordination_preference.fallback == "writeStateHandoff":
  n8n -> local HTTP listener -> file inbox -> claude -p --payload-file
ELSE:
  halt: no runtime configured
```

Cross-reference to `references/runtime-coordination.md` for the full writeStateHandoff semantics.

**Section 10 `## Route File Format (.json, per RESEARCH amendment)`:** prose: "n8n route files ship at `.agentbloc/runtime/n8n-routes/<agent-id>.json`. The `.json` extension is chosen over `.yaml` because n8n's native export format is JSON, route files are machine-read and machine-compared by runtime-engine (JSON RFC 8785 canonicalization per D-60 is well-defined; YAML canonicalization is not), and the evidence.verified_at field is machine-updated when the user confirms a route is live. Schema is the inner object from the worked examples (schema_version + agent_id + trigger + n8n_config + payload_schema + evidence)."

**Section 11 `## Cross-References`:** links to `correlation-id.md` (Section 2 envelope.correlation_id), `runtime-coordination.md` (Section 9 writeStateHandoff fallback), `telegram-patterns.md` (Worked Example 5 Telegram transport), `incident-response.md` (agentbloc-stop Telegram /stop route, distinct from wake routes), `scheduling.md` (cron scheduling alternative when no webhook source exists).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/n8n-integration.md && test $(grep -c "—" .claude/skills/agentbloc/references/n8n-integration.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/references/n8n-integration.md) -ge 180 && test $(wc -l < .claude/skills/agentbloc/references/n8n-integration.md) -le 220 && grep -q "^# n8n Integration Contract" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "^## When This Applies" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "^## Envelope Schema" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "schema_version.*1" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "correlation_id" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "agent_id" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "trigger" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "payload" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "gmail" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "plaid" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "telegram" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "google-calendar" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "gestor-documental" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "gestor-cobros" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "recepcionista" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "writeStateHandoff" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "n8n-routes/.*\.json" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "correlation-id\.md" .claude/skills/agentbloc/references/n8n-integration.md && grep -q "runtime-coordination\.md" .claude/skills/agentbloc/references/n8n-integration.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.claude/skills/agentbloc/references/n8n-integration.md` exists
    - `grep -c "—" <file>` returns 0 (zero em-dashes)
    - `wc -l <file>` returns 180..220 inclusive
    - H1 `# n8n Integration Contract` on first line
    - Section headers present: When This Applies, Envelope Schema (D-74), Bounded Enum trigger.source, 5 Worked Examples (Gmail, Plaid, form, Google Calendar, Telegram), Runtime-Agnostic Fallback, Route File Format, Cross-References
    - Envelope schema contains all 4 required fields: schema_version, correlation_id, agent_id, trigger (with nested payload)
    - 5 worked examples present with realistic agent_id values (gestor-documental, gestor-cobros, recepcionista)
    - All 7 bounded trigger.source enum values cited: gmail, plaid, bbva, google-calendar, telegram, form, custom-<name>
    - writeStateHandoff fallback documented
    - n8n-routes/<agent-id>.json extension cited (.json, not .yaml; per RESEARCH amendment)
    - Cross-references to correlation-id.md and runtime-coordination.md present
  </acceptance_criteria>
  <done>
n8n-integration.md wired. Phase 13 runtime-engine subagent can load this file and emit conforming n8n route .json stubs. RUNTIME-02 (event triggers via n8n) and RUNTIME-03 (webhook-to-agent mapping documented) substantially covered at reference layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create `.claude/skills/agentbloc/references/runtime-coordination.md` , TeamCreate/SendMessage primitive contract + topology-to-primitive mapping + writeStateHandoff fallback + single-agent bypass + team dissolution semantics + crontab stdin install discipline (structural twin of browser-fallback.md)</name>
  <files>.claude/skills/agentbloc/references/runtime-coordination.md</files>
  <read_first>
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md (D-76 TeamCreate/SendMessage contract; D-77 kill-switch three-point enforcement; D-23 topology default mesh; D-24 ADK vocabulary; D-80 Bash narrow allow-list including stdin install form; D-82 line budget 180-220)
    - .planning/phases/13-multi-agent-runtime/13-RESEARCH.md (refinement: writeStateHandoff PRIMARY for non-interactive wakes; TeamCreate PRIMARY only for interactive leads)
    - .claude/skills/agentbloc/references/browser-fallback.md (FULL file; PRIMARY structural twin; copy protocol grammar + decision tree sections + halt semantics)
    - .claude/skills/agentbloc/references/scheduling.md (for pipeline spacing + DST safety cross-reference in topology-to-primitive mapping)
    - .claude/skills/agentbloc/references/orchestration-patterns.md (for 5-pattern table Sequential / Parallel / Loop / Event-driven / Conversational + ADK-naming verbatim)
    - .claude/skills/agentbloc/references/agent-profile-schema.md (for topology enum: pipeline / mesh / hierarchy / swarm)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - registry.yaml runtime block (workflows[].agents[] + spawn_rule + coordination_preference)
      - agent-profile-schema.md topology enum + triggers[] array
      - orchestration-patterns.md 5-pattern table for pattern-to-primitive mapping
    </consumes>
    <provides>
      - TeamCreate/SendMessage primitive invocation contract
      - Topology-to-primitive mapping table (pipeline / mesh / hierarchy / swarm -> primitive calls)
      - Single-agent bypass rule (workflows[].agents.length === 1 skips TeamCreate; length > 1 triggers it)
      - writeStateHandoff file-based fallback PRIMARY for non-interactive wakes per RESEARCH refinement
      - Team dissolution semantics: all-members-returned | kill-switch | timeout | error
      - Crontab stdin install form (NEVER crontab -e); 4-command Bash allow-list preview for runtime-engine
    </provides>
  </interfaces>
  <action>
Create a NEW file at `.claude/skills/agentbloc/references/runtime-coordination.md` (180-220 lines) with EXACTLY these sections in order. Structural twin of `references/browser-fallback.md`: copy H1 + blockquote + TOC + decision-tree grammar + halt semantics. CRITICAL: zero em-dashes. Use the Write tool.

**H1:** `# Runtime Coordination Contract`

**Blockquote (verbatim):** `> Phase 13 runtime reference. Defines how deployed agents coordinate with each other when workflows.agents.length > 1. Primitive contract: ClaudeClaw TeamCreate + SendMessage for interactive leads; writeStateHandoff file-based fallback for non-interactive wakes and runtime-agnostic deployments. Single-agent workflows bypass TeamCreate entirely at template-selection time per RUNTIME-05. Crontab mutations use the stdin install form exclusively; crontab -e is explicitly disallowed because it launches an interactive editor that hangs forked subagents.`

**TOC:** bullet list of the 10 sections below.

**Section 1 `## When This Applies`:** one paragraph. Prose: "Phase 13 runtime-engine subagent (`.claude/agents/runtime-engine.md`) loads this file in its forked context on invocation. The contract runs every time an agent's workflow classification in registry.yaml workflows[<workflow-id>] declares agents.length > 1 (multi-agent workflow) OR the agent detects a dependency at runtime. Single-agent workflows (agents.length === 1) skip this contract entirely; the wake template invokes the agent's SKILL.md directly. Loaded UNCONDITIONALLY at Phase 5 entry per D-58 context-budget discipline."

**Section 2 `## Single-Agent Bypass (RUNTIME-05)`:** prose + code block.

Prose: "Per RUNTIME-05, single-agent tasks run without TeamCreate overhead. Enforcement is at the wake-template selection level (runtime-engine picks `wake-job-cron.md.tmpl` or `wake-job-webhook.md.tmpl` for workflows with agents.length === 1; picks `wake-job-inter.md.tmpl` or embeds a TeamCreate call in the cron/webhook template for agents.length > 1). The check is deterministic: runtime-engine reads `registry.runtime.workflows[<workflow-id>].agents` and dispatches the template accordingly. No runtime conditional branching."

Pseudocode:
```
workflow = registry.runtime.workflows[workflow_id]
if workflow.agents.length == 1:
  # Bypass: direct wake, no TeamCreate
  template = wake-job-<trigger-type>.md.tmpl
  materialize(template, agent=workflow.agents[0])
else:  # length > 1
  if workflow.spawn_rule == "declared":
    # First agent wakes, immediately TeamCreate
    lead = workflow.agents[0]
    template = wake-job-<trigger-type>.md.tmpl (with TeamCreate section embedded)
    materialize(template, agent=lead, roster=workflow.agents)
  elif workflow.spawn_rule == "dynamic":
    # Any agent may detect need and call TeamCreate
    template = wake-job-inter.md.tmpl (for re-entry on SendMessage)
    materialize(template, agent=each, dependencies=agent.dependencies)
```

**Section 3 `## Multi-Agent Workflow Invocation (D-76)`:** prose describing PDF page 5 "primer agente que detecta spawna el equipo" pattern.

Cases:
- **Declared roster (spawn_rule: declared):** The first agent in `workflow.agents[]` (the lead per D-23 topology selection) wakes via cron or webhook, immediately issues `TeamCreate(agents=workflow.agents, correlation_id=<ID>)`, and coordinates via `SendMessage` from then on. The correlation_id is propagated verbatim into TeamCreate metadata and every subsequent SendMessage call.
- **Dynamic detection (spawn_rule: dynamic):** Any agent during execution may detect a dependency via its SKILL.md `dependencies` block + the incoming payload shape. When detected, the agent calls `TeamCreate([self, detected-agent, ...transitive-deps])` with a new sub-correlation-ID (parent ID + `-sub-<NNN>` per audit-logging.md).

Primitive call signatures (ClaudeClaw docs, subject to verification in Plan 13-02 if signature drifts):
```
TeamCreate(agents: string[], correlation_id: string, metadata: {...}) -> team_id
SendMessage(to: agent-id, body: object, metadata: { correlation_id: string }) -> message_id
```

**Section 4 `## Topology-to-Primitive Mapping (D-23 + D-24)`:** table from PDF page 5 orchestration patterns + topology enum.

| Topology | Orchestration Pattern (ADK) | Primitive Invocation |
|----------|-----------------------------|----------------------|
| pipeline | Sequential | Staggered cron (scheduling.md pipeline-spacing 30-min gaps); NO TeamCreate |
| mesh (default per D-23) | Parallel or Conversational | TeamCreate + SendMessage fan-out |
| hierarchy | Event-driven | Lead spawns TeamCreate; children SendMessage to lead only |
| swarm | Conversational | TeamCreate + peer SendMessage (any agent talks to any agent) |

Cross-reference to `orchestration-patterns.md` for the 5-pattern Sequential / Parallel / Loop / Event-driven / Conversational table.

**Section 5 `## writeStateHandoff Fallback (PRIMARY for non-interactive wakes per RESEARCH refinement)`:** prose + file-flow diagram.

Prose: "Per Phase 13 RESEARCH refinement, writeStateHandoff is the PRIMARY coordination mechanism for non-interactive wakes (cron-triggered agents that need to coordinate; webhook-triggered agents without interactive lead context). TeamCreate is PRIMARY only for interactive leads (an agent handling a Telegram conversation that dynamically detects a dependency; the human is in the loop). This refinement applies because ClaudeClaw's TeamCreate primitive is session-scoped; cron wakes have no session to attach to, so a file-based handoff is both simpler and more debuggable."

File flow (writeStateHandoff):
```
Agent A (non-interactive wake, e.g., cron)
  -> Writes handoff payload to .agentbloc/agents/<agent-b>/inbox/<correlation-id>.json
  -> Invokes: claude -p --payload-file <path> .agentbloc/agents/<agent-b>/wake-inter.md
     as foreground subprocess (synchronous, blocks until Agent B exits)
Agent B (wake-inter.md)
  -> Reads inbox/<correlation-id>.json
  -> Processes per SKILL.md
  -> Writes response to .agentbloc/agents/<agent-a>/inbox/<correlation-id>-reply.json
  -> Exits
Agent A resumes
  -> Reads reply
  -> Continues execution
```

Trade-offs:
- writeStateHandoff: debuggable (files on disk; manually inspectable), runtime-agnostic (works on plain Claude Code), no concurrency (sequential only)
- TeamCreate: concurrent (parallel agent execution), ClaudeClaw-only (primitive is not available in plain Claude Code)

**Section 6 `## Team Dissolution Semantics (D-77)`:** bounded enum from D-18 discipline.

`team_dissolution_reason` enum (one of 4 values):
- `all-members-returned` (happy path; every member agent has returned a non-continuation output via final state.json write)
- `kill-switch` (D-77 three-point enforcement fires; see `references/incident-response.md` Runtime Kill-Switch Semantics)
- `timeout` (correlation-ID-scoped timeout exceeds `registry.runtime.team_timeout_minutes`; default 15 minutes)
- `error` (unrecoverable error in any member; team-wide halt; failure logged with correlation_id and bubbled to lead for v1.0 SECR escalation)

Dissolution is logged to `.agentbloc/runtime/TEAM_SESSIONS.jsonl` per D-46 append-only ledger discipline.

**Section 7 `## Kill-Switch Integration (D-77)`:** prose documenting the three-point enforcement inside multi-agent workflows.

Prose: "Per D-77, kill-switch is checked at 3 points per agent wake: (1) top of wake.md before any reads, (2) before every side-effect tool call via PreToolUse hook (Phase 12 artifact), (3) at every state transition within a TeamCreate session. Inside a multi-agent workflow, the team-transition check (#3) is the load-bearing semantic: every agent checks `.agentbloc/KILL_SWITCH` before SendMessage send AND before SendMessage consume. If active, the agent returns `{status: halted-kill-switch}` and the team lead dissolves the team via explicit TeamCreate teardown. Worst-case latency is one SendMessage round-trip (typically under 5 seconds). See `references/incident-response.md` Runtime Kill-Switch Semantics for the full protocol."

**Section 8 `## Crontab Stdin Install Discipline (D-80)`:** prose + pseudocode.

Prose: "Phase 13 runtime-engine subagent installs crontab entries via the stdin form EXCLUSIVELY. `crontab -e` is EXPLICITLY DISALLOWED because it launches an interactive editor (`$EDITOR`) that hangs a forked subagent waiting for user input. Scripted install uses the stdin form verbatim:"

```bash
(crontab -l 2>/dev/null; cat .agentbloc/runtime/crontab.applied) | crontab -
```

Bash allow-list for runtime-engine (preview; full spec in D-80 and Plan 13-02):
- `crontab -` (stdin install; the `-` suffix reads from stdin)
- `crontab -l` (list current crontab; for diff presentation)
- `shasum -a 256` (fingerprint computation for crontab.applied manifest)
- `claude agents list` (verification: every deployed agent registered)
- `claude mcp list` (verification: every MCP server declared)

Disallowed (explicit NEVER): `crontab -e` (interactive editor), `bash -c` (arbitrary shell), `sh` (arbitrary shell), `curl` (network egress), `rm -rf` (destructive wildcards).

**Section 9 `## Coordination Preference in Registry (D-78)`:** prose + yaml block showing the coordination_preference field.

```yaml
runtime:
  coordination_preference:
    prefer: "claudeclaw"  # or "writeStateHandoff"
    fallback: "writeStateHandoff"
```

Rules:
- When `prefer: claudeclaw`, runtime-engine emits TeamCreate calls in the wake-job-inter.md.tmpl output; writeStateHandoff is the fallback if TeamCreate throws
- When `prefer: writeStateHandoff`, runtime-engine skips TeamCreate entirely and materializes writeStateHandoff semantics in the wake-job-inter.md.tmpl output
- When both are absent, runtime-engine defaults to `prefer: writeStateHandoff` per RESEARCH refinement

**Section 10 `## Cross-References`:** links to `n8n-integration.md` (event-bus contract for webhook-triggered multi-agent workflows), `correlation-id.md` (propagation through TeamCreate metadata + SendMessage metadata), `incident-response.md` (kill-switch three-point enforcement prose), `orchestration-patterns.md` (5-pattern table), `scheduling.md` (pipeline spacing for Sequential topology), `agent-profile-schema.md` (topology enum + dependencies[] for dynamic spawn_rule).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/runtime-coordination.md && test $(grep -c "—" .claude/skills/agentbloc/references/runtime-coordination.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/references/runtime-coordination.md) -ge 180 && test $(wc -l < .claude/skills/agentbloc/references/runtime-coordination.md) -le 220 && grep -q "^# Runtime Coordination Contract" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "workflows\[\].agents.length > 1" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "writeStateHandoff" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "TeamCreate" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "SendMessage" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "all-members-returned" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "kill-switch" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "timeout" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "pipeline" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "mesh" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "hierarchy" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "swarm" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "crontab -$" .claude/skills/agentbloc/references/runtime-coordination.md && ! grep -E '\bcrontab -e\b(?!.*DISALLOWED)' .claude/skills/agentbloc/references/runtime-coordination.md > /dev/null 2>&1 || grep -q "crontab -e" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "DISALLOWED" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "correlation-id\.md" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "n8n-integration\.md" .claude/skills/agentbloc/references/runtime-coordination.md && grep -q "incident-response\.md" .claude/skills/agentbloc/references/runtime-coordination.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "—"` returns 0; `wc -l` 180..220
    - H1 `# Runtime Coordination Contract` on first line
    - Section headers: When This Applies, Single-Agent Bypass, Multi-Agent Workflow Invocation, Topology-to-Primitive Mapping, writeStateHandoff Fallback, Team Dissolution Semantics, Kill-Switch Integration, Crontab Stdin Install Discipline, Coordination Preference in Registry, Cross-References
    - Contains literal string `workflows[].agents.length > 1` (used for grep verify)
    - Contains literal string `writeStateHandoff`
    - Contains all 4 dissolution reasons: all-members-returned, kill-switch, timeout, error
    - Contains all 4 topology enum values: pipeline, mesh, hierarchy, swarm
    - Crontab stdin install form `crontab -` (end of line) present
    - `crontab -e` appears in DISALLOWED context (grep verifies NOT present in a recommended form)
    - Cross-references to correlation-id.md, n8n-integration.md, incident-response.md present
  </acceptance_criteria>
  <done>
runtime-coordination.md wired. TeamCreate/SendMessage contract documented; writeStateHandoff PRIMARY for non-interactive wakes per RESEARCH; single-agent bypass enforced at template level; topology-to-primitive mapping; team dissolution semantics; crontab stdin install discipline. RUNTIME-04 + RUNTIME-05 + RUNTIME-07 substantially covered at reference layer.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create `.claude/skills/agentbloc/references/correlation-id.md` , D-75 correlation-ID format spec + 3 propagation channels + child sub-ID convention + grep recipes (structural twin of audit-logging.md)</name>
  <files>.claude/skills/agentbloc/references/correlation-id.md</files>
  <read_first>
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md (D-75 format spec; D-82 line budget 120-150)
    - .planning/phases/13-multi-agent-runtime/13-RESEARCH.md (6-hex nonce math: 16^6 = 16.7M collision space per second per source)
    - .claude/skills/agentbloc/references/audit-logging.md (FULL file; PRIMARY structural twin; copy format spec + propagation rules + grep recipes grammar; child sub-ID convention inherited verbatim)
    - .claude/skills/agentbloc/references/telegram-patterns.md (for Telegram message header correlation-ID propagation example)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - audit-logging.md correlation_id field pattern + sub-<NNN> child rule (inherited)
    </consumes>
    <provides>
      - D-75 format: `<trigger-source>-<UTC-Z-compact>-<nonce6>` + regex
      - Bounded trigger-source enum: cron | webhook-<source> | telegram | inter | manual
      - 3 propagation channels (env var + JSON payload + SendMessage metadata)
      - Child-ID sub-<NNN> append rule (zero-padded 3 digits; supports up to 999 children per parent)
      - At least 4 grep recipes for end-to-end tracing
      - 6-hex nonce math (16^6 = 16.7M collision space) + v2.5 SQLite scalability note
    </provides>
  </interfaces>
  <action>
Create a NEW file at `.claude/skills/agentbloc/references/correlation-id.md` (120-150 lines) with EXACTLY these sections in order. Structural twin of `references/audit-logging.md`. CRITICAL: zero em-dashes. Use the Write tool.

**H1:** `# Correlation ID Format + Propagation`

**Blockquote (verbatim):** `> Phase 13 runtime reference. Every trigger seeds a new correlation ID; the ID rides through SendMessage metadata, TeamCreate team metadata, JSON webhook payloads, env vars at claude -p invocation, and every downstream log line. A single user event is grep-traceable end-to-end via one correlation ID.`

**TOC:** bullet list of the 8 sections below.

**Section 1 `## When This Applies`:** one paragraph. Prose: "Phase 13 runtime-engine subagent loads this file in its forked context on invocation. Phase 13 wake-job templates cite this file in Section 2 (Correlation-ID ingest) and Section 6 (State + log write) for format compliance. Phase 14 Monitor (MONITOR-01..06) will consume correlation IDs from the JSONL logs; this file is the single source of truth for the format. Loaded UNCONDITIONALLY at Phase 5 entry per D-58 context-budget discipline."

**Section 2 `## Format Spec (D-75)`:** format definition + regex + 5 examples.

Format: `<trigger-source>-<UTC-Z-compact>-<nonce6>`

- `<trigger-source>`: bounded enum (see Section 3)
- `<UTC-Z-compact>`: ISO-8601 UTC with Z suffix, colons and dashes stripped (e.g., `20260424T091523Z`)
- `<nonce6>`: 6 hex chars from a cryptographic RNG at ID-generation time

Regex (verbatim):
```
^(cron|webhook-[a-z][a-z0-9-]*|telegram|inter|manual)-[0-9]{8}T[0-9]{6}Z-[a-f0-9]{6}(-sub-[0-9]{3})*$
```

5 examples (verbatim):
```
cron-20260424T090000Z-a3f21b
webhook-plaid-20260424T091523Z-b8c41e
webhook-telegram-20260424T092045Z-c7d92a
inter-20260424T093101Z-f0e82a
manual-20260424T094512Z-02db9c
```

**Section 3 `## Bounded Enum: trigger-source`:** table.

| trigger-source | When used | Example |
|----------------|-----------|---------|
| `cron` | System cron fires a scheduled agent wake | `cron-20260424T090000Z-a3f21b` |
| `webhook-<source>` | n8n webhook fires; `<source>` matches the n8n route trigger.source enum (gmail, plaid, bbva, google-calendar, telegram, form, custom-<name>) | `webhook-plaid-20260424T091523Z-b8c41e` |
| `telegram` | Short alias for `webhook-telegram` when the Telegram message path is used (either form is valid) | `telegram-20260424T092045Z-c7d92a` |
| `inter` | Agent-to-agent invocation via SendMessage | `inter-20260424T093101Z-f0e82a` |
| `manual` | `claude -p` invoked directly by the user without any trigger system | `manual-20260424T094512Z-02db9c` |

**Section 4 `## Propagation Channels (3 mechanisms)`:** prose + 3 subsections.

**Channel 1: Env var `AGENTBLOC_CORRELATION_ID`**

The `claude -p` wrapper receives `AGENTBLOC_CORRELATION_ID=<id>` as an environment variable. Cron entries set it via `AGENTBLOC_CORRELATION_ID=$(agentbloc-gen-correlation cron)` where `agentbloc-gen-correlation` is a shell function that runtime-engine emits to `.agentbloc/runtime/helpers.sh`. The function:
```bash
agentbloc-gen-correlation() {
  local source="${1:-manual}"
  local ts=$(date -u +%Y%m%dT%H%M%SZ)
  local nonce=$(od -An -N3 -tx1 /dev/urandom | tr -d ' \n')
  echo "${source}-${ts}-${nonce}"
}
```

**Channel 2: JSON payload top-level field**

For webhook triggers, n8n's Set node seeds `correlation_id` into the D-74 envelope (see `references/n8n-integration.md`). The envelope carries it as a top-level field; the agent's wake-job-webhook.md.tmpl reads it via `{{payload.correlation_id}}` during Section 2 correlation-ID ingest.

**Channel 3: SendMessage metadata**

ClaudeClaw's `SendMessage` primitive carries a `metadata` field. `references/runtime-coordination.md` specifies `metadata.correlation_id` is MANDATORY for AgentBloc-generated SendMessage calls. The receiving agent's wake-job-inter.md.tmpl reads it via `{{message.metadata.correlation_id}}`. For writeStateHandoff fallback, the correlation_id rides in the inbox file's JSON body.

**Section 5 `## Child-ID Convention (inherited from audit-logging.md)`:** prose + examples.

When agent A spawns child B via SendMessage or sub-session, B's correlation ID is `<parent-id>-sub-<NNN>` where `NNN` is zero-padded to 3 digits. Supports up to 999 children per parent, which exceeds any realistic fan-out.

Examples:
```
Parent: webhook-telegram-20260424T143212Z-c7d92a
Child 1: webhook-telegram-20260424T143212Z-c7d92a-sub-001
Child 2: webhook-telegram-20260424T143212Z-c7d92a-sub-002
Grandchild: webhook-telegram-20260424T143212Z-c7d92a-sub-001-sub-001
```

Cross-reference to `references/audit-logging.md` for the original `sess-<agent>-<NNN>` pattern this extends.

**Section 6 `## Grep Recipes`:** at least 4 recipes for end-to-end tracing.

Recipe 1 - Trace one user event end-to-end:
```bash
grep 'correlation_id":"webhook-plaid-20260424T091523Z-b8c41e' .agentbloc/logs/audit.jsonl
```

Recipe 2 - List all wakes for a given trigger source on a given day:
```bash
grep 'correlation_id":"webhook-plaid-20260424' .agentbloc/logs/audit.jsonl | jq -r '.agent_id' | sort -u
```

Recipe 3 - Team session outcome for a correlation ID:
```bash
grep '"correlation_id":"cron-20260424T090000Z-a3f21b"' .agentbloc/runtime/TEAM_SESSIONS.jsonl
```

Recipe 4 - All child IDs under a parent (for multi-agent fan-out inspection):
```bash
grep -E 'correlation_id":"webhook-telegram-20260424T143212Z-c7d92a(-sub-[0-9]{3})*"' .agentbloc/logs/audit.jsonl
```

Recipe 5 - Count wake events per agent per day (operational summary):
```bash
grep 'correlation_id":"(cron|webhook|inter)-20260424' .agentbloc/logs/audit.jsonl | jq -r '.agent_id' | sort | uniq -c
```

**Section 7 `## Scalability + Math`:** prose.

Nonce space: 16^6 = 16,777,216 combinations per second per source. At v2.0 scale (3 to 30 agents; at most 1 wake per second per trigger), collision probability is negligible. The 6-hex length was chosen as the smallest power-of-2 hex count that guarantees grep-friendly fixed-width tokens while remaining shell-safe.

v2.5 SQLite migration note: When JSONL log scan becomes sluggish (roughly 1M entries, or 3 to 6 months of high-traffic runtime), `.agentbloc/logs/*.jsonl` will migrate to SQLite with correlation_id as the primary-key column. The format in this file remains stable across the migration; only storage changes. See `.planning/REQUIREMENTS.md` Deferred to v2.5+ "SQLite persistence for log search".

**Section 8 `## Cross-References`:** links to `audit-logging.md` (original sess-<agent>-<NNN> pattern this extends), `n8n-integration.md` (envelope.correlation_id field), `runtime-coordination.md` (SendMessage metadata.correlation_id contract), `incident-response.md` (halted-kill-switch log entries include correlation_id for forensic tracing).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/correlation-id.md && test $(grep -c "—" .claude/skills/agentbloc/references/correlation-id.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/references/correlation-id.md) -ge 120 && test $(wc -l < .claude/skills/agentbloc/references/correlation-id.md) -le 150 && grep -q "^# Correlation ID" .claude/skills/agentbloc/references/correlation-id.md && grep -q "AGENTBLOC_CORRELATION_ID" .claude/skills/agentbloc/references/correlation-id.md && grep -q "cron-20260424T090000Z" .claude/skills/agentbloc/references/correlation-id.md && grep -q "webhook-plaid-20260424" .claude/skills/agentbloc/references/correlation-id.md && grep -q "sub-" .claude/skills/agentbloc/references/correlation-id.md && grep -q "16" .claude/skills/agentbloc/references/correlation-id.md && grep -q "audit-logging\.md" .claude/skills/agentbloc/references/correlation-id.md && grep -q "n8n-integration\.md" .claude/skills/agentbloc/references/correlation-id.md && grep -q "runtime-coordination\.md" .claude/skills/agentbloc/references/correlation-id.md && python3 -c "import re; r=re.compile(r'^(cron|webhook-[a-z][a-z0-9-]*|telegram|inter|manual)-[0-9]{8}T[0-9]{6}Z-[a-f0-9]{6}(-sub-[0-9]{3})*$'); assert r.match('cron-20260501T080000Z-a3f21b'); assert r.match('webhook-telegram-20260504T143212Z-c7d92a-sub-001'); assert not r.match('bogus')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "—"` returns 0; `wc -l` 120..150
    - H1 `# Correlation ID Format + Propagation` on first line
    - Section headers present: When This Applies, Format Spec, Bounded Enum trigger-source, Propagation Channels, Child-ID Convention, Grep Recipes, Scalability + Math, Cross-References
    - Format regex present and valid (validated by python3 compile + match tests)
    - All 5 bounded trigger-source enum values cited (cron, webhook-<source>, telegram, inter, manual)
    - All 3 propagation channels documented (env var, JSON payload, SendMessage metadata)
    - At least 4 grep recipes present
    - 6-hex nonce math cited (16^6 = 16.7M-class claim)
    - v2.5 SQLite migration note present
    - Cross-references to audit-logging.md, n8n-integration.md, runtime-coordination.md present
  </acceptance_criteria>
  <done>
correlation-id.md wired. D-75 format spec fixed; 3 propagation channels documented; 4+ grep recipes for mobile-first debugging; v2.5 scalability note. RUNTIME-06 substantially covered at reference layer.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create `.claude/skills/agentbloc/templates/wake-job-cron.md.tmpl` , D-73 6-section cron-trigger wake template with kill-switch pre-check at section 1 (structural twin of deployed-agent-skill-full.md.tmpl)</name>
  <files>.claude/skills/agentbloc/templates/wake-job-cron.md.tmpl</files>
  <read_first>
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md (D-73 6-section structure; D-77 kill-switch pre-check at top of wake.md; D-82 line budget 80-100)
    - .claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl (FULL file; PRIMARY structural twin for pure {{var}} substitution discipline; copy template header comment + anchor-point placement)
    - .planning/phases/13-multi-agent-runtime/13-VALIDATION.md Task 13-01-05 (section count verification: exactly 6 numbered sections) + Task 13-01-06 (kill-switch presence verification)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - Template anchor points: {{agent.id}}, {{agent.role}}, {{agent.autonomy}}, {{agent.skill_path}}, {{agent.memory_dir}}, {{team.correlation_prefix}}, {{cron.schedule}}
    </consumes>
    <provides>
      - Materialized wake-cron.md per agent when runtime-engine dispatches cron template
      - Kill-switch pre-check at section 1 (D-77 enforcement point #1)
    </provides>
  </interfaces>
  <action>
Create a NEW file at `.claude/skills/agentbloc/templates/wake-job-cron.md.tmpl` (80-100 lines) with EXACTLY 6 numbered H2 sections. Pure `{{var}}` substitution only; ZERO `{% if %}` / `{% for %}` / `{% else %}` / `{% endfor %}` / `{% endif %}` directives. CRITICAL: zero em-dashes. Use the Write tool.

Template body (verbatim skeleton; agent-specific anchors substitute at runtime-engine materialize time):

```
<!-- agentbloc wake-job template: cron trigger -->
<!-- schema_version: 1 -->
<!-- anchor points: {{agent.id}}, {{agent.role}}, {{agent.autonomy}}, {{agent.skill_path}}, {{agent.memory_dir}}, {{team.correlation_prefix}}, {{cron.schedule}} -->

# Wake Job: {{agent.id}} (cron)

You are waking to execute {{agent.role}} per the cron schedule `{{cron.schedule}}`.

## 1. Kill-switch pre-check

Check if `.agentbloc/KILL_SWITCH` exists.
- If YES: Append this entry to `.agentbloc/logs/audit.jsonl`:
  `{"correlation_id":"<ID>","event":"halted-kill-switch","agent_id":"{{agent.id}}","wake_at":"<now UTC Z>","trigger":"cron"}`
  EXIT IMMEDIATELY. Do not read state, do not call tools, do not emit Telegram.
- If NO: Continue to section 2.

## 2. Correlation-ID ingest

Read the `AGENTBLOC_CORRELATION_ID` environment variable set by the cron entry.
- If present: use it as `<ID>` for this wake.
- If missing: generate via the D-75 format `cron-<UTC-Z-compact>-<nonce6>` using `.agentbloc/runtime/helpers.sh agentbloc-gen-correlation cron`. Tag with `correlation_id_source: self-generated` in the log entry (indicates orphan wake worth investigating).

See `.claude/skills/agentbloc/references/correlation-id.md` for the full format + propagation spec.

## 3. Memory + state read

BEFORE any other work, load:
- `{{agent.memory_dir}}/memory.md` (section-headed Domain Knowledge + Decisions + Integration Quirks + Open Items; agent-editable markdown)
- `{{agent.memory_dir}}/state.json` (schema_version + working_state + processed_ids[] + locks[] + retries[])

These two files are the authoritative state for {{agent.id}}. Do not proceed without loading both.

## 4. Input parse

For cron triggers, derive input from `state.working_state`. Typical fields (examples; actual shape is agent-specific):
- `month_to_process` (e.g., "2026-04")
- `last_processed_id` (cursor for incremental processing)
- `pending_retries[]` (carry-over from prior wake's retries)

NO payload is provided on cron wake; everything is state-derived.

## 5. Execute

Load `{{agent.skill_path}}` and proceed per the agent's prompt. Include the correlation ID `<ID>` in every log call AND in every tool invocation that propagates it (e.g., Telegram messages sent by this agent carry the correlation ID in the message header for human grep).

Autonomy: `{{agent.autonomy}}`. The SKILL.md template already encodes autonomy-appropriate language per Phase 12 D-62. No autonomy branching at this layer.

## 6. State + log write

After execution:
- Update `{{agent.memory_dir}}/state.json`: advance `working_state` cursors, append any new `processed_ids`, record any new `retries`.
- Update `{{agent.memory_dir}}/last-run.json`: `{action, result, timestamp, status}` per agent-memory-schema.md.
- Append one JSONL line to `.agentbloc/logs/audit.jsonl`: `{correlation_id, agent_id: {{agent.id}}, event: wake-completed, wake_outcome, timing_ms, trigger: cron}`.

`wake_outcome` enum (per D-18 bounded discipline): `completed | failed | halted-kill-switch | halted-upstream-failure`.

End of wake.
```

Ensure `grep -c "^## [1-6]\. "` returns exactly 6 (verifiable via VALIDATION.md Task 13-01-05).
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl && test $(grep -c "—" .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl) -eq 0 && test $(wc -l < .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl) -ge 80 && test $(wc -l < .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl) -le 100 && test $(grep -cE "^## [1-6]\. " .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl) -eq 6 && grep -q ".agentbloc/KILL_SWITCH" .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl && grep -q "{{agent.id}}" .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl && grep -q "{{agent.role}}" .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl && grep -q "{{agent.skill_path}}" .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl && grep -q "{{agent.memory_dir}}" .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl && grep -q "AGENTBLOC_CORRELATION_ID" .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl && ! grep -q "{% if" .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl && ! grep -q "{% for" .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "—"` returns 0; `wc -l` 80..100
    - Exactly 6 numbered H2 sections (grep -cE "^## [1-6]\. " returns 6)
    - Section 1 is Kill-switch pre-check and contains `.agentbloc/KILL_SWITCH`
    - Section 2 is Correlation-ID ingest and contains `AGENTBLOC_CORRELATION_ID`
    - Section 3 is Memory + state read
    - Section 4 is Input parse
    - Section 5 is Execute
    - Section 6 is State + log write
    - Anchor points present: {{agent.id}}, {{agent.role}}, {{agent.skill_path}}, {{agent.memory_dir}}
    - NO `{% if` or `{% for` directives present (pure {{var}} substitution per D-62 discipline)
  </acceptance_criteria>
  <done>
wake-job-cron.md.tmpl wired. runtime-engine subagent (Plan 13-02) can load + materialize per-agent wake.md files for cron triggers. Kill-switch pre-check at section 1 satisfies D-77 enforcement point #1 + RUNTIME-07 at wake-time.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create `.claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl` , D-73 6-section webhook-trigger wake template with section 4 specialized for payload parse per D-74 envelope (structural twin of wake-job-cron.md.tmpl with webhook specialization)</name>
  <files>.claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl</files>
  <read_first>
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md (D-73 6-section; D-74 envelope fields for section 4 payload parse; D-77 kill-switch at section 1)
    - .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl (produced Task 4; sibling skeleton; sections 1+2+3+5+6 identical; only section 4 differs)
    - .claude/skills/agentbloc/templates/deployed-agent-skill-full.md.tmpl (FULL file; structural twin for pure {{var}} substitution discipline)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - Same anchor points as wake-job-cron.md.tmpl PLUS {{trigger.source}}, {{trigger.event_name}}, {{payload.schema}}
      - D-74 envelope shape (correlation_id, agent_id, trigger, payload)
    </consumes>
    <provides>
      - Materialized wake-webhook-<source>-<event>.md per (agent, event-source, event-name) tuple
      - Payload parse in section 4 per envelope schema
    </provides>
  </interfaces>
  <action>
Create a NEW file at `.claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl` (80-100 lines) with EXACTLY 6 numbered H2 sections. Identical skeleton to wake-job-cron.md.tmpl EXCEPT:
- Title reflects webhook trigger
- Intro prose mentions `{{trigger.source}}` and `{{trigger.event_name}}`
- Section 2 reads correlation_id from the D-74 envelope payload FIRST, env var SECOND (webhook path precedence)
- Section 4 parses the payload per the declared schema
- Section 6 logs with `trigger: webhook-{{trigger.source}}`

Pure `{{var}}` substitution only; ZERO `{% if %}` / `{% for %}`. CRITICAL: zero em-dashes. Use the Write tool.

Template body:

```
<!-- agentbloc wake-job template: webhook trigger -->
<!-- schema_version: 1 -->
<!-- anchor points: {{agent.id}}, {{agent.role}}, {{agent.autonomy}}, {{agent.skill_path}}, {{agent.memory_dir}}, {{team.correlation_prefix}}, {{trigger.source}}, {{trigger.event_name}}, {{payload.schema}} -->

# Wake Job: {{agent.id}} (webhook: {{trigger.source}}/{{trigger.event_name}})

You are waking to execute {{agent.role}} in response to a {{trigger.source}} event (`{{trigger.event_name}}`).

## 1. Kill-switch pre-check

Check if `.agentbloc/KILL_SWITCH` exists.
- If YES: Append this entry to `.agentbloc/logs/audit.jsonl`:
  `{"correlation_id":"<ID>","event":"halted-kill-switch","agent_id":"{{agent.id}}","wake_at":"<now UTC Z>","trigger":"webhook-{{trigger.source}}"}`
  EXIT IMMEDIATELY. Do not read state, do not call tools, do not emit Telegram.
- If NO: Continue to section 2.

## 2. Correlation-ID ingest

Read the incoming webhook envelope (the payload file passed via `claude -p --payload-file <path>` or the `AGENTBLOC_CORRELATION_ID` env var).
- From envelope: extract `correlation_id` top-level field (per D-74 envelope schema).
- From env var: fallback if envelope parse fails.
- If both missing: generate via the D-75 format `webhook-{{trigger.source}}-<UTC-Z-compact>-<nonce6>` using `.agentbloc/runtime/helpers.sh agentbloc-gen-correlation webhook-{{trigger.source}}`. Tag with `correlation_id_source: self-generated`.

See `.claude/skills/agentbloc/references/correlation-id.md` for format spec + `.claude/skills/agentbloc/references/n8n-integration.md` for the D-74 envelope shape.

## 3. Memory + state read

BEFORE any other work, load:
- `{{agent.memory_dir}}/memory.md`
- `{{agent.memory_dir}}/state.json`

These are the authoritative state for {{agent.id}}. Do not proceed without loading both.

## 4. Input parse

Parse the D-74 envelope payload. Expected shape (declared by the n8n route file at `.agentbloc/runtime/n8n-routes/{{agent.id}}.json`):

```
{{payload.schema}}
```

Validate that:
- `schema_version` equals 1
- `agent_id` equals `{{agent.id}}` (reject if mismatched; log `routing-mismatch` event + halt)
- `trigger.source` equals `{{trigger.source}}`
- `trigger.event_name` equals `{{trigger.event_name}}`
- `payload` matches the declared schema

On validation failure, append `{"correlation_id":"<ID>","event":"payload-invalid","agent_id":"{{agent.id}}","reason":"<specific>"}` to `.agentbloc/logs/audit.jsonl` and EXIT with `wake_outcome: halted-upstream-failure`.

## 5. Execute

Load `{{agent.skill_path}}` and proceed per the agent's prompt. Pass the parsed payload as the primary input. Include the correlation ID `<ID>` in every log call AND in every tool invocation that propagates it.

Autonomy: `{{agent.autonomy}}`. The SKILL.md template already encodes autonomy-appropriate language per Phase 12 D-62.

## 6. State + log write

After execution:
- Update `{{agent.memory_dir}}/state.json`: advance `working_state` cursors, append any new `processed_ids`, record any new `retries`.
- Update `{{agent.memory_dir}}/last-run.json`: `{action, result, timestamp, status}` per agent-memory-schema.md.
- Append one JSONL line to `.agentbloc/logs/audit.jsonl`: `{correlation_id, agent_id: {{agent.id}}, event: wake-completed, wake_outcome, timing_ms, trigger: webhook-{{trigger.source}}}`.

`wake_outcome` enum: `completed | failed | halted-kill-switch | halted-upstream-failure`.

End of wake.
```
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl && test $(grep -c "—" .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl) -eq 0 && test $(wc -l < .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl) -ge 80 && test $(wc -l < .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl) -le 100 && test $(grep -cE "^## [1-6]\. " .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl) -eq 6 && grep -q ".agentbloc/KILL_SWITCH" .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl && grep -q "{{trigger.source}}" .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl && grep -q "{{trigger.event_name}}" .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl && grep -q "{{payload.schema}}" .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl && grep -q "{{agent.memory_dir}}" .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl && grep -q "D-74" .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl && grep -q "n8n-integration\.md" .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl && ! grep -q "{% if" .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl && ! grep -q "{% for" .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "—"` returns 0; `wc -l` 80..100
    - Exactly 6 numbered H2 sections
    - Section 1 contains `.agentbloc/KILL_SWITCH`
    - Webhook-specialized anchors present: {{trigger.source}}, {{trigger.event_name}}, {{payload.schema}}
    - D-74 envelope validation in section 4 (schema_version, agent_id, trigger.source, trigger.event_name)
    - Cross-reference to n8n-integration.md present
    - NO `{% if` or `{% for` directives
  </acceptance_criteria>
  <done>
wake-job-webhook.md.tmpl wired. runtime-engine can materialize per-(agent, event-source, event-name) webhook wake.md files. RUNTIME-02 (event triggers fire via n8n webhooks -> agent wake) and RUNTIME-07 (kill-switch at section 1) satisfied at template level.
  </done>
</task>

<task type="auto">
  <name>Task 6: Create `.claude/skills/agentbloc/templates/wake-job-inter.md.tmpl` , D-73 6-section inter-agent wake template with section 4 specialized for SendMessage body parse + team dispatch logic (structural twin of wake-job-cron.md.tmpl)</name>
  <files>.claude/skills/agentbloc/templates/wake-job-inter.md.tmpl</files>
  <read_first>
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md (D-73 6-section; D-76 SendMessage body contract; D-77 kill-switch at team transitions)
    - .claude/skills/agentbloc/templates/wake-job-cron.md.tmpl (produced Task 4; sibling skeleton)
    - .claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl (produced Task 5; sibling)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - SendMessage body + metadata.correlation_id (from ClaudeClaw primitive or writeStateHandoff inbox file)
    </consumes>
    <provides>
      - Materialized wake-inter.md per agent (one per agent; dispatches internally by message.type)
    </provides>
  </interfaces>
  <action>
Create a NEW file at `.claude/skills/agentbloc/templates/wake-job-inter.md.tmpl` (80-100 lines) with EXACTLY 6 numbered H2 sections. Identical skeleton to wake-job-cron.md.tmpl EXCEPT:
- Title reflects inter-agent trigger
- Section 2 reads correlation_id from SendMessage metadata (ClaudeClaw) OR inbox file (writeStateHandoff fallback)
- Section 4 parses SendMessage body + applies team dispatch logic (message.type + calling_agent_id routing)
- Section 5 includes the team-transition kill-switch check per D-77 enforcement point #3 (before any outgoing SendMessage)
- Section 6 additionally emits a TEAM_SESSIONS.jsonl entry if wake is part of an active TeamCreate session

Pure `{{var}}` substitution only. CRITICAL: zero em-dashes. Use the Write tool.

Template body:

```
<!-- agentbloc wake-job template: inter-agent trigger -->
<!-- schema_version: 1 -->
<!-- anchor points: {{agent.id}}, {{agent.role}}, {{agent.autonomy}}, {{agent.skill_path}}, {{agent.memory_dir}}, {{team.correlation_prefix}} -->

# Wake Job: {{agent.id}} (inter-agent)

You are waking to execute {{agent.role}} in response to a SendMessage from another agent in your team.

## 1. Kill-switch pre-check

Check if `.agentbloc/KILL_SWITCH` exists.
- If YES: Append this entry to `.agentbloc/logs/audit.jsonl`:
  `{"correlation_id":"<ID>","event":"halted-kill-switch","agent_id":"{{agent.id}}","wake_at":"<now UTC Z>","trigger":"inter"}`
  Emit `{status: halted-kill-switch}` to the calling agent (team lead will dissolve the team per D-77 team-transition check).
  EXIT IMMEDIATELY.
- If NO: Continue to section 2.

## 2. Correlation-ID ingest

Read the correlation ID from:
- `{{message.metadata.correlation_id}}` when invoked via ClaudeClaw SendMessage, OR
- The inbox file top-level `correlation_id` field when invoked via writeStateHandoff fallback at `{{agent.memory_dir}}/inbox/<correlation-id>.json`

Inherited child-ID: if this is a sub-invocation (the caller spawned you via TeamCreate), the correlation ID already has the `-sub-<NNN>` suffix per `references/audit-logging.md` child-ID convention.

## 3. Memory + state read

BEFORE any other work, load:
- `{{agent.memory_dir}}/memory.md`
- `{{agent.memory_dir}}/state.json`

## 4. Input parse

Parse the SendMessage body. Expected shape (declared by the registry workflow's message contract):

```
{
  "type": "<message-type>",
  "calling_agent_id": "<agent-id>",
  "body": { ... }
}
```

Dispatch by `message.type`:
- Validate `calling_agent_id` is in the team roster (check `registry.runtime.workflows[<workflow-id>].agents`)
- Route to the agent's SKILL.md handler for the given `message.type` (the SKILL.md declares which message types this agent handles)
- If `message.type` is unknown, log `{event: inter-message-unknown-type, type: "<type>", calling_agent_id: "<id>"}` and return `{status: rejected, reason: unknown-message-type}` to the caller.

## 5. Execute

Load `{{agent.skill_path}}` and proceed per the agent's prompt. Pass the SendMessage body as the primary input. Include the correlation ID `<ID>` in every log call.

**Team-transition kill-switch check (D-77 enforcement point #3):** Before issuing any outgoing SendMessage (to reply to the caller OR to fan out to another team member), RE-CHECK `.agentbloc/KILL_SWITCH`. If active, emit `{status: halted-kill-switch}` to the caller and do NOT send the outgoing message. This prevents kill-switch cascades from being delayed by long-running tool invocations.

Autonomy: `{{agent.autonomy}}`.

## 6. State + log write

After execution:
- Update `{{agent.memory_dir}}/state.json`.
- Update `{{agent.memory_dir}}/last-run.json`.
- Append one JSONL line to `.agentbloc/logs/audit.jsonl`: `{correlation_id, agent_id: {{agent.id}}, event: wake-completed, wake_outcome, timing_ms, trigger: inter, calling_agent_id}`.
- If this wake is part of an active TeamCreate session, ALSO append one JSONL line to `.agentbloc/runtime/TEAM_SESSIONS.jsonl`: `{correlation_id, team_id, agent_id: {{agent.id}}, event: team-member-returned, wake_outcome}`.

`wake_outcome` enum: `completed | failed | halted-kill-switch | halted-upstream-failure`.

End of wake.
```
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl && test $(grep -c "—" .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl) -eq 0 && test $(wc -l < .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl) -ge 80 && test $(wc -l < .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl) -le 100 && test $(grep -cE "^## [1-6]\. " .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl) -eq 6 && grep -q ".agentbloc/KILL_SWITCH" .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl && grep -q "SendMessage" .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl && grep -q "calling_agent_id" .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl && grep -q "TEAM_SESSIONS.jsonl" .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl && grep -q "writeStateHandoff" .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl && grep -q "D-77" .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl && grep -q "team-transition" .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl && ! grep -q "{% if" .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl && ! grep -q "{% for" .claude/skills/agentbloc/templates/wake-job-inter.md.tmpl</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "—"` returns 0; `wc -l` 80..100
    - Exactly 6 numbered H2 sections
    - Section 1 contains `.agentbloc/KILL_SWITCH`
    - Section 2 mentions both ClaudeClaw metadata path AND writeStateHandoff inbox path
    - Section 4 dispatches by message.type + validates calling_agent_id
    - Section 5 contains the team-transition kill-switch re-check (D-77 enforcement point #3)
    - Section 6 emits TEAM_SESSIONS.jsonl entry when in active team session
    - NO `{% if` or `{% for` directives
  </acceptance_criteria>
  <done>
wake-job-inter.md.tmpl wired. runtime-engine can materialize one wake-inter.md per agent; agent internally dispatches by message.type. Team-transition kill-switch check at section 5 satisfies D-77 enforcement point #3. RUNTIME-04 + RUNTIME-07 satisfied at template level.
  </done>
</task>

<task type="auto">
  <name>Task 7: Create `.claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md` , canonical 3-scenario narrative fixture per D-79 (cron wake + webhook + TeamCreate + KILL_SWITCH mid-team)</name>
  <files>.claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md</files>
  <read_first>
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md (D-79 3-scenario structure: A cron + B webhook+TeamCreate + C kill-switch)
    - .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml (for realistic agent IDs + topology; verifies Arco Rooms team shape)
    - .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md (FULL file; structural twin for fixture narrative depth)
    - .claude/skills/agentbloc/examples/mapfre-discovery-report.md (SECONDARY fixture twin)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - Arco Rooms agent IDs: gestor-cobros, recepcionista, gestor-documental
      - D-74 envelope, D-75 correlation-ID format, D-77 kill-switch semantics
    </consumes>
    <provides>
      - Canonical golden-file reference for Phase 16 validation
      - Mobile-first debugging example (correlation IDs grep-readable in Telegram bubble)
      - Team dissolution reason demonstration for kill-switch path
    </provides>
  </interfaces>
  <action>
Create a NEW file at `.claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md` (180-240 lines) with EXACTLY 3 scenarios matching D-79. CRITICAL: zero em-dashes. Use the Write tool.

**H1:** `# Arco Rooms Runtime Correlation Flow`

**Blockquote (verbatim):** `> Canonical narrative fixture for Phase 13 runtime behavior. Walks through 3 scenarios demonstrating correlation-ID propagation, TeamCreate coordination, and kill-switch team-wide halt. This is the Phase 16 golden-file reference for the end-to-end runtime validation. Companion structural fixture: arco-rooms-runtime-artifacts.md (literal wake.md + crontab + n8n route .json + registry.yaml shapes).`

**Introductory Context section:** short paragraph establishing the Arco Rooms team (3 requested agents: Gestor Cobros, Recepcionista, Gestor Documental; 2 anticipated in Phase 15). Trigger matrix from PDF page 3:
- Gestor Cobros: monthly cron (1st 09:00 Europe/Madrid) + Plaid payment-received webhook
- Recepcionista: Telegram tenant-message webhook + inter (receives SendMessage from Gestor Cobros)
- Gestor Documental: weekly cron (Monday 08:00) + Gmail new-invoice-email webhook

### Scenario A: Cron wake of Gestor Cobros (single-agent bypass)

Setup: 1st of month, 09:00 Europe/Madrid (= 08:00 UTC Z for CEST). System cron fires the crontab entry for Gestor Cobros. `registry.runtime.workflows.monthly-collections.agents` = `[gestor-cobros]` (length 1, single-agent bypass per D-76).

Flow:
1. Cron entry triggers `AGENTBLOC_CORRELATION_ID=$(agentbloc-gen-correlation cron) claude -p .agentbloc/agents/gestor-cobros/wake-cron.md`
2. Correlation ID generated: `cron-20260501T080000Z-a3f21b`
3. wake-cron.md section 1: checks `.agentbloc/KILL_SWITCH`. Not present. Continues.
4. Section 2: ingests `AGENTBLOC_CORRELATION_ID=cron-20260501T080000Z-a3f21b` from env var.
5. Section 3: reads `memory.md` (tenant contracts) + `state.json` (last month processed, pending retries).
6. Section 4: derives input from `state.working_state.month_to_process = "2026-05"`.
7. Section 5: loads `.claude/skills/gestor-cobros/SKILL.md`; executes per autonomy = semi; calls BBVA MCP to list transactions for 2026-05.
8. Section 6: writes updated state.json + last-run.json; appends log line:
   ```
   {"correlation_id":"cron-20260501T080000Z-a3f21b","agent_id":"gestor-cobros","event":"wake-completed","wake_outcome":"completed","timing_ms":4821,"trigger":"cron"}
   ```
9. Sends Telegram message to tenant with correlation ID in header for human grep:
   ```
   [id: cron-20260501T080000Z-a3f21b]
   Estimado inquilino, recordatorio de cuota de mayo.
   ```

Key observation: single-agent workflow takes the bypass path; no TeamCreate call; one log line, one correlation ID, end-to-end grep-traceable.

### Scenario B: Telegram tenant message wakes Recepcionista, TeamCreate with Gestor Cobros (multi-agent dynamic spawn)

Setup: tenant sends Telegram message "Cuando vence mi contrato?" at 14:32:12 UTC on 2026-05-04. n8n Telegram route fires the webhook. `registry.runtime.workflows.tenant-inquiry.agents` = `[recepcionista, gestor-cobros]` with `spawn_rule: dynamic`. Coordination preference = `claudeclaw` with `writeStateHandoff` fallback.

Flow:
1. n8n Telegram route seeds envelope with `correlation_id: webhook-telegram-20260504T143212Z-c7d92a`, `agent_id: recepcionista`, `trigger.source: telegram`, `trigger.event_name: tenant-message`, payload carries chat_id + sender + text.
2. ClaudeClaw job endpoint reads envelope, invokes `claude -p --payload-file <path> .agentbloc/agents/recepcionista/wake-webhook-telegram-tenant-message.md`.
3. wake-webhook.md section 1: KILL_SWITCH not present. Continues.
4. Section 2: ingests `webhook-telegram-20260504T143212Z-c7d92a` from envelope.
5. Section 3: reads Recepcionista memory + state.
6. Section 4: validates envelope (schema_version=1, agent_id=recepcionista, source=telegram); parses payload.
7. Section 5: loads SKILL.md; detects "cuando vence mi contrato" needs contract expiry + payment-status (dependency on gestor-cobros per `dependencies[]` in agent-profiles.yaml).
8. Recepcionista calls `TeamCreate([recepcionista, gestor-cobros], correlation_id=webhook-telegram-20260504T143212Z-c7d92a)`. Team ID assigned.
9. Recepcionista sends `SendMessage(to: gestor-cobros, body: {type: payment-status-check, tenant_id: "T-042"}, metadata: { correlation_id: "webhook-telegram-20260504T143212Z-c7d92a-sub-001" })`.
10. Gestor Cobros wakes via wake-inter.md. Section 1: KILL_SWITCH not present. Section 2: ingests sub-001 child ID. Section 3: reads state. Section 4: dispatches on message.type=payment-status-check; validates calling_agent_id=recepcionista is in team roster.
11. Section 5: queries BBVA MCP for tenant T-042 payment history; detects no overdue balances.
12. Gestor Cobros sends `SendMessage(to: recepcionista, body: {type: payment-status-reply, status: "current", last_paid: "2026-05-01"}, metadata: { correlation_id: "webhook-telegram-20260504T143212Z-c7d92a-sub-001" })`.
13. Gestor Cobros log line:
    ```
    {"correlation_id":"webhook-telegram-20260504T143212Z-c7d92a-sub-001","agent_id":"gestor-cobros","event":"wake-completed","wake_outcome":"completed","timing_ms":1203,"trigger":"inter","calling_agent_id":"recepcionista"}
    ```
14. Recepcionista receives reply; drafts Telegram response combining contract expiry (from its own memory) + payment status (from reply). Sends Telegram message with correlation ID header.
15. Recepcionista log line; team dissolves with `team_dissolution_reason: all-members-returned`; TEAM_SESSIONS.jsonl entry appended.

Key observation: ONE user event -> TWO agent wakes -> SAME parent correlation ID `webhook-telegram-20260504T143212Z-c7d92a` grep-traces both; child ID sub-001 distinguishes the Gestor Cobros sub-invocation.

Grep verification:
```bash
grep 'correlation_id":"webhook-telegram-20260504T143212Z-c7d92a' .agentbloc/logs/audit.jsonl
# Returns both agent log lines (Recepcionista + Gestor Cobros with sub-001)
```

### Scenario C: KILL_SWITCH fires mid-team, team dissolves cleanly

Setup: Scenario B is in progress at step 11 (Gestor Cobros about to query BBVA MCP). Operator notices unusual activity in the Telegram ops thread at 14:32:15 UTC and types `/stop`.

Flow:
1. n8n agentbloc-stop route fires on `/stop` message in ops thread; runs `touch .agentbloc/KILL_SWITCH` via shell node.
2. Gestor Cobros at step 11 is executing BBVA MCP call (pre-tool-use hook from Phase 12 will check KILL_SWITCH before the next tool call).
3. Phase 12 PreToolUse hook `.claude/hooks/kill-switch-check.sh` detects KILL_SWITCH; returns `permissionDecision: deny` for the BBVA MCP call. Gestor Cobros logs:
   ```
   {"correlation_id":"webhook-telegram-20260504T143212Z-c7d92a-sub-001","agent_id":"gestor-cobros","event":"tool-call-denied-kill-switch","wake_outcome":"halted-kill-switch","trigger":"inter"}
   ```
4. Gestor Cobros returns `{status: halted-kill-switch}` to Recepcionista via SendMessage (this SendMessage itself passes the D-77 enforcement point #3 team-transition check because we are RETURNING an error status; the check gates OUTGOING new work, not error returns).
5. Recepcionista wake-inter.md section 5: re-checks KILL_SWITCH before issuing any outgoing SendMessage. KILL_SWITCH present. Emits `{status: halted-kill-switch}` back toward the team.
6. Team lead (Recepcionista per D-23 mesh/default) dissolves the team via TeamCreate teardown. TEAM_SESSIONS.jsonl entry:
   ```
   {"correlation_id":"webhook-telegram-20260504T143212Z-c7d92a","team_id":"<id>","event":"team-dissolved","team_dissolution_reason":"kill-switch","dissolved_at":"2026-05-04T14:32:16Z"}
   ```
7. No Telegram reply is sent to the tenant (KILL_SWITCH gates all outbound side effects). The conversation resumes when operator runs `/resume` (clears KILL_SWITCH via the sibling n8n route).

Key observation: KILL_SWITCH activation mid-team halts within one SendMessage round-trip (typically under 5 seconds). `team_dissolution_reason: kill-switch` is logged with the shared correlation ID, so forensics can reconstruct the exact chain end-to-end. The D-77 three-point enforcement (wake + tool + team-transition) covers all three latency windows.

Grep verification:
```bash
grep '"correlation_id":"webhook-telegram-20260504T143212Z-c7d92a"' .agentbloc/runtime/TEAM_SESSIONS.jsonl | jq '.team_dissolution_reason'
# "kill-switch"
```

### Cross-References

- `.claude/skills/agentbloc/references/correlation-id.md` (format spec + grep recipes)
- `.claude/skills/agentbloc/references/n8n-integration.md` (envelope schema + Telegram worked example)
- `.claude/skills/agentbloc/references/runtime-coordination.md` (TeamCreate/SendMessage contract + dissolution semantics)
- `.claude/skills/agentbloc/references/incident-response.md` (KILL_SWITCH + three-point enforcement)
- `.claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md` (companion structural fixture: literal wake.md + crontab + n8n route .json + registry.yaml shapes)
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && test $(grep -c "—" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md) -ge 180 && test $(wc -l < .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md) -le 240 && test $(grep -cE "^### Scenario [A-C]" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md) -eq 3 && grep -q "cron-20260501T080000Z" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && grep -q "webhook-telegram-20260504T143212Z" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && grep -q "sub-001" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && grep -q "gestor-cobros" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && grep -q "recepcionista" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && grep -q "TeamCreate" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && grep -q "SendMessage" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && grep -q "KILL_SWITCH" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && grep -q "kill-switch" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && grep -q "team_dissolution_reason" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md && grep -q "arco-rooms-runtime-artifacts\.md" .claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "—"` returns 0; `wc -l` 180..240
    - Exactly 3 scenario H3 headers: `### Scenario A`, `### Scenario B`, `### Scenario C`
    - Scenario A demonstrates cron single-agent bypass with correlation ID `cron-20260501T080000Z-<nonce>`
    - Scenario B demonstrates webhook + TeamCreate + sub-001 child ID with `webhook-telegram-20260504T143212Z-<nonce>`
    - Scenario C demonstrates KILL_SWITCH mid-team halt with `team_dissolution_reason: kill-switch`
    - All 3 Arco Rooms agents mentioned (gestor-cobros, recepcionista, gestor-documental or at least the 2 active in scenarios)
    - Cross-reference to arco-rooms-runtime-artifacts.md present
  </acceptance_criteria>
  <done>
arco-rooms-correlation-flow.md wired. Phase 16 golden-file reference for end-to-end runtime validation. Demonstrates RUNTIME-04 (TeamCreate + SendMessage), RUNTIME-05 (single-agent bypass), RUNTIME-06 (correlation-ID propagation), RUNTIME-07 (kill-switch team-wide halt).
  </done>
</task>

<task type="auto">
  <name>Task 8: Create `.claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md` , structural fixture showing literal wake.md + crontab.applied + n8n route .json + extended registry.yaml for the 3 Arco Rooms agents per D-79</name>
  <files>.claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md</files>
  <read_first>
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md (D-78 registry runtime block; D-79 structural fixture spec; D-82 line budget 200-280)
    - .planning/phases/13-multi-agent-runtime/13-RESEARCH.md (amendment: n8n route files use .json, not .yaml)
    - .claude/skills/agentbloc/examples/arco-rooms-registry.yaml (FULL file; PRIMARY structural twin; extend with runtime block)
    - .claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml (FULL file; source for agent IDs + topology + triggers)
    - .claude/skills/agentbloc/examples/arco-rooms-deploy-report.md (secondary twin; fixture narrative style)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - Arco Rooms agent-profiles.yaml triggers[] arrays
      - D-74 envelope schema for n8n route .json shape
      - D-78 registry.yaml runtime block schema
    </consumes>
    <provides>
      - Literal wake.md artifacts for 3 trigger paths (cron + webhook + inter)
      - crontab.applied manifest with monthly + weekly entries
      - 3 n8n route .json stubs (telegram + gmail + plaid)
      - Extended registry.yaml with full runtime block (workflows + webhook_endpoints + coordination_preference)
    </provides>
  </interfaces>
  <action>
Create a NEW file at `.claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md` (200-280 lines). CRITICAL: zero em-dashes. Use the Write tool.

**H1:** `# Arco Rooms Runtime Artifacts`

**Blockquote (verbatim):** `> Companion structural fixture to arco-rooms-correlation-flow.md. Shows the literal file contents runtime-engine materializes for the Arco Rooms team: wake.md per trigger path, crontab.applied manifest, n8n route .json stubs, and the extended registry.yaml runtime block per D-78. Textual (not executable); for audit + Phase 16 golden-file comparison.`

**H2: Context**

Short paragraph: Arco Rooms team with 3 agents. Trigger matrix per PDF page 3:
- Gestor Cobros: 1 cron (monthly 1st 09:00 Europe/Madrid = 08:00 UTC) + 1 webhook (Plaid payment-received) -> 2 wake.md files
- Recepcionista: 1 webhook (Telegram tenant-message) + 1 inter (SendMessage receiver) -> 2 wake.md files
- Gestor Documental: 1 cron (weekly Monday 08:00 = 07:00 UTC) + 1 webhook (Gmail new-invoice-email) -> 2 wake.md files

Total: 6 wake.md files. Phase 13 runtime-engine materializes each deterministically from agent-profiles.yaml triggers[] + the 3 templates per D-73 Option D.

**H2: Wake.md Artifact 1 - Gestor Cobros cron wake**

Path: `.agentbloc/agents/gestor-cobros/wake-cron.md`

Show literal materialized file content (post-substitution): insert anchor values inline. Include all 6 sections with realistic content (month_to_process cursor, BBVA MCP call example, Telegram header with correlation ID).

**H2: Wake.md Artifact 2 - Recepcionista webhook-telegram wake**

Path: `.agentbloc/agents/recepcionista/wake-webhook-telegram-tenant-message.md`

Show literal materialized file content with {{trigger.source}}=telegram, {{trigger.event_name}}=tenant-message, {{payload.schema}} expanded (chat_id + sender + text).

**H2: Wake.md Artifact 3 - Recepcionista inter wake (team handler)**

Path: `.agentbloc/agents/recepcionista/wake-inter.md`

Show literal materialized file content demonstrating dispatch by message.type, team-transition kill-switch check, TEAM_SESSIONS.jsonl emission.

**H2: Crontab manifest**

Path: `.agentbloc/runtime/crontab.applied`

```
# agentbloc:fingerprint sha256=<64-hex> generated_at=2026-04-24T18:00:00Z

# Gestor Cobros - monthly rent collection (1st of month, 09:00 Europe/Madrid = 08:00 UTC)
0 8 1 * * AGENTBLOC_CORRELATION_ID=$(/home/user/.agentbloc/runtime/helpers.sh agentbloc-gen-correlation cron) claude -p /home/user/.agentbloc/agents/gestor-cobros/wake-cron.md

# Gestor Documental - weekly document review (Monday 08:00 Europe/Madrid = 07:00 UTC)
0 7 * * 1 AGENTBLOC_CORRELATION_ID=$(/home/user/.agentbloc/runtime/helpers.sh agentbloc-gen-correlation cron) claude -p /home/user/.agentbloc/agents/gestor-documental/wake-cron.md

# Recepcionista has NO cron; webhook-only (Telegram + inter paths)
```

Installation discipline (per D-80): runtime-engine installs via stdin form, NEVER via `crontab -e`:
```bash
(crontab -l 2>/dev/null; cat .agentbloc/runtime/crontab.applied) | crontab -
```

**H2: n8n Route 1 - Recepcionista Telegram**

Path: `.agentbloc/runtime/n8n-routes/recepcionista-telegram-tenant-message.json`

Literal .json content (per D-74 envelope + RESEARCH amendment locking .json extension):

```json
{
  "schema_version": 1,
  "agent_id": "recepcionista",
  "trigger": {
    "source": "telegram",
    "event_name": "tenant-message"
  },
  "n8n_config": {
    "webhook_path": "/webhook/recepcionista-tenant-message",
    "http_method": "POST",
    "filter": "chat.type == 'private' AND message.text IS NOT NULL"
  },
  "payload_schema": {
    "chat_id": "integer",
    "sender": "{ id: integer, username: string }",
    "text": "string"
  },
  "evidence": {
    "verified_at": null
  }
}
```

**H2: n8n Route 2 - Gestor Documental Gmail**

Path: `.agentbloc/runtime/n8n-routes/gestor-documental-gmail-new-invoice-email.json`

Similar structure. source=gmail, event_name=new-invoice-email, payload_schema carries message_id + from + subject + attachment_ids.

**H2: n8n Route 3 - Gestor Cobros Plaid**

Path: `.agentbloc/runtime/n8n-routes/gestor-cobros-plaid-payment-received.json`

source=plaid, event_name=payment-received, payload_schema carries account_id + amount + transaction_id.

**H2: Extended registry.yaml with runtime block (D-78)**

Path: `.agentbloc/agents/registry.yaml`

Show the BASE registry (from Phase 12 arco-rooms-registry.yaml) EXTENDED with the full runtime top-level block per D-78:

```yaml
schema_version: 1
team:
  name: arco-rooms
  lead: gestor-cobros
  topology: hierarchy
agents:
  - id: gestor-cobros
    skill_path: .claude/skills/gestor-cobros/SKILL.md
    memory_dir: .agentbloc/agents/gestor-cobros/
  - id: recepcionista
    skill_path: .claude/skills/recepcionista/SKILL.md
    memory_dir: .agentbloc/agents/recepcionista/
  - id: gestor-documental
    skill_path: .claude/skills/gestor-documental/SKILL.md
    memory_dir: .agentbloc/agents/gestor-documental/
reporting_hierarchy:
  gestor-cobros:
    children: [recepcionista, gestor-documental]
dashboard_agent: null  # Phase 14 populates

runtime:
  schema_version: 1
  correlation_prefix: "arco-rooms"
  team_timeout_minutes: 15
  coordination_preference:
    prefer: "claudeclaw"
    fallback: "writeStateHandoff"
  cron_registered_at: "2026-04-24T18:05:00Z"
  crontab_manifest: ".agentbloc/runtime/crontab.applied"
  workflows:
    monthly-collections:
      agents: [gestor-cobros]
      spawn_rule: "declared"
      trigger:
        type: "cron"
        schedule: "0 8 1 * *"
    weekly-document-review:
      agents: [gestor-documental]
      spawn_rule: "declared"
      trigger:
        type: "cron"
        schedule: "0 7 * * 1"
    tenant-inquiry:
      agents: [recepcionista, gestor-cobros]
      spawn_rule: "dynamic"
      trigger:
        type: "webhook"
        webhook_route: ".agentbloc/runtime/n8n-routes/recepcionista-telegram-tenant-message.json"
    plaid-payment-received:
      agents: [gestor-cobros]
      spawn_rule: "declared"
      trigger:
        type: "webhook"
        webhook_route: ".agentbloc/runtime/n8n-routes/gestor-cobros-plaid-payment-received.json"
    gmail-invoice-received:
      agents: [gestor-documental]
      spawn_rule: "declared"
      trigger:
        type: "webhook"
        webhook_route: ".agentbloc/runtime/n8n-routes/gestor-documental-gmail-new-invoice-email.json"
  webhook_endpoints:
    - agent_id: recepcionista
      source: telegram
      event_name: tenant-message
      route_file: ".agentbloc/runtime/n8n-routes/recepcionista-telegram-tenant-message.json"
      evidence:
        verified_at: null
    - agent_id: gestor-documental
      source: gmail
      event_name: new-invoice-email
      route_file: ".agentbloc/runtime/n8n-routes/gestor-documental-gmail-new-invoice-email.json"
      evidence:
        verified_at: null
    - agent_id: gestor-cobros
      source: plaid
      event_name: payment-received
      route_file: ".agentbloc/runtime/n8n-routes/gestor-cobros-plaid-payment-received.json"
      evidence:
        verified_at: null
```

**H2: Evidence table**

| Agent | Trigger Paths | wake.md Files | Evidence verified_at |
|-------|---------------|---------------|----------------------|
| gestor-cobros | cron (monthly 1st 08:00 UTC) + webhook (plaid) + inter (receives from recepcionista) | wake-cron.md + wake-webhook-plaid-payment-received.md + wake-inter.md | null (user confirms live n8n route) |
| recepcionista | webhook (telegram) + inter (team lead on dynamic spawn) | wake-webhook-telegram-tenant-message.md + wake-inter.md | null |
| gestor-documental | cron (weekly Mon 07:00 UTC) + webhook (gmail) | wake-cron.md + wake-webhook-gmail-new-invoice-email.md | null |

Per D-39 evidence protocol + D-78 registry.runtime.webhook_endpoints.evidence.verified_at: null until user confirms each route is live in their n8n instance. Phase 13 does not auto-ping.

**H2: Cross-References**

- `.claude/skills/agentbloc/examples/arco-rooms-correlation-flow.md` (companion narrative fixture)
- `.claude/skills/agentbloc/examples/arco-rooms-agent-profiles.yaml` (input to Phase 13 runtime-engine)
- `.claude/skills/agentbloc/examples/arco-rooms-registry.yaml` (Phase 12 base registry; extended above)
- `.claude/skills/agentbloc/references/n8n-integration.md` (envelope schema + route file format)
- `.claude/skills/agentbloc/references/runtime-coordination.md` (TeamCreate + crontab stdin install)
- `.claude/skills/agentbloc/references/correlation-id.md` (format + helpers.sh generator)
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && test $(grep -c "—" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md) -ge 200 && test $(wc -l < .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md) -le 280 && grep -q "^# Arco Rooms Runtime Artifacts" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "wake-cron.md" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "wake-webhook-telegram-tenant-message.md" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "wake-inter.md" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "crontab.applied" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "n8n-routes/.*\.json" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && ! grep -q "n8n-routes/.*\.yaml" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "runtime:" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "coordination_preference" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "cron_registered_at" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "workflows:" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "webhook_endpoints:" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "gestor-cobros" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "recepcionista" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "gestor-documental" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md && grep -q "arco-rooms-correlation-flow\.md" .claude/skills/agentbloc/examples/arco-rooms-runtime-artifacts.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "—"` returns 0; `wc -l` 200..280
    - H1 `# Arco Rooms Runtime Artifacts` on first line
    - 3 wake.md artifact blocks present (cron + webhook-telegram + inter)
    - crontab.applied manifest with monthly + weekly cron lines
    - 3 n8n route files cited with `.json` extension (NOT `.yaml`; per RESEARCH amendment)
    - Extended registry.yaml contains runtime top-level block with: schema_version, correlation_prefix, team_timeout_minutes, coordination_preference (prefer + fallback), cron_registered_at, crontab_manifest, workflows, webhook_endpoints
    - All 3 Arco Rooms agents mentioned
    - Cross-reference to arco-rooms-correlation-flow.md present
    - Evidence verified_at: null convention applied per D-78 + D-39
  </acceptance_criteria>
  <done>
arco-rooms-runtime-artifacts.md wired. Phase 16 golden-file reference for structural artifact shapes. Demonstrates D-78 registry runtime block + RESEARCH amendment .json extension + per-agent trigger path count (2 wake.md per agent except Recepcionista which uses 2).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n instance -> ClaudeClaw job endpoint (or local HTTP listener fallback) | Untrusted webhook payload crosses here; envelope shape validation at section 4 of wake-job-webhook.md.tmpl is the gate |
| calling agent -> receiving agent via SendMessage or writeStateHandoff inbox file | Untrusted inter-agent message body; wake-job-inter.md.tmpl section 4 validates calling_agent_id + message.type |
| cron system -> claude -p invocation | Trusted boundary (cron entries emitted by runtime-engine under approval gate); env var AGENTBLOC_CORRELATION_ID carries integrity-sensitive identifier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | n8n envelope | mitigate | Section 4 of wake-job-webhook.md.tmpl validates envelope.schema_version=1 + agent_id match + trigger.source enum membership; routing-mismatch halts wake before any tool call |
| T-13-02 | Elevation-of-Privilege | wake-job template | mitigate | Every template opens with section 1 kill-switch pre-check; EXIT IMMEDIATELY before any state read; grep -q ".agentbloc/KILL_SWITCH" on every template is gate |
| T-13-03 | Information-Disclosure | correlation-id.md grep recipes | mitigate | Correlation IDs embed only trigger-source + UTC timestamp + 6-hex nonce; NO PII in the ID itself; prose discipline documented in Section 7 Scalability |
| T-13-04 | Spoofing | inter-agent message calling_agent_id | mitigate | wake-job-inter.md.tmpl section 4 validates calling_agent_id is in registry.runtime.workflows[<id>].agents; unknown calling agents rejected before SKILL.md dispatch |
| T-13-05 | Denial-of-Service | team timeout | mitigate | registry.runtime.team_timeout_minutes (default 15); team dissolution on timeout per D-77 prevents zombie teams; logged with team_dissolution_reason=timeout |
| T-13-06 | Tampering | n8n route .json file | accept | route .json files are user-installable; evidence.verified_at: null until user confirms; no auto-ping per D-39; user accepts risk of route-file drift |
| T-13-07 | Repudiation | wake log entries | mitigate | Every wake emits JSONL log with correlation_id + agent_id + timestamp + wake_outcome; append-only discipline per D-46; TEAM_SESSIONS.jsonl for team-scoped events |
</threat_model>

<verification>
**Per-task file emission + em-dash gate:**
- [ ] 8 files exist at emitted paths
- [ ] Zero em-dashes across all 8 files (sum = 0)
- [ ] Each file within its stated line budget

**Structural integrity:**
- [ ] n8n-integration.md contains 4-field envelope block + 5 worked examples + 7 bounded trigger.source enum values
- [ ] runtime-coordination.md contains literal `workflows[].agents.length > 1` (single-agent bypass check) + writeStateHandoff + crontab stdin form
- [ ] correlation-id.md regex compiles and matches 2 valid IDs; rejects bogus input (per VALIDATION.md Task 13-01-03)
- [ ] 3 wake-job templates each have exactly 6 numbered H2 sections (per VALIDATION.md Task 13-01-05)
- [ ] 3 wake-job templates all contain `.agentbloc/KILL_SWITCH` (per VALIDATION.md Task 13-01-06)
- [ ] arco-rooms-correlation-flow.md contains exactly 3 Scenario H3 headers (per VALIDATION.md Task 13-01-07)
- [ ] arco-rooms-runtime-artifacts.md cites n8n route files with .json extension (per VALIDATION.md Task 13-01-08)

**Cross-reference integrity:**
- [ ] n8n-integration.md -> correlation-id.md + runtime-coordination.md
- [ ] runtime-coordination.md -> n8n-integration.md + correlation-id.md + incident-response.md
- [ ] correlation-id.md -> audit-logging.md + n8n-integration.md + runtime-coordination.md
- [ ] wake-job templates -> correlation-id.md + n8n-integration.md
- [ ] arco-rooms-correlation-flow.md -> arco-rooms-runtime-artifacts.md
- [ ] arco-rooms-runtime-artifacts.md -> arco-rooms-correlation-flow.md

**D-62/D-73 template discipline:**
- [ ] NO `{% if` / `{% for` / `{% else %}` directives in any of the 3 templates (pure {{var}} substitution)
- [ ] Anchor points consistent across templates: {{agent.id}}, {{agent.role}}, {{agent.skill_path}}, {{agent.memory_dir}}
</verification>

<success_criteria>
1. 8 new files exist at emitted paths with zero em-dashes
2. n8n-integration.md documents D-74 envelope + 5 worked examples + n8n .json route format (RESEARCH amendment)
3. runtime-coordination.md documents D-76 TeamCreate/SendMessage + single-agent bypass + writeStateHandoff PRIMARY for non-interactive + crontab stdin install (NEVER crontab -e)
4. correlation-id.md documents D-75 format + regex + 3 propagation channels + 4+ grep recipes
5. 3 wake-job templates have identical 6-section skeleton with kill-switch pre-check at section 1; pure {{var}} substitution
6. arco-rooms-correlation-flow.md shows 3 scenarios (cron bypass + webhook+TeamCreate + KILL_SWITCH mid-team)
7. arco-rooms-runtime-artifacts.md shows literal wake.md + crontab.applied + 3 n8n .json stubs + extended registry.yaml runtime block per D-78
8. RUNTIME-02 + RUNTIME-03 + RUNTIME-04 + RUNTIME-05 + RUNTIME-06 + RUNTIME-07 substantially covered at reference + template layer (Plan 13-02 adds subagent layer; Plan 13-03 wires into SKILL.md + existing references)
9. 8 atomic task commits on master; 1 SUMMARY commit at end
10. SUMMARY.md at `.planning/phases/13-multi-agent-runtime/13-01-SUMMARY.md` with file list + line counts + em-dash counts + RUNTIME-0X traceability
</success_criteria>

<output>
After completion, create `.planning/phases/13-multi-agent-runtime/13-01-SUMMARY.md` documenting each artifact + per-requirement traceability + Plan 13-02 handoff (runtime-engine subagent reads these 3 references in its Mandatory Initial Read).
</output>
</content>
