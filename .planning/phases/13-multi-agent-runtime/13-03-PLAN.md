---
phase: 13-multi-agent-runtime
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - .claude/skills/agentbloc/references/deploy-protocol.md
  - .claude/skills/agentbloc/references/phase-5-deployment.md
  - .claude/skills/agentbloc/references/incident-response.md
  - .claude/skills/agentbloc/SKILL.md
autonomous: true
requirements_addressed:
  - RUNTIME-01
  - RUNTIME-02
  - RUNTIME-03
  - RUNTIME-04
  - RUNTIME-05
  - RUNTIME-06
  - RUNTIME-07
estimated_tasks: 4

must_haves:
  truths:
    - "deploy-protocol.md has a new Step 7 (Runtime Wiring) inserted between existing Step 6 (Post-Deploy Verification) and the terminal Halt Protocol / Quick Reference sections; Step 7 cites runtime-engine subagent invocation + the 3 Plan 13-01 references (n8n-integration + runtime-coordination + correlation-id); existing Steps 1-6 + Halt Protocol + Quick Reference preserved byte-for-byte"
    - "phase-5-deployment.md has a new Step 7.5 section (Runtime Wiring hand-off between deploy-engine and runtime-engine) inserted AFTER the existing Step 7 job-definition section and BEFORE Step 8 SUMMARY.md / Step 9 Deployment Guide sections; existing Steps 1-7 + 8 + 9 preserved byte-for-byte; prior phase's Priority 1 ClaudeClaw-Native Deploy section preserved byte-for-byte"
    - "incident-response.md gains a new H2 section 'Runtime Kill-Switch Semantics' appended at the end of the file; the new section documents D-77 three-point enforcement (wake / per-tool / team-transition) with concrete numbered paragraphs; existing dual-path (file + Telegram /stop) prose preserved byte-for-byte"
    - "SKILL.md Phase 5 entry gains a See-line block appended with 3 new See-lines (n8n-integration.md + runtime-coordination.md + correlation-id.md) per D-58 context-budget discipline; existing See-lines for Phase 5 preserved byte-for-byte"
    - "SKILL.md State Transitions section gains a new `runtime_wired` sub-gate bullet joining the Phase 5 deployment_artifacts_emitted sub-gate (from Phase 12); both sub-gates ANDed into the Phase 5 -> Phase 6 transition per D-81"
    - "SKILL.md Phase 6 precondition extends to verify `registry.yaml runtime.cron_registered_at` is non-null OR `runtime.webhook_endpoints` is non-empty (proves Phase 13 completed before Evolution-scans touch live agents)"
    - "SKILL.md final line count within 202..225 inclusive (current 182 + Phase 13 budget ~20, under 250 cap per D-29, with headroom for Phase 14 + Phase 15)"
    - "Zero em-dashes introduced in any of the 4 edits (per-file grep -c '—' returns 0 for emitted sections; legacy em-dashes in existing unchanged prose are NOT touched per Phase 12 0ab97cd precedent)"
    - "D-58 context-budget discipline held: n8n-integration.md + runtime-coordination.md + correlation-id.md load at Phase 5 entry via SKILL.md See-lines; wake-job-*.md.tmpl + runtime-engine.md are subagent-only (NOT loaded at Phase 5 entry); SKILL.md grep-for-absence of wake-job-cron/webhook/inter.md.tmpl + runtime-engine.md returns NO match"
  artifacts:
    - path: ".claude/skills/agentbloc/references/deploy-protocol.md"
      provides: "After edit: Step 7 (Runtime Wiring) inserted between Step 6 verification and terminal sections; cites runtime-engine subagent + 3 references from Plan 13-01; existing 6 steps + Halt Protocol + Quick Reference preserved"
      min_lines: 300
      max_lines: 330
      contains: "Step 7"
    - path: ".claude/skills/agentbloc/references/phase-5-deployment.md"
      provides: "After edit: Step 7.5 Runtime Wiring hand-off section inserted between existing Step 7 and Step 8; cites runtime-engine + 3 Plan 13-01 references; existing Priority 1 + Steps 1-7 + 8 + 9 preserved"
      min_lines: 1370
      max_lines: 1395
      contains: "Runtime Wiring"
    - path: ".claude/skills/agentbloc/references/incident-response.md"
      provides: "After edit: new H2 'Runtime Kill-Switch Semantics' appended; documents D-77 three-point enforcement (wake + tool + team-transition); cites correlation-ID scoped dissolution"
      min_lines: 225
      max_lines: 250
      contains: "Runtime Kill-Switch Semantics"
    - path: ".claude/skills/agentbloc/SKILL.md"
      provides: "After edit: Phase 5 See-line block appended with 3 refs per D-58; State Transitions runtime_wired sub-gate; Phase 6 precondition extended to verify registry.yaml runtime.cron_registered_at"
      min_lines: 202
      max_lines: 225
      contains: "runtime_wired"
  key_links:
    - from: ".claude/skills/agentbloc/references/deploy-protocol.md"
      to: ".claude/agents/runtime-engine.md"
      provides: "Step 7 Runtime Wiring invokes runtime-engine subagent as the terminal step of the 8-step deploy protocol"
    - from: ".claude/skills/agentbloc/references/phase-5-deployment.md"
      to: ".claude/skills/agentbloc/references/deploy-protocol.md"
      provides: "Step 7.5 hand-off section cross-references deploy-protocol.md Step 7 for canonical flow"
    - from: ".claude/skills/agentbloc/references/incident-response.md"
      to: ".claude/skills/agentbloc/references/runtime-coordination.md"
      provides: "Runtime Kill-Switch Semantics section cites runtime-coordination.md team-transition semantics"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/n8n-integration.md"
      provides: "Phase 5 See-line for unconditional load"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/runtime-coordination.md"
      provides: "Phase 5 See-line for unconditional load"
    - from: ".claude/skills/agentbloc/SKILL.md"
      to: ".claude/skills/agentbloc/references/correlation-id.md"
      provides: "Phase 5 See-line for unconditional load"
---

<objective>
Complete Phase 13 by surgically wiring the Plan 13-01 references + Plan 13-02 subagent into the existing skill surface. Four targeted edits: deploy-protocol.md gains Step 7 (Runtime Wiring); phase-5-deployment.md gains Step 7.5 (hand-off between deploy-engine and runtime-engine); incident-response.md gains a Runtime Kill-Switch Semantics section; SKILL.md gains 3 See-lines + runtime_wired sub-gate + Phase 6 precondition extension.

Purpose: Close the Phase 13 structural loop. After this plan lands, a user-facing Phase 5 entry loads the 3 new references unconditionally (n8n-integration + runtime-coordination + correlation-id); deploy-engine's canonical 8-step flow gains the runtime-wiring Step 7; the Phase 5 -> Phase 6 transition gates behind BOTH `deployment_artifacts_emitted` (Phase 12) AND `runtime_wired` (Phase 13); the kill-switch runtime semantics are formally documented.

Output: 4 existing files surgically edited with ~90 added lines total. Committed to git. Phase 13 structurally complete after Wave 3 commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-multi-agent-runtime/13-CONTEXT.md
@.planning/phases/13-multi-agent-runtime/13-01-PLAN.md
@.planning/phases/13-multi-agent-runtime/13-02-PLAN.md
@.claude/skills/agentbloc/references/deploy-protocol.md
@.claude/skills/agentbloc/references/phase-5-deployment.md
@.claude/skills/agentbloc/references/incident-response.md
@.claude/skills/agentbloc/SKILL.md
@.claude/skills/agentbloc/references/n8n-integration.md
@.claude/skills/agentbloc/references/runtime-coordination.md
@.claude/skills/agentbloc/references/correlation-id.md
@.claude/agents/runtime-engine.md

<interfaces>
<!-- Insertion-point anchors + preservation contracts. -->

deploy-protocol.md current structure (Phase 12 output, 290 lines):
- H1: Deploy Pipeline Protocol
- Blockquote
- TOC (13 sections)
- Step 1: Input Validation
- Step 2: Fingerprint Compare
- Step 3: Diff Presentation
- Step 4: Template Render
- Step 5: Atomic Write
- Step 6: Registry Update (CORRECT position per existing file; the H1-level numbering in the current file may differ from CONTEXT narrative which said Step 6 is verification; Plan 13-03 executor MUST read the current file and insert Step 7 Runtime Wiring AFTER the post-deploy verification step, WHICHEVER number it currently carries, but the new header MUST be "Step 7: Runtime Wiring" per CONTEXT D-83)
- Step 7: .mcp.json Merge (OR renumbered if the current file's verification step is Step 8)
- Step 8: Post-Deploy Verification
- Halt Protocol
- Quick Reference

INSERTION DISCIPLINE for deploy-protocol.md: The executor MUST read the current file first to determine the exact existing step numbering. Insert the NEW Step 7 (Runtime Wiring) AFTER the post-deploy-verification step and BEFORE the Halt Protocol section. If the current file numbers verification as Step 8 (per Plan 12-01), then the new Runtime Wiring becomes Step 9 (or the existing Step 8 renumbers to Step 9 and the new one slots in as Step 8; executor picks the less invasive path). In either case, the new section title begins with "Step 7: Runtime Wiring" per CONTEXT D-83 narrative , the EXECUTOR handles renumbering if needed, but the planner notes here that the new conceptual "Step 7" may become Step 9 in actual file if renumbering is avoided. SIMPLEST PATH: insert directly after the current terminal step (verification) before the Halt Protocol, titling the NEW section `## Step 7: Runtime Wiring` (appending to the step sequence, not inserting in the middle); this avoids renumbering and keeps existing Step 1-8 byte-for-byte preserved. If the current file's last numbered step is Step 8, the new Step 7 becomes Step 9 in actual numbering but the H2 title stays `## Step 9: Runtime Wiring`. CONTEXT narrative D-83 "Step 7 (Runtime Wiring)" is conceptual, not a file-position constraint.

phase-5-deployment.md current structure (1358 lines, v1.0 + Phase 12 edits):
- v1.0 content (Priorities 2..N, Summary block) preserved
- Phase 12 Plan 12-03 added Priority 1 ClaudeClaw-Native Deploy section
- Existing Step 7 job-definition template (line 728-ish)
- Step 8 SUMMARY.md section (line 840-ish)
- Step 9 Deployment Guide section

INSERTION DISCIPLINE for phase-5-deployment.md: Insert new Step 7.5 Runtime Wiring section BETWEEN the existing Step 7 (Job Definition Template) and Step 8 (SUMMARY.md). "Step 7.5" is a half-step to avoid renumbering all subsequent steps. Same half-step numbering pattern used by prior phases. Target line delta: +20 lines maximum.

incident-response.md current structure (212 lines):
- v1.0 dual-path kill-switch (file-based + Telegram /stop)
- PreToolUse hook template
- Existing sections through line ~212

APPEND DISCIPLINE for incident-response.md: Append new H2 section `## Runtime Kill-Switch Semantics` at the END of the file (after existing final section). New section cites D-77 three-point enforcement (wake-time check + per-tool check + team-transition check); references runtime-coordination.md for TeamCreate + team-dissolution semantics; explicitly calls out the /stop remote path emitted by runtime-engine at `.agentbloc/runtime/n8n-routes/agentbloc-stop.json`. Target added lines: ~25.

SKILL.md current structure (182 lines post-Phase 12):
- Phase 1-6 entries with See-line blocks
- Hard Gates section
- State Transitions section with phase-specific sub-gates (including Phase 12 `deployment_artifacts_emitted`)
- Quality Checklist
- Reference Implementation

EDIT DISCIPLINE for SKILL.md: 3 targeted Edit calls:
1. Phase 5 entry See-line block: APPEND 3 new See-lines (n8n-integration.md + runtime-coordination.md + correlation-id.md) after existing Phase 5 See-lines. Per D-58, these 3 are the ONLY new Phase-5-entry loads; wake-job templates + runtime-engine.md are subagent-only.
2. State Transitions section: APPEND new `runtime_wired` sub-gate bullet ANDed with existing `deployment_artifacts_emitted` per D-81. Phase 5 -> Phase 6 transition gates on BOTH.
3. Phase 6 precondition: EXTEND existing Phase 6 precondition paragraph with 1 additional sentence verifying `registry.yaml runtime.cron_registered_at` is non-null OR `runtime.webhook_endpoints` is non-empty (proves Phase 13 completed).

Target added lines across SKILL.md: ~20 (within D-29 cap 250 with headroom for Phase 14 + 15).

D-58 GREP-FOR-ABSENCE discipline (enforced on SKILL.md after edit):
- `grep -q "wake-job-cron\.md\.tmpl" SKILL.md` returns NO match
- `grep -q "wake-job-webhook\.md\.tmpl" SKILL.md` returns NO match
- `grep -q "wake-job-inter\.md\.tmpl" SKILL.md` returns NO match
- `grep -q "runtime-engine\.md" SKILL.md` returns NO match (templates + subagent are subagent-only per D-58)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgical edit `.claude/skills/agentbloc/references/deploy-protocol.md` , insert new Step 7 (Runtime Wiring) after the post-deploy verification step and before the Halt Protocol section; cite runtime-engine subagent + 3 Plan 13-01 references; preserve all existing steps + Halt Protocol + Quick Reference byte-for-byte</name>
  <files>.claude/skills/agentbloc/references/deploy-protocol.md</files>
  <read_first>
    - .claude/skills/agentbloc/references/deploy-protocol.md (FULL current file; READ COMPLETELY before editing to determine exact numbering of existing steps + insertion point; Plan 12-01 emitted this file with the 8-step protocol + Halt Protocol + Quick Reference)
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md D-83 (Step 7 Runtime Wiring insertion between verification and reporting) + D-80 (runtime-engine scope) + D-81 (runtime_wired sub-gate)
    - .claude/skills/agentbloc/references/n8n-integration.md (produced Plan 13-01; target of new See-line)
    - .claude/skills/agentbloc/references/runtime-coordination.md (produced Plan 13-01; target of new See-line)
    - .claude/skills/agentbloc/references/correlation-id.md (produced Plan 13-01; target of new See-line)
    - .claude/agents/runtime-engine.md (produced Plan 13-02; cited in Step 7 body)
    - .planning/phases/12-deploy-pipeline-agent-memory/12-03-PLAN.md Task 1 (structural analog: Phase 10 to Phase 11 priority transition pattern)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - Existing deploy-protocol.md with Steps 1-8 + Halt Protocol + Quick Reference
    </consumes>
    <provides>
      - New Step 7 Runtime Wiring section inserted after post-deploy-verification, before Halt Protocol
      - Existing steps renumbered (only if necessary; prefer append pattern that makes the new section the highest-numbered step and preserves all prior numbering)
    </provides>
  </interfaces>
  <action>
**Step 1: Read deploy-protocol.md in full.** Note the exact current line count (~290 lines per Phase 12 Plan 12-01). Identify:
- Current last numbered step (probably Step 8: Post-Deploy Verification)
- Halt Protocol section location
- Quick Reference section location
- TOC entries (13 currently per Plan 12-01)

**Step 2: Insert new Runtime Wiring step AFTER the current last numbered step and BEFORE the Halt Protocol section.**

The insertion point is: between `## Step 8: Post-Deploy Verification` (or whichever is the current last step) and `## Halt Protocol`. The new section title is `## Step 9: Runtime Wiring` IF the current last step is Step 8 (append pattern avoids renumbering). If the current file somehow has a different structure, the executor picks the least-invasive insertion (append to step sequence, insert before Halt Protocol).

**New Step (insert verbatim, replacing `9` with the correct next number if file structure differs):**

```markdown
## Step 9: Runtime Wiring

**Action:** Invoke the runtime-engine subagent as the final step of the deploy protocol. runtime-engine materializes wake.md per (agent, trigger) tuple, emits n8n route .json stubs per webhook trigger, installs the crontab via stdin form (NEVER `crontab -e`), and extends `.agentbloc/agents/registry.yaml` with the D-78 runtime block.

**Input:** `.agentbloc/agents/registry.yaml` (Phase 12 deploy-engine output) + `.agentbloc/team/agent-profiles.yaml` (Phase 9 Designer output) + `.agentbloc/deploy/crontab.proposed` (Phase 12 deploy-engine output). All three are hard preconditions.

**Reference loads (runtime-engine forked context):**
- `references/n8n-integration.md` , D-74 envelope schema + 5 worked examples + .json route file format
- `references/runtime-coordination.md` , D-76 TeamCreate/SendMessage contract + writeStateHandoff fallback + topology-to-primitive mapping + crontab stdin install discipline
- `references/correlation-id.md` , D-75 format spec + 3 propagation channels + helpers.sh generator

**Subagent:** `.claude/agents/runtime-engine.md` per D-80 (narrow Bash allow-list: `crontab -` (stdin install), `crontab -l`, `shasum -a 256`, `claude agents list`, `claude mcp list`; `crontab -e` EXPLICITLY DISALLOWED).

**Dispatch (per D-73 Option D):**
- agent-profile trigger.type = cron -> `.claude/skills/agentbloc/templates/wake-job-cron.md.tmpl`
- agent-profile trigger.type = event (webhook) -> `.claude/skills/agentbloc/templates/wake-job-webhook.md.tmpl` (one per (agent, source, event-slug) tuple)
- agent-profile trigger.type = inter-agent -> `.claude/skills/agentbloc/templates/wake-job-inter.md.tmpl` (one per agent; internally dispatches by message.type)

**If success:** runtime-engine emits `.agentbloc/runtime/RUNTIME-REPORT.md` + extends registry.yaml with the `runtime` block + closes the `runtime_wired` sub-gate per D-81. Phase 5 -> Phase 6 transition unblocks.

**If halt:** runtime-engine emits `.agentbloc/runtime/RUNTIME-FAILED-REPORT.md` per D-35/D-71 halt-and-name discipline with halt_reason enum value (`missing-input` | `template-load-failure` | `yaml-parse-error` | `crontab-install-failure` | `registry-edit-failure` | `user-rejected-crontab-diff` | `permission-denied`). The runtime_wired sub-gate remains false; Phase 6 Evolution entry halts until the user resolves and re-runs.

**Arco Rooms example:** For the 3-agent Arco Rooms team (Gestor Cobros + Recepcionista + Gestor Documental), runtime-engine materializes 6 wake.md files (2 per agent), emits 3 n8n route .json stubs (plaid + telegram + gmail) plus agentbloc-stop.json for remote kill-switch, installs 2 crontab entries (monthly + weekly), and extends registry.yaml with the runtime block containing 5 workflows + 3 webhook_endpoints. See `examples/arco-rooms-correlation-flow.md` and `examples/arco-rooms-runtime-artifacts.md` for literal shapes.

**Rationale:** Phase 13 is a thin wiring layer on top of ClaudeClaw primitives. Step 7 (this step) is intentionally the terminal step: after deploy-engine emits artifacts (Steps 1-8), runtime-engine wires them to triggers. Splitting into two subagents preserves D-31 concern-separation and lets each fork stay within context budget.
```

**Step 3: Update TOC at top of file.** Add a new TOC bullet entry for the new step. If the TOC had 13 entries, it becomes 14. Insert the new bullet in the correct position (between current post-deploy-verification TOC bullet and Halt Protocol TOC bullet).

**Step 4: Preserve everything else byte-for-byte.** Steps 1-8 (or however many currently exist), Halt Protocol, Quick Reference.

**Step 5: Zero em-dashes check.** After edit, `grep -c "—" .claude/skills/agentbloc/references/deploy-protocol.md` should not increase from the pre-edit count (this file may have legacy em-dashes from Phase 12 that are not our concern per Phase 12 0ab97cd precedent). The NEW prose added in Step 7 inserts MUST contain zero em-dashes.

**Step 6: Line-count budget.** Net delta +15 to +30 lines (new step is ~25 lines including TOC update). Verify final `wc -l` is within [original, original+30].

**Edit discipline:** Use the Edit tool (2 Edit calls preferred: one for TOC update, one for Step insertion). Do NOT use Write to replace the whole file.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "Runtime Wiring" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "runtime-engine\.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "n8n-integration\.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "runtime-coordination\.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "correlation-id\.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "runtime_wired" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "RUNTIME-REPORT\.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "RUNTIME-FAILED-REPORT\.md" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "D-73" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "wake-job-cron\.md\.tmpl" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "wake-job-webhook\.md\.tmpl" .claude/skills/agentbloc/references/deploy-protocol.md && grep -q "wake-job-inter\.md\.tmpl" .claude/skills/agentbloc/references/deploy-protocol.md && test $(wc -l < .claude/skills/agentbloc/references/deploy-protocol.md) -le 330 && test $(wc -l < .claude/skills/agentbloc/references/deploy-protocol.md) -ge 300</automated>
  </verify>
  <acceptance_criteria>
    - File exists (was edited in place)
    - New Runtime Wiring step present (grep `Runtime Wiring`)
    - runtime-engine.md cited
    - All 3 Plan 13-01 references cited (n8n-integration.md, runtime-coordination.md, correlation-id.md)
    - All 3 wake-job templates cited (cron, webhook, inter)
    - runtime_wired sub-gate cited (links to D-81)
    - RUNTIME-REPORT.md + RUNTIME-FAILED-REPORT.md cited
    - Line count within [300, 330] (original 290 + ~25 new)
    - Existing Steps 1-8 preserved (grep for each existing H2 title returns 1)
    - Existing Halt Protocol section preserved
    - Existing Quick Reference section preserved
    - No em-dashes introduced in the new section
  </acceptance_criteria>
  <done>
deploy-protocol.md Step 7 Runtime Wiring inserted. deploy-engine now has a canonical delegation path to runtime-engine as the terminal step. RUNTIME-01 + RUNTIME-02 + RUNTIME-04 + RUNTIME-05 + RUNTIME-06 + RUNTIME-07 wired at deploy-protocol layer (references cite the subagent + Plan 13-01 refs + templates).
  </done>
</task>

<task type="auto">
  <name>Task 2: Surgical edit `.claude/skills/agentbloc/references/phase-5-deployment.md` , insert new Step 7.5 (Runtime Wiring hand-off) between existing Step 7 and Step 8; preserve Priority 1 ClaudeClaw-Native Deploy + Steps 1-7 + Step 8 + Step 9 byte-for-byte</name>
  <files>.claude/skills/agentbloc/references/phase-5-deployment.md</files>
  <read_first>
    - .claude/skills/agentbloc/references/phase-5-deployment.md (FULL current file; ~1358 lines; READ COMPLETELY for insertion-point identification; Phase 12 Plan 12-03 Task 1 added Priority 1 ClaudeClaw-Native Deploy section; v1.0 added Steps 7, 8, 9)
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md D-83 (Step 7.5 insertion between Step 7 and Step 8)
    - .planning/phases/12-deploy-pipeline-agent-memory/12-03-PLAN.md Task 1 (structural analog)
    - .claude/skills/agentbloc/references/deploy-protocol.md (post-Task-1; contains the new Runtime Wiring step Step 7.5 cross-references)
    - .claude/agents/runtime-engine.md (produced Plan 13-02)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - Existing phase-5-deployment.md with Priority 1 + Steps 1-7 + 8 + 9 preserved
    </consumes>
    <provides>
      - New Step 7.5 Runtime Wiring hand-off section inserted between existing Step 7 (Job Definition Template) and Step 8 (SUMMARY.md)
    </provides>
  </interfaces>
  <action>
**Step 1: Read phase-5-deployment.md in full.** Identify:
- Priority 1 ClaudeClaw-Native Deploy section (Phase 12 addition; line ~100-150-ish)
- Step 7 Job Definition Template section (line ~728-ish per CONTEXT)
- Step 8 SUMMARY.md section (line ~840-ish per CONTEXT)
- Step 9 Deployment Guide section

Find the EXACT closing boundary of Step 7 (either end-of-Step-7 content or a blank line before `## Step 8`). This is the insertion point.

**Step 2: Insert new Step 7.5 section BETWEEN Step 7 closing and Step 8 opening.**

**New Step 7.5 section (insert verbatim):**

```markdown
### Step 7.5: Runtime Wiring Hand-off (Phase 13)

After deploy-engine emits DEPLOY-REPORT.md with `verification_status: PASSED` or `PARTIAL`, control hands to runtime-engine (Phase 13 subagent at `.claude/agents/runtime-engine.md`). runtime-engine materializes the wake.md templates per (agent, trigger) tuple, installs the crontab via the stdin form (NEVER `crontab -e`), emits n8n route .json stubs for webhook triggers, and extends `.agentbloc/agents/registry.yaml` with the D-78 runtime block.

The hand-off is enforced by the Phase 5 `runtime_wired` sub-gate per D-81: Phase 5 -> Phase 6 transition requires BOTH `deployment_artifacts_emitted` (Phase 12) AND `runtime_wired` (Phase 13). If runtime-engine halts with RUNTIME-FAILED-REPORT.md, the `runtime_wired` sub-gate remains false and Phase 6 Evolution entry halts until the user resolves.

See:
- [references/deploy-protocol.md](deploy-protocol.md) for the canonical 8-step flow; the new Step (Runtime Wiring) is the terminal step delegating to runtime-engine.
- [references/n8n-integration.md](n8n-integration.md) for the D-74 4-field webhook envelope + 5 worked examples + .json route file format.
- [references/runtime-coordination.md](runtime-coordination.md) for the D-76 TeamCreate/SendMessage contract + writeStateHandoff fallback + single-agent bypass per RUNTIME-05 + crontab stdin install discipline.
- [references/correlation-id.md](correlation-id.md) for the D-75 format spec + 3 propagation channels (env var + JSON payload + SendMessage metadata) + grep recipes for mobile-first debugging.
- [.claude/agents/runtime-engine.md](../../agents/runtime-engine.md) for the subagent definition orchestrating the runtime wiring step.

**Closes requirements:** RUNTIME-01 (cron registration), RUNTIME-02 (n8n route emission), RUNTIME-03 (webhook-to-agent mapping), RUNTIME-04 (TeamCreate/SendMessage), RUNTIME-05 (single-agent bypass), RUNTIME-06 (correlation-ID propagation), RUNTIME-07 (kill-switch three-point enforcement).
```

**Step 3: Preserve everything else byte-for-byte.**
- Priority 1 ClaudeClaw-Native Deploy section (Phase 12 artifact)
- Steps 1-7 (v1.0 + Phase 12 content)
- Step 8 SUMMARY.md (immediately after the new Step 7.5)
- Step 9 Deployment Guide

**Step 4: Zero em-dashes in the new section.** After edit, the new Step 7.5 section must contain zero em-dashes. Legacy em-dashes elsewhere in the file are NOT our concern per Phase 12 0ab97cd precedent.

**Step 5: Line-count budget.** Net delta +15 to +25 lines. Verify final `wc -l` within [1358, 1395].

**Edit discipline:** Single Edit tool call with sufficient context (3-5 lines before + after insertion point) to make the edit unambiguous.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "Step 7.5" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "Runtime Wiring" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "runtime-engine\.md" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "n8n-integration\.md" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "runtime-coordination\.md" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "correlation-id\.md" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "runtime_wired" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "deployment_artifacts_emitted" .claude/skills/agentbloc/references/phase-5-deployment.md && grep -q "ClaudeClaw-Native Deploy" .claude/skills/agentbloc/references/phase-5-deployment.md && test $(wc -l < .claude/skills/agentbloc/references/phase-5-deployment.md) -ge 1370 && test $(wc -l < .claude/skills/agentbloc/references/phase-5-deployment.md) -le 1395</automated>
  </verify>
  <acceptance_criteria>
    - File edited in place
    - New `### Step 7.5: Runtime Wiring Hand-off` section present
    - All 3 Plan 13-01 references cited + runtime-engine.md cited
    - runtime_wired + deployment_artifacts_emitted both cited (shows D-81 AND relationship)
    - Priority 1 ClaudeClaw-Native Deploy preserved (grep for existing "ClaudeClaw-Native Deploy" still returns hits)
    - Line count [1370, 1395]
    - RUNTIME-01..07 all cited in "Closes requirements" sentence
  </acceptance_criteria>
  <done>
phase-5-deployment.md Step 7.5 Runtime Wiring Hand-off inserted. User-facing Phase 5 unconditional load now documents the deploy-engine -> runtime-engine chain. RUNTIME-01..07 referenced at user-facing layer.
  </done>
</task>

<task type="auto">
  <name>Task 3: Surgical edit `.claude/skills/agentbloc/references/incident-response.md` , append new H2 'Runtime Kill-Switch Semantics' section at end of file documenting D-77 three-point enforcement; preserve existing dual-path kill-switch prose byte-for-byte</name>
  <files>.claude/skills/agentbloc/references/incident-response.md</files>
  <read_first>
    - .claude/skills/agentbloc/references/incident-response.md (FULL current file; ~212 lines; v1.0 kill-switch dual-path + PreToolUse hook)
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md D-77 (kill-switch three-point enforcement: wake + tool + team-transition)
    - .claude/skills/agentbloc/references/runtime-coordination.md (produced Plan 13-01; cross-reference target for TeamCreate dissolution semantics)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - Existing incident-response.md with v1.0 dual-path kill-switch + PreToolUse hook template
    </consumes>
    <provides>
      - New H2 Runtime Kill-Switch Semantics section appended at end
      - 3-point enforcement documented (wake / tool / team-transition)
      - Cross-reference to runtime-coordination.md for team-dissolution
      - Reference to agentbloc-stop.json n8n route stub (emitted by runtime-engine per D-77)
    </provides>
  </interfaces>
  <action>
**Step 1: Read incident-response.md in full.** Note the line count (~212) and identify the file's terminal section. Confirm the file ends cleanly (not mid-section).

**Step 2: Append new H2 section at END of file.**

**New section (insert verbatim at end of file after existing terminal section; add one blank line separator before the new H2):**

```markdown

## Runtime Kill-Switch Semantics

Phase 13 formalizes kill-switch behavior inside the runtime layer. Per D-77, the kill-switch is checked at 3 points per agent wake; coverage across these three enforcement points spans the full wake-to-completion latency window.

1. **Wake-time check (new in Phase 13 runtime layer):** Every materialized wake.md (produced by runtime-engine from `templates/wake-job-{cron,webhook,inter}.md.tmpl`) opens with Section 1 checking `.agentbloc/KILL_SWITCH`. If the file exists, the agent appends a `halted-kill-switch` entry to `.agentbloc/logs/audit.jsonl` with the correlation ID and EXITS IMMEDIATELY before reading state, loading the SKILL.md, or calling any tool. This prevents new damage from a wake that fires during an active halt window.

2. **Per-tool check (existing v1.0 SECR-05 + Phase 12 PreToolUse hook):** The hook at `.claude/hooks/kill-switch-check.sh` (generated by Phase 12 deploy-engine; documented earlier in this file) runs before every tool invocation. On KILL_SWITCH activation mid-run, the next tool call is blocked with `permissionDecision: deny`. This prevents in-flight damage from a long-running agent that passed the wake-time check but then encountered activation before completing.

3. **Team-transition check (new in Phase 13 runtime coordination layer):** Inside a TeamCreate session, every agent checks `.agentbloc/KILL_SWITCH` before SendMessage send AND before SendMessage consume. If active, the agent returns `{status: halted-kill-switch}` to the caller and the team lead dissolves the team via explicit TeamCreate teardown. Dissolution is logged to `.agentbloc/runtime/TEAM_SESSIONS.jsonl` with `team_dissolution_reason: kill-switch` alongside the shared correlation ID. Worst-case latency for a team-wide halt is one SendMessage round-trip (typically under 5 seconds). See `references/runtime-coordination.md` for the full TeamCreate + dissolution semantics.

**Remote-trigger path (Telegram /stop):** Phase 13 runtime-engine emits an n8n route stub at `.agentbloc/runtime/n8n-routes/agentbloc-stop.json` that listens for `/stop` in the configured Telegram operations thread and runs `touch .agentbloc/KILL_SWITCH` via a shell node. A sibling route `agentbloc-resume.json` runs `rm .agentbloc/KILL_SWITCH` on `/resume`. Both are user-installable into the user's n8n instance. The v1.0 dual-path (file-based + Telegram /stop) remains operational; Phase 13 ships the route YAML stubs so the user does not hand-author them.

**Correlation-ID-scoped forensics:** Every halt event logs the correlation ID. A single grep over `.agentbloc/logs/audit.jsonl` and `.agentbloc/runtime/TEAM_SESSIONS.jsonl` reconstructs the full chain from the triggering user event through every halted agent. See `references/correlation-id.md` for grep recipes.
```

**Step 3: Preserve everything else byte-for-byte.** Existing v1.0 dual-path kill-switch prose + PreToolUse hook template + all prior sections.

**Step 4: Zero em-dashes in the new section.** New section must contain zero em-dashes. Legacy em-dashes elsewhere in the file are NOT our concern per Phase 12 0ab97cd precedent.

**Step 5: Line-count budget.** Net delta +25 to +35 lines. Verify final `wc -l` within [225, 250].

**Edit discipline:** Single Edit tool call (append to end of file); old_string is the last few lines of the current file, new_string is the same last few lines + blank separator + new section.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/references/incident-response.md && grep -q "^## Runtime Kill-Switch Semantics" .claude/skills/agentbloc/references/incident-response.md && grep -q "three-point enforcement\|three.point\|three points\|3.point" .claude/skills/agentbloc/references/incident-response.md && grep -q "wake-time check" .claude/skills/agentbloc/references/incident-response.md && grep -q "Per-tool check" .claude/skills/agentbloc/references/incident-response.md && grep -q "Team-transition check" .claude/skills/agentbloc/references/incident-response.md && grep -q "agentbloc-stop\.json" .claude/skills/agentbloc/references/incident-response.md && grep -q "agentbloc-resume\.json" .claude/skills/agentbloc/references/incident-response.md && grep -q "team_dissolution_reason" .claude/skills/agentbloc/references/incident-response.md && grep -q "runtime-coordination\.md" .claude/skills/agentbloc/references/incident-response.md && grep -q "TEAM_SESSIONS\.jsonl" .claude/skills/agentbloc/references/incident-response.md && test $(wc -l < .claude/skills/agentbloc/references/incident-response.md) -ge 225 && test $(wc -l < .claude/skills/agentbloc/references/incident-response.md) -le 250</automated>
  </verify>
  <acceptance_criteria>
    - File edited in place
    - New `## Runtime Kill-Switch Semantics` H2 section appended at end
    - Three numbered enforcement points present (1. Wake-time, 2. Per-tool, 3. Team-transition)
    - agentbloc-stop.json + agentbloc-resume.json both cited
    - runtime-coordination.md + correlation-id.md cross-references present
    - Existing v1.0 dual-path prose preserved (legacy file-based + Telegram /stop content still present)
    - Line count [225, 250]
    - Zero em-dashes in new section
  </acceptance_criteria>
  <done>
incident-response.md Runtime Kill-Switch Semantics section appended. D-77 three-point enforcement formally documented. RUNTIME-07 fully wired at documentation layer. v1.0 SECR-05 remains as the cross-cutting kill-switch contract; Phase 13 is the enforcement layer.
  </done>
</task>

<task type="auto">
  <name>Task 4: Surgical edit `.claude/skills/agentbloc/SKILL.md` , append 3 See-lines to Phase 5 entry (n8n-integration + runtime-coordination + correlation-id) per D-58 + add runtime_wired sub-gate to State Transitions per D-81 + extend Phase 6 precondition to verify registry.yaml runtime.cron_registered_at non-null OR runtime.webhook_endpoints non-empty</name>
  <files>.claude/skills/agentbloc/SKILL.md</files>
  <read_first>
    - .claude/skills/agentbloc/SKILL.md (FULL current ~182-line file; READ COMPLETELY before editing; Phase 5 entry location + State Transitions + Phase 6 precondition vary)
    - .planning/phases/13-multi-agent-runtime/13-CONTEXT.md D-58 (context-budget discipline: only 3 new refs load at Phase 5 entry; wake-job templates + runtime-engine are subagent-only) + D-81 (runtime_wired sub-gate) + D-29 line budget +~20 targeting ~202
    - .planning/phases/12-deploy-pipeline-agent-memory/12-03-PLAN.md Task 2 (structural analog: Phase 12 deploy-protocol.md See-line + deployment_artifacts_emitted sub-gate pattern)
    - .claude/skills/agentbloc/references/n8n-integration.md (produced Plan 13-01; target of new See-line)
    - .claude/skills/agentbloc/references/runtime-coordination.md (produced Plan 13-01; target of new See-line)
    - .claude/skills/agentbloc/references/correlation-id.md (produced Plan 13-01; target of new See-line)
    - /Users/pablodelarco/agentbloc/CLAUDE.md
  </read_first>
  <interfaces>
    <consumes>
      - Existing SKILL.md Phase 5 entry See-line block (currently loads phase-5-deployment.md + deploy-protocol.md per Phase 12)
      - Existing State Transitions section (currently has Phase 12 deployment_artifacts_emitted sub-gate)
      - Existing Phase 6 precondition paragraph
    </consumes>
    <provides>
      - Phase 5 See-line block with 3 new See-lines appended (n8n-integration + runtime-coordination + correlation-id)
      - State Transitions runtime_wired sub-gate ANDed with deployment_artifacts_emitted per D-81
      - Phase 6 precondition extended with runtime.cron_registered_at OR runtime.webhook_endpoints verification
    </provides>
  </interfaces>
  <action>
**Step 1: Read SKILL.md in full.** Note the exact current line count (182 per Bash check) and locate:
- Phase 5 entry See-line block (includes phase-5-deployment.md + deploy-protocol.md from Phase 12 Plan 12-03 Task 2)
- State Transitions section (includes Phase 12 deployment_artifacts_emitted sub-gate)
- Phase 6 entry and its precondition paragraph

**Step 2: Extend Phase 5 See-line block (Edit #1 of 3).**

Connector sentence rewrite + append 3 See-lines:

OLD (example pattern; adapt to actual current text):
```
See [references/phase-5-deployment.md](references/phase-5-deployment.md)
See [references/deploy-protocol.md](references/deploy-protocol.md)
```

NEW:
```
See [references/phase-5-deployment.md](references/phase-5-deployment.md)
See [references/deploy-protocol.md](references/deploy-protocol.md)
See [references/n8n-integration.md](references/n8n-integration.md)
See [references/runtime-coordination.md](references/runtime-coordination.md)
See [references/correlation-id.md](references/correlation-id.md)
```

Per D-58 context-budget discipline, ONLY these 3 new references are added at Phase 5 entry. Do NOT add:
- wake-job-cron.md.tmpl (subagent-only)
- wake-job-webhook.md.tmpl (subagent-only)
- wake-job-inter.md.tmpl (subagent-only)
- runtime-engine.md (subagent-only)

The runtime-engine subagent loads the 3 references in its Mandatory Initial Read (Plan 13-02). Main session never loads the templates or the subagent definition at Phase 5 entry.

**Step 3: Add runtime_wired sub-gate to State Transitions (Edit #2 of 3).**

Find the State Transitions Phase-5-specific bullet added by Phase 12 Plan 12-03 Task 2 (the `deployment_artifacts_emitted` bullet). Extend or append a companion bullet for `runtime_wired`:

NEW bullet (append after deployment_artifacts_emitted bullet):
```
- Phase 5 specific: `runtime_wired` must be true before Phase 6 Evolution entry AND must be ANDed with `deployment_artifacts_emitted`. Set by runtime-engine subagent (Phase 13) after RUNTIME-REPORT.md is written (on success) or explicitly set to false if RUNTIME-FAILED-REPORT.md is emitted instead. Phase 5 -> Phase 6 transition requires BOTH sub-gates true. Per D-81, if RUNTIME-FAILED-REPORT.md is emitted, `runtime_wired` is false and Phase 6 entry halts with the failed report surfaced for user resolution.
```

**Step 4: Extend Phase 6 precondition (Edit #3 of 3).**

Find the Phase 6 precondition paragraph (typically 1-3 sentences). Append 1 sentence:

NEW sentence (append to Phase 6 precondition):
```
Additionally, verify `.agentbloc/agents/registry.yaml` has `runtime.cron_registered_at` set to a non-null ISO-8601 timestamp OR `runtime.webhook_endpoints` is a non-empty array (at least one trigger path wired per D-81); if neither, return Phase 5 with gate `pending` and re-run deploy-protocol Step 7 (Runtime Wiring) via the runtime-engine subagent.
```

**Step 5: Preservation checks.**
- Existing See-lines for Phase 5 (phase-5-deployment.md + deploy-protocol.md) preserved byte-for-byte
- Existing Phase 12 `deployment_artifacts_emitted` sub-gate bullet preserved
- Existing Phase 1-4 + Phase 6 entries untouched (except Phase 6 precondition sentence append)
- Hard Gates section untouched
- Quality Checklist untouched
- Reference Implementation section untouched

**Step 6: D-58 grep-for-absence.**

After editing, verify these subagent-only references are NOT present in SKILL.md:
- `grep -q "wake-job-cron\.md\.tmpl" SKILL.md` returns NO match
- `grep -q "wake-job-webhook\.md\.tmpl" SKILL.md` returns NO match
- `grep -q "wake-job-inter\.md\.tmpl" SKILL.md` returns NO match
- `grep -q "runtime-engine\.md" SKILL.md` returns NO match

If any of these returns a match, D-58 is violated; remove the offending reference and re-verify.

**Step 7: Zero em-dashes in new content.**

After edit, `grep -c "—" .claude/skills/agentbloc/SKILL.md` returns 0 (current SKILL.md has zero em-dashes per Phase 12 verification; new content must preserve zero-count).

**Step 8: Line-count budget.**

Final line count between 202 and 225 inclusive (current 182 + expected +~20 for 3 See-lines + 1 sub-gate bullet + 1 sentence; D-29 cap 250 with headroom).

**Edit discipline:** 3 Edit tool calls (one per edit point) for safety. Use unique old_string anchors.
  </action>
  <verify>
    <automated>test -f .claude/skills/agentbloc/SKILL.md && grep -q "n8n-integration\.md" .claude/skills/agentbloc/SKILL.md && grep -q "runtime-coordination\.md" .claude/skills/agentbloc/SKILL.md && grep -q "correlation-id\.md" .claude/skills/agentbloc/SKILL.md && grep -q "runtime_wired" .claude/skills/agentbloc/SKILL.md && grep -q "deployment_artifacts_emitted" .claude/skills/agentbloc/SKILL.md && grep -qE 'runtime\.(cron_registered_at|webhook_endpoints)' .claude/skills/agentbloc/SKILL.md && ! grep -q "wake-job-cron\.md\.tmpl" .claude/skills/agentbloc/SKILL.md && ! grep -q "wake-job-webhook\.md\.tmpl" .claude/skills/agentbloc/SKILL.md && ! grep -q "wake-job-inter\.md\.tmpl" .claude/skills/agentbloc/SKILL.md && ! grep -q "runtime-engine\.md" .claude/skills/agentbloc/SKILL.md && test $(grep -c "—" .claude/skills/agentbloc/SKILL.md) -eq 0 && test $(wc -l < .claude/skills/agentbloc/SKILL.md) -ge 202 && test $(wc -l < .claude/skills/agentbloc/SKILL.md) -le 225</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md edited in place
    - 3 new See-lines present in Phase 5 entry: n8n-integration.md, runtime-coordination.md, correlation-id.md
    - runtime_wired sub-gate present in State Transitions
    - deployment_artifacts_emitted (Phase 12) preserved alongside new runtime_wired
    - Phase 6 precondition extended with registry.yaml runtime.cron_registered_at OR runtime.webhook_endpoints check
    - D-58 context-budget discipline held: wake-job-cron.md.tmpl + wake-job-webhook.md.tmpl + wake-job-inter.md.tmpl + runtime-engine.md all ABSENT from SKILL.md
    - Line count 202..225 inclusive
    - Zero em-dashes (grep -c "—" returns 0)
    - Existing Phase 1-6 entries preserved (only Phase 5 See-line block + Phase 6 precondition extended)
    - Hard Gates, Quality Checklist, Reference Implementation sections preserved
  </acceptance_criteria>
  <done>
SKILL.md final Phase 13 wiring complete. Phase 5 entry loads 3 new Plan 13-01 references unconditionally. State Transitions gates Phase 6 behind BOTH sub-gates (deployment_artifacts_emitted AND runtime_wired) per D-81. Phase 6 Evolution entry verifies registry.yaml runtime block presence. D-58 context-budget held: 3 references + 3 templates + 1 subagent load ONLY in runtime-engine forked context. Phase 13 structurally closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main session -> Phase 5 unconditional load | 3 new references (n8n-integration + runtime-coordination + correlation-id) loaded unconditionally per D-58; user-facing reads |
| Phase 5 -> Phase 6 gate | deployment_artifacts_emitted AND runtime_wired sub-gates ANDed per D-81; Phase 6 entry blocked until both true |
| deploy-protocol.md Step 7 -> runtime-engine subagent | Prose boundary; subagent invocation is the named terminal step of the 8-step flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Elevation-of-Privilege | SKILL.md Phase 5 load-list bloat | mitigate | D-58 grep-for-absence verifies wake-job templates + runtime-engine are NOT loaded at Phase 5 entry (subagent-only); VALIDATION.md Task 13-03-01 enforces SKILL.md <= 225 lines |
| T-13-13 | Tampering | State Transitions sub-gate erosion | mitigate | VALIDATION.md Task 13-03-02 grep-verifies `runtime_wired` present; sub-gate bullet preserves byte-for-byte alongside Phase 12 deployment_artifacts_emitted per D-81 AND semantics |
| T-13-14 | Tampering | Phase 6 precondition bypass | mitigate | VALIDATION.md Task 13-03-03 grep-verifies precondition checks registry.yaml runtime.cron_registered_at OR runtime.webhook_endpoints; bypass attempts would require editing SKILL.md Phase 6 precondition, which is subject to Phase 13 verification re-scan |
| T-13-15 | Information-Disclosure | incident-response.md runtime section leaks kill-switch implementation | accept | Kill-switch implementation is public by design (v1.0 SECR-05); any attacker with project-read access already has the info; Phase 13 runtime section adds no new attack surface |
| T-13-16 | Tampering | deploy-protocol.md step insertion mid-file | mitigate | Task 1 insertion is AFTER the current last numbered step (append pattern); existing Steps 1-8 + Halt Protocol + Quick Reference preserved byte-for-byte |
| T-13-17 | Style-regression | em-dash re-introduction | mitigate | Every emitted section in the 4 edits passes `grep -c "—" = 0` on new prose; legacy em-dashes NOT touched per Phase 12 0ab97cd precedent |
</threat_model>

<verification>
**Post-edit state (per task):**
- [ ] deploy-protocol.md has new Step (Runtime Wiring) inserted; line count 300..330
- [ ] phase-5-deployment.md has new Step 7.5 section inserted between Step 7 and Step 8; line count 1370..1395
- [ ] incident-response.md has new Runtime Kill-Switch Semantics H2 section appended; line count 225..250
- [ ] SKILL.md has 3 new See-lines + runtime_wired sub-gate + Phase 6 precondition sentence extension; line count 202..225

**Preservation (per task):**
- [ ] deploy-protocol.md Steps 1-8 + Halt Protocol + Quick Reference preserved byte-for-byte
- [ ] phase-5-deployment.md Priority 1 ClaudeClaw-Native Deploy + Steps 1-7 + 8 + 9 preserved
- [ ] incident-response.md v1.0 dual-path kill-switch + PreToolUse hook template preserved
- [ ] SKILL.md Phase 1-4 + Phase 6 entries + Hard Gates + Quality Checklist + Reference Implementation preserved (except Phase 6 precondition sentence extension)

**D-58 context-budget discipline (SKILL.md only):**
- [ ] `wake-job-cron.md.tmpl` NOT in SKILL.md
- [ ] `wake-job-webhook.md.tmpl` NOT in SKILL.md
- [ ] `wake-job-inter.md.tmpl` NOT in SKILL.md
- [ ] `runtime-engine.md` NOT in SKILL.md

**Cross-reference integrity:**
- [ ] deploy-protocol.md Step 7 cites runtime-engine.md + 3 Plan 13-01 refs + 3 templates
- [ ] phase-5-deployment.md Step 7.5 cites runtime-engine.md + 3 Plan 13-01 refs + deploy-protocol.md
- [ ] incident-response.md Runtime Kill-Switch Semantics cites runtime-coordination.md + correlation-id.md
- [ ] SKILL.md Phase 5 See-lines cite all 3 Plan 13-01 refs

**Requirement traceability:**
- [ ] RUNTIME-01..07 visible in the Phase 5 entry down-chain (SKILL.md See-lines -> 3 refs -> runtime-engine.md -> 6 RUNTIME reqs)
- [ ] RUNTIME-03 specifically cited in phase-5-deployment.md Step 7.5 "Closes requirements"

**Style discipline:**
- [ ] Zero em-dashes in any NEW content across all 4 edits
- [ ] No AI attribution / no Co-Authored-By anywhere
- [ ] Line counts within budget for each file
</verification>

<success_criteria>
1. deploy-protocol.md: New Runtime Wiring step inserted after existing last numbered step, before Halt Protocol; cites runtime-engine.md + 3 Plan 13-01 references + 3 templates + RUNTIME-REPORT.md + runtime_wired sub-gate. Existing Steps 1-8 + Halt Protocol + Quick Reference preserved.
2. phase-5-deployment.md: New Step 7.5 section inserted between Step 7 (Job Definition Template) and Step 8 (SUMMARY.md); cites runtime-engine + 3 Plan 13-01 refs + deploy-protocol.md Step 7; cites both D-81 sub-gates; RUNTIME-01..07 "Closes requirements" sentence.
3. incident-response.md: New Runtime Kill-Switch Semantics H2 section appended at end; 3-point enforcement (wake + tool + team-transition); agentbloc-stop.json + agentbloc-resume.json n8n route stubs cited; cross-references to runtime-coordination.md + correlation-id.md.
4. SKILL.md: 3 new See-lines at Phase 5 (n8n-integration + runtime-coordination + correlation-id); runtime_wired sub-gate in State Transitions ANDed with deployment_artifacts_emitted per D-81; Phase 6 precondition extended with registry.yaml runtime.cron_registered_at OR runtime.webhook_endpoints check. D-58 grep-for-absence passes for wake-job templates + runtime-engine.md. Line count 202..225 (target ~202).
5. Zero em-dashes across ALL NEW content in ALL 4 edits; legacy em-dashes untouched.
6. 4 atomic task commits on master; 1 SUMMARY commit at end.
7. SUMMARY.md at `.planning/phases/13-multi-agent-runtime/13-03-SUMMARY.md` lists the 4 surgical edits + preservation checks + decisions applied (D-58 context-budget, D-77 three-point enforcement wired, D-81 runtime_wired sub-gate, D-83 Step 7 insertion, D-73 template dispatch referenced).
8. Phase 13 structurally complete: ready for `/gsd-verify-phase 13` to confirm all 7 RUNTIME requirements closed end-to-end through SKILL.md -> 3 refs -> runtime-engine -> templates -> fixtures chain.
</success_criteria>

<output>
After completion, create `.planning/phases/13-multi-agent-runtime/13-03-SUMMARY.md` documenting:
- 4 surgical edits + exact line counts
- Preservation checks passed
- D-58 + D-77 + D-81 + D-83 decisions applied
- All 7 RUNTIME-0X requirements closed (RUNTIME-01 cron + RUNTIME-02 n8n + RUNTIME-03 webhook-to-agent mapping + RUNTIME-04 TeamCreate + RUNTIME-05 single-agent bypass + RUNTIME-06 correlation ID + RUNTIME-07 kill-switch)
- Phase 13 structural completion signal (ready for `/gsd-verify-phase 13`)
</output>
</content>
